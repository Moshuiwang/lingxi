# Changelog

本文件记录 Lingxi 对用户可见与外部边界有影响的变化，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 约定；版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)。版本规则、发布 tag 与镜像 tag 的关系见 [deploy/README.md「版本号规则」](deploy/README.md#版本号规则)。

条目按发布批次维护，不逐 PR 记录；每条引用对应 Issue 便于追溯。当前能力的完整事实、证据层级与已知边界以 [docs/当前能力.md](docs/当前能力.md) 为准，本文件不重复其细节。

## [2.5.2] - 2026-09-18

补丁版（Trace [#843](https://github.com/Moshuiwang/lingxi/issues/843)）：候选 `v2.5.2-rc.107` 经预发验收后由 Release Promotion 发布为正式版，生产由拉取代理 + 部署器无人值守升级（停机数十秒），产品负责人一次真实问数通过（L5）；装入 [#784](https://github.com/Moshuiwang/lingxi/issues/784) / [#834](https://github.com/Moshuiwang/lingxi/issues/834) / [#836](https://github.com/Moshuiwang/lingxi/issues/836) / [#837](https://github.com/Moshuiwang/lingxi/issues/837) 四处改动。

### Changed

- **助手在飞书里的自称与授权页标题统一为「BI Plus」**：系统提示词首行改为「你是 BI Plus，面向公司内部员工的只读数据问答助手。」并写入自称规则（自称与对外名称只用 BI Plus，不使用任何内部代号）；提示词文件与公开配置登记随同一预发 / 生产窗口由脚本更新、回读核对。仓库侧只改 Bot-Test 授权页的标题字符串一处，该 Cloudflare Worker 本次不重新部署、Cloudflare 侧仍显示旧标题直到下次受控验收部署（Issue [#784](https://github.com/Moshuiwang/lingxi/issues/784)）。
- **三条清理路径每轮最多处理 500 行，积压由下一轮继续**：空闲会话正文清理（两小时空闲到点清除已送达的安全结果正文）从「一轮清完全部积压、逐会话无上限」改为一条带行级上限的语句，一个会话攒下数万条待清正文时每轮仍然前进，不再整轮被 3 秒语句超时取消、正文再也回收不掉；一处行锁冲突只作废本轮这一条语句，被锁行不被跳过、下一轮重来；日志与返回值只有行数。队列执行服务侧的二十四小时到期收敛与心跳超时回收候选集加同型上限。用户可见行为不变：两小时空闲清正文的时机与范围、九十天保留承诺、二十四小时到期语义都不动（Issue [#837](https://github.com/Moshuiwang/lingxi/issues/837)）。

### Fixed

- **门禁新增「数据库连接与游标不得逃出 `with` 块」判据**：对全仓既有调用点退出码 0、一处不改；裸 `connect()` 赋值后使用、`with` 内关闭后再建连、游标赋给实例属性、游标逃出 `with` 再执行四种违规样本各判红并由测试钉住；连接工厂模块说明改为与实现一致（目标合同句保留并注明尚未完全兑现）。零运行时改动（Issue [#834](https://github.com/Moshuiwang/lingxi/issues/834)）。
- **棘轮门禁三处过期表述勘误**：文件体量棘轮「基线登记的路径找不到时判红、须 `--refresh` 清除」写回正文、CI 浅克隆注释与实际工作流一致、纯逻辑基线头部改为如实登记「不再迁移」；门禁判定与全部基线数值零变化（Issue [#836](https://github.com/Moshuiwang/lingxi/issues/836)）。

## [2.5.1] - 2026-09-17

本版由**拉取代理 + 部署器**从 `release/2.5` 候选 rc.106 原样提升发到生产（Trace [#812](https://github.com/Moshuiwang/lingxi/issues/812)）：正式版 07:58Z 发布 → 生产代理自动拾取 → 三服务 08:01Z 重建（用户可见停机不到 1 分钟）→ 15 分钟观察 `verified`，迁移头 `0098` 不变、回退不降库，产品负责人一次真实问数通过。镜像里装下列五处修复与一处运维脚本修复；同批收口四张不进镜像的单：[#748](https://github.com/Moshuiwang/lingxi/issues/748) 裁决层剩余验收（同名伪造 PR 被拒、不改裁决核心的工作流 PR 免批合并、规则集应急恢复路径演练）、[#735](https://github.com/Moshuiwang/lingxi/issues/735)「申请使用」核实（= 飞书应用可用范围申请，不是业务授权来源）、[#742](https://github.com/Moshuiwang/lingxi/issues/742) 偶发失败复现量测（N = 24 未复现，测试侧修法随本版）、[#756](https://github.com/Moshuiwang/lingxi/issues/756) 峰值样本与恢复证据。生产 [#807](https://github.com/Moshuiwang/lingxi/issues/807) 受影响的 4 人：3 位返聘者按新规则解析为唯一在职并入组，旧正式表里属于旧身份的 3 行由管理员归档后开通完成、就绪核验通过；第 4 人（同一人员 ID 多行）按规则仍拒绝，花名册去重后再扩员。证据等级与已知边界以 [docs/当前能力.md](docs/当前能力.md) 为准。

### Fixed

- **只在准备阶段失败的部署计划不再占着主机，下一次发布自动接上**：此前部署器在 `prepare`（装控制包、拉镜像，不动任何服务）失败后，主机在途标记仍指向这份失败计划，拉取代理不接续失败计划而改建新计划，新计划每轮都以 `unfinished_host_deployment` 被拒、失败计划逐轮堆积，只能人工改 `host.active.json`；现在新计划在主机锁内先取证再收敛——标记确属该失败计划、其阶段账明确是 `failed`、只记过 `prepare` 的意图（阶段意图先于动作落盘，`stop` 及之后哪怕只是「即将进入」也不收敛）、旧执行者不在跑（持锁即证据）、有可信的上一次验证成功指针（新增 `host.verified.json`，没有时从阶段账重建；本机从未验证过则删除标记回到「从未由部署器部署过」）——五条件全有证据才把标记恢复为上一次验证成功的指针并照常部署，缺一条仍零动作。失败计划的状态与文件原地保留并留审计（`released_by` / `released_markers` 固定字段），不产生任何新的验证成功记录；`unknown` / 已进副作用阶段的失败仍走接续或独立批准的恢复，不自动清理（Issue [#805](https://github.com/Moshuiwang/lingxi/issues/805)、Trace [#812](https://github.com/Moshuiwang/lingxi/issues/812)）。
- **管理员在两次部署之间调整过公开配置后，下一次版本部署不再永久卡住**：此前只要 `public-config.json` 在两次部署之间改过（例如补齐运行文件清单），下一次发布就会在停任何服务之前以 `unplanned_service_artifact_or_config` 停住、每轮重复、失败计划逐轮堆积，唯一出路是把文件改回去——根因是部署器拿「当前配置」的指纹去核对「上一次部署起的旧容器」；现在部署器核对旧服务只用自己账里上一次已验证部署的配置指纹（主机在途标记指向的那份计划，指纹相符才用），新一侧仍用当前配置，配置改动随本次部署落到容器；两侧指纹在 `plan` 摘要（`configuration`）、阶段账与 `status`（`inventory`）里分开列出；上一次的可信旧值取不到时以 `previous_verified_configuration_unavailable` 零动作停住，不拿当前配置冒充、不读容器标签自证；篡改旧容器的配置标签、控制包标签或镜像摘要仍按原规则拒绝。修法随控制包到达，拉取代理不需重装；不提供同版本重应用入口——只改配置不发新版本，改动不会落到容器（Issue [#804](https://github.com/Moshuiwang/lingxi/issues/804)、Trace [#812](https://github.com/Moshuiwang/lingxi/issues/812)）。
- **同一提交在本机与 CI 上，两条部署测试给出一致的结论**：此前两条部署用例（控制包安装根、公开运行文件）把自己创建的文件与目录的权限位交给运行者 shell 的 umask 决定，`umask 002` 的开发机稳定判红、CI（`umask 022`）稳定判绿，同一提交两边结论相反，只能靠人工排查分辨「环境性」还是「改动引入」；现在测试自己显式建立它要核对的权限位——安全位是测试输入，组 / 其他可写位同样是测试输入——两种 umask 下结论一致，部署器与控制包的权限判据零放宽（Issue [#739](https://github.com/Moshuiwang/lingxi/issues/739)）。
- **按邮箱开通支持同邮箱候选中唯一在职的人员**：沿用实时在职回读；受限通道扩员准备遇到多候选时先登记为待执行阶段解析，由执行阶段核对。已有工号绑定保持原身份，管理员按邮箱查询和通知触达核对已有可信绑定；多在职、同人员 ID 多行、身份不一致或信息不足时明确拒绝或保留待校正，不把未知状态当离职，不自动重绑定旧账号、令牌、会话或本地授权；组织快照可用且新鲜时，候选人员 ID 不在快照视为明确非在职（旧账号已移出租户），同邮箱新账号可作唯一在职被选中，快照不可用或过期仍为未决。首次开通和预开通遇到无法确认历史归属的旧正式表记录时，会明确拒绝沿用旧令牌和旧权限，并保留追溯与管理员校正入口；已有正常账号不受影响，不自动改绑、覆盖旧令牌或批量撤权（Issue [#807](https://github.com/Moshuiwang/lingxi/issues/807)）。
- **跨公司欢迎示例只展示各公司共有的可用指标**：使用“各公司”等跨公司范围时，示例从各公司可用指标的交集选择；没有共有指标时不生成该示例段；公司特有指标仍可按原授权查询，欢迎示例不缩减查询权限（Issue [#745](https://github.com/Moshuiwang/lingxi/issues/745)）。
- **受限管理入口已开启的环境里，管理员按名单预开通不再在装配一步整批失败**：此前受控预开通脚本原样重建 scheduler 的启动装配，其中也包含受限管理入口的 socket 监听器；常驻 scheduler 已持有该 socket 及其文件锁，同容器内的第二个进程再建一个就以 `socket_in_use` 失败关闭，脚本报「开通编排装配失败，未执行任何一人」退出、零写入，运营审计账只留一行「未开始」；现在脚本以显式参数告知装配「不注册受限管理入口」——监听器、后台阶段消费者与租约保活三者都不建，socket 与文件锁仍由常驻 scheduler 独占，其余装配（发布闸、翻译映射、令牌供给、内测名单闸）一字不动，常驻 scheduler 的行为不变。内测名单闸对脚本同样生效：数据库模式下只对已在内测名单的人执行，名单外的人逐人失败关闭、零写入。脚本头部与命令行说明同步改成实际可跑的形态：脚本与名单都经标准输入送进容器（镜像不带脚本目录、容器根文件系统只读），不需要额外设置模块搜索路径（Issue [#826](https://github.com/Moshuiwang/lingxi/issues/826)、Trace [#812](https://github.com/Moshuiwang/lingxi/issues/812)）。

## [2.5.0] - 2026-09-15

本版是**第一次由拉取代理 + 部署器发到生产的正式版**（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)）：`release/2.5` 候选 rc.102 经预发验收后原样提升，生产由 `v2.4.3` 升级、停机约 30 秒、迁移头 `0096` → `0098`，产品负责人一次真实问数通过。用户与管理员得到：① 发布不再靠人在主机上手工操作，门禁的裁决层也不再由被审查的 PR 自己产出（Issue [#748](https://github.com/Moshuiwang/lingxi/issues/748)）；② 预开通、欢迎卡、内测扩员三件运营操作有了具名身份与持久审计账（Issue [#675](https://github.com/Moshuiwang/lingxi/issues/675) / [#788](https://github.com/Moshuiwang/lingxi/issues/788)），管理员可以用自己的 Agent 客户端经受限通道查状态并准备停用 / 恢复 / 补授 / 撤销、确认仍在本人飞书卡片上（Issue [#716](https://github.com/Moshuiwang/lingxi/issues/716)），职位授权重叠时只补缺的项、撤销只撤本笔（Issue [#715](https://github.com/Moshuiwang/lingxi/issues/715)），每次真实问数收口后留存问答语料并可受控读取（Issue [#664](https://github.com/Moshuiwang/lingxi/issues/664)）；③ 队列执行服务必配问数 MCP 地址（Issue [#604](https://github.com/Moshuiwang/lingxi/issues/604)）、预发监控小项（Issue [#756](https://github.com/Moshuiwang/lingxi/issues/756)）、两块纯函数平移（Issue [#658](https://github.com/Moshuiwang/lingxi/issues/658)，行为零变化）与一批真机接缝修法。生产内测名单自本版起改为库内动态名单，新人首聊只能经扩员确认卡准入。迁移 `0097` / `0098` 只加不删，回退不降库。证据等级与已知边界以 [docs/当前能力.md](docs/当前能力.md) 为准。

### Added

- **预发 / 生产升级在迁移前被打断时，操作者有可核对的续跑入口，不再需要手改状态文件**：部署器在迁移记录写下之后、迁移作业容器起来之前被打断，下一轮按设计以 `migration_unknown_no_retry` 停住（设计性停机）；此前唯一出路是人工备份并手改阶段账。现在部署器新增 `resume-migration` 子命令：沿用原 apply 批准，在主机锁内核对「迁移记录仍是 `running`、没有作业容器、迁移镜像回读的库头逐字等于计划记录」与操作者从 `status` 抄来的阶段账指纹（`status` 新增 `state_sha256`），任一不成立就零写、退出码 1 并给出各自的结果码；全部成立才把阶段账原文归档到 root 私有目录、只删这一条迁移记录并追加结构化审计，迁移仍由下一轮 apply 自己起、apply 的拒绝规则不变（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **预发可以把一个测试账号复位为「从未开通」以重走开通旅程，不影响生产与正式表**：新增预发专用运维脚本 `scripts/ops/stage_user_reset.py`（干跑 / 备份 / 确认删除 / 回退四步，确认摘要绑定将删行，备份走宿主 0600 文件），只动预发库该用户的行及派生行与预发权限发布表该行，不碰正式表、不吊销令牌；写入面指向正式表或环境自称生产时拒绝一切子命令；问答语料与内测采集这类留存内容默认不删、拦停待产品负责人裁定（Issue [#675](https://github.com/Moshuiwang/lingxi/issues/675)、Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)）。
- **管理员可以经受限通道用自然语言准备停用 / 恢复 / 补授 / 撤销，确认仍在本人飞书卡片上**：受限通道新增五个「准备」工具（停用、恢复、按职位 × 公司范围补授、按编号撤销、按用户 × 公司 × 指标撤销），把结构化参数逐字转译成私聊命令走同一条判定与审计，卡片由 gateway 直发发起人本人；同一意图重发沿用同一条待确认操作、不发第二张卡，职位或范围不可用时附候选清单；准备、确认 / 取消、执行三阶段都落进运营审计账（私聊发起的动作同样落账），审计写不进即不改状态。通道里没有任何工具能确认、取消或执行（Issue [#716](https://github.com/Moshuiwang/lingxi/issues/716)）。
- **管理员可以用自己的 Agent 客户端（Claude CLI / Codex）经受限 SSH 通道查用户状态、权限来源与待确认动作**：服务端对任意客户端版本都只回自己支持的 2025-11-25、接不接受由客户端决定，忽略通知帧、空闲连接不再五秒即断（默认 30 分钟），工具面从扩员三工具扩成六个（新增 `get_user_status` / `get_user_permission_sources` / `get_pending_actions`，只读、零写库、每次调用一行审计）；普通用户在四层上都不可达（系统层、UID 层、参数层、登记表层各有否定断言）。停用 / 恢复 / 补授 / 撤销的五个「准备」工具已提供，见上一条（Issue [#716](https://github.com/Moshuiwang/lingxi/issues/716)）。
- **生产升级后开始留存问答语料**：每一次真实问数收口后，用户问题原文、用户实收的回答正文、模型原文回答与工具调用详情写入新表 `qa_corpus`（迁移 `0098`），凭据形状照常过滤、不设保留上限、只追加；用户从当前环境删除时该用户语料随删（裁定前默认）。开关只由预发与生产的 compose 声明，本地与 CI 默认关闭；读取、检索与导出入口及其审计已由运维脚本 `scripts/ops/qa_corpus.py` 提供，见下一条（Issue [#664](https://github.com/Moshuiwang/lingxi/issues/664)）。
- **被授权的语料读取者可以按人、时间窗或关键词读语料并受控导出，每次都留审计**：新增运维脚本 `scripts/ops/qa_corpus.py`（`read` / `export` / `stats` / `grant` / `revoke`），读取权只认为此设立的窄读取角色登记表（管理员用脚本授予 / 撤销、各留一行审计；读取角色独立于管理员登记表，读取者是否须同时为管理员待产品负责人确认，默认不要求）；读取与导出都在鉴权之后、审计行落账之后才输出——审计写不进去就不打印、删文件；导出文件只能落在 scheduler 专属持久卷内的受控目录（目录 0700、文件 0600），审计里记文件摘要与条数，关键词与正文都不进账。容量：不自动删除；行数 / 字节可由 `stats` 读出，水位告警接线为后续工作项（Issue [#664](https://github.com/Moshuiwang/lingxi/issues/664)）。
- **运营操作从此有一张持久的审计账**：新表 `operation_audit`（迁移 `0097`）记录每一次运营操作「谁发起、谁确认、谁执行、目的与目标范围、结果、证据指针」，只追加不可改、九十天后自动删除；此前这些只在进程日志里、跨进程不可关联。本条只建载体与读写口；预开通、欢迎卡、内测扩员三件运营操作已接入，见下一条（Issue [#788](https://github.com/Moshuiwang/lingxi/issues/788)）。
- **预开通、欢迎卡、内测扩员三件运营操作接入同一套具名身份与持久审计账**：两个受控脚本在管理员闸之后、执行之前先写「已准备」，写不进去就退出码 2、一个人都不执行；逐人终态与运行汇总各一行挂在同一操作号下，写失败不改任何人的结局、只以退出码 3 提醒留档；内测扩员的准备、确认、取消、拒绝与批次状态同一事务落账（写不进去整笔回滚），后台两个阶段逐人记「已执行」。账里只有固定码、计数与标识，邮箱、姓名、正文与异常正文都进不去；扩员专用窄表照旧双写（Issue [#675](https://github.com/Moshuiwang/lingxi/issues/675)）。

### Fixed

- **受限 relay 账号不再可能被别的公钥来源拿到 shell**：此前受限通道只靠 `authorized_keys` 那一行的 `command=` 钉住强制命令，而云主机的 sshd 常带全局 `AuthorizedKeysCommand`（如 EC2 Instance Connect，预发与生产实读都有），能为该账号临时推送另一把公钥、绕过强制命令拿到 shell；现在 `sshd` 模板与引导安装在该账号的 `Match` 块里关掉其它公钥来源（`AuthorizedKeysCommand none` / `TrustedUserCAKeys none`）并把强制命令钉在 `Match` 块上（`ForceCommand`），公钥从哪来都只能进 relay；预发与生产已按同形加固并复测三路否定（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#566](https://github.com/Moshuiwang/lingxi/issues/566)）。
- **宿主侧监控单元与拉取代理不再依赖主机默认 `python3`**：预发主机默认 `python3` 是 3.9，仓库单元写死系统 `python3`、资源采样脚本从 PATH 取 `python3`，结果资源采样每分钟失败 80 分钟无人察觉、阈值告警候选版起不来；现在 host-monitor / release-pull 的 `ExecStart` 与 resource-sample 注入给采样脚本的解释器统一指向固定符号链接 `/opt/lingxi/bin/python3`，由引导安装用前提核对定下的 `<PYTHON_ABSOLUTE_PATH>`（3.11 以上）建链，两台主机只差链接目标、仓库单元逐字相同、本机 drop-in 不再需要覆盖 `ExecStart`；采样脚本在链接缺失或版本低于 3.11 时退出码 `2` 并打印实际版本，不静默落回 PATH（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#756](https://github.com/Moshuiwang/lingxi/issues/756)）。
- **预发 / 生产 env 文件里的 DSN 带引号时，升级不再在迁移头回读处停住**：三个常驻服务由 Compose 起、读 env 文件会去掉值两端成对的引号，部署器回读迁移头与业务探针却直接 `docker run --env-file`，docker CLI 不去引号，预发 env 文件历来给 DSN 加单引号，迁移容器拿到带引号的连接串、升级停在 `migration_revision_unknown`（此前只能手工改 root 副本去引号）；现在部署器先把 env 文件按 Compose 口径规范化（去成对引号、处理转义、去注释与 `export` 前缀）写成状态目录下用完即删的 `0600` 私有副本再喂给 `docker run`，原文件不动、副本不进日志；文件读不到、不合 Compose 语法或规范化后为空时在 `docker run` 之前以 `env_file_unreadable` / `env_file_malformed` / `env_file_empty` 停住，不猜（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **候选与正式版 Release 附带控制包索引附件，拉取代理不再因缺附件拒绝该版本**：拉取代理按契约要求 Release 有清单、控制包与外部索引 `control-index.json` 三个附件，此前发布流程只上传前两个，代理核包时会以 `bundle_digest_mismatch` 拒绝该版本、不安装也不 apply，无人值守升级走不到安装；现在候选写入与正式提升都从控制包内读出索引作为第三个附件上传，摘要等于清单的 `index_sha256`；中断重跑时已有相同附件不再上传、已有不同附件拒绝覆盖并保持草稿。此前已发布、没有索引附件的版本不回填（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **Release 上没有 `control-index.json` 附件的版本也能被拉取代理下载核对**：已发布的 Release 上没有外部索引附件，GitHub 又已对本仓库启用不可变 Release、不能事后补附件（生产现网 v2.4.3 永远没有它），此前拉取代理把「附件缺失」折成「摘要不符」，预发拉取在核包一步就停住，生产升级 2.5.0 时目标版本与作为恢复材料的当前版本都会同样停下；现在外部索引附件可选：有则必须与清单 `index_sha256` 相等，没有则以 tar 内嵌索引为准（同一摘要钉住），包摘要、索引摘要与逐文件核对一项不减，日志标明索引来源（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **拉取代理首轮不再因版本目录被写入缓存而停在 `unknown`**：代理以 root 起的清单工具子进程在只读版本目录里加载同目录模块时会留下 `__pycache__`，下一轮安装回读就以「存在可写项」拒绝已装版本，预发首轮升级连续三轮停在核包一步、日志只见 `unknown`；现在代理进程、清单工具与部署器入口各自关掉字节码缓存，代理起的子进程带 `-B` 并以 `PYTHONDONTWRITEBYTECODE=1` 兜底，仓库 systemd 单元同步带上这两项，不依赖本机 drop-in；核包一步被拒时日志行带 `reason=`（状态账与告警正文不变），文档写明只删 `__pycache__`、不动任何文件即可恢复（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **部署器不再因 `public-config.json` 装成 root 0644 而在 plan 一步停住**：非秘密映射按引导安装约定装成 `root:root 0644`，此前部署器入口却按私有材料要求 `0600`，拉取代理过了核包就记 `阶段=plan 结果码=unknown`、一次部署都不会发生；现在部署器与拉取代理、运行时读同类文件口径一致（属主为运行者且他人不可写即可，`0600` 同样接受），宿主契约、请求、批准仍只认 `0600`；引导安装写明该文件的权限位（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **预发权限发布表新增宿主侧备份、台账、差异、干跑撤回与读回核对守卫**：撤回只认预发 `publish_outbox` 台账中的外部记录标识，写表前备份与显式摘要确认成为升级前后的安全边界（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)）。
- **一次性欢迎卡发送脚本在真实运行时能看见自己的审计记录**：此前「记为说过话」「记为联系不上」这类审计只写进进程内日志、没有任何输出口，真实运行里一行都看不到，只能事后回读数据库；现在脚本运行时把这些记录一并写到标准错误。同批补齐三条只钉住既有行为、不改变任何用户可见结果的用例（文档投递回收判定顺序、「联系不上」标记从库到名单的接线、收件人级拒绝码只认清单成员）（Issue [#751](https://github.com/Moshuiwang/lingxi/issues/751)）。
- **跨版本升级不再被旧版本控制包拒绝**：控制包核对改以包自身索引（由发布清单摘要钉住）为文件集合依据，而不是核对工具自己版本的打包清单；此前 2.5.0 的工具拒绝 v2.4.3 的控制包，预发升级停在「读取当前版本」一步，生产 v2.4.3 升级 2.5.0 也会同样停下。越界路径（含 NUL 字节的路径）、缺必备文件、包与索引不一致仍拒；同批把拉取代理「同键告警已去重」的轮次日志从 `sent` 改记 `deduplicated`（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **部署器能首次接管手工启动的旧服务**：此前部署器只认自己带标签起过的容器，而预发与生产现网的三个常驻服务都是人手工用 Compose 启动、没有部署器标签，首次拉式部署会在核对服务清单时以「计划外服务」停住。现在两枚被核对的部署器标签都缺且镜像摘要等于计划登记旧镜像的容器按「接管前的旧服务」接受；新版本一侧永不放行无标签容器，只缺一枚标签或标签值不等仍拒；豁免只在本机首次由部署器接管（没有 `host.active.json` 或同一计划接续）时生效，本机已由部署器部署过之后出现的无标签容器仍拒；首次接管中断在「首个服务更新后」再走 `recover` 会在核对服务清单时安全停下，登记为已知边界（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **宿主契约的 `host` 写成短主机名时，拉取代理在取锁前就报 `host_mismatch`**：部署器按内核主机名（`socket.gethostname()`，通常是 `/etc/hostname` 全名）比对契约，`hostname -s` 写出的短名每轮都会让 apply 在停服务前停住；现在代理先做同一比对，不等即退出、不写状态账、不告警，引导安装与代理文档写明取值命令（Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770)、Issue [#567](https://github.com/Moshuiwang/lingxi/issues/567)）。
- **扩员准备不再把管理员自己的其他在途管理动作误报成「确认已过期」**：在途守卫只看扩员这一类待确认动作；若发起人本人已有别的在途动作占着名额，返回明确的「目标已有在途动作」原因，管理员能自诊断（残留：他类已过期动作要等懒清扫或本人点击才释放名额）（Issue [#708](https://github.com/Moshuiwang/lingxi/issues/708)）。

### Changed

- **管理员补授与已有本地授权重叠的职位范围时只登记缺的项**：此前所选范围里任一项已有生效本地授权就整笔拒绝；现在按差集补齐，确认卡与终态卡显示「本次新增 N 项、已有 M 项沿用」，一项都不缺才拒绝并提示「所选职位范围的 N 项均已登记，无需补授」。撤销只撤本笔新增的行（原组保留，随后重算并重新推送多维表格），撤销终态卡与管理群通知加一句「该用户经银河来源仍持有其中 K 项」（按准备时刻的银河快照现算；读不到时写「银河来源暂不可读」，不写成 0 项）。本地补充库与银河来源仍是两套独立体系，登记不查银河、交付时并集去重（Issue [#715](https://github.com/Moshuiwang/lingxi/issues/715)）。
- **#567 拉取代理进控制包**：每台主机按环境筛选 Release、核对控制包与清单摘要、调用固定部署器并在失败或超时时告警；目标已在位与中断计划均保持幂等接续。
- **worker-queue 要求入口注入并校验问数 MCP 目标地址**：地址缺失、为空或无效时以 `exit=3` 失败且不接单，同源不同路径仍可用（Issue [#604](https://github.com/Moshuiwang/lingxi/issues/604)）。
- **监控库原始样本按 30 天窗口收缩**：每轮成功上推后删除 `sampled_at` 早于 30 天的 `sample` 行，并记录收缩行数（Issue [#756](https://github.com/Moshuiwang/lingxi/issues/756)）。
- **候选镜像包核验工具不再依赖本机镜像存储驱动**：`verify_epic_candidate_bundle.py --import` 改为先从每个 tar 内的 `manifest.json` 读出 config 摘要与清单比对，再逐个导入；在 containerd 快照器存储的机器上也能给出「一致 / 不一致」，摘要对不上或读不到的 tar 不导入（Issue [#765](https://github.com/Moshuiwang/lingxi/issues/765)）。
- **裁决层的代码审查范围收窄**：只对 `verdict.yml`、`verdict_decision.py` 和 `test_verdict_decision.py` 保留产品负责人审批，其他工作流改动由主干版门禁复跑裁决（Issue [#748](https://github.com/Moshuiwang/lingxi/issues/748)）。

## [2.4.3] - 2026-09-11

本版把 `2.4.0`、2.4.1（Trace [#709](https://github.com/Moshuiwang/lingxi/issues/709)）与 2.4.2（Trace [#732](https://github.com/Moshuiwang/lingxi/issues/732)）三批此前只在预发验证过的工作一次性发到生产——三版原定分两批发布，产品负责人裁定改为一次发完（Issue [#700](https://github.com/Moshuiwang/lingxi/issues/700)）。生产由 `2.3.1` 直接升级到本版，**不补发 `2.3.2`**：`2.3.2` 只是一次正式制品打包，从未部署过。本版新增数据库迁移 `0091`–`0095`（前三批产生，均只在预发验证过）与本批新增的 `0096_plpgsql_search_path`（数据库函数查找路径加固，Issue [#661](https://github.com/Moshuiwang/lingxi/issues/661)）；其中 `0091`、`0092` 只要表里已有数据就拒绝降级，`0095` 降级会静默丢失「说过话」状态——因此**本版不提供数据库降级**，回滚只切回旧版本的镜像与四份 digest，不触碰数据库结构与数据。**随 v2.4.3 发布到生产；真实用户验收另行记录。**

### Added

- **管理员可以通过受限内测入口发起批量扩员**：默认关闭的可选装配，不主动打开时对现有部署零影响；打开后由管理员本人确认才会加人，系统逐人核对开通与真实就绪情况，尚未就绪或者还没取得通知授权的人不会收到「可以使用了」的通知（Issue [#668](https://github.com/Moshuiwang/lingxi/issues/668)）。
- **内测名单可以从「改配置重启」切换成数据库动态维护**：新增只读预演、按摘要校验写入、写入后回读核对三个步骤；摘要对不上就一个字都不写，重复邮箱、名单前后不一致、邮箱查不到人等情形整份拒绝，不猜测、不取一半（Issue [#668](https://github.com/Moshuiwang/lingxi/issues/668)）。
- **部署有了完整计划、可从中断处接续、以及需要单独批准才能触发的恢复动作**：四份镜像与不含秘密的控制信息包一起固定发布；具体的真实安装与灰度仍按发布记录单独验收（Issue [#566](https://github.com/Moshuiwang/lingxi/issues/566)）。
- **「这个人有没有跟我们说过话」变成一条不会过期的档案状态**：此前只活在一张 90 天到期即整行清除的流水表里；现在欢迎卡发送成功记「说过话」，被平台明确拒绝记「联系不上」并提醒管理群一次、且下次开口前不再重试发送，另外两处旧的「从没说过话」判断也一并改读这条新状态（Issue [#673](https://github.com/Moshuiwang/lingxi/issues/673)）。

### Changed

- **一批产品文档与部署 / 核验工具的表述完成与实现对账**：产品合同里关于撤销粒度的描述改为与实现一致（Issue [#666](https://github.com/Moshuiwang/lingxi/issues/666)）；两处部署文档不再与生产实际安装记录矛盾（Issue [#713](https://github.com/Moshuiwang/lingxi/issues/713)）；镜像推送结果的说明补齐了此前遗漏的一种正常结局（Issue [#598](https://github.com/Moshuiwang/lingxi/issues/598)）；跨机器核验工具在存储驱动不同的机器上不再给出误导性的通过结论，查不到可比对依据时如实判定不通过（Issue [#707](https://github.com/Moshuiwang/lingxi/issues/707)）；「我们现在具备什么能力」文档瘦身腾出记录空间，「apps 目录只做组装、不写业务判断」这条规则也第一次有了自动检查（Issue [#689](https://github.com/Moshuiwang/lingxi/issues/689)、[#656](https://github.com/Moshuiwang/lingxi/issues/656)）。以上均不改变用户与管理员当前看到的任何行为。

### Fixed

- **管理员确认过的后台工作，遇到重启不会再半途而废**：进度会被安全地接续处理；外发结果暂时不确定有没有成功时，系统会保留下来等待核实，而不是不清不楚地重复通知或悄悄放弃（Issue [#603](https://github.com/Moshuiwang/lingxi/issues/603)）。
- **问数失败后的追溯号，在三个后台容器里查的都是同一份记录**：此前有一个容器根本查不到，会被直接当成缺少配置拒绝查询（Issue [#657](https://github.com/Moshuiwang/lingxi/issues/657)）。
- **动作类型还没接线时点确认卡不再报错崩溃**：现在会明确拒绝并把这次操作整体撤销，接好线以后管理员直接重新点击即可，不需要人工修数据（Issue [#668](https://github.com/Moshuiwang/lingxi/issues/668)）。
- **内测名单导入之后，受限入口能立刻正常使用**：此前导入名单的命令与入口各自生成一份本该相同的绑定标识、两者对不上，入口装上了却认不出任何请求；这个问题只有真实部署才会暴露，已在预发验收中发现并修复（Issue [#668](https://github.com/Moshuiwang/lingxi/issues/668)）。
- **管理员点确认卡前，如果对方的权限绑定恰好发生了变化，结果不再显示「暂时无法确认」**：保护本身一直有效（不会误发权限），现在会明确告诉管理员「这次没有执行，请重新发起」（Issue [#734](https://github.com/Moshuiwang/lingxi/issues/734)）。
- **一次硬重启不会再让用户收到两份一模一样的文档**：文档或表格已经生成、但还没来得及在本地记一笔账时如果发生重启，恢复后不会再重复生成一份，而是转交人工核对（Issue [#718](https://github.com/Moshuiwang/lingxi/issues/718)）。
- **数据库里负责业务逻辑的函数加固了一处可能被利用的路径**：全部函数改为显式声明可信的查找范围、不再依赖运行时可能被篡改的默认值，相关清理函数也收回了此前不必要的直接执行权限（Issue [#661](https://github.com/Moshuiwang/lingxi/issues/661)）。
- **告警本身在数据库短暂故障时不再失效**：此前数据库一抖，负责通知管理员的告警机制自己也会跟着挂掉，最该发出告警的时刻反而发不出来；现在告警的认领、发送、收尾各自独立上锁，不会被同一次故障一起拖垮（Issue [#685](https://github.com/Moshuiwang/lingxi/issues/685)）。
- **数据库短暂故障时，正在处理中的问数任务不再被取消，常驻进程也不再反复重启**：此前一遇到数据库抖动，负责处理的进程会直接退出、不停重启，用户的任务卡在半路；现在同类故障改为原地降级、继续处理，真正的代码缺陷仍然会正常暴露、不会被这道降级顺手盖住（Issue [#683](https://github.com/Moshuiwang/lingxi/issues/683)）。
- **数据库故障不再被误报成「飞书发送失败」**：管理群收到的告警此前会把数据库连不上的故障指向错误的方向，让排查的人从飞书那头查起；现在会准确标成投递链路故障（Issue [#684](https://github.com/Moshuiwang/lingxi/issues/684)）。
- **翻页查询不会再悄悄漏读数据**：此前对方接口没有明确告知「还有没有下一页」时会被当成「没有下一页」直接放行，少读的数据没有任何人知道；现在这类含糊或非法的翻页信息会被判定为错误而不是当成「读完了」，真实的空结果仍然正常返回（Issue [#719](https://github.com/Moshuiwang/lingxi/issues/719)）。
- **系统繁忙时的提示不会再被误以为操作已经生效**：忙时「未受理」与「已经排队」是两种不同的情况，此前共用同一句话让人分不清；现在拆成两句话分别说清楚，已经排队的人不会被多余的提示误导去重新发一次（Issue [#717](https://github.com/Moshuiwang/lingxi/issues/717)）。

## [2.3.2] - 2026-09-08

- 发布机制：版本维护分支独立打包，候选预发布与正式提升分开；正式版复用已验收镜像，生产部署另行记录（#669）。业务行为与数据库结构不变；本条不代表已经部署生产。

## [2.3.1] - 2026-09-07

欢迎卡在生产预检时被产品负责人逐条校正：指标中文名、展示顺序、示例问法与可用范围的说明。**只改用户可见的文案与随包数据，不改任何业务逻辑，无数据库迁移。**

### Fixed

- **指标中文名改用业务上真实的叫法**（Issue [#586](https://github.com/Moshuiwang/lingxi/issues/586)）：`订户数`→`用户数`（新增 / 充值 / 扣费三条）、`渠道市场份额`→`收视市占率`、`渠道费率`→`收视率`、`增值税率`→`税率`。前三条同时把我们的叫法与问数系统自己返回的名字对齐（此前是自造词）。这份别名表同时供管理卡的下拉、确认卡、终态卡与群通知使用，因此**管理员一侧那批错名字也一并修正**。

### Changed

- **指标按业务顺序展示，顺序写在数据里而不是代码里**（Issue #586）：欢迎卡的「指标」字段与示例句都按随包别名表里键的先后排（新增 → 充值 → 扣费 → 收视 → 汇率 → 税率）；此前是按内部标识的字母序，那个顺序与业务无关。同一份文件还登记了三条示例句各自优先使用哪个指标（`[examples] prefer`，按位置对应、允许重复）。**改顺序或换示例指标只需改这一个文件，不必改代码。**
- **三条示例问题按预估执行时间由简到繁重写**（Issue #586）：一个现成读数 → 本月累计与环比 → 分区汇总加排名分析并出飞书文档。让人先拿到一次快的成功，再知道它能做更重的事。第一句用「前天」而不是「昨天」：**昨天的数据往往还没落库，让人第一次提问就扑空是最坏的开场**。
- **「你当前可用范围」增加「维度」一行**（Issue #586）：`时间（最近 3 年）、大区、城市`。它对所有人一样、不随权限变，因此是固定文案；公司位单列是因为只有它受权限约束。
- **出问题反馈只要求附截图**（Issue #586）：此前还要求写下提问原话与大致时间，这两样服务端自己查得到，不必让用户做。


## [2.3.0] - 2026-09-07

一个制品同时上生产两批工作：Trace [#606](https://github.com/Moshuiwang/lingxi/issues/606) 的用户可见文案热修，与 Trace [#643](https://github.com/Moshuiwang/lingxi/issues/643) 质量整改批一的八项缺陷修复。生产由 `2.2.0` 升到本版。

**两批都是第一次进生产**：#643 那一批此前的证据封顶在预发（受控测试身份，非真实用户），本版是它八项修复第一次面对真实用户与真实数据。因此**本次生产观察的成败不能单独归因于其中任何一批**。含两条数据库迁移（`0089_carrier_retention`、`0090_delivery_retry_backoff`），都是加列与加索引，不建表、不删除。

### Fixed

- **用户看到的指标一律是中文名，不再是 `sub_recharge_money` 这类内部标识**（Issue [#586](https://github.com/Moshuiwang/lingxi/issues/586)）：欢迎卡的「指标」字段与两条示例句、以及所有「当前可用范围」通知（开通完成、迟到就绪的开通完成、预开通用户首聊时补的那一句、权限变化告知）此前都把权限文档里的内部指标标识原样给用户看。中文名产品里本来就有——管理员一侧早已按同一份别名表显示中文——只是这几条路径从没调用过它。**欢迎卡查不到中文名时整条跳过不发**（宁可漏发也不把内部标识印上卡面），**范围通知查不到时原样展示标识**（不发通知比发一条说不全范围的通知更糟），两处姿势不同的理由各自写在代码里。随包指标目录里的每一个指标都必须有中文名，缺一个当场判红。
- **信息不完整时不再提交冒充完整结果的权限决定；撤销后的迟到补齐不再让权限复活**（Trace #643，IN-01 / IN-02 / IN-05）：来源读取失败与「确实是空」此前分辨不出来，读不到就可能被当成空集提交；现在两者可分辨，信息不完整就不提交。提交事务内还会复核授权组仍然有效，因此一笔已经撤销的授权不会被迟到的补齐工作重新变成生效权限。三个入口（首聊、每日刷新、定向重算）的完整决定收拢成同一条实现。
- **没送到就不会记成已通知**（Trace #643，IN-03）：文件链接与结果的送达确认此前会把旧进度卡的消息编号、空响应、平台的「仍在发送」当成已送达；现在只有本次终态的有效回执才允许确认，缺证据时保留「不明」并留恢复入口，卡片终态与文本兜底都失败时任务不记成功。
- **到期的内容真的会被清掉**（Trace #643，IN-04）：提问原文、入站事件、待确认动作等内容载体此前没有全部接进实际清理入口，九十天上限只写在承诺里；现在逐个接上，且从旧备份恢复的数据按原始期限计算，不因为恢复而延长保留。
- **一个人的失败任务不再长期挡住别人**（Trace #643，IN-06）：待重试的任务此前会持续占满候选机会；现在重试退避落库并进入候选过滤，混合失败与重启下健康任务继续得到处理，且不重复交付。
- **管理员点了确认就能看到结果，出错时看得到原因**（Trace #643，IN-09 / IN-10）：管理动作已提交但后置回读故障时，此前可能把已执行说成未执行；现在返回事务内已知的决定与终态快照，或明确告知「已提交、展示待恢复」，重复确认与取消都不重做业务动作。执行侧的错误原因不再在传递中丢失，按有界、脱敏的形态保留到下游报告。

## [2.2.0] - 2026-09-05

体验批（Trace [#606](https://github.com/Moshuiwang/lingxi/issues/606)）：预开通完成之后的主动告知、名单的一人多公司范围、管理卡结果只说真话、用户可见文案可从宿主机外置。**证据只到本机**（本机用例、真库用例与变异验红）；`biai-stage` 的受控真实验证与生产真人验证**一步都没有做**，逐项未验证清单见 `docs/当前能力.md` 2026-09-05 体验批条。本版**尚未部署到生产**；它上生产时，`2.1.0` 的可读性重构与门禁批（Issue #591、Trace #592）也随同一制品**首次**进入生产（生产当前仍为 `2.0.2`）。

### Added

- **主动告知（欢迎卡）**（Issue [#586](https://github.com/Moshuiwang/lingxi/issues/586)）：预开通完成的人可以主动收到一张 BI Plus 卡片——问好、自我介绍、他本人当前可用的范围、示例问题、出问题找谁，不用再等他自己发现机器人。这是一项与预开通解耦的通用能力，预开通名单只是第一个调用方。发起入口只有容器内的运维脚本，默认只出清单零发送，真发与预检都必须给出发起人且发起人须是有效管理员；**发送前先用当前环境的机器人把同一张卡真发到管理员私聊做预检**（stage 预检＝Bot-Test；生产真发前的预检＝生产机器人，只发给管理员本人、不发名单内用户；同一渲染函数、同一份数据）。只对已激活且账号正常的人发，同一份名单重跑零新增发送，失败逐人关闭并通知管理群、可重试；卡片正文不进日志也不进数据库。
- **预开通名单支持一人多公司范围**（Issue [#587](https://github.com/Moshuiwang/lingxi/issues/587)）：公司范围一格可用顿号写多个公司，合成**一笔**可整组一次撤销的预授权；顿号之间的空段、以及「全部」与具体公司混写，一律整份名单拒绝、零写入，不替人猜。五位多公司范围的 CEO 因此不再需要顺延或用「全部」代替。
- **用户可见文案可从宿主机外置覆盖**（Issue [#585](https://github.com/Moshuiwang/lingxi/issues/585)）：已上线的一句话可以只改宿主机上的一个文件加重启，不必走完整发布链；只能改已登记键的正文，键集合、占位符与卡片结构仍以镜像内那份为唯一权威，写坏则整份忽略、退回镜像内文案并告警。**生效时机是进程启动，不是编辑即生效**。**生产本批默认不启用**（不配置那个变量时行为与现在逐字相同）。操作正文见 `docs/技术设计/文案说明.md`。

### Fixed

- **管理卡的结果只说这一次操作的真话**（Issue [#602](https://github.com/Moshuiwang/lingxi/issues/602)）：同一张管理卡被复用时，上一次操作的已发布记录会把还没有发布记录的新操作说成「已生效」；现在结果查询与后台结算都只认这张卡当前那一次操作，迟到的旧回调既不能把新操作说成已生效，也不能把它降级成未完成。
- **「已激活重认领」通知正文改用翻译后的指标中文名**（Issue [#605](https://github.com/Moshuiwang/lingxi/issues/605)）：已开通的用户再次触发开通链时，通知正文的职能位此前显示的是内部标签，用户读完仍不知道自己能查什么；翻译查不到时按本侧故障收口，绝不回落成内部标签。

## [2.1.0] - 2026-09-05

内部工程基线升级：`src/lingxi/` 按 PEP 8 / PEP 257 / Google 风格整体重写可读性，并把四道门禁接进 CI（Issue #591、Trace #592、PR #596，merge `d18b774`）。用户可见行为零变化。计为次版本的口径：产品负责人 2026-09-05 裁定「工程基线的重大变化按次版本处理」（见 `deploy/README.md`「版本号规则」）。本版尚未部署到生产。

### Changed

- **正式代码可读性重写**（Issue #591、#592）：函数硬上限 60 行（超 60 行函数 185 → 0）、注释只留契约不留来历（注释与 docstring 占比 34.6% → 23.8%，Issue 号 / 日期 / 审查人等来历标记 2,409 → 0）、全仓 `ruff format`（行宽 100；格式化提交已登记 `.git-blame-ignore-revs`）、六个千行以上文件拆到 1000 行以下并新增 31 个兄弟模块。纯移动与改名，既有测试断言未改。
- **门禁四件套进 CI 与本机检查**（Issue #592）：`ruff check` / `ruff format --check`、函数体量棘轮、注释卫生棘轮（`src/lingxi/` 内禁一切抑制注释）、文件阈值 1000 行；规范正文见 `docs/技术设计/代码规范.md`。

### Fixed

- **空闲连接栈弹出已关闭连接时显式 `discard()`**（Issue #593 审核遗留 P2-a，Issue #592 决策清单 D-17）。
- **三个常驻进程停机时显式关闭空闲连接**（Issue #593 审核遗留 P2-b）。

## [2.0.2] - 2026-09-04

热修，2026-09-04 18:30 北京上生产（镜像 `20260904-c6fa5ac5f9ad`）。git tag `v2.0.2` 为事后补打；`pyproject.toml` 未随本版升版，自 2.1.0 起补齐。

### Fixed

- **数据库连接进程内复用**（Issue #593，PR #595）：三个常驻服务不再每次数据库操作都新建连接，Supabase 侧建连从每分钟约 400 次降到个位数，消除 TLS 握手造成的出口流量超限。

### Changed

- 生产投递轮询 1 → 5 秒、任务发现 2 → 10 秒（`LINGXI_GATEWAY_DELIVERY_POLL_INTERVAL_SECONDS` / `LINGXI_WORKER_POLL_INTERVAL_SECONDS`，运维配置，按产品负责人裁定保留）。

## [2.0.1] - 2026-09-04

rc25 上线后技术清账批（Trace #544，PR #583，main `d67f772`），2026-09-04 升级到生产（镜像 `20260903-d67f772a3a6f`，git tag `v2.0.1`；`pyproject.toml` 未随本版升版）。本条为标题级摘要，正文与证据等级见 `docs/当前能力.md` 2026-09-03 段。

### Changed

- 同邮箱闸、指标映射同源、导入值卫生、docx 正文改走一次建档写全文、管理员预开通核心链（Issue #538、#541、#544）。

## [2.0.0] - 2026-09-02

首个正式版本号；本仓库此前无对外版本号（起版即从 2.0 开始，见 Issue #417）。本条目汇总 2.0.0 的全部发布内容，含发布就绪批（rc21）、体验打磨批（rc22）与上线前清仓批（rc23）。所列能力均已在 `biai-stage` + `Bot-Test` 受控环境完成 L4a 及以上验收；`biplus-prod` / `Bot-Prod` 的生产部署随本版本进行，其结果由上线记录而不是本文件断言。

### Added

- **问数**：飞书私聊自然语言问数闭环，多轮上下文追问、`/new` 重置当前话题、忙碌态与并发提示、结果安全过滤（`withheld`/`masked`）、`/stop` 停止（Issue #154、#90、#189）。
- **首次开通链**：花名册与银河权限自动匹配、开通编排、权限发布后 MCP 就绪确认、开通中途失败的可追溯与自愈重发（Issue #65、#89、#203、#280、#282）。
- **文档交付**：正式产物随对话在线文档路由，文档级可管理授权（Issue #97）。
- **表格交付**：飞书电子表格产物，复用文档交付同构路由与检查点架构（Issue #354）。
- **用户记忆**：`/memory remember`·`list`·`forget` 显式登记式记忆（术语映射/口径偏好/惯例模板），跨用户隔离，停用即清（Issue #357）。
- **管理员入口**：唯一管理员在飞书私聊完成查询与操作——用户状态、审计、开通失败追溯（`/admin user`·`audit`·`trace`），停用与恢复，以及本地权限的补充授权、屏蔽指标、撤销三个动作；每一次改变状态的操作都要管理员本人在飞书确认卡上再点一次，并留下完整审计（Issue #95、#96、#337、#319）。
- **用户权限管理卡**：管理员查一个用户就得到一张卡——看得到他的银河来源权限与本地补充授权，直接在卡上填「职位 + 公司范围 + 原因」发起补充授权，提交后仍需在确认卡上确认才生效（Issue #439、#493）。
- **权限发布**：用户真实可见的数据范围 =（银河职位翻译出的权限 ∪ 管理员补充授权）− 管理员屏蔽的指标，未翻译标签发布闸（Issue #226、#227、#328）。
- **监控告警与部署运维**：四进程镜像发布、compose 资源限制、30 天日志留存、最小监控告警（unhealthy 注入→管理群告警+恢复通知）、生产部署 runbook、备份恢复演练（Issue #62、#153、#343、#135、#369）。
- **版本号管理**：引入 SemVer、整仓单一版本、发布随批次打 `v<版本>` git tag、CHANGELOG 随发布批次维护（Issue #417）。
- **等待与忙碌可感知**：问数超过约 12 秒仍无输出时给出明确的等待提示，长时间无进展会告诉你它卡在哪一步；同时有多个问题排队时，卡片如实显示"正在排队"而不是假装已经在算（Issue #444、#465）。
- **失败可追溯**：一次失败的问数会记下可区分的失败原因与稳定的失败签名，管理员用 `/admin trace <追溯号>` 就能查到究竟是哪一类失败，而不是所有失败都长得一样。签名只留可比对的摘要，不含任何用户标识或异常正文（Issue #495）。

### Changed

- **回答里含表格时的文档交付，从「整次失败」改为「降级交付并如实告知」**（Issue #499）：此前回答里出现表格会让整篇文档交付失败、用户空手而归（浸泡实测 22 次投递里失败 4 次）。现在改为把正文按纯文本段落写进同一篇文档，交付照常完成，就绪通知会明说"格式已简化"及原因，并提示可以改用电子表格重新生成。**边界如实**：降级解决的是"拿不到"，不保证"好看"——表格会被拍平成文字，`|` 与 `---` 这类符号会留在正文里；表格转飞书文档的完整排版仍未实现。
- **Word 风格排版默认开启**（Issue #467）：文档交付的 markdown→飞书文档结构转换由"默认关、要显式打开"改为默认开启，运维不再需要每次手动开。
- **管理员补充授权的填写方式改为「职位 + 公司范围」**（Issue #493）：此前要一条一条地填公司和指标，现在选一个银河职位加一个公司范围（单个公司或"全部"），系统按职位职能映射自动展开成对应的公司×指标；撤销时按"一个职位+范围"整组一次撤掉。历史上逐条填出来的记录照旧显示、照旧可以逐条撤销，不迁移也不改写。同批把管理卡的"权限正在下发/已生效"改成如实展示：确认成功只表示已记录并正在下发，真正发布完成后才显示已生效，超时则显示未完成并给出追溯号。
- **对外名称统一为 BI Plus**（Issue #443）：所有用户可见文案、通知与管理员出口使用同一个名字。
- **权限动作的用词统一**（Issue #439）：用户与管理员看到的一律是"补充授权"、"屏蔽指标"、"撤销"，不再混用旧系统的说法；管理卡上不再出现任何内部 ID。
- **同时处理的问数任务上限固定为 4**（Issue #496）：这个上限现在是显式配置，并由代码、编排文件与部署门禁共同钉住，漏配直接启动失败，不会悄悄退回一个没人核对过的默认值。

### Fixed

- **停用之后不会被重新放开权限**（Issue #468、#483）：已停用的账号此前可能被每日权限重算重新发回权限，或在批量重算的时间窗里被重新发权；现在停用即停用。
- **确认卡不再卡死或失效**（Issue #437、#438）：待确认操作过期后会明确失效并要求重新发起，不再让管理员停在一张永远点不动的卡片上；确认之后权限即时生效，不必等到第二天的批处理。
- **权限范围不再被错误放大**（Issue #440）：银河"全部指标"通配的判定口径修正，三个发布路径统一走同一份合并实现，不再各写一份可能走偏的等价逻辑。
- **文档正文顺序正确**（Issue #442）：交付的飞书文档不再出现段落次序错乱。
- **繁忙时不再整体变慢**（Issue #464）：任务领取改为滚动并发，一个慢任务不再拖住同批其他人的问数。
- **服务不再静默写满内存盘**（Issue #494）：会话转录此前会在约 45 分钟内把 worker 的 `/tmp` 内存盘写满，用户开始失败而健康检查一路显示正常。现在有常规回收、分环境的容量上限，健康检查也能看见这块盘——"用户在失败、监控显示健康"这个组合被消除。
- **问数后端 502 有了明确结论**（Issue #496）：问数 MCP 返回 502 时按有界的、可行动的终态失败处理并给出稳定失败码，不再无限重试或落进"未知失败"。

### Removed

- JumpServer 高级工作台入口：不再属于产品范围，飞书私聊成为唯一用户入口（Issue #368，[决策记录](docs/决策记录/2026-08-28-取消JumpServer高级工作台.md)）。
- 旧系统"存量沿用"权限来源：这条来源已整体退役（Issue #441），原有权限一次性导入为管理员的本地补充授权。此后用户的数据范围只有两个来源——银河职位翻译与管理员补充授权，不再有第三套看不见的权限来源。
