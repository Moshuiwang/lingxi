# 显式声明构建后端：package-data 依赖 setuptools>=61 读取 pyproject 配置，
# 不声明时打包行为随环境里碰巧解析到的工具链漂移（独立复查发现）。
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lingxi"
version = "2.0.0"
description = "Lingxi 企业内部受控 AI 业务分析服务"
requires-python = ">=3.12"

# extras 按**进程**分组，不按外部系统分组（Issue #56）。每组 = 从该进程入口
# 追 import 链得到的第三方依赖闭包，组名与 `python -m lingxi.apps.<name>` 对齐，
# 也就与 Docker Compose 的一个服务对齐（见 docs/技术设计/代码框架.md 第一、六节），
# 这样镜像构建只装该进程真正要用的东西。同一依赖出现在多个组里是正常的。
#
# 旧名 `feishu` 名实不符：它实际装的是 Bot-Test 受控验证那一整套（含与飞书无关的
# psycopg / cryptography），而正式的飞书目录适配器 `adapters/feishu_directory.py`
# 走裸 HTTP、根本不用 lark-oapi。旧名 `agent` 描述的是「依赖是什么」而不是
# 「谁需要它」，一并按进程改名。
#
# **`src/lingxi/` 里没有任何模块级第三方 import，全部是函数内延迟导入。**
# 因此「模块能 import 成功」证明不了依赖装上了——校验必须显式导入第三方模块本身，
# 见 scripts/ci/check_installed_package.py 的 `--process` 模式与 PROCESS_RUNTIME_IMPORTS。
[project.optional-dependencies]
# lingxi-scheduler 进程（`python -m lingxi.apps.scheduler`）。现在跑**三个**定时职责：
# 专用授权凭据续期扫描、九十天保留清理（#54）、花名册审计日报（#52）。
# 依据：
#   cryptography → adapters/delegated_credentials.py（Fernet 加密宿主机凭据文件）
#   psycopg      → adapters/delegated_credentials.py、retention.py、postgres_roster_audit.py
# **本组仍然不含 lark-oapi。** 同链上的 adapters/feishu_directory.py 与 #52 新增的
# adapters/feishu_group_message.py（管理群群发）都只用标准库 urllib——发消息走 SDK 会
# 为一个尚未接线的职责把整个 SDK 拉进常驻进程的依赖面。
# cryptography 精确锁版：Fernet 是宿主机凭据文件的安全边界，范围区间会让
# 依赖升级在代码未变时改变该边界（终轮 Codex）；升级须重跑凭据保管验证。
# scheduler 镜像也承载一次性 reauthorize job，因此显式带上 OAuth Bridge WebSocket
# 传输依赖；常驻 scheduler 本身仍不加载 OAuth Bridge 业务模块。
scheduler = [
    "cryptography==45.0.7",
    "psycopg[binary]>=3.2,<4",
    "websockets>=15,<16",
]
# 正式重授权是 scheduler 镜像里的独立一次性 job，但 #76 的干净环境门禁按进程
# 闭包逐项对账，因此保留同一依赖闭包的独立 extra 供 CI 验证；部署仍复用 scheduler
# 镜像，不额外构建第五个生产镜像。
reauthorize = ["cryptography==45.0.7", "psycopg[binary]>=3.2,<4", "websockets>=15,<16"]
# lingxi-worker 进程（`python -m lingxi.apps.worker`：turn/queue 两种受控执行模式）。
# 依据：入口 → queue service → PostgreSQL queue adapter，以及 Agent SDK session adapter。
# 精确锁版本，不用范围：执行层的只读边界建立在这个版本的 hook 事件名与工具名上
# （`PostToolUseFailure` 存在、子代理叫 `Agent` 不叫 `Task`、`HookMatcher` 的签名），
# 这些都是实测得到的、SDK 未作稳定性承诺的细节。升级前必须重跑 L4a，见
# docs/技术设计/验证与门禁.md 的 V-部署-08。
# 判定逻辑不依赖本组：`lingxi.core.execution` 的全部断言在无 SDK 环境即可跑，
# 绑定层的形状由桩模块锁住。CI 另有一步装上本组做真实冒烟（构造 `HookMatcher`，
# 不调模型、不用凭据），见 scripts/ci/check_agent_sdk_binding.py。
worker = ["claude-agent-sdk==0.2.128", "psycopg[binary]>=3.2,<4"]
# lingxi-gateway 进程（`python -m lingxi.apps.gateway`：飞书长连接接入、任务入队与
# 投递消费循环）。依据是从入口逐个追 import 链：
#   lark-oapi → adapters/feishu_longconn.py（ws.Client 建长连接）
#               adapters/feishu_outbound.py（加表情 message_reaction.create、
#               回文本 message.reply）
#               adapters/feishu_delivery.py（Issue #152：CardKit
#               cardkit.v1.card.create/update/settings、
#               cardkit.v1.card_element.content 流式更新、
#               投递文本兜底 im.v1.message.reply）
#   psycopg   → adapters/postgres_conversation/（会话、任务队列、入站事件幂等、
#               投递 outbox 消费记账）
#   websockets→ lark-oapi 的 ws 客户端实际用它建 wss（lark_oapi/ws/client.py:12
#               `import websockets`、:13 `from websockets.exceptions import
#               InvalidHandshake`）
# **websockets 显式声明，尽管 lark-oapi 会传递携带它。** 理由是独立复查已经证明
# extras 矩阵抓不到传递依赖缺失：矩阵装的是 `.[gateway]`，lark-oapi 把 websockets
# 带进来，于是"我们漏声明"这件事永远不会变红；一旦上游哪天把它挪出依赖表，
# 长连接会在部署时才失败。这一组**不含 cryptography**——gateway 不碰 Fernet，首次开通
# 编排（连同它的 MCP 令牌加解密）自 2026-08-18 裁定起住在 scheduler。
gateway = [
    "lark-oapi==1.7.1",
    "psycopg[binary]>=3.2,<4",
    "websockets>=15,<16",
]
# 2026-08-23 #146 清退：`bot-test` 组随 feishu_onboarding.py/refresh_tokens.py/
# postgres_onboarding.py 三个 Bot-Test 消费者一并删除——它们是这组里 lark-oapi、
# cryptography、psycopg 三项依赖唯一的存在理由。2026-08-24 #203 清退：曾经的保留件
# `adapters/oauth_bridge.py`（#67 裁定的 E1 授权基础设施参考实现）本身也已删除；
# 它此前唯一的传输依赖——本地 `oauth_bridge_client` 的 `websockets`——已由
# `scheduler`/`reauthorize` 两组声明覆盖，从一开始就不需要为它专设一组。详见代码
# 框架第五、六节。
# 数据库迁移（Issue #53）。**这一组不是常驻进程**，是部署时跑一次的迁移作业：
# `LINGXI_MIGRATION_DSN=... python -m alembic upgrade head`。它单独成组而不是挂进
# scheduler，有三个理由：
#   1. 迁移要 DDL 权限、业务进程不要，两者本就该是不同的数据库角色；
#   2. `src/lingxi/` 里不得出现 sqlalchemy / alembic（断言 V-迁移-04），把它塞进
#      某个进程组等于声称那个进程运行时需要它，是假的；
#   3. 生产镜像因此不必为了跑业务而装上 SQLAlchemy。
# 窄区间而不是 `==`：按 2026-08-06 裁定①，alembic 属迁移工具链，不在运行时进程内，
# 不是 V-部署-08 所说的"安全边界组件"。上界不能省——alembic 的 revision 解析与
# 事务语义是次版本可变的，开区间等于允许它在代码没变时改变迁移行为。
#
# **psycopg 必须显式列在这里。** alembic 不依赖任何数据库驱动，驱动由 URL 的 scheme
# 决定；env.py 强制 `postgresql+psycopg`，所以这一组不带 psycopg 就根本连不上库
# （外审实测：干净环境跑 upgrade 报 `ModuleNotFoundError: No module named 'psycopg'`）。
# 此前 gate 是绿的，因为它同时装了 scheduler 组，把驱动顺带带了进来——正是「别的组
# 带进来的包让窄的那组永远不会红」这一类缺陷。区间与 scheduler 组同款。
# 传递依赖（`pip install '.[migrate]'` 实测闭包，五项）：SQLAlchemy、Mako、MarkupSafe、
# greenlet、typing-extensions。psycopg-binary 不在此列，它来自上面 `psycopg[binary]`
# 的直接声明。这份清单在 docs/技术设计/代码框架.md 与 migrations/README.md 各有一份，
# 三处必须一致。
migrate = ["alembic>=1.19,<1.20", "psycopg[binary]>=3.2,<4"]

# 角色职能映射是随包发布的配置文件，不是源码。不显式声明就不会进 wheel，
# 而缺失表现为「全部角色显示未映射」——一种看起来正常的失败。
# CI 的 scripts/ci/check_installed_package.py 校验它确实在已安装的制品里。
[tool.setuptools.package-data]
"lingxi.config" = ["*.toml"]

[tool.unittest]
start-directory = "tests"

# ruff 锁精确版本 0.16.4，装法见 scripts/dev/gate_spec.py 与 scripts/dev/README.md；
# 本机与 CI 用同一个版本，避免规则集随 ruff 升级静默改变判定结果。不写
# target-version：解释器下限已经在 [project] 的 requires-python 里声明一次，
# 两处各写一份迟早漂移，ruff 能从 requires-python 自动推断。
[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "T10", "N", "D", "PLR0913"]
# E203/E501：与 black/ruff format 的空格与行长判定天然冲突，交给 format 处理。
# UP031/UP042/UP046：分别是 %-格式化改 f-string、Enum 改 StrEnum、PEP 695 泛型
# 语法——前者改动面过大且非本批目标，后两者存量各自只有个位数处、且 StrEnum
# 会改变 str() 结果（37 处需逐处评估，登记不做）。
# D400/D415：摘要行必须以句号/问号/叹号结尾——本仓库大量中文文档字符串以
# 「）」「：」等中文标点或代码引用收尾，强制英文标点收尾的规则与既有写作习惯
# 冲突，不采纳（决策清单登记不做：不接受 Google 的这条硬性标点约束）。
ignore = ["E203", "E501", "UP031", "UP042", "UP040", "UP046", "D400", "D415"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.pylint]
max-args = 10

# 存量 D（docstring 结构）与 PLR0913（参数个数）违规文件的登记表：由
# `ruff check --statistics` / `--output-format concise` 实测生成，清单收窄是
# 后续清理批的工作，本批只保证「登记完整、接线后零告警」。收官目标是把这张表
# 清空。集成者收口，施工者不得手改。D 强制范围 = src/lingxi/，其余目录级豁免
# 见决策清单 D-21。
[tool.ruff.lint.per-file-ignores]
"migrations/**" = ["D"]
"migrations/alembic/env.py" = ["I001"]
"scripts/**" = ["D"]
"scripts/ops/import_local_permission_override.py" = ["F401"]
"scripts/probe_drive_folder_permissions.py" = ["N818"]
"src/lingxi/adapters/admin_metric_alias_map_file.py" = ["D"]
"src/lingxi/adapters/admin_post_callback.py" = ["D"]
"src/lingxi/adapters/admin_registry.py" = ["D"]
"src/lingxi/adapters/claude_agent_hooks.py" = ["D"]
"src/lingxi/adapters/claude_agent_session.py" = ["D", "PLR0913", "N818"]
"src/lingxi/adapters/company_function_metric_map_file.py" = ["D"]
"src/lingxi/adapters/delegated_credentials.py" = ["D"]
"src/lingxi/adapters/delegated_subject_lookup.py" = ["D"]
"src/lingxi/adapters/feishu_admin_card.py" = ["D"]
"src/lingxi/adapters/feishu_bitable_association.py" = ["D"]
"src/lingxi/adapters/feishu_delivery.py" = ["D"]
"src/lingxi/adapters/feishu_directory.py" = ["D"]
"src/lingxi/adapters/feishu_docx_delivery.py" = ["D"]
"src/lingxi/adapters/feishu_events.py" = ["D"]
"src/lingxi/adapters/feishu_group_message.py" = ["D"]
"src/lingxi/adapters/feishu_longconn.py" = ["D"]
"src/lingxi/adapters/feishu_org_snapshot_reader.py" = ["D"]
"src/lingxi/adapters/feishu_outbound.py" = ["D"]
"src/lingxi/adapters/feishu_permission_bitable.py" = ["D"]
"src/lingxi/adapters/feishu_reauthorization.py" = ["D"]
"src/lingxi/adapters/feishu_roster_bitable.py" = ["D"]
"src/lingxi/adapters/feishu_sheets_delivery.py" = ["D"]
"src/lingxi/adapters/feishu_tenant_token.py" = ["D"]
"src/lingxi/adapters/feishu_user_message.py" = ["D"]
"src/lingxi/adapters/galaxy_csv_export.py" = ["D"]
"src/lingxi/adapters/galaxy_import.py" = ["D"]
"src/lingxi/adapters/mcp_token_cipher.py" = ["D"]
"src/lingxi/adapters/oauth_bridge_client.py" = ["D"]
"src/lingxi/adapters/postgres.py" = ["D"]
"src/lingxi/adapters/postgres_content_capture.py" = ["D"]
"src/lingxi/adapters/postgres_content_capture_retention.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_dataclasses.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_gateway_store.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_listener.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_queue_base.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_queue_gateway_delivery.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_queue_lifecycle.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_queue_outbox.py" = ["D", "PLR0913"]
"src/lingxi/adapters/postgres_conversation/_queue_session_cleanup.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_task_queue.py" = ["D"]
"src/lingxi/adapters/postgres_conversation/_transaction.py" = ["D"]
"src/lingxi/adapters/postgres_daily_report.py" = ["D"]
"src/lingxi/adapters/postgres_daily_report_watermark.py" = ["D"]
"src/lingxi/adapters/postgres_document_delivery.py" = ["D", "N818"]
"src/lingxi/adapters/postgres_email_binding.py" = ["D"]
"src/lingxi/adapters/postgres_galaxy_snapshot.py" = ["D"]
"src/lingxi/adapters/postgres_identity.py" = ["D"]
"src/lingxi/adapters/postgres_late_readiness_recovery.py" = ["D"]
"src/lingxi/adapters/postgres_local_permission.py" = ["D", "PLR0913", "N818"]
"src/lingxi/adapters/postgres_management_card_context.py" = ["D", "PLR0913"]
"src/lingxi/adapters/postgres_mcp_token.py" = ["D"]
"src/lingxi/adapters/postgres_onboarding_failure.py" = ["D"]
"src/lingxi/adapters/postgres_pending_action.py" = ["D", "PLR0913"]
"src/lingxi/adapters/postgres_permission_publish.py" = ["D", "N818"]
"src/lingxi/adapters/postgres_permission_recompute_trigger.py" = ["D"]
"src/lingxi/adapters/postgres_roster_audit.py" = ["D"]
"src/lingxi/adapters/postgres_roster_snapshot.py" = ["D", "N818"]
"src/lingxi/adapters/postgres_stalled_provisioning.py" = ["D"]
"src/lingxi/adapters/postgres_targeted_recompute_lookup.py" = ["D"]
"src/lingxi/adapters/postgres_user_memory.py" = ["D"]
"src/lingxi/adapters/query_mcp_probe.py" = ["D"]
"src/lingxi/adapters/retention.py" = ["D"]
"src/lingxi/adapters/role_function_map_file.py" = ["D"]
"src/lingxi/adapters/stock_token_bitable.py" = ["D"]
"src/lingxi/adapters/user_environment.py" = ["D"]
"src/lingxi/adapters/user_mcp_config.py" = ["D"]
"src/lingxi/apps/admin_bootstrap/__init__.py" = ["D"]
"src/lingxi/apps/gateway/config.py" = ["D"]
"src/lingxi/apps/gateway/delivery.py" = ["D", "PLR0913"]
"src/lingxi/apps/gateway/document_delivery.py" = ["D"]
"src/lingxi/apps/gateway/group_mention_hint.py" = ["D"]
"src/lingxi/apps/gateway/log_redaction.py" = ["D"]
"src/lingxi/apps/gateway/management_status.py" = ["D"]
"src/lingxi/apps/gateway/onboarding.py" = ["D"]
"src/lingxi/apps/healthcheck/__init__.py" = ["D"]
"src/lingxi/apps/liveness.py" = ["D"]
"src/lingxi/apps/reauthorize/__init__.py" = ["D"]
"src/lingxi/apps/scheduler/alerting_assembly.py" = ["D"]
"src/lingxi/apps/scheduler/assembly.py" = ["D"]
"src/lingxi/apps/scheduler/audit.py" = ["D"]
"src/lingxi/apps/scheduler/config.py" = ["D"]
"src/lingxi/apps/scheduler/credential_rotation.py" = ["D"]
"src/lingxi/apps/scheduler/daily_report.py" = ["D"]
"src/lingxi/apps/scheduler/document_delivery_dead_letter.py" = ["D"]
"src/lingxi/apps/scheduler/late_readiness_recovery.py" = ["D", "PLR0913"]
"src/lingxi/apps/scheduler/loop.py" = ["D"]
"src/lingxi/apps/scheduler/onboarding.py" = ["D"]
"src/lingxi/apps/scheduler/org_snapshot_sync.py" = ["D", "N818"]
"src/lingxi/apps/scheduler/permission_publish.py" = ["D", "PLR0913"]
"src/lingxi/apps/scheduler/permission_readiness_assembly.py" = ["D"]
"src/lingxi/apps/scheduler/retention.py" = ["D"]
"src/lingxi/apps/scheduler/roster_audit.py" = ["D"]
"src/lingxi/apps/scheduler/stalled_provisioning.py" = ["D", "PLR0913"]
"src/lingxi/apps/trace/__init__.py" = ["D"]
"src/lingxi/apps/worker/__init__.py" = ["D"]
"src/lingxi/apps/worker/cli.py" = ["D"]
"src/lingxi/apps/worker/config.py" = ["D"]
"src/lingxi/apps/worker/report.py" = ["D", "PLR0913"]
"src/lingxi/apps/worker/report_extraction.py" = ["D"]
"src/lingxi/apps/worker/session_cleanup.py" = ["D"]
"src/lingxi/apps/worker/turn.py" = ["D"]
"src/lingxi/config/content.py" = ["D"]
"src/lingxi/core/admin/card_callback.py" = ["D", "PLR0913"]
"src/lingxi/core/admin/card_dispatch.py" = ["D", "PLR0913"]
"src/lingxi/core/admin/card_layout.py" = ["D"]
"src/lingxi/core/admin/commands.py" = ["D"]
"src/lingxi/core/admin/display_names.py" = ["D"]
"src/lingxi/core/admin/management_card.py" = ["D"]
"src/lingxi/core/admin/notification.py" = ["D", "N818"]
"src/lingxi/core/admin/pending_action.py" = ["D", "N818"]
"src/lingxi/core/admin/registry.py" = ["D", "N818"]
"src/lingxi/core/admin/views.py" = ["D"]
"src/lingxi/core/alerting.py" = ["D"]
"src/lingxi/core/conversation/commands.py" = ["D"]
"src/lingxi/core/conversation/onboarding_recovery.py" = ["D"]
"src/lingxi/core/conversation/ports.py" = ["D"]
"src/lingxi/core/conversation/session_window.py" = ["D"]
"src/lingxi/core/daily_report.py" = ["D"]
"src/lingxi/core/delivery/ports.py" = ["D"]
"src/lingxi/core/execution/audit.py" = ["D"]
"src/lingxi/core/execution/card_stream.py" = ["D", "PLR0913", "N818"]
"src/lingxi/core/execution/document_delivery.py" = ["D"]
"src/lingxi/core/execution/hooks.py" = ["D"]
"src/lingxi/core/execution/input_safety.py" = ["D"]
"src/lingxi/core/execution/message_stream.py" = ["D"]
"src/lingxi/core/execution/tool_policy.py" = ["D"]
"src/lingxi/core/identity/access_token_supply.py" = ["D", "N818"]
"src/lingxi/core/identity/credentials.py" = ["D", "N818"]
"src/lingxi/core/identity/first_contact.py" = ["D"]
"src/lingxi/core/identity/identifiers.py" = ["D"]
"src/lingxi/core/identity/innertest_roster_gate.py" = ["D"]
"src/lingxi/core/identity/legacy_permission_import.py" = ["D"]
"src/lingxi/core/identity/onboarding.py" = ["D"]
"src/lingxi/core/identity/onboarding_guards.py" = ["D"]
"src/lingxi/core/identity/onboarding_ports.py" = ["D"]
"src/lingxi/core/identity/onboarding_support.py" = ["D"]
"src/lingxi/core/identity/onboarding_terminal.py" = ["D", "N818"]
"src/lingxi/core/identity/org_snapshot.py" = ["D"]
"src/lingxi/core/identity/preprovision.py" = ["D"]
"src/lingxi/core/identity/provisioning.py" = ["D"]
"src/lingxi/core/identity/roster_audit.py" = ["D"]
"src/lingxi/core/identity/roster_report.py" = ["D"]
"src/lingxi/core/identity/roster_snapshot.py" = ["D"]
"src/lingxi/core/identity/stock_token_source.py" = ["D"]
"src/lingxi/core/ids.py" = ["D"]
"src/lingxi/core/innertest_content_capture.py" = ["D"]
"src/lingxi/core/permission/account_match.py" = ["D"]
"src/lingxi/core/permission/galaxy_export.py" = ["D"]
"src/lingxi/core/permission/galaxy_scope.py" = ["D"]
"src/lingxi/core/permission/legacy_diff.py" = ["D"]
"src/lingxi/core/permission/local_override.py" = ["D"]
"src/lingxi/core/permission/mcp_readiness.py" = ["D"]
"src/lingxi/core/permission/merge_sources.py" = ["D"]
"src/lingxi/core/permission/metric_translation.py" = ["D", "N818"]
"src/lingxi/core/permission/notification.py" = ["D"]
"src/lingxi/core/permission/position_override.py" = ["D"]
"src/lingxi/core/permission/publish.py" = ["D", "N818"]
"src/lingxi/core/permission/publish_row.py" = ["D"]
"src/lingxi/core/permission/role_function.py" = ["D"]
"src/lingxi/core/permission/table_access_token_supply.py" = ["D", "N818"]
"src/lingxi/core/permission/targeted_recompute.py" = ["D", "PLR0913"]
"src/lingxi/core/permission/tenant_token_supply.py" = ["D"]
"src/lingxi/core/user_memory.py" = ["D"]
"src/lingxi/core/year_grounding_guard.py" = ["D"]
"tests/**" = ["D"]
"tests/test_claude_agent_session_adapter.py" = ["PLR0913"]
"tests/test_document_delivery.py" = ["PLR0913"]
"tests/test_gateway_transport.py" = ["N818"]
"tests/test_management_card.py" = ["N802"]
"tests/test_metric_map_single_source.py" = ["N818"]
"tests/test_permission_publish_duty.py" = ["PLR0913"]
"tests/test_permission_publish_postgres.py" = ["N818"]
"tests/test_permission_publish_row.py" = ["N802"]
"tests/test_permission_refresh_duty.py" = ["PLR0913"]
"tests/test_roster_access_token_supply.py" = ["PLR0913", "N818"]
"tests/test_targeted_permission_recompute.py" = ["PLR0913"]
"tests/test_worker_entry.py" = ["PLR0913"]

# 六个贴线/冻结文件（结构性拆分之前的暂缓对象）暂不参与全仓格式化：它们各自
# 贴着文件体量棘轮阈值，ruff format 的重排（引号风格、换行）会改动行数，可能顶穿
# 阈值或让棘轮基线数字对不上。批 1 拆分完成后再移出本清单、纳入格式化。
[tool.ruff.format]
exclude = []
