[project]
name = "lingxi"
version = "0.1.0"
description = "Lingxi 企业内部受控 AI 业务分析服务"
requires-python = ">=3.12"

[project.optional-dependencies]
feishu = ["lark-oapi==1.7.1", "psycopg[binary]>=3.2,<4"]

[tool.unittest]
start-directory = "tests"
