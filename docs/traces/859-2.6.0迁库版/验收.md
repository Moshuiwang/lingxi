# 验收（逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace Issue 或 PR）

> 证据等级口径见《验证与门禁》；每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末。

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准（被判的值由谁写入） | 目标等级 | 现值 | 关联 `V-*` | 证据 |
| --- | --- | --- | --- | --- | --- |
| E-1 #815 / #856 代理自替换与 PAT 告警 | 假世界：替换成功 / 摘要不在核对链拒绝 / 写一半崩溃不落地 / 版本不高于在位不替换 / 开关关闭不替换（`Ran N`）；PAT 四态各一次（未到期零告警 / 窗内每日一条 / 到期 unknown+告警 / 换新后停止）；否定：告警正文零 PAT 值。**预发真机**：`ExecStart` 路径文件 sha256（系统读）= rc.B 控制包内代理 sha256；`.bak-*` 在；下一轮日志由新版本标记写出（代理写）；部署 `verified`（状态账写）；`gh.env.meta` 置窗内 → 下一轮告警（飞书回读）→ 还原 → 静默 | L3 + L4a | **L3 + L4a**（生产 L5 段：v2.5.3 新代理 `agent_version=2` 首轮 + `pat_expiry ok`） | V-部署-拉取代理（新行，≤ 800B） | 假世界 #859 [5748850713](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5748850713) / [5749384629](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5749384629)；真机 [5750432568](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5750432568)；生产 [5753379190](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5753379190) |
| E-2 #838 代理停摆告警 | 假世界三态 + 未知形态 + 脱敏否定；**预发真机**：`systemctl stop lingxi-release-pull.timer` → 下一轮健康巡检内管理群收到一条（含主机 / 环境 / 单元状态 / 最近一轮时间；无凭据、无路径、无日志原文）→ 恢复后不再重复（飞书回读 + 状态账） | L3 + L4a | **L3 + L4a** | V-监控（新行） | 假世界 5748850713；真机 [5750209523](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5750209523) |
| E-3 #839 依赖漏洞扫描 | `Epic Full` 出现独立作业；故意钉已知高危版本 → 判红并列编号（CI 写）→ 恢复 → 绿；豁免清单到期条目自动判红；工具装不上判「未知」失败；`main` 每周定时作业存在；`验证与门禁.md` 第五节回写；`gate` 上限 20 分（run 页可读） | L4 | **L4**（定时首触 2026-09-27 未验证） | V-门禁（新行） | [#839 评论 5749080019](https://github.com/Moshuiwang/lingxi/issues/839#issuecomment-5749080019)；#872 第二轮 OSV 502 判未知 fail-closed 为反向证据 |
| E-4 发布1 生产生效（v2.5.3） | 生产三容器 digest = 正式版清单（代理只读回读）；生产代理文件 sha256 = v2.5.3 控制包内代理（产品负责人脚本回读）；`host_health_alert.py` sha = 仓库同提交（同上）；`lingxi-release-pull.timer` active；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**（worker 终态审计写） | L5 | **L5**（2026-09-20 23:10Z 一次真实问数 `terminal_kind=success`） | 版本验收单 rc.B | [5753379190](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5753379190)；验收记录 `deploy/releases/acceptance/v2.5.3-rc.109.json` |
| E-5 #835 连接借用修复 | 六条时序真库用例（含归还前取得的游标）全过；`postgres.py` 说明无「尚未完全兑现」；调用点零改动计数（实施报告）；变异：去掉句柄失效检查 → 用例红（`Ran N`） | L3 | **L3**（真库 Ran 24 + Ran 9 OK；`尚未完全兑现` 0；调用点 195 / 4 = 199 零改动；实施者四处变异 + 审核者五处独立变异各红；随 #877 合入 `main`，CI Epic Full 绿 = L4 待 rc.C） | V-数据（既有行现值） | r7 台账 [5754245808](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5754245808)；r8 [5754457449](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5754457449)；PR #877 |
| E-6 发布2 生产切换（v2.6.0） | `verify` 零差异（脚本写：对象计数 / 行数 / sequence / 触发器 / 扩展 / alembic head 与来源逐项相等）；三服务 healthy 且 `SELECT inet_server_addr()` 为本机 / 容器网段（只读 SQL）；停写窗口实测 ≤ 30 分（脚本记时）；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**；预发同链路先过一遍（L4a） | L4a → L5 | **L6（生产，2026-09-22）**：verify 零差异（46/145/0/0/24/24、pg_trgm 1.6@extensions、retention_cleanup 属主 lingxi_retention_owner prosecdef=true）；三服务 healthy 且连本地库（pg_stat_activity 按容器地址 3 / 2 / 3，scheduler 视角 server_addr = 容器网段）；**用户可见停机 11 分 28 秒**（gateway FinishedAt 02:25:59.25Z → 健康首过 02:37:27.49Z；三服务全恢复 11 分 35 秒；分段人核 5:34 / Promotion 误填损耗 3:51 / 部署 2:03；≤ 30 分）；**产品负责人 Bot-Prod 真实问数 `terminal_kind=success`**（69 秒、含真实业务数据）；4a 三方全摘要一致、4l `verified` / `already_in_place` 幂等轮 / `alerts[verified:v2.6.0] sent=true`。预发 L4a：纯净窗口 1 分 51 秒（rc.111），首跑 rc.110 26:37 含接缝 F-W4-4 / F-W4-5（修法 = s30 v2.2 + runbook #881） | 版本验收单 rc.C；V-数据（新行） | 生产 [5770488610](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5770488610) / [5770560370](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5770560370) / 收口 [5770704207](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5770704207)；预发 [5756579636](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5756579636)；[5756757491](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5756757491)；首跑 5755746503 / 5755856060；工具链 #809 5754416342 |
| E-7 备份与恢复闭环（RPO 24h / RTO 4h） | 预发：定时器一轮成功 → 副本到达 biai-stage（sha 相等）→ 恢复到独立实例零差异 → 恢复时长实测（≤ 4h）→ `rollback-dsn` 演练切回；生产：观察 7 天每日定时器成功 + 副本到达 + 第 3 天从副本恢复一次零差异 + 零告警 | L4a（预发）/ L5（生产 7 天） | **L4a（预发）+ 生产第 0 天 L5（2026-09-22，7 天观察至 09-29）**：生产 s31 装入（`lingxi-db-backup.timer` 每日 18:30Z；首轮 1 s / 传输形一轮 2 s，46 表，`sha256sum -c` OK）；副本到达接收主机 sha 相等（8c5eb8c887e3…）、传输只搬新件；巡检本地库五项开（drop-in 42f3b16d… 推算值被实测确认，随后两轮 Result=success）；预发定时首轮 09-21 18:30:59Z 成功（15 s、scp 到 tz sha 相等）。预发 L4a：`run-once` 两轮成功；副本模式恢复到独立实例零差异 **≈ 7 秒**（副本完整性演练，**不构成 RTO 实测**——到可服务状态的完整演练见 #885，属 2.6.1）；`rollback-dsn` 首次真机 2 秒 + 三容器重建于 Supabase 35 秒 | V-数据（新行） | 生产 [5770560370](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5770560370) / [5770638971](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5770638971)；预发定时首轮 [5765574867](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5765574867)；[5755921686](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5755921686)；[5756579636](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5756579636)；工具链 r7 台账 5754245808 |
| E-8 #854 远程门禁 | 同一提交 tx-sg 与 tz `check.sh full` 结论一致（同绿或同失败签名）；回收用时 / `Ran N` / 内存峰值；配置样例指向第二台主机即可运行（说明或第二份样例）；开通清单无机器专属写死值；跑完无残留容器 | L3 | **L3 + 对拍** | V-门禁（新行） | [#854 评论 5748656478](https://github.com/Moshuiwang/lingxi/issues/854#issuecomment-5748656478) |
| E-9 #855 迁移链去平方 | V-迁移-01/02/03/05/06/08 全过、判定口径不变；故意假 `downgrade()` 仍判红（日志原文）；改前 / 改后同链用时或迁移执行次数对比（线性）；scratch 库清理不变 | L3 | **L3** | V-迁移-08（现值改写） | PR #863；r1 台账 [5748521636](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5748521636) |

## 批终链（全绿才进下一阶段）

tx-sg 串行完整门禁绿（T-1 前 tz）→ 唯一审核者批量审查（一次）→ 统一修复包 → 同一审核者定向复核（一次）→（发布前）外审两路 → 冻结候选 → CI 完整门禁 + `Epic Verdict` → 合 main → 同步 / 建分支 → 候选 → 预发部署回读 → 验收记录 → Promotion → 生产回读。冻结后回填：候选 A `0bf6426e` → A′ `6293dc51`（#865 合 `8e0946cb`）；候选 B `61ed6cd9` → B″ `50bd63ba`（#868 合 `16e73d2d`）；候选 C `ac3d7e58` → C′ `fbd9c9ec`（#877 合 `71aa3a44`）；制品 rc.A = `v2.5.3-rc.108`（清单 sha 见 Release）、rc.B = `v2.5.3-rc.109`（`ea80a579…`）、正式版 `v2.5.3`（`2da4cddb…`，控制包 `ce1f695c…`）、rc.C = `v2.6.0-rc.111`（清单 `0b18f203…`、控制包 `0a55e372…`、来源 `1034d0fa` = #882 合并提交、树 = `main@1d4cdff2`；正式版 `v2.6.0` = Promotion run 35679955480（02:35:24Z 发布，images / 控制包与 rc.111 逐字相同；前两次点击输入错被门禁拒）；rc.110（`4344767c…`）为首跑载体、预发 verified 后被 rc.111 替换；#881 运行手册修订 + #882 同步各 1 次云端门禁）；门禁 run：#865 `Epic Full` 见 PR、#868 35507922712 / Verdict 35508642893、#870 35509031125、#871 见 PR、#872 第三轮见 PR；tx-sg 串行 9 次（T-1 头 / 主干基线 712s / A 695s / A′ 691s / B 696s / B′ 703s / B″ 701s / C 710s / C′ 714s；上限 9，产品负责人预批 +2 仅限 W3、未动用、随 #877 合并失效）。

## 版本验收单 ×2（rc.B → v2.5.3；rc.C → v2.6.0）

固定项：① 四镜像 digest 与候选清单逐一相同；② 控制包 sha256 与清单一致；③ 三服务 healthy 15 分钟；④ 迁移头（0098 / 或更新）；⑤ 告警通道一次真实投递；⑥ 状态账 `verified`。随版本项：rc.B = E-1 / E-2 预发真机段；rc.C = E-6 / E-7 预发段 + 停写实测（rc.111 六项 + 1 分 51 秒：[5756757491](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5756757491)）；生产六项：① 4a 三方全摘要一致 ② 控制包 0a55e372… ③ 三服务 healthy（4l 15 分钟后 `verified`）④ 迁移头 0098 ⑤ `alerts[verified:v2.6.0] sent=true` + 巡检 `release_pull_timer_inactive` 告警 / 恢复各一次真实投递 ⑥ 状态账 `verified`（[5770560370](https://github.com/Moshuiwang/lingxi/issues/859#issuecomment-5770560370)）。

## 未验证层级（收口时如实列）

- 发布1 不含应用代码改动，L5 只做一次问数以证服务未受影响，不做功能验收。
- 生产代理自替换的「正向路径」在生产首次真实经过发生在发布2（rc.C 部署时），发布1 生产段只证幂等（相同 → 不替换）。
- 备份传输通道若走 tz 中转，tz 单点未纳入告警（生产实际走 scp 直达接收主机，不经 tz）。
- 生产代理自替换正向路径至今未触发：v2.5.3 与 v2.6.0 控制包内 `deploy/release_pull_agent.py` sha 相同（d6483293…），两次生产部署都走幂等路径；首次真正自替换发生在下一个改动代理的版本。
- 「从每日备份恢复到可服务状态」未演练、RTO 4 小时是合同 D-5 的承诺值而非实测（预发只测到副本 → 独立实例零差异 ≈ 7 秒）；完整演练 = #885（2.6.1）。
