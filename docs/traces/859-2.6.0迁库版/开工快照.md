# 开工快照：2.6.0 迁库版（Trace #859）

> 按 `#678` v23 §二与 `docs/traces/README.md`：开工能力、额度、环境快照与里程碑分诊表放本文件。**授权、验收门槛、已裁定事项、各道 Gate 一律留在 `合同.md`，不得移出以绕过批准。**

## 合同区字段

- 方法版本 **v23**（[#678](https://github.com/Moshuiwang/lingxi/issues/678)，2026-09-20 发布；上一版修订 Issue [#842](https://github.com/Moshuiwang/lingxi/issues/842) 随 v23 发布关闭 = 上一 Trace 方法候选已结清）；下一版修订 Issue **#857**；合同版本 **v1**；执行阶段 **Planning → 待批准**；整体交付结果 **Undetermined**。
- **运行面**：Claude Code **2.1.278**；执行机 tz（Ubuntu 24.04，Xeon E5-2609v3 @1.9GHz 6 核、15 GB）；**远程串行完整门禁主机 tx-sg**（Ubuntu 24.04.4、AMD EPYC 7K62 2 vCPU、1.9 GiB + 2 GiB swap、Docker 在位、python3.12 + venv 已装、`~/lingxi-gate-trial` 检出与 venv 已在；2026-09-20 04:11–04:23Z 实测 `check.sh full` 全绿 758s、`Ran 7459 tests in 557.839s`、峰值 848 MB；接入 = `tailscale ssh ubuntu@tx-sg`）。**本机时钟是 UTC**，面向产品负责人的时刻一律 UTC + 北京（+8）双标注且两个值都实读。本快照采样：**2026-09-20 05:18–05:3xZ｜北京 13:18–13:3x（周日）**。
- **基线提交与核对时间**：`origin/main` = **`24925aa4`**（05:2xZ 实读；主检出干净、无开放 PR、无其他工作树、`.claude/worktrees/` 空）；`origin/release/2.5` 头 = 正式版 `v2.5.2`（同步 PR #849 合并提交）；`release/2.6` **不存在**；`pyproject.version` = 2.5.2；最新正式版 `v2.5.2`、最新候选 `v2.5.2-rc.107`（rc 序号全局递增，下一枚 rc.108）；`CHANGELOG.md` 无 `[Unreleased]` 节。
- **生产现网（#843 收口留痕 09-18 + 本次未再回读）**：三容器 `v2.5.2` healthy；`lingxi-release-pull.timer` enabled；迁移头 0098；拉取代理为引导安装时的固定副本（**版本未知**，不自升级——#815）；`scripts/ops/host_health_alert.py` 装在宿主 `/opt/lingxi/scripts/`（不进控制包，靠人工复制）；生产库 = **Supabase 付费档（有自动备份、无 PITR）**，PostgreSQL 17.6，27 MB，主机余盘 27 GB / 可用内存 2.2 GB（#809 S1 表 09-16）。
- **预发现网（05:3xZ 只读回读）**：`lingxi-release-pull.timer` **inactive**；无运行容器（只停不删）；主机 2 vCPU / 3.8 GB（可用 2.7 GB）/ 余盘 **16 GB（61% 已用）**；Docker 25.0.14；`/usr/bin/python3.12` 3.12.14；`/opt/lingxi/{bin,control,deploy,monitoring,relay,release-pull,runtime-config,scripts}` 在位；预发库 = Supabase stage 项目（与生产不同库）。
- **规划者会话与停止点**：本会话 = 主检出 `~/project/lingxi`，tmux `TZ-server:1`（Fable 5.1）；三件套走 `plan/trace-859-2.6.0` 分支。停止点 = 合同 v1 合并；**随后转任守望者**（窗口更名 `859-plan`）——不实施、不持编排权与合并权。
- **当前顶层编排任务**：尚未拉起；合同合并后由守望者拉起 tmux `TZ-server:859-w1`（编排 opus max，`--permission-mode auto`，工作目录主检出）；批次 = T 工具轨（与 W1 并行）/ W1 发布1 代码批 / W2 发布1 预发 + 生产 / W3 发布2 代码批 / W4 发布2 预发演练 / W5 发布2 生产切换 / W6 观察与收口。
- **编排者接手入口与第一恢复点**：`/trace-kit:takeover` → 读本目录四份文件 + Trace Issue 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + Trace Issue 最新评论**。

## 里程碑分诊表（v23 §三；2026-09-20 实读 GitHub）

### 里程碑 2.6.0（#13）

| Issue | 事实 | 验收标准 | 标签 | 归属 | 写动作（合同批准后由编排者执行） |
| --- | --- | --- | --- | --- | --- |
| #809 迁库 | 已核：S1 表（09-16）+ 09-20 更正（付费档、动机成本 + 治理、PG 17、RPO 24h / RTO 4h / biai-stage 副本 / 30 分钟停写 + 7 天观察）；**未核**：`supabase_vault` 是否被应用依赖、来源库两项 idle 超时现值（W3 只读补） | 8 条（正文）；判定值 = 迁移脚本回读（对象计数 / 行数 / alembic head）+ 备份定时器状态账 + 真人问数 | 变更 / pre-ready / P2 ✓ | **留（发布2）** | W3 开工时正文顶部补裁定块、转 `Ready`、领取留言 |
| #835 连接借用修复 | 已核（09-18 裁定并入迁库 Trace） | 4 条可观察 + 时序真库用例 | 维护 / pre-ready / P2 ✓ | **留（发布2）** | 同上 |
| #815 代理自替换 | 已核 + 09-20 裁定（信任来源 = 复用摘要链）；**本次补一条**：控制包 `FILES` 含 `deploy/release_pull_agent.py`（`deploy/control_bundle.py:40`），代理不装自己 | 4 条 | 变更 / pre-ready / P2 ✓ | **留（发布1）** | 转 `Ready`、领取留言 |
| #838 代理停摆告警 | 已核；`host_health_alert.py` 不进控制包、人工复制到宿主（`deploy/监控告警.md` §三） | 5 条 | 变更 / pre-ready / P2 ✓ | **留（发布1）** | 同上 |
| #856 PAT 到期告警 | 09-20 建单 | 5 条 | 变更 / pre-ready / P2 ✓ | **留（发布1）** | 同上 |
| #839 pip-audit 进 CI | 已核（09-18 裁定做进 CI，默认阈值） | 5 条 | 变更 / pre-ready / P2 | **提前并入（发布1）**；挂里程碑 2.6.0 | 挂里程碑、转 `Ready` |
| #854 远程门禁主机 | 09-20 建单 + tx-sg 实测 | 5 条 | 维护 / pre-ready / P2 ✓ | **留（T 工具轨，不绑发布）** | 转 `Ready` |
| #855 迁移链去平方 | 09-20 建单 | 5 条 + 变异验红 | 维护 / pre-ready / P2 ✓ | **留（T 工具轨）** | 转 `Ready` |

### 反向扫描

| Issue | 状态 | 判定 | 理由 |
| --- | --- | --- | --- |
| #803 凭据轮换台账 | 无里程碑 / 长期参考 + ops / P1 | 不动（#856 承接其 PAT 告警项） | 长期待办；`长期参考` 与 P1 并存的标签冲突留产品负责人一字（本 Trace 不改） |
| #814 部署链改道四条 | 无里程碑 / pre-ready / P2 | 不动 | 登记不排期 |
| #810 MCP v2 | 3.0.0 / P1 | 不动 | 上游阻塞 |
| #712 分片提速 | 已关（not planned） | — | 09-20 裁定 |
| #842 / #678 / #857 | [template] | 不适用 | 方法容器 |
| （新建）方法 v23 仓库侧同步项 C9–C10 | 建单后挂 2.6.0 / pre-ready | **并入 T 工具轨 docs PR** | docs-only，秒级路由 |

无 P0 / P1 无里程碑的工作项（#803 属 `长期参考`）。

## 开工能力与额度快照（v23 §三；2026-09-20 05:18–05:25Z 实读或实机测试）

| 项 | 值 | 怎么查 |
| --- | --- | --- |
| Claude Code | **2.1.278**；本规划会话 Fable 5.1（产品负责人 09-20 设为新会话默认）；编排者用 opus max（见 `合同.md` §5） | `claude --version` |
| Claude 额度（05:18Z / 13:18 北京；**已用**） | 5 小时窗 **5%**（06:30Z 重置）；**7 天窗 31%**（**09-23 23:00Z = 09-24 07:00 北京**重置）；Fable **29%**（同一重置）。结论：不构成开工门槛 | 主：`~/.claude/tmux-claude-usage-api.dat`（时刻 5h% 5h重置 7d% 7d重置 Fable% Fable重置 + 燃速四列）；备：`~/.claude/tmux-claude-usage.dat` |
| 外审一：codex **0.155.1** | `gpt-6-astra` high；**实机测试 05:24:25Z：13 秒、rc 0、stdout「就绪 gpt-6-astra high」** | `printf '%s' "<提示>" \| codex exec -s read-only -c model="gpt-6-astra" -c model_reasoning_effort="high" -`；判成功只认 stdout 非空 |
| codex 额度（05:18Z） | 7 天窗**已用 16%**（09-26 11:10Z 重置） | `~/.local/state/tmux-codex-quota/tmux-codex-usage.dat` |
| 外审二：agy **1.2.6** | `gemini-3.8-flash-high`；**实机测试 05:24:38Z：8 秒、rc 0、stdout「就绪 gemini-3.8-flash-high」** | `agy --model gemini-3.8-flash-high --print-timeout 600s --print='<提示>'`（提示首行「不要调用任何工具」） |
| agy 额度（05:25Z） | Gemini 周剩 **98%**（09-23 06:33Z 重置）、五小时 100%；Claude / GPT 系 100% | `agy --print='/usage'` |
| 外审频率 | 三家充裕 → 每次发布前 codex + agy 各一轮 = 预估 4 / 上限 6；降档：任一家周窗 ≥ 80% 或五小时 ≥ 90% → 只留 codex 一轮 | 每次登记时间 / 模型 / 候选 SHA / 读入字节 / 结果条数 |
| 完整门禁 | T-1（#854）合入前：tz `check.sh full` 约 26 分独占；合入后：**tx-sg 串行约 13 分**（05:1xZ 实测 758s），tz 空出给实施并行 | #854 正文 |
| CI | `gate` 10–12 分 / **15 分上限（余量 < 两成，C11a 拟上限 20 分）**；`image` 4–5 分；纯 docs 约 13 秒；`FULL_PREFIXES = .github/workflows/ deploy/ migrations/ scripts/ci/` + `Dockerfile / pyproject.toml / alembic.ini` 走完整门禁；`scripts/dev/`、`scripts/ops/`、`docs/` 不在完整前缀 | `scripts/ci/classify_story_changes.py:16-17` |

## 开工能力与环境快照（其余）

| 项 | 值 | 来源 |
| --- | --- | --- |
| 发布路径 | `publish.yml` 触发于推送 `release/**`；`release.yml` 手动（机器人 403，产品负责人网页点）；验收记录 `deploy/releases/acceptance/<候选标签>.json`（CODEOWNERS，产品负责人批）；同步 PR 第二次起先并 `release/X.Y` 现头；`release/2.6` 建自带 Epic Full 证明的 `main` 头（机器人尝试 `git-claude-tz push origin <sha>:refs/heads/release/2.6`，被拒则产品负责人建） | `deploy/releases/README.md`；#754 / #812 实测 |
| 机器人权限边界 | `gh run rerun` / `workflow run` 403；`accept/ record/ trace/ task/ sync/ plan/` 前缀可推；`push --delete` 被拦；`docs/traces/**/合同.md`、`deploy/releases/acceptance/`、裁决层三文件受 CODEOWNERS | `.github/CODEOWNERS`；#812 实测 |
| 主机侧组件交付方式（本 Trace 关键事实） | 控制包 `FILES` 含 `deploy/release_pull_agent.py` 与两只 systemd 单元，但代理**不装自己**（#815）；`scripts/ops/host_health_alert.py` **不在控制包**，靠 `git`/`scp` 复制到宿主 + systemd 单元（`deploy/监控告警.md` §三）。因此 **发布1 的宿主文件（新代理 + 新监控脚本）须各主机人工装一次**：预发由编排者经白名单路径脚本装；生产由产品负责人 `!` 亲跑同一脚本 | `deploy/control_bundle.py:28-44`；`deploy/监控告警.md:56` |
| 本机 allow 规则 | `Bash(ssh biai-stage *)`、`Bash(scp * biai-stage:/tmp/lingxi-s26/*)`、`Bash(ssh biai-stage sudo -n bash /tmp/lingxi-s26/*)`（预发持久化动作走此路径）；`Bash(ssh biplus-prod 'sudo -u bi-ai-deploy *)`（生产只读）；`tailscale ssh ubuntu@tx-sg` 本次自动模式放行（无显式规则） | `.claude/settings.local.json` 实读 |
| 预发脚本形态 | 幂等脚本 + 逐步回读 + 回退子命令，先在本机假根目录按目标机事实跑一遍；投放 `/tmp/lingxi-s26/`（sha 两端相等）；被分类器拦下即回退为产品负责人 `!` 亲跑 | #843 S-1-6 / G-1 实测 |
| 预发验收读数 | 预发库 DSN 在 `/etc/lingxi/.env.stage.*`（root 0600、值带单引号，psql 前去引号并 `2>/dev/null`）；任务表叫 `task` | 记忆 `stage-env-dsn-quoting` |
| 生产 | 代理只读形态 `ssh biplus-prod 'sudo -u bi-ai-deploy …'` 扁平单条；生产写动作一律产品负责人本人（守望者会话 `!`）；`pg_dump` 姿势沿 #812（`prod_pre_apply_pg_dump.py` → `/root/lingxi-backups/`，镜像钉 `postgres@sha256:67f41722…` PG 17） | #812 / #843 收口 |
| 备份传输 prod → stage | **未知**：两主机均在 tailnet（biplus 100.92.75.80 / biai 100.67.131.20）且 09-20 已启用 Tailscale SSH（产品负责人账号、非 root），节点间 ACL 未测；W3 P-F 先只读探测，不通则 tz 中转 | 09-20 产品负责人转贴 |
| tmux / 工作树 | `TZ-server` 常驻，仅窗口 1（本会话）；无其他 Claude 进程；主检出无其他工作树 | 本机现场 |
| 分类器边界 | 生产主机判敏感：写命令须产品负责人亲加 allow 规则、一次一条扁平命令；`ssh` + 本地重定向被拦；`push --delete` 被拦；预发持久化不在白名单路径会被拦 | 记忆 `prod-access-posture` |

## 批后继任条件（命中）

六个批次窗口可能跨会话接力（收口点当任上下文 ≥ 50% 默认换人，低于可续任；W6 观察期 7 天不需常驻编排者，由守望者定时型机制巡检、到期拉起 W6 收口编排者）。继任：读本目录四份文件 + Trace Issue 最新交接评论 → 核实现场（工作树、批次分支 tip、PR 状态、预发 / 生产 / tx-sg 现状、额度余量）→ 接管登记 → 前任确认停止写入并释放租约 → 退场。**未确认转移前保持等待，不得出现两个写入者。**守望者失效时（窗口关闭即两类机制全失效）由产品负责人在主检出重新拉起守望者会话，接手 prompt 见 Trace Issue 最新交接评论。
