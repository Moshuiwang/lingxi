# 开工合同:rc23 清仓批(Trace [#502](https://github.com/Moshuiwang/lingxi/issues/502))

> 方法版本:#147 **v16**(2026-08-31 发布,首批按正文执行)。合同版本:**v2**(Codex 执行适配修订)。v2 只改执行适配器、角色模型与接力方式,不改既定目标、范围、依赖、验收、权限边界、产品裁定或预算;本 PR 合并即产品负责人批准。批准依据=PM 2026-08-31 指令「下一批八卡开始做起来吧」以及本次明确的 Codex/模型/审核改造指令。
> v1 的 Claude/tmux 路由由 v2 替代;已有提交的作者与历史不改写,v2 生效后的未完成实施、复修和审核按新角色执行。
> 规划者=本 Codex 规划任务;停止点=合同 v2 合并、#502 回写和新编排任务完成接管登记,随后本任务退场,不参与实施或审核。

> **#492/#495 最新 PM 口径(2026-09-01)**:[#502 PM 评论](https://github.com/Moshuiwang/lingxi/issues/502#issuecomment-5488293484) 已将 #492 收窄为管理员↔Bot-Test **p2p** 私聊邮箱参数的 mailto 适配：真实 raw fixture 或群 GET 不再是实现前置,先完成自动化边界矩阵、mutation 验红与本机门禁,候选冻结并部署后再做一次 PM 管理员 p2p L4a。用户名/邮箱在本 Trace 验证中按非敏感处理;token、凭据、授权码的秘密边界不变。

## 1. 目标

- **用户结果一句话**:清仓现存全部可做卡,使 prod 放行的技术前置只剩 #263 硬切与放行动作本身——管理员输邮箱不再被 mailto 坑(#492)、管理卡按「职位+范围」一次改到位(#493)、生产不再有静默写满/黑盒失败/权限窗口三颗雷(#494/#495/#483)、下游容量有数字有降级(#496)、docx 表格暴露面有答案(#499)、配置改动各归其档(#498)。
- **里程碑**:9 张输入卡各按其 issue 可观察完成标准关闭(或按早退条款关闭);main 合入且 stage 升级回读绿;逐卡自证 L4a 通过。
- **完成定义(Delivered)**:9 卡关闭各留对账(#497 凭 PM 数据裁定关闭或明确转开放);#411 条件步执行或明确顺延;批次收口评论落 #502;环境盘点零残留。**prod 放行不在本 Trace**。

## 2. 授权终点

- **可自动执行**(沿 #469 口径):合并批次 PR、部署 biai-stage、关闭 issue、Bot-Test 真实收发与受控注入 L4a、文档回写(含 #493 文档术语重写、#499 当前能力回写、#498 门禁文档)、stage sudo 既有形态、Workflow 扇出。
- **#411 例外条款**:观察期 ≥09-01 且 PM 回「无异议」后,按 #411 登记执行清理(删本机旧库容器+dump 属不可逆,凭该单字授权执行并回帖盘点)。
- **显式除外**:biplus-prod / Bot-Prod / 生产实例任何写入;prod 放行;#263 本体;权限白名单修改;删除用户数据(#411 例外除外);指标后端(外部系统)任何改动——#496 需对方调整时走 PM 转发。
- **常设授权引用**:当前 Codex Desktop remote 的 Git push 凭据、gh 机器身份、bootstrap PR 先例、P2 豁免、stage 全权限常设授权(PM 2026-08-30,prod 仍除外);不新增权限范围。
- **中途裁定**:攒两单(见执行图 PM◇流),单字有效留痕 #502;临时授权按 v16 §6.7 带失效时点。

## 3. 不做清单

- 生产动作全部不做;#263 不做;凭据轮换不做;指标后端不动。
- #493 不新增/不调整管理员角色、不做映射只读界面;#496 不无限重试;#495 不落无脱敏异常全文;#499 不做完整表格转换、不回退 #467;#498 不把权限配置放轻通道、不涵盖管理员增减、L2 提示词档挂起(PM 已裁)。
- 新发现默认只登记不立项(主线灯塔:不做它,9 卡能不能关);疑问句只答不动手。

## 4. 验收标准

- **每卡关卡**=其 issue 可观察完成标准(全部自带自证闭环条款),细则见[验收.md](验收.md);#499 早退条款(markdown 列从不填充→读码结论落卡+回写当前能力即关)与 #497 数据门照卡执行。
- **批终链**:本机 full 绿 → `sol/xhigh` 独立审核 subagent 批量审查无 P0/P1 遗留 → 统一修复包 → 冻结 trailer → Epic Full 绿 → 合 main → Main Publish → stage 升级(`--profile mvp up -d`)三容器回读 → 逐卡 L4a。
- 证据等级:关键路径 L6(受控测试身份非生产);双通道纪律沿用;请 PM 真实验证前先出示就绪自检;变异验红按 v16 §6.6 硬姿势(`python -B` 或逐次清 `__pycache__`)。

## 5. 成本预算(PM 2026-08-31 裁定框架)

- **模型与角色(PM 2026-08-31 本次改造)**:编排者 **`gpt-5.6-sol`/`max`**;所有 v2 生效后新派发及复修的实施者均为 **`gpt-5.6-luna`/`max` subagent**;唯一审核者为独立 **`gpt-5.6-sol`/`xhigh` subagent**(不参与实现、不直接修复);**无外部审核**。
- Epic Full 主动消费 **≤3**(建议值);时间 **≤2 天含跨夜**;**夜间全速推进**(PM 裁定;v16 护栏全程有效:外部证据核账、临时授权失效时点、模型偏离即异常信号)。
- Workflow 扇出开跑前查配额余量。

## 6. 主线声明与批次纪律

- **主线**:9 卡关闭。单批 `epic/rc23`(bootstrap PR 先刷 Story Fast)→ 新 Codex 编排任务按执行图并行派发实施 subagent(独立 worktree)→ 串行合并 → 批终链 → 收口。
- **单一写入者**:编排者 Codex 任务持有主工作树与合并权;实施 subagent 使用独立 worktree;审核 subagent 对固定候选独立只读核对;commit 先行再汇报;子代理等待由编排者任务持续观察;派发卡含 v16 §6.4 六条款。
- 收尾必含:环境盘点(tz+stage 零残留)、回写、收口评论(UTC+北京、预算消耗、Epic Full 次数/commit 数)、Trace 终判。

---

# 执行图(节点=Step;边四类:`──►`硬依赖 / `(域)`合并次序 / `⚡`独占 stage 注入通道 / `◇`PM 裁定门)

```
━━ Wave 0:四节点零依赖,批准即全并行 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  W0-1 #499读码       W0-2 #492自动化边界   W0-3 #493探针⚡+映射核实   W0-4 #496后端同源
  (markdown列填充?)   (parser矩阵+mutation) (表单必填?message_id?)    (stage/prod同一套?)
      │「从不填充」→#499直接关卡            │                       ▼
      │否则→◇PM:降级vs失败                 │                  E1 #496容量实验⚡
      ▼                   ▼               │                  (1/2/4/6/8递增)
                      (S-4就绪)           │                       ▼
                                          │                  ◇PM:并发上限取值
━━ Wave 1:四卡独立worktree全并行 ━━━━━━━━━│━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  S-1 #495       S-2 #494        S-3 #483        S-4 #492
  失败签名        /tmp三修+规格    TOCTOU实施       mailto解析(合成边界+mutation)
    │(worker域先合) │(deploy域独立)  │(权限域先合)     │(卡面先合)
    │              │               ├──────────────►├──► S-7 #493 大改版
    │              │               │      (W0-3 也是其硬依赖)
    └──────────────┴─ S-1+S-2+S-3 合入后 ──► S-8 #498 分级门禁(PM已裁殿后)
  S-5 #496降级实施(等E1+◇;worker域S-1后合)──┐  S-6 #499实施(等◇,或已关卡)──┐
━━ 批终全串行 ━━━━━━━━━━━━━━━━━━━━━━━━━━━┷━━━━━━━━━━━━━━━━━━━━━━━━━━┷━
  串行合并队列(content.toml四写入者#492/#493/#496/#499:后合者续升)
   → sol/xhigh审核 subagent → 统一修复包 → 冻结trailer → Epic Full → 合main
   → Publish → stage升级 → 逐卡L4a⚡ ────────────────────────┐
  S-9 #497(◇PM:CloudWatch数据,零依赖随到随裁) ────────────────┤
  S-11 #411(时间闸≥09-01 + ◇PM:无异议→清旧库容器+dump回帖) ───┤
                                                            ▼
                                                      S-12 收口终判
```

**车道视图**:

```
worker道 │ S-1 #495 ━━━━┫ S-5 #496实施 ━━━━━━┓
deploy道 │ S-2 #494 ━━━━━━━━━━━━━━━━━━━━━━━━┫
权限道   │ S-3 #483 ━━━━┫ S-7 #493 ━━━━━━━━━━┫ 串行合并→审查→修复包→冻结
卡面道   │ S-4 #492 ━━━━┛(汇入S-7)           ┣ →EpicFull→main→stage→L4a→收口
CI道     │              S-8 #498 ━━━━━━━━━━━┛
docx道   │ S-6 #499 ━(可能W0-1后即关卡)━━━━━━┛
stage⚡  │ [W0-3探针]→[E1容量实验]→──────────────────────────[L4a legs]
PM◇流   │ 攒单①(Wave0末):后端同源+并发上限+#499语义 │ 攒单②(批终):#494prod规格+#497数据+#411无异议
```

要点:①Wave 1 四卡零互依全并行;②S-7 #493 唯一三入度节点(W0-3/S-3/S-4),与批终链构成关键路径;③stage 注入通道三段错峰;④PM 裁定攒两单。

---

# 合同区(v16 必填字段)

- 方法版本:v16;合同版本:v2;执行阶段:In Progress(本次 Codex 接管合同合并后继续);整体交付结果:Undetermined。
- 基线:main=`107c87a`(2026-08-31 核对);stage 三容器 healthy on `20260831-3048d265cad1`(Supabase 测试实例);迁移链头 `0079`;**#493 存量逐指标覆盖数据 PM 已裁放弃不迁移**(如需迁移/新表看 `check_alembic_revisions`)。
- `execution_adapter`:`codex_fresh_top_level_task`(v16 形态)——每次编排接力新建一个 Codex 顶层 task/thread;当前为 Desktop App remote,**不使用 tmux**, task/thread ID 写入 #502 交接评论。
- 规划者会话与停止点:本 Codex 规划任务;合同 v2 合并、#502 回写、新编排任务完成接管登记后停止,不代替编排者实施或审核。
- 角色模型(PM 2026-08-31 本次改造):编排者 **`gpt-5.6-sol`/`max`**;实施者池 **`gpt-5.6-luna`/`max` subagent**;唯一独立审核者 **`gpt-5.6-sol`/`xhigh` subagent**(批终一次,不参与实现不直接修复);无外部审核。
- 编排者接手入口:新建 Codex 顶层 task → 读本三件套+#502 最新交接评论 → 接管登记(任务 ID/模型/授权终点)→ 合并权原子转移。第一恢复点=任务表勾选+#502 最新评论。批后继任:本批为最终批可收口退场;PM 追加则按 v16 §6.8。

## 输入 Issue 准入与关闭资格(可达性审计 A)

| 输入 | 完成标准要点 | 当前事实 | Step | 收尾去向 |
| --- | --- | --- | --- | --- |
| #495 | 兜底分支落类型+失败码,零泄露锁测;任何失败型/非成功终态 `failure_code` 非 null,成功保持 null | turn.py:682 读码坐实;P2-5 脱敏先例可循 | S-1 | 关闭 |
| #494 | 回收收敛+分环境容量+健康探针真红+资源限制,各变异验红 | S-12 实测坐实;因果判定材料在 #478;prod 规格=◇ | S-2 | 关闭 |
| #483 | 批快照窗口内停用不被回发;真库并发实测 | 设计稿+缺口②③已坐实,PM 事实确认毕 | S-3 | 关闭 |
| #492 | 管理员 p2p 邮箱 mailto 适配:合成形态夹具(如实非 raw)+全邮箱入口覆盖+不误伤+mutation 验红+文案改进 | 不以真实 raw fixture 或群 GET 为实现前置;自动化/本机门禁先行,候选冻结并部署后 PM p2p L4a | W0-2,S-4 | 关闭 |
| #496 | 容量曲线落卡+上限成显式配置+降级有用例 | 仅 2 样本 502,上限未知;同源确认=W0-4 | W0-4,E1,S-5 | 关闭 |
| #499 | 第 2 问读码结论;若填充→按◇语义实施 | 早退条款明确;stage 原显式置开无影响 | W0-1,S-6 | 关闭(或早退关闭) |
| #493 | 职位+范围表单/懒过期/按钮态/下发如实化/文档重写 | 各节「已定」齐;前提=W0-3 两项;屏蔽取消已裁 | W0-3,S-7 | 关闭 |
| #498 | 三档分级+防伪装变异验红+权限影响面 diff | PM 已裁分歧与范围;classify 脚本骨架已在 | S-8 | 关闭 |
| #497 | PM CloudWatch 数据到→按数据定去向 | 调查已完,纯 PM 输入门 | S-9 | 关闭或明确开放 |
| #411 | 观察期满+PM 无异议→清理回帖 | 09-01 起届满;旧库零连接已证 | S-11 | 关闭或顺延留痕 |
| #263 | —— | PM 裁定全部完成后放行 | 不纳入 | 保持开放 |

## 共享资源租约

| 资源 | Owner | 规则 |
| --- | --- | --- |
| tz 主工作树 | 编排者 | 唯一写入者;实施代理独立 worktree 用完即清 |
| 合并权 | 当任编排者 | 接管登记原子转移;元守护不持有 |
| stage 注入通道⚡ | 编排者调度 | 单客户端;W0-3 探针→E1 容量实验→L4a 三段错峰;探针/测试卡用后即删 |
| content.toml | 四写入卡 | 各自升版+lock 自绿;合并队列后合者续升 |
| Codex 顶层编排 task/thread | 当任编排者 | Desktop App remote 每批新建;task/thread ID 写 #502;交接后旧任务退场 |

## 现场事实与已知坑(编排者必读)

- 沿 #469 合同「现场事实与已知坑」全部仍适用条目+tz 硬事实(memory 2026-08-30/08-31 段:`--env-file` 引号、`--profile mvp`、`fix:#NNN` 自动关卡、敏感 env 只列变量名、form name 必填、变异 `python -B`、comments 端点排序坑);Codex 模型/推理档位由新顶层 task 配置,不使用 Claude `/effort` 或 tmux 接力。
- #492/#493/#496/#499 均触 content.toml;#483 与 #493 同触权限覆盖模型(S-3 先合);#493 的 message_id 映射持久化是实打实前置工作量(rc22 P1-2 内存 TTL 表,重启即丢——实施前自行核实)。
- #494 判定试验(清 /tmp 重跑)结果在 #478/#494 留痕,「写满→session_failed」在拿到判定前只是时间相关。
- #498 动 scripts/ci 会被分类器提级,搭批终车合并省 Epic Full。
- 凭据纪律零回显;停止条件与 amendment 按 v16(元守护可代持规划者修订,走三件套 PR 重批)。
