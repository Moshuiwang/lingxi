# 验收(逐卡关卡=issue 可观察完成标准,全部自带自证闭环;证据等级目标 L6,受控测试身份非生产)

> **#492/#495 最新 PM 口径(2026-09-01)**:[#502 PM 评论](https://github.com/Moshuiwang/lingxi/issues/502#issuecomment-5488293484) 将 #492 收窄为管理员↔Bot-Test p2p 私聊邮箱参数的 mailto 适配;真实 raw fixture/群 GET 不再是实现前置,自动化+mutation+本机门禁先行,候选冻结并部署后 PM p2p L4a。用户名/邮箱在本 Trace 验证中非敏感,token/凭据/授权码秘密边界不变。

- **#495**:多种底层异常各留可区分类型+失败码(变异验红);泄露锁测(ou_/lpo_/pac_/邮箱零出现,沿 P2-5 真实样式);任何失败型/非成功终态 `failure_code` 非 null,成功终态保持 null;/admin trace 可读;既有归因分支回归绿。
- **#494**:持续问数流量下转录占用有界收敛(变异验红);/tmp 上限分环境显式配置+check_deploy_contract 断言;**人为写满时健康检查如实变红(核心验收项,变异验红)**;资源限制按各环境实际规格钉死;stage 受控双验证。
- **#483**:并发窗口构造(批快照后停用+force_revoke)断言不被旧基线回发;缺口②(停用用户本地权限动作)按设计稿语义;真库并发实测;停用即时撤销与恢复重纳入回归绿。
- **#492**:管理员 p2p 邮箱参数仅适配裸邮箱、裸 `mailto:<email>` 与显示/目标一致的 `[显示邮箱](mailto:邮箱)`;合成形态夹具(如实标注非 raw)+全邮箱入口覆盖+mutation 验红;纯文本/非邮箱@/真未知命令行为不变;解析失败文案说清哪段没懂;候选冻结并部署后 PM p2p L4a。
- **#496**:并发→成功率曲线落卡(含同源标注);并发上限经◇裁定成显式配置+断言;降级用例(502→区分文案+零重复交付,变异验红);外因日志可归因回归。
- **#499**:第 2 问读码结论落卡;早退则回写当前能力即关;否则按◇语义实施+用例钉死+变异验红+stage 真实建档含表格回读;prod 就绪检查项入 runbook。
- **#493**:表单=补充授权(职位+必选范围含「全部」显数字)单方向;服务端缺范围拒绝;懒过期(数据变即拦+24h 硬上限)用例;按钮态状态机;下发两段式文案+每日批补齐群汇总;卡面/文档零退役术语;真人体验=PM 加菜。
- **#498**:分类器三档判据+防伪装变异验红(权限配置伪装成文案路径必须仍判 L3);L1 通道实测更快+术语扫描挡退役词;影响面 diff 输出对账(扩权/缩权分列,零内部 ID 锁测);路径伪装/quotePath 绕过覆盖。

  L3 非空影响面的计数只接受 trusted-base reader 产出的
  `lingxi.permission-impact-provenance/v2`：reader 用官方 REST API 重 GET 当前 PR
  与普通评论，信任根固定为 `Moshuiwang`（id=`200755707`、node_id=`U_kgDOC_dJ-w`、
  `type=User`、`author_association=OWNER`），且 PR 作者不得是该用户。OWNER 评论正文必须是
  无 Markdown 包装、无未知字段或重复键的严格
  `lingxi.permission-impact-attestation/v1` JSON，绑定 repo id/name、PR number、精确
  base/head SHA、manifest/facts/grant/shrink 摘要与 counts、exporter commit+blob 或
  image digest、query version、captured/issued/expires 和随机 nonce；created_at 必须等于
  updated_at、评论未 minimized，且严格满足 `issued_at <= created_at <= now`、TTL 不超过
  15 分钟。普通评论按 `id` 去重；PR files 按官方 REST 的 `filename`、可空
  `previous_filename`、40 位 `sha`、受限 `status` 去重，不假定 files item 有 `id`；PR
  response 的 `changed_files` 必须是非 bool 的 0..3000 整数，且与完整分页枚举数相等，
  截断/3001/重复/错误 shape/API/JSON/时间/缺失均 fail closed。provenance 与独立证据
  artifact 保存已验证的 exact comment body、comment URL/id、GitHub user id、run id/SHA、
  正文及 API response digest，不保存 token 或用户明细；另有由这些不可变字段计算的
  `challenge_sha256` 绑定本次 trusted Actions transcript。无新增权限、写操作或外部
  ledger 时，它是可回读的 per-run challenge，不虚称跨次运行的全局一次性；同一 nonce/
  comment 在未来重跑仍需由待定的外部 replay ledger 或规则裁定解决。消费者和 workflow
  从 trusted base 执行。

  trusted stage 生成正文的命令路径固定为：
  `python3 scripts/ops/render_permission_impact_owner_attestation.py --manifest
  .github/permission-impact-counts.json --pr-number <n> --base-sha <base> --head-sha
  <head> --exporter-commit <commit> --exporter-blob <blob> --output
  /tmp/permission-impact-owner-attestation.json`；输出是单个 raw JSON 文件，再由 OWNER
  作为未编辑普通评论发布。L3 PR 与 evidence-only PR 分模式；两者只能改预先限定的
  两份 facts、claim、Trace 合同/验收说明和列明的 tests。evidence-only PR 还必须带
  `.github/permission-impact-evidence-only` sentinel（exact content
  `lingxi.permission-impact-evidence-only/v1`），trusted workflow 硬失败阻止合并/部署，
  Main Publish 的本地 verifier 也拒绝带 sentinel 的树；这只是仓库内 fail-closed sentinel，
  不是冒充 GitHub ruleset。

  2026-09-01 只读回读 `main` 当前 ruleset：`20559295`「main：仅 Epic Full 后合并」为
  active，required check 是固定 GitHub Actions integration `15368` 的 `Epic Full`；
  `20559403`「epic/**：仅 Story Fast 后合并」同样 active，required check 是 `Story Fast`。
  未执行任何 ruleset mutation；拟由 PM 批准且具 repo administration write 的管理员先
  保存 `GET /repos/Moshuiwang/lingxi/rulesets/20559295` 原文，再以 `PUT` 保留现有
  deletion/non-fast-forward/PR 规则，仅把 required status checks 增加
  `{context: "Permission Impact / trusted base", integration_id: 15368}`，随后 GET
  回读并核对前后集合。可执行的最小 CLI 草案（仅待授权管理员运行）是：

  ```bash
  gh api repos/Moshuiwang/lingxi/rulesets/20559295 > before.json
  jq '
    .rules |= map(
      if .type == "required_status_checks" then
        .parameters.required_status_checks |= (
          if any(.[]; .context == "Permission Impact / trusted base" and .integration_id == 15368)
          then .
          else . + [{"context":"Permission Impact / trusted base","integration_id":15368}]
          end
        )
      else .
      end
    )
    | {name,target,enforcement,conditions,rules}
  ' before.json > update.json
  gh api --method PUT repos/Moshuiwang/lingxi/rulesets/20559295 --input update.json
  gh api repos/Moshuiwang/lingxi/rulesets/20559295 > after.json
  diff -u <(jq '{name,target,enforcement,conditions,rules}' before.json) \
          <(jq '{name,target,enforcement,conditions,rules}' after.json)
  jq '{name,target,enforcement,conditions,rules}' before.json > rollback.json
  # 仅在需要回滚时执行：
  gh api --method PUT repos/Moshuiwang/lingxi/rulesets/20559295 --input rollback.json
  gh api repos/Moshuiwang/lingxi/rulesets/20559295 > rollback-after.json
  ```

  上述 `PUT` 与回滚都必须以 GET readback 对账；当前 token 对 branch protection GET
  已返回 403，说明实际变更至少需要 repo admin/PM 显式授权。draft 测试 PR 只允许使用
  上述 sentinel，验证该 check 失败且 publish verifier 拒绝；测试后关闭 PR/删除 sentinel，
  均不在本 Trace 自动执行。
- **#497**:PM 数据到→结论归档落卡,去向明确(关闭或转优化卡)。
- **#411**:清理后回帖盘点(容器+dump 去除、回滚路径失效声明);未获无异议则顺延留痕。

## 批终链(全绿才收口)

本机 full 绿 → `sol/xhigh` 独立审核 subagent 批量审查无 P0/P1(变异实测全程 `python -B` 或逐次清缓存)→ 冻结 `Image-Candidate: true` → Epic Full 绿 → 合 main → Main Publish → stage 三容器 healthy 回读 → 逐卡 L4a(双通道:服务端结构化日志+平台 API 回读)。

## 留证

逐卡证据落各卡对账评论;收口评论落 #502(UTC+北京、Epic Full 次数/commit 数、预算消耗、残留盘点)。
