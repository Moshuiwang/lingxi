# 任务表(状态即文件当前值,编排者随执行 commit 更新)

> v2 执行路由:新建 Codex 顶层编排 task/thread(Desktop App remote 不使用 tmux);v2 生效后的实施与复修全部派给 `gpt-5.6-luna/max` subagent;批终唯一审核派给独立 `gpt-5.6-sol/xhigh` subagent;无外部审核。既有提交历史不改写。

> **#492/#495 最新 PM 口径(2026-09-01)**:[#502 PM 评论](https://github.com/Moshuiwang/lingxi/issues/502#issuecomment-5488293484) 将 #492 限定为管理员↔Bot-Test p2p 私聊的邮箱 mailto 适配;真实 raw fixture/群 GET 不再是实现前置,自动化+mutation+本机门禁先行,候选冻结并部署后再做 PM p2p L4a。用户名/邮箱在本 Trace 验证中非敏感,token/凭据/授权码秘密边界不变。

## Wave 0(零依赖全并行)

- [ ] W0-1 #499 读码判定:真实回答路径是否填充 markdown 列(若否→#499 直接关卡+回写当前能力)
- [ ] W0-2 #492 自动化邮箱形态边界+mutation(不以真实 raw fixture/群 GET 为前置;候选冻结部署后 PM p2p L4a)
- [ ] W0-3 #493 前提:表单原生必填探针⚡+message_id→identifier 映射现状核实(持久化前置设计)
- [ ] W0-4 #496 前提:stage/prod 指标后端同源确认 → E1 容量实验⚡(1/2/4/6/8 递增,曲线落卡)

## Wave 1(独立 worktree 全并行;合并次序见合同执行图)

- [ ] S-1 #495 失败签名(类型+失败码+脱敏锁测;任何失败型/非成功终态 failure_code 非 null,成功保持 null;worker 域先合)
- [ ] S-2 #494 /tmp 三修+资源限制(回收/分环境容量/健康探针真红;prod 规格=攒单②)
- [ ] S-3 #483 TOCTOU 实施(按设计稿;真库并发实测;权限域先于 S-7 合)
- [ ] S-4 #492 mailto 解析(合成夹具如实标注+全邮箱入口+不误伤+mutation+文案;候选冻结部署后 PM p2p L4a;卡面先于 S-7 合)
- [ ] S-5 #496 降级实施(等 E1+◇并发上限;worker 域在 S-1 后合)
- [ ] S-6 #499 实施(若 W0-1=会填充,等◇语义裁定)
- [ ] S-7 #493 管理卡改版(等 W0-3+S-3+S-4;职位+范围/懒过期/按钮态/下发如实化/文档术语重写)
- [ ] S-8 #498 分级门禁 L0/L1/L3+权限影响面 diff(S-1+S-2+S-3 合入后;搭批终车)

## 批终与收口

- [ ] S-10 批终链:串行合并(content.toml 后合者续升)→ `sol/xhigh` 审核 subagent 批量审查 → 统一修复包 → 冻结 trailer → Epic Full → 合 main → Publish → stage 升级回读 → 逐卡 L4a⚡
- [ ] S-9 #497 数据裁定点(◇PM CloudWatch 数据,随到随裁)
- [ ] S-11 #411 条件步(时间闸 ≥09-01+◇PM 无异议 → 清旧库容器+dump 回帖;未得授权则顺延留痕)
- [ ] S-12 收口:9 卡对账+回写+环境盘点(tz/stage 零残留)+收口评论+Trace 终判
