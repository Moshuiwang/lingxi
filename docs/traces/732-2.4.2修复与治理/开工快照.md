# 开工快照：2.4.2 修复与治理（Trace #732）

> 按 `#678` §二与 `docs/traces/README.md:18`：合同正文贴到 40KB 硬上限时，把**开工能力与环境快照**类内容移入本文件。**授权、验收门槛、已裁定事项、共享资源租约、外部写与恢复、各道 Gate 一律留在 `合同.md`，不得移出以绕过批准。**

## 合同区字段（`METHOD.md` §八）

- 方法版本 **v19**；合同版本 **v1**；执行阶段 **Ready**（本 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- **基线提交与核对时间**：`origin/main` = **`b52a98b`**（`git ls-remote` 实读，2026-09-10）。链路：`d9c07fb` = Trace #709 的 PR #726 合并（08:29Z）→ `b52a98b` = 收口记录 PR #736 合并。**`d9c07fb..b52a98b` 只动了 `docs/traces/709-.../` 的任务表与验收两份**（2.4.1 自己的收口文档），与本 Trace 要重算的坐标零交集。#711 准备批的全部行号取自更早的 `3a4038a`，**S-0-2 仍须按 `b52a98b` 逐条重算**。
- **规划者任务 / 会话与停止点**：2.4.2 规划者会话（工作树 `/tmp/lingxi-242-plan-20260910`，现处 `plan/trace-732-2.4.2` 分支）。停止点 = **本合同 PR 合并后转任守望者**（D-19，推翻起草时写的「不转任」）。转任前写交接文档；转任后不实施、不持编排权与合并权，只以外部证据判活，并落实合同 §6 的两类值守机制。
- **角色模型 / 推理档位**：同 §5。
- **当前顶层编排任务 / 会话**：未指派——由守望者在两条硬边界满足后拉起 `242-w0` window 并完成接管登记。
- **编排者接手入口与第一恢复点**：`/takeover` → 读本三件套 + #732 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + #732 最新评论**。


### 批后继任条件（命中）

六个阶段必然跨会话接力。继任：读本三件套 + #732 最新交接评论 → 核实现场（worktree、阶段分支 tip、PR 状态、预发现状）→ 接管登记 → 前任确认停止写入并释放租约 → 退场。**未确认转移前保持等待，不得出现两个写入者。**
