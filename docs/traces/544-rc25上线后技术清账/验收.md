# 验收（rc25 上线后技术清账批；逐 Step 关卡＝卡面 / 报告最小修法的可观察标准；证据落 GitHub）

> v1（2026-09-03 00:xx 北京，按产品负责人 [D-1…D-24 裁定](https://github.com/Moshuiwang/lingxi/issues/544#issuecomment-5512183720)修订）。证据等级目标：代码批 L2（Epic Full）＋L3（独立审核 3 轮 + codex 外审 1 次）；stage 复验 L4a（受控测试身份非生产，全局 6 须连写「受控测试身份」）；生产升级＝全局 5「已部署」＋一次真问数、#538 复现与 #541 名单预开通（全局 6）。每项完成把「现值」改成实际等级并附指针。S-1 热修的验收以 #540 卡面 8 条为准，本文件只登记其终值。

## 已达成

- **S-1 #540**：卡面 8 条全部达成，2026-09-02 12:43Z 关闭。现值：**全局 6**（生产真人首聊 + 真实问数，含数据面）。
- **W0-3 GHCR 可行性**：三条外部事实已坐实并升级为 D-25/D-26。现值：**全局 7（回源确认）**——依据是 GitHub 官方文档与 GitHub 员工在 community 的逐字答复。
- **W0-4 Codex 债定稿**：六项结论各有 `文件:行` 或 SHA 证据。现值：**全局 1（已分析，只读）**。
- **W0-6① X-1 prod 预检**：`app_user` 1 行、同邮箱多行分组 0。现值：**全局 5（生产库只读回读）**⇒ G-3 成立。
- **W0-6② 花名册在职过滤**：结论「未知，上界 3 组 / 7 人」，判据＝工号。现值：**全局 1**（读取被授权边界挡住，如实记未知，未重授权）。转 A-6 由产品负责人一分钟坐实。
- **W0-9 #539 对比稿**：三方向对比 + 建议 + 9 个待裁点，文档门禁绿。现值：**全局 2（产物已形成）**，待裁定。
- **W0-10 #538 定位**：四层证据齐，归属＝降级路径代码。现值：**全局 5**（prod 库与容器只读回读 + stage 两次真实 convert 对照）⇒ G-6 成立。

## 逐 Step 关卡

- **W0-1 探针①**：现值 **全局 5＋7**——活体（stage 受控双轮回合，含敏感性对照）＋ 回源（SDK 自带 CLI 二进制的超时分支原文）双重坐实；`reclaim_stale` 7 小时计数 0、无重投递；探针为独立进程、服务配置零改动，清场回读已确认。
- **W0-2 探针**：现值 **全局 5**——三项计数只出数字未出值；清场回读通过。
- **W0-5**：Trace Issue 评论含附表 C 逐项现值。现值：—
- **W0-7 通路冒烟**：规则片段由产品负责人亲手加入；一条只读＋一条无害写（同一临时文件 `touch`/`rm`）回读通过；失效时点写入 Trace Issue。现值：只读半边已通过（15:5xZ），写半边—
- **W0-11 #541 设计稿**：五个裁定点各两案＋建议，产品负责人逐点答复留痕 ⇒ G-7。现值：—
- **W0-12 拉取凭据与就绪探针**（D-25 裁定后）：两机 `docker login` 成功且 `docker pull` 实际拉到（**不接受只看 login 返回码**——GHCR 的失败形态正是「login 绿、pull 红」）；就绪探针单元每 6 小时 manifest GET 成功、`OnFailure=` 告警可触发、`User=` 由 drop-in 参数化；`systemctl list-timers 'lingxi-*'` 回读。现值：—
- **W0-8 私有化**（D-25/D-26 裁定后）：**先用一次性探针包**验证 public→private 可行且可逆，再动四包；四包 private 后匿名 `manifest inspect` 401、凭据 pull 200；本批 Main Publish 成功且 `push_image.py` 未走降级分支。现值：—
- **S-2a X-1**：迁移新增部分唯一索引真往返；同邮箱第二人开通在签发/采纳令牌前失败关闭并告警，不写正式表行；真库用例＋去索引/去检查各变异红；stage 受控复验：构造同邮箱两行 → 第二人「开通失败（内部错误）」且 `publish_outbox` 零行，用后删除回读。**同一检查点须同时覆盖 S-8 的预开通路径**（否则预开通会绕过 X-1 防线——这是两条主线的交叉面，审核者必查）。现值：—
- **S-2b W-1**（**前提已变**：W0-1 排除了「hook 超时失败开放」，本项只剩两条实打实的缺陷）：`_housekeep` 不再在事件循环内同步调 DB（用例：阻塞 DB 时 hook 回调仍在 30 s 内应答）；`check_deploy_contract` 对 stage/prod worker-queue env 断言 `LINGXI_WORKER_WORKSPACE` **必配、且不得等于或位于用户目录根之下**（去断言变异红）——**stage 现值 `/var/lib/lingxi/users` 会被判红，先改 stage env 再合；prod 现值 `/tmp/lingxi-workspace` 合规不动**；`setting_sources`/`strict_mcp_config`/`permission_mode` 三行断言（改任一变异红）。**不再要求 stage 复跑探针①**（结论已由 W0-1 双重坐实，重跑不增加证据）。现值：—
- **S-2c P-1**：gateway 三处与 scheduler 读同一变量；配置外置文件删一指标 → 管理动作发布 payload 不含该指标（stage 探针）；变量缺文件时失败关闭同 scheduler 语义。现值：—
- **S-2d P-2**：含 `["*"]` 值/空白/换行的 CSV `--apply` 整份拒绝 exit≠0 且零写；映射外公司键按 S-1 裁定保留导入并审计计数；`--initiated-by` 非登记管理员拒绝；变异 3 项红；stage 副本 dry-run 假 CSV 拒绝。现值：—
- **S-3a–S-3d P2 修复包**：每项 Story PR 正文逐条「已改/未改＋原因」＋变异红绿；A-1 `n=20000` 输入 ≤1.5 s 返回 `bad_identifier`；A-3 非发起人点过期卡不记 decider 不发群通知；**D-5：`/admin grant_permission` 与 `suppress_permission` 撤除后，管理员发该文本命令得到「无此命令」类回应且审计不产生任何授权动作（否定用例，变异红）**；#493 大授权确认在飞书回调窗口内应答（stage 387 项复现无「回调超时」）；R6-D2 一次真问数中 `/proc/*/environ` 含 DSN 的 claude 进程数＝0（stage）；R6-D3 pids 达阈值 healthcheck 变红（本地复现）；R6-D4 删两行渲染报错。现值：—
- **S-4a 双 CAS**：**先补一条「两恢复者同状态并发领号」用例**（现有 5 组并发用例的写方全是 `update_state`、两列同时 +1，去 `card_sequence` CAS 的变异会存活——不补就是假绿，W0-4 坐实）；补完后：只留 `card_sequence` CAS 时全部真库并发用例仍绿、去掉该 CAS 变异红、生产写路径不变。现值：—
- **S-4b/S-4d**：「限时」措辞一条 P2 合入；另两条不摘的原因落痕（宿主文件 main 不存在）；#499 禁词断言（含「排版完好」类措辞变异红）；F1 AST 绕过用例红；`load_optional_provenance` OSError 登记。现值：—
- **S-4c**：L1 重设计草案落文档，`L1_LIGHT_ROUTE_ENABLED` 保持 False。现值：—
- **S-4e 分支删除**：五条具名分支删除前后 `git branch --list 'codex/*'` 与 `git ls-remote --heads origin 'codex/*'` 各一次回读；其余 10 条清单落收口评论。现值：—
- **S-5 文档面**：README `:84,:154-164`、`Dockerfile:86-89` 注释、runbook `:171,:208-210,:51-62,§2.4`、配置清单 `:141,:240`、监控单元参数化＋两 timer 入库、架构 6.9 七项登记、C-8 归属更正、Codex 过重代码边界、**#496 502 语义补进当前能力**、**#499 降级触发率由 18.2% 更正为生产实测 100%**、**D-5 撤命令后的产品文档与验收矩阵同步**；docs 门禁绿。现值：—
- **S-6 rc24 遗留**：附表 C 仓库侧每项有 `文件:行` 或 Issue 指针。现值：—
- **S-7 #538**：改用 `descendant` 端点后，stage 用**含表格的同一问题**复现，文档块 API 回读出现标题 / 列表 / 表格结构（不再出现 `body_degraded_reason=unsupported_nested_blocks`）；无表格问题的排版保持原样不退化（回归）；变异红（去掉嵌套块处理 → 复现回到降级）。**回归夹具用 stage 受控样本构造，不取用户数据**。现值：—
- **S-8 #541**（主线二）：一份产品负责人名单（≥2 人：1 存量 + 1 新用户）在 stage 预开通后——`app_user` active、令牌就绪、正式表行含合成权限、MCP 就绪探针通过、**用户零消息**；随后该用户首聊**无开通等待**、直接问数成功（双通道）；审计标明「预开通路径」；**幂等**：同名单重跑零变化、预开通后再走一次真实首聊零重复写入；名单中花名册匹配失败 / 零权限 / 令牌解密失败者逐个报告且不阻塞其他人；变异 ≥3。现值：—
- **S-9 批终链**：见下。
- **S-10 生产升级**：X-1 precheck 复跑 0 行；四 digest 渲染 `@sha256:` 与固定值一致；迁移头前进；三服务 healthy 15 分钟 `RestartCount` 0；真问数双通道（平台回读＋`worker.task.terminal` `system_prompt_digest` 同 stage、`degraded`=0）；#538 复现出现结构（文档块 API）；**#541 生产名单 dry-run→`--apply`→幂等复跑零变化**；版本 tag 指向代码批提交；`systemctl list-timers 'lingxi-*'` 含新装单元。现值：—

## 批终链（全绿才进生产）

本机 `check.sh full` 绿 → 冻结 → **审核①批终（Opus 5/max，含两条主线交叉面与 S-1 接缝）** → 唯一 fixpack → **审核②定向复核** → **codex 外审 ×1（`gpt-5.6-sol`/xhigh）** → 外审 fixpack（若有）→ `Image-Candidate: true` → Epic Full 绿 → 合 main → Main Publish → stage 升级回读 → stage 受控复验 6 项（X-1 / W-1 / P-1 / P-2 / #538 / #541）→ **审核③生产升级前 runbook 走查** → 块 Z。制品回填：

- 上线 tag：`________`；四 digest：scheduler `________` / gateway `________` / worker `________` / migrate `________`；Epic Full run：`________`；Main Publish run：`________`；prod 迁移头：`________`。

## 静态排除对账（[合同.md](合同.md) §3 不做清单 → 本文件）

D-3/D-4/D-8/D-9/D-10/D-18/D-19 在收口时标「未做（已裁）」；**D-23 标「登记不做（零对象）」**；上游项（hook 超时默认拒绝、argv Bearer）标「登记，未修」；MCP 口径回源、新用户 issue 路径标「产品负责人 / 观察项，rc25 只登记结论」；**花名册去重标「已取消（产品负责人裁定）」**；**旧系统退役全部标「不在本 Trace → #546」**；**GHCR 私有化按 D-25/D-26 裁定结果标终态，若裁「缓」则标「明确不做，非欠账」**；L5/L6 真实用户验收、CD 实施不在本文件。

## 留证

逐项证据落 Trace Issue 或对应 Story PR；stage 探针与复验落 Trace Issue（含清场回读）；生产时间线落 rc25 ops 评论；#538/#539/#541 各落本卡；收口评论落 Trace Issue（UTC＋北京、Epic Full/commit、代理人次、审核轮次、配额读数、残留盘点）；任务表随执行 commit。
