# 验收（逐卡关卡＝issue 可观察完成标准＋自证闭环；证据等级目标 L6，受控测试身份非生产）

- **#439**：受控注入 `/admin user <邮箱>` → 卡体 JSON 回读断言（公司「中文名 (ID)」、指标中文名、零 ou_/lpo_/pac_、术语「补充授权/屏蔽指标/撤销」、column_set 横排＋flex_mode 声明）；form 提交回调**真实到达**（200530 消除）；群通知渲染断言（被操作者姓名＋邮箱＋动作/公司/指标中文名＋结果，pac_ 仅审计）；确认卡/终态卡/router 输出端同口径（TOP 1/4/5/7 防倒退断言）；收官=一轮真实 查看→授权→收回 全程零内部 ID（stage 实测）。真人体验=PM 加菜。
- **#464**：stage 受控注入先重后轻两任务，轻任务在重任务完成前进入 running 并正常回复（真实回读）；停机窗口否定用例保留且绿。
- **#465**：受控复现原场景——重任务占满槽位时新消息在阈值内收到排队提示；构造 handler 异常→用户收到报错文案非静默；同会话闸文案区分处理中/排队中；集成测试枚举全部 handler 异常出口逐一断言可见响应（变异验红）；产品合同新条款＋验收矩阵新 V-行（cell ≤800B）合入。
- **#467**：三态用例（未设置=开、显式关=关、旧值 "1"=开）＋变异验红；stage 无该 env 的容器完成一次真实建档为转换后结构化文档；文档口径 grep 零残留。
- **#468**：真库用例——suspended 用户跑批不进发权基线、不回发；恢复后重新纳入；变异验红（去掉排除条件必红）；停用即时撤销路径回归绿；stage 受控停用/恢复回读发布行。

## 批终链（全绿才收口）

本机 full 绿 → opus 批量审查无 P0/P1 遗留（测试异化抽查沿 rc21）→ 冻结 `Image-Candidate: true` → Epic Full 绿 → 合 main → Main Publish → stage 升级三容器 healthy 回读 → 逐卡 L4a。

## 双通道与留证

- 用户可见断言=服务端结构化日志＋平台 API 回读（卡片 JSON/发布行/消息回执）双通道一致；冲突升 PM 并登记证据对应性问题。
- 逐卡证据落各卡对账评论；收口评论落 #469（UTC＋北京双标注、Epic Full 次数/commit 数、预算消耗）。
