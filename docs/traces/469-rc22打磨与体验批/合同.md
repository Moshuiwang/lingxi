# 开工合同：rc22 打磨与体验批（Trace [#469](https://github.com/Moshuiwang/lingxi/issues/469)）

> 方法版本：#147 **v15**＋v16 候选首批实践（tmux 常驻 session＋每批命名 window＋元守护，登记于 #147 2026-08-30 评论）。合同版本：**v1**。本 PR 合并即产品负责人批准。
> 规划依据：PM 2026-08-30 rc21 收口现场口头授权整条链（规划 → 批准 → 拉起编排者 → 规划者转任元守护）＋同日逐条裁定（范围①~⑤、#263/prod 除外、codex 遗产废弃、凭据轮换不做、#411 自裁）。

## 1. 目标

- **用户结果一句话**：管理员和用户在下一个 stage 版本上得到三条可见改善——管理链全程零内部 ID（中文名/姓名邮箱/人话术语/横排按钮/表单能提交）、多用户并发不再被长任务饿死、任何消息在任何系统状态下必有可见响应；外加 docx 转换默认开启与「停用即停发」权限边界修复。
- **里程碑（何谓「到了」）**：五张卡 #439/#464/#465/#467/#468 全部达到各自 issue 可观察完成标准并关闭；main 合入且 stage 升级回读绿；逐卡自证 L4a 通过。
- **完成定义（Delivered）**：五卡关闭各留对账；产品合同（#465 响应覆盖条款）与验收矩阵（新 V-行）回写合入；批次收口评论落 #469；环境盘点零残留。**prod 放行不在本 Trace**（PM 动作，全部 issue 完成后另立 ops 卡，材料在 #418 B 段）。

## 2. 授权终点

- **可自动执行**（沿 #445 合同口径，PM 2026-08-30 明示沿用）：合并批次 PR、部署 biai-stage、关闭 issue、Bot-Test 真实收发与受控注入 L4a、文档回写（含 #465 产品合同条款与验收矩阵 V-行——PM 已在 #465 正文裁定该承诺入合同）、stage sudo（既有监控/systemd 形态）、**Workflow 多代理扇出**（PM 已显式授权，沿用）。
- **显式除外（单独请示）**：biplus-prod / Bot-Prod / 生产实例任何写入；prod 放行；#263 本体；#411 观察期清理（未满，见不做清单）；权限白名单修改；删除用户数据；凭据轮换（PM 裁定不做，不立项）。
- **常设授权引用**：git-claude-tz push、gh 机器身份、bootstrap PR 先例（建 epic/* 前刷 Story Fast）、P2 豁免（红线/P1 不豁免，留痕）。
- **中途裁定**：决策攒清单一次提交；单字裁决有效并留痕 #469；新决策按「最小风险＋可回滚」自裁留痕，产品边界类等 PM。

## 3. 不做清单

- 生产动作全部不做；#263 硬切与 prod 放行不做（PM 裁定：全部 issue 完成后才放行，届时另立 ops 卡）。
- #411 观察期清理不做：观察期至 2026-09-01~05 未满（核对留痕 #411 评论 5468557944）；期满后由元守护/当任编排者向 PM 取「无异议」再按该卡执行。
- codex 遗产不复用：#448/#449/#450 已关废弃，不复用其合同；外部 codex 审查本批配额 0（auto-mode 下不可用＋遗产废弃）。
- 凭据轮换不做（PM 2026-08-30 裁定，不立项）。
- 新发现问题默认只登记不立项（主线灯塔：不做它，五卡关闭是否受阻？）；疑问句只答不动手。

## 4. 验收标准

- **每卡关卡** = 其 issue 可观察完成标准＋自证闭环条款，逐条对账后关闭；细则见[验收.md](验收.md)。真人主观体验确认一律为 PM 加菜非关卡。
- **批终链**：本机 full 绿 → opus 批量审查无 P0/P1 遗留 → 冻结 trailer（`Image-Candidate: true`）→ Epic Full 绿 → 合 main → Main Publish → stage 升级（runbook 序列，`--profile mvp up -d`）三容器 healthy 回读 → 逐卡自证 L4a。
- **证据等级**：关键路径 L6（受控测试身份非生产，全程如实标注）；未验证层级宣称前列明。
- **双通道纪律**：用户可见断言=服务端结构化日志＋平台 API 回读（卡片 JSON/发布行）双通道一致才定论；冲突升 PM。
- 请 PM 做真实验证前先出示就绪自检（前提核对＋日志回读＋预判断点）。

## 5. 成本预算（PM 2026-08-30 批准时调整定稿）

- Epic Full 主动消费 **<4 次（即 ≤3；PM 批准时原话「epic full <4即可」，由建议值 ≤2 调大）**；分类器自动提级不计主动消费，按 rc21 口径报告。
- 时间预算 **≤8h**（rc21 实测 8 卡 9h；本批 1 大＋2 中＋2 小、零迁移预期）。
- **模型配比**：实施/调查 **sonnet**；审查/对抗验证 **opus**（批终一次四视角＋测试异化抽查）；**编排者 Fable（#147 必填字段，待 PM 批准时确认）**；元守护=本规划者会话（Fable，已在跑，无新增会话成本）。
- Workflow 扇出开跑前查配额余量；对抗验证优先编排者 grep/diff 坐实机械性发现，只派行为面。
- 外部审查（codex）配额：0。

## 6. 主线声明与批次纪律

- **主线**：五卡关闭。任何派发/立项/修复先问「不做它，五卡能不能关」。
- **单批结构**（沿 rc21 先例）：bootstrap PR 刷基线 Story Fast → `epic/rc22-polish` → 五卡并行实施（独立 worktree / Workflow）→ Story PR 串行合并（每合一个 `gh pr update-branch` 下一个等重跑）→ opus 批量审查 → 统一修复包 → 冻结 squash PR 携带 `Image-Candidate: true` → Epic PR → 合 main → stage 升级 → 逐卡 L4a → 收口。
- **单一写入者**：tz 主工作树仅编排者会话；实施代理独立 worktree；commit 先行再汇报；子代理声称等待必挂编排者兜底观察。
- 收尾必含：环境盘点（tz＋stage 零残留）、任务表/验收回写、批次收口评论（UTC＋北京双标注、预算消耗、Epic Full 次数/commit 数比率）、接力或收口退场。

---

# 合同区（v15 必填字段）

- 方法版本：v15（＋v16 候选实践）；Trace 合同版本：v1；执行阶段：Ready（本 PR 合并即生效）；整体交付结果：Undetermined。
- 产品负责人批准：2026-08-30 对话内批准 v1，原话「批准，预算可以再调大一下，epic full <4即可」（Epic Full 预算随之调整，编排者模型 Fable 一并确认）；合并本 PR（#470）即批准留痕。
- 基线：main=`f24de90`（2026-08-30 核对；本 PR 合入后以合入 HEAD 为准）；stage=三容器 healthy on `20260830-4c33c6729971`，数据库=Supabase 测试实例（session 池化）；迁移链头 `0079`，本批零新迁移预期（如有，看 `check_alembic_revisions` 不看文件名）。
- `execution_adapter`：`claude_tmux_named_session`，按 v16 候选形态实例化——常驻 tmux session **`lingxi`**，本批编排者 window **`rc22`**，元守护驻同 session 独立 window。
- 规划者会话与停止点：本会话（Fable，tz，窗口现名 rc22 将让渡改名）；停止点=编排者接管登记完成，随即转任元守护。
- 角色模型：编排者 **Fable**（待 PM 确认）；实施者池 sonnet；唯一独立审核者 opus（批终一次，不参与实现、不直接修复）。
- 编排者接手入口：读本三件套＋#469 最新评论 → 在 #469 留接管登记（代理/分支/授权终点）→ 合并权原子转移。第一恢复点=任务表当前勾选＋#469 最新收口/交接评论。批后继任条件：本批为最终批可直接收口退场；若 PM 追加批次，按 v15 §6.8 接力序列。

## 输入 Issue 准入与关闭资格（可达性审计 A）

| 输入 | 完成标准（自 issue） | 当前事实 | Step | 收尾去向 |
| --- | --- | --- | --- | --- |
| #439 | 管理员全程不接触 open_id/lpo_/英文指标名完成查看→授权→收回（stage 实测）；PM 7 条裁定＋TOP 1-10＋群通知高可读性裁定全落 | 规格完整（issue 三评论＋补充裁定）；200530 根因已锤死（form 按钮缺 name）；column_set×form 可行性未验证 | W0-1、S-1 | 关闭 |
| #464 | 轻任务在重任务未完时即被领取处理；停机纪律回归绿 | 根因读码坐实（claim 批→gather 到底）；#153 停机语义为既有约束 | S-2 | 关闭 |
| #465 | 排队阈值提示＋queued_stuck 用户可见说明＋文案如实＋全 handler 出口响应断言 | PM 已裁「产品合同级承诺」；阈值 10~15s 实施定值 | S-3 | 关闭；产品合同＋验收矩阵回写 |
| #467 | 三态用例钉死＋无 env 容器真实建档转换生效 | 开关在 gateway/config.py:245（默认关仅认 "1"）；stage 探针四步已过（rc21） | S-4 | 关闭 |
| #468 | suspended 不进发权基线（真库用例＋变异验红）＋stage 回读 | postgres_permission_publish.py:797 只排 deleting/deleted，代码事实已复核 | S-5 | 关闭 |
| #411 | —— | 观察期未满（至 2026-09-01~05） | 不纳入 | 保持开放，元守护跟踪 |
| #263 | —— | PM 裁定全部 issue 完成后放行 | 不纳入 | 保持开放（pre-ready） |

## 计划执行步骤（任务表.md 为状态账）

| Step | 动作 | 启动条件 | 成功证据 | 失败/等待去向 |
| --- | --- | --- | --- | --- |
| W0-1 | 飞书卡片探针：column_set 嵌 form 可行性＋form 按钮补 name 后回调真实到达（Bot-Test 受控测试卡，用后删） | 批准即行，与 S-2~S-5 并行 | 探针卡回读＋回调日志，结论留 #439 | 不可行→按钮布局降级方案（form 外 column_set / overflow）报 PM 单字裁 |
| S-1 | #439 打磨全量：7 裁定＋TOP 1-10＋群通知姓名邮箱；**共享渲染警示：TOP 1/4/5/7（notification.py 确认卡/终态卡/群通知/render_card_payload、_ACTION_LABEL、router 输出端）必须同批，防「管理卡中文、确认卡裸 ID」倒退** | 布局子项待 W0-1；其余即行 | 卡体 JSON 回读断言＋form 提交回调到达＋一轮真实操作零内部 ID | 组件不可行项降级留痕；产品取舍升 PM |
| S-2 | #464 滚动并发：领取循环改在途集合，任一终态即领新；停机语义不弱化 | 即行 | 轻重并发注入回归＋停机否定用例绿 | 停机语义冲突→退规划者 |
| S-3 | #465 响应覆盖：阈值提示＋queued_stuck 接线＋文案如实＋全出口枚举断言＋产品合同/矩阵回写 | 即行（合并次序在 S-2 后） | 集成测试枚举全出口＋变异验红 | 与 S-2 行为耦合冲突→编排者裁定分域 |
| S-4 | #467 默认开启：默认翻转＋三态用例＋文档对齐 | 即行 | 变异验红＋grep 零旧口径 | —— |
| S-5 | #468 停用回发权：发权基线排除 suspended＋用例钉死；roster 审计基线默认不动留痕 | 即行 | 真库用例＋变异验红 | 审计基线语义有疑→登记不扩 |
| S-6 | 批终流水线（见第 6 段） | S-1~S-5 全合入 epic 分支 | Epic Full 绿＋Main Publish＋stage 回读 | 审查 P0/P1→统一修复包后重冻结 |
| S-7 | 逐卡自证 L4a（细则见验收.md） | S-6 完成 | 双通道证据落各卡 | 失败→缺陷账本→修复包 |
| S-8 | 收口：五卡对账关闭＋回写＋环境盘点＋收口评论＋元守护交接 | S-7 完成 | #469 收口评论 | —— |

## 合并次序与写入分域

- **S-2 先于 S-3 合并**（配对卡先消根因）；S-3 不动领取循环本体，worker 域内文件分域在派发卡写明。
- **content.toml 双写入者**（S-1 卡片文案/术语、S-3 排队/超时文案）：各卡自行升版＋lock 保自绿，合并队列 update-branch 时后合者续升，队尾版本为准（rc21 规则）。
- S-1（admin 卡面/notification/router 输出端）、S-4（gateway config）、S-5（adapters 发权基线）互不相交；S-1 与 S-3 均触文案层，冲突由合并队列消解。

## 共享资源租约

| 资源 | Owner | 规则 |
| --- | --- | --- |
| tz 主工作树 | 编排者会话 | 唯一写入者；实施代理独立 worktree/Workflow，用完即清 |
| 合并权 | 当任编排者 | 接管登记时原子转移；规划者/元守护不持有 |
| Bot-Test/stage 受控注入 | 编排者（L4a 窗口） | 同一时刻单客户端；探针/测试卡用后即停即删；发布行测试后逐字节复原（rc21 先例） |
| tmux session `lingxi` | 元守护 | window 命名=批次代号；退场 window 保留供回翻，PM 清理 |

## 元守护合同（v16 候选首批实践，操作细节按 #147 候选授权由本合同定义）

- 本规划者会话在编排者接管登记后转任元守护：驻 session `lingxi` 独立 window；**只信外部证据判活**（tmux window 存活＋#469/PR 留痕新鲜度），30~60 分钟一查；不实施、不持合并权。
- 编排者收口/退场→若有后续批次按接力制拉起继任 window；失联（>90 分钟无外部留痕且 window 死）→ 先取证留痕 #469，再拉起继任接管（读三件套＋最新交接评论）。
- 承接 PM 插入裁定并转发当任编排者；旧元守护（window `meta-`，rc21 编排者转任）在新链就位后退场，交接留痕 #469。

## 现场事实与已知坑（编排者必读）

- 沿 #445 合同「现场事实与已知坑」全部条目（epic/*与 main 拒直推、冻结走 squash trailer、两个计时偶发用例重跑即绿、确认卡 TTL 10 分钟、受控注入姿势见 #373 两评论、/admin 写命令单一在途闸）。
- tz 八条硬事实（memory 2026-08-30 段）：`--env-file` 不解析引号（用 `set -a; source`）、stage HOME 700 需 chmod 755 挂载目录、squash 标题 `fix: #NNN` 自动关卡（收口前查状态）、stage compose 必须 `--profile mvp up -d`、查敏感 env 只列变量名、form 组件 name 必填（200530）、Workflow 批量超时=API 瞬时故障（scriptPath＋resumeFromRunId 重跑）、worker 批闸即 #464 本体。
- 凭据纪律：零回显、不进 argv/日志/Git；L4a 派发卡写明「公开可见」与凭据红线。
- 停止条件：改变产品承诺/权限/数据边界或不可逆外部副作用→停该项等 PM；合同缺项→退回规划者（元守护可代持规划者职责修订，修订走三件套 PR 重批）。
