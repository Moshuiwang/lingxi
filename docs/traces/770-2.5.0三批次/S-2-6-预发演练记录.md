# S-2-6 预发拉式部署演练记录（rc.101，2026-09-15）

> 出处：Trace [#770](https://github.com/Moshuiwang/lingxi/issues/770) 批次 2 S-2-6；原始评论 5674074895（演练记录）、5673409789 / 5673571816 / 5673807019 / 5673840211（接缝⑤–⑧）、5673916831（④ 手术留痕）、5674166451（接缝⑨ / F17）、5674231709（#756 ①②）。本文只收长期有用的事实：结果、判定值出处、真机接缝与修法、预发临时解除与残留；逐轮日志留在 Issue 评论与本机证据副本。

**结果**：预发三个常驻服务由拉取代理 + 部署器从 v2.4.3（rc.98 镜像）升到 **v2.5.0-rc.101**（`verified_digests` = 清单三 digest：scheduler `749b68b2…` / gateway `5a9a6ef1…` / worker `ef711332…`；库头 0096 → **0098_qa_corpus**；容器带三枚 `io.lingxi.*` 标签）；状态账 `last_result=already_in_place`、`deployer_state=verified`、`highest_deployed_tag=v2.5.0-rc.101`、`consecutive_failures=0`；`host.active.json` = 计划 `33204abf` verified。全程编排者只读盯（观察哨 bh8b2yh42 / bvmwwsn3n），四次守护启动均按常设授权自跑。

**E-8 逐项**（判定值来源：journal `阶段=… 结果码=…` + 阶段账 `stages`）：

| 项 | 实测 | 判定值出处 |
| --- | --- | --- |
| 只出网、看到候选即核清单与摘要 | 每轮 `release_list selected` → `bundle index_verified index_source=asset`（rc.101）/ `embedded`（v2.4.3 旧版本作恢复材料，F11）→ 部署器 | journal 01:32–03:06 共 31 轮 |
| P2-1 计划窗口过期 | 计划 `da230d0d` 02:09:17 落盘 → 钩子失败停 `planned` 四轮 → 02:29:45 `阶段=approval 结果码=unknown` → 守护按 §五四步归档三文件解除 | journal + `drill-20260915T020901Z.log` `window_expired_observed ok=True` |
| 篡改拒绝 | 已装包 `compose.prod.yaml` +16 字节 → 02:29:59 `阶段=bundle 结果码=unknown` → 还原 → 下一轮恢复 | journal + 守护 `tamper_refused ok=True` |
| 九处中断接续 | ①计划落盘后 / ②部分镜像到位后 / ③停接收后（计划 `33204abf`，02:30–02:40）与 ⑤迁移提交后 / ⑥首个服务更新后 / ⑦全服务更新后 / ⑧观察中（02:48–02:50）共 **7 处 `systemctl kill -s SIGKILL` 整 cgroup 后自动接续**（每次下一轮 `approval_reused` + `continuation apply_same_plan` + 钩子通过 + 部署器按阶段账续做）；**④迁移提交前 = 设计性停机**（`migration_unknown_no_retry`，库头未变、无作业）+ 人工续跑（产品负责人裁甲 5673904138，手术留痕 5673916831）；⑨ 完成标记前毫秒级窗口不注入（部署器单测覆盖） | 四份守护日志 `kill / after_kill / snapshot` + 阶段账 |
| 并发拒绝 | 02:50:46 观察期内第二个代理进程 → `阶段=lock 结果码=concurrent_run_refused`，退出码 1 | journal + 守护 `concurrent_run refused=True` |
| 15 分钟观察 | `observe` 02:50:5x → 03:06:12 `verified`，52 个样本，三容器 healthy | 阶段账 `observe:verified(52)` |
| 幂等 | 03:06:21 下一轮 `阶段=idempotence 结果码=already_in_place`（状态账级） | journal + 守护 `idempotent_round ok=True` |
| 告警触发一次 + 去重 + 恢复 | `sent` 4 次：`unknown`（01:32:43）/ `pre_apply_hook_failed`（02:09:19）/ `failed`（02:30:31）/ `verified` 成功通知（03:06:20）；同键 `deduplicated` 17 轮；03:06:14–18 四条 `:recovery` 恢复通知 | journal + 状态账 `alerts` |
| 首次接管（F8/F9） | 无标签的 v2.4.3 旧容器被计划 `old` 侧接受（`prepare` / `stop` verified），部署器起的新容器带三枚标签 | 阶段账 `prepare` / `stop` `actual` |
| 收据（F5） | 02:00:23 `installation_receipt refreshed`（`bundle_sha256` → 3407f574），接续轮 `unchanged` | journal |
| 权限表写表前备份（L-8，守卫钩子） | 每次 apply 前钩子跑一次备份：`/opt/lingxi/release-pull/guard/backup-20260915T02*.json` 共 13 份（0600 root，各 2610 字节）；台账 / 整批撤回演示待有本批写入行（#659 每日批或管理员授权）后做，S-2-8 与共用面清零一起 | `ls guard/` |

**未做 / 待做**：首触冒烟（产品负责人用预发账号问一句数，#785 第 6 条）；权限表整批撤回演示（需先有本批写入行）；#659 每日批 + 管理员授权双通道回读（新 scheduler 已起，每日批下一轮自动跑）；#756 ③④。

**真机接缝与解除（详见各评论）**：⑤ `__pycache__`（5673409789，F12 已合）/ ⑥ public-config 0644（5673571816，F13 已合）/ ⑦ deploy_root 0750（5673807019，F14 文档随记录 PR）/ ⑧ `docker run --env-file` 不去引号（5673840211，F15 待派）/ ④ 设计性停机无 recover 工具（F16，W4 生产前必补）。预发现存四处临时解除：drop-in `Environment=PYTHONDONTWRITEBYTECODE=1`、`public-config.json` 0600、`deploy_root` 0700、`.env.stage.migrate` 去引号——rc.102 装配脚本已同步前两处（`s26_install.sh` 8b03fda9 含 Environment 行；0600 / 0700 / 去引号三处待补进装配脚本，登记）。

**残留盘点（S-2-8 清）**：`/root/lingxi-s26-drill-archive/`（da230d0d 三文件、22b67585 四文件、33204abf 阶段账手术前备份）、`/root/lingxi-s26-backup/`（pull-agent.json 旧账、监控脚本旧版、`.env.stage.migrate.before-unquote`、binding.json 备份）、`guard/backup-*.json` 13 份（含令牌密文，只删不留）、state 目录两份孤儿 request（`bf719f49` / `a740c341`）、`/tmp/lingxi-s26/evidence/` 四份守护日志 + `plan-dry-run.json`。完整证据副本（688 行 journal / 阶段账 / 四份守护日志）留在批次 2 编排者本机与 Issue 评论，不进仓库。

## 真机接缝 vs 合入前测试覆盖（产品负责人 2026-09-15 要求的专项复盘条目，编号沿用 Issue 评论）

| # | 接缝（真机首见） | 为什么合入前的测试没抓到 | 修法 / 落点 | 代价 |
| --- | --- | --- | --- | --- |
| ① | 控制包 `FILES` 白名单双向拒：rc.99 工具核 rc.98 / v2.4.3 旧包（15 项）也拒 → `current_release unknown`（09-14 11:45Z） | 单测只用「同版本工具核同版本包」；跨版本（新工具核旧包、旧工具核新包）没有夹具 | F7 集合以包内索引为准 + 三前缀 + 必备文件；新增 `test_control_bundle_cross_version.py` | rc.100 |
| ② | 契约 `host` 用 `hostname -s`，部署器按 `socket.gethostname()`（预发是 FQDN）比对 → `host_mismatch` | 环境性事实，单测无法覆盖；引导文档没写清取值口径 | 装配脚本改取内核主机名 + 归档旧状态账；文档补口径 | 装配脚本一版 |
| ③ | 手工 compose 起的旧容器没有 `io.lingxi.*` 标签 → `unplanned_service_artifact_or_config` | 假世界里的旧容器都是「部署器自己起的」（带标签）；首次接管场景没建模 | F8/F9 首次接管规则（三枚全缺 + 旧镜像摘要相等 + `host.active.json` 不存在或同计划） | rc.100 |
| ④ | Release 没有 `control-index.json` 附件 → `bundle_digest_mismatch`；GitHub 不可变 Release 不能事后补附件（HTTP 422） | 假 `gh` 永远给两份附件；「不可变 Release」是平台属性，真机才知道 | F10 发布流程上传附件 + F11 代理容忍缺附件（tar 内嵌索引为准，清单 `index_sha256` 钉住） | rc.101 + 一次 422 |
| ⑤ | 代理子进程以 root 往 0555 版本目录写 `deploy/__pycache__` → `verify_install` 拒 → `bundle unknown`（且 journal 不带原因） | 单测以普通用户跑、目录可写、从不「先 import 再 install 同一目标」；根因（`-B` 不传子进程）本可用「import 后断言无 `__pycache__`」钉住 | F12：子进程 `-B` + `PYTHONDONTWRITEBYTECODE=1`、入口 `sys.dont_write_bytecode`、单元 `Environment=`、journal `reason=`；预发临时 = 清缓存 + drop-in 环境行 | 两次 PM 亲跑 |
| ⑥ | 部署器入口按私有材料（0600）读 `public-config.json`，装配按「非秘密」写 0644 → `plan private_file_permissions` | 单测用 `atomic_write` 生成该文件（恰是 0600）；引导文档没写权限位；三处读同一文件的口径不一致（代理 / runtime 公开，部署器私有） | F13 部署器改公开读 + 文档写明 0644 + 测试用 0644 | 一次 PM 亲跑 |
| ⑦ | 部署器 apply 把 `host.active.json` 写到 `lock_path` 同目录（= `deploy_root`），`atomic_write` 要求父目录 0700；引导安装权限表与装配脚本按 0750 建 → `apply failed / private_directory_permissions`（D 段首轮 02:30Z） | 单测夹具用 0700 建锁目录；文档权限表没有被任何用例钉住 | F14：文档 `deploy_root` 改 0700 + 装配脚本同步（随批次 2 记录 PR）；预发临时 = chmod 0700 + 归档失败计划 | 一次自理脚本 |
| ⑧ | 部署器 `migration()` / `job()` 用 `docker run --env-file` 读 env 文件，docker **不去引号**（compose 会去），预发 env 文件 DSN 一直单引号 → 容器内 scheme '' → `migration_revision_unknown`（02:34Z） | 假 docker 不解析 env 文件；「stage env DSN 引号陷阱」在 memory 只登记了 psql 场景；单测没有真 docker 对照 | F15（待派，250-w4）：部署器把 env 文件按 compose 口径规范化后写私有临时 env 文件再 `docker run`；预发临时 = root 副本去引号 | 一次自理脚本 |
| （④ 设计） | 「迁移提交前」打断 → 部署器 `migration_unknown_no_retry`（按设计不盲跑迁移），三常驻容器已停；唯一出路「独立批准 recover」没有脚本 / runbook（02:41Z） | 不是缺陷是缺口：假世界里迁移瞬时完成、④ 从未真正命中「记录存在但作业未起」；文档只写「部署器检查迁移作业实际状态」 | F16（W4 生产前必补）：可核对的人工续跑入口（作业未起 + 库头未变）或 recover 工具 + 文档；预发按产品负责人裁甲做阶段账手术（备份 + 删 migrate 记录）续跑 | PM 一次裁定 |

**共性**：假世界（假 `gh` / 假容器 / 普通用户 / 可写目录）与引导文档、装配脚本各说各话；九条里八条在第一次真机跑之前都可以用「按文档权限位与身份跑一遍代理各阶段」的夹具抓到。第一次真机跑两天抓出九条，每条平均耗一个候选、一次产品负责人亲跑或一次自理脚本。

## 预发现存的临时解除（rc.102 装配脚本须覆盖；生产引导安装文档已同步的打 ✓）

| 项 | 预发现状 | 正式落点 |
| --- | --- | --- |
| drop-in `Environment=PYTHONDONTWRITEBYTECODE=1` | `lingxi-release-pull.service.d/10-local.conf` | F12 仓库单元已带（PR #795）✓ |
| `public-config.json` 0600 | 收紧后部署器与代理都接受 | F13 部署器改公开口径（PR #795）✓；文档写明 0644 ✓ |
| `deploy_root` 0700 | `/opt/lingxi/deploy` 已收紧 | F14 引导安装权限表改 0700 ✓ |
| `.env.stage.migrate` 去引号 | root 副本已去引号（原件备份） | F15 待派：部署器按 compose 口径规范化 env 文件 |
| ④ 阶段账手术 | 计划 `33204abf` 阶段账删 `migrate` 记录（备份在归档目录） | F16 待派（W4 生产前必补）：可核对的人工续跑入口或 recover 工具 + 文档 |
| 监控单元钉 `python3.12` | resource-sample PATH 垫片 drop-in、host-monitor ExecStart drop-in（含 `--enable-resource-thresholds`） | 产品负责人裁「升级系统」；仓库单元与生产 G-2 自检待改（登记） |
| `/opt/lingxi/runtime-config` + public-config `values` / `files` | 已建并登记 | F18 引导安装文档已补 ✓；装配脚本待同步 |
