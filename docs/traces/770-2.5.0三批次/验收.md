# 验收（Trace #770 · 2.5.0 三批次；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace Issue 或 PR）

> 证据等级口径见 `docs/技术设计/验证与门禁.md`（L 名 = 成熟度 · 环境 · 执行者；全局 1–7 级对照见该文第三节）。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，不得为了收口好看而标绿。**判定字段只认系统写出的值，前置状态允许人工构造；答不出「这个值是谁写的」即无效判据。**

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准 | 目标等级 | 现值 | 关联验收矩阵 | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1** #748 裁决层 | ① 引入 PR 合并后任一真实 PR 由 `lingxi-verdict[bot]` 上报 `Epic Verdict`；② 两条规则集含 `Epic Verdict` 且来源 = 该 App（产品负责人动作 D，回读）；③ 造 PR 实测：把必需检查替换为同名普通作业 → 合并被拒；改工作流的 PR 由主干版门禁自动复跑一次（D2）；④ 撤 `/.github/workflows/` 属主整行、保留只护 `verdict.yml` 的窄行（D4）后，一个不改 verdict 的工作流 PR 无需产品负责人批准即可合并；⑤ 恢复路径演练：临时去掉规则集里的 `Epic Verdict` 行 → 合并 → 加回，全程记录 | L2 + 真实 PR 实测（全局 4） | — | — | — |
| **E-2** #751 三条钉住 + 审计可见 | 三条变异各自判红（删被验证的那一行仍全绿即无效）；`outreach.py` 在容器内运行时 stderr 有审计行；`数据库设计.md` 第九节补句 | L2 | **L2**（三条变异红 + 用例；容器内 stderr 待 W2 预发运行） | — | `4dfcc7c`；#770 评论 5654521549 |
| **E-3** #743 探针 → #712 裁定 | 只改 `test_roster_audit_postgres.py` 清场方式后 `--shards 2` 一次跑批，失败签名消失 / 未消失都有 run 记录；据此请产品负责人裁「继续 / 保留旧路径收口」并留痕 | L3（本机）| **L3**（签名消失；分片 0 另露两条隔离面失败）；裁定 = 收口 | — | `f02280a`；#770 评论 5655294503 ① / 5657479164 |
| **E-4** #765 制品核验 | containerd 存储机器（tz）与 CI 双侧核验结论一致；既有失败分支逐条仍在（否定断言） | L2 + 本机实测 | **L2 + 本机实测**（containerd 一致 / 不一致；真实 PR #759 候选包通过）；CI overlay2 侧未实机复核 | V-部署-（编排者登记） | `7661245`；#770 评论 5655107074 |
| **E-5** #658 core 平移 + #708 P2-4 修法 + #659 第一步 | #658 两个 PR 行为零变化：既有测试不改断言全绿 + 两处变异哨兵；棘轮基线只缩不增；#708：构造非内测待确认动作，扩员准备不再返回「确认已过期」，原过期判定仍红；#659 第一步：固定样本走两个入口结论逐条相同的对照测试 + 预发每日批一轮 + 一次管理员授权双通道回读 | L2；#659 第一步 L4a | **部分**：#658 L2（零变化 AST 逐字比对、变异哨兵红）；#708 L2（新码 `target_has_pending_action`，产品负责人接受）；#659 第一步 **L2 对照测试已做**（`tests/test_permission_decision_tree_parity.py`，10 分支 × 两入口全红）+ **预发每日批一轮 L4a**（rc.101 scheduler 2026-09-15 03:19:31Z `每日权限重算完成 已开通用户=4 无变化=4`，#770 评论 5674166451）；管理员授权双通道回读待产品负责人一次授权 | — | `5ee373f` / `055ae14` / `f83a97f` / `03306d1`；#770 评论 5656130411 |
| **E-6** #604 必配 | 缺 / 空 / 无效地址 → 队列执行服务启动失败退出码 3、不接单（用例）；同源 / 不同源 / 同源不同路径三分支用例；预发 env 先补变量再升级镜像并回读；生产 env 写值在 G-4 放行后回读（值不回显） | L2 → 预发 L4a → 生产「真实链路 · 生产 · 代理 · 只读」 | **L2 + 预发 L4a**（缺 / 空 / 无效 → exit 3 不接单用例；同源三分支用例；口径四处已改；预发 env 已补变量并随 rc.101 升级；缺 / 空 / 无效地址三次一次性容器演示 exit 3 不接单，#770 评论 5663907265） | V-执行-26 | `66cd4b9`；PR #779 |
| **E-7** #715 差集补齐 + 撤销回执 | 本地库内重叠：只登记缺的 N 项，卡片显示「新增 N / 沿用 M」；撤销只撤本笔新增行并触发重算与多维表格重推；回执含「经银河来源仍持有 K 项」；本地 vs 银河：登记不查银河、交付并集（既有用例不改）；合同「合成规则」「撤销」两处回写 | L2 → 预发 L4b（产品负责人点一次补授、一次撤销） | **L2 + 预发 L4b（rc.101，验证请求 #799 r1）**：A-1 补授「A财务 × 一家公司」→ `result_counts {new: 7, reused: 0}`、新组 7 行 active、`publish_outbox` v19 published；A-2 同公司「A运营」→ `{new: 2, reused: 7}`、新组 2 行、**未产生新的发布版本**（交付并集不变）；A-3 撤销第 ② 笔 → `{revoked: 2, galaxy_retained: 2}`（= 回执「经银河来源仍持有其中 2 项」），只撤该组 2 行、7 行仍 active；`pending_action` 三条 `executed`、`operation_audit` confirmed + executed 落账；合同两处已回写 | — | `311a5be`（PR #790 → `8c67fc4`）；#770 评论 5667392132；#799 评论 5675722283；#770 收口 5676302009 |
| **E-8** 拉式部署链（#566 ① ② + #567 拉取代理，预发） | 预发主机装受限通道（G-1 放行后）；拉取代理只出网、看到新候选即核对清单与摘要并调用部署器，**目标版本已在位时零动作（幂等，S-4-3 在生产回读一次）**；判定值由部署器状态账（planned / running / verified / failed）与拉取代理日志写出，不由人填；九处中断接续、并发拒绝、篡改拒绝、15 分钟观察各一次实测；首触冒烟；shell / scp / TTY 三路否定；**权限发布表**：预发写表前备份、写入行可识别、整批撤回演示一次；引导安装文档 + 四份样例 + 能力对照表合入 | L4a（预发 · 代理） | **L4a（预发 · 代理，rc.101，2026-09-15 03:06Z `verified`）**：预发三常驻服务由拉取代理 + 部署器从 v2.4.3 升到 v2.5.0-rc.101（`verified_digests` = 清单三 digest、库头 0096 → 0098、容器带三枚 `io.lingxi.*` 标签），下一轮 `already_in_place`；计划窗口过期 → `approval unknown` → 归档解除一次；篡改拒绝一次；打断 ①②③⑤⑥⑦⑧ 七处 `SIGKILL` 整 cgroup 后自动接续，**④「迁移提交前」= 部署器设计性停机（`migration_unknown_no_retry`）+ 人工续跑（产品负责人裁甲，阶段账手术留痕）**，⑨ 毫秒级不注入（单测覆盖）；并发第二代理 `concurrent_run_refused`；观察 15 分钟 52 样本；告警 `sent` 4 / 去重 17 轮 / 恢复 4；写表前守卫备份 13 份、台账 0 行、diff 0、`verify` 退出 0（预发权限发布表为独立预发表，生产正式表未涉；产品负责人裁定撤回演示不做，L-8 按机制实测收口）；受限通道三路否定 S-2-1 实测。真机接缝 ①–⑨（F7–F13 已合 main，F14 / F18 文档随本记录，F15 / F16 / F17 已随 PR #800 合 main）见 [S-2-6 预发演练记录](S-2-6-预发演练记录.md)；首触冒烟随 #796 C06。**rc.102（发布候选，2026-09-15）**：预发拉取代理计划 `82f99425` 08:10Z 计划 → 08:15Z 接续 apply → 08:31:13Z `verified`（52 样本），下一轮 `already_in_place`；此前两轮零动作拒绝 = 接缝⑩（人工改过 public-config → preflight `unplanned_service_artifact_or_config`，登记 F20（#804））/ ⑪（编排者即席命令写字节码 → prepare `operation_failed`）/ ⑫（failed 计划不接续、在途标记不释放 → `unfinished_host_deployment`，登记 F21（#805）），三者都在停任何服务之前；F17 注入点预发 systemd 起单元一轮退出 0，F15 由本次迁移头回读覆盖，F16 `resume-migration` 未触发。**F19**（外审 codex P0-4）：预发 `lingxi-relay` Match 块补 `AuthorizedKeysCommand none` / `TrustedUserCAKeys none` / `ForceCommand`，三路否定复测通过，模板与引导安装随本记录回写 | V-部署-30…33 | #770 评论 5674074895（演练记录）/ 5673409789 / 5673571816 / 5673807019 / 5673840211 / 5673916831 / 5674166451；PR #787 / #791 / #795 / #800；验证请求 #796；rc.102：5676853376 / 5676903792 / 5676920849 / 5676974398 / 5677188860；F19：5676759452 / 5676777321 |
| **E-9** #756 预发监控四小项 | 30 天收缩机制装上并首轮回读最旧样本；阈值启用回读；同候选正常任务容量峰值样本（脱敏）；停 queue 时重启 gateway 同一 task 唯一送达 | L4a | **部分 L4a**：① 收缩机制装上（候选 `push_to_monitoring.sh` + 监控库 `GRANT DELETE` 重放），每轮「收缩完成 pruned=0」，最旧样本 2026-08-29（未到窗口）；② 预发原未启用阈值、脚本为 08-28 旧版——按产品负责人「升级系统」裁定单元钉 `python3.12` 后启用 `--enable-resource-thresholds`（03:27 起每分钟一轮，无误报）；③ 容量峰值样本随 #796 C06b；④ T2「停 queue 时重启 gateway」子步待做（登记 250-w4） | — | #770 评论 5673596832 / 5674166451 / 5674231709 |
| **E-10** #735 对照 + 「申请使用」 | 同批两名受控账号：一人送达 `delivered`、停用者零行、脚本汇总跳过 1（同一次运行）；事后恢复；「申请使用是否真实授权机制」在飞书核实并留痕；验收矩阵登记改「方法已定、已执行」 | L4b | **预发 L4b（产品负责人裁定并入单账号，#799）**：同一脚本同一名单——停用期间 A-5 `--precheck` 回显 `skipped not_active`、「送达 0 / 跳过 1（共 1 人）」零发送、操作号落 `operation_audit`；恢复并预开通后 C-3 真发 `outreach_message` `delivered` 07:01:53Z；停用 / 恢复各经本人确认卡 `executed`。**「申请使用是否真实授权机制」飞书核实未做** → 去向 S-5-1；验收矩阵登记待 S-5-1 | （编排者登记） | #799 评论 5675788978 / 5676228997；#770 收口 5676302009 |
| **E-11** #675 首切片 + 三件操作 | 持久审计载体表（谁发起 / 谁确认 / 谁执行 / 目的与范围 / 结果 / 证据指针）建成并有迁移；预开通、欢迎卡、扩员三件操作接到同一具名身份与记录面；一条真实受控运营旅程（准备 → 确认 → 执行 → 回读）；凭据与业务正文不进普通日志 | L4b | **L2 + 预发 L4b（rc.101，#799 r2 C-2…C-5；账号先经 R1 复位为「从未开通」）**：C-2 扩员经受限通道准备 → 本人确认卡 → 执行（`operation_audit` `innertest.additions` prepared / confirmed / executed，`innertest_batch` executed、`innertest_membership` 1 行）→ 预开通 `onboarding.legacy_permission_import imported=4` → **`onboarding.stock_token_adopted status_approved=True`**（存量沿用，未签新令牌）→ `app_user` 重建 active / enabled v1、`publish_outbox` v1 published、预发表新建一行；C-3 欢迎卡 `delivered`；C-4 首聊直接可用；C-5 问数 `qa_corpus` success 19 秒 | — | `ea2cb49` / `d8ac140`（#788，PR #790 → `8c67fc4`）；#770 评论 5664129753 / 5667392132 / 5675969293 / 5675992710；#799 评论 5676128990 / 5676228997 |
| **E-12** #664 留存 | 合同修订 + 决策记录先合并（G-6）；正式通道表（用户实收安全正文 + 模型原文两列）、独立抗漂移启用判据与门禁、窄读取角色、读取 / 导出审计（每次一行不含正文）、按人 / 时间窗 / 关键词检索与受控导出；生产该表在升级后开始写入 | L2 → 预发 L4a → 生产回读 | **L2**（合同修订 + 决策记录 PR #786 已合 `c5aa62b`，G-6；表 0098 两列回答 + 开关写死 compose + 等价门禁 + 窄读取角色表 + 读取 / 导出审计 + 三种检索 + 受控导出 `0600`；真库 + 变异红）；容量判据与用户删除随删已回写合同（本 docs PR）。**预发 L4a + 读取 / 导出审计（rc.101，#799）**：A-7 / C-4 / C-5 三次真实对话各落 `qa_corpus` 一行（两列回答；A-7 行 `terminal_kind=success`，随阶段 B 复位删除），rc.102 版本验收单 ④ 再落一行 `user_result=obtained`；C-6 编排者段 `grant`（管理员登记为读取者）→ `read`（2 行，正文宿主侧丢弃）→ `stats` → `export`（受控根 0600 文件，sha256 留痕），读取 / 导出各一行审计不含正文。观察项：A-7 两行 `user_result=unknown`、C-4 `no_tool_call`，口径待核（S-5-1）；生产「表开始写入」待 E-14 | V-语料-01…08 | `a50e8c7` / `7217927`（PR #790 → `8c67fc4`）；PR #786；#770 评论 5667392132 / 5677188860；#799 评论 5675959608 / 5676228997 |
| **E-13** #716 管理 Agent（受限通道） | 受限 SSH 通道内的 Agent 客户端：只读查询三种 + 五种写动作的准备 → 本人真实飞书卡片确认 → 执行；普通用户不可达（否定断言）；五类稳妥性场景；AM-01–08 真人对照材料备好 | L4b | **L2**（通道升级为 Agent 客户端可用；11 工具 = 扩员 3 + 只读 3 + 准备 5，抑制不设；准备 → 本人确认卡 → 执行，账落准备 / 确认 / 取消 / 执行行；四层否定（普通用户不可达复用 V-管理-25）；五类稳妥性场景；真库 + Linux 隔离）；**预发 L4b（rc.101，#799）**：tz 经受限通道 `initialize` + `tools/list` = 11 工具；一次查询 `get_user_status`；三次写动作准备 → 本人确认卡 → 执行：A-4 `prepare_suspend_user` → `suspend_user` `executed`（`account_state=suspended`，v20）、A-6 `prepare_resume_user` → `resume_user` `executed`（`enabled`，v21，A-3 撤销不回滚）、C-2 扩员 → `innertest.additions` executed；`operation_audit` prepared / confirmed / executed 三段落账。AM-01–08 真人对照材料仍未备（未验证） | V-管理-09 / 10 / 12 / 15 / 17（总册覆盖清单已登记由 #716 用例承担，分册状态待翻） | `d053f19` / `a1e956f`（PR #790 → `8c67fc4`）；#770 评论 5667392132 / 5675123356；#799 评论 5675788978 / 5675820518 / 5676128990 |
| **E-14** 2.5.0 上生产（拉式） | `release/2.5` 候选四镜像 digest 与正式版逐一相同（清单由 CI 写）；生产部署留痕由拉取代理与部署器状态账写；真实问数 `succeeded` 由队列执行服务写入 task 表；预发由拉取代理自动部署 rc、真人验收通过、验收记录 PR 合入 main；产品负责人点 Release Promotion 后生产拉取代理自动部署（G-2 / G-3 已放行）；生产迁移头 = 链头；三服务 healthy 且 15 分钟无非预期重启；既有业务行数不变；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**；部署代理自带失败告警至少触发一次演练（预发） | **L5（只认真人）** | **待 S-4-4**（生产前置已齐：预发 rc.102 版本验收单六项全过 5677188860、验收记录 PR #802 → main `091ea14`；生产受限通道 G-2 / #604 写值 G-4 / 旧栈内测覆盖 G-2b / 拉取代理 G-3 已装并空转，两轮幂等零动作；待产品负责人手动备份 → Release Promotion → 生产自动部署 → 真人问数） | V-部署-33 | #770 评论 5677188860 / 5677785161 / 5678067028 / 5678143132 / 5678263477 / 5678300240 / 5678369073 |
| **E-15** #769 外部代理试点 + L-10 影子实施评价 | 四项试点各有五项指标记录（耗时 / 额度 / 通道故障次数 / 审核发现缺陷数 / 返工次数），与内部子代理同类任务对照；三张影子卡按本 Trace 开工合同第 5 段的六项尺子评分并出「切换 / 维持」裁定（守望者评论）；一次双外包交叉验证的分歧清单与处置；收口一条结论评论 | 全局 1（分析）+ 指标可测 | **指标已记录**（四项试点 + 影子评价裁定「切换」）；结论评论待 S-5-1 | — | #770 评论 5655309937 / 5655800302 / 5656303192 / 5657496995 |
| **E-16** 里程碑与工作单收口 | 2.5.0 里程碑 open = 0 或余项各有去向评论；#712 按 E-3 裁定关闭或收口；四处既有瑕疵（#658 / #659 双类型标签、#730 `[verify]` 字段、#712 / #743 标题「12 个」）顺手处理 | — | **本批新单去向（S-5-1 建单 / 对账）**：F20（#804） 公开配置在两次部署之间被人工改动后无「同版本重应用」入口（5676853376，2.5.1 工作项）；F21（#805） `failed` 且未越过 prepare 的计划既不接续也不释放在途标记（5676974398）；R1 复位工具 P2-1 四条用例缺口 + P2-4 飞书表「记录不存在」业务码 1254043（5675403229 / 5675992710）；20 人扩员一批（静态名单未开通者，裁甲 5678077329，G-5 后经受限通道准备）；#735「申请使用是否真实授权机制」飞书核实（5676302009）；E-1 ④⑤ 恢复路径演练未做；#756 ④ T2 子步未做；#803 凭据轮换长期待办已建 | — | #770 评论 5676853376 / 5676974398 / 5675992710 / 5678077329 / 5676302009 |

## 批终链（每个批次全绿才进下一阶段）

本机完整门禁绿（tmux 窗口跑）→ 唯一独立审核者批量审查（一次）→ 统一修复包 → 同一审核者定向复核（一次）→ 外审（按额度：codex + agy 各一轮，不同视角）→ 把关（按本 Trace 开工合同第 5 段的配比：默认 Opus xhigh，五个高风险 PR 用 Fable xhigh）→ 冻结候选 → CI 完整门禁 → 合 main。冻结后回填：**批次 1**：候选 SHA `c204a40`（树 = `09b45a8`）；门禁 run `34783181191`；外审登记行 #770 评论 5656180414；合并 `e905a65`。**批次 2**：候选 SHA `111de13`（树 `8f52e3f`）；门禁 run = PR #779 `Epic Full` + `Epic Verdict`（路径 a）；外审登记行 #770 评论 5660447208；合并 `8616bc1`。**批次 3**：候选 SHA `272da5a`（树 `ffcd1fb`；冻结空提交 `9271881` 同树）；本机 full `Ran 7274 tests OK`；Fable 把关 5668293082；外审登记行 #770 评论 5668293082（累计 14 / 14）；门禁 = PR #790 gate / image / `Epic Verdict` 绿；合并 `8c67fc4`。**W4**：候选 SHA `492805e`（F16 / F15 / F17，树 `f33abd5`；并入 main 后 `ebb8889`，树 `6b1c12e`）；Opus xhigh 把关 5676140305（P2 ×3 豁免 5676149557）；外审 = 生产前走查 5676575952 / 5676672058（16 / 16，4 条真缺口修进生产脚本）；门禁 = PR #800 `Epic Full` + `Epic Verdict` 绿；合并 `bcd0635`；同步 PR ④ #801 → `release/2.5` `176ca2e`（树 `6b1c12e` = main）→ `v2.5.0-rc.102`。

## 版本验收单（预发一次、生产固定项一次；E-14 用）

固定项：① 迁移头 = 链头；② 三服务 healthy 15 分钟；③ 既有业务行数不变；④ 一次真实问数 `succeeded`；⑤ 四镜像 digest 与正式版清单逐一相同；⑥ 部署由拉取代理 + 部署器完成的留痕（计划 / 执行 / 状态三份记录）。随版本项：#604 必配生效（缺配置启动失败在预发演示一次）；#715 一次补授 + 一次撤销；#675 一条运营旅程；#664 表开始写入；#716 一次受限通道查询。

## 未验证层级（收口时如实列）

- 规划期全部为静态回源与只读回读；预发 / 生产环境配置现值、监控库最旧样本未登机。
- `lingxi-verdict` App 的权限 / Webhook / 安装范围只有产品负责人口述，待引入 PR 首次上报自证。
- 拉取代理今天零代码，部署器零次真机运行；E-8 是第一次。
- **W4 收口时（2026-09-15，S-4-4 前）仍未验证**：E-1 ④⑤ 恢复路径演练（规则集临时去掉 `Epic Verdict` 行 → 合并 → 加回）未做；#756 ④「停 queue 时重启 gateway 同一 task 唯一送达」未做；E-13 AM-01–08 真人对照材料未备；`qa_corpus.user_result` 口径未核（预发三行 unknown / unknown / no_tool_call，rc.102 一行 obtained）；F15 / F16 真机只在预发部署路径上覆盖（`resume-migration` 从未在真机触发）；F19 只在预发与生产 G-2 脚本生效，仓库模板随本记录回写、未再装机；F20（#804） / F21（#805） 只有预发处置脚本，代码未改；生产侧 E-14 全部环节（提升、自动部署、迁移 0096 → 0098、真人问数）待 S-4-4；生产 G-4 写值只有产品负责人回显与守望者留痕（5678292846），编排者未单独留回读评论。
