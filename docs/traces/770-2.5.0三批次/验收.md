# 验收（Trace #770 · 2.5.0 三批次；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace Issue 或 PR）

> 证据等级口径见 `docs/技术设计/验证与门禁.md`（L 名 = 成熟度 · 环境 · 执行者；全局 1–7 级对照见该文第三节）。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，不得为了收口好看而标绿。**判定字段只认系统写出的值，前置状态允许人工构造；答不出「这个值是谁写的」即无效判据。**

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准 | 目标等级 | 现值 | 关联验收矩阵 | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1** #748 裁决层 | ① 引入 PR 合并后任一真实 PR 由 `lingxi-verdict[bot]` 上报 `Epic Verdict`；② 两条规则集含 `Epic Verdict` 且来源 = 该 App（产品负责人动作 D，回读）；③ 造 PR 实测：把必需检查替换为同名普通作业 → 合并被拒；改工作流的 PR 由主干版门禁自动复跑一次（D2）；④ 撤 `/.github/workflows/` 属主整行、保留只护 `verdict.yml` 的窄行（D4）后，一个不改 verdict 的工作流 PR 无需产品负责人批准即可合并；⑤ 恢复路径演练：临时去掉规则集里的 `Epic Verdict` 行 → 合并 → 加回，全程记录 | L2 + 真实 PR 实测（全局 4） | — | — | — |
| **E-2** #751 三条钉住 + 审计可见 | 三条变异各自判红（删被验证的那一行仍全绿即无效）；`outreach.py` 在容器内运行时 stderr 有审计行；`数据库设计.md` 第九节补句 | L2 | **L2**（三条变异红 + 用例；容器内 stderr 待 W2 预发运行） | — | `4dfcc7c`；#770 评论 5654521549 |
| **E-3** #743 探针 → #712 裁定 | 只改 `test_roster_audit_postgres.py` 清场方式后 `--shards 2` 一次跑批，失败签名消失 / 未消失都有 run 记录；据此请产品负责人裁「继续 / 保留旧路径收口」并留痕 | L3（本机）| **L3**（签名消失；分片 0 另露两条隔离面失败）；裁定 = 收口 | — | `f02280a`；#770 评论 5655294503 ① / 5657479164 |
| **E-4** #765 制品核验 | containerd 存储机器（tz）与 CI 双侧核验结论一致；既有失败分支逐条仍在（否定断言） | L2 + 本机实测 | **L2 + 本机实测**（containerd 一致 / 不一致；真实 PR #759 候选包通过）；CI overlay2 侧未实机复核 | V-部署-（编排者登记） | `7661245`；#770 评论 5655107074 |
| **E-5** #658 core 平移 + #708 P2-4 修法 + #659 第一步 | #658 两个 PR 行为零变化：既有测试不改断言全绿 + 两处变异哨兵；棘轮基线只缩不增；#708：构造非内测待确认动作，扩员准备不再返回「确认已过期」，原过期判定仍红；#659 第一步：固定样本走两个入口结论逐条相同的对照测试 + 预发每日批一轮 + 一次管理员授权双通道回读 | L2；#659 第一步 L4a | **部分**：#658 L2（零变化 AST 逐字比对、变异哨兵红）；#708 L2（新码 `target_has_pending_action`，产品负责人接受）；#659 第一步 L2（对照测试与预发回读归 W2 S-2-4，未做） | — | `5ee373f` / `055ae14` / `f83a97f` / `03306d1`；#770 评论 5656130411 |
| **E-6** #604 必配 | 缺 / 空 / 无效地址 → 队列执行服务启动失败退出码 3、不接单（用例）；同源 / 不同源 / 同源不同路径三分支用例；预发 env 先补变量再升级镜像并回读；生产 env 写值在 G-4 放行后回读（值不回显） | L2 → 预发 L4a → 生产「真实链路 · 生产 · 代理 · 只读」 | — | V-执行-26 | — |
| **E-7** #715 差集补齐 + 撤销回执 | 本地库内重叠：只登记缺的 N 项，卡片显示「新增 N / 沿用 M」；撤销只撤本笔新增行并触发重算与多维表格重推；回执含「经银河来源仍持有 K 项」；本地 vs 银河：登记不查银河、交付并集（既有用例不改）；合同「合成规则」「撤销」两处回写 | L2 → 预发 L4b（产品负责人点一次补授、一次撤销） | — | — | — |
| **E-8** 拉式部署链（#566 ① ② + #567 拉取代理，预发） | 预发主机装受限通道（G-1 放行后）；拉取代理只出网、看到新候选即核对清单与摘要并调用部署器，**目标版本已在位时零动作（幂等，S-4-3 在生产回读一次）**；判定值由部署器状态账（planned / running / verified / failed）与拉取代理日志写出，不由人填；九处中断接续、并发拒绝、篡改拒绝、15 分钟观察各一次实测；首触冒烟；shell / scp / TTY 三路否定；**权限发布表**：预发写表前备份、写入行可识别、整批撤回演示一次；引导安装文档 + 四份样例 + 能力对照表合入 | L4a（预发 · 代理） | — | V-部署-30…33 | — |
| **E-9** #756 预发监控四小项 | 30 天收缩机制装上并首轮回读最旧样本；阈值启用回读；同候选正常任务容量峰值样本（脱敏）；停 queue 时重启 gateway 同一 task 唯一送达 | L4a | — | — | — |
| **E-10** #735 对照 + 「申请使用」 | 同批两名受控账号：一人送达 `delivered`、停用者零行、脚本汇总跳过 1（同一次运行）；事后恢复；「申请使用是否真实授权机制」在飞书核实并留痕；验收矩阵登记改「方法已定、已执行」 | L4b | — | （编排者登记） | — |
| **E-11** #675 首切片 + 三件操作 | 持久审计载体表（谁发起 / 谁确认 / 谁执行 / 目的与范围 / 结果 / 证据指针）建成并有迁移；预开通、欢迎卡、扩员三件操作接到同一具名身份与记录面；一条真实受控运营旅程（准备 → 确认 → 执行 → 回读）；凭据与业务正文不进普通日志 | L4b | — | — | — |
| **E-12** #664 留存 | 合同修订 + 决策记录先合并（G-6）；正式通道表（用户实收安全正文 + 模型原文两列）、独立抗漂移启用判据与门禁、窄读取角色、读取 / 导出审计（每次一行不含正文）、按人 / 时间窗 / 关键词检索与受控导出；生产该表在升级后开始写入 | L2 → 预发 L4a → 生产回读 | — | — | — |
| **E-13** #716 管理 Agent（受限通道） | 受限 SSH 通道内的 Agent 客户端：只读查询三种 + 五种写动作的准备 → 本人真实飞书卡片确认 → 执行；普通用户不可达（否定断言）；五类稳妥性场景；AM-01–08 真人对照材料备好 | L4b | — | — | — |
| **E-14** 2.5.0 上生产（拉式） | `release/2.5` 候选四镜像 digest 与正式版逐一相同（清单由 CI 写）；生产部署留痕由拉取代理与部署器状态账写；真实问数 `succeeded` 由队列执行服务写入 task 表；预发由拉取代理自动部署 rc、真人验收通过、验收记录 PR 合入 main；产品负责人点 Release Promotion 后生产拉取代理自动部署（G-2 / G-3 已放行）；生产迁移头 = 链头；三服务 healthy 且 15 分钟无非预期重启；既有业务行数不变；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**；部署代理自带失败告警至少触发一次演练（预发） | **L5（只认真人）** | — | V-部署-33 | — |
| **E-15** #769 外部代理试点 + L-10 影子实施评价 | 四项试点各有五项指标记录（耗时 / 额度 / 通道故障次数 / 审核发现缺陷数 / 返工次数），与内部子代理同类任务对照；三张影子卡按本 Trace 开工合同第 5 段的六项尺子评分并出「切换 / 维持」裁定（守望者评论）；一次双外包交叉验证的分歧清单与处置；收口一条结论评论 | 全局 1（分析）+ 指标可测 | **指标已记录**（四项试点 + 影子评价裁定「切换」）；结论评论待 S-5-1 | — | #770 评论 5655309937 / 5655800302 / 5656303192 / 5657496995 |
| **E-16** 里程碑与工作单收口 | 2.5.0 里程碑 open = 0 或余项各有去向评论；#712 按 E-3 裁定关闭或收口；四处既有瑕疵（#658 / #659 双类型标签、#730 `[verify]` 字段、#712 / #743 标题「12 个」）顺手处理 | — | — | — | — |

## 批终链（每个批次全绿才进下一阶段）

本机完整门禁绿（tmux 窗口跑）→ 唯一独立审核者批量审查（一次）→ 统一修复包 → 同一审核者定向复核（一次）→ 外审（按额度：codex + agy 各一轮，不同视角）→ 把关（按本 Trace 开工合同第 5 段的配比：默认 Opus xhigh，五个高风险 PR 用 Fable xhigh）→ 冻结候选 → CI 完整门禁 → 合 main。冻结后回填：**批次 1**：候选 SHA `c204a40`（树 = `09b45a8`）；门禁 run `34783181191`；外审登记行 #770 评论 5656180414；合并 `e905a65`。批次 2：候选 SHA `________`；门禁 run `________`；外审登记行 `________`。

## 版本验收单（预发一次、生产固定项一次；E-14 用）

固定项：① 迁移头 = 链头；② 三服务 healthy 15 分钟；③ 既有业务行数不变；④ 一次真实问数 `succeeded`；⑤ 四镜像 digest 与正式版清单逐一相同；⑥ 部署由拉取代理 + 部署器完成的留痕（计划 / 执行 / 状态三份记录）。随版本项：#604 必配生效（缺配置启动失败在预发演示一次）；#715 一次补授 + 一次撤销；#675 一条运营旅程；#664 表开始写入；#716 一次受限通道查询。

## 未验证层级（收口时如实列）

- 规划期全部为静态回源与只读回读；预发 / 生产环境配置现值、监控库最旧样本未登机。
- `lingxi-verdict` App 的权限 / Webhook / 安装范围只有产品负责人口述，待引入 PR 首次上报自证。
- 拉取代理今天零代码，部署器零次真机运行；E-8 是第一次。
