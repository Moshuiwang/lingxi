# 开工快照：2.5.0 三批次（Trace #770）

> 按 `#678` v20 r1 §二与 `docs/traces/README.md`：开工能力、额度与环境快照类内容放本文件。**授权、验收门槛、已裁定事项、共享资源租约、外部写与恢复、各道 Gate 一律留在 `合同.md`，不得移出以绕过批准。**

## 合同区字段

- 方法版本 **v20 r1**（[#678](https://github.com/Moshuiwang/lingxi/issues/678)，2026-09-13 发布）；合同版本 **v1**；执行阶段 **Planning → 待批准**（合同 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- **执行者与运行面**：Claude Code 运行面；执行机 tz（Ubuntu 24.04）；**本机时钟是 UTC**，面向产品负责人的时刻一律 UTC + 北京（+8）双标注且两个值都实读（`date -u` 与 `TZ=Asia/Shanghai date`）。
- **基线提交与核对时间**：`origin/main` = **`00c9464`**（2026-09-13 14:21Z 实读，工作树干净、无开放 PR）；`origin/release/2.4` 头 `64af8c4`（= v2.4.3）；生产现网 **v2.4.3 / 迁移头 `0096`**；预发三服务只停不删，rc.98 四镜像保留；`pyproject.version` = 2.4.3（主干开发版本号在 S-1-1 抬到 2.5.0）。
- **规划者任务 / 会话与停止点**：规划者会话在主检出 `~/project/lingxi`（tmux `TZ-server:250-plan`），规划工作树 `.claude/worktrees/plan-770`（分支 `plan/trace-770-2.5.0`）。停止点 = 合同 v1 合并；**随后转任守望者**（合同 §6，裁定 L-4）——不实施、不持编排权与合并权，只以外部证据判活、承接并转发裁定、失联时取证后拉继任。
- **角色模型 / 推理档位**：同合同 §5。
- **当前顶层编排任务 / 会话**：尚未拉起；合同合并后由守望者拉起 tmux `TZ-server:250-w1`（工作树 `.claude/worktrees/250-w1`，编排 opus max）；接管登记见 Trace Issue。
- **编排者接手入口与第一恢复点**：`/trace-kit:takeover` → 读本目录四份文件 + Trace Issue 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + Trace Issue 最新评论**。

## 开工能力与额度快照（v20 r1 新增；全部 2026-09-13 14:2xZ 实读或实机测试）

| 项 | 值 | 怎么查（可复用） |
| --- | --- | --- |
| 当前主编码代理 | Claude Code **2.1.270**；本规划会话 Fable 5.1 **xhigh**；本机默认档位 xhigh（`~/.claude/settings.json` effortLevel） | `claude --version`；会话状态行 |
| Claude 额度（采样 14:22Z / 22:22 北京） | 5 小时窗 **13%**（22:30 北京重置）；7 天窗 **43%**（**周四 09-17 07:00 北京**重置）；Fable **33%**（同一重置） | tmux 状态栏右侧橙底段；原始值 `~/.claude/tmux-claude-usage-api.dat`（七列：时刻 5h% 5h重置 7d% 7d重置 Fable% Fable重置，epoch 秒），由 `tmux-claude-usage-poll.sh` 每 5 分钟从官方用量接口拉取；CLI 本身无额度查询命令 |
| 外审一：codex **0.154.0** | 模型 **`gpt-6-astra`**，档位 high（产品负责人指定）；**实机测试 2026-09-13 14:42Z：13 秒、退出码 0、回话「就绪 gpt-6-astra high」**；回退 `gpt-5.6-sol` high（14:21Z 实测 14 秒、退出码 0）；`config.toml` 默认模型 gpt-5.6-luna / max（显式传模型，不靠默认） | 调用姿势：`printf '%s' "<提示>" \| codex exec -s read-only -c model="gpt-6-astra" -c model_reasoning_effort="high" -`，输出直写文件；证据内联进提示、明示不跑命令；`--json` 只给每回合 token 用量 |
| codex 额度（采样 14:23Z） | 7 天窗**已用 5%**（**周六 09-19 19:10 北京**重置）；5 小时窗无数据 | tmux 状态栏右侧蓝底段；原始值 `~/.local/state/tmux-codex-quota/tmux-codex-usage.dat`（五列：时刻 5h% 5h重置 7d% 7d重置），由 `~/.codex/hooks.json` 的会话启动 / 结束钩子刷新——跑一次 `codex exec` 即触发更新；协议层另有 app-server `account/rateLimits/read`（schema 实读存在，本次未完整走通） |
| 外包实施：codex | 模型 **`gpt-5.6-luna`**，档位 max（产品负责人指定，#769 试点的小编码任务）；**实机测试 2026-09-13 14:42Z：11 秒、退出码 0、回话「就绪 gpt-5.6-luna max」** | 同上姿势，沙箱按派发卡（独立工作树内 `-s workspace-write`），改完先提交再汇报；只读核实类任务改用 agy |
| 外审二：agy **1.2.1**（Antigravity CLI） | 模型 **`gemini-3.8-flash-high`**（档位在模型名里，**不传 `--effort`**）；**实机测试 2026-09-13 14:21Z：10 秒、退出码 0、回话「就绪 gemini-3.8-flash-high」**；14 个模型可选（含 Claude / GPT 系） | 调用姿势：`agy --add-dir <工作树> --model gemini-3.8-flash-high --print-timeout 600s --print='<提示>'`（`--print=` 带等号、`--model` 在前）；判成功只认 stdout 非空；白名单外命令静默零输出，审核提示不要让它跑测试 |
| agy 额度（14:24Z 实读） | Gemini 系：**周额度剩 98%**（09-18 12:00 北京重置）、五小时窗剩 99%；Claude / GPT 系经 agy：周与五小时均 **100%** | `agy --print='/usage'`（打印模式支持只读斜杠命令，四行制表输出） |
| 外审频率（按额度定，v20 r1） | **额度充裕**：三家周窗都在半程以下。安排 = 每个批次终点 codex + agy 各一轮（不同视角：codex 绕过面与越权路径，agy 产品承诺与用户可见后果）+ #748 引入 PR 加一轮 codex + 生产前走查一轮 + #769 双外包交叉验证 1–2 次；预估 **10** 人次 / 上限 **14**。**触发降档**：任一家周窗用量 ≥ 80% 或 5 小时窗 ≥ 90% 时，该家只保留批次终点一轮；两家都触发则只在最终 PR 前各一轮 | 每次外审当场在任务表或阶段评论登记：时间、工具与模型、候选 SHA、卡号、结果条数（v20 §五对账） |

## 开工能力与环境快照（其余）

| 项 | 值 | 来源 |
| --- | --- | --- |
| 发布路径 | `publish.yml` 触发于推送 `release/**`（候选 job 用 `verify_epic_candidate.py` 要求该提交恰有一个已合并、base 为该 release 分支的 PR）；`release.yml` 手动 `workflow_dispatch`（分支 main，填候选标签）；建 `release/2.5` 的基点必须是**带 `Epic Full` 成功记录的 PR 头提交**（规则集 22523565 `do_not_enforce_on_create=false`） | 仓库实读；#754 W2 实测 |
| 机器人权限边界 | `gh run rerun` / `workflow run` 403；`accept/` 与 `record/` 前缀可推；`deploy/releases/acceptance/`、`docs/traces/*/合同.md`、`.github/workflows/` 受 CODEOWNERS；机器人可建 `release/X.Y`（前提同上） | #669 评论 5595436970；#754 复盘 |
| #748 前置现值 | App `lingxi-verdict` 已建并装到 lingxi（Checks 读写 / Metadata 只读 / Webhook 关，**产品负责人口述、机器人 403 未核**）；Environment `verdict` 仅 `main`、无审核人、无等待（**已核**）；`VERDICT_APP_ID`（11:14Z）/ `VERDICT_APP_PRIVATE_KEY`（11:30Z）在 Environment 级（**已核**） | 2026-09-13 只读回读 |
| CI 成本 | `gate` 9–10 分 / 15 分上限（余量约 30%，#743 探针前不并行）；`image` 4–5 分；纯 docs PR 约 13 秒；`deploy/**` 或 `.github/**` 路径走完整门禁；`trace/**` 前缀把纯记录 PR 也路由成完整门禁 → 记录 PR 用 `record/` 前缀 | 记忆与 #754 实测 |
| 预发 | 三服务只停不删（`--profile mvp down`），rc.98 四镜像、卷、env 保留；口令已于 #754 轮换；`pg_dump` 客户端 16 对 Supabase 17 须用 `postgres:17` 容器；同一测试机器人只允许一个客户端 | #754 终态 |
| 生产 | v2.4.3 / 0096；回滚点 `.env.prod.before-243-v2.4.3` + 迁移前 dump（删除属显式除外，待产品负责人）；托管备份点可列出、恢复到新项目演练 2026-09-13 成功、Owner 具备恢复权限、PITR 未验（#567 评论 5653761783） | #754 终态；#567 |
| 权限发布表与问数 MCP | **预发与生产共用同一问数 MCP 与同一张权限发布表（产品负责人 2026-09-13 确认）**；预发写表前备份、写入行可识别、可整批撤回是预发自动化的硬前提（合同 §4 判据 3） | 产品负责人裁定 |
| 受限通道现状 | 预发与生产均未安装（部署器 `plan` 步硬依赖；裁定 9 管理 Agent 入口也依赖） | G2 分诊 |
| tmux / 工作树 | `TZ-server` 常驻；本会话窗口 `250-plan`；**会话一律在主检出拉起，隔离工作树放主检出的 `.claude/worktrees/`，不放系统临时目录**（产品负责人 2026-09-13；已写进本 Trace 开工合同第 6 段主线声明） | 本机现场 |
| 分片路径 | `check.sh full --shards N` 默认关，首跑即红（#743）；本机 full 约 15–16 分钟，须在 tmux 窗口跑（看门狗看 free） | #732 / #754 |

## 批后继任条件（命中）

三个批次加上生产发布必然跨会话接力。继任：读本目录四份文件 + Trace Issue 最新交接评论 → 核实现场（工作树、批次分支 tip、PR 状态、预发与生产现状、额度余量）→ 接管登记 → 前任确认停止写入并释放租约 → 退场。**未确认转移前保持等待，不得出现两个写入者。** 守望者失效时（窗口关闭即两类机制全失效）由产品负责人在主检出重新拉起守望者会话，接手 prompt 见 Trace Issue 最新交接评论。
