# 开工合同：清仓批一（Trace #630，Issue 链接：https://github.com/Moshuiwang/lingxi/issues/630）

> 方法版本：`METHOD.md`（[#147](https://github.com/Moshuiwang/lingxi/issues/147)）**v17**（2026-09-06，32,876 B）。合同版本：**v1**。批准依据 = 产品负责人 2026-09-06 13:0x 北京裁定本批范围（原话要点：「第一批 清仓（纯文档，不碰生产）：#600 查因收尾并另立修复卡 · #591 docs 对账 · #597 清「腿」字与用词门禁 · #576 / #603 / #604 处置留痕」；#566 与生产备份恢复演练归第二批；#623 本体不碰）。**本 PR 合并即批准 v1**。
> 一条获批合同 = 编排者长程自治的全部授权依据；未写进合同的授权不存在。
> **合同只认两种来源**：产品负责人裁定（原话 + 时间）与输入 Issue 的可观察完成标准。上一任执行者自设的约束（一次性额度、重型 harness、审核轮次、工具选择）不是合同——继任者按卡面重估，不继承。

## 1. 目标

- **用户结果一句话**：把三件已经查清但没落地的账收干净——生产内存那条「要 OOM 了」的假警报正式作废并留下正确的看法、代码可读性战役的文档账对平、以及仓库里 41 行看不懂的「腿」字清零且以后写不进来。
- **里程碑（何谓「到了」）**：#600 关闭且修复方向另立卡；#591 的 docs 尾项全部落地（卡本身按 K-8 抽读留开）；#597 存量清零 + 用词门禁 fail-closed 上线并变异验红；#576 / #603 / #604 三张卡各有一条带产品负责人裁定依据的处置留痕。
- **完成定义（哪些 Issue / 断言收口即 Delivered）**：#600 Closed（另立修复卡 Open）；#597 Closed；#591 三项 docs 尾项完成并留状态评论（**卡留开**）；#576 / #603 / #604 按裁定项 1/2/3 的结论各自处置完毕（关闭或留状态评论）。
- **既定条款**：交付终点按 `METHOD.md` §3.2 逐项声明——PR 合并、组件存在、CI 通过都不自动升级实际终点。本批四个 Epic 的计划交付终点分别是 `Research / Decision Result`（E-1、E-4）与 `Engineering Result`（E-2、E-3），**没有任何一项声称用户可见能力变化或生产结果**。

## 2. 授权终点

- **编排者可自动执行到**：合并批次 PR ☑（面向 `main`，唯一合并人 = 当任编排者；含 CODEOWNERS 保护文件的 PR 除外，见下）／ 部署预发环境 ☐（本批不部署）／ 其他：**GitHub 工作项写入**——按本合同与获批裁定项修订 #600 / #591 / #597 / #576 / #603 / #604 正文与评论、关闭其中已达完成定义者、新建一张 `[bug]` 修复卡与（若裁定项 4 判「跑」）一张实验卡；更新看板 #547 线 C。
- **显式除外（每次单独请示）**：生产写入（含只读 ssh 进 `biplus-prod` 之外的任何动作）、stage 启停与任何占用、权限变更、对外发布、删除仓库文件或远端分支、修改 #147 正文、改动 `src/` 与产品代码、改动 `scripts/ops/monitoring/` 采集脚本、启动 #566 与生产备份恢复演练、启动 #623 本体。
- **CODEOWNERS 例外（本批必然命中，不是意外）**：`docs/traces/**/合同.md` 由 `@Moshuiwang` 独占，**只能由产品负责人本人 Approve 后合并**。本批有两个 PR 命中：①本三件套 PR（新建 `630-清仓批一/合同.md`）；②#597 存量清理 PR（要改 `docs/traces/469-rc22打磨与体验批/合同.md` 与 `docs/traces/521-rc24正式上线/合同.md` 各含「腿」字，除非裁定项 5 判「不改」）。编排者**不得**用 `--admin` 或任何方式绕过，只能等产品负责人 Approve。
- **生产 / 外部系统动作的执行通路**：本批**无生产动作**。GitHub 写入由编排者在 tz 本机用 `gh`（机器人身份 `Moshui Claude Code tz[bot]`，每次调用短期令牌，不落盘）；仓库推送用 `git-claude-tz push`，不用裸 `git push`。
- **常设授权引用（项目已登记项）**：满足既定条件的 `gh pr ready` / `gh pr merge` 不必逐次请示（产品负责人已登记「以后不要再问」）——但**该登记不覆盖 CODEOWNERS 保护的 PR**，那两个 PR 仍须等产品负责人本人 Approve。
- **既定条款**：决策攒清单一次提交；裁定项编号并带默认值，**不回按默认执行；产品负责人怎么回、回多长都有效，执行方不得要求特定回复格式**（`METHOD.md` §6.3）。临时授权带失效时点，到点失效不得滚动沿用（`METHOD.md` §6.7）。

## 3. 不做清单

- **本 Trace 明确不做**：
  - **#566 幂等部署脚本**与**生产备份恢复演练**——归第二批（占 stage、碰生产部署路径、需产品负责人在场放行，且会与 09-08 块 Z-2 抢同一个 stage 租约）。
  - **#623 七专题整改本体**——它挂 `后置`，启动条件是产品负责人明确要求；本批只对 #603 / #604 做「是否并入 #623」的处置留痕，不代它编制、不接管其 Owner。
  - **#600 的修复实施**——本批只另立修复卡（方向：`PYTHONDONTWRITEBYTECODE=1` 或预编译标准库），改镜像走发布，不在本批做。
  - **任何生产与 stage 动作**——不启停、不部署、不采样、不连库。生产现状（v2.2.0、三容器 `RestartCount` 0）在本批全程只作为既有事实引用，不重新取证。
  - **#591 的整卡关闭**——卡在人类同事 K-8 抽读，那不是代理能推的（产品负责人 2026-09-05 07:0x「不用这个了，回头再读」）。本批把 docs 尾项做完并如实写明卡为何留开。
  - **改动 `src/`、`tests/` 的断言语义、`scripts/ops/monitoring/` 采集脚本**——#597 只改注释措辞，一个字符都不碰行为与断言语义。
  - **清理「腿」以外的其他英文直译词**——发现了只登记（#597 非范围）。
- **既定条款**：新发现问题默认只登记不立项——主线灯塔：不做它，里程碑能不能达成？（`METHOD.md` §6.2）
- **既定条款**：疑问句只答不动手；产品品味类修订夜间只出草案不落地。

## 4. 验收标准

**可观察完成标准**（逐条可勾选；细则见 [`验收.md`](验收.md)）：

- **E-1 #600 查因收尾**：#600 正文四处被推翻的判断逐条改写并标注推翻依据；监控口径由「`docker stats` 总量直线」改为「`anon` / `slab_reclaimable` / `memory.events` 分项」；根因结论（只读根文件系统 + 未设 `PYTHONDONTWRITEBYTECODE` → 健康检查每次启 Python 写 pyc 被 EROFS 拒 → negative dentry 累积，每次恰 74,192 B）固化进正文；#600 Closed 并带对账表；修复卡 Open 且写明方向与「改镜像走发布」的边界；看板 #547 线 C 同步新口径。
- **E-2 #591 docs 对账**：`docs/技术设计/验收矩阵-部署与迁移.md` 的 `V-部署-23 / 24 / 25` 由「已认领」改「已验证」并各附 #592 证据指针；`docs/traces/592-代码可读性/任务表.md` V-1c 行的元守护评论号勘误 `5547344691` → `5547375862`；#591 留一条状态评论写明「docs 尾项已清、卡因 K-8 抽读留开」。
- **E-3 #597 用词治理**：`grep -rn "腿" docs .github scripts src tests migrations` 为 0；`scripts/ci/check_docs_wording.py` 新增且 fail-closed，挂进 `scripts/ci/verify_docs.sh`；**在任一 `.md` 临时写入「一腿」能让它判红**（变异实测记录进 PR 正文）；`AGENTS.md` 工作底线加一行禁用英文直译词并给出替换表入口；替换表落 `docs/技术设计/代码规范.md`；**存量清零与开闸在同一个 PR 内完成**（先清后开，否则判红）。
- **E-4 三张卡处置留痕**：#576 / #603 / #604 各有一条评论，写明处置结论、产品负责人裁定依据（裁定项 1 / 2 / 3 的回复或默认值）、以及去向（关闭 / 并入 #623 / 留开等外部提供方）。

**证据等级要求**：关键路径至少 **L3（本地测试通过）**——具体为 `scripts/dev/check.sh docs` 本机绿 + CI 完整门禁绿（**L4**）；#597 的门禁生效性额外要求**变异验红**实测。**本批不产生 L5 / L6 / L7 结论**：不部署、不碰生产、无真实用户旅程。#600 的根因结论沿用 codex 2026-09-06 核查的既有证据等级（生产只读分项采样 + 本机同源基础镜像隔离对照），**本批不重新取证、不升级其等级**；「未做旧制品长时对照」这条边界原样保留。

- **既定条款**：用户可见断言双通道观测（`METHOD.md` §4.6）——**不适用：本批无用户可见断言**（纯文档与 Issue 治理，用户可见行为零变化）。
- **既定条款**：**证据只认仓库 / GitHub 留痕**——审核结论、失败事件一律当天落对应 Issue 评论或 PR；只存临时目录、会话或交接词的状态视为不存在（`METHOD.md` §6.7）。
- **既定条款**：**产品负责人真人验证块**只放两处——实施最前（前置输入 / 裁定，一次给齐）或 Trace 最后（收口验收）；中途不请产品负责人试错。**本批的产品负责人参与只有三处**：①批准本合同（含九条裁定项）；②Approve 两个 CODEOWNERS PR；③收口确认。**没有真人验证块**——本批无用户旅程可验。

## 5. 成本预算

- **完整门禁次数上限：≤ 3 次**，仅批次终点消费（`METHOD.md` §6.3 金字塔频率纪律）。依据：`.github/workflows/` 与 `scripts/ci/` 都在 `classify_story_changes.py` 的 `FULL_PREFIXES` 里，#597 必然提级到完整门禁；本三件套 PR 与 E-1 / E-4 的纯 Issue 写作不消耗。**时间窗：2026-09-06 14:00 北京 → 2026-09-08 09:00 北京**（含跨夜与否：**不含**——夜间不作业，09-08 09:00 是硬让位点，见 §6）。
- 产品负责人批的是本段上限；配比为规划者建议值，**怎么回都算数**；切换规则按 `METHOD.md` §3.3 不变。
- **模型配比**（产品负责人 2026-09-06 两次裁定合并；后一次覆盖前一次的「审核一律 Opus 5」）：
  - **编排者**：**Opus 5 / xhigh**（Claude Code，常驻 tmux session + window `clr-b1`）。**不外移**——编排者持工作树、合并权与三件套写入权，`METHOD.md` §6.8 的接力与元守护机制绑在该形态上；换厂商等于换 `execution_adapter`，属合同修订不属成本优化。
  - **实施 / 复修**：**Opus 5 / high**（按产品负责人 2026-09-06 裁定）。机械项是否降 Sonnet 5 见裁定项 7，默认不降。
  - **唯一独立审核者**：**Opus 5 / xhigh**（Claude 子代理）。持唯一缺陷账本与定向复核权，不参与候选实现、不直接修复。
  - **规划者 / 元守护**：规划者 = 本会话 Opus 5 / xhigh，window `clr-plan`，停止点见合同区。**元守护本批默认不设**（单批次、预计当日收口）；若跨到 09-08 与 `ux-b2` 并行则必须设，见 §6 与裁定项 8。
  - **Fable 5.1：本批一次不用**（额度已用 86%，2026-09-10 07:00 北京重置，留给关键 review）。
- **外部审查配额与指定位置**（产品负责人 2026-09-06 放宽至**两名外部审核者**；两者**都不占**唯一独立审核者位，`METHOD.md` §6.6）：
  - **codex `gpt-6-astra`，`model_reasoning_effort=medium`，≤ 2 轮**。指定位置：**E-3 门禁绕过面**——`check_docs_wording.py` 的 fail-closed 是否真 fail、正则能否被绕过、存量是否真清零、`verify_docs.sh` 接线是否可跳过。选它的依据是同族 gpt-5.6 在 #592 审门禁脚本抓到 8 条 P1，且历史两轮都抓到内部全体漏掉的绕过面。**边界如实：`gpt-6-astra` 在本仓零实测**（2026-09-06 规划者真实调用确认可用、模型 ID 必须带 `gpt-6-` 前缀，仅此而已）；能力只有传闻，因此**不给它唯一审核位并预置回退**：挂死或结论明显不可用即退回 Opus 5 xhigh，本批当作对它的一次标定跑，读数落收口评论供下一批引用。
  - **agy `gemini-3.8-flash-high`，无头（`--print='…'`，`--model` 放前面），≤ 3 轮**。指定位置：**事实一致性冷读**——(a) #600 改写后的正文与 codex 核查报告是否逐条一致、有无夸大证据等级；(b) #597 替换后 41 行中文是否通顺、有没有把「腿」换成更别扭的词。**信任级别明确下调**（产品负责人 2026-09-06：「agy 的模型能力一般，你不要全信」）：它的任何结论**必须由编排者用 `grep` / `git diff` 回源坐实后才能进缺陷账本**，未坐实的一律不派修复、不计轮次；探针**禁用工具**（给工具会跑偏，#592 七轮实测）。
- **子代理人次上限：≤ 12**；每次派发编排者自挂兜底观察（预计时长 + 超时查外部证据）；**子代理不得再派子代理**（`METHOD.md` §6.4）。subagent 报告「在等 X」时必须当场挂编排者自己的兜底观察，绝不裸等。
- **既定条款**：大 workflow 开跑前查配额余量；对抗验证分批派。本批**不跑 workflow**。

## 6. 主线声明

- **本 Trace 的主线是**：把 #600 / #591 / #597 三笔已查清但未落地的账收干净，并给 #576 / #603 / #604 三张卡各留一条有裁定依据的去向。任何派发前先问「不做它主线是否受阻」。
- **`execution_adapter`**：`claude_tmux_named_session`——常驻 tmux session + 批次代号 window **`clr-b1`**（拉起姿势见 `guardian` skill）；规划者 window 为 `clr-plan`。**元守护 = 本批默认不设**（治理批 #624 元守护已退场，本批单批次预计当日收口）；触发设立的条件见下一条。
- **硬边界：与 `ux-b2` 互斥，谁先谁后写死如下。**
  - **09-06 / 09-07 本批优先**：产品负责人 2026-09-08 尚未放行块 Z-2，此窗口内 `clr-b1` 是**唯一活动编排者与唯一共享写入者**（tz 主工作树 + `main` 合并道）。
  - **09-08 09:00 北京起 `ux-b2` 优先**：产品负责人一旦对 #606 块 Z-2 说「批」，**`clr-b1` 立即让位**——按 `METHOD.md` §6.8 固定序列写收口评论 → 自足交接评论 → 退场，**不与 `ux-b2` 并行**。剩余 Step 转入 Z-2 之后的续批，由续批编排者按本合同继续（合同不失效、不需重批）。
  - 让位判据只认外部证据：#606 上产品负责人的「批」评论。**不得**由本批编排者自行判断「Z-2 还没开始所以我可以继续」。
  - 若产品负责人明确要求两批并行（非默认），才设元守护并由其持唯一性检查——见裁定项 8。
- **单一写入者**：当任编排者持 tz 主工作树与合并权；实施子代理各用独立 worktree，**改完先 commit 再汇报**；审核子代理只读固定候选；面向 `main` 的 PR **串行合并**。本批远端 46 个历史分支不动。
- **既定条款**：默认攒批进批次分支（子改动只跑快检），批次终点一次完整门禁 + 一次批量审查 + 唯一合并人合并（`METHOD.md` §6.3）；批次收口固定序列（收口评论 → 交接评论 → 继任 window → 接管登记 → 租约转移 → 退场，`METHOD.md` §6.8）。
- **收尾必含**：环境清理与残留盘点（临时进程 / 容器 / worktree / 远端分支谁建谁清）、任务表勾选回写、批次收口评论（UTC + 北京双标注 + 预算消耗 + 完整门禁次数 / commit 数 / 子代理人次 / 外审轮次）。**本批额外必含**：`gpt-6-astra` 首次使用的标定读数（抓到几条、其中几条经编排者回源坐实、与内部审核者的重叠率），供下一批决定是否继续用它。

---

## 合同区（`METHOD.md` §八字段；各表条件触发，未命中不写）

- 方法版本 **v17**；合同版本 **v1**；执行阶段 **Ready**（本 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- **基线提交与核对时间**：`main = 08e00f5`（Merge PR #629），2026-09-06 13:3x 北京由规划者核对；工作区干净、worktree 只剩主树、开放 PR 0、本地分支只剩 `main`、远端 46 个历史分支未动。
- **规划者任务 / 会话与停止点**：本会话（Claude Code，window `clr-plan`，Opus 5 xhigh）。**停止点 = 三件套 PR 开出并报告 Issue 号与 PR 号**；不转编排者、不开始实施（`METHOD.md` §一第 6 步）。
- **角色模型 / 推理档位**：同 §5。
- **当前顶层编排任务 / 会话、工作目录与版本快照**：**尚未创建**——产品负责人批准本合同后由全新独立顶层会话在 window `clr-b1` 接手，工作目录 `/home/wangzp/project/lingxi`，基线 `08e00f5`。
- **编排者接手入口与第一恢复点**：`/trace-kit:takeover` → 读本三件套 + Trace #630 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = [`任务表.md`](任务表.md) 勾选 + #630 最新评论**。

### 现场合同来源与共享事实包

| 来源 | 版本 / 时间 | 本批用到的结论 |
| --- | --- | --- |
| `METHOD.md` [#147](https://github.com/Moshuiwang/lingxi/issues/147) | **v17**，2026-09-06，32,876 B | 六段式合同、条件触发合同区、依赖自检两问、裁定项编号带默认值 |
| `AGENTS.md` | `08e00f5` | 工作底线；E-3 要在「工作底线」加一行禁用直译词 |
| [`docs/traces/README.md`](../README.md) | `08e00f5` | 三件套载体规范；任务表回写仍要走 PR（`main` 分支保护，直推 `GH013`） |
| `.github/CODEOWNERS` | `08e00f5` | `docs/traces/**/合同.md @Moshuiwang`——只覆盖合同文件，任务表 / 验收不受影响 |
| [#600](https://github.com/Moshuiwang/lingxi/issues/600) codex 核查评论 | 2026-09-06 06:00–06:07 北京 | 根因链、四处应修正的判断、监控口径建议、修复方向 |
| [#592](https://github.com/Moshuiwang/lingxi/issues/592) 收口 / [#591](https://github.com/Moshuiwang/lingxi/issues/591) 收尾说明 | 2026-09-05 | E-2 三项 docs 尾项的确切内容与指针勘误值 |
| [#597](https://github.com/Moshuiwang/lingxi/issues/597) 正文 | 2026-09-05 | 替换表、门禁形态、「先清后开同一 PR」硬约束 |
| [#623](https://github.com/Moshuiwang/lingxi/issues/623) 正文第 138–139 行 | `后置` | #603 做覆盖核对若里程碑必需则绑原项；#604 涉 IN-11 契约 |
| 看板 [#547](https://github.com/Moshuiwang/lingxi/issues/547) | 2026-09-06 | 线 C 内存观察口径与待办；线 F 块 Z-2 周一放行 |

### 开工能力与环境快照（规划者 2026-09-06 只读实测）

| 项 | 实测值 | 对本批的影响 |
| --- | --- | --- |
| 「腿」存量 | **41 行 / 52 处**（`docs` 16 行、`.github` 11、`tests` 7、`scripts` 6、`docs/参考证据` 含在 docs 内、`migrations` 1；`src` **0**） | **卡面写的 27 处已过期**，实施者按 41 行干活；差异因 #592 / #606 / #624 三批新增留痕 |
| 含「腿」的 Trace 合同文件 | `469-rc22.../合同.md`（2 处）、`521-rc24.../合同.md`（1 处） | **命中 CODEOWNERS**，E-3 的 PR 须产品负责人本人 Approve；见裁定项 5 |
| 归属核对登记表 | 含「腿」的登记行 **0 条** | E-3 **不会**触发 `check_contract_attribution.py` 重登记（#592 B 批曾因此返工） |
| 验收矩阵单行 800B 棘轮 | 通过；396 条断言行、20 条在基线内 | 含「腿」的行**都不是** `V-*` 表格行，替换（腿 3B → 链路/环节 6B）**不会**顶穿棘轮 |
| 开工必读集体量预算 | **36,163 B / 40,960 B（余 4,797 B）** | E-3 的替换表放 `代码规范.md` 放得下；不得改投 `验证与门禁.md` 后超预算 |
| `docs/产品合同与外部边界.md` | 74,461 B / 74,461 B（**零余量**） | 本批不碰该文件；任何一字新增都会判红 |
| CI 分类器 | `.github/workflows/`、`scripts/ci/`、`deploy/`、`migrations/` ∈ `FULL_PREFIXES` | E-3 必然提级完整门禁；E-2 若只改 `docs/` 则走文档门禁 |
| `codex` | 0.153.4；`gpt-6-astra` + `medium` **真实调用 exit 0** | 外审①可用；裸 `astra` / `gpt-6` 被后端拒，模型 ID 必须带 `gpt-6-` 前缀 |
| `agy` | 1.1.24；`gemini-3.8-flash-high` 在列，真无头 | 外审②可用；`--print='…'` 必须等号附着且 `--model` 在前 |
| 生产 / stage | 生产 v2.2.0 健康、三容器 `RestartCount` 0；stage 三容器**停着** | 本批全程不碰；停着的 stage 不得被本批启动 |

### 批准前可达性审计

**A. 输入 Issue 准入与关闭资格**

| Issue | 自身完成标准 | 当前事实 | 由哪个 Step 补齐 | Ready 条件 | 关闭或保留去向 |
| --- | --- | --- | --- | --- | --- |
| [#600](https://github.com/Moshuiwang/lingxi/issues/600) | 给出「热修引入 / 热修前已有」结论；若定位到路径则转修复卡；stage 跑完即停、残留清零 | 根因已由 codex 09-06 查清并经元守护独立复核；结论**推翻**了原卡的四处判断；原定 stage 两镜像对照实验价值下降 | S-1-1…S-1-5 | **Ready**（结论与证据已在卡内，无外部依赖） | **Closed**，带对账表；修复方向另立 `[bug]` 卡 Open |
| [#591](https://github.com/Moshuiwang/lingxi/issues/591) | 门禁 + 全量清理 + 人类同事抽读 | 工程实现已由 PR #596 合入 `main`（`d18b774`）、Trace #592 收口；剩 `V-部署-23/24/25` 状态、一处指针勘误、K-8 抽读 | S-2-1…S-2-3 补齐前两项 | **Ready**（docs 尾项无外部依赖） | **保持 Open**——K-8 人类同事抽读由产品负责人另行安排，代理不能推；本批留状态评论写明 |
| [#597](https://github.com/Moshuiwang/lingxi/issues/597) | 「腿」grep 为 0；门禁判红变异实测；`AGENTS.md` 一行；一个 PR 一次合并 | `pre-ready`（产品负责人 2026-09-05「不要着急做」）→ **2026-09-06 已解锁**；存量实测 41 行 / 52 处 | S-3-1…S-3-6 | **Ready**（解锁 + #592 已合入 `main`，原依赖解除） | **Closed** |
| [#576](https://github.com/Moshuiwang/lingxi/issues/576) | 批量形态收益评估 + 产品负责人裁定是否实施 | `pre-ready`；正文自陈「若典型等待远低于 900s 上限，本卡收益有限，可裁不做」；改的是刚过三轮审核的开通链 | S-4-1 | **Ready**（只需裁定，无需实测） | **裁定项 1**，默认 **Closed（不做）** |
| [#603](https://github.com/Moshuiwang/lingxi/issues/603) | 后台职责统一退出与恢复协议 | `pre-ready`；#623 正文第 138 行明写「与本轮恢复链触及处做覆盖核对；若是里程碑必需则绑定原项」 | S-4-2 | **Ready**（只需裁定） | **裁定项 2**，默认**延后并入 #623**，留状态评论、卡 Open |
| [#604](https://github.com/Moshuiwang/lingxi/issues/604) | 期望 MCP origin 由可选改必需 | `待决策`+`安全风险`；#623 IN-11 契约卡在外部提供方确认 | S-4-3 | **Ready**（只需裁定） | **裁定项 3**，默认**延后并入 #623**，留状态评论、卡 Open |

**B. 结果可达性**：本批四个 Epic **全部不产生用户可见结果**，`真实入口 → 运行时承接 → 核心处理 → 外部副作用 → 可观察终态`链路**不适用**。唯一有运行时性质的产物是 E-3 的 `check_docs_wording.py`：入口 = `scripts/ci/verify_docs.sh`（已有唯一文档门禁入口）→ 调用方 = 本机 `scripts/dev/check.sh docs` 与 CI `Epic Full / docs` → 终态 = 命中「腿」即非零退出。**缺件：无**（接线点已存在，照 `check_contract_attribution.py` 先例）。

**C. 实现 / 验收双账**

| 工作线 | 还要实现什么 | 实现齐备后验什么 | 窗口预测 |
| --- | --- | --- | --- |
| E-1 #600 | 无代码；正文改写 + 修复卡 + 看板同步 | 四处判断逐条对上 codex 报告、口径已改分项、卡已关、修复卡已开 | 实施 60–90 分钟；验收 20 分钟 |
| E-2 #591 | 无代码；3 行状态 + 1 处指针 | 矩阵三行现值为「已验证」且各带指针；勘误值逐字正确 | 实施 20–30 分钟；验收 10 分钟 |
| E-3 #597 | **有代码**：1 个新门禁脚本 + `verify_docs.sh` 接线 + 41 行替换 + `AGENTS.md` 一行 + 替换表 | grep 为 0；**变异验红**；本机 `check.sh docs` 绿；CI 完整门禁绿；两名外审的绕过面结论 | 实施 90–150 分钟；审核 + 修复 60–90 分钟；门禁 7–10 分钟 |
| E-4 三张卡 | 无代码；三条留痕评论 | 每条评论都能指回裁定依据与去向 | 实施 30 分钟；验收 10 分钟 |

**实现量不跨批次**，无须拆 Implementation Epic。

### 共享资源租约与环境清理

| 资源 | 作用域 | 唯一 Owner | 并发上限 | 开工前存量 | 取得 / 释放 | 清理方法 | 恢复路径 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **活动编排者身份** | 全项目 | `clr-b1` 编排者 | **1** | 治理批 #624 元守护已退场，当前 **0** 个活动编排者 | 合同批准即取得；**09-08 产品负责人对 Z-2 说「批」即释放给 `ux-b2`** | 写收口 + 交接评论后关窗 | 交接评论自足，续批按本合同继续 |
| **tz 主工作树 + `main` 合并道** | `/home/wangzp/project/lingxi` | 同上 | **1 个写入者** | 干净、只剩 `main`、开放 PR 0 | 同上 | 实施子代理各用独立 worktree，用完 `git worktree remove` | 主树始终保持可回到 `main` |
| **GitHub 工作项写入** | #600/#591/#597/#576/#603/#604/#547 + 新建卡 | 同上 | 1 | — | 合同批准即取得 | — | 评论可追加更正，不删改历史留痕 |
| **stage** | biai-stage | **本批不取得** | — | 三容器**停着** | **本批全程不启动** | 若被误启动，立即 `docker compose stop` 并落评论 | — |
| **生产** | biplus-prod | **本批不取得** | — | v2.2.0 健康 | **本批全程不连接** | — | — |

**收尾残留盘点必做**：worktree（预期 0 个遗留）、远端分支（本批新建的 `claude/630-*` 用完删，46 个历史分支不动）、临时目录、后台进程（含 codex 挂死进程：判据 `ps -o etime,time` 显示 ELAPSED 大而 TIME=00:00:00，处置 `pkill -9 -f 'vendor/x86_64-unknown-linux-musl/bin/codex'`）。

### 授权与外部动作

| 动作 | 授权状态 | 副作用 | 回滚路径 |
| --- | --- | --- | --- |
| 修订 / 关闭 #600、#597 | **已授权**（本合同 §2） | GitHub 状态变化 | 重新打开 + 更正评论 |
| 新建 `[bug]` 修复卡 | **已授权** | 新增工作项 | 关闭并说明 |
| 新建 stage 对照实验卡 | **仅当裁定项 4 判「跑」** | 新增工作项 + 未来占 stage | 关闭并说明 |
| #591 / #576 / #603 / #604 留状态评论 | **已授权** | 无 | 追加更正评论 |
| 关闭 #576 | **仅当裁定项 1 判「关」**（默认即关） | GitHub 状态变化 | 重新打开 |
| 更新看板 #547 线 C | **已授权** | 无 | 编辑回退 |
| 合并非 CODEOWNERS PR 到 `main` | **已授权**（常设授权，唯一合并人 = 编排者） | `main` 前进 | `git revert` |
| 合并 CODEOWNERS PR（三件套、E-3） | **须产品负责人本人 Approve** | `main` 前进 | `git revert` |
| 改 `src/` / 采集脚本 / 生产 / stage | **未授权** | — | — |

**不可逆副作用清单：无。**本批所有动作都可用「重新打开 Issue」或 `git revert` 恢复。

### 验收、审核与修复合同

- **验收断言与证据层级**：见 §4 与 [`验收.md`](验收.md)；关键路径 L3 → L4（CI 完整门禁），**不产生 L5+**。
- **唯一独立审核者**：Opus 5 xhigh Claude 子代理，一名。对固定候选（E-3 的批次分支 HEAD）独立核对需求、实现、测试、静态排除与合并面；**不得参与候选实现、不得直接修复**。CI、实施者自查、收口事实核账**都不是**第二名独立审核者。
- **两名外部审查者不占审核位**（`METHOD.md` §6.6 + §5 配额）：`gpt-6-astra` 与 `gemini-3.8-flash-high` 的产出都是**输入**，由编排者裁定后才进唯一缺陷账本；agy 的结论**必须先回源坐实**。
- **唯一缺陷账本与一次统一修复包**：审核发现由编排者裁定后进入唯一缺陷账本 → 一次统一修复包 → 实施者修复 → **同一审核者定向复核**；候选 SHA 改变后旧结论失效。接受不修的项写进 PR 正文「已知边界」并说明证据。
- **P1 在进入下一阶段前必须解决**、缩小范围或经产品负责人修订合同。
- **重构 / 搬移类审查固定项**：**不适用**（本批无搬移）。
- **并行分支 ≥ 2 且有共同文件面**：**不适用**（本批单批次分支，串行合并）。
- **变异实测硬姿势**：E-3 的门禁验红是本批唯一变异实测——在任一 `.md` 临时写入「一腿」→ 跑 `scripts/dev/check.sh docs` → 必须非零退出 → **还原后复跑必须绿**；还原用 `git checkout --` 而非手改，实测记录进 PR 正文。
- **首触冒烟 / 双通道观测 / 受控触发夹具 / 冻结前静态排除对账**：前三项**不适用**（无用户可见路径、无受控失败旅程）；**静态排除对账适用**——把 §3 不做清单逐项对到 E-1…E-4 的验收断言上，命中者标缺件或取得产品负责人明示接受，不得静默滑过。

### Ready 与交接自检（第五节交接门）

- [x] 所有输入 Issue 都有当前事实、Ready 条件和收尾去向；适用结果完成可达性检查；实施与验收已分账。
- [x] 可能推翻合同的无依赖未知已在规划期只读查清（存量 41 行、CODEOWNERS 命中、棘轮与预算余量、两条外部通道可用性），**无 Wave 0 需求**。
- [x] 至少一个 Step 可立即执行（S-0-1 与 S-1-1 在合同合并当刻即 Ready）；每个 Step 有稳定 ID、启动条件、成功证据及成功 / 失败 / 等待分支。
- [x] 每个 Epic 的生命周期、计划交付终点和完成 / 关闭条件明确（§1、§4、可达性审计 A 表）。
- [x] 规划者与编排者已分为独立顶层任务 / 会话；`execution_adapter`、角色模型映射、唯一独立审核者已实例化；共享资源已有租约与唯一性检查。
- [x] 正式验收 / 发布对象规则：**不适用——本批无正式验收对象与发布**（不构建镜像、不部署、不产生制品）。
- [x] 外部动作、不可逆副作用（**无**）、恢复路径和授权终点明确；发现去向与收尾可执行。
- [x] 正文已读回，方法版本 v17、合同版本 v1、批准证据（本 PR 合并）完整。

**批后继任条件**：本批预计单批次收口，**默认不创建继任 window**。触发继任的唯一情形是 09-08 09:00 让位（见 §6）——此时按 `METHOD.md` §6.8 固定序列写收口 + 自足交接评论后退场，剩余 Step 由 `ux-b2` 之后的续批编排者按本合同继续，**合同不失效、不需重新批准**。

---

## 批准时需一并裁定

> 九项，每项带默认值。**不回按默认执行；您怎么回、回多长都有效，不限格式。**

1. **#576 预开通批量执行怎么处置？** 正文自陈「若典型等待远低于 900s 上限，本卡收益有限，可裁不做」，且它要改的是刚过三轮审核的开通链；rc25 的单人就绪等待读数至今未落。**默认：关闭（不做）**，留一条写明依据的关闭评论。
2. **#603 后台退出与恢复怎么处置？** #623 正文第 138 行已明写「与本轮恢复链触及处做覆盖核对；若是该里程碑必需则绑定原项，不能用新条目重复实现」。**默认：延后并入 #623**，本批只留状态评论，卡保持 Open。
3. **#604 MCP 目标配置由可选改必需怎么处置？** 它涉及的 MCP 契约是 #623 的 IN-11，卡在外部提供方确认；本轮未证实存在普通用户可利用的漏洞。**默认：延后并入 #623**，本批只留状态评论，卡保持 Open。
4. **#600 原定的 stage 两镜像对照实验还跑不跑？** 根因已查清（健康检查写 pyc 被只读文件系统拒 → negative dentry），该实验的原目的「判定是否热修引入」已被更直接的证据回答；跑它要占 stage 约 8 小时、出口 20–30 MB，且会与 09-08 块 Z-2 抢 stage 租约。**默认：不跑**，在 #600 关闭说明里如实写「未做旧制品长时对照」这条边界仍保留，并把看板 #547 待办里那条「读完 Supabase 用量页就开跑」撤下。
5. **#597 清「腿」要不要动两份已收口 Trace 的合同文件？** `469-rc22.../合同.md`（2 处）与 `521-rc24.../合同.md`（1 处）含「腿」字，它们是历史批准记录，且命中 CODEOWNERS——改就要您本人 Approve 那个 PR。**默认：改（全仓清零）**，纯措辞替换不动语义，换来「grep 为 0 + 门禁 fail-closed 无豁免路径」这个干净终态。若您选不改，则门禁须排除已收口 Trace 目录，那条豁免路径要写进 `代码规范.md`。
6. **#600 的监控口径要不要写进仓库正文？** 关掉 #600 后，「不能用 `docker stats` 总量预测 OOM、要看 `anon` / `slab_reclaimable` / `memory.events` 分项」这条长期运维事实就只剩在一张已关闭的 Issue 里，而下一任跑巡检 cron 的人照的是仓库与 cron 文案。**默认：写**——在 `deploy/监控告警.md`「七、已知限制 / 未验证事项」加一条最小注记，**不改** `scripts/ops/monitoring/` 采集脚本（那是产品代码，本批不碰）。
7. **机械项的实施模型降不降？** #591 的三行状态 + 一处指针、#597 的 41 行纯替换是机械活。您 2026-09-06 裁定「实施 Opus 5 high」。**默认：不降，全部 Opus 5 high**（尊重已下的裁定）。若您愿意省额度，可把这两项降到 Sonnet 5，判断类的 #600 正文改写与三张处置留痕仍留 Opus 5 high。
8. **本批若到 09-08 09:00 还没收口怎么办？** **默认：`clr-b1` 无条件让位**——写收口 + 交接评论后退场，剩余 Step 转入 Z-2 之后的续批，全程不与 `ux-b2` 并行。若您要两批并行，则须另设元守护持唯一性检查，且明确哪一批持 `main` 合并权。
9. **两名外部审核者的位置与信任级别对不对？** 您 2026-09-06 放宽到两名，并提醒 agy 能力一般不要全信。**默认：** `gpt-6-astra`（medium，≤2 轮）审 #597 门禁绕过面并作为它在本仓的首次标定跑，挂死或不可用即退回 Opus 5 xhigh；`gemini-3.8-flash-high`（≤3 轮）只做事实一致性冷读，结论**必须经编排者回源坐实**才进缺陷账本；两者**都不占**唯一独立审核者位（那仍是一名 Opus 5 xhigh）。
