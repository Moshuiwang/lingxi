# 验收（清仓批一；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace #630 或 PR）

> 证据等级口径见[验证与门禁](../../技术设计/验证与门禁.md)；每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，**不得为了收口好看而标绿**。
> **本批的等级天花板是 L4（CI 完整门禁）**——不部署、不碰生产与 stage、无真实用户旅程，因此**不产生 L5 / L6 / L7 结论**。任何一行写出 L5 以上即为错误。

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准 | 目标等级 | 现值 | 关联验收矩阵 `V-*` | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1 #600 查因收尾** | #600 正文四处被推翻的判断逐条改写并标注推翻依据（①各服务速率不同，不能统称 8–10；②未分 `anon`/`slab` 导致由总量直推 OOM 时点；③「升级后仍涨」不能证明与 #593 无关；④热修前触发条件早已存在、未做旧制品实测不能声称已复原历史曲线）；根因链固化进正文；监控口径改 `anon`/`slab_reclaimable`/`memory.events` 分项；#600 Closed 带对账表；修复卡 Open | **L1**（已分析，沿用 codex 09-06 既有证据） | **L1**（已达） | 不适用 | [#600](https://github.com/Moshuiwang/lingxi/issues/600) CLOSED/completed 2026-09-06T05:04:53Z；[对账评论](https://github.com/Moshuiwang/lingxi/issues/600#issuecomment-5557085932) |
| **E-1b 修复卡另立** | 新 `[bug]` 卡写明方向（`PYTHONDONTWRITEBYTECODE=1` 或预编译标准库）、边界（改镜像走发布、不在本批做）、验收草案 | **L1** | **L1**（已达） | 不适用 | [#632](https://github.com/Moshuiwang/lingxi/issues/632) Open，标签 `缺陷`+`pre-ready` |
| **E-2 #591 docs 对账** | `验收矩阵-部署与迁移.md` 的 `V-部署-23/24/25` 现值由「已认领」改「已验证」并各附 #592 证据指针；`592-代码可读性/任务表.md` V-1c 行评论号 `5547344691` → `5547375862`；#591 留状态评论且**卡保持 Open** | **L4**（文档门禁 + CI 完整门禁） | **L4**（已达） | `V-部署-23`／`V-部署-24`／`V-部署-25` | PR [#633](https://github.com/Moshuiwang/lingxi/pull/633) merge `db70992`；[Epic Full run 34014732492](https://github.com/Moshuiwang/lingxi/actions/runs/34014732492) |
| **E-3 #597 用词治理** | **按[裁定留痕](https://github.com/Moshuiwang/lingxi/issues/630#issuecomment-5557003520)第二节口径执行——裁定项 5 已由默认「改」改判为「不动已收口 Trace 的合同文件」，本行原「grep 为 0」被取代**：`grep -rn "腿" docs .github scripts src tests migrations` 的命中**恰好等于六个豁免文件内的命中**、其余为 0（**全仓 `git ls-files` 实测口径**，基线 `0d55417`：存量 62 行 / 77 处，豁免 23 行 / 27 处，**实际清理 39 行 / 50 处**——清理面是验收数字，豁免文件内的命中数会随这几份文件自身的写作继续变化，不作为验收数字；裁定留痕写的 38 行 / 49 处是按 `docs .github scripts src tests migrations` 六个根目录统计的，少算 `deploy/README.md` 一行，编排者按全仓口径清理）；`check_docs_wording.py` 新增且 fail-closed，豁免**按文件精确列举、不按目录通配**且每条注明理由，**同时挂进 `verify_docs.sh` 与 `verify_repository.sh`**（文档作业只在 `docs_changed=true` 时触发，纯代码 PR 跑不到）；**变异验红①**：任一**非豁免** `.md` 写入「一腿」→ `check.sh docs` 非零退出，`git checkout --` 还原后复跑绿；**变异验红②（豁免面）**：往豁免清单新增一个文件必须让钉住测试判红（照 `tests/test_ruff_config.py` 先例，清单只能变短）；**变异验红③（接线面）**：门禁必须同时挂在 `verify_docs.sh` 与 `verify_repository.sh`，摘掉任一处即判红（docs 作业只在 `docs_changed=true` 时触发，纯代码 PR 跑不到它，而存量主要住在 `ci.yml` / `tests/` / `scripts/`）；`AGENTS.md` 工作底线加一行禁用英文直译词并给出替换表入口；替换表落 `代码规范.md` 且开工必读集 ≤ 40,960 B；**存量清零与开闸在同一 PR** | **L4**（含三次变异验红） | **L4**（已达，含五次变异验红） | 待定：本批不新增 `V-*` 行（纯工程门禁，不认领产品断言） | 同上 PR；本机 full `Ran 6017 tests … OK`；五次变异读数见 PR 正文 |
| **E-4 三张卡处置留痕** | #576／#603／#604 各一条评论，写明处置结论、裁定依据（裁定项 1／2／3 的回复或默认值）、去向；**不实施任何一张卡的内容** | **L1** | **L1**（已达） | 不适用 | [#576](https://github.com/Moshuiwang/lingxi/issues/576) CLOSED/not planned、[#603](https://github.com/Moshuiwang/lingxi/issues/603) / [#604](https://github.com/Moshuiwang/lingxi/issues/604) 保持 Open 各一条留痕 |

## 批终链（全绿才进下一阶段）

本机 `check.sh docs` + `check.sh full` 绿（见 `Ran N tests`）→ 唯一独立审核者批量审查（一次）→ 两名外部审查并行（codex `gpt-6-astra` 绕过面 / agy `gemini-3.8-flash-high` 事实一致性，**结论均须编排者回源坐实后才进缺陷账本**）→ 统一修复包 → 同一审核者定向复核（一次）→ 冻结候选 → CI 完整门禁 → **产品负责人本人 Approve（CODEOWNERS）** → 合 `main`。

**发布 / 部署回读：不适用——本批不构建镜像、不部署、不产生制品。**冻结后回填：候选 SHA **`4615566`**；CI 门禁 run **[34014732492](https://github.com/Moshuiwang/lingxi/actions/runs/34014732492)**（Epic Full 全绿，`gate` 5m50s / `image` 4m11s）；本机 `check.sh full` 三次全绿，终值 `Ran 6017 tests in 591.491s / OK`；merge commit **`db70992`**；**制品标识：不适用**。

## 未验证层级（收口时如实列）

以下各条在本批**必然**保持未验证，收口时原样保留、不得改写为已验证：

- **#600 根因未做旧制品长时对照**——`d67f772a3a6f` 的历史曲线未复原（codex 09-06 自陈）；裁定项 4 若判「不跑」，这条边界长期保留。
- **#600 修复未验证**——`PYTHONDONTWRITEBYTECODE=1` 只在本机同源基础镜像做过隔离对照（100 进程、slab 增量 3.90 MiB → 848 B），**生产修复效果零验证**，由另立的修复卡承接。
- **slab 在内存压力下能否被足额回收未实测**——既不能断言必然 OOM，也不能保证永不 OOM（codex 09-06 原话）。
- **#591 K-8 人类同事抽读未做**——产品负责人 2026-09-05 07:0x「不用这个了，回头再读」；**这是 #591 保持 Open 的唯一原因**，不是本批遗漏。
- **#597 门禁只在本机与 CI 验证**——不覆盖「代理在 Issue 评论 / 汇报里写「腿」」这条链路（仓库外，门禁管不到）；`AGENTS.md` 那一行是流程挡不是机器挡。
- **六个豁免文件内的「腿」仍在仓库里**——两份已收口 Trace 的合同文件按产品负责人 2026-09-06 裁定不动，本 Trace 三件套与 `代码规范.md` 因正文即在讨论该词而豁免；门禁对这六个文件不生效，靠钉住测试保证豁免清单**只能变短不能变长**。全仓「零命中」这个终态**本批未达成，也不打算达成**。
- **「腿」以外的英文直译词未清理**——#597 非范围，发现只登记。
- **#576／#603／#604 三卡内容未实施**——本批只做处置留痕。
- **`gpt-6-astra` 能力未经本仓充分验证**——本批是它的首次标定跑，读数落收口评论；**不得**据本批一次结果宣称它优于或劣于现有审核者。
- **本批全程未碰生产与 stage**——生产 v2.2.0 健康、stage 三容器停着这两条事实来自开工前引用，本批未重新取证。
