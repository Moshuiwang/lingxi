# 开工快照：2.5.2 补丁版（Trace #843）

> 按 `#678` v22 §二与 `docs/traces/README.md`：开工能力、额度、环境快照与里程碑分诊表放本文件。**授权、验收门槛、已裁定事项、各道 Gate 一律留在 `合同.md`，不得移出以绕过批准。**

## 合同区字段

- 方法版本 **v22**（[#678](https://github.com/Moshuiwang/lingxi/issues/678)，2026-09-18 02:33Z 发布，[发布指针](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5724287367)；上一版修订 Issue [#840](https://github.com/Moshuiwang/lingxi/issues/840) 已于 2026-09-18 02:42Z 关闭 = 上一 Trace 方法候选已结清）；下一版修订 Issue **#842**；合同版本 **v1**；执行阶段 **Planning → 待批准**；整体交付结果 **Undetermined**。
- **执行者与运行面**：Claude Code 运行面（**2.1.276**）；执行机 tz（Ubuntu 24.04，15 GB 内存、6 核）；**本机时钟是 UTC**，面向产品负责人的时刻一律 UTC + 北京（+8）双标注且两个值都实读（`date -u` 与 `TZ=Asia/Shanghai date`）。本快照采样时刻：**2026-09-18 03:09–03:2xZ｜北京 11:09–11:2x**。
- **基线提交与核对时间**：`origin/main` = **`a9610d1f`**（2026-09-18 03:1xZ 实读；主检出工作树干净、无开放 PR、无其他工作树）；`origin/release/2.5` 头 `5639500`（= 正式版 `v2.5.1`）；`pyproject.version` = 2.5.1；最新正式版 Release `v2.5.1`、最新候选 `v2.5.1-rc.106`（rc 序号全局递增，下一枚预计 rc.107）；`CHANGELOG.md` 顶部为 `[2.5.1] - 2026-09-17`，无 `[Unreleased]` 节。
- **生产现网（2026-09-18 03:2xZ 代理只读回读）**：三容器 `v2.5.1` `Up 19 hours (healthy)`；`lingxi-release-pull.timer` enabled / active；迁移头 0098（#812 收口留痕）；`/opt/lingxi/runtime-config/system_prompt.md` `root:root 0644` 4078 字节，sha256 `84741170e3c2…`，含内部代号行数 1，strip 摘要 `2272bd4d40ae`；`/etc/lingxi/public-config.json`（879 字节，root 0644）`values.LINGXI_WORKER_RUNTIME_CONFIG_DIR = /opt/lingxi/runtime-config`、`files.worker = {"system_prompt.md": "84741170…"}`（**生产已登记，#784 须文件与登记同批改**）。
- **预发现网（同时刻只读回读）**：`lingxi-release-pull.timer` **disabled / inactive**；三容器 `Exited (0) 16 hours ago`、镜像 `v2.5.1-rc.106`（只停不删 ✓）；worker 容器挂载 `/opt/lingxi/runtime-config → /etc/lingxi/runtime`，`LINGXI_WORKER_SYSTEM_PROMPT_FILE=/etc/lingxi/runtime/system_prompt.md`；`/opt/lingxi/runtime-config/system_prompt.md` 与生产同 sha256 `84741170…`（`root:root 0644`，目录 0755）；`~/lingxi-runtime-config/system_prompt.md`（compose 时代遗留副本，wangzhipeng 0644，同摘要，**非挂载**，另有 7 份 `.bak-*`）；`/etc/lingxi/public-config.json`（378 字节，2026-09-16 22:37 = Y）`values` 只有 `LINGXI_GATEWAY_PIDS_LIMIT` 与四个 `LINGXI_INNERTEST_*`，**无 `RUNTIME_CONFIG_DIR`、`files = {"scheduler": {}, "worker": {}}`**（**预发未登记提示词文件，部署器不核对**）；`/root/lingxi-s27-backup/` 两份（public-config X 副本 338 字节、F11 代理备份 94813 字节）；拉取代理单元 `ExecStart=/usr/bin/python3.12 -B /opt/lingxi/release-pull/release_pull_agent.py --host-contract /opt/lingxi/release-pull/host-contract.json --config /opt/lingxi/release-pull/config.json --state-directory /opt/lingxi/deploy/state run`。
- **规划者任务 / 会话与停止点**：规划者会话在主检出 `~/project/lingxi`（tmux `TZ-server:1`，Fable 5.1 xhigh），不建规划工作树（三件套走 `plan/trace-843-2.5.2` 分支）。停止点 = 合同 v1 合并；**随后转任守望者**（合同 §6，裁定 9）——不实施、不持编排权与合并权，只以外部证据判活、承接并转发裁定、失联时取证后拉继任、核编排者退场序列。
- **当前顶层编排任务 / 会话**：尚未拉起；合同合并后由守望者拉起 tmux `TZ-server:843-w1`（工作树 `.claude/worktrees/843-w1`，编排 opus max）；批次 = W1 代码批 / W2 预发 + 生产 / W3 收口；接管登记见 Trace Issue。
- **编排者接手入口与第一恢复点**：`/trace-kit:takeover` → 读本目录四份文件 + Trace Issue 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + Trace Issue 最新评论**。

## 里程碑分诊表（v22 §三；2026-09-18 03:xxZ 实读 GitHub）

### 里程碑 2.5.2（#14）

| Issue | 事实 | 验收标准 | 标签 | 归属 | 建议 / 写动作（裁定 11） |
| --- | --- | --- | --- | --- | --- |
| #784 自称 BI Plus | 已核（09-17 分诊 + 09-18 裁定转贴 5717598628）；**本次补两条**：预发 `public-config.json` 未登记提示词文件、生产已登记（见上）；预发 worker 实际挂载 `/opt/lingxi/runtime-config`（家目录副本为遗留） | 5 条可观察；判定值 = 脚本回读 sha / 计数 + worker 终态审计 `system_prompt_digest`（系统写）+ 真人截图 | 缺陷 / pre-ready / P1 ✓ | **留** | 合同合并后：正文顶部补 09-18 裁定块 + 两条新事实，转 `Ready` |
| #834 连接借用围栏 | 已核（分诊 confirmed 5717148990；裁定 5717599641） | 5 条 + 否定断言 5 条；判定值 = 门禁退出码、测试输出 | 维护 / Ready / P2 ✓ | **留** | 无 |
| #836 棘轮勘误 | 已核（5717205125；裁定 5717601667 在评论、正文未补） | 8 条机器可判 + 3 条逐字对照 | 维护 / Ready / P2 ✓ | **留** | 正文顶部补裁定块（第三处改为「不再迁移」） |
| #837 行级上限 | 已核（5717258002；裁定块已在正文顶部） | 9 条 + 否定断言 8 条 | 维护 / Ready / P2 ✓ | **留** | 无 |

### 反向扫描（其他里程碑与无里程碑的 open Issue）

| Issue | 状态 | 判定 | 理由 |
| --- | --- | --- | --- |
| #841 方法 v22 仓库侧四条 | 无里程碑 / pre-ready / P2 | **提前并入**（裁定 5，默认并入） | docs-only、秒级路由、空档可做；不并入则 v22 的仓库侧收尾悬空 |
| #839 依赖漏洞扫描 | 无里程碑 / pre-ready / P2 | 不动 | 产品负责人 09-18 裁定不排期 |
| #814 部署链改道四条 | 无里程碑 / pre-ready / P2 | 不动 | 登记不排期（#812 合同 §3） |
| #803 凭据轮换 | 无里程碑 / `长期参考` + ops / **P1** | 不动；登记标签冲突 | 硬期限 2026-12-13 另做；`长期参考` 与优先级标签并存违反协作约定「长期参考不打优先级」，留产品负责人一字（本 Trace 不改） |
| #827 工程质量评估 | 无里程碑 / [research] / 待验收 / P2 | 收口时按裁定 8 处置（默认关闭） | 四条发现已转化为 #834–#839 并各有裁定 |
| #809 / #835 / #815 / #838 / #712 | 2.6.0 | 不动 | D-6：2.6.0 = 迁库另开 Trace |
| #810 | 3.0.0 / P1 | 不动 | 版本顺序既定 |
| #678 | [template] | 不适用 | 方法正文 |

无 P0 / P1 无里程碑的工作项（#803 属 `长期参考`，不进状态链）。

## 开工能力与额度快照（v22 §三；全部 2026-09-18 03:0x–03:1xZ 实读或实机测试）

| 项 | 值 | 怎么查（可复用；主备两处） |
| --- | --- | --- |
| 当前主编码代理 | Claude Code **2.1.276**；本规划会话 Fable 5.1 **xhigh**；本机默认档位 xhigh（`~/.claude/settings.json` effortLevel） | `claude --version`；会话状态行 |
| Claude 额度（采样 03:08Z / 11:08 北京；**已用**百分比） | 5 小时窗已用 **11%**（04:30Z / 12:30 北京重置）；**7 天窗已用 19%**（**2026-09-23 23:00Z = 09-24 07:00 北京**重置）；Fable 已用 **17%**（同一重置）。**结论：额度不构成开工门槛，合同批准即开工** | 主：`~/.claude/tmux-claude-usage-api.dat`（十一列：时刻 5h% 5h重置 7d% 7d重置 Fable% Fable重置 + 燃速四列，epoch 秒，轮询器每 5 分钟拉官方用量接口）；备：渲染器 `~/.claude/tmux-claude-usage.dat`（五列）与 tmux 状态栏 |
| 外审一：codex **0.155.0** | 模型 **`gpt-6-astra`** high；**实机测试 2026-09-18 03:09:47–03:09:58Z：11 秒、退出码 0、stdout 27 字节「就绪 gpt-6-astra high。」、16,974 token**；回退 `gpt-5.6-sol` high（本次未测，#812 09-16 实测可用）；`config.toml` 默认 `gpt-6-astra` / low（显式传档位，不靠默认） | 调用姿势：在 trusted 目录（`~/project/lingxi` 已登记）`printf '%s' "<提示>" \| codex exec -s read-only -c model="gpt-6-astra" -c model_reasoning_effort="high" -`，输出直写文件；证据内联进提示、明示不跑命令；判成功只认 stdout 非空 |
| codex 额度（采样 03:08Z） | 7 天窗**已用 56%**（**2026-09-19 11:10Z = 19:10 北京**重置）；5 小时窗无数据 | 主：`~/.local/state/tmux-codex-quota/tmux-codex-usage.dat`（五列；`~/.codex/hooks.json` 会话钩子刷新，跑一次 `codex exec` 即更新）；备：tmux 状态栏蓝底段 |
| 外审二：agy **1.2.4**（Antigravity CLI） | 模型 **`gemini-3.8-flash-high`**（档位在模型名里，**不传 `--effort`**）；**实机测试 2026-09-18 03:10:00–03:10:10Z：10 秒、退出码 0、stdout 29 字节「就绪 gemini-3.8-flash-high」** | 调用姿势：`agy --add-dir <工作树> --model gemini-3.8-flash-high --print-timeout 600s --print='<提示>'`（`--print=` 带等号、`--model` 在前；提示首行写「不要调用任何工具」或让它只读工作树文件）；判成功只认 stdout 非空；白名单外命令静默零输出 |
| agy 额度（03:10Z 实读） | Gemini 系：**周剩 98%**（2026-09-23 06:33Z 重置）、五小时窗剩 99%；Claude / GPT 系经 agy：周与五小时均 100% | 主：`agy --print='/usage'`（四行制表）；备：无 |
| 外审频率（按额度定） | 三家充裕 → 批次终点 codex + agy 各一轮（= 生产前走查）= 预估 2 / 上限 4。**降档触发**：任一家周窗 ≥ 80% 或五小时窗 ≥ 90% → 只保留 codex 一轮 | 每次外审当场登记：时间、工具与模型、候选 SHA、读入字节数、卡号、结果条数 |
| 本机门禁 | `scripts/dev/check.sh full` 空闲机器约 26 分钟（#812 实测），须 tmux 窗口独占机器跑；本会话 shell `umask 0002`，跑门禁前 `umask 022`；`.dev-check/venv-fast` 非 editable 安装，变异实测走 `PYTHONPATH=src` 或清 `__pycache__` | #812 / #770 实测 |

## 开工能力与环境快照（其余）

| 项 | 值 | 来源 |
| --- | --- | --- |
| 发布路径 | `publish.yml` 触发于推送 `release/**`；同步 PR 须先并入 `release/2.5` 现头（`5639500`）；候选版本号取自 `pyproject.version`；`release.yml` 手动 `workflow_dispatch`（机器人 403，产品负责人网页点）；验收记录 `deploy/releases/acceptance/<候选标签>.json`（CODEOWNERS，产品负责人批）；Promotion 读记录后把同四镜像发布为正式版 | `deploy/releases/README.md` 实读；#812 实测 |
| 机器人权限边界 | `gh run rerun` / `workflow run` 403；`accept/`、`record/`、`trace/`、`task/`、`sync/` 前缀可推；`push --delete` 被本机分类器拦（远端已合并分支由产品负责人网页删，仓库 `delete_branch_on_merge = false`）；`docs/traces/**/合同.md`、`deploy/releases/acceptance/`、裁决层三文件受 CODEOWNERS | `.github/CODEOWNERS` 实读；#812 实测 |
| CI 成本与路由 | `gate` 约 10 分 / 15 分上限；`image` 4–5 分；纯 docs PR 约 13 秒；`FULL_PREFIXES = .github/workflows/ deploy/ migrations/ scripts/ci/` + `CHANGELOG.md` 走完整门禁（`deploy/releases/acceptance/` 亦在 `deploy/` 下）；`trace/**` 分支要 `Image-Candidate: true` 空提交；发布分支 PR 的 App 裁决层复跑约 16 分钟，判「全绿」须把 App 检查算进去 | `scripts/ci/classify_story_changes.py` 实读；#812 复盘 |
| 部署链现状 | 拉取代理 + 部署器已在预发（rc.99 → rc.106）与生产（v2.5.0 / v2.5.1 两轮）真机跑过；#804（人工改公开配置后 old / new 分离）与 #805（prepare-only 收敛）已修并实证；代理不随控制包自升级（#815 登记；预发代理已是 F12 版，生产代理版本本次未核——不影响本 Trace，修法都在部署器侧随控制包到达）；`verify_public_files` 只核对 `values.LINGXI_<ROLE>_RUNTIME_CONFIG_DIR` 登记的目录下 `system_prompt.md` / `content.override.toml` 等非秘密文件的 sha256，目录未登记即跳过 | `deploy/deploy_runtime.py:229-264` 实读 |
| 预发脚本投放与执行 | 本机 allow 规则：`Bash(ssh biai-stage *)`、`Bash(scp * biai-stage:/tmp/lingxi-s26/*)`、`Bash(ssh biai-stage sudo -n bash /tmp/lingxi-s26/*)`（#770 W2 登记）→ 本 Trace 脚本一律投放到 `/tmp/lingxi-s26/`；#812 的 `s27_g1a_stage_revive.sh` / `s27_g1a_rollback.sh` 原文仍在 tz 旧会话 scratchpad（sha `44a0bed2…` / `3cc4c3cb…` 本次核对相等），复用不改 | `.claude/settings.local.json` 实读；本机文件 |
| 预发验收读数 | 预发库经 `/etc/lingxi/.env.stage.*`（root 0600）里的 DSN；DSN 值带单引号，psql 前去引号并 `2>/dev/null`（否则报错回显口令）；任务表叫 `task`；`qa_corpus` 行含 `system_prompt_digest` | 记忆 `stage-env-dsn-quoting`；`src/lingxi/core/qa_corpus.py` |
| 生产 | v2.5.1 / 0098；代理只读形态 `ssh biplus-prod 'sudo -u bi-ai-deploy …'` 扁平单条（本机 allow 规则）；生产写动作一律产品负责人本人；生产状态账 / 阶段账 root 0600 对代理不可读，`verified` 靠管理群通知 + systemd 结果间接取证；`pg_dump` 姿势沿 #812（`prod_pre_apply_pg_dump.py` → `/root/lingxi-backups/`） | #812 收口评论；本次只读回读 |
| 里程碑（03:1xZ 实读） | 2.5.2（#14）= #784 P1 pre-ready + #834 / #836 / #837 P2 Ready；2.6.0（#13）= #809 / #835 / #815 / #838 / #712；3.0.0（#12）= #810；无里程碑 open：#841 / #839 / #814 / #803 / #827 / #678 | GitHub 实读 |
| tmux / 工作树 | `TZ-server` 常驻，窗口 1（本会话）与窗口 2（空 bash）；无其他编排者在跑；主检出无其他工作树；无开放 PR | 本机现场 |
| 分类器边界 | 生产主机判敏感：写命令须产品负责人亲加 allow 规则、一次一条扁平命令；`ssh` + 本地重定向被拦；`push --delete` 被拦；预发持久化动作若不在白名单路径会被拦 → 回退为产品负责人 `!` 亲跑 | 记忆 `prod-access-posture` / #812 实测 |

## 批后继任条件（命中）

三个批次窗口可能跨会话接力（收口点当任上下文 ≥ 50% 默认换人，低于可续任）。继任：读本目录四份文件 + Trace Issue 最新交接评论 → 核实现场（工作树、批次分支 tip、PR 状态、预发与生产现状、额度余量）→ 接管登记 → 前任确认停止写入并释放租约 → 退场。**未确认转移前保持等待，不得出现两个写入者。** 接力只发生在批次收口序列或失联判定；退场前复盘评论落地才算退场完成。守望者失效时（窗口关闭即两类机制全失效）由产品负责人在主检出重新拉起守望者会话，接手 prompt 见 Trace Issue 最新交接评论。
