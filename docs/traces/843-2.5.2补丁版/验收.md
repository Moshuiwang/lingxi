# 验收（Trace #843 · 2.5.2 补丁版；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace Issue 或 PR）

> 证据等级口径见 `docs/技术设计/验证与门禁.md`（L 名 = 成熟度 · 环境 · 执行者；全局 1–7 级对照见该文第三节）。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末。**判定字段只认系统写出的值，前置状态允许人工构造；答不出「这个值是谁写的」即无效判据。**

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准（判定值由谁写出） | 目标等级 | 现值 | 关联验收矩阵 | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1** #837 三条清理路径行级上限 | ① 空闲会话正文清理、`expire_undelivered_terminals`、心跳超时回收各为一条带 `LIMIT`（常量 500）的语句；② 上限生效：构造远多于上限的待清行，单轮清除行数 ≤ 500 且等于候选集按 (会话 id, 事件 id) 升序的前 N 行——与 `/new`、按用户清理同一锁序（W1 修订：Fable r2 真库对抗实测发现按事件 id 单键排序与既有路径锁序不一致可死锁，修法经 r3 / r4 复核）；③ 连续前进：重复执行剩余行单调下降至 0；④ 未处理行 `content` 与低敏字段逐字不变；⑤ 大单会话（≥ 6 万行）在仓库默认 3 秒语句超时下单轮正常返回；⑥ 另一会话 `FOR UPDATE` 占住排序靠后一行时本轮按 `lock_timeout` 失败回滚、解除后下一轮清空；⑦ 日志与返回值不含行内容；⑧ 既有 sweep 用例全绿（`Ran N` 可见）；⑨ `check_deploy_contract.py` 默认超时对账仍成立，新矩阵行 ≤ 800B（判定值 = 隔离真库里的表内容与测试输出，由测试用例实跑写出） | L3 + L4a（CI） | L3 达成（本机隔离真库 12 条用例；本机 full ×2 `Ran 7451` / `7459`）+ L4a（PR #847 Epic Full 绿） | V-投递-10 旁新行；V-管理-39 勘误 | PR #847 `97974be8`；#843 评论 5725573363 / 5725986433 / 5726564897 / 5726806274 |
| **E-2** #834 连接借用围栏 | ① 门禁对全仓退出码 0，199 处调用点（默认复用 195 / 独占 4）一处不改；② 四种违规样本各判红一次并钉住：裸 `connect()` 赋值后使用、`with` 内 `close()` 后再 `connect()`、游标赋给 `self.`、游标逃出 `with` 再 `execute`；③ `test_postgres_connection_reuse` 真库全绿且含新增记录性用例（N > 22）；④ `postgres.py` 四处说明：目标合同句仍在、其后有「尚未完全兑现」段、三件堵不住的事逐条写明；⑤ CI Epic Full 绿（判定值 = 门禁脚本退出码与测试输出） | L2 + L3 | L2 + L3 达成（全仓 195 / 4 / 0；`Ran 37` 门禁钉住 + `Ran 23` 真库；Fable r2 / r4 独立复查） | 无新增断言 | PR #847；#843 评论 5725986433 / 5726486279 / 5726806274 |
| **E-3** #836 棘轮门禁三处勘误 | ①（逐字对照）`代码框架.md` 正向写明「基线登记的路径找不到时判红，须 `--refresh` 清除」且已移出「已知边界」从句；②（逐字对照）浅克隆注释与 `ci.yml` / `story.yml` 一致，给出 `c34b6989` / 2026-09-01 依据并写明「结论按 #592 D-24 不变」；③（逐字对照）基线头部与 `BASELINE_HEADER` 改为「不再迁移（产品负责人 2026-09-18 裁定），无承接单」；④ `check_apps_pure_logic_ratchet.py --refresh` 后 `git diff` 为空；⑤ `check_docs_size_budget.py` 退出码 0 且未改 `TOTAL_BUDGET_BYTES`；⑥ 用词门禁与三条棘轮脚本退出码 0；⑦ 两个钉住测试文件 `Ran N`；⑧ 五份基线文件条目零变化 | L1 + L2（+ L3 逐字对照） | L1 + L2 + L3 达成（三处逐字对照 Fable r1；`Ran 53`；PR #845 Epic Full 绿） | 无新增断言 | PR #845 `e12f1c08`；#843 评论 5725391087 |
| **E-4** #784 自称 BI Plus | **脚本规格**（`s28_784_prompt.sh`，不入仓库）：模式 `dry / apply / rollback`；参数 = 目标目录、`public-config.json` 路径、备份目录；只把第 1 行替换为「你是 BI Plus，面向公司内部员工的只读数据问答助手。」、末尾追加一行「自称与对外名称只用 BI Plus，不使用任何内部代号。」，其余字节不动；`public-config.json` 的 `files.worker` 登记了该文件才同步改登记值，未登记则跳过并打印；写临时文件后原子替换、`root:root 0644`；改前副本 = `system_prompt.md.bak-84741170` 与 `<备份目录>/public-config.json.before-784-<UTC>`；每步回读打印 sha256 / 代号计数 / strip 摘要 / 登记值，与卡面预期不等即退出非零。**判据**：① 本机假根目录两形（无登记 / 有登记）dry → apply → rollback 各通过；② 预发 dry 输出与预期逐字相等；③ 预发 apply 后：文件 sha256 = 预期、`grep -c -i -E '灵犀\|lingxi'` = 0、旧副本在、家目录遗留副本同步；④ 预发真人「你是谁」回答含「BI Plus」且不含代号（截图脱敏）；⑤ 该轮 worker 终态审计 `system_prompt_digest` = 新 strip 摘要（系统写出；`qa_corpus` / 终态审计回读）；⑥ 生产 dry 相等 → apply 后文件 sha256 = 预期、登记摘要 = 新 sha256、两份副本在；⑦ v2.5.2 部署 preflight 以新登记通过（状态账无 `public_file_*` 失败）；⑧ 生产真人「你是谁」含「BI Plus」+ 终态审计摘要 = 新值；⑨ `workers/oauth-bridge` 页面标题不含代号（代码 + 该目录测试）；⑩ `deploy/拉取代理.md` §十 结果码行、runbook 11.2 两行摘要回写 | L2 → 预发 L4a / L4b → 生产 L5（真人「你是谁」）+ 「真实链路 · 生产 · 代理 · 只读」（摘要回读） | **全部达成**：L2（标题 + §十行 + 脚本两形 151 断言）；预发 ②③（dry ×3 相等、apply sha `95195ca5…` 代号 0）、④ 真人「你好，你是谁」→「你好，我是 BI Plus…」无代号（5730503193 转录）、⑤ 两轮 `system_prompt_digest = ee39a24ea28d`（系统写出）；生产 ⑥（dry 相等 → apply：文件 `95195ca5…`、登记 = 新 sha、两份副本在，产品负责人亲跑 5730835559 + 代理只读回读）、⑦（v2.5.2 部署预检以新登记通过，容器已切换）、⑧ **L5** 真人「你是谁」→「我是 BI Plus…」无代号 + 生产两轮摘要 `ee39a24ea28d`（5731911531 / 5731923398）；⑨⑩ 随 W1 / S-3-1 回写。授权页标题 Cloudflare 侧未部署（裁定 12，未验证层级已列） | 验收矩阵对「对外名称规范」无 V-* 断言（`验收矩阵.md:58`）；本 Trace 不新增 | PR #847；#843 评论 5725297616 / 5726402083 / 5727546061 / 5730503193 / 5730517022 / 5730835559 / 5731911531 / 5731923398 |
| **E-5** #841 方法 v22 仓库侧四条 | 四条各有一处落点并合入 `main`（批终链三项自检 / 记录 PR 门禁计费 / 判全绿含 App 检查 → `验证与门禁.md`；估算基准 → `docs/traces/README.md`）；开工必读集体量预算退出码 0（加多少删多少）；不改产品行为与 CI 定义 | 文档（全局 1） | 文档（全局 1）达成：四条落点合入 `main`，预算 45046B | — | PR #846 `df0d86eb`；#843 评论 5725391087 / 5725489219 |
| **E-6** v2.5.2 上生产（拉式） | `release/2.5` 候选四镜像 digest 与正式版逐一相同（清单由 CI 写）；预发由拉取代理自动部署 rc、版本验收单固定项全过、验收记录 PR 合入 `main`；产品负责人点 Release Promotion 后生产拉取代理自动部署；生产迁移头 = 链头（0098）；三服务 healthy 且 15 分钟无非预期重启；既有业务行数不变；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**（由队列执行服务写入 task 表） | **L5（只认真人）** | **L5 达成**：Release `v2.5.2` 四 digest = rc.107 清单、`promotion.acceptance_commit = 3b4cf2a7`；生产拉取代理 13:46:30Z 切换、观察期满 healthy 零重启；迁移头 0098；行数与提升前逐一相同；产品负责人 Bot-Prod 真实问数 `tsk_01M2TGBY…` **`succeeded`**（队列执行服务写出） | — | #843 评论 5731923398（回读）/ 5731911531（真人）/ 5731955178（W2 收口）；Promotion run 35351716704 |
| **E-7** 里程碑与工作单收口 | 2.5.2 里程碑 open 只剩 #843 或为 0；#784 / #834 / #836 / #837 / #841 各有关闭评论；#827 按裁定处置；CHANGELOG `[2.5.2]`、runbook 摘要行、验收矩阵现值回写；方法候选归档到 #842 | — | **已完成**：CHANGELOG `[2.5.2]` / runbook 11.2 / 矩阵 V-投递-11 `已验证` / 当前能力 v2.5.2 + 自称一句（PR #852）；#784 / #834 / #836 / #837 / #841 各有关闭对账评论、#827 按裁定 8 关；方法候选见复盘评论 §6，由守望者归档 #842 | — | PR #852 `87178d40`；关闭评论 5732298812 / 5732300707 / 5732302740 / 5732304795 / 5732306711；收口 5732344438；复盘 5732359764 |

## 批终链（W1 唯一代码批；全绿才进 W2）

本机完整门禁绿（tmux 窗口独占跑，`umask 022`，期间零其他测试进程）→ 唯一独立审核者 fable xhigh 批量审查（一次，审查卡带 §4 威胁模型与假证据六型；r1 已先审 #836 / #841 两枚小 PR 头）→ 统一修复包 → 同一审核者定向复核并对冻结候选出「可合」（一次，兼把关）→ 外审（codex `gpt-6-astra` high 绕过面 + agy `gemini-3.8-flash-high` 用户可见后果，各一轮 = 生产前走查）→ 冻结候选 → CI 完整门禁 + `Epic Verdict` → 合 `main`。冻结后回填：候选 SHA `13f59848`（候选 3；候选 1 `1368c79e` / 候选 2 `7c48d5a5` 已被修复包取代）（PR #847 头 `13f59848`）；本机 full `Ran 7451`（候选 1）/ `Ran 7459`（候选 3）；门禁 run PR #847 Epic Full（gate 11m52s / image 5m37s）+ `Epic Verdict` pass；外审登记行 #843 评论 5726486279（codex `gpt-6-astra` high 07:00–07:04Z 136,460B 输入 → P1 ×1 / P2 ×6，agy `gemini-3.8-flash-high` 07:00–07:03Z → 0）；合并 `97974be8`（2026-09-18 08:24:00Z）。Fable：r1 5725391087（#845 / #846 可合）、r2 5725986433（修后可合）、r3 5726564897（可合）、r4 5726806274（可合）。

## 版本验收单（预发一次、生产固定项一次；E-6 用）

固定项：① 迁移头 = 链头（0098）；② 三服务 healthy 15 分钟；③ 既有业务行数不变；④ 一次真实问数 `succeeded`；⑤ 四镜像 digest 与正式版清单逐一相同；⑥ 部署由拉取代理 + 部署器完成的留痕（计划 / 执行 / 状态三份记录）。

随版本项：#784 真人「你是谁」+ 终态审计 `system_prompt_digest` = 新值（预发、生产各一次）；生产 preflight 以新登记通过（E-4 ⑦）；#837 / #834 / #836 / #841 无预发或生产项（各自 L2 / L3 / 文档层级随批次取得）。

验收记录：`deploy/releases/acceptance/v2.5.2-rc.107.json`（PR #851 `3b4cf2a7`；`manifest_sha256 = 76c0cf03…`；`recovery.previous_tag = v2.5.1`；迁移头不变，回滚无库结构回退项；`pg_dump` `pre-promotion-252-20260918T133735Z.dump` 为最后恢复点）。**预发单**：固定项 ①–⑥ + 随版本项全过（5730517022）；**生产单**：固定项 ①②③⑤⑥ + ④ L5 + #784 ⑧ 全过（5731923398）。

## 未验证层级（收口时如实列）

- **#837 真实积压下的单轮耗时与事务时长**：现网稳态候选 0–2、不具备触发条件，本 Trace 不认领 L4b / L5，如实留白。
- **#834 / #836**：零运行时改动，不涉及预发或生产验证。
- **#784 授权页标题**：`workers/oauth-bridge` Worker 本 Trace 不重新部署（裁定 12），Cloudflare 侧仍显示旧标题直到下次受控验收部署。
- **预发与生产的配置差异**：预发 `public-config.json` 未登记 `runtime-config`（裁定 6 不登记），提示词文件的登记核对只在生产被真机验证一次。
- **拉式部署链**：`recover` / `resume-migration` / 回滚未在真机触发（沿 #770 / #812 结论）。
