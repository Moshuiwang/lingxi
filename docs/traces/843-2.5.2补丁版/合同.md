# 开工合同：2.5.2 补丁版（Trace #843，Issue 链接：https://github.com/Moshuiwang/lingxi/issues/843）

> 方法版本：[#678](https://github.com/Moshuiwang/lingxi/issues/678) **v22**（2026-09-18 发布）；下一版修订 Issue：[#842](https://github.com/Moshuiwang/lingxi/issues/842)。合同版本：**v1**。批准依据 = 产品负责人 2026-09-18 指令（原话要点：「作为规划者，读取 678 的 v22 版本，开始规划下一个版本的具体 trace」）+ 2026-09-18 北京 0 时前后经 #827 复核选择题给出的裁定（「2.5.2 单独发一版」，逐条见「已裁定事项」）。**本 PR 合并即批准 v1。**
> 一条获批合同 = 编排者长程自治的全部授权依据；未写进合同的授权不存在。合同只认产品负责人裁定（原话 + 时间）与输入 Issue 的可观察完成标准；上一任执行者自设的约束不继承。
> 体量：本文约 37KB（超 32KB 去重线、未超 40KB 硬上限；必须留在本文的是授权终点与各道 Gate、已裁定事项、预算上限与带文件边界的步骤表——移出即绕过批准）；开工能力、额度、环境快照与里程碑分诊表在 `开工快照.md`；逐 Epic 判据与版本验收单在 `验收.md`；输入单的产品裁定已写进各单正文或裁定转贴评论，本文只留编号。

## 1. 目标

- **用户结果一句话**：**只发一版 v2.5.2**，由**拉式部署器**发到生产并经产品负责人一次真人验收；用户在飞书里从此只看到「BI Plus」自称（#784，含 Bot-Test 授权页标题）；三条清理路径单轮有行级上限、大积压不再永久撞语句超时（#837）；连接借用的误用面由门禁钉死、模块说明与实现一致（#834）；棘轮门禁三处过期表述改为如实（#836）；方法 v22 归仓库侧的四条同步项落文档（#841，裁定 5 并入）。
- **里程碑（何谓「到了」）**：生产三服务跑 v2.5.2 正式版四镜像（digest 与正式版清单逐一相同），升级由生产主机的拉取代理 + 部署器完成，15 分钟观察全 healthy；**产品负责人用 Bot-Prod 完成一次真实问数（L5）并在同一会话问「你是谁」，回答含「BI Plus」、不含内部代号**；该轮 worker 终态审计 `system_prompt_digest` 等于新提示词摘要。
- **完成定义**：里程碑 2.5.2 关闭 #784 / #834 / #836 / #837（+ #841）；里程碑 open 只剩本 Trace 或为 0；#827 按裁定 8 处置。
- **里程碑分诊表**：见 `开工快照.md`（四张单四项齐；#784 的两条规划期未知已由本次实读补齐，转 Ready 只差合同批准与裁定 11 的写动作）。
- **既定条款**：交付终点按 `#678` §一逐项声明；**正式版发布 ≠ 生产部署 ≠ 真实用户拿到**，三者分别取证。

## 2. 授权终点

- **编排者可自动执行到**：合并批次 PR 进 `main` ☑（CODEOWNERS 保护路径除外：`docs/traces/**/合同.md`、`deploy/releases/acceptance/`、裁决层三文件——由产品负责人批）；预发部署 ☑（拉取代理自动部署候选 + 收口后停用；预发常设授权 #469）；**预发持久化脚本（G-1、G-4）按裁定 3 由编排者直接跑**——投放与执行沿用本机已登记的白名单路径 `/tmp/lingxi-s26/`（规则 `scp * biai-stage:/tmp/lingxi-s26/*`、`ssh biai-stage sudo -n bash /tmp/lingxi-s26/*`），被分类器拦下即回退为幂等脚本 + 逐步回读 + 回退脚本交产品负责人 `!` 亲跑；**生产发布 = 拉式部署器自动执行，触发只有一个：产品负责人点 Release Promotion（提升即放行）**。
- **唯一授权终点**：v2.5.2 由拉式部署器发到生产并经真人验收（E-6），且 #784 在生产真实生效（E-4 生产段）。
- **只有产品负责人本人能做的动作**：① 合并本合同 PR 与修订 PR；② 批准一个进 `release/2.5` 的同步 PR 与一份验收记录 PR；③ G-5 同一在线段：`pg_dump` → 生产 #784 脚本 dry + apply → 点 Release Promotion；④ Bot-Test 一次「你是谁」+ 一句问数；⑤ Bot-Prod 一次真实问数 + 「你是谁」；⑥ 收口裁定（裁定 10 可预批）。
- **G-1 预发复活（合同预授权）**：脚本 = #812 已实跑的 `s27_g1a_stage_revive.sh`（sha256 `44a0bed2…`：起三容器 → 等 healthy ≤ 180 秒 → `enable --now` timer → 只读回读状态账；回退 `s27_g1a_rollback.sh` sha `3cc4c3cb…`）逐字复用；触发 = W1 已合 `main`；有效期 = 当次预发窗口。
- **G-4 #784 预发应用（合同预授权）**：脚本 `s28_784_prompt.sh`（S-1-3 产出；模式 `dry | apply | rollback`）只把 `/opt/lingxi/runtime-config/system_prompt.md` 第 1 行换为 D-2 措辞、末尾追加 D-2 自称规则一行，旧文件以 `.bak-84741170` 保留；预发 `public-config.json` 未登记该文件（本次实读 `files.worker = {}`），预发不改登记；触发 = 假根目录两形通过 + 预发 `dry` 输出与预期摘要逐字相等；先 dry 后 apply。
- **G-5 生产（显式动作，产品负责人亲跑）**：顺序固定 = `pg_dump` → `s28_784_prompt.sh dry`（只读打印将改什么、新摘要、登记差异）→ `apply`（改文件 + 把 `public-config.json` 的 `files.worker["system_prompt.md"]` 改为新 sha256；两份改前副本落 `/root/lingxi-s28-backup/`；属主权限保持 `root:root 0644`）→ 点 Release Promotion；部署器 preflight 以新登记核对文件、以 #804 的 old / new 语义过配置指纹变化；任一步失败即停在该步，`rollback` 还原两份副本，不带病提升。脚本投放按裁定 4。
- **显式除外（每次单独请示）**：生产库任何 `downgrade` / `pg_restore` / 控制台恢复；生产任何凭据变更；生产 `public-config.json` 与 `system_prompt.md` 除 G-5 所列以外的任何改动；删除生产回滚点（产品负责人已说不重要、不催）；向未获批真人发飞书消息；预发之外任何主机的权限变更；规则集与 CODEOWNERS 改动；重新部署 `workers/oauth-bridge` Worker（裁定 12）；远端分支删除（`push --delete` 被本机分类器拦，列为产品负责人网页动作）。
- **生产执行通路**：生产写动作只有产品负责人本人；代理在生产只读，固定形态 `ssh biplus-prod 'sudo -u bi-ai-deploy …'` 扁平单条（本机已登记 allow 规则）。
- **常设授权引用**：预发全权限（#469，不含 sshd / 系统账号变更）；`gh pr ready` / `gh pr merge` 满足既定条件不再请示；生产只读 allow 规则；白名单路径 `/tmp/lingxi-s26/`。
- **既定条款**：决策攒清单一次提交；单字裁决有效并留痕；临时授权带失效时点，到点失效不得滚动沿用。

## 3. 不做清单

- 不做 2.6.0（#809 迁库另开 Trace、#835、#815、#838、#712）、3.0.0 #810；不做 #839（产品负责人裁定不排期）、#814、#803（期限另做）、#673（2026-12-01 前另做）。
- 各单「不做」段全部沿用：#784 不改仓库代码标识与内部文档里的「Lingxi」、不改 `content.toml`、不改模型与问数逻辑；#834 不改 `connect()` 返回类型、不包装游标、不改池机制与调用点写法；#836 不实现基线历史比较（D-24）、不改门禁判定与基线数值、不搬两个 apps 文件（D-4）；#837 不改事务结构、不做环境变量上限、不加索引、不放大超时常量。
- **不重新部署 `workers/oauth-bridge` Worker**（裁定 12）：仓库改动合入即收口，Cloudflare 侧留下次受控验收。
- 不在预发登记 `runtime-config` 到 `public-config.json`（裁定 6：记录为预发与生产的已知差异）；不清生产回滚点；不修订方法 #678（候选归档到 #842）；不给外部代理合并权、不让其实施合入仓库的产出（D-8）。
- **既定条款**：新发现问题默认只登记不立项——主线灯塔：不做它，v2.5.2 能不能由拉式部署器到达真实用户且用户看到 BI Plus？疑问句只答不动手。

## 4. 验收标准

- **可观察完成标准**（细则见 `验收.md` E-1 … E-7）。Trace 级硬判据：
  1. **#837**（E-1）：三条路径各为一条带 `LIMIT` 的语句；上限生效、连续前进、未处理行逐字不变、大单会话仍前进、冲突只作废一条语句、日志与返回值不含行内容——本机隔离真库红绿各一次（`Ran N` 可见）；停止条件命中（须改事务结构）即该路径移出 2.5.2 转登记，不硬做。
  2. **#834**（E-2）：门禁对全仓 199 处调用点退出码 0、一处不改；四种违规样本各判红一次并钉住；`test_postgres_connection_reuse` 真库全绿且 N > 22；`postgres.py` 四处说明与实现逐条一致、目标合同句仍在。
  3. **#836**（E-3）：三处文字与实现 / CI 配置 / Issue 状态一致（审查者逐字对照）；`--refresh` 后 `git diff` 为空；三条棘轮脚本、用词门禁、文档体量预算退出码 0；五份基线文件条目零变化。
  4. **#784**（E-4）：预发与生产 `system_prompt.md` 代号计数 = 0、sha256 = 预期值、`.bak-84741170` 保留；生产登记摘要 = 新 sha256；预发与生产各一次真人「你是谁」回答含「BI Plus」、不含代号（脱敏截图）；该轮 worker 终态审计 `system_prompt_digest` = 新 strip 摘要（系统写出）；授权页标题不含代号；runbook 两行摘要与 `deploy/拉取代理.md` §十 结果码行回写。
  5. **v2.5.2 上生产**（E-6）：digest 三方一致（候选清单 = 正式版 = 生产运行容器）；迁移头 = 链头（0098，无新迁移）；15 分钟 healthy；既有业务行数不变；**产品负责人 Bot-Prod 真实问数 `succeeded`，L5 只认真人**。
  6. **候选变化即旧结论失效**：预发验的是 rc 四镜像，正式版必须是同一组 digest；候选 SHA 变化后旧审核结论按影响失效。
- **证据等级**（`验证与门禁.md` L 口径）：生产关键路径 **L5** 只认真人；代理在生产的回读写作「真实链路 · 生产 · 代理 · 只读」；预发 L4a；纯代码 L2；本机真库 L3。**每条判据写明被判的值由谁写入**：部署器状态账、拉取代理日志、worker 终态审计、task 表由系统写出；提示词文件与登记摘要由脚本写出并回读；「你是谁」回答由真人截图取证。
- **真人验证块只放两处**：预发（与验收记录 PR 同一次触点）与生产（与 G-5 同一在线段）。先出示就绪自检，一次一条指令；中途不请产品负责人试错。**真人验收是缺陷发现口**：预留 **1 轮打回**，收口对账写「因打回重做的轮次：预估 / 实际」。
- **门禁不打折**：批次冻结候选跑一次本机完整门禁（tmux 独占、`umask 022`、期间零其他测试进程）+ 一名未参与实现的 fable xhigh 内审 + 修复包后同一审核者定向复核出「可合」+ CI 完整门禁 + `Epic Verdict`；#836 按裁定 7 以卡面机器可判清单 + CI Epic Full 替代本机完整门禁。**假证据六型每个固定候选必查**（#837 测试绕过真实 UPDATE、上限断言删掉仍绿；#834 正反例被条件跳过、豁免过宽；#784 断言写「已实测预发」而未跑）；门禁绿与变异红都以日志 `Ran N`（N > 0）为准。
- **审查卡威胁模型写死**：#837 = 上限做成环境变量或参数、`SKIP LOCKED` 把被锁行变成永久跳过、为本轮干净清掉未到期 / 未送达正文、日志带正文、连续执行不收敛、事务结构被改；#834 = 别名 import / `getattr` / 额外关键字参数绕过 AST 判据、豁免路径过宽、「三件堵不住的事」被写成已堵；#784 = 脚本非幂等或非原子替换、改文件不改登记（生产）、改错文件（家目录遗留副本 vs 挂载目录）、除第 1 行与追加句外任何字节变化。同类绕过第二个变体即停止补丁、上提「信任来源」裁定。
- **既定条款**：证据只认仓库 / GitHub 留痕；外审每次调用当场登记一行（时间、工具与模型、候选 SHA、读入字节数、卡号、结果条数）。

## 5. 成本预算

> 每项写「预估 / 上限」；收口对账分「可测」与「估算」两行。能力与额度快照见 `开工快照.md`（2026-09-18 03:1xZ 实读）。估算基准 = #812 实测：Opus high 实施单卡 30–60 分 / 0.2–0.4M token；Fable xhigh 审核 8–17 分 / 0.10–0.29M；本机完整门禁约 26 分独占；批次链固定成本 ≈ full 26 + Fable 15 + CI 20 + 触点等待 ≥ 30 分；`deploy/**` 与 `CHANGELOG.md` 触发完整门禁约 12 分；同步 PR 按 CI 30 分 + 产品负责人批估。

- **经济化措施（不降质量与门禁）**：一个代码批 + 一个预发 / 生产窗口；把关并入定向复核；两枚小 PR 合在 Fable r1 一起审、先绿先合；外审只在批次终点一轮即生产前走查；预发脚本由编排者直接跑；runbook 摘要行并进收口 docs PR 与 CHANGELOG 同一次完整门禁；复修续派原子代理。
- **云端完整门禁**：预估 **6** / 上限 **9**——#836 PR 1（+1 重跑）、批次 PR 1（+1）、同步 PR 1（含 App 裁决层复跑）、验收记录 PR 1（`deploy/releases/acceptance/` 走完整门禁）、收口 docs PR 1；#841 与任务表记录 PR 走秒级路由不计；1 轮真人打回已计入上限。门禁耗时余量每次登记，低于两成先调上限。
- **Release Candidate** 预估 **1** / 上限 **3**；**Promotion** 1 / 1；**本机完整门禁** 预估 **2** / 上限 **4**（冻结候选 + 修复包）；**预发窗口** 1 / 2；**生产部署** 1 / 1（回滚不计）。
- **产品负责人触点**：预估 **6** / 上限 **9**——合同批准 + 裁定清单 1；同步 PR 批 1；预发「你是谁」+ 验收记录 PR 批 1；G-5 1；生产真人问数 + 「你是谁」1；收口裁定 1（裁定 10 预批则为 0）；打回与脚本被拦回退余量 3。每次先出示就绪自检、一次一条指令。
- **时间窗**：**合同批准当日（09-18 北京下午）开工 W1，当晚冻结候选并合 `main`**；W2 预发 + 生产排产品负责人 **09-19（周五）在线段**；收口 09-20 前；硬上限 09-23（裁定 1）。W1 实施串行估 3.5 小时 / 并行估 1.2 小时 + 整合 0.5 小时；批次链固定成本约 2 小时；W2 机器时间约 3.5 小时 + 触点等待；W3 约 2 小时。
- **PR 数**：预估 **9** / 上限 **12**：合同 1；#836 1；#841 1；批次 1；同步 1；验收记录 1；收口 docs 1；任务表记录 2。
- **子代理**：预估 **10** / 上限 **14**——实施 5（P-A #837、P-B #834、P-C #784、P-D #836 → P-E #841 同一人顺序）+ 复修 2 + 内审 3（Fable r1 小 PR 批审、r2 批次批审、r3 定向复核兼把关）；规划期 0 个子代理（规划者自核约 120 万 token 单列）。每次派发自挂兜底观察；不得再派子代理；单体预计超 60 分钟必须切分；预计时长写进派发卡。
- **模型配比**（沿用 2026-09-01 路由与 #812 终态）：编排者 **opus max**；实施 / 复修 **opus high**；唯一独立审核者 **fable xhigh**（**3 次 / 上限 5**；Fable 周窗 ≥ 80% 时降为 opus xhigh 并留痕）；规划者 / 守望者 fable 常驻不计次。
- **外审配额**：预估 **2** / 上限 **4**（快照：codex 周窗已用 56%、agy 周窗剩 98%）：批次终点 codex **`gpt-6-astra` high**（绕过面：#837 锁与超时、#834 门禁绕过、#784 脚本）+ agy **`gemini-3.8-flash-high`**（用户可见后果：提示词改动语义、授权页标题、文档措辞）各一轮；两条通道 2026-09-18 03:09–03:10Z 实机测试通过；astra 不可用回退 `gpt-5.6-sol` high 并留痕。**降档**：任一家周窗 ≥ 80% 或五小时窗 ≥ 90% 时只保留 codex 一轮；外审不作为独立审核者，发现逐条回源裁定；判活用一次真实调用，stdout 非空才算。外部代理只做只读核实 / 探针实跑 / 交叉验证（D-8）。
- **额度停派线**：任一家周额度剩余 **≤ 2%** 停止派发、完成当前安全单元后交接或等重置；2%–5% 照常派发。
- **并行**：并行组 **G1** = P-A / P-B / P-C / P-D（→ P-E）四路同时（文件边界见步骤表，不重叠）；**并行上限 4 路**（tz 15 GB 内存、6 核；每路独立 worktree + 独立测试库容器 `lingxi-i<单号>-pg` 独立端口、`trust` 认证；本机完整门禁期间零并行测试进程；编排者一次最多回收 4 路）；整合、门禁、内审、合并道一次一个。收口对账写计划并行度 / 实际并行度 / 因冲突返工次数。
- **token**：规划期可测约 120 万；执行期估算 **300–500 万** / 上限 **800 万**。

## 6. 主线声明

- **主线**：让 v2.5.2 经拉式部署器到达生产真实用户，且用户看到的自称是 BI Plus。W1 只装四张单的改动，#836 / #841 小 PR 在等 CI 的空档里做；任何派发前先问「不做它主线是否受阻」。
- `execution_adapter`：`claude_tmux_named_session`——常驻 tmux session `TZ-server` + 每批次一个 window（`843-w1` / `843-w2` / `843-w3`；收口点当任上下文 < 50% 可续任同一窗口）。**守望者 = 规划者会话转任**（窗口 `TZ-server:1` 更名 `843-plan`，裁定 9）；**本合同授权守望者拉起首任编排者与失联后的继任者**（只拉起、不派活、不持合并权）。守望者须同时挂事件型与定时型两类值守机制，上线前先对机制本身跑一遍有效性判据；汇报固定五项；不得声称未被机制支撑的巡检频率；窗口关闭即两类机制失效，由产品负责人重新拉起。
- **会话与工作树纪律**：会话在主检出 `~/project/lingxi` 拉起，隔离工作树放 `.claude/worktrees/`；派发卡「现场」四行照 #812（基于 `main` 头、`umask 022`、门禁红先在基点复现、禁改 `git config`）；长命令显式传超时；改完先 commit 再汇报。
- **编排者只派不写**：不写产品代码、不写迁移；例外只有 `docs/traces/` 记录、任务表勾选、收口 docs PR（CHANGELOG / runbook 摘要行 / 验收矩阵现值）、预发脚本投放与执行、预发与生产只读回读。
- **转达与留痕**：会话间转达一律走运行时会话间消息；裁定与交接落 Trace Issue，代理窗口里键入的裁定以 Issue 回执为准。
- **分支纪律**：批次分支 `trace/843-w1-patch`；小 PR `task/843-836-ratchet-wording`、`task/843-841-method-sync`；同步 `sync/release-2.5-843-w1`；验收记录 `accept/843-rc107`；纯记录 `record/843-…`。**分支名不以 close / fix / resolve 结尾，PR 标题不以 Issue 号开头**（#797 事故）；`trace/**` 首推带 `Image-Candidate: true` 空提交；候选冻结后的纯记录提交不推回该 PR；收口时 PR 头 SHA 逐字等于验收单里的候选 SHA；合并前先核审核「可合」留痕评论链接。
- **单一写入者**：当任编排者持主工作树与合并权；实施子代理各用独立工作树；审核子代理只读固定候选；面向 `main` 的 PR 串行合并、先绿先合、后合的先并 `main` 现头再重跑门禁；`CHANGELOG.md` 与 `pyproject.toml` 只由 P-A 一人改。
- **既定条款**：批次终点一次完整门禁 + 一次批量审查 + 唯一合并人合并；收口固定序列（收口评论 → 复盘评论 → 交接评论 → 释放租约 → 继任 window → 接管登记 → 退场）；收尾必含环境盘点、任务表回写、收口评论（UTC + 北京、预算与外审台账、门禁余量、触点、墙钟按环节、并行度对账）。

---

## 合同区

- 方法版本 **v22**；下一版修订 Issue **#842**；合同版本 **v1**；执行阶段 **Planning → 待批准**（本 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- 基线提交与核对时间、规划者会话、能力与额度、环境快照、里程碑分诊表：见 `开工快照.md`。角色模型 / 推理档位：同 §5。
- 编排者接手入口与第一恢复点：`/trace-kit:takeover` → 读本目录四份文件 + Trace Issue 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + Trace Issue 最新评论**。
- 触发项：外部写 = **有**（预发主机文件与 timer、生产提示词文件与公开配置登记、生产发布）；共享资源 = **有**（预发、主检出、合并道、本机完整门禁）；跨任务 = **有**（三个批次窗口可能接力）；独立审核 = **有**（Fable 内审 + 双路外审）；特殊预算 = **无**（三家额度充裕）；首次真机 = **有**（#784 宿主脚本：假根目录两形 → 预发 dry → 预发 apply → 生产 dry → 生产 apply，按 3 倍估时，候选与亲跑各预留 2）。

## 已裁定事项（产品负责人裁定；编排者不得再就这些请示）

| 编号 | 裁定（原话要点 + 时间） | 对执行的约束 |
| --- | --- | --- |
| **D-1** | 2026-09-18 北京 0 时前后：**2.5.2 单独发一版** = #784 / #834 / #836 / #837（裁定转贴评论 5717598628 / 5717599641 / 5717601667 / 5717603211） | §1；一个代码批 + 一个窗口 |
| **D-2** | 同时：#784 首行**只换名字**「你是 BI Plus，面向公司内部员工的只读数据问答助手。」；**写入**自称规则「自称与对外名称只用 BI Plus，不使用任何内部代号」；`workers/oauth-bridge` 授权页标题**算用户可见面并入**；宿主改动与登记同赶同一预发 / 生产窗口 | S-1-3 / S-2-2 / G-5 |
| **D-3** | 同时：#834 **保留**目标合同句、只追加「尚未完全兑现」；修复卡 #835 并入 2.6.0 | S-1-2 |
| **D-4** | 同时：#658 延后未搬的两个 apps 文件**不再迁移**，基线头部如实记录、无承接单 | S-1-4 |
| **D-5** | 同时：#837 worker 侧两处同型无界路径**一并纳入**，同型行级上限；停止条件对三处各自适用 | S-1-1 |
| **D-6** | #812 D-14（09-16）+ 09-18：2.6.0 = #809 迁库另开 Trace，追加 #835 / #815 / #838；#839 不排期；#811 已关 | §3 |
| **D-7** | 09-17 盘点留项回复「**Y**」：预发 `public-config.json` 保留 Y、不还原 X | S-2-1 不动登记；X 副本可清 |
| **D-8** | 方法面已定：额度停派线 2%、收口点上下文 ≥ 50% 换人、退场前必写复盘（v22 §五）；外包分档（09-15，合入产出一律主编码代理）；方法候选归档到 #842 | §5 / §6 / S-3-4 |

## 计划执行步骤

> Owner：「实施」= opus high 子代理；「内审」= fable xhigh；「外审」= codex / agy 按 §5。**PR 终链固定两步**：本机段（秒级检查 → 本机完整门禁 → 内审 → 统一修复包 → 定向复核出「可合」→ 外审）与远端段（冻结候选 → 推送 → 开 PR → CI + `Epic Verdict` → 合并）。下表只写附加项；文件边界 = 该卡可改的全部文件，越界即停。

### W0 前置（编排者；合同合并后立即开工）

| ID | 产物 | Ready 条件 / Gate | Owner | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- |
| S-0-1 | 开工硬边界核实：合同已合并、守望者两类机制已挂并自检、三家额度实读、预发只停不删（timer disabled、三容器 Exited）、生产 v2.5.1 三容器 healthy（只读回读）、`main` 头与 `release/2.5` 头实读 | 合同 PR 合并 | 编排者 | 接管登记评论含机制标识与启动证据、额度现值、两台主机回读 | 缺任一项 → 停，登记等待 |
| S-0-2 | Issue 写动作（裁定 11）：#784 正文顶部补 09-18 裁定块并转 `Ready`；#836 正文顶部补裁定块；#841 挂里程碑 2.5.2 并转 `Ready`；各单领取留言（执行者 + 有效期）换 `执行中` | S-0-1 | 编排者 | 各单读回 | — |

### W1 代码批（分支 `trace/843-w1-patch`；并行组 G1 四路同时 → 串行整合）

| ID | 产物 | Ready / Gate | Owner / 工作区 | 并行组 / 文件边界 | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- | --- |
| S-1-1 | **P-A #837**：三条路径各改为一条带行级 `LIMIT`（常量 500）的语句 + 返回值改为行数 + 文档字符串写实 + 本机隔离真库用例六项 + 验收矩阵 V-投递 新行（≤ 800B）+ V-管理-39 邻近勘误；**同卡改 `pyproject.version` → 2.5.2、`CHANGELOG.md` `[Unreleased]` 写四张单条目** | S-0-2 | 实施 ×1 / `.claude/worktrees/843-pa` | G1 · `src/lingxi/adapters/postgres_conversation/{_transaction,_queue_outbox,_queue_lifecycle}.py`、`src/lingxi/apps/scheduler/retention.py`、`tests/test_delivery_outbox.py`、`tests/test_scheduler_process.py`、新增测试文件、`docs/技术设计/验收矩阵-交付与投递.md`、`…-接入与管理.md`、`pyproject.toml`、`CHANGELOG.md` | E-1（L3）；`Ran N` | 停止条件命中 → 该路径移出并登记，卡面如实 |
| S-1-2 | **P-B #834**：`check_db_timeouts.py` 新增 AST 判据（默认复用 `connect()` 须在 `with` 上下文表达式位置；游标 / `execute()` 返回值最小逃逸检查）+ 四种违规样本正反例 + `postgres.py` 四处说明追加「尚未完全兑现」段（不动实现）+ 用例措辞与记录性真库用例 | S-0-2 | 实施 ×1 / `843-pb` | G1 · `scripts/ci/check_db_timeouts.py`、`tests/test_db_timeouts.py`、`src/lingxi/adapters/postgres.py`（仅说明文字）、`tests/test_postgres_connection_reuse.py` | E-2（L2 + L3） | 全仓非零 → 先查判据，不改调用点 |
| S-1-3 | **P-C #784**：① `workers/oauth-bridge/src/protocol.js` 标题改「BI Plus 开通」（该目录测试若引用同改）；② `deploy/拉取代理.md` §十 补 `public_file_configuration_changed` 一行；③ 宿主脚本 `s28_784_prompt.sh`（不入仓库；全文与 sha 贴 Issue；行为规格与判据见 `验收.md` E-4）；④ **本机假根目录两形测试**（预发形无登记、生产形有登记）各跑 dry → apply → rollback，预期新 sha256 与 strip 摘要由实施者本地推算并写进卡面 | S-0-2 | 实施 ×1 / `843-pc` | G1 · `workers/oauth-bridge/**`、`deploy/拉取代理.md`（仅 §十 一行）、scratchpad 脚本 | E-4（L2 + 假根目录） | 任一形失败 → 修脚本，不进 S-1-6 |
| S-1-4 | **P-D #836**（独立 PR `task/843-836-ratchet-wording` 直进 `main`；卡面禁与 #834 同 PR）：三处勘误 + 可选 ④ 括注；必读集体量余量 8B，落笔后实跑 `check_docs_size_budget.py`；**随后同一实施者做 P-E #841**（独立 PR `task/843-841-method-sync`，docs-only）：四条各落一处（建议：批终链三项自检、记录 PR 门禁计费、判全绿含 App 检查 → `验证与门禁.md`；估算基准 → `docs/traces/README.md`），加多少字同文件删多少字 | S-0-2；P-E 以裁定 5 为准 | 实施 ×1 / `843-pd` | G1 · P-D：`docs/技术设计/代码框架.md`、`scripts/ci/check_size_ratchet.py`、`scripts/ci/check_apps_pure_logic_ratchet.py`、`scripts/ci/apps_pure_logic_ratchet_baseline.txt`；P-E：`docs/技术设计/验证与门禁.md`、`docs/traces/README.md` | E-3 / E-5；Fable r1 审两枚头 → 先绿先合 | 体量超限 → 同文件删字，不抬预算 |
| S-1-5 | **串行整合**：P-A / P-B / P-C 各自 commit 后由编排者按 A → B → C 并入批次分支，先并 `main` 现头（含已合 #836 / #841），追加 `Image-Candidate: true` 空提交，**冻结候选 SHA**；本机段（本机完整门禁独占 → Fable r2 批审 → 修复包 → Fable r3 定向复核「可合」→ 外审两路）→ 远端段 → 合 `main`；W1 收口评论 | S-1-1…S-1-4 | 编排者 + 内审 + 外审 | 串行 | 收口评论；候选 SHA；`Ran N`；门禁 run；外审登记行 | 红 → 修复包 → 定向复核；两分支碰同一文件 → 退回规划者 |
| S-1-6 | **#784 脚本首次真机 dry（预发）**：投放到 `/tmp/lingxi-s26/`（sha 两端相等）→ `sudo -n bash … dry`（只读）→ 输出与卡面预期摘要逐字相等；不 apply | S-1-3 通过；预发只停不删也可 dry | 编排者 | 串行；不与本机门禁互斥 | dry 输出贴 Issue | 不等 → 回 S-1-3 |

### W2 预发 + 生产（同一预发窗口；产品负责人 2026-09-19 在线段）

| ID | 产物 | Ready / Gate | Owner | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- |
| S-2-1 | **G-1 预发复活**：`s27_g1a_stage_revive.sh` 逐字复用 → timer active、三容器 healthy、下一轮 `already_in_place`；预发 `public-config.json` 保持 Y（D-7） | S-1-5 已合 `main` | 编排者（裁定 3；被拦 → 产品负责人 `!` 亲跑） | 回读评论 | 未 healthy → 回退脚本，登记等待 |
| S-2-2 | **G-4 #784 预发应用**：`apply` → 回读 sha256 = 预期、代号计数 0、`.bak-84741170` 在；家目录遗留副本 `~/lingxi-runtime-config/system_prompt.md`（同摘要、非挂载）同步改为同内容并留 `.bak`，免下次误传；不改预发登记（裁定 6） | S-1-6 相等；S-2-1 | 编排者 | 回读评论 | 回读不等 → `rollback`，回 S-1-3 |
| S-2-3 | **同步 PR**：`main` 现头 → `release/2.5`（先并 `release/2.5` 现头 `5639500`；产品负责人批）→ 候选 **rc.107**（序号以工作流实际分配为准，下同）→ 预发拉取代理自动部署 → `verified`（预发无登记，配置指纹不变 → old = new） | S-2-2 | 编排者 + 产品负责人 | 候选清单 + 状态账 `verified` | 候选判红 → 产品负责人网页重跑；部署失败 → 按 `deploy/拉取代理.md` §十 处置 |
| S-2-4 | **预发验收**：版本验收单固定项 ①–⑥ + 随版本项（#784：产品负责人在 Bot-Test 问「你是谁」+ 一句问数 → 编排者回读该轮终态审计 `system_prompt_digest` = 新 strip 摘要）→ 验收记录 PR `accept/843-rc107`（`deploy/releases/acceptance/v2.5.2-rc.107.json`，恢复段 previous_tag v2.5.1、迁移头 0098 不变；产品负责人批） | S-2-3 `verified` | 编排者 + 产品负责人（同一次触点） | `验收.md` 版本验收单；脱敏截图留 Issue | 打回 → 修复包 → rc 再一枚（预留 1 轮） |
| S-2-5 | **G-5 生产**：就绪自检 → 产品负责人同一在线段：`pg_dump` → `s28_784_prompt.sh dry`（生产）→ `apply` → 点 Release Promotion → 生产拉取代理自动部署 v2.5.2（preflight 用新登记核对提示词文件；old = 上次 `verified` 指纹、new = 现值）→ 固定项 → 15 分钟 → **产品负责人 Bot-Prod 真实问数 + 「你是谁」** → 编排者只读回读（digest 三方一致、迁移头、行数、终态审计 `system_prompt_digest`） | S-2-4 记录 PR 合入 | 产品负责人 + 编排者（只读） | E-6 L5 + E-4 生产段 | dry 不等 → 不 apply、不提升；部署失败 → 回滚 = 切回 v2.5.1 四镜像（不降级迁移），单独请示；#784 回滚 = `rollback` 还原两份副本 |
| S-2-6 | 生产只读巡检（再看一轮 healthy；`qa_corpus` 新行 `system_prompt_digest`）；**预发不用即停**（`s27_g1a_rollback.sh`）；清 `/tmp/lingxi-s26/` 本 Trace 脚本与 `/root/lingxi-s27-backup/`（D-7 已裁 Y，X 副本与 F11 代理备份无用）；W2 收口评论 | S-2-5 | 编排者 | 收口评论 | — |

### W3 收口

| ID | 产物 | Ready / Gate | Owner | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- |
| S-3-1 | 收口 docs PR（`record/843-w3-docs`，走完整门禁）：`CHANGELOG.md` `[Unreleased]` → `[2.5.2] - 日期`；`deploy/生产部署runbook.md` 11.2 两行摘要改为新值（注明 2.5.2 起）；验收矩阵现值（#837 新行 L3 + L4a）；`docs/当前能力.md` 只在体量余量（134B）允许时补一句自称 BI Plus，否则不改并写理由；Trace 验收表回填 | S-2-6 | 编排者 | PR 合并 | 体量超限 → 先瘦身 |
| S-3-2 | 逐 Issue 对账关闭（#784 / #834 / #836 / #837 / #841）+ 里程碑 2.5.2 关闭 + #827 按裁定 8 + #842 登记本 Trace 候选去向 | S-3-1 | 编排者 | 各单关闭评论 | — |
| S-3-3 | 环境盘点（固定列「已清 / 留什么 · 为什么 · 谁清 · 何时」）：本机 worktree / 本地分支 / 测试库容器；远端已合并分支列产品负责人网页删；预发已停、脚本目录已清；生产 `/root/lingxi-s28-backup/` 两份副本留至下一版由产品负责人清；生产回滚点不催 | S-3-2 | 编排者 | 盘点评论 | — |
| S-3-4 | 收口评论 + 编排者复盘评论（六项）+ 交接或退场；守望者把方法候选归档到 #842 并写机制对账；规划者（守望者代写）合同对账复盘；按裁定 10 三条件满足即自关 Trace | S-3-3 | 编排者 + 守望者 | 收口 / 复盘评论 | — |

## 命中才展开

- **共享资源与释放**：预发（同一时刻一个客户端；S-2-1 起、S-2-6 停；**预发验收一律经拉取代理自动部署 rc，不走手工 compose**）；主检出（单写入者）；合并道（串行、先绿先合）；本机完整门禁（独占）；测试库容器每路独立（收口 `docker rm`）；`CHANGELOG.md` / `pyproject.toml` 单写者 P-A。
- **外部写与恢复**：预发主机（G-1 自带回退；#784 `rollback` 还原副本）；生产（发布 = 拉式；回滚 = 切回上一正式版镜像、不降级迁移，单独请示；#784 = `rollback` 还原两份副本；`pg_dump` 为最后恢复点，恢复动作显式除外）；GitHub（Issue 标签 / 里程碑改动按裁定 11 一次批准；PR 合并按 §2）。
- **首次真机**：#784 宿主脚本——假根目录两形 → 预发 dry → 预发 apply → 生产 dry → 生产 apply，每步回读；3 倍估时；候选与亲跑各预留 2。
- **独立审核 / 跨任务接力**：Fable 三轮 + 外审两路见 §5；接力见 `开工快照.md`「批后继任条件」。

## 批准时需一并裁定

1. **时间窗**：W1 今日（09-18）冻结候选并合 `main`；预发 + 生产排 **09-19（周五）** 在线段；收口 09-20 前；硬上限 09-23（可回复：是 / 生产改 09-21）。默认：**是**。
2. **§5 各项上限**（云端门禁 9 / rc 3 / 预发窗口 2 / 本机门禁 4 / PR 12 / 子代理 14 / Fable 5 / 外审 4 / 触点 9 / token 800 万）（可回复：批 / 改）。默认：**批**。
3. **预发脚本执行者**：G-1 复活与 #784 预发应用由编排者直接跑（沿用白名单路径 `/tmp/lingxi-s26/`，预发常设授权 #469），被拦即回退为你 `!` 亲跑（可回复：是 / 全部我跑）。默认：**是**。
4. **#784 生产脚本投放**：脚本全文与 sha 贴 Issue，由你亲自复制到生产 `/tmp/lingxi-252/` 后 dry → apply（可回复：我投 / 授权代理 scp 只投放不执行）。默认：**我投**。
5. **#841**（方法 v22 归仓库侧四条同步项，docs-only）并入 2.5.2 作独立小 PR、空档做（可回复：并入 / 顺延 2.6.0）。默认：**并入**。
6. **预发 `public-config.json` 是否登记 `runtime-config`**（本次实读预发 `files.worker = {}`、生产已登记）：默认不登记，记录为已知差异、留 2.6.0 迁库 Trace 一并处理（可回复：不登记 / 登记）。默认：**不登记**。
7. **#836 门禁形态**：以卡面机器可判清单 + CI Epic Full 替代本机完整门禁（可回复：是 / 照跑本机 full）。默认：**是**。
8. **#827**（[research] 工程质量评估，待验收）在 2.5.2 收口时关闭——四条发现已转化为 #834–#839 并按裁定处置（可回复：关 / 留）。默认：**关**。
9. **守望者 = 本规划会话转任**（窗口 `TZ-server:1` → `843-plan`）并获授权拉起首任编排者与继任者（只拉起、不派活、不持合并权）（可回复：是 / 否）。默认：**是**。
10. **收口裁定预批**：生产 `verified` + 真人问数 `succeeded` + 「你是谁」含 BI Plus、收口 docs PR 合 `main`、盘点无未登记残留——三条全满足即编排者自关 Trace 与里程碑（可回复：批 / 到时再说）。默认：**批**。
11. **GitHub 写动作一次批准**：规划者建 `[tracking]` Issue 与 v23 修订 Issue、开本合同 PR；合同合并后编排者把 09-18 裁定块补进 #784 / #836 正文、#784 转 `Ready`、#841 挂 2.5.2 转 `Ready`（可回复：批 / 改）。默认：**批**。
12. **`workers/oauth-bridge` Worker 不重新部署**：仓库改动合入即收口，Cloudflare 部署留下次受控验收（可回复：是 / 本次部署）。默认：**是**。
