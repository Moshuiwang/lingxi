# 开工合同：体验批——预开通主动告知与可发现性、多公司范围、管理卡真话、文案外置，上生产并放行九人（Trace [#606](https://github.com/Moshuiwang/lingxi/issues/606)）

> 方法版本：#147 **v16**。合同版本：**v1**。批准依据：产品负责人 2026-09-05 08:5x–09:1x 北京在 tz 本机会话逐项批准（范围「批」＝#586 / #587 / #602 / #585 四张；生产终点「批」＝九人名单、赵晋霞先行；#576「不纳」；未翻译标签「立」→ [#605](https://github.com/Moshuiwang/lingxi/issues/605)），原话「行，按你的说法来吧。批准你做的这些。开始做 trace 并安排编排者干活吧」。**本 PR 合并即批准 v1。**
> 合同只认两种来源：产品负责人裁定（原话＋时间）与输入 Issue 的可观察完成标准。上一任执行者自设的约束不继承；证据只认 GitHub / 仓库留痕。

## 1. 目标

- **用户结果一句话**：九位 CEO 在生产被预开通后**主动收到**一张 BI Plus 欢迎卡（问好、自我介绍、本人可用范围、示例、找谁），赵晋霞先行完成首聊问数，通过后其余八位照放；管理员不再被管理卡误报「已生效」；改一句用户可见文案不必重发版。
- **里程碑（何谓「到了」）**：① 四张输入卡＋#605 达到各自可观察完成标准并在 stage 自证通过；② 生产升级到 **v2.2.0**（含 #592 可读性重构）观察通过；③ 赵晋霞收到欢迎卡并首聊问数成功；④ 其余八位 apply 成功、欢迎卡送达 8/8。
- **完成定义（Delivered）**：#586 / #587 / #602 / #585 / #605 关闭各留对账；产品合同（主动告知条款、多公司范围）、当前能力、验收矩阵 V-行、部署配置清单回写合入；收口评论落 #606；环境盘点零残留（tz / stage / prod 临时物）。

## 2. 授权终点

- **编排者可自动执行到**：建分支与 worktree、开 **唯一** Draft PR（`batch/606-ux → main`）、`gh pr ready`、**合 main（K-9 有条件预批形态，条件见 §4）**、Main Publish 后升级 `biai-stage` 并做全部 L4a、Bot-Test 向产品负责人私聊发预检 / 样式卡（**累计 ≤ 8 条**）、关闭输入 Issue、文档回写、tz / stage 临时物清理、#606 评论留痕。
- **生产（块 Z）**：D-5 默认＝**整段预批**——就绪自检落 #606 逐项通过后，生产升级序列（步 0 工作副本更新 → digest 固定 → `migrate` → `--profile mvp up -d` → 健康回读 → 15 分钟观察 → 版本 tag）由编排者一次跑完、一条一条扁平命令逐条留痕；任一步异常即停并报产品负责人，不重试第二次、不自行回滚（回滚也是写动作，需单字）。**名单 apply 与 Bot-Prod 向真实用户发卡仍逐批单字**：dry-run → 「批」→ 赵晋霞 apply → 生产预检卡发给产品负责人本人 → 「批」→ 发赵晋霞 → 她首聊 → 「批」→ 其余八位。
- **显式除外（每次单独请示）**：生产任何未列入上段的写入；权限规则修改（分类器硬拦代理自扩白名单，需要时列清单请产品负责人亲加）；删除用户数据；撤销已发出消息；Bot-Prod 向名单外任何人发送；对外发布文案变更；#576 / #603 / #604 的任何实施。
- **生产 / 外部动作执行通路**：prod 命令唯一形态 `ssh biplus-prod 'sudo -u bi-ai-deploy <单条扁平命令>'`（现存唯一 allow 规则），docker 以 `sudo -u bi-ai-deploy sh -c "docker …"`；ops 脚本用 `/home/bi-ai-deploy/projects/lingxi-ops-venv/bin/python` ＋ `PYTHONPATH=src`，DSN 远端 source 不落 tz；#585 若需在 prod 建 `/opt/lingxi/runtime-config/` 下文件，属主由 W0-1 只读核实，若需 root 则列规则请产品负责人亲加（本批默认**不在生产启用外置文件**，只交付能力与文档，见 D-3）。stage：常设全权限（PM 2026-08-30，prod 除外）。
- **常设授权引用**：`git-claude-tz push`；gh 机器身份；`gh pr ready` / `gh pr merge`（PM 2026-08-06「以后不要再问」，单独成条执行）；P2 豁免（PM 2026-08-14，红线 / P1 不豁免、留痕）；stage 全权限；K-9 有条件预批形态（#592 先例）。
- **中途裁定方式**：决策攒清单一次提交；单字裁决（批 / 守 / 缓 / 不发）有效并留痕 #606；临时授权带失效时点（默认本批收口）；**决策清单 D-1…D-9 均有默认值，产品负责人不回即按默认**。

## 3. 不做清单

- 不做 #576 批量等就绪（登记，八人串行约 1–2 小时无人值守等待，已知情）；不做 #603 后台退出恢复；不做 #604 MCP 目标校验；不做群发 / 定时推送；不做管理员命令面发送入口；不做镜像私有化；不做 #566 / #567 部署自动化（生产仍走 runbook 逐步形态）；不改预开通静默裁定④本身（欢迎卡是独立的第二个动作）。
- 不单独先上 v2.1.0：生产从 v2.0.2 一次升到 v2.2.0，只开一个生产窗口。
- #600 内存查因由元守护并行做（stage 对照实验），**不在本批派发**；其修复若在冻结前落 main 则随批次自然带上，否则另立热修，不改本合同。
- 新发现问题默认只登记不立项（主线灯塔：不做它，九人能不能收到欢迎卡并问数？）；疑问句只答不动手；产品品味类修订夜间只出草案不落地。

## 4. 验收标准

- **每卡关卡**＝其 Issue「可观察完成标准（自证闭环版）」逐条，细则见 [验收.md](验收.md)。真人主观体验（截图好不好看）一律为产品负责人加菜、非关卡。
- **批终链**：本机 `check.sh full` 绿 → 推送 #1（Draft PR）→ Epic Full → Fable 独立审核①（一次、多视角）＋ **外审两轮并行：agy（产品承诺与文案真话 / 失败形态 / 用户可见后果）、codex（绕过面与越权路径 / 幂等与重复送达）** → 唯一缺陷账本 → 统一修复包 → 同一审核者定向复核② → 推送 #2（含 v2.2.0 定版）→ Epic Full 绿 → **K-9 条件**：CI 全绿、PR ready、就绪自检落 #606 逐项通过、main 仍在分叉点且无其他开放 PR、`gh pr merge --merge` → Main Publish → stage 升级回读 → 逐卡 L4a → stage 用后即停。
- **证据等级要求**：代码批 L2（Epic Full）＋ L3（独立审核＋两轮外审）；stage 关键路径 **全局 6（受控测试身份）**；生产升级 5（部署＋观察）；九人 **全局 6（真实用户）**——宣称交付前未验证层级逐条列明。
- **双通道纪律**：用户可见断言＝平台 API 回读（消息 / 卡片 / 正式表行）＋ 服务端结构化日志或审计行，两者一致才定论；冲突升产品负责人。
- **证据只认仓库 / GitHub 留痕**：审核结论、外审结论、L4a 结果、失败事件当天落 #606 评论或 PR；只存 `/tmp`、会话或交接词的状态视为不存在。
- **产品负责人真人验证块只放两处**：块 A（前置输入，2026-09-05 已给齐；剩余 D 项不回按默认；W0-3 样式选择是唯一一次中途单字）与块 Z（生产升级放行形态 D-5、名单 dry-run 单字、生产预检卡单字、赵晋霞首聊结果、八人放行）。每块先出示就绪自检（前提核对＋日志回读＋预判断点），一次只给一条指令。

## 5. 成本预算（建议值，本 PR 合并即批准）

- **CI 完整门禁（Epic Full）≤ 5 次，目标 3**：推送 #1（冻结候选）、推送 #2（修复包＋定版）、余量 1–3 次给门禁红复修；分类器自动提级不计主动消费。**PR：批次 Draft PR 1 个**（Trace 期间不开独立文档 PR，三件套回写随分支 commit）＋ 收口纯文档 PR ≤ 1（只跑文档门禁）。
- **时间窗**：2026-09-05 → 2026-09-09 北京（W0 与实施跨夜允许；生产窗口只在白天静默时段；名单 apply 与发卡只在工作时间）。超窗即停下报产品负责人，不滚动。
- **模型配比**：编排者 **Fable 5.1 / xhigh**（#592 先例，Opus 5 近日 529 频发；偏离 09-01 路由，批次边界可由产品负责人改）；实施 / 复修 **Opus 5 / high**（Opus 不可用时降 Fable xhigh 并留痕）；唯一独立审核者 **Fable 5.1 / xhigh 独立上下文子代理**（不参与实现、不直接修复；独立性第二级，产品负责人本合同批准）；外审 **agy 1 轮 ＋ codex 1 轮**（PM 2026-09-04 裁定）；规划者 / 元守护＝本 Fable 会话。
- **人次上限**：施工 / 复修子代理 ≤ 12；Fable 审核 ≤ 4（①②＋生产前 runbook 走查③＋余量 1）；stage 累计运行 ≤ 6 小时（Supabase 出口预算，用后即停）；Bot-Test → 产品负责人私聊 ≤ 8 条。
- 大 workflow（> 50 万 token）开跑前查配额余量；对抗验证先由编排者 grep / diff 坐实机械性发现，只把行为面派子代理。

## 6. 主线声明与批次纪律

- **主线**：九人在生产收到欢迎卡并可问数。任何派发 / 立项 / 修复前先问「不做它主线是否受阻」。
- `execution_adapter`：`claude_tmux_named_session`——常驻 session `TZ-server`，本批编排窗口 **`ux-b1`**（继任 `ux-b2`…），元守护＝规划者会话（窗口 `guardian`，Fable），只以外部证据判活（窗口存活＋ #606 留痕新鲜度，每 2 小时随生产巡检一并查）、不实施、不持合并权、转贴产品负责人裁定。
- **单批结构**：`batch/606-ux` 从 main `462eadf` 切出 → W0（只读盘点＋样式样卡＋#602 真库复现）→ S-1 / S-2 / S-4 / S-5 / S-6 并行施工（各自 worktree，`isolation: worktree`）→ S-3（S-2 后）→ 编排者集成 → 本机 full → 推送 #1 → 审核 / 外审 → 修复包 → 推送 #2 → 合 main → stage → 块 Z-1 生产升级 → 块 Z-2 名单 → 收口。
- **单一写入者**：tz 主工作树只由编排者会话使用（现检出 `main`）；实施代理独立 worktree、改完先 commit 再汇报、进等待必须上报；子代理声称「在等 X」时编排者当场挂兜底观察；**subagent 不得再派 subagent**。
- **批次收口固定序列**（v16 §6.8）：收口评论 → 交接评论 → 继任 window → 接管登记 → 租约转移 → 退场。收尾必含：环境清理与残留盘点（worktree / 远端分支 / stage 容器与演练数据 / prod 临时文件谁建谁清）、任务表勾选回写、收口评论（UTC＋北京双标注、Epic Full 次数 / PR 数 / commit 数、人次、审核轮次）。

---

# 执行图（`──►` 硬依赖；`◇` 产品负责人门；`⚡` stage 独占；`☢` 生产 / 真实用户写动作）

```
A-1 前置输入(已给齐) ──► W0-1 接管+只读盘点 ──┬─► W0-2 G-1 主动私聊探针⚡ ──┐
                                             ├─► W0-3 样式样卡⚡ ◇(单字/24h默认甲) ─┤
                                             ├─► W0-4 #602 真库复现(红) ──► S-5 #602 修复 ─┤
                                             └─► W0-5 #605 定位 ──► S-6 小修 ──────────┤
S-1 #587 多公司 ─────────────────────────────────────────────────────────────────┤
S-2 #586 核心 ──► S-3 #586 预检+ops 入口 ───────────────────────────────────────┤
S-4 #585 文案外置 ────────────────────────────────────────────────────────────────┤
S-7 文档面+v2.2.0 定版 ──────────────────────────────────────────────────────────┤
                                                                                   ▼
S-8 集成 → full → 推送#1 → Epic Full → 审核①+agy+codex → 修复包 → 复核② → 推送#2 → K-9 合 main → Main Publish
                                                                                   ▼
S-9 stage 升级⚡ → T-1…T-6 L4a（含预检真发 PM 1 条）→ 停 stage → 就绪自检落 #606
                                                                                   ▼
S-10 ☢ 块 Z-1 生产升级 v2.2.0（D-5 整段预批）→ 15 分钟观察 → PM 真问数(加菜) → tag
                                                                                   ▼
S-11 ☢ 块 Z-2 名单：dry-run ◇ → 赵晋霞 apply → 就绪 → 生产预检卡→PM ◇ → 发赵晋霞 → 首聊问数 ◇ → 八人 apply → 发卡 8 人 → 24h 观察
                                                                                   ▼
S-12 收口：docs 回写 → 环境盘点 → 收口评论 → #606 关闭 → 看板 / 记忆
```

---

# 合同区（v16 必填字段）

- 方法版本：v16；Trace 合同版本：v1；执行阶段：**Ready**（本 PR 合并时）；整体交付结果：Undetermined。
- 基线提交与核对时间：main = `462eadf`（v2.1.0，2026-09-05 00:4xZ；含 #592 可读性重构与 #593 热修）；发布镜像 `20260905-462eadfab562`（未部署）；生产 = v2.0.2 `20260904-c6fa5ac5f9ad`，迁移头 `0087`，三服务 healthy，网关 00:38Z 预防性重启后 166 MiB（#600 观察中）；stage 三容器停着（镜像 c6fa5ac，`.env.stage*` 含 5 s / 10 s 轮询键）；正式表 26 行；内测白名单 34（含九人 Bot-Prod open_id）；生产 `app_user` 1。
- 目标与范围 / 明确不做：见 §1 / §3。
- 整体用户 / 工程结果与证据终点：`User-path Delivered`（九人欢迎卡＋赵晋霞问数，全局 6）＋ `Release / Production Result`（v2.2.0 在生产，等级 5）。
- 授权内动作与明确不授权动作：见 §2 与「授权与外部动作」表。
- 停止条件：配额 < 10%；Epic Full 第 5 次仍红；G-1 探针坐实「平台不允许向从未对话用户主动私聊」且无替代（停下报产品负责人，本批退化为「预开通＋首聊补一句」并另议）；生产升级起不来（停在原地报产品负责人，不自行回滚）；任一 stage / prod 动作造成非预期重复送达。**Trace amendment 条件**：任一 D 项答复改变范围 / 权限 / 数据 / 授权终点；产品负责人要求纳入 #576 或改变裁定④。
- 产品负责人批准时间、批准版本与证据：2026-09-05 09:1x 北京口头批准范围与终点（原话见页首）；v1 = 本 PR 合并。
- `execution_adapter`：`claude_tmux_named_session`（session `TZ-server`，窗口 `ux-b1`）。
- 规划者任务 / 会话与停止点：本会话（Fable 5.1 xhigh，tz 窗口 3）；停止点＝编排者接管登记完成，随即转任元守护。
- 编排者、实施者池、唯一独立审核者的模型 / 推理档位：见 §5。
- 当前顶层编排任务 / 会话、主机、工作目录与版本快照：`TZ-server:ux-b1`，tz，`~/project/lingxi`（main `462eadf`），分支 `batch/606-ux`。
- 编排者接手入口、第一恢复点与批后继任条件：接手卡 `~/project/lingxi-archive/ux-b1-kickoff.md` → `/takeover` → 读三件套＋#606 最新评论 → 只读核实基线 → #606 接管登记 → 合并权转移。**第一恢复点＝任务表勾选＋#606 最新收口 / 交接评论。**继任条件：ctx ≥ 85% 或换班，先写交接评论再拉 `ux-b2`；最终批可不建继任。

## 现场合同来源与共享事实包

- 来源核对（2026-09-05 08:0x–09:2x 北京）：AGENTS.md；协作约定；产品合同「管理员预开通」「本地权限覆盖」段；当前能力「尚待实现」与「已知边界」；验证与门禁「已知边界与上线后项」；#147 v16；#541 收口评论与名单 v1 / v2；#544 收口评论第五节；#592 收口；记忆 rc25-blockz-state / prod-access-posture / model-routing / external-review-posture。冲突处理：09-01 路由「编排 opus max」与 #592 先例「Fable xhigh」冲突 → 取先例并在 §5 登记为偏离。
- 已生效决定与边界：#541 裁定①–⑥（名单形态职位＋公司范围、静默、首聊补一句、逐人失败关闭、已 active 不应用）；⑤联系方式不放链接；rc25 D-9 L1 不动；A-9 抑制无入口；D-3 名单外限流只登记；外审两轮 agy＋codex；K-9 有条件预批形态；「Lingxi」不得出现在用户可见面。
- 当前基线、模块、真实入口和关键依赖：见基线行；关键模块 `core/permission/position_override.py`、`scripts/ops/preprovision.py`、`adapters/feishu_user_message.py`、`adapters/feishu_admin_card.py`、`core/admin/management_card.py`、`config/content.py`、`apps/gateway/management_cards.py`、`adapters/postgres_management_card_context.py`、`core/permission/notification.py`。
- 验收断言与权威链接：`V-开通-28/29`（预开通入口与静默）、`V-权限-19/20`（名单形态与写入极性）、`V-内容-08`（首聊文案）、`V-告警-09`（预开通失败告警）；本批新增 V-行由 S-7 认领。
- 仍未知但不阻塞当前 Ready 工作：G-1（平台是否允许向从未对话用户主动私聊）；`/opt/lingxi/runtime-config` 在 prod 的属主；stage 是否有「从未与 Bot-Test 对话」的受控测试身份；#586 是否需要新表。

## 开工能力与环境快照

| 机器 / 环境 | 实际可用 Agent / 模型 / 并发与权限 | 本 Trace 最小冒烟及证据 | 不可做 / 未知 | Owner |
| --- | --- | --- | --- | --- |
| tz（`~/project/lingxi`，main） | Claude Code auto 模式：Fable 5.1 xhigh / Opus 5 high 可用；`agy`、`codex` 外部 CLI 可用；`git-claude-tz push`；gh 机器身份 | W0-1：`git fetch` ＋ `gh auth status` ＋ `agy --print` 一次真实调用 ＋ `codex exec` 一次真实调用 | 不得改 `.claude/settings*`；Opus 529 时降 Fable 留痕 | 编排者 |
| `biai-stage`（Bot-Test） | `ssh biai-stage` 全权限；三容器停着；stage 库；受控测试身份 | W0-1：`docker ps` 为 0；W0-2 探针一条真实发送 | Supabase 出口预算：用后即停；#600 实验优先窗口 | 编排者（#600 期间：元守护） |
| `biplus-prod`（Bot-Prod） | 唯一规则 `ssh biplus-prod 'sudo -u bi-ai-deploy *'`；只读随时；写动作按 §2 | W0-1：`docker ps` 三服务 healthy 回读 | root 级动作不可做（需要则列规则请 PM 亲加）；未知：runtime-config 属主 | 编排者（块 Z） |
| GitHub | Epic Full ≤ 5；Main Publish；`batch/**` 无 ruleset 可直推 | 推送 #1 Draft PR 建立 | `epic/**` 前缀勿用（ruleset 逼 PR） | 编排者 |

## 批准前可达性审计

### 输入 Issue 准入与关闭资格

| 输入 Issue | 自身完成标准 | 当前事实与证据 | 负责补齐的 Step | Ready 条件 | 收尾去向 |
| --- | --- | --- | --- | --- | --- |
| #586 主动发送＋预检 | 卡面 8 条（组卡规则 / stage 真发双通道 / 幂等 / 失败可见 / 同源 / 记录 / 生产九人 / 文档） | 文案已批；缺用户卡片适配器、组卡、记录、ops 入口；G-1 未知 | W0-2, W0-3, S-2, S-3, S-9, S-11 | 已 Ready（D-1/2/4 默认） | 关闭＋产品合同回写 |
| #587 多公司范围 | 卡面 5 条 | 展开函数只接单公司 / 全部（`position_override.py:76-77`）；名单 5 人公司编号已核 | S-1, S-9, S-11 | 已 Ready | 关闭 |
| #602 管理卡误报 | 卡面「建议验收」4 条＋「补充验收」4 项 | 静态定位三处（`card_callback_management.py:380`、`management_cards.py:319`、`postgres_management_card_context.py:426/64`）；未真库复现 | W0-4, S-5, S-9 | 已 Ready（G-2 复现红后修） | 关闭 |
| #585 文案外置 | 卡面 5 条 | gateway 无 runtime-config 挂载（已知边界）；content 三道校验存在；提示词外置先例 | S-4, S-9 | 已 Ready（D-3 默认） | 关闭＋部署配置清单 / 文案说明回写 |
| #605 未翻译标签 | 卡面 4 条 | 登记于验证与门禁；改动点待定位 | W0-5, S-6 | 已 Ready | 关闭＋删已知边界登记 |

### 结果可达性

| 用户 / 系统结果 | 真实入口 / 触发 | 运行时承接与调用方 | 核心处理与状态 | 外部副作用 / 投递确认 | 成功 / 失败终态 | 当前缺件 | 负责 Step |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 九人收到欢迎卡并可问数 | 产品负责人在 prod 跑 `preprovision.py --apply`（多公司）→ 就绪 → `outreach.py --apply` | ops 脚本（第二进程，令牌 5 分钟间隔）→ 开通链（既有）→ 组卡 → 用户卡片适配器 → Bot-Prod | `active` 状态驱动；发送记录 pending→delivered/failed；去重 uuid | 飞书 `im/v1/messages` 卡片送达（API 回读 message_id）＋ 审计行 | delivered / failed（告警、可重试） | 多公司展开（S-1）、适配器 / 组卡 / 记录 / 脚本（S-2, S-3）、G-1 | S-1, S-2, S-3, S-11 |
| 管理员看到与本次操作对应的状态 | 同一管理卡连做两次操作 | gateway 观察器＋后台结算 | 结果查询按当前 pending action 关联 | 卡片更新 | 已生效 / 处理中 / 未完成 | 关联条件修正（S-5） | W0-4, S-5 |
| 改一句文案不重发版 | 编辑宿主机覆盖文件 | 三服务加载期叠加 | 校验＋摘要＋回退 | 用户看到新文案；审计 content_digest | 生效 / 整份忽略并告警 | gateway 挂载、覆盖加载、校验 CLI（S-4） | S-4 |

### Epic 交付、双账与窗口预测

| Epic | 类型 | 生命周期 | 计划终点 | 尚需实现 / 装配 | 实现齐备后验什么 | 依赖 | 窗口预测与超时预警 | 完成与关闭条件 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E1 预开通完整体验（#586 #587 #605） | Implementation / Integration → Acceptance | Ready | User-path Delivered | 适配器、组卡、记录、ops 入口、多公司解析、标签翻译 | T-1, T-2, T-6, T-7 | G-1, W0-3 | 实施 1–1.5 天；审核 / 修复 0.5 天；超 2 天预警 | 三卡关闭＋产品合同回写 |
| E2 管理卡真话（#602） | Implementation | Ready | Component Delivered（stage 卡片路径验证） | 关联条件修正＋真库回归 | T-3 | G-2 | 0.5 天 | 关闭 |
| E3 文案外置（#585） | Implementation | Ready | Runtime Assembled（能力交付，生产默认不启用） | 挂载、覆盖加载、校验 CLI、文档 | T-4 | 无 | 0.5–1 天 | 关闭＋文档 |
| E4 发布与生产（S-8…S-11） | Release / Ops ＋ Acceptance | Pre-ready（E1–E3 冻结后） | Release / Production Result ＋ 九人全局 6 | 定版 v2.2.0、批终链、runbook 逐步 | T-5, T-6, T-7 | E1–E3、stage 租约、PM 块 Z | 批终链 0.5 天；生产窗口 1 小时；名单 1 天 | 收口评论＋#606 关闭 |

## Wave 0 研究与 Decision Gate

| Gate ID | 待确认问题 | 无副作用研究 / 探针 | Owner | 所需证据 | 依赖它的 Step | 成立后 | 不成立 / 未知后 | 超时动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G-1 | 应用能否向**从未与它对话过**的用户主动发私聊 | Bot-Test 向一个从未对话的受控测试身份发一条纯文本（既有适配器）；回读 message_id | 编排者 / 实施 | L4a（stage） | S-2, S-3, S-11 | 照设计 | 平台拒绝：记录错误码，停下报 PM（停止条件）；无此身份：登记未知，S-11 以赵晋霞首证、失败即停 | 2 小时内未得结论 → 登记未知继续实施 |
| G-2 | #602 反例能否在真库复现 | 真库用例（复用同卡两次操作、B 无发布记录、观察器超时） | 实施 | L3 | S-5 | 修复并让该用例绿 | 不能复现：缩小为「关联条件加固＋回归」并登记 | 1 小时 |
| G-3 | 样式（D-1） | W0-3 ≤ 3 变体真发 PM（Bot-Test） | 编排者 | PM 单字 | S-2 定稿 | 冻结样式 | 24 小时未回按甲 | 同左 |

## 计划执行步骤

| Step ID | Wave | Epic / Issue | 动作与可观察产物 | 启动条件 | Owner / 主路线 / 备选 | 资源租约 | 成功证据 | 成功后下一步 | 失败 / 未知 / 等待去向 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A-1 | A | — | 产品负责人前置输入（范围 / 终点 / #576 不纳 / #605 立；D-1…D-9 默认） | 已给齐 2026-09-05 | PM | — | #606 正文＋决策清单 | W0-1 | — |
| W0-1 | W0 | — | 接管登记＋只读盘点：main SHA、stage 停着、prod 三服务与内存读数、`/opt/lingxi/runtime-config` 属主与 gateway 挂载现状、受控测试身份清单、外部 CLI 各一次真实调用、#600 实验排期与 R-1 协调 | 接手卡 | 编排者 | R-2 | #606 接管评论（含盘点表） | 并行派 W0-2…W0-5、S-1、S-2、S-4 | 基线不符 → 停下报元守护 |
| W0-2 | W0 | #586 | G-1 探针 | W0-1 | 实施 Opus high（备 Fable） | R-1⚡, R-3 | #606 评论：错误码 / message_id | S-2 照设计 | 见 G-1 |
| W0-3 | W0 | #586 | 样式样卡：用既有卡片能力按已批文案渲染 ≤ 3 变体，Bot-Test 真发 PM，附一句「回 甲/乙/丙」 | W0-1 | 编排者 | R-1⚡, R-3（≤3 条） | #606 评论＋PM 单字 | S-2 定稿样式 | 24h 未回按甲 |
| W0-4 | W0 | #602 | 真库复现红用例 | W0-1 | 实施 Opus high | R-2（worktree） | 用例文件＋红输出贴 #606 | S-5 | 见 G-2 |
| W0-5 | W0 | #605 | 定位改动点（#544 任务表附表＋ grep） | W0-1 | 实施 | — | #605 评论 | S-6 | 定位不到 → 登记，S-6 缩为静态对账 |
| S-1 | W1 | #587 | 多公司解析（顿号分隔）、dry-run 累加、单测＋变异、管理卡路径回归与说明 | W0-1 | 实施 Opus high（worktree） | R-2 | commit＋报告（已改 / 未改＋原因） | 集成 | 与管理卡路径冲突 → 改在名单层 |
| S-2 | W1 | #586 | 组卡纯函数（三条取值规则、content.toml 键）、用户卡片适配器、发送记录与重试幂等、审计；样式按 G-3 定稿 | W0-1（G-1 不阻塞开工） | 实施 Opus high（worktree） | R-2 | commit＋单测 | S-3 | 需新表 → 迁移随批次，PR 说明 |
| S-3 | W1 | #586 | `scripts/ops/outreach.py`：dry-run 渲染 / `--precheck --to admin` 真发 / `--apply` 名单；状态驱动只发 active；`--list` 回查 | S-2 | 同 S-2 实施者 | R-2 | commit＋单测 | 集成 | — |
| S-4 | W1 | #585 | gateway 挂载补齐、覆盖文件加载（摘要 / 校验 / 回退 / 告警）、校验 CLI、部署契约断言、文档 | W0-1 | 实施 Opus high（worktree） | R-2 | commit＋单测 | 集成 | D-3 改判 → 退回编排者 |
| S-5 | W1 | #602 | 修复关联（观察器＋后台结算同规则）、真库回归、变异红 | W0-4 | 实施 Opus high | R-2 | commit＋红绿输出 | 集成 | — |
| S-6 | W1 | #605 | 标签翻译小修＋静态对账＋删已知边界登记 | W0-5 | 实施 | R-2 | commit | 集成 | — |
| S-7 | W1 | 文档 | 产品合同（主动告知条款、多公司范围、与静默 / 补一句关系）、当前能力、验收矩阵 V-行、部署配置清单、文案说明、CHANGELOG＋pyproject 2.2.0 | S-1…S-6 定形 | 实施（文档位） | R-2 | commit | 集成 | 措辞属产品品味 → 草案留痕不落地 |
| S-8 | W2 | 集成＋批终链 | 集成各 worktree → `check.sh full` → 推送 #1 → Draft PR → Epic Full → Fable 审①＋agy＋codex → 缺陷账本 → 修复包 → 复核② → 推送 #2 → 绿 → K-9 条件 → `gh pr merge --merge` → Main Publish | S-1…S-7 | 编排者；审核 Fable xhigh 子代理 | R-2, GitHub | PR、run 链接、审核 / 外审评论、合并执行记录 | S-9 | P1 未闭 → 第二轮修复包（计入预算）；Epic Full 第 5 次红 → 停 |
| S-9 | W2 | stage | 升级到发布镜像 → T-1…T-6（含预检真发 PM 1 条、#585 改一句、#602 卡片路径、#587 1 人 2 公司演练、#605）→ 演练数据清零 → 停 stage → 就绪自检落 #606 | S-8；#600 实验已结束或让路 | 编排者（验收者子代理可） | R-1⚡, R-3, R-6 | #606 L4a 评论（双通道） | S-10 | 任一 T 红 → 修复走热修流程（小改随第二次 Epic Full）；两轮仍红 → 停下报 PM |
| S-10 | W3 | ☢ 块 Z-1 | 就绪自检 → 步 0 工作副本 → digest 四变量 → `migrate` → `--profile mvp up -d` → healthy → 15 分钟观察 → tag v2.2.0；PM 一次真问数（加菜） | S-9；D-5 条件成立；白天静默窗口 | 编排者 | R-4 | ops 评论：每条命令与回读 | S-11 | 起不来 → 停在原地报 PM（回滚需单字） |
| S-11 | W3 | ☢ 块 Z-2 | 九人 dry-run ◇ → 赵晋霞 apply → 就绪 → 生产预检卡发 PM 本人 ◇ → 发赵晋霞 → 她首聊问数（双通道）◇ → 八人 apply（串行，约 1–2 h）→ 发卡 → 送达回读 8/8 → 24 小时观察 | S-10 | 编排者 | R-4, R-5, R-7 | #606 评论：逐人结局表（不写邮箱） | S-12 | 任一人失败：逐人报告不阻塞他人；发送失败：告警＋重试一次；仍失败报 PM |
| S-12 | 收口 | — | docs 回写（若有剩余，一次纯文档 PR）、环境盘点、任务表勾选、收口评论、#606 关闭、看板、记忆 | S-11 | 编排者 | — | 收口评论 | Closed / Complete | 未完项逐条转去向 |

## 共享资源租约与环境清理

| Lease ID / 资源 | 作用域与并发上限 | 唯一 Owner / 开工前存量 | 创建与释放点 | 停止 / 精确清理 | 恢复路径 | 收工证据 |
| --- | --- | --- | --- | --- | --- | --- |
| R-1 stage（三容器＋Bot-Test＋stage 库） | 同时只一个使用者 | 编排者；**#600 对照实验（元守护）优先窗口＝PM 读完 Supabase 用量页后至 S-9 前，冲突时 #600 让路** | W0-2 / W0-3 起；S-9 后停 | `docker compose … down`；演练数据按撤销脚本清零 | 重启即恢复 | `docker ps` 为 0 ＋ 白名单复原 |
| R-2 tz 主工作树与 `batch/606-ux` | 单一写入者 | 编排者；实施者各自 `~/project/lingxi-wt/<step>` | W0-1 建分支；收口删 worktree | `git worktree remove` | — | `git worktree list` 只剩主树 |
| R-3 Bot-Test → PM 私聊 | 累计 ≤ 8 条 | 编排者 | W0-3 / S-9 | — | — | 计数落 #606 |
| R-4 prod（bi-ai-deploy 规则） | 块 Z 独占 | 编排者 | S-10 起 | 不留临时文件（若建 `rc26-*` 目录，收口清） | runbook §六 | 盘点评论 |
| R-5 正式表 Bitable＋MCP 15 分钟拉表 | 串行 | 编排者（块 Z-2） | S-11 | 撤销脚本（逐表断言、不符整体回滚） | 同左 | 逐人回读 |
| R-6 Supabase 出口预算 | stage 累计 ≤ 6 h | 编排者 | — | 用后即停 | — | PM 用量页读数 |
| R-7 Bot-Prod → 真实用户 | 逐批单字 | 编排者 | S-11 | 不可撤回 | — | 送达回读表 |

## 正式验收 / 发布对象合同

| Epic / 路线 | 对象类型与构建链 | 绑定方式 | 冻结时回填 | 替换后的失效与重验 | 回滚对象 |
| --- | --- | --- | --- | --- | --- |
| E1–E3（stage L4a） | 合并后由 Main Publish 产出的四镜像 `YYYYMMDD-<sha12>` | tag＋digest | 合并 SHA / 镜像 tag / 四 digest | 任何新提交 → 新镜像 → T-1…T-6 重跑 | stage 回 c6fa5ac 镜像 |
| E4 生产 | 同一镜像组，digest 四变量钉住 | `.env.prod` 四 digest | 同上 | 同上 | 上一批 digest（v2.0.2）＋ `migrate downgrade` 一级（需单字） |

## 授权与外部动作

| 动作 / 环境 | 是否在授权内 | 执行者与前置证据 | 副作用与幂等边界 | 恢复 / 回滚路径 | 完成回读 |
| --- | --- | --- | --- | --- | --- |
| 分支 / Draft PR / ready / K-9 合 main | 是（常设＋K-9 条件） | 编排者；条件逐项落 #606 | 可逆 | revert PR | 合并执行记录 |
| stage 升级、L4a、演练、停 | 是（常设） | 编排者 | 演练数据清零 | 重启 | 回读评论 |
| Bot-Test → PM 私聊 ≤ 8 条 | 是 | 编排者 | 消息不可撤回 | — | message_id |
| 生产升级序列（S-10） | **是，D-5 整段预批，条件成立才跑** | 编排者；就绪自检 | 迁移可 downgrade；up 可回滚 digest | runbook §六（写动作需单字） | 每条命令回读 |
| 名单 apply（赵晋霞 / 八人） | 逐批单字 | 编排者；dry-run 逐字一致 | 幂等；失败逐人关闭 | 撤销脚本 | 逐人结局表 |
| Bot-Prod → 真实用户发卡 | 逐批单字 | 编排者；生产预检卡 PM 已过目 | 不可撤回；去重 uuid | — | 送达回读 |
| 权限规则修改 / 删除数据 / 撤回消息 | 否 | — | — | — | — |

## 验收、审核与修复合同

- Story Fast 不适用（`batch/**` 无 ruleset）；Epic Full 只在推送 #1 / #2 消费；Main Publish 一次；金字塔频率：commit 免费、推送计数、Epic Full ≤ 5。
- 每个 Epic 的验收断言、证据层级和环境：见 [验收.md](验收.md)（T-1…T-9）。
- 每条用户可见路径的首触冒烟：W0-2（主动私聊）、W0-3（卡片渲染）在正式候选前完成，发现进缺陷账本。
- 双通道观测：消息 / 卡片＝平台 API 回读＋审计行；正式表＝Bitable 回读＋`publish_outbox`；管理卡＝卡片 JSON 回读＋状态表。
- 受控触发夹具：#602 复现（受控卡片两次操作＋观察器超时注入）；#586 失败注入（错误收件人形状 / 平台拒绝桩）；#585 坏文件样本；全部默认关闭、用后撤除并回读。
- 固定候选的唯一独立审核者：Fable 5.1 xhigh 独立上下文子代理（不参与实现、不直接修复），对象＝推送 #1 的 SHA；静态排除对账：把「已知边界」清单（预开通失败多告警、通配旧行、抑制无入口、gateway 挂载）逐条对到 T-断言。
- 单一审核结论＋唯一缺陷账本：编排者裁定 P1 / P2 / 接受；统一修复包；同一审核者定向复核；候选 SHA 变化旧结论失效。外审两轮结论并入同一账本，「无法从证据判定」由编排者回源分辨后再派。
- 审核 / 修复预测：审核① 1 小时、外审并行 1 小时、修复包 2 小时、复核② 0.5 小时；超出一倍预警；循环退出＝P1 为零且 P2 有去向。
- 每批收口、继任、接管登记与租约转移：§6.8 序列；本批预期单批完成，ctx ≥ 85% 即写交接拉 `ux-b2`。

## Ready 与交接自检

- [x] 第五节交接门逐项满足（输入 Issue 事实 / Ready / 去向齐；G-1…G-3 进 Wave 0；至少 W0-1 可立即执行；Epic 终点明确；规划 / 编排分会话；租约齐；发布对象与授权明确）
- [x] 正文已读回，合同版本 v1，产品负责人批准证据＝页首原话＋本 PR 合并
- [x] 规划者已停止实施；编排窗口 `ux-b1` 与接手卡已明确；批后继任条件已写

## 现场事实与已知坑（编排者必读）

- prod 命令只认 `ssh biplus-prod 'sudo -u bi-ai-deploy …'` 原形：不加 `-n` / `-o ConnectTimeout`、不套 `for` / `cd` / 管道前缀；容器内 SQL 字符串走 argv、内层不得出现单引号。`compose cp` 在只读 rootfs 被拒，只能 stdin 投放；digest 回读用钉住形态。
- 预开通三条真实接缝（rc25）：内测闸按 open_id 且预开通逐字继承（九人 open_id 已在两份 env 白名单）；ops 脚本是第二进程，领专用令牌最小间隔 5 分钟（常驻刚续期时逐人 `AccessTokenUnavailable`，等 5 分钟原样重跑）；MCP 每 15 分钟拉表，新人探针 401 直到被拉上（预算 900 秒，超时交常驻迟到就绪恢复静默激活）。
- 赵晋霞银河已含后台管理员（全指标×全公司）⇒ 名单权限零新增，发布应判「不变、零写入」；生产上她应首探即 ready。批 2 三位新人与五位 CEO 真正要等拉表。
- stage：`.env.stage*` 带 5 s / 10 s 轮询键且保留；stage 库口令按 D-31 不轮换；stage 用完必须停（Supabase 出口预算，09-06 限流风险仍在）；上一批「stage 真实名单演练须早于生产窗口一天」。
- 变异实测全程 `python -B`；并行代理不得共用 scratchpad 同名文件；「full 绿」必须见 `Ran N tests`；ruff `--isolated` 时加 `--target-version py312`；`src/` 禁一切抑制注释、函数 > 60 行门禁红（#592 门禁现状）。
- 花名册：重复邮箱＝离职再入职同一人，不可改、有在职标记，别再提去重。
- 主工作树 `~/project/lingxi` 现检出 `main`；`.claude/` 已在 `.git/info/exclude`。
