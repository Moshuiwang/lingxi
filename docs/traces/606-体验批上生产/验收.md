# 验收（体验批 Trace #606；关卡＝各 Issue「可观察完成标准（自证闭环版）」；证据落 GitHub）

> v1（2026-09-05）。证据等级目标：代码批 L2（Epic Full）＋ L3（Fable 独立审核＋agy / codex 外审）；stage 关键路径 **全局 6（受控测试身份，非生产）**；生产升级 5；九人 **全局 6（真实用户）**。宣称交付前，未达层级逐条列明。双通道：平台 API 回读＋服务端审计 / 日志一致才定论。

## 总关卡

| # | 关卡 | 证据形态 | 目标等级 | 现值 |
| --- | --- | --- | --- | --- |
| T-1 | #587：stage「1 人 2 公司」dry-run 条数累加 → apply → 正式表行含两公司、`publish_outbox` 一笔、本地授权同一组；重跑零变化；整组撤销一次撤全部；单测含未知公司整份拒绝、「全部」不变、管理卡路径回归；变异红 | #606 评论（回读表）＋PR 用例 | 6 | — |
| T-2 | #586：受控名单 `--precheck` 真发 PM Bot-Test 私聊，`message_id` 回读＋审计行；同名单 `--apply` 重跑零新增；同去重 uuid 重试后收件人消息数仍 1；失败注入 → `failed`＋管理群告警＋重试成功 → `delivered`；预检与正式同一渲染函数（静态断言）；`--list` 可回查发给谁 / 内容键＋版本 / 何时 / 结果；正文不进日志 | #606 评论＋PR 用例 | 6 | — |
| T-3 | #602：真库反例（A 已发布、B 无发布记录、观察器超时）修复前红、修复后绿，恢复旧关联条件再红；迟到的 A 回调不覆盖 B；stage 同一张卡两次操作，第二次观察器超时后卡片显示「处理中 / 未完成」而非「已生效」（卡片 JSON 回读＋状态表） | PR 红绿输出＋#606 评论 | 6 | — |
| T-4 | #585：单测四类坏文件整份拒绝回退、缺文件零变化；校验 CLI 退出码；stage 改 `onboarding.completed` 一句 → 受控触发 → 用户侧回读新文案＋审计 `content_digest` 变化；改坏 → 旧文案＋一次告警；三服务都读到（gateway 挂载）；部署契约：变量未配零变化 | #606 评论＋PR | 6（stage） | — |
| T-5 | #605：该分支正文不含职能标签原文（单测＋变异红）；静态对账无直拼调用；stage 受控触发或如实登记「仅单测＋静态」 | PR＋#606 评论 | 3–6 | — |
| T-6 | 首触冒烟对账：G-1（主动私聊）与 G-3（样式）结论已落 #606，缺陷进账本；已知边界清单逐条对到 T-断言 | #606 评论 | — | — |
| T-7 | 生产升级：四 digest 渲染与固定值一致；迁移头前进（若有）；三服务 healthy 15 分钟 `RestartCount` 0；PM 真问数（加菜，非关卡）；tag v2.2.0 指向合并提交 | ops 评论 | 5 | **达成，且 09-07 又连做两次**：v2.2.0（09-05）、v2.3.0 与 v2.3.1（09-07）三次升级各自 digest 逐字对、迁移头前进（0088 → 0090，v2.3.1 无迁移）、15 分钟观察三容器 healthy `RestartCount=0` 错误数 0，三个 tag 均指向各自合并提交。附带在生产实测到 `V-部署-05`「旧镜像跑在新库结构上照常 healthy」成立 |
| T-8 | 九人：赵晋霞 apply 到 `active`、欢迎卡送达（`message_id`＋审计）、首聊问数成功（`worker.task.terminal`＋平台回读）；八人 apply 逐人结局（不写邮箱）、发卡 8/8 送达；24 小时观察零开通失败、零重复送达 | #606 评论（逐人表） | 6 | **达成（发卡口径经 Z-6 按平台现实改写）**。**开通面**：范围由九人扩到十三人（增补宋威、陆啸、薛晋；另有李博凯经常规首聊链自助开通），`app_user` **14 人全部 active**、预授权 **1434 行 / 13 组**全部有效、发布表 **14/14 published**、零开通失败。**发卡面**：欢迎卡 **送达 7 / 失败 6**，发送记录 **16 行 / 16 个去重键 ⇒ 零重复送达**。失败的 6 位全部是 `feishu_code_230013`——平台不允许向从未与机器人对话过的跨组织用户发起单聊，**不是配置或实现能修的**（证据见[飞书机器人主动私聊的跨组织前提](../../参考证据/飞书机器人主动私聊的跨组织前提.md)）。原九人中 **3 人已收卡**（赵晋霞、肖亮、杨俭），六位未开口者按 **Z-6 裁定不再等待**，其补卡转为日常运营动作、不挂本 Trace。**真实旅程**：赵晋霞首聊连问三次全部 `succeeded`；截至收口生产累计 **37 个问数任务、全部 `succeeded`、零失败**，提问人含赵晋霞、宋威（5）、杨俭（3）、肖亮、李博凯。**24 小时观察**：补卡守望 2026-09-07T10:08:28Z → 2026-09-08T10:09:28Z 整 24 小时，期间自动补卡 3 人（宋威、肖亮、杨俭），零开通失败、零重复送达，到期自停。**已知瑕疵**：守望首版用反向排除条件筛收件人，导致产品负责人本人误收一张正式欢迎卡（**只此一张**，已核实无第二例），当日改为写死白名单，教训写入 runbook §14.7 |
| T-9 | 文档：产品合同「主动告知」条款＋多公司范围＋与静默 / 补一句关系；当前能力回写；验收矩阵新增 V-行（认领 → 已验证）；部署配置清单（gateway 挂载、可选变量）；文案说明；验证与门禁删 #605 已知边界；CHANGELOG 2.2.0 | 合入 main 的 commit | 2 | — |

## 制品（S-8d 回填，2026-09-05）

| 项 | 值 |
| --- | --- |
| main 合并提交 | `92d016b`（PR #608 `--merge`，父 `c41db20` ← `5006c7c`） |
| Main Publish | run 33950814520 success |
| 镜像 tag | `20260905-92d016be943e` |
| scheduler digest | `sha256:92312bd247e6b91002cac9d6a6153433bf36a89237dd1137d2147d929e1634f2` |
| gateway digest | `sha256:028e403eb09a2f69e21da4dc93cf752e16a036ad66404c3f0adbf3e8f9717493` |
| worker digest | `sha256:756aeac09f1edd772679b87643b6ba20e7b86c1ab79d6de7ac33e9b9ab25bb6f` |
| migrate digest | `sha256:7e530a9c6f9b6ab6d3ba3636bf263507611715d0032ecaa46428a1c4b633661d` |
| env 形态 | `LINGXI_<SVC>_IMAGE_DIGEST=@sha256:…`（`@` 前缀是 compose 拼接所需） |
| 迁移头 | `0088_outreach_message` |

## 制品（S-11b / Z-3 回填，2026-09-07 两次发布）

| 项 | `v2.3.0` | `v2.3.1`（**生产现值**） |
| --- | --- | --- |
| main 合并提交 | `c28ef39`（PR #660 `--merge`） | `71fcc88`（PR #663 `--merge`） |
| Main Publish | run 34083695500 success | run 34088346450 success |
| 镜像 tag | `20260907-c28ef398423a` | `20260907-71fcc88e7059` |
| scheduler digest | `sha256:9c3a54c0be02562f3ba5d3b04977f58f091817cd6507855fe8e5114b919db7e5` | **未留痕**（见下） |
| gateway digest | `sha256:f2960f84873fa30cc26d2de8bb70403725d3892f0f538404db08b007d5035e14` | 同上 |
| worker digest | `sha256:7a57d78ce5e0a16a1ce097b4bf4c5c11e87dee8dbcfe93e888eaddb2e3532e0a` | 同上 |
| migrate digest | `sha256:126400bc10d7285c7419bad146f990d7477a6725a3788a87b8f34c266349f7a3` | 同上 |
| 迁移头 | `0090_delivery_retry_backoff`（`0089` / `0090` 两条新迁移） | `0090_delivery_retry_backoff`（本版无新迁移） |
| 回滚点 | `20260905-92d016be943e` ＋上一组四 digest | `20260907-c28ef398423a` ＋上表左列四 digest |

两次的四 digest 逐条取自生产主机 `~/606-z2/digests-<tag>.env`、未手敲；`.env.prod` 各改动 5 行、键集合不变，改前均留 `before-<tag>` 备份，回读与 `.env.prod` 逐字一致。

**一处缺口，如实登记**：`v2.3.1` 的四个 digest **值**在 #606 没有任何一条评论写下来（执行记录只写了「与 `.env.prod` 逐字一致」这个判定，没抄值）。现在它们只存在于生产主机的 `.env.prod` 与 `~/606-z2/digests-20260907-71fcc88e7059.env`——而后者在 S-12c 收口清理清单里。**清理前须先把这四个值取回来落进 #606 或本表**，否则生产现值将失去仓库侧的可回读记录，回滚点也只剩一份主机上的备份文件。

## 批终链（全绿才进 stage / 生产）

本机 `check.sh full` 绿（见 `Ran N tests`）→ 推送 #1 → Epic Full 绿 → Fable 审①（P1 为零或全部进修复包）→ agy＋codex 外审结论并入账本 → 修复包 → 复核②（P1 为零、P2 有去向或豁免留痕）→ 推送 #2 → Epic Full 绿 → K-9 四条件落 #606 → 合 main → Main Publish success → stage T-1…T-6 → 停 stage → 就绪自检（前提逐项＋日志回读＋预判断点）落 #606 → 块 Z。

## 就绪自检模板（块 Z-1 前，逐项写现值）

1. main 合并 SHA 与镜像 tag / 四 digest（钉住形态回读）
2. stage T-1…T-6 全绿链接；stage 已停
3. prod 现状：三服务 healthy、镜像 c6fa5ac、迁移头 `0087`、在途任务 0、内存读数
4. 白天静默窗口（无用户任务）；回滚点：上一批四 digest 记录在案
5. `.env.prod*` 本批需要改的键清单（新变量只登记不启用，D-3）
6. D-5 条件四项逐项「成立」

## 块 Z-2 名单关卡

- dry-run 逐人「邮箱 → 职位 → 公司范围 → 条数」与名单 v1 / v2 逐字一致（多公司条数累加），PM 过目单字。
- 赵晋霞：发布判「不变、零写入」（她已全指标×全公司）；探针 ready；生产预检卡先发 PM 本人（同一渲染、同一数据）。
- 八人：串行 apply，逐人结局表；令牌 5 分钟间隔规则；探针 401 等拉表属正常；超时者由迟到就绪恢复静默激活后再发卡（状态驱动）。
- 任何一人失败只终止他自己；发送失败告警＋重试一次；仍失败报 PM，不重复送达。
