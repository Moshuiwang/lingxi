# 任务表（体验批 Trace #606；状态即文件当前值，编排者随执行 commit 更新；第一恢复点）

> v1（2026-09-05 09:3x 北京 / 01:3xZ）。执行路由：`claude_tmux_named_session`，session `TZ-server`：**`ux-b1`**（Fable 5.1 xhigh，auto）；元守护＝规划者会话（窗口 `guardian`）。证据只认 GitHub / 仓库留痕；每个 Step 完成时勾选并附指针。裁定终值见 [决策清单.md](决策清单.md)，关卡见 [验收.md](验收.md)。
> **PR / CI 纪律**：只开一个 Draft PR（`batch/606-ux → main`）；推送只在两个点（#1 冻结候选、#2 修复包＋定版），Epic Full ≤ 5；三件套与文档回写随分支 commit，Trace 期间不开独立文档 PR；进度写 #606 评论（不触发 CI）。

## 块 A（产品负责人前置输入）

- [x] A-1…A-6 已给齐（2026-09-05 08:5x–09:1x 北京，规划者会话；终值见决策清单）

## Wave 0（只读盘点＋首触冒烟）

- [x] W0-1 接管登记＋只读盘点（main `462eadf`、stage 停着、prod 三服务与内存、runtime-config 属主与 gateway 挂载、受控测试身份、agy/codex 各一次真实调用、#600 与 R-1 协调）→ #606 接管评论（已落：[评论 5548835303](https://github.com/Moshuiwang/lingxi/issues/606#issuecomment-5548835303)，2026-09-05 10:5x 北京；G-1 无受控身份→登记未知，W0-2 不派发）
- [x] W0-2 G-1 主动私聊探针（Bot-Test → 从未对话的受控身份；结论：允许 / 拒绝＋错误码 / 无此身份记未知）—— **结论：无此身份，登记未知（W0-1 盘点：stage 5 个受控身份均已与 Bot-Test 对话过）；S-11 以赵晋霞首证、失败即停。W0-3 顺带证明向已对话用户主动发卡成功**
- [x] W0-3 样式样卡 ≤ 3 变体真发 PM（R-3 计数），附「回 甲/乙/丙」；24h 未回按甲 —— 已发 3 张（2026-09-05 10:53 北京，R-3 = 3/8）；**产品负责人 11:17 北京裁定「乙」**（#606 评论 03:17:43Z，决策清单 Z-0），S-2 按乙定稿
- [x] W0-4 #602 真库复现红用例（G-2）—— **复现成立**：真库 13 条中 6 条红（`'effective' != 'incomplete'` 等），用例合入 `c6bf81b`（worktree 原提交 `ca2caec`）
- [x] W0-5 #605 改动点定位 —— 坐实唯一渲染点 `core/identity/onboarding_steps.py:292`（已 active 提前收口分支未过翻译层），见 #606「S-6 合入」评论

## Wave 1（实施；各自 worktree，改完先 commit 再汇报）

- [x] S-1 #587 多公司解析（顿号）＋ dry-run 累加＋单测＋变异＋管理卡路径回归与说明 —— 合入 `dda97fb`（worktree 原提交 `bad1423`；full 5760 绿；变异红 2 轮；名单层拆分、共用展开函数未动；管理卡路径两道回归用例）
- [x] S-2 #586 组卡纯函数（三条规则、content.toml 键）＋用户卡片适配器＋发送记录 / 重试幂等＋审计（样式按 D-1 定稿）—— 合入 `b4f88c1`（worktree 原提交 `5f2d8f3`；样式默认乙；新表 `outreach_message` 迁移 0088；content 版本 2026-09-05 ＋16 键；变异 7 处红；编排者亲跑 109 单测＋13 真库 OK）
- [x] S-3 #586 `scripts/ops/outreach.py`（dry-run / `--precheck --to admin` / `--apply` / `--list`；只发 active）—— 合入 `5e9cc49`（worktree 原提交 `6d4a52f`；full 5855 绿）；`--initiated-by` 管理员闸已合入 `0ba30f6`（worktree 原提交 `1a5535d`；fast 5866 绿、变异红 3；发起人落审计不落表）
- [x] S-4 #585 gateway 挂载补齐＋覆盖文件加载（摘要 / 校验 / 回退 / 告警）＋校验 CLI＋部署契约断言＋文档 —— 合入 `3a9a2bb`（worktree 原提交 `af76942`；fast 5790 / l1 / docs 绿；变异红 4 轮；**生效方式＝加载期叠加、重启生效**（编排者裁定接受，沿映射表外置先例，热重载登记为后续候选）；告警只由 scheduler 发；`worker.task.terminal` 加 `content_digest`；改了 `scripts/ci/check_installed_package.py` 登记新模块）
- [x] S-5 #602 修复（观察器与后台结算同一关联规则）＋真库回归＋变异红 —— 合入 `40fdc82`（worktree 原提交 `00ef574`；full 5764 绿；变异 3 点各红 2/3/2 条；编排者一次性真库亲跑 18 条 OK；无迁移）
- [x] S-6 #605 标签翻译小修＋静态对账＋删验证与门禁已知边界登记 —— 合入 `7dd048f`（fast 5751 绿、变异红 4/5；登记位实际在 `当前能力.md:120` 已改写，`验证与门禁.md` 本无此条；stage 未触发，仅单测＋静态，证据等级 3）
- [x] S-7 文档面：产品合同（主动告知条款、多公司范围、与静默 / 补一句关系）、当前能力、验收矩阵 V-行、部署配置清单、文案说明、CHANGELOG＋pyproject **2.2.0** —— 合入 `722793f` / `f7606db` / `d9c7ab0` / `c3f6a93`（worktree 原提交 `bf73c9c`…`73b37a5`）：产品合同 :72/:80-87、当前能力 :22/:138-144、九条 V-行（V-开通-30…35 / V-权限-21 / V-管理-42 / V-内容-10）＋V-权限-19 补充、数据库设计 §九、代码框架、runbook §十三、架构设计 ⑬撤销/⑭/⑮、职位与公司范围授权 :85/:103、pyproject 2.2.0 ＋ CHANGELOG；docs 门禁通过；部署配置清单与文案说明由 S-4 已写。v2.2.0 git tag 在 S-10 生产观察通过后打（S-10 步骤末项）

## Wave 2（集成、批终链、stage）

- [x] S-8a 集成各 worktree → `check.sh full` 绿（见 `Ran N tests`）→ 推送 #1 → Draft PR → Epic Full #1 —— 推送 #1 `9692ace`、Draft PR #608；**Epic Full #1 红**（归属核对门禁：任务表一句未登记的归属短语，见 #606 评论 5549430398；编排者误读本机 full 退出码，已登记）；措辞修正 `f1ce0a5` 随推送 #2 走；主树 full 对代码面 `Ran 5947 tests` 绿（推送前一轮）
- [x] S-8b Fable 独立审核①（多视角）＋ agy 外审（产品承诺 / 文案真话 / 失败形态）＋ codex 外审（绕过面 / 越权 / 重复送达）→ 唯一缺陷账本落 #606 —— 审核① P1 0 / P2 3 / P3 7（十点变异全红）；codex 9 P1 / 6 P2；agy 3 P1 / 3 P2；编排者坐实后 P1 0、修复包 17 条（14 代码 / 3 文档）、登记 11、不成立 1（#606 评论 5549518940）
- [ ] S-8c 统一修复包 → 同一审核者定向复核② → 推送 #2（含定版）→ Epic Full #2 绿
- [ ] S-8d K-9 条件逐项落 #606（CI 绿 / ready / 就绪自检 / main 未动且无他 PR）→ `gh pr merge --merge` → Main Publish → 镜像 tag＋四 digest 记录
- [ ] S-9a stage 升级到发布镜像 → 回读三容器 healthy
- [ ] S-9b T-1 #587 演练 ／ T-2 #586 预检真发 PM（R-3 计数）＋幂等＋失败注入 ／ T-3 #602 卡片路径 ／ T-4 #585 改一句 ／ T-5 #605 ／ T-6 首触冒烟对账
- [ ] S-9c 演练数据清零 → 停 stage → `docker ps` 为 0 → 就绪自检落 #606

## Wave 3（块 Z，生产）

- [ ] S-10 ☢ 生产升级 v2.2.0（D-5 整段预批：就绪自检 → 步 0 → digest → migrate → up -d → healthy → 15 分钟观察 → tag）→ PM 一次真问数（加菜）
- [ ] S-11a ☢ 九人名单 dry-run（含多公司）→ 贴 #606 → PM「批」
- [ ] S-11b ☢ 赵晋霞 apply → 就绪（探针 ready）→ 生产预检卡发 PM 本人 → PM「批」→ 发赵晋霞 → 送达回读
- [ ] S-11c 赵晋霞首聊问数（双通道）→ PM「批」放八人
- [ ] S-11d ☢ 八人 apply（串行）→ 逐人结局表 → 发卡 → 送达回读 8/8 → 24 小时观察
- [ ] S-11e Bot-Prod 发送记录回查表落 #606（不写邮箱）

## 收口

- [ ] S-12a 输入 Issue 逐卡对账关闭（#586 #587 #602 #585 #605）
- [ ] S-12b 剩余文档回写（如有）一次纯文档 PR
- [ ] S-12c 环境盘点：tz worktree / 远端 `batch/606-ux` / stage 容器与演练数据 / prod 临时文件 / R-3 计数 / stage 累计运行时长
- [ ] S-12d 收口评论（UTC＋北京、Epic Full 次数 / PR 数 / commit 数 / 人次 / 审核轮次）→ #606 关闭 → 看板 #547 → 记忆
