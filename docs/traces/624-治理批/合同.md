# 开工合同：治理批——合同门禁收尾核验、文档减负首批与第二批、#147 v17 反向审计（Trace [#624](https://github.com/Moshuiwang/lingxi/issues/624)）

> 方法版本：#147 **v16**。合同版本：**v2**（2026-09-06 修订，见页末「修订记录」）。批准依据：产品负责人 2026-09-06 07:2x 北京在 tz 本机会话裁定 **B 方案**（原话要点：「立即开工，目标 09-08 09:00 北京前四步全部收口并退场；届时未完成的步骤写交接评论交给批次 2，在 #606 收口后继续」）；07:4x 北京补裁模型配比（原话要点：「拉起的会话一律 Opus xhigh；Fable 5.1 额度不足要省着用，只留给关键 review」）。**本 PR 合并即批准 v1——且本 PR 就是 #620 门禁生效后的第一个合同 PR，必须由产品负责人本人 Approve 后才能合并。**
> 合同只认两种来源：产品负责人裁定（原话＋时间）与输入 Issue 的可观察完成标准。上一任执行者自设的约束不继承；证据只认 GitHub / 仓库留痕。

## 1. 目标

- **用户结果一句话**：把三件挂着的工程治理收口——「合同 PR 只能由产品负责人本人合并」第一次被真实验证，开工必读文档把历史沿革搬出正文只留现行规则，方法正文净减到 v17。**产品与用户侧零可见变化**。
- **里程碑（何谓「到了」）**：① 本三件套 PR 被门禁拦下、由产品负责人本人批准后合并，`mergedBy` 读回为产品负责人 → #620 关闭；② #598 首批六份文件的历史外移合入 main，开工必读集体量下降且门禁全绿；③ #598 第二批合入，`当前能力.md` 每项能力一屏内可见现值与证据链接、审核明示「现值零变化」，两份文件纳入体量预算脚本；④ #621 六条候选逐条带数字给出建议、产品负责人裁定留痕，获授权后 #147 正文修订至 v17 且不超过 v16 的 33,343 字节。
- **完成定义（Delivered）**：#620 关闭并在 Moshuiwang/trace-kit#13 留痕；#621 关闭；#598 的首批与第二批两条完成项打勾并留状态评论（**#598 整张是否关闭见裁定项 G-3**）；四个 PR 全部合入 main；收口评论落 #624；tz 环境零残留；编排者在 09-08 09:00 北京前退场。
- **既定条款**：交付终点按 §3.2 逐项声明——PR 合并、CI 通过都不自动升级实际终点。本批**没有部署面**，最高实际终点是 `Docs / Method Delivered`（文档与方法正文已在 main / #147），不宣称任何用户结果。

## 2. 授权终点

- **编排者可自动执行到**：建分支与 worktree；开 **S-2 / S-3 / S-4 三个纯文档 PR**（`docs/…` → main，一步一个）；`gh pr ready`；**自行合并这三个 PR**（它们不含 `合同.md`，不触发 CODEOWNERS）；关闭 #620、#621；在 #598、#620、#621、#624 与 **Moshuiwang/trace-kit#13** 写评论留痕；文档回写；tz 临时 worktree / 分支清理。
- **显式除外（每次单独请示）**：
  1. **本三件套 PR 的合并**——由产品负责人本人 Approve 并合并（技术门禁强制，机器人 `gh pr merge` 会被拒），编排者不得尝试绕过、不得 force、不得改 CODEOWNERS 或规则集。
  2. **任何合同修订 PR**（改 `docs/traces/624-治理批/合同.md`）同样须产品负责人本人批准。
  3. **#147 正文修订至 v17**——反向审计裁定后**单独请示**，产品负责人单字「授」才动正文（裁定项 G-4）。
  4. 生产与 `biai-stage` 的任何动作；权限规则 / 仓库设置修改；删除任何用户数据或未提交改动；改 `AGENTS.md` 按需读取表（**v2 豁免其中一行，范围见 §3**）；#598 中不属首批 / 第二批的任何实施（两个未合入的 `codex/*` 分支、135 KB 实验归档清理）。
- **执行通路**：全部动作在 tz 本机 `~/project/lingxi`，用 `gh`（机器人身份 `Moshui Claude Code tz[bot]`）与 `git commit` / `git-claude-tz push`。产品负责人的 Approve 在 GitHub 网页端本人操作，机器人对仓库设置是 403。
- **常设授权引用**：满足既定条件的 `gh pr ready` / `gh pr merge` 已登记为常设授权（不含被 CODEOWNERS 拦下的合同 PR）。
- **既定条款**：决策攒清单一次提交；单字裁决（批 / 守 / 缓 / 授 / 不发）有效并留痕；临时授权带失效时点，到点失效不得滚动沿用（§6.7）。

## 3. 不做清单

- 不碰产品代码（`src/`、`tests/`、`migrations/`、`deploy/`）；不部署；不改 CI 门禁脚本与工作流。
- 不改 `AGENTS.md` 的**按需读取表**，**唯一例外（v2，产品负责人 2026-09-06 10:4x 北京裁定「那就现在走吧」）**：在「要改变或说明用户体验、业务规则、权限、安全、身份或外部平台边界」这一行的既有内容之后追加一个指针，指向验收矩阵第二章覆盖清单，并写明只读命中项。目标形态：


  ```markdown
  [产品合同与外部边界](docs/产品合同与外部边界.md)；同步核对[验收矩阵覆盖清单](docs/技术设计/验收矩阵.md#二合同条款覆盖清单)中该章节的断言，只读命中项
  ```

  （上面是 `AGENTS.md` 里那一格的**完整目标内容**，路径按 `AGENTS.md` 自身位置书写。）

  **豁免边界**：只动这一行、只追加不删改、不新增表行、不改其他任何行的触发条件与指向；`AGENTS.md` 其余部分与 #598 首批已完成的改动一律不再动。**理由**：改产品规则的任务按现表只被要求读产品合同，而「改规则须同步更新断言」这句话住在验收矩阵内部，不打开矩阵就读不到，形成闭环缺口；补的是导航，不新增机制、不新增门禁、不新增维护面。
- 不动**证据等级两套并存**（#334 对照表维持，Moshuiwang/trace-kit#14 裁定项 1）。
- 不做 Moshuiwang/trace-kit#14 第四节明确不做的任何东西：配置 DSL（execution.yaml / trace.yaml）、Graph Linter、事件日志真源、指标平台、代理手工维护的状态 JSON。
- 不抬开工必读集体量上限（40 KB）；不改产品合同的章节结构；不改任何能力的**现值**、证据层级或产品承诺。
- **不与 #606 块 Z-2 并行**：`ux-b2` 开工前本 Trace 编排者必须已退场；任一时刻只有一个活动编排者与一个共享写入者。
- 不新增常驻进程、容器、stage / 生产实例。
- **既定条款**：新发现问题默认只登记不立项——主线灯塔：不做它，里程碑能不能达成？（§6.2）
- **既定条款**：疑问句只答不动手；产品品味类修订夜间只出草案不落地。

## 4. 验收标准

逐条可观察（细则与关卡见 [验收.md](验收.md)）：

1. **S-1 / #620**：本三件套 PR 的 `gh api …/pulls/<n>` 读回 `merged_by.login` ＝ 产品负责人本人；证据落 #620 评论；#620 四条完成标准逐条对账后关闭；Moshuiwang/trace-kit#13 留一条「lingxi 侧首个被拦合同 PR 已验证」的评论——**只作旁证，不关闭 #13**（#13 最后一项要的是 trace-kit 仓**自己**的首个合同 PR，产品负责人 2026-09-06 08:0x 北京明确）。
2. **S-2 / #598 首批**：六份文件按 #598 正文首批动作表改完并合入 main；`scripts/ci/verify_docs.sh` 与 Epic Full 绿；开工必读集体量预算读数下降且 ≤ 40 KB；`scripts/dev/README.md` 的「检查后 Git 状态必须为空」错误表述已纠正；迁移两端同批生效、旧锚点保留导航；**没有整节搬走仍在生效的门禁例外**（审核逐条核对）。
3. **S-3 / #598 第二批**：`当前能力.md` 每项能力可在一屏内找到现值与证据链接，按验收矩阵分册的产品域加小标题；带日期的沿革搬到 `docs/参考证据/`（决策性的进 `docs/决策记录/`）并双向可点；**独立审核明示「现值零变化」**（逐条比对搬移前后文本，与 #479「门禁语义零变化」同一口径）；`产品合同与外部边界.md` 只加不增长棘轮、结构不改；两份文件纳入 `scripts/ci/check_docs_size_budget.py` 各自独立上限；`check_acceptance_matrix.py` 与 `check_contract_attribution.py` 全绿。
4. **S-4 / #621**：反向审计评论对六条候选逐项写明「进 v17 / 不进 / 等下次」建议、理由与**自采数字**；产品负责人逐条裁定留痕；进入 v17 的条款按 §九以 #147 评论登记为候选并注明对应删除候选；获单独授权后正文修订至 v17，**字节数 ≤ 33,343**，版本记录写明出处。
- **证据等级要求**：关键路径至少 **L4（CI 通过）**；`mergedBy`、体量读数、v17 字节数三项要求 **L7（GitHub API / 文件回源读回）**。本批**没有 L5 / L6**（无部署、无真实用户旅程），收口时如实列明。
- **既定条款**：用户可见断言双通道观测——本批无用户可见断言，该条不适用；文档断言以「仓库文件回读＋CI 结论」两条独立通道核对。
- **既定条款**：证据只认仓库 / GitHub 留痕；只存临时目录、会话或交接词的状态视为不存在（§6.7）。
- **既定条款**：产品负责人真人验证块只放两处——本 Trace 的两处是「批准本 PR（最前）」与「S-4 六条候选裁定＋v17 授权（最后）」；中途不请产品负责人试错，每块先出示就绪自检、一次只给一条指令。

## 5. 成本预算

- **完整门禁次数上限：≤ 5 次**，仅批次终点消费（§6.3 金字塔频率纪律）。**已知消费点**：S-2 含 `scripts/dev/README.md`，分类器 `classify_story_changes.py` 把 `scripts/dev/` 归入 fast 前缀（非 `docs`），因此该 PR 会真跑 `Epic Full / gate`（实测 7–10 分钟）＋ image，占 1 次；S-1 / S-3 / S-4 三个 PR 全在 `docs/` 前缀，走纯文档路由不计次。余量 4 次给门禁红复修与裁定项 G-1 的拆分方案。**PR 数：4 个**（三件套 1 ＋ S-2 / S-3 / S-4 各 1），加合同修订 PR ≤ 1。
- **时间窗：2026-09-06 → 2026-09-08 09:00 北京**（含跨夜）。到点即停，未完成步骤写交接评论交批次 2，编排者退场，**不滚动**（裁定项 G-5）。
- 产品负责人批的是本段上限；配比按 2026-09-06 07:4x 北京裁定**写死**，非建议值；切换规则按 §3.3 不变。
- **模型配比（产品负责人 2026-09-06 裁定，写死）**：编排者 **Opus 5 / xhigh**（`gov-b1` 以 `--model opus --effort xhigh` 拉起）；实施 / 复修 **Opus 5 / high**；唯一独立审核者 **Opus 5 / xhigh 独立上下文子代理**，**仅两处可用 Fable 5.1 / xhigh**——S-3 的「现值零变化」逐条核对、S-4 的 v17 正文修订核对；规划者 / 元守护＝产品负责人当前会话。**Fable 5.1 额度已用 86%，2026-09-10 07:00 北京重置**，除上述两处外不得消费。
- **外部审查配额**：**0 轮**（本批纯文档、无产品承诺面与绕过面，agy / codex 不派；裁定项 G-2）。外审不作为第二名独立审核者。
- **子代理人次上限**：同时活跃**实施 ≤ 1、审核 ≤ 1**；累计派发实施 ≤ 6 人次、审核 ≤ 5 人次（裁定项 G-6）。每次派发编排者自挂兜底观察（预计时长＋超时查外部证据）；**子代理不得再派子代理**；实施子代理各用独立 worktree、改完先 commit 再汇报、进等待必须上报。
- **既定条款**：大 workflow（> 50 万 token）开跑前查配额余量；对抗验证分批派。

## 6. 主线声明

- **本 Trace 的主线是**：#620 / #621 关闭、#598 首批与第二批合入 main、#147 到 v17。任何派发 / 立项 / 修复前先问「不做它主线是否受阻」；**新发现默认只登记不立项**（例：#620 评论里的分类器未登记 `.github/CODEOWNERS`、`publish.yml` 路径过滤过宽两条 P2，本批只登记，不修——它们改的是 CI 自身，须另立工作项）。
- `execution_adapter`：`claude_tmux_named_session`——常驻 session **`TZ-server`** ＋ 本批编排窗口 **`gov-b1`**（继任 `gov-b2`…，拉起姿势见 `guardian` skill）；**元守护 ＝ 规划者会话转任（`TZ-server` 窗口 2 `gov-plan`，Fable 5.1 xhigh）**——产品负责人 2026-09-06 08:0x 北京裁定：窗口 1 的会话退出，只留本窗口，**后续无人替元守护兜底**。元守护只以外部证据判活（窗口存活＋ #624 留痕新鲜度，30–60 分钟一查），**不实施、不持合并权**，承接并转发产品负责人裁定。**元守护三项职责（产品负责人同次裁定，写死）**：
  1. 本三件套 PR 由产品负责人本人 Approve 并合并后，拉起编排窗口——`tmux new-window -d -t TZ-server -n gov-b1 -c ~/project/lingxi`，命令 `claude --model opus --effort xhigh --permission-mode auto` ＋ 接手 prompt（首步 `/trace-kit:takeover` 读三件套与 #624）；本机判窗口只能用 `$TMUX_PANE`，`send-keys` 后须补 `C-m`。
  2. 合并后核对该 PR 的 `mergedBy` 为产品负责人，作为 #620 完成标准第 3 条证据写进 #620 并关闭，同时在 Moshuiwang/trace-kit#13 留痕（**旁证，不关 #13**）。
  3. 周一 2026-09-08 产品负责人对 #606 块 Z-2 说「批」时，**先核对 `gov-b1` 已退场**，再按 #606 最新交接评论与 2026-09-05T23:23Z 指令评论拉起 `ux-b2`（同样 Opus 5 xhigh）。
- **单一写入者**：当任编排者持 tz 主工作树 `~/project/lingxi` 与合并权；实施子代理各用独立 worktree；面向 main 的 PR **串行**合并。**跨 Trace 互斥**：#606 的 `ux-b2` 与本 Trace 的 `gov-b1` 不得同时存在，先退场者释放主工作树与合并权。
- **既定条款**：四个 Step **顺序执行**，每步一个 PR、开完即合，不攒批（本批改动彼此无依赖但触碰相邻文件，串行避免冲突）；批次终点一次完整门禁 ＋ 一次独立审核 ＋ 唯一合并人合并（§6.3）。
- **批次收口固定序列**（§6.8）：收口评论 → 交接评论 → 继任 window → 接管登记 → 租约转移 → 退场。**继任条件**：ctx ≥ 85%、换班，或时间窗到点。
- **收尾必含**：环境清理与残留盘点（worktree / 远端分支 / 临时目录谁建谁清，**本批不产生容器与外部实例**）、任务表勾选回写、批次收口评论（UTC ＋ 北京双标注 ＋ 预算消耗 ＋ 完整门禁次数 / commit 数 / 子代理人次 / 审核轮次）。

---

# 执行图（`──►` 硬依赖；`◇` 产品负责人门；`▣` 被 CODEOWNERS 拦下的合同 PR）

```
▣ 三件套 PR ◇(PM 本人 Approve + 合并) ──► W0 接管登记 + 只读盘点
                    │
                    └──► S-1 #620 收尾核验（读回 mergedBy → 对账关 #620 → trace-kit#13 留痕）
                              │
                              ▼
                         S-2 #598 首批（6 文件历史外移）→ 审核 → PR#2 → Epic Full(gate) → 合 main
                              │
                              ▼
                         S-3 #598 第二批（当前能力现值/沿革分离 + 两份纳入体量预算）→ 审核(现值零变化) → PR#3 → 合 main
                              │
                              ▼
                         S-4 #621 反向审计（自采 #592/#606 交接数字）→ 六条建议 ◇(PM 逐条裁定)
                              │                                              │
                              │                                    ◇(PM 单字「授」)
                              ▼                                              ▼
                         登记 v17 候选评论（#147 §九）───────────► 修订 #147 正文至 v17（≤33343B）→ PR#4 文档回写
                              │
                              ▼
                         收口：对账关 Issue → 环境盘点 → 收口评论 → #624
```

---

# 合同区（`METHOD.md` / #147 §八字段）

- 方法版本 **v16**；合同版本 **v1**；执行阶段 **Ready**（本 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- **基线提交与核对时间**：main = `631aeb0`（2026-09-05T23:07Z，PR #622 合入 CODEOWNERS 与协作约定一句）；`.github/CODEOWNERS` ＝ `docs/traces/**/合同.md @Moshuiwang`；规则集 `20559295` 已 `require_code_owner_review=true`、`dismiss_stale_reviews_on_push=true`、`required_approving_review_count=0`、`bypass_actors=0`、`enforcement=active`（#620 评论 2026-09-05T23:15Z 只读回读）；trace-kit 侧规则集 `22359032` 同样已勾代码所有者审查、`bypass_actors=0`（Moshuiwang/trace-kit#13 评论 2026-09-05T23:25Z）。体量读数：`当前能力.md` 100,210 B、`产品合同与外部边界.md` 74,461 B、开工必读集 39,906 B / 40,960 B（97%）。tz 主工作树 `~/project/lingxi` 在 `main`，工作区干净；无临时 worktree、无容器；`biai-stage` 已停、生产 v2.2.0 不在本批范围。
- **规划者任务 / 会话与停止点**：本会话（`TZ-server` 窗口 2 `gov-plan`，Fable 5.1 xhigh）；**停止点＝三件套 PR 开出并报告 Issue / PR 号**，随后等待产品负责人批准，**不转编排者**；按 §6.8 **本会话随即转任元守护**（产品负责人 2026-09-06 08:0x 北京裁定），合同合并后由它拉起 `gov-b1`。
- **角色模型 / 推理档位**：同 §5（产品负责人裁定写死）。
- **当前顶层编排任务 / 会话、工作目录与版本快照**：**未知 + Owner：元守护 + 补齐时点：本 PR 合并后拉起 `gov-b1` 时由编排者在 #624 接管评论登记**（预期 `TZ-server:gov-b1`，tz，`~/project/lingxi`，main `631aeb0` 之后的合并点）。
- **编排者接手入口与第一恢复点**：`/trace-kit:takeover` → 读本三件套 ＋ #624 最新交接 / 裁定评论 → 只读核实现场（main SHA、CODEOWNERS、规则集、体量读数、无并行编排者）→ #624 接管登记 → 合并权转移。**第一恢复点 ＝ [任务表.md](任务表.md) 勾选 ＋ #624 最新评论。**
- **批后继任条件**：ctx ≥ 85%、换班或时间窗到点时，先写交接评论再拉 `gov-b2`；**最终批可不创建继任**——09-08 09:00 北京到点若仍有未完成步骤，写交接评论交批次 2（在 #606 收口后继续），编排者退场，不拉继任。
- **停止条件**：Epic Full 第 5 次仍红；配额 < 10%；产品负责人未授权 v17 正文修订（则 S-4 止于候选登记评论，#621 留开）；发现 #606 的 `ux-b2` 已启动（立即停下报产品负责人，两个编排者不并行）；**元守护会话失联**（窗口 1 已退出，元守护之上无人兜底——编排者每次收口评论落 #624，产品负责人可据留痕新鲜度自行判活）；任一改动被审核判定「搬走了仍在生效的规则 / 改动了现值」（回退该切片，不硬推）。
- **Trace amendment 条件**：任一裁定项答复改变范围、授权终点或验收标准；产品负责人要求把 #598 剩余项或 #620 评论里两条 P2 纳入本批。
- **§八各表**：按产品负责人 2026-09-06 指示**不单列**。命中触发条件的两项已就地写入正文，不另起表：① 输入 Issue ≥ 2（#620 / #598 / #621）的准入与关闭资格 → §1 完成定义 ＋ §4 逐条验收 ＋ 裁定项 G-3；② 跨会话继任 → 本合同区「批后继任条件」行。其余各表：**不适用——未命中触发条件**（无共享独占资源、无生产 / 外部发布、无可能推翻合同的未知、无多 Epic）。

---

## 修订记录

- **v2（2026-09-06）**：产品负责人 2026-09-06 10:3x–10:4x 北京会话内裁定（原话要点：「顺手带上吧，不用太复杂的实施」「那就现在走吧」）。改动三处：§2 显式除外第 4 项加豁免指向；§3 不做清单第 2 条写明唯一例外与豁免边界；`验收.md` 的 S-3 行由「`AGENTS.md` 未改」改为「仅追加覆盖清单指针一处」。**范围、授权终点、时间窗、模型配比、成本上限与裁定项 G-1…G-7 均不变**；本次只放开一行导航指针。触发依据＝合同区「Trace amendment 条件」之「改变验收标准」。
- **v1（2026-09-06）**：PR [#625](https://github.com/Moshuiwang/lingxi/pull/625) 由产品负责人本人 Approve 并合并（`mergedBy=Moshuiwang`，`863205b`）。

## 批准时需一并裁定

1. **G-1 `scripts/dev/README.md` 是否拆出单独 PR**？它属 CI 分类器的 fast 前缀，与 `docs/` 同 PR 会让 S-2 真跑一次 `Epic Full / gate`＋image（约 11–15 分钟，占 1/5 次）。拆出则 S-2 走纯文档路由不计次，但多 1 个 PR。可回：不拆 / 拆。**默认：不拆**（预算 5 次充裕，少一次上下文切换）。
2. **G-2 本批是否派外部审查（agy / codex）**？纯文档、无产品承诺面与绕过面。可回：0 / 各 1 轮。**默认：0 轮**。
3. **G-3 #598 是否随本批关闭**？其完成状态里还有两项不属首批 / 第二批：两个未合入的 `codex/*` 分支处理、约 135 KB 实验归档清理（Codex Desktop 持有）。可回：关 / 留开。**默认：留开**——本批只把首批与第二批两条完成项打勾并留状态评论，#598 由其余项收口后关闭。
4. **G-4 #147 v17 正文修订的授权形态**？可回：随本合同一并预批 / 裁定后单独请示。**默认：单独请示**——反向审计裁定留痕后，编排者出示「删除候选 ＋ 净减字节预算」再请你单字「授」；未授则 S-4 止于 #147 候选登记评论，#621 留开。
5. **G-5 超窗处置**？可回：到点即停 / 允许顺延到 #606 块 Z-2 之前。**默认：到点即停**——09-08 09:00 北京写交接评论交批次 2，编排者退场。
6. **G-6 子代理人次上限**？可回：按默认 / 你给数字。**默认：实施 ≤ 6 人次、审核 ≤ 5 人次，同时活跃各 ≤ 1**。
7. **G-7 S-3 瘦身后的体量上限怎么定**？可回：按实测留小余量 / 你指定数字。**默认：按实测留小余量**——`当前能力.md` 目标 30 KB 上下（#598 第二批估计值，以实测为准），`产品合同与外部边界.md` 以当前 74,461 B 封顶；两条上限与瘦身在同一次改动里落地并写明理由。
