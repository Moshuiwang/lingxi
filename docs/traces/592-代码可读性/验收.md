# 验收（代码可读性规范与门禁批；逐 Step 关卡＝#591 可观察完成标准的拆解；证据落 GitHub）

> v1 草案（2026-09-04 北京）。证据等级目标：门禁与每批清理 L2（本机 `check.sh full` ＋ Draft PR Epic Full）＋ L3（每批 agy 冷读一轮；codex 两轮：G-2 门禁脚本、R-1 集成后全分支，见 [决策清单.md](决策清单.md) K-4；门禁脚本与收官全分支 Fable 审）；stage 真实旅程 L4a（受控测试身份非生产，全局 6 须连写「受控测试身份」）；本 Trace 不含生产升级。每项完成把「现值」改成实际等级并附指针。
> **行为不变是每一步的前提**：任何一批若既有测试需要改断言才能过，该批打回，不进集成。

## 总关卡（#591 可观察完成标准）

| # | 标准 | 证据 | 现值 |
| --- | --- | --- | --- |
| T-1 | 人类同事抽读批 1 六个文件（拆分后），不问人能说出各模块职责与主函数在做什么 | #592 评论（PM 转述或同事评论） | — |
| T-2 | `function_size_ratchet_baseline.txt` 与 `comment_ratchet_baseline.txt` 为空；`size_ratchet_baseline.txt` 在阈值 1000 下条目为 0 或每条附拆分理由 | 分支文件 ＋ Epic Full 绿 | `9bdb526`：三份基线均为空文件（函数 0 / 注释 0 / 文件阈值 1000 下 0）；Epic Full #3（`d761fdb`）全绿 ✅ |
| T-3 | `ruff check`、`ruff format --check`、`D` 规则全仓零告警；per-file-ignores 与 format exclude 为空；`test_ruff_config.py` 与两条棘轮测试变异验红 | Epic Full 绿 ＋ 变异实测记录 | `ac32cb8`：全仓 `ruff check` / `format --check` 零告警；`D` 在 `src/lingxi/` 全部生效（三条目录 glob 按 D-21）；`src/lingxi/` per-file-ignores 只剩 PLR0913 ×15（合同 §3 登记不做）与 `notification.py` N818（有意保留）；format exclude 空；`test_ruff_config.py` 与两条棘轮测试变异验红记录见 G-1b / G-1d / G-2 |
| T-4 | 本机 `check.sh full` 与 Draft PR Epic Full 同时绿；既有测试不改断言全绿 | 收口评论 | — |
| T-5 | stage（Bot-Test ＋ 生产备份恢复的独立库）一轮真实旅程与上线前行为一致 | V-1c 双通道证据 | — |
| T-6 | `readability_report.py` 收官读数：注释＋docstring 占比、>60 行函数数、来历标记数三项相对起点（W0-5）均下降 | 起点与收官两份读数落 #592 | 起点（`c6fa5ac`）→ 收官（`ac32cb8`）：注释＋docstring **34.6% → 23.8%**、> 60 行函数 **185 → 0**、来历标记 **2,409 → 0** 行（docstring > 10 行 524 → 135、`#` 块 > 5 行 351 → 0、总行 72,933 → 68,350、文件 191 → 221）三项均下降 ✅ |
| T-7 | Draft PR 转 Ready 并合入 main（生产部署另立 `[ops]`） | PR 合并记录 | — |

## 逐 Step 关卡

- **W0-1…W0-5**：只读；每项一条 #592 评论。W0-5 起点读数必须用与收官同一脚本口径（脚本先于读数落分支；#591 的临时测量只作参考）。W0-4 不在生产执行（决策清单 D-20）。现值：W0-1/2/3/5 已登记（[W0 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5539586786)）；W0-4 待 K-2；W0-5 正式读数已落（G-1 完成评论）
- **G-1a A 组**：`ruff check`（requests 同款规则）全仓零告警；手修 13 处逐条在 commit 正文列出；`onboarding_runner.py` 退到 1497 且其基线条目归零（`apps/worker/service.py` 的 1533 行登记是 #593 热修所留，归零移到 B-1 关卡），`test_size_ratchet_check.py` 同步；三个贴线文件行数未涨（涨了则改 import 写法，不登记基线）。现值：L2（本机 full 绿；`ruff check` 全仓零告警；手修 13 处在 `adecb04` 正文；三个贴线文件未涨）；偏离：`onboarding_runner.py` 回到 1502、基线归零移到 B-1（D-23）
- **G-1b B 组**：`python3 scripts/dev/gate_spec.py fast` 与 `gate` 输出含 `RUFF_VERSION=0.16.4`；去掉工作流 pin 时 `test_dev_check_gate_spec.py` 判红；`verify_repository.sh` 缺 `ruff` 时明确失败而非跳过；`src/lingxi/` 内放一行 `# noqa` 判红；`test_ruff_config.py` 对 per-file-ignores 新增条目判红；`V-部署-23` 行 < 800B 且 `check_acceptance_matrix.py` 绿。现值：L2（`RUFF_VERSION=0.16.4`；去 pin 判红、缺 ruff 明确失败、八种抑制形态含 `# noqa: CODE` 判红、清单变长判红均实测；`V-部署-23` 276B）
- **G-1c C 组**：`ruff format --check` 除 exclude 六文件外零差异；`.git-blame-ignore-revs` 含该 commit；文件棘轮基线与实测精确相等；`git blame` 任一被重排文件不显示格式化 commit（本机验证一次）。现值：L2（`ruff format --check --force-exclude` 零差异；`.git-blame-ignore-revs` = `f171b95`；文件棘轮基线精确相等；施工者本机 blame 验证一次）
- **G-1d D 组**：函数棘轮 `--bootstrap` 条数以实测为准并写进 commit 正文（`c6fa5ac` 参考约 185 条 / 87 文件），键都以 `src/lingxi/` 开头含 `::`；文件存在时 `--bootstrap` 拒绝；`--refresh` 拒绝增长；把一个 59 行函数改成 61 行判红（变异）；注释棘轮三类计数对固定样本正确（docstring 11 行判超、`#` 块 6 行判超、`Issue #12` / `2026-09-04` / `审查` 命中）；两份基线改动走 fast（`test_ci_layering.py`）；`代码规范.md` ≤ 4KB、`check_docs_size_budget.py` 合计 ≤ 40KB；`AGENTS.md` 读取表含该文件；`代码框架.md:135` 过期句已删。现值：L2（`--bootstrap` 189 条键全为 `src/lingxi/…::…`；文件存在时拒绝；`--refresh` 拒绝增长；59→61 判红；三类固定样本正确；两份基线走 fast；`代码规范.md` 3348B、预算 39194B ≤ 40960B；`AGENTS.md` 含该文件；`代码框架.md` 过期句已删）
- **G-1e**：本机 `check.sh full` 绿 → Draft PR 开出 → Epic Full #1 绿（真库、全部 extras、四镜像）。现值：推送 #1 `ae004e7`、[PR #596](https://github.com/Moshuiwang/lingxi/pull/596) Draft、[Epic Full #1](https://github.com/Moshuiwang/lingxi/actions/runs/33884484757)（结论待回填）
- **G-2 Fable 审门禁脚本**（codex 第 1 轮同期审同一对象，结论同落 #592）：结论逐条「已改 / 未改＋原因」；至少覆盖：棘轮同提交改基线的已知边界如实登记、`--bootstrap` 只在文件不存在时可用、noqa 禁用 grep 无法被注释形态绕过（如 `#noqa` 无空格、`# NOQA` 大写）、`test_ruff_config.py` 钉住的清单与 `pyproject.toml` 实际一致。现值：审核完成（[G-2 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5542026520)）——四条必覆盖项：同提交改基线边界**未登记**（P2-1，修）；`--bootstrap` 只在文件不存在时可用 ✅；noqa grep 漏四种 ruff 真实形态（P1-1，修）；钉住清单与 pyproject 逐字一致 ✅ 但整表其他键未钉（P1-3，修）。修复 `242b135`（集成 `4d9c207`）：15 条全部「已改」（抑制注释 16 种形态判红、`git grep` 退出码语义、`--config pyproject.toml` ＋ 禁 `ruff.toml`、整表白名单钉住、同名函数取 max、ruff 版本核对、注释行不取 pin、陈旧基线键判红、`ls-files -z`、来历正则收窄 411→391 条、边界登记、`验证与门禁.md` 补 ruff、报表口径、措辞）；「未改＋原因」= D-24 只登记项

### 每批关卡（B-1…B-7 同一模板）

| 关卡 | 标准 |
| --- | --- |
| 行为不变 | 既有测试**不改断言**全绿（本机 `check.sh fast`，含独立真库容器时 `full`）；纯移动用 `git diff --stat` ＋ 新旧模块符号表对照说明 |
| 转发 | 被搬走的公开符号在原模块保留 `from .new import X` 转发；其他批的 import 不因本批变化而断 |
| 函数棘轮 | 本批文件在 `function_size_ratchet_baseline.txt` 中条目归零；新函数 ≤ 60 行 |
| 注释棘轮 | 本批文件三类计数归零：docstring ≤ 15 / 10 行、`#` 块 ≤ 5 行、来历标记 0 |
| docstring 结构 | 本批文件从 `D` per-file-ignores 删除后 `ruff check` 零告警（摘要行＋空行＋正文；`Args/Returns/Raises` 按签名自明程度） |
| 文件体量 | 本批每个文件 ≤ 1000 行 |
| 合同归属 | `check_contract_attribution.py` 绿（登记行改动同步重登记） |
| 「为什么」不丢 | 每条被删的来历 / 论证，要么以更短形式留下，要么已在测试用例名、测试 docstring 或 `docs/决策记录/` 有对应——由集成者合入时逐条抽查（抽样 ≥ 10 条，列在批收口评论）；R-1 时 codex 第 2 轮与 Fable 再抽 |
| codex 外审 | K-4：不再每批；G-2 同期审门禁脚本、R-1 同期审集成后全分支各 1 次。角度固定：「行为有没有变」「被删的为什么有没有丢」「有没有绕过门禁」；结论落 #592 |
| agy 冷读探针 | `agy --model gemini-3.8-flash-high --print='…'`，不给任何上下文，只给本批 2 个文件全文，要求说出模块职责与主函数在做什么；答不出或答错即打回该文件；结论落 #592 |
| 集成 | 集成者合入后本机 `check.sh full` 绿；基线冲突以重跑 `--refresh` 解决，不手改数字 |

- **B-1 额外**：`_process_task`（538 行）与 `build_supervisor`（453 行）拆后每段职责一句话写在各自 docstring 摘要；`AutoOnboardingRunner.__init__` 33 参数改为参数对象后，所有调用点与测试构造同步且不改断言；六文件从 format exclude 删除后 `ruff format --check` 零差异；`apps/worker/service.py` 从 `size_ratchet_baseline.txt` 归零（#593 热修登记的 1533 行）。现值：L2（合入 `2ed3d30`，集成 full 抓到 2 处遗漏由 `7227a54` 修复后 5741 用例绿；`_process_task` 537→46 行、`build_supervisor` 拆散后最长 56 行；参数对象化后 `apps/scheduler/onboarding.py` 与 `tests/test_onboarding_runner.py` 构造同步、断言零改动（`git diff -- tests | grep assert` = 0）；六文件出 exclude 后 `ruff format --check` 零差异；`service.py` 1533→634、`onboarding_runner.py` 1502→798 且两条基线归零；agy 冷读 `service.py` / `onboarding_runner.py` 均说对；注释棘轮剩 `onboarding_steps.py` 6 处业务词「复核」待 G-2 正则收窄）
- **B-2…B-7**：按模板；B-6 另含 #593 P2-a 一行修法（决策清单 D-17），既有测试不改断言。现值：B-2 L2（合入 `abd3911`，集成分支 full 5741 用例绿；三条棘轮本批条目归零；D 条目删除、PLR0913 两条保留（合同 §3 登记不做）；agy 冷读两文件通过）；B-7 L2（D 复修 `0a9a3d1` 后隔离检查零告警，豁免删除）；B-3 L2（补做后合入 `6dc55e9`）；B-5 L2（合入 `2f0c3f5`）；B-6 L2（合入 `56b3259`，P2-a 含变异实测）；B-4 L2（合入 `4223212`）；七批合入后集成分支 full 5742 用例绿、函数棘轮 0 条、注释棘轮 4 条（归属登记行，收官清）
- **I-1…I-3**：每次推送 Epic Full 绿；推送前本机 `check.sh full` 绿；推送后 Draft PR 检查结论落 #592。现值：I-1 推送 #2 前本机 full 5741 用例绿（`7c6c034`）；Epic Full #2 = run 33904612534 **全绿**（`5445791`）；I-2 合入 B-5 / B-3 后本机 full 5741 绿；I-3 推送 #3 = `d761fdb`（本机 full 5746 绿），[Epic Full #3](https://github.com/Moshuiwang/lingxi/actions/runs/33922297743) 全绿
- **B-8 收官**：转发全部清除且 `ruff check` 零告警（无未用 import）；两条棘轮基线为空；`D` per-file-ignores 与 format exclude 为空；`THRESHOLD_LINES = 1000` 且文件棘轮基线为空或每条附理由；`代码规范.md` 定稿并与 `pyproject.toml` 实际规则一致；`readability_report.py` 收官读数落 #592（与 W0-5 同口径对照）；#593 P2-b 三处 `close_idle_connections()` 调用落地（D-17）。现值：L2（`ac32cb8`：转发与别名全清、`ruff check` 零告警无未用 import；三份基线全空；`D` per-file-ignores 与 format exclude 为空，剩 PLR0913 ×14 与 `notification.py` N818（有意保留）；`THRESHOLD_LINES = 1000` 且基线为空；`代码规范.md` 3659B 与 pyproject 一致；P2-b 三处 `close_idle_connections()` 含单测；收官读数见 T-6）
- **R-1 Fable 审收官全分支**（codex 第 2 轮同期审同一 diff）：以 `origin/main...refactor/readability` 全量 diff 为对象；结论逐条「已改 / 未改＋原因」；至少覆盖：随机抽 20 个被删注释核对「为什么」去向、随机抽 10 处纯移动核对符号与行为一致、门禁绕过口复查。现值：L3——通过。20 条被删注释逐条给出去向（19 条约束保留、1 条为现状描述属来历）；10 处纯移动逐字一致（1 处 payload 在提前返回路径不再保留，无可观察差异）；门禁绕过口复查：ruff 两条命令、抑制注释 grep 三态退出码、三条棘轮、整表白名单、`ruff.toml` 禁入、`--target-version` 推断 3.12、31 个新模块登记均 ✅；**P1-1 钉住快照过宽**已修（收紧到 34 条现状 ＋ format exclude 空集，加回 D 条目 / exclude 各判红）；P2-1 `_ChainAborted`、P2-2 gateway 七个私有转发别名已清（两个测试改从 `management_cards` 导入）；P3 五处已改
- **R-2 资源占用静态审核（K-5）**：对象与时点同 R-1（R-1 修复后、推送 #3 前，与 codex 第 2 轮同期）；对抗式——至少找出一处「连接可能跨等待持有」或「查询进了循环」的候选并证伪 / 证实；六面逐条表（面 / 证据 / 结论 / 修复去向），每条发现给 `file:line` ＋ 机制 ＋ 可量化影响。必查：`with connect()` 作用域未被拆坏、LISTEN 与 `dedicated=True` 未丢、空闲栈常量（每键 2 / 30 秒探活 / 300 秒寿命 / keepalive / `prepare_threshold=None`）未改、P2-a / P2-b 改对且有测试、健康检查建连数不变；`git diff c6fa5ac...HEAD -- 'src/lingxi/**/config.py' deploy/` 无数值与类型改动；无游标整读 list / 无边界缓存 / 新线程 / 新定时器；日志量与轮转不变；四镜像大小 ±5%。现值：L1 静态＋局部 L3（venv 内 MRO / 常量 / 导入成本实测、`check_installed_package.py --source-only` 退出 0）——**通过**，六面零 P1 / P2，对抗式六个候选全部证伪为基点同形；F-1 登记不修（注释已如实改写 `0c5af08`）；V-1d 预判：建连 7–9 / 分钟同量级、日志 info 集合逐条相同、RSS 漂移远小于 +10%
- **V-1a 备份恢复**：按 K-2 通路取得文件；恢复到 stage 独立库（库名含 `readability`）；`app_user`、`task`、`publish_outbox` 三表计数回读（stage 现库实测表名，无 `task_queue`）；stage 现有库未被覆盖（回读库列表）。现值：L4a 前置完成——独立库 `lingxi_readability`（stage 本机 `postgres:17` 容器，dump 为 pg_dump 17 / 服务端 17.6，`postgres:16` 不能读），只恢复 `public` schema（Supabase 的 auth / storage / realtime / vault 等 schema 与角色不恢复），零错误；`app_user` 1、`task` 3、`publish_outbox` 1、alembic 头 0084（V-1b 迁移应前进到 0087）；stage 现有库未被覆盖（`docker ps` 仅新增本容器，Supabase 库未动）。限制：备份为上线前唯一一份，数据量小
- **V-1b stage 升级**：四镜像 digest 与推送 #3 的 Epic Full 制品一致；两份 env 加轮询键 5s / 10s 与生产对齐；三服务 healthy 15 分钟 `RestartCount` 0；迁移头前进；V 段做完 stage 三服务停掉并回读。现值：四镜像 digest 与 Epic Full #3 制品一致（bundle `--import` 逐一核对）；两份 env 轮询键 5s / 10s；迁移头 0084→0087；三服务 healthy（15 分钟窗口与 RestartCount 见 V-1d 采样评论）；V 段结束停机待做
- **V-1c 真实旅程**：受控测试身份首聊 → 开通 → 问数 → 管理卡一次操作 → 一个定时职责手动触发；飞书侧结果 ＋ `worker.task.terminal` 等结构化日志双通道；与上线前（main 制品）同一脚本对照无差异；清场回读（用户环境、测试行删除）。现值：—
- **V-1d 资源对比（K-5）**：基线 = 当前生产（`20260904-c6fa5ac5f9ad` = 本分支基点，轮询 5 / 10 秒，与 V-1b 对齐后可比），由元守护只读采集并以评论落 #592（空闲建连 / 分钟、会话数、三容器 RSS / CPU、日志行 / 分钟、一次真实问数的外呼计数）；stage 在推送 #3 镜像上 10 分钟空转 ＋ 一次 V-1c 旅程，测 `DISCARD ALL` 60 秒增量、`pg_stat_activity` 峰值（上限 15）、容器 netns `ss -tin dport = :5432` 的 `bytes_received`、`docker stats` RSS / CPU、日志行 / 分钟、外呼计数；通过：建连 / 会话 / 外呼 ≤ 基线，RSS / CPU / 日志 ≤ 基线 × 1.10；出口字节生产侧取不到，以建连次数为代理指标；做完停 stage 并回读。现值：—
- **V-2 同事抽读**：2 个文件（1 个来自批 1、1 个随机），同事不问人说出模块职责与主函数在做什么；结果记 #592。现值：—
- **Z-1**：推送 #4 绿 → `gh pr ready` → 就绪自检（T-1…T-6 逐条指针）落 #592 → PM 单字「批」→ `gh pr merge --merge`（D-15）→ main 合入记录。现值：—
- **C-1…C-3**：`git worktree list` 只剩主工作树；`docker ps -a` 无 `lingxi-*` 临时容器；stage 备份库去向按 PM 一句话执行并回读；远端 `readability/*` 批分支已删；收口评论含全部读数。现值：—

## 关联验收矩阵

- `V-部署-23`（ruff 锁版进门禁）、`V-部署-24`（函数体量棘轮）、`V-部署-25`（注释卫生棘轮）：本 Trace 新增，落 `docs/技术设计/验收矩阵-部署与迁移.md`，状态「已认领」→ 收官后「已验证」。
