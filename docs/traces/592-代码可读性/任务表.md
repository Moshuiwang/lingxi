# 任务表（代码可读性规范与门禁批；状态即文件当前值，编排者随执行 commit 更新；第一恢复点）

> v1 草案（2026-09-04 北京）。执行路由：`claude_tmux_named_session`，session `TZ-server`：`rd-b1`（门禁四件套＋集成＋stage 验证，Fable 5.1 xhigh（见 [决策清单.md](决策清单.md) D-16），持分支写入权与 Draft PR）/ `rd-close`（收口，可由 b1 兼）。七批施工为子代理各自 worktree。模型路由见 [合同.md](合同.md) §5。
> 证据只认 GitHub / 仓库留痕；每个 Step 完成时勾选并附指针。行格式 `- [x] S-X 一句话（关键指针）`。
> **本表只随四次推送更新**（推送点与文档纪律见 [决策清单.md](决策清单.md) D-18 / D-19），Trace 期间不开独立文档 PR；**合并后步骤（V-2 / Z-1 / C-1 / C-3）的证据落 #592 收口评论**，本表最终状态以推送 #4 为准。

## 块 A（产品负责人前置输入）

- [x] S-0 三件套 PR 合并 = v1 批准（PR #594，main `86f42dc`）
- [x] K-1 冻结；唯一例外 #593 热修 `c6fa5ac`（[裁定评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5537788621)）
- [x] K-2 形态已裁（PM 亲加只读规则）→ 实际落地（2026-09-04 23:4x 北京，PM「批」）：甲不可行（生产备份目录 700 / 文件 600，`admin-wzp` 读不到），由元守护经管道代取直送 stage `/home/wangzhipeng/readability-backup/`，不落 tz 盘、编排者不碰生产、不加任何 prod 规则（留痕见 #592 元守护评论）
- [x] K-3 块 Z 单字「批」后编排者 `gh pr merge --merge`（同上；合并方式见决策清单 D-15）
- [x] K-4 外审压成两轮：codex 审门禁脚本（G-2 同期）与集成后全分支（R-1 同期）各 1 次，agy 每批 1 次（[决策清单.md](决策清单.md) K-4）

## W0 只读前置（合同批准前即可开始，不派施工）

- [x] W0-1 本机残留盘点：主工作树＋本会话集成 worktree；`lingxi-test-pg` 为 #358 登记保留项；`.dev-check/` 782M 登记不清（[W0 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5539586786)）
- [x] W0-2 ruff 0.16.4 离线可用（`uvx --offline ruff@0.16.4 --version` = 0.16.4；同上）
- [x] W0-3 stage 现状：三容器 Exited、镜像 `20260904-c6fa5ac5f9ad`、库 `postgres` 51 MB（Supabase pooler，无 `task_queue` 表，实际为 `task`）、Bot-Test 键存在（同上）
- [x] W0-4 生产最新（唯一）备份：`lingxi-prod-20260902T053005Z.dump`，653,817 字节，custom 格式 v1.16（pg_dump 17，服务端 17.6），09-02 05:30Z 即上线前；在 stage 回读，sha256 两侧一致（元守护留痕）
- [x] W0-5 正式起点读数（`readability_report.py` 对 `c6fa5ac` 树）：> 60 行函数 185、注释＋docstring 34.6%、来历标记 2,409 行 / 172 文件、docstring > 10 行 524、`#` 块 > 5 行 351（参考版见 [W0 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5539586786)；正式读数落 G-1 完成评论）

## G 门禁四件套（`refactor/readability`，Sonnet 5，四组顺序 commit）

- [x] G-1a A 组：`ruff check --fix`（不带 unsafe）→ 手修 13 处 → `check_size_ratchet.py --refresh`（`onboarding_runner.py` 归零；`apps/worker/service.py` 的 1533 行登记留到 B-1）与 `test_size_ratchet_check.py` 同步（`1d8e4d9` / `adecb04` / `4591633`；偏离：`onboarding_runner.py` 恢复误删 re-export 后回到 1502、归零移到 B-1；E402 实测 8；决策清单 D-23）
- [x] G-1b B 组：`[tool.ruff]`（行宽 100、规则集、per-file-ignores 初始清单）→ 两工作流 pin `ruff==0.16.4` → `gate_spec.py` / `check.sh` / `README` → `verify_repository.sh`（ruff check、noqa 禁用 grep、版本打印）→ `.gitignore` → `test_dev_check_gate_spec.py` ＋ 新增 `test_ruff_config.py` → `V-部署-23`（`12e15c7` ＋ 修订 `2c07bf3`：抑制注释全形态封死、`D` 范围收窄到 `src/lingxi/`，决策清单 D-21 / D-22）
- [x] G-1c C 组：`ruff format` 全仓（六个贴线文件进 format exclude）→ `.git-blame-ignore-revs` → `verify_repository.sh` 加 `format --check` → 文件棘轮基线刷新（`25ecb82` / `f171b95` 纯格式化 / `cf4fe23`）
- [x] G-1d D 组：`check_function_size_ratchet.py`（`--bootstrap` 条数以实测为准并写进 commit 正文，`c6fa5ac` 参考约 185 条 / 87 文件）＋ `check_comment_ratchet.py`（三类计数）＋ 两份基线登记 `FULL_PREFIX_DATA_FILES` ＋ 测试 ＋ `verify_repository.sh` 两行 ＋ `V-部署-24/25` ＋ `docs/技术设计/代码规范.md` ＋ `代码框架.md` 指针与过期句删除 ＋ `AGENTS.md` 读取表 ＋ `check_docs_size_budget.py` 纳入并上调 40KB ＋ `scripts/dev/readability_report.py`（`a6cdf7a` / `faa21c0` / `bc6cbc0` / `4b13d34`；`--bootstrap` 实测 189 条函数、注释棘轮 411 条；`代码规范.md` 3348B、预算 39194B / 40960B）
- [x] G-1e 本机 `check.sh full` 绿 → 推送 → 开 Draft PR `refactor/readability → main` → Epic Full #1 绿（PR 链接）（编排者 full 绿 → 推送 #1 `ae004e7` → [Draft PR #596](https://github.com/Moshuiwang/lingxi/pull/596) → [Epic Full #1](https://github.com/Moshuiwang/lingxi/actions/runs/33884484757)，结论见后续评论）
- [x] G-2 Fable 审门禁脚本（绕过口、失败关闭、变异验红）＋ codex 第 1 轮（门禁脚本，K-4）同期 → 修复本地留着随推送 #2 走 → 结论落 #592（Fable 实测审核 ＋ codex 第 1 轮已出结论：8 条 P1 必修＋P2/P3 顺手改，见 [G-2 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5542026520)；修复 commit 随推送 #2）；修复 commit `242b135`（cherry-pick 为 `4d9c207`）：15 条 P1/P2/P3 全部收口，变异实测逐条验红，gate 与集成分支 fast 绿

## B 清理战役（七个 worktree 并行；每批：施工 → `check.sh fast` → agy 冷读 → 集成合入 → `check.sh full`；K-4 后 codex 不再每批）

- [x] B-1 六个贴线文件（Opus 5 / high）：`core/conversation/pipeline.py`、`apps/worker/service.py`、`apps/gateway/__init__.py`、`core/identity/onboarding_runner.py`（33 参数 `__init__` → 参数对象）、`core/admin/router.py`、`apps/scheduler/permission_refresh.py`；完成后从 format exclude 删除；`apps/worker/service.py` 的文件棘轮登记（1533）在本批归零（已分叉 2026-09-04 20:54 北京：`readability/b1`，基点 `12e15c7`，Opus 5 high）；**已合入** `2ed3d30`（B-1 14 个 commit `7cef030…8dcc101`：21 个新模块、28 文件全部 ≤ 1000 行、无 > 60 行函数、`AutoOnboardingRunner.__init__` 31 参数 → 5 个参数对象；函数棘轮 189→155、文件棘轮归零、六文件出 format exclude 与 per-file-ignores；agy 冷读两文件均通过；台账 110 条抽 12 条）；集成后 full 抓到 2 处遗漏（私有钩子转发、撤权动作误传来源卡片号）由集成者修复 `7227a54`，full 5741 用例绿
- [x] B-2 `apps/worker/`、`apps/gateway/` 其余（已分叉 2026-09-04 22:2x 北京：`readability/b2`，基点 `4b13d34`，Sonnet 5）；**已合入** `abd3911`（11 个 commit `e4a741d…39345c6`：16 文件全部 ≤ 1000 行、`cli.py::main` / `turn.py::run_turn` 拆散、`cli.py` 按 B-1 参数对象签名装配；函数棘轮 155→133、注释棘轮 391→354；agy 冷读 `turn.py` / `document_delivery.py` 通过；台账 115 行；集成分支 full 5741 用例绿）
- [ ] B-3 `apps/scheduler/` 其余（已分叉 2026-09-04 22:2x 北京：`readability/b3`，基点 `4b13d34`，Sonnet 5）（首次汇报 2026-09-05 01:4x 北京：D 规则未收口 178 处、`daily_report.py` 1162 行超关卡，已退回补做）
- [ ] B-4 `core/identity/`、`core/permission/`（已分叉 2026-09-04 22:2x 北京：`readability/b4`，基点 `4b13d34`，Sonnet 5）
- [ ] B-5 `core/admin/`、`core/execution/`、`core/conversation/` 其余、`core/` 零散（已分叉 2026-09-04 22:2x 北京：`readability/b5`，基点 `4b13d34`，Sonnet 5）
- [ ] B-6 `adapters/postgres_*` ＋ #593 P2-a（`_IdleConnectionPool.acquire()` 弹出已关闭连接时 `discard()`，决策清单 D-17）（已分叉 2026-09-04 22:2x 北京：`readability/b6`，基点 `4b13d34`，Sonnet 5）
- [x] B-7 `adapters/feishu_*` 及其余 adapters、`config/`、`apps/` 零散（已分叉 2026-09-04 22:2x 北京：`readability/b7`，基点 `4b13d34`，Sonnet 5）；**已合入** `7c6c034`（32 个 commit：48 文件全部 ≤ 1000 行，`feishu_directory.py` 1069→402 拆出 `feishu_paged_client.py`；函数棘轮 133→99、注释棘轮 354→272；agy 冷读 `apps/reauthorize` / `adapters/feishu_directory` 通过；台账 476 行；集成分支 full 5741 用例绿）。**偏差**：D101/D102/D103/D107/D205 未收口（隔离命令 79 处），D 豁免暂留，复修代理在 `readability/b7-docs` 补做，随推送 #3 删除条目
- [x] I-1 第一次集成推送（B-2…B-7 中先完成的三批合入后）→ 推送 #2 → Epic Full #2（B-1 / B-2 / B-7 三批合入后 full 绿，推送 #2 = `7c6c034` 之后的文档 commit，见 #592 推送评论）
- [ ] I-2 第二次集成：其余批次合入、本机 `check.sh full`，**不推送**（D-19）
- [ ] I-3 第三次集成推送 = 全部七批 ＋ B-8 收官 ＋ R-1 修复（src 终态）→ 推送 #3 → Epic Full #3（镜像制品供 V-1b）
- [ ] B-8 收官（集成者）：清转发、两条棘轮基线归零、`D` per-file-ignores 与 format exclude 清空、文件阈值降到 1000、`代码规范.md` 定稿、#593 P2-b 三个 app 停机显式 `close_idle_connections()`（D-17）、`readability_report.py` 读数落 #592 → 随推送 #3
- [ ] R-1 Fable 审收官全分支（行为不变、「为什么」未丢、门禁绕过口）＋ codex 第 2 轮（集成后全分支，K-4）→ 修复 → 结论落 #592 → 随推送 #3
- [ ] R-2 资源占用静态审核（K-5）：Fable 5.1 xhigh 对 src 终态全量 diff（`c6fa5ac...refactor/readability` 的 `src/` ＋ `deploy/`），R-1 修复后、推送 #3 前，与 codex 第 2 轮同期（前五面清单并入其提示词，不加外审轮次）；六面逐条表（数据库连接 / 轮询与定时常量 / 外部调用次数 / 内存与 CPU / 磁盘与制品 / 本批资源账）→ 结论落 #592 → 修复随推送 #3

## V stage 验证（Bot-Test ＋ 生产备份恢复的独立库）

- [x] V-1a 按 K-2 通路取得备份文件；恢复到 stage 独立库（库名带 `readability` 标识，不覆盖 stage 现有库）；回读表计数；**已做**：stage 容器 `lingxi-readability-pg`（`postgres:17`，与 dump / 生产服务端 17 一致，`postgres:16` 读不了 v1.16 dump，偏离登记）、trust 认证只绑 `127.0.0.1:15433`、备份目录只读挂载；`pg_restore --no-owner --no-privileges -n public`（先建 `extensions` schema 装 uuid-ossp / pgcrypto）零错误；回读：public 34 表、`app_user` 1 / `task` 3 / `publish_outbox` 1、alembic 头 `0084_management_card_state_cas`、13 MB；stage 现有 Supabase 库未触碰。**限制**：备份早于 09-02 上线，用户 1 / 任务 3，数据形态代表性有限（T-5 证据如实标注）
- [ ] V-1b 用推送 #3 的 Epic Full 镜像制品升级 stage（`--profile mvp`）；`deploy/.env.stage.gateway` / `deploy/.env.stage.worker-queue` 加 `LINGXI_GATEWAY_DELIVERY_POLL_INTERVAL_SECONDS=5` / `LINGXI_WORKER_POLL_INTERVAL_SECONDS=10` 与生产对齐；健康检查绿；迁移头前进；V 段做完停掉 stage 三服务
- [ ] V-1c 真实旅程：首聊、问数、管理卡、定时职责各一次（受控测试身份）；飞书侧可见结果 ＋ 服务端结构化日志双通道；清场回读
- [ ] V-1d 资源对比（K-5）：stage 升到推送 #3 镜像后 10 分钟空转 ＋ 一次 V-1c 旅程，对比生产基线（元守护用只读命令采集并以评论落 #592；编排者不碰生产）：建连 / 分钟、会话峰值、出口字节（stage 侧 `ss` 绝对值）、三容器 RSS / CPU、日志行 / 分钟、各类外呼计数；**通过标准：建连 / 会话 / 外呼不高于基线，RSS / CPU / 日志不高于基线 +10%**；做完停 stage
- [ ] V-2 人类同事抽读 2 个文件（块 Z，结果由 PM 转述或同事直接评论 #592）

## Z 合入与收口

- [ ] Z-1 推送 #4（收官纯文档）→ Epic Full #4 绿 → Draft 转 Ready（`gh pr ready`）→ 就绪自检落 #592 → PM 单字「批」（K-3）→ `gh pr merge --merge`（D-15）合入 main
- [ ] C-1 环境清理与残留盘点：七个 worktree、临时真库容器、stage 备份库（保留或删除按 PM 一句话）、受控测试身份留痕、远端分支
- [ ] C-2 任务表勾选回写随推送 #4（收官纯文档）合入；不开独立文档 PR（D-18）
- [ ] C-3 收口评论（UTC＋北京、预算消耗、Epic Full 次数 / commit 数、代理人次、外审轮次、`readability_report.py` 起点 vs 收官读数）＋ #147 修订候选（派发卡与审查清单「可读性」条款）＋ #591 关闭说明
