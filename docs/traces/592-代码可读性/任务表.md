# 任务表（代码可读性规范与门禁批；状态即文件当前值，编排者随执行 commit 更新；第一恢复点）

> v1 草案（2026-09-04 北京）。执行路由：`claude_tmux_named_session`，session `TZ-server`：`rd-b1`（门禁四件套＋集成＋stage 验证，Opus 5 / max，持分支写入权与 Draft PR）/ `rd-close`（收口，可由 b1 兼）。七批施工为子代理各自 worktree。模型路由见 [合同.md](合同.md) §5。
> 证据只认 GitHub / 仓库留痕；每个 Step 完成时勾选并附指针。行格式 `- [x] S-X 一句话（关键指针）`。

## 块 A（产品负责人前置输入）

- [ ] S-0 三件套 PR 合并 = v1 批准
- [ ] K-1 战役期间 main 是否冻结 src 改动（默认冻结）
- [ ] K-2 生产最新备份到达 stage 的通路（默认 PM 亲加一条只读 scp 规则）
- [ ] K-3 合入 main 的放行形式（默认块 Z 单字「批」后编排者执行 merge）

## W0 只读前置（合同批准前即可开始，不派施工）

- [ ] W0-1 本机残留盘点：`git worktree list`、`docker ps -a` 中 `lingxi-*` 临时容器、`.dev-check/` 体量；有残留先登记再清
- [ ] W0-2 ruff 0.16.4 离线可用复核（`uvx --offline ruff@0.16.4 --version`）；不可用则改为在线拉取并记录来源
- [ ] W0-3 stage 现状只读盘点：compose 栈状态、当前镜像 tag、stage 库名与体量、Bot-Test 应用配置存在
- [ ] W0-4 生产最新备份文件名与大小（只读，扁平单条 `ls -l`）；不复制、不打开
- [ ] W0-5 `readability_report.py` 起点读数（用 #591 的测量脚本先出一版，落 #592 评论作为基线）

## G 门禁四件套（`refactor/readability`，Sonnet 5，四组顺序 commit）

- [ ] G-1a A 组：`ruff check --fix`（不带 unsafe）→ 手修 13 处 → `check_size_ratchet.py --refresh` 与 `test_size_ratchet_check.py` 同步
- [ ] G-1b B 组：`[tool.ruff]`（行宽 100、规则集、per-file-ignores 初始清单）→ 两工作流 pin `ruff==0.16.4` → `gate_spec.py` / `check.sh` / `README` → `verify_repository.sh`（ruff check、noqa 禁用 grep、版本打印）→ `.gitignore` → `test_dev_check_gate_spec.py` ＋ 新增 `test_ruff_config.py` → `V-部署-23`
- [ ] G-1c C 组：`ruff format` 全仓（六个贴线文件进 format exclude）→ `.git-blame-ignore-revs` → `verify_repository.sh` 加 `format --check` → 文件棘轮基线刷新
- [ ] G-1d D 组：`check_function_size_ratchet.py`（`--bootstrap` 出 184 条）＋ `check_comment_ratchet.py`（三类计数）＋ 两份基线登记 `FULL_PREFIX_DATA_FILES` ＋ 测试 ＋ `verify_repository.sh` 两行 ＋ `V-部署-24/25` ＋ `docs/技术设计/代码规范.md` ＋ `代码框架.md` 指针与过期句删除 ＋ `AGENTS.md` 读取表 ＋ `check_docs_size_budget.py` 纳入并上调 40KB ＋ `scripts/dev/readability_report.py`
- [ ] G-1e 本机 `check.sh full` 绿 → 推送 → 开 Draft PR `refactor/readability → main` → Epic Full #1 绿（PR 链接）
- [ ] G-2 Fable 审门禁脚本（绕过口、失败关闭、变异验红）→ 修复 → 结论落 #592

## B 清理战役（七个 worktree 并行；每批：施工 → `check.sh fast` → codex → agy → 集成合入 → `check.sh full`）

- [ ] B-1 六个贴线文件（Opus 5 / high）：`core/conversation/pipeline.py`、`apps/worker/service.py`、`apps/gateway/__init__.py`、`core/identity/onboarding_runner.py`（33 参数 `__init__` → 参数对象）、`core/admin/router.py`、`apps/scheduler/permission_refresh.py`；完成后从 format exclude 删除
- [ ] B-2 `apps/worker/`、`apps/gateway/` 其余
- [ ] B-3 `apps/scheduler/` 其余
- [ ] B-4 `core/identity/`、`core/permission/`
- [ ] B-5 `core/admin/`、`core/execution/`、`core/conversation/` 其余、`core/` 零散
- [ ] B-6 `adapters/postgres_*`
- [ ] B-7 `adapters/feishu_*` 及其余 adapters、`config/`、`apps/` 零散
- [ ] I-1 第一次集成推送（含 B-1 ＋ 2 批）→ Epic Full #2
- [ ] I-2 第二次集成推送 → Epic Full #3
- [ ] I-3 第三次集成推送（全部七批）→ Epic Full #4
- [ ] B-8 收官（集成者）：清转发、两条棘轮基线归零、`D` per-file-ignores 与 format exclude 清空、文件阈值降到 1000、`代码规范.md` 定稿、`readability_report.py` 读数落 #592 → Epic Full #5
- [ ] R-1 Fable 审收官全分支（行为不变、「为什么」未丢、门禁绕过口）→ 结论落 #592

## V stage 验证（Bot-Test ＋ 生产备份恢复的独立库）

- [ ] V-1a 按 K-2 通路取得备份文件；恢复到 stage 独立库（库名带 `readability` 标识，不覆盖 stage 现有库）；回读表计数
- [ ] V-1b 用 Draft PR Epic Full 的镜像制品升级 stage；健康检查绿；迁移头前进
- [ ] V-1c 真实旅程：首聊、问数、管理卡、定时职责各一次（受控测试身份）；飞书侧可见结果 ＋ 服务端结构化日志双通道；清场回读
- [ ] V-2 人类同事抽读 2 个文件（块 Z，结果由 PM 转述或同事直接评论 #592）

## Z 合入与收口

- [ ] Z-1 Draft 转 Ready（`gh pr ready`）→ 就绪自检落 #592 → PM 单字「批」（K-3）→ 合入 main
- [ ] C-1 环境清理与残留盘点：七个 worktree、临时真库容器、stage 备份库（保留或删除按 PM 一句话）、受控测试身份留痕、远端分支
- [ ] C-2 任务表勾选回写 PR（纯文档）
- [ ] C-3 收口评论（UTC＋北京、预算消耗、Epic Full 次数 / commit 数、代理人次、外审轮次、`readability_report.py` 起点 vs 收官读数）＋ #147 修订候选（派发卡与审查清单「可读性」条款）＋ #591 关闭说明
