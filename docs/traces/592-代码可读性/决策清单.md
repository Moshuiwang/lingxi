# 决策清单（代码可读性规范与门禁批）

> 已裁定项全部来自产品负责人 2026-09-04 在本机会话的裁定，正文已回写 [#591](https://github.com/Moshuiwang/lingxi/issues/591)「裁定汇总」与「裁定记录」；本文件保留原话与执行侧含义。**K-1…K-3 为本 Trace 仅剩的开放项**，随三件套 PR 批准一并裁定，单字回复有效。

## 一、已裁定

| # | 事项 | 裁定 | 产品负责人原话（节选） | 执行侧变成什么 |
| --- | --- | --- | --- | --- |
| D-1 | 整改深度 | 门禁＋全量清理 | 选项「门禁＋全量清理」 | 七批清理覆盖 `src/lingxi/` 全部 174 个涉及文件 |
| D-2 | 注释政策 | 只留契约、限长；决策史出代码 | 选项「只留契约、限长；决策史出代码」 | 注释棘轮三类计数；`代码规范.md`「为什么」去处表 |
| D-3 | 新函数硬上限 | 60 行 | 选项「60 行」 | `check_function_size_ratchet.py` 阈值 60 |
| D-4 | 全仓格式化时机 | rc25 收尾后单独做 | 选项「rc25 收尾后单独一个 PR 做」 | G-1c 组；rc25 已收口，条件成立 |
| D-5 | 分支与发布 | 新分支，稳定并经 stage 验证后再发布部署 | 「用一个新的分支执行，未来分支稳定和安全，经过了测试及 stage 验证成功之后，再发布及部署」 | `refactor/readability` ＋ 唯一 Draft PR；合入 main 在 stage 验证后 |
| D-6 | stage 环境 | Bot-Test；只读来源共用；**数据库不共用，用备份恢复到 stage** | 「stage 使用 bot-test，其他与 prod 可以共用（数据库、权限表格等）」→ 规划者指出共用活库会领走生产任务后：「数据库不共用，用备份恢复到 stage」 | V-1a 恢复到 stage 独立库；生产侧只读取一个备份文件（K-2） |
| D-7 | 编排形态 | 少开 PR、少跑 CI，Trace 内最大化并行，保障质量 | 「争取在 trace 中进行最大化的并行编排，减少 pr 及 ci 次数，防止进度过慢，但的确要保障施工质量」 | 1 个 PR、Epic Full ≤ 6、七批 worktree 并行、质量四道 |
| D-8 | 模型路由 | 施工 Sonnet 5（批 1 Opus 5 high）；外审 codex ＋ agy；Fable 审门禁与收官；编排 Opus 5 max | 「现在 tz 有 agy gemini 3.8 flash，codex luna max，都可以作为施工或者审核决策，claude 的施工角色是否可变成 sonnet，我的目标是快速施工，谨慎验证」；随后「四条修改意见批准」 | [合同.md](合同.md) §5；不用 gemini / codex 施工 |
| D-9 | 行宽 | 100 | 「五条都按你的建议」 | `[tool.ruff] line-length = 100` |
| D-10 | 注释长度上限 | 模块 15 / 类与函数 10 / `#` 块 5 行 | 同上 | 注释棘轮阈值 |
| D-11 | `代码规范.md` 进开工必读集 | 进；预算上调到 40KB | 同上 | `check_docs_size_budget.py` 纳入并上调 |
| D-12 | `tests/` 范围 | 只做 lint 与格式化 | 同上 | 不做长度与注释清理；收官后可另立小 Trace |
| D-13 | 执行形态 | 直接生成一个 `[tracking]` Trace | 同上 | 本 Trace #592 |
| D-14 | 规划者角色 | 只创建 Trace，不执行 | 「你只负责创建 trace，不负责执行」 | 编排者与元守护由产品负责人另行拉起 |

## 二、待裁定（随本 PR 批准一并回复）

| # | 事项 | 默认值 | 备选 | 不裁定的影响 |
| --- | --- | --- | --- | --- |
| K-1 | 战役期间 main 是否冻结 src 改动 | **冻结**，只放文档；#585 / #586 / #587 顺延到收官后 | 不冻结：集成者每批结束把 main 合进分支，其他作者 rebase 后跑 `ruff check --fix` + `ruff format` | 按默认执行；若期间 main 合入 src 改动，收官合并冲突由集成者承担 |
| K-2 | 生产最新备份如何到达 stage | **产品负责人亲手加一条只读 scp 规则**（从生产备份目录取一个文件到 stage），代理只执行该条 | 产品负责人亲自复制到 stage 固定目录，代理零接触生产 | V-1a 无法开始；stage 验证退化为 stage 现有库（数据形态与生产不同，T-5 证据降级并如实标注） |
| K-3 | 合入 main 的放行形式 | **块 Z 产品负责人在 #592 单字「批」，编排者执行 `gh pr merge`** | 产品负责人亲自合并 | Draft 停在 Ready 不合，T-7 不达成 |
