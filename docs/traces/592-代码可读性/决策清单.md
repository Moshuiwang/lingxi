# 决策清单（代码可读性规范与门禁批）

> 已裁定项全部来自产品负责人 2026-09-04 在本机会话的裁定，正文已回写 [#591](https://github.com/Moshuiwang/lingxi/issues/591)「裁定汇总」与「裁定记录」；本文件保留原话与执行侧含义。**K-1…K-3 已随三件套 PR #594 合并一并裁定**（终值见第二节，[裁定评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5537788621)）；K-4 与第三节的偏离、例外、执行注记由编排者 rd-b1 开工时登记（[接管评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5539545516)）。

## 一、已裁定

| # | 事项 | 裁定 | 产品负责人原话（节选） | 执行侧变成什么 |
| --- | --- | --- | --- | --- |
| D-1 | 整改深度 | 门禁＋全量清理 | 选项「门禁＋全量清理」 | 七批清理覆盖 `src/lingxi/` 全部 174 个涉及文件 |
| D-2 | 注释政策 | 只留契约、限长；决策史出代码 | 选项「只留契约、限长；决策史出代码」 | 注释棘轮三类计数；`代码规范.md`「为什么」去处表 |
| D-3 | 新函数硬上限 | 60 行 | 选项「60 行」 | `check_function_size_ratchet.py` 阈值 60 |
| D-4 | 全仓格式化时机 | rc25 收尾后单独做 | 选项「rc25 收尾后单独一个 PR 做」 | G-1c 组；rc25 已收口，条件成立 |
| D-5 | 分支与发布 | 新分支，稳定并经 stage 验证后再发布部署 | 「用一个新的分支执行，未来分支稳定和安全，经过了测试及 stage 验证成功之后，再发布及部署」 | `refactor/readability` ＋ 唯一 Draft PR；合入 main 在 stage 验证后 |
| D-6 | stage 环境 | Bot-Test；只读来源共用；**数据库不共用，用备份恢复到 stage** | 「stage 使用 bot-test，其他与 prod 可以共用（数据库、权限表格等）」→ 规划者指出共用活库会领走生产任务后：「数据库不共用，用备份恢复到 stage」 | V-1a 恢复到 stage 独立库；生产侧只读取一个备份文件（K-2） |
| D-7 | 编排形态 | 少开 PR、少跑 CI，Trace 内最大化并行，保障质量 | 「争取在 trace 中进行最大化的并行编排，减少 pr 及 ci 次数，防止进度过慢，但的确要保障施工质量」 | 1 个 PR、Epic Full ≤ 6、七批 worktree 并行、质量四道 |
| D-8 | 模型路由 | 施工 Sonnet 5（批 1 Opus 5 high）；外审 codex ＋ agy；Fable 审门禁与收官；编排 Opus 5 max | 「现在 tz 有 agy gemini 3.8 flash，codex luna max，都可以作为施工或者审核决策，claude 的施工角色是否可变成 sonnet，我的目标是快速施工，谨慎验证」；随后「四条修改意见批准」 | [合同.md](合同.md) §5；不用 gemini / codex 施工 |
| D-9 | 行宽 | 100 | 「五条都按你的建议」 | `[tool.ruff] line-length = 100` |
| D-10 | 注释长度上限 | 模块 15 / 类与函数 10 / `#` 块 5 行 | 同上 | 注释棘轮阈值 |
| D-11 | `代码规范.md` 进开工必读集 | 进；预算上调到 40KB | 同上 | `check_docs_size_budget.py` 纳入并上调 |
| D-12 | `tests/` 范围 | 只做 lint 与格式化 | 同上 | 不做长度与注释清理；收官后可另立小 Trace |
| D-13 | 执行形态 | 直接生成一个 `[tracking]` Trace | 同上 | 本 Trace #592 |
| D-14 | 规划者角色 | 只创建 Trace，不执行 | 「你只负责创建 trace，不负责执行」 | 编排者与元守护由产品负责人另行拉起 |

## 二、随批准裁定的 K 项（终值）

| # | 事项 | 终值 | 产品负责人原话（节选） | 执行侧变成什么 |
| --- | --- | --- | --- | --- |
| K-1 | 战役期间 main 是否冻结 src 改动 | **冻结**：main 只放文档，#585 / #586 / #587 顺延到收官后。**唯一例外**：先行的 #593 数据库连接复用热修（PR #595，main `c6fa5ac`） | 「裁定也可以按你的走」（2026-09-04，取合同默认值）；同日「暂时不拉起编排者。我有个 hotfix 要马上修改」 | `refactor/readability` 从 `c6fa5ac` 切出；期间 main 若再合入 src 改动，收官合并冲突由集成者承担 |
| K-2 | 生产最新备份如何到达 stage | **产品负责人亲手加一条只读 scp 规则**（从生产备份目录取一个文件到 stage），代理只执行该条；不含任何生产写入 | 同上 | V-1a 前规则须就位；未就位则 V-1a 顺延、T-5 证据降级如实标注。生产侧其他动作（含 W0-4 的 `ls`）一律不做，备份文件名与大小在取回 stage 后回读 |
| K-3 | 合入 main 的放行形式 | **块 Z 产品负责人在 #592 单字「批」后，编排者执行 `gh pr merge`** | 同上 | 就绪自检落 #592 → 「批」→ `gh pr merge --merge`（合并方式见 D-15） |
| K-4 | 外审轮次 | **codex 压成两轮**：只审「门禁脚本」（与 G-2 同期）和「集成后的全分支」（与 R-1 同期）各 1 次；agy 冷读保留每批 1 次（无头、并行） | 「压成两轮」（2026-09-04 19:0x 北京，接手卡记载） | 合同 §5「codex 每批 1 次 ＋ 门禁 1 次 ≤ 9」作废，codex ≤ 2；每批质量道改为：本机门禁 → agy 冷读 → 集成合入 → `check.sh full`；「为什么」不丢的逐条抽查由集成者每批做，R-1 时 codex 与 Fable 再抽 |
| K-5 | 资源占用审核 | **#592 结束前加一轮资源占用审核**：R-2 静态（Fable，R-1 修复后、推送 #3 前，与 codex 第 2 轮同期，前五面清单并入其提示词）＋ V-1d 动态（stage 新镜像 vs **当前生产**基线，基线由元守护只读采集落 #592，编排者不碰生产）；**通过标准：建连 / 会话 / 外呼不高于基线，RSS / CPU / 日志不高于基线 +10%**；不加推送、不加外审轮次 | 「① 通过标准 +10%：认。② 基线：用当前生产，不在 stage 另跑旧镜像（省 40 分钟 stage 与出口）。③ 现在发。」（2026-09-04 23:2x 北京，`~/project/lingxi-archive/rd-b1-resource-review.md` §4）；理由原话：「R-1 只审『行为对不对』，不审『跑起来花多少』；#593 的教训是资源问题在功能测试里看不见、只在账单上看见」 | 任务表加 R-2 / V-1d，验收加判据；R-2 修复随推送 #3；收口评论附本批资源账（Epic Full 次数、镜像构建次数、worktree 数与磁盘、测试容器、模型额度）并证明已清 |
| K-6 | 合同 §5 预算修订 | Fable 审核轮次 2 → **4**（R-2 用 1，余 1 给修复后复核）；门禁施工复修余量 1 → **3**（再给 2 次，升 Opus 5 high；G-2 本次修复失败时直接用、不必再问）；B-1 复修余量 1 → **2**；施工人次总上限 12 → **15**；Epic Full 6、PR 1、codex 2、agy 8 不变 | 「好的，加，可以再稍微多一点赋予」（2026-09-04 23:3x 北京，`~/project/lingxi-archive/rd-b1-resource-review.md` §6） | 预算表以本行为准；收口评论按新上限核账 |
| K-7 | V-1c 旅程所用数据库 | **乙：stage 自己的 Supabase 库**——四份 env 只复原 DSN 行 → migrate（空跑，head 0087 前后一致）→ 三服务 healthy → 旅程 → V-1d 旅程段 → 停三服务（Supabase 窗口 21 分 49 秒）；恢复库 `lingxi_readability` 完成 V-1a / V-1b（迁移头前进）/ V-1d 空转用途后连同容器与数据卷删除，K-2 备份由元守护清。未选甲（Bot-Test 重授权后刷新快照，要走授权入口）、丙（手工把 Bot-Test 身份种进恢复库快照 = 假数据过） | 产品负责人 2026-09-05 06:4x 北京裁定，[元守护留痕](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5547260895)；起因：06:33 首聊回「当前没有可用的银河权限」，日志 `onboarding.result failure_reason=not_located`，恢复库组织快照是 Bot-Prod 视角 open_id、与 Bot-Test 不同应用，非代码回归 | T-5 现值如实标注两库分工；rd-b1 退场、rd-b2 接手（[接管评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5547309796)） |
| K-8 | V-2 同事抽读时点 | **延后**到合入 main 之后另行安排，不再作为块 Z 前置；T-1 与合同 M3 的「同事抽读通过」现值记「延后（K-8）」（合同正文不改） | 「不用这个了，回头再读」（2026-09-05 07:0x 北京，元守护转达） | 块 Z 前置只剩 V-1d 旅程段结论、推送 #4 Epic Full 绿、`gh pr ready`、就绪自检落 #592；V-2 结果日后落 #592 或后续 Issue |

## 三、开工时登记的偏离、例外与执行注记（编排者 rd-b1，2026-09-04）

| # | 事项 | 内容 | 来源 |
| --- | --- | --- | --- |
| D-15 | Z-1 合并方式 | `gh pr merge --merge`（保留分支提交历史）。理由：G-1c 全仓格式化依赖 `.git-blame-ignore-revs` 登记格式化 commit 的 SHA，squash 会让该 SHA 不落 main、blame 屏蔽失效；近三次 epic→main（#583 / #533 / #517）为 squash，本 Trace 不沿用。ruleset 允许 merge / squash / rebase 三种 | 编排者执行注记，[接管评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5539545516)已示；产品负责人可单字改 |
| D-16 | 编排者模型偏离 | rd-b1 = Fable 5.1 xhigh（合同 §5 写 Opus 5 max；接手卡记载当日 Opus 开工即 529 四次）。施工路由不变：B-1 Opus 5 high，门禁四件套与 B-2…B-7 Sonnet 5 | 产品负责人 2026-09-04 19:0x 裁定（接手卡） |
| D-17 | 热修 #593 的两条 P2 纳入本 Trace（合同 §3「行为缺陷只登记不修」的例外） | P2-a `adapters/postgres.py` `_IdleConnectionPool.acquire()` 弹出已关闭连接时未 `discard()` → **B-6**；P2-b 三个 app 停机时显式调用 `close_idle_connections()`（`apps/gateway/__init__.py`、`apps/scheduler/loop.py`、`apps/worker/service.py`）→ **B-8 由集成者做**。两处均为一行级修法、不改用户可见行为 | #593 [元守护审核评论](https://github.com/Moshuiwang/lingxi/issues/593#issuecomment-5538973426)与[交接评论](https://github.com/Moshuiwang/lingxi/issues/593#issuecomment-5539415500)；接手卡 §3 |
| D-18 | 文档改动不开独立 PR | 三件套、任务表勾选、裁定与收口回写都是 `refactor/readability` 上的 commit，随四次推送之一走；C-2 改为「随推送 #4 合入」；V-2 / Z-1 / C-1 / C-3 天然在合并后，证据落 #592 收口评论 | 产品负责人 PR / CI 纪律（接手卡 §2） |
| D-19 | 推送与 Epic Full 计划 | 只在四个点推送：#1 G-1e、#2 I-1（合入三批后）、#3 I-3 ＋ B-8 ＋ R-1 修复（src 终态，镜像制品供 V-1b）、#4 收官纯文档；目标 Epic Full = 4，上限 6，第 6 次前先在 #592 留言。分支名保持合同原值 `refactor/readability`（`epic/**` ruleset 要求 PR ＋ Story Fast、零绕过，元守护 19:04 修正），每次推送 image 都构建（实测 4–5 分钟），不需要 `Image-Candidate` trailer | 同上 |
| D-20 | W0-4 不在生产执行 | 任务表的「扁平单条 `ls -l`」与接手卡的「生产只允许 K-2 那一条」取更严者；文件名与大小在 K-2 取回 stage 后回读 | [接管评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5539545516)差异 3 |
| D-21 | `D`（docstring 结构）规则的强制范围 | 只对 `src/lingxi/` 强制；`tests/**`、`migrations/**`、`scripts/**` 以三条目录 glob 豁免 `D`（其他规则码仍逐文件登记）。理由：D-12 已裁 tests 不做注释清理；migrations 为生成文件；scripts 不在七批范围。T-3「per-file-ignores 为空」判读为 `src/lingxi/` 条目清空 ＋ 三条 glob 登记 | 编排者 2026-09-04 裁定（[G-1 完成评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5540749348)第 5 条），产品负责人可单字改 |
| D-22 | `src/lingxi/` 抑制注释政策 | 一切形态（`# noqa`、`# noqa: CODE`、`# fmt: off/skip`、`# ruff: noqa`，大小写与空格无关）一律判红；存量 61 文件 225 处 `# noqa: CODE`（212 处 `BLE001`，均为未选中规则族的惰性标记）机械剥掉、理由文字保留为普通注释；唯一豁免入口 per-file-ignores | #591 §五原文；编排者不采纳施工者「限定形态放行」的收窄（同上评论第 4 条） |
| D-23 | G-1a 实测偏离登记 | `onboarding_runner.py` 未退到 1497：#591 预期建立在 `ruff --fix` 误删 5 个 re-export 名字之上，恢复后回到 1502，该文件无可安全删除的死代码，基线归零移到 B-1；E402 实测 8 处（非 6）；`PLR0913` 实测 26 文件（合同「12 个」是 `__init__` 口径），存量留在 per-file-ignores 只减不增（合同 §3 登记不做） | [G-1 完成评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5540749348)偏离 1–3 |
| D-24 | 门禁的已知边界（只登记不改） | 同一提交同步改基线、或同一提交改门禁脚本 / 钉住测试本身，都可绕过棘轮与 ruff 门禁——威胁模型是「提交者即编排者本人在受控机器上」，不是敌手，与文件棘轮（#238）同一边界；两份新基线与文件棘轮同走 fast 分层（fast 层同样跑 `verify_repository.sh`）；物理行数指标可被压成单行（`E501` 已忽略，口径取舍）；来历正则 `#\d{2,4}` 不认 5 位编号；本机未跟踪的新文件不进 `git ls-files` 的 lint 范围（CI 不受影响）；字符串字面量里的 `noqa` 会被 grep 误伤（失败关闭方向，可接受）。脚本头与 `代码规范.md` §三登记前四条 | [G-2 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5542026520)；codex 第 1 轮 P1-2 / P1-4 / P1-6 / P1-7，Fable P3-3 / P3-5 |
| D-25 | `.git-blame-ignore-revs` 效果为零 | 文件已正确登记纯格式化 commit `f171b95`，但 git 2.43 的 ignore-revs 启发式对本次折行式重排没有把任何一行归因回去（施工者穷举 136 个被重排文件、编排者抽 3 文件复核一致）；机制配置正确、效果为零，属 git「best effort」限制。保留登记不再追；来历仍以 commit / PR 为准（D-2） | 门禁施工者 C/D 组汇报；[G-2 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5542026520) |
| D-26 | codex 第 2 轮的「证据缺口」类 P1 不计为缺陷 | 集成后全分支 diff 约 6 万行，codex 载荷按预算裁到 100KB，其中合并 commit 的 `git show` 为空、抽样 hunk 为空，codex 据此报「无法判定」；编排者逐条回源：`_number→_duration` 为假阳性（基点已用 `_duration`）、dedicated 连接由持有者关闭为既有设计、新增模块登记由 `check_installed_package.py --source-only` 坐实；坐实的四项小修（`close_all()` 逐连接隔离、worker 清理调度异常、去掉代码里的 `D-17` 决策编号与 `inspect.getsource` 测试细节、超时 docstring）落 `86f7c45` | 外部审查姿势记忆「无法判定多数是 payload 证据面不够」；[R 段评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5546415614) |
| D-27 | R-2 F-1：P2-a 对已关闭连接的 `discard()` 在驱动侧为直接返回 | 登记不修：该分支在生产实际不可达（空闲栈里的连接没人发语句，`pgconn.status` 不会自行变 BAD），即便可达释放延迟为引用计数级；保留显式 `discard()` 调用（不依赖回收时机），注释改为如实描述（`0c5af08`） | R-2 汇报 F-1 |
| D-28 | 纯移动带来的 PLR0913 豁免可随函数迁入新文件 | `handle_management_form_submit`（12 参数）从 `card_callback.py` 纯移动进新文件 `card_callback_management.py`，原文件本就有 PLR0913 豁免；新文件继承该条目并同步登记钉住快照（`2f0c3f5` 正文说明）。合同 §3 的「PLR0913 存量登记不做」按函数而非按文件路径理解；`src/lingxi/` 现存 PLR0913 条目 15 条、每条隔离检查仍有真实违规（R-1 实测） | R-1 P3-2 |
| D-29 | 编排者换任 | rd-b1（ctx 87%）2026-09-04 22:23Z 写交接快照后退场；rd-b2（Fable 5.1 xhigh）22:47Z 按 takeover 程序只读核实后登记接管与唯一合并权，从 K-7 乙路线的 V-1c 继续 | [交接快照](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5547122822)、[补记](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5547285568)、[接管登记](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5547309796) |
| D-30 | V-1c 执行注记 | ① 定时职责「手动触发一次」的形态 = `docker compose restart scheduler`（scheduler 无单次触发开关；首轮全职责 22:58:25Z 六条 `completed` / `already_completed_today`）；② `花名册审计日报 AccessTokenUnavailableError(no_credential_available)` 判定为 stage 环境固有：凭据卷只有锁与状态文件、无凭据文件本体，宿主机持久日志显示 08-28 / 08-30 / 09-02 旧镜像在同一库上同报；③ 产品负责人自发加做补充授权与撤销两个环节（`local_permission_grant` / `local_permission_revoke` 均 executed，`permission_targeted_recompute` unchanged），清场以撤销完成；④ worker 跑过任务后 RSS 278 MiB 对生产（空闲基线 97.9 / 07:0x 现值 161.4 MiB）无同态读数，按元守护裁定「不可比，登记观察」，生产趋势由元守护 7 天观察继续采 | [V-1c 评论](https://github.com/Moshuiwang/lingxi/issues/592#issuecomment-5547526650) |
