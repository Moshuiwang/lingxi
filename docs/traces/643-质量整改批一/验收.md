# 验收（质量整改批一；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace #643 或 PR）

> **等级口径 = 产品负责人全局协议 1–7**（1 已分析 / 2 已改代码 / 3 本地测试通过 / 4 CI 通过 / 5 已部署 / 6 真实旅程验证 / 7 回源确认）。与仓库 L0–L6 的换算见[验证与门禁「与全局证据等级对照」](../../技术设计/验证与门禁.md)。**本文件一律写全局数字、不写 `L` 前缀**——该文件明写「跨体系引用时直接查下表，不再混写」，写成 `L1（已分析）` 就是把两套体系混在一起。
> 每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，**不得为了收口好看而标绿**。
> **本 Trace 的等级天花板 = 5（已部署，环境=预发）**——不碰生产、无真实用户、不做外部提供方回源。**任何一行写出「5（生产）」「6」「7」即为错误。**交付终点封顶 `Runtime Assembled`（[合同](合同.md) §1）。

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准 | 目标等级 | 现值 | 关联验收矩阵 `V-*` | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1 权限完整决定链**（IN-01 + IN-02 + IN-05） | ①来源读取失败与合法空**可分辨**，信息不完整时不提交冒充完整结果的权限决定；②首聊 / 每日 / 定向三入口用「同输入 / 不可读 / 合法空集 / 身份变化」四组比较后完整决定一致，各入口身份与通知差异**逐条保留**；③提交事务内复核授权组仍有有效授权依据，**撤销后的迟到补齐不再产生生效权限**；④正常补齐、重复补齐、单指标撤销、重新导入四种既有行为**逐条不变**；⑤受控交错下过时计算不覆盖较新决定；⑥重构后源码路径守卫仍扫真正持有实现的文件，关键防线**变异能验红** | **4** + 真库否定用例 + 变异验红 | **4（CI 通过）** + 真库否定用例 + 变异验红；**⑤ 只部分成立**（撤销当轮生效已取证，读→提交毫秒窗口未关，见文末） | `V-权限-07`／`V-权限-08`／`V-权限-12`／`V-权限-15` | 批次 1 候选 `982b1da`／PR #652／`Epic Full` run 34050915510；批次 2 候选 `482870d`／PR #653／run 34065635369。`tests/test_local_override_read_failure_parity.py`、`tests/test_permission_decision_chain.py`；审核裁定见 #643 comment-5561949823 |
| **E-2 终态送达确认**（IN-03） | ①只有**本次终态的有效回执**才允许确认——旧进度卡消息编号、空响应都不算；②`230049`「仍在发送」不再归为明确拒绝，安全重试有依据；③卡片终态更新与文本兜底**都失败**时任务不记成功、会话不释放；④成功 / 明确失败 / 不明 / 安全重试 / 会话释放五分支各有真库回放；⑤按操作分别验文档写入与通知，**不为所有普通提示新增通用事务系统** | **4** + 真库回放；预发段 **5（环境=预发）** | **4（CI 通过）** + 真库五分支回放；**预发段未做** | `V-投递-03`／`V-投递-04`／`V-交付-06` | 批次 1 候选 `982b1da`／PR #652／run 34050915510。`TerminalReceiptRequiredBeforeConfirmTests` 五分支；`core/delivery/ports.py` 的 `DELIVERY_OPERATIONS` 分档 |
| **E-3 到期清理覆盖**（IN-04） | ①盘点出的全部适用载体（`task.prompt`、`inbound_event`、`pending_action` 及其余）接入实际清理入口与调度；②过期 / 未过期对照、边界时间、重复执行、清理失败后恢复、**旧备份恢复后按原始期限**五组全成立；③不擅改任何保留承诺、不靠删全部历史任务「修复」；④清理不会重开已结束会话或破坏必要交付事实 | **4** + 真库五组对照 | **4（CI 通过）** + 真库五组对照 | `V-保留-01`…`V-保留-20`／`V-投递-06`／`V-投递-07` | 批次 1 候选 `982b1da`／PR #652／run 34050915510。`tests/test_carrier_retention_duty.py`；迁移 `0089`；含真实 ACCESS EXCLUSIVE 锁冲突与同链建库 + COPY 复现 |
| **E-4 调度公平性**（IN-06） | ①真库里构造超过一批的不同用户任务，待重试任务**不持续占满**候选机会；②混合失败、恢复与重启下健康任务继续得到处理且**不重复交付**；③记录恢复后重复次数与等待时延；④**扩大批次上限只作对照、不作为修复** | **4** + 真库多用户 | **4（CI 通过）** + 真库多用户交错 | `V-队列-04`／`V-队列-06`／`V-队列-08` | 批次 2 候选 `482870d`／PR #653／run 34065635369。`tests/test_gateway_delivery_fairness.py`；迁移 `0090`；第 2 轮候选由 20 变 4、重复交付 0 次 |
| **E-5 管理回读与 SDK 错误**（IN-09 + IN-10） | ①管理动作已提交、后置回读故障时返回事务内已知决定与终态快照，或明确「已提交、展示待恢复」，**不把已执行说成未执行**；②重复确认 / 取消不重做业务动作；③按固定 SDK `0.2.128` 真实类型保留**有界、脱敏**的 `errors` 与 `api_error_status`，核到下游报告与恢复分支；④`is_error` 现有阻断行为保持——**不得**扩大成「失败必然判成功」 | **4** + 真库故障注入 + 官方数据类离线契约样例；预发段 **5（环境=预发）** | **4（CI 通过）** + 真库故障注入 + 官方数据类离线契约样例；**预发段未做** | `V-管理-01`／`V-管理-30`／`V-管理-31`／`V-管理-32`／`V-执行-04`／`V-执行-06`／`V-执行-17` | 批次 2 候选 `482870d`／PR #653／run 34065635369。`PostCommitReadbackFailureRealDbTests` 六条；`tests/test_worker_report.py`；`scripts/ci/check_agent_sdk_binding.py` 无条件断言 + 两条变异验红 |
| **E-6 版本装配与预发受控验收** | ①候选镜像构建成功、四个 tag 与 digest 冻结回填「批终链」；②预发升级后三容器 `healthy`；③预发**首触冒烟**一条普通问数走通（发现进缺陷账本，不消耗正式候选）；④E-1…E-5 断言按 S-0-5 归类的取证方法逐条取证，用户 / 管理员可见断言**双通道一致**；⑤预发**还原镜像 tag 并停回 `exited`** | **5（已部署，环境=预发）**；写全为 `真实链路 · 预发 · 代理` | **`真实链路 · 预发 · 代理`（全局 5，环境=预发）**——①四 tag 与 digest 已冻结回填；②三容器 `healthy`；③首触冒烟**并入 `D01`**（架构无注入口，见任务表 `S-3-9`）；④断言逐条取证已做，**但 E-1/E-2/E-4/E-5 的行为面在预发未被触发**；⑤已还原并停回不运行 | 同 E-1…E-5 各行 | `S-3-8` comment-5563687938；`S-3-10` comment-5563750701；`S-3-12` 见 #645（N01/D01/A04 三条全 PASS、4/4 完成标准由执行者回填）；`S-3-13` 还原回读见任务表 |
| **E-7 技术债**（#632 + #598） | **#632**：①方向定档带取舍依据（进程启动耗时实测 / 镜像体积 / 构建复杂度）；②改动落地、本机检查绿、CI 完整门禁绿；③**预发一段空闲期的分项读数**显示每次健康检查 `slab_reclaimable` 增量由 **74,192 B** 显著下降、`anon` 稳定、`memory.events` 三项仍全 0；④读数**只读采集**、与 #600 同一口径（按秒读 cgroup `memory.stat`，增量与健康检查时刻对齐），不进容器、不改配置、不重启。**#598**：⑤两条未合入分支按最新方案处置完毕；⑥约 135 KB 任务专属实验归档清理完成（**不删除用户原生历史**）；⑦卡关闭 | #632 **5（环境=预发）**；#598 **4** | #632 **`真实链路 · 预发 · 代理`（全局 5，环境=预发）**——预发回读达标：可对齐的 8 次健康检查 `slab_reclaimable` 增量**全部 +0 B**（基线 74,192 B/次），整窗净累积 0 B，`anon` 稳定、`memory.events` 五项全 0。#598 **未处置**（待产品负责人裁定关闭资格） | 不适用（纯工程，不认领产品断言） | #632 readback [comment-5563733933](https://github.com/Moshuiwang/lingxi/issues/632#issuecomment-5563733933)（384 秒空闲期、三容器各 360 点）；本机构建两目标见 #632 comment-5559718493。#598：`S-2-11` 已盘点上报 |
| **E-8 流程两卡**（#641 + #642） | **#641**：①判据两栏表进《浏览器验收》与《协作约定》；②#638 模板改七列并要求完整 digest；③完成标准由执行者回填的约定落文档；④案例库文件建立、BA-001 六条入库；⑤待裁定项按裁定项 5 落地。**#642**：⑥分类表与四环节要求进《协作约定》；⑦只读自检脚本可跑并输出待修正清单（**先只报不拦**）；⑧存量 14 个 Issue 归位、自检输出为空或只剩已知豁免；⑨`长期参考` 与状态标签互斥落文档 | **4** | **4（CI 通过）** | 不适用 | 流程车道候选 `01220dc`／PR #651／`Epic Full` run 34038274928；merge commit `d4366a1`。#641／#642 均已 Closed |

## 批终链（全绿才进下一阶段；三批各走一次）

本机 `check.sh full` 绿（**必须见到 `Ran N tests` 行**；管道 `grep` 滤掉该行不算数；改 `src/` 后 venv 非 editable，不重装会假绿）→ **唯一独立审核者**批量审查（一次；Opus 5，档位比该候选内最高实施档高一级——批次 1 / 2 = `xhigh`，批次 3 = `high`）→ **外审并行**（codex `gpt-6-astra`，批次 1 `xhigh` 绕过面 / 批次 2 `high` 守卫失效面 / 批次 3 `medium` 配置与文档面；agy `gemini-3.8-flash-high` ≤ 3 轮做一般审查与一致性冷读，**额度不足时由 `gpt-6-astra` / `medium` 承接**；**两者结论均须编排者回源坐实后才进缺陷账本**）→ **统一修复包 → 同一审核者定向复核（每批 ≤ 2 轮；第 2 轮只处理第一轮未闭合项；进入第 2 轮时仍有未闭合 P1 即命中动态升档触发，必须升档而非再耗一轮）** → **Fable 5.1 `xhigh` 终审 ≤ 2 次**（`S-1F-1` / `S-2F-1` / `S-3F-1`；余量不足如实记「未跑」，不顺延不挪用）→ 冻结候选 → CI 完整门禁 → 合 `main` → `Main Publish` 自动出镜像。

**批次 3 额外接**：镜像 digest 冻结 → 预发升级回读 → 首触冒烟 → 受控验收逐条取证 → 浏览器实测（≤ 2 次）→ **预发还原并停容器**。

冻结后回填：

- 批次 1 候选 SHA `982b1da`（分支 `batch/643-nx1`）；`Epic Full` run `34050915510`（全绿）；merge commit `7fc80f0`（PR #652）；`Main Publish` run `34051610346` → tag `20260906-7fc80f082e13`
- 批次 2 候选 SHA `482870d`（分支 `batch/643-nx2`）；`Epic Full` run `34065635369`（全绿；同码 `d61c711` 上 run `34061862028` 亦绿，`482870d` 首跑 run `34062385795` 因时间敏感用例抖动判红、同一 SHA 重跑转绿）；merge commit `f78aa17`（PR #653）；`Main Publish` run `34066239695` → tag `20260906-f78aa17619bf`
- **批次 3（= 本 Trace 的正式验收对象）** 候选 SHA `238cb54`（分支 `batch/643-nx3`；审查与终审的对象是 `c3d2809`，其后只叠加记账提交）；`Epic Full` run `34071410905`（全绿，`l1` 按规则跳过）；merge commit `c118f991`（PR #654）；`Main Publish` run `34072061769`
  - 四个镜像 tag **`20260907-c118f9915d7c`**；digest（GHCR API 与 `docker manifest inspect` **两条独立取法交叉验证一致**，带 `@` 前缀全值）：
    - scheduler `ghcr.io/moshuiwang/lingxi-scheduler:20260907-c118f9915d7c@sha256:d103ac8c799c38f8383015c80b4674ab704b6cc376097829ab54ef466ca5e1db`
    - migrate `ghcr.io/moshuiwang/lingxi-migrate:20260907-c118f9915d7c@sha256:5d29a5bae1085bb78b86d5293337231741b3d8cba830b65d0c29b8c658c85bfc`
    - gateway `ghcr.io/moshuiwang/lingxi-gateway:20260907-c118f9915d7c@sha256:a0677e21e91d8083b3d2e6b16350b09b0aafdd08911029d50a59042e0627e0f0`
    - worker `ghcr.io/moshuiwang/lingxi-worker:20260907-c118f9915d7c@sha256:ad6a501a9989caac40d9db8525d39b7a568c1e7751e7e35305d53fb4b0bff669`
- **发布：不适用——本 Trace 不发布生产。**预发升级前的原 tag = `20260905-92d016be943e`（还原基准）。

## 未验证层级（收口时如实列）

以下各条在本 Trace **必然**保持未验证，收口时原样保留、**不得改写为已验证**：

- **生产上零验证**——生产全程停在 `v2.2.0`（`20260905-92d016be943e`），本 Trace 的任何修复在生产的实际效果**一次都没测**。预发读数不得用来陈述生产结论。
- **无真实用户结论**——预发受控验收用的是 `Bot-Test` 受控测试身份，不是真实用户；因此不产生 6（真实旅程验证），也不产生 `User-path Delivered`。
- **#632 的生产验收未做**——该卡真正的终点是「生产上线后一段真实空闲期的分项对照读数」，本 Trace 只到预发；卡保持 Open。
- **slab 在内存压力下能否被足额回收仍未实测**（#600 原话）——既不能断言必然 OOM，也不能保证永不 OOM。
- **外部提供方契约未取得**（IN-11 / #616 IF-R5）——MCP 调用模式与版本、401 / 403 的真实原因、逐工具成功 / 空 / 失败样例矩阵**全部仍是未知**；本 Trace 不联系外部团队、不代其新增承诺，`#604` 的加固也因此不做。
- **真实飞书平台的重投时限与次数未独立取证**——E-2 的修复基于官方文档回源与真库回放，不覆盖平台自身的重投行为。
- **旧制品回退未验证**（IN-14 / #617 DB-R3）——本 Trace 只产出一个新制品，**没有**用上一正式版本对新库做关键读写；「结构往返不等于数据无损」这条边界原样保留。
- **真实备份与恢复目标未核定**（IN-15）、**运行负载 / 访问 / 审计 / 容量证据未取得**（IN-16）、**成本计量未做**（IN-18）、**部署启用 / 回退记录未建立**（IN-19）——四项都需要生产面证据。
- **20 个真实业务任务 × 三名用户 × 连续七个工作日未做**（IN-22）——必须在生产，与本 Trace 的「不碰生产」直接冲突。
- **#623 的 22 个 IN 未处理**——逐条去向见[合同](合同.md)覆盖对照表与 S-Z-4 的移交评论；**本 Trace 的 `Complete` 不等于 #623 的 `Complete`。**
- **IN-13 只做了本 Trace 触及的四个接口族**——文档全量收口未做；与 #598 第二批的分工按 #623 第 6 节，两者验收不互代。
- **IN-28 只做了「重构后守卫与变异验红」那半**——冷读可理解性、影响范围、维护耗时抽样未做；#591 既有可读性重构不重做，其 K-8 人类同事抽读仍未做。
- **Fable 5.1 三批的实际情况（2026-09-07 回填，原「批次 1 可能未跑」的预判已不成立）**——批次 1 与批次 2 的 `xhigh` 终审**都跑成了**（`S-1F-1` 2026-09-06 17:40Z 有独立留痕；`S-2F-1` 的留痕只在批次 2 收口评论的质量闸链与预算表里，无独立评论），每批 ≤ 2 次的配额各用 1 次。批次 3 的那次开跑前仍须先实测余量；跑不成就如实记「额度不足未跑」，**不顺延、不挪用**，也**不得**用别批的终审结论回溯覆盖本批候选。
- **`luna` 系模型在本机 codex 账号不可用**——2026-09-06 实测 `luna` 与 `gpt-6-luna` 均返回 `400`「not supported when using Codex with a ChatGPT account」；一致性冷读的承接方因此是 `gpt-6-astra` / `medium`，不是原建议的 `codex luna max`。
- **浏览器实测请求的交接方式尚未经多次验证**——[#640](https://github.com/Moshuiwang/lingxi/issues/640) 首跑出现过「Issue 已关闭、结果只在评论、正文完成标准复选框全空」；[#641](https://github.com/Moshuiwang/lingxi/issues/641) 第三节的「完成标准由执行者回填」是**新约定**。本 Trace 的两次 attempt **已跑完**：`#645` 已 CLOSED，且**完成标准四个复选框由执行者逐项回填 4/4**——与 [#640](https://github.com/Moshuiwang/lingxi/issues/640) 「已关闭但正文全空」正好相反，**这是该新约定的第二个成功样本**。**但仍不足以称为已验证机制**：样本量 2，且本次 attempt 2 的回报写在 #645 正文末尾而非评论里，元守护的巡检口径只看评论、因此误判了两轮——**巡检口径必须同时看正文**，这条已由元守护自陈并进移交材料。元守护 ≤ 30 分钟巡检必看这一项。
- **身份映射的接手方式已写下、但尚未经真实换机验证**（2026-09-07 订正，原写「未定」已不成立）——映射按裁定项 5 乙留在执行机本地并 git 忽略；**接手方式已由 E-8 随 PR #651 落地**，写在[浏览器验收](../../参考证据/浏览器验收.md)「身份映射的接手方式」一节：能访问原执行环境者直接复用，换了环境则原映射不可继承、按请求 Issue 记录的别名与角色描述在新环境重做一次账号识别。**仍未验证的是这条替代路径本身**——至今没有真的换过执行环境去走一遍，因此它是一条写下来但没跑过的约定。
- **#632 的字节码预编译未覆盖 migrate 镜像的迁移脚本目录**——`base` 阶段预编译的是标准库，四个目标共享该层；但 `migrate` 另有一步 `COPY migrations /opt/lingxi/migrations`，其后既未预编译也未禁写，因此加载这些模块时仍可能向 `/opt/lingxi/migrations/**/__pycache__/` 写缓存。**这是一个目录范围，不是单个文件**（外审 `gpt-6-astra` medium 2026-09-07 指出，编排者回源坐实）。判为可接受的依据是 migrate 只是一次性作业、**没有周期健康检查**——该判据已用实证补齐：`deploy/compose.yaml` 的 `migrate` 服务是 `profiles: ["job"]` 加 `restart: "no"`，**`deploy/` 下三份 compose（`compose.yaml` / `compose.stage.yaml` / `compose.prod.yaml`）都定义了 `migrate` 服务，三份均未给它定义 `healthcheck`**。**仍未取得的证据**：最终镜像在真实启动参数、环境变量与挂载下的运行期写入面未逐一排除，因此「运行时绝无第三条写入路径」**不成立为结论**，只成立为「本批材料内未发现第三条」。
- **`A04` 只验了「取消」路径，「确认」路径的管理卡终态可见性未验证**——原用例写的是「确认后再 `/admin resume` 还原」，编排者回源发现该处置**不成立**：`/admin suspend` 的执行分支在停用之外还会 `clear_delivered_content_for_user()` 与 `clear_user_memory()`，源码注释原话「记忆没有『resume 恢复』语义（硬 DELETE）」，**`resume` 恢复不了**。因此改用取消路径（终态卡判据 = 没有任何按钮，`core/admin/notification.py:252-257`），**完整覆盖了「管理卡终态是否真的不可再操作」这条需要飞书真实验真的断言**，但「确认路径下管理员看到的是真实执行结果」这一面**在飞书侧未被真实验真**（它在 CI 有真库故障注入覆盖）。
- **执行方主动做的三处收窄，原样保留、不得被后来人放宽**：①**不把总事件数说成 1**（`D01` 观察到事件总数 5、其中终态交付事件 1）；②**空记忆观察不外推**——执行方原话「取消前后账号均 `enabled`，记忆均 0 条且摘要相同。此项证明本次空记忆状态未变化，**不证明非空记忆保留**」；③**不把 CardKit API 的兼容占位当作卡片正文**。
- **`A04` 用例的操作序列可逆性，在写用例与贴表两处都没有被核过**——编排者写用例时要求「业务用户-B ≠ 业务用户-A」却**没有对着执行机的实际身份映射核过**（该映射按裁定项 5（乙）在执行机本地、git 忽略，编排者与元守护都读不到，**因此只能在写用例的那一刻核**）；元守护贴表时**只核了格式与 digest、没核语义**。两处都漏，最终是执行方因缺参数停下才避免了不可逆破坏。**这是本 Trace 最应该被后来人记住的一条流程缺口。**
- **旧制品与新库结构不兼容，已由本 Trace 变成一个具体实例（IN-14 不再是抽象风险）**——迁移 `0089` 给 `pending_action` 增加 `retention_expires_at`，**NOT NULL 且无默认值**。因此把预发镜像 tag 还原到 `20260905-92d016be943e` 之后，**旧镜像若真的启动，往 `pending_action` 的插入（管理卡、停用/恢复/撤销）会失败**。合同原写的「回滚到 `20260905-92d016be943e` 重启即恢复原状」**在事实层面不成立**。编排者据此**只把容器重建到还原基准镜像、刻意不启动**（状态 `Created`、运行中 0 个），并在 `biai-stage` 留下 `deploy/STAGE-WARNING-20260907.md` 就地说明与三条处置路径。**旧制品回退本身仍未验证。**
- **本 Trace 的八项修复在生产上零验证，因此与 [#606](https://github.com/Moshuiwang/lingxi/issues/606) 九人金丝雀的结果不可互相推断**——两者制品不同、环境不同、身份不同：本 Trace 的候选是 `20260907-c118f9915d7c` 且只在预发用 `Bot-Test` 跑过；#606 在生产、用真实用户。**不得用金丝雀的成败去陈述本 Trace 修复的效果，也不得用本 Trace 的预发结论去为金丝雀背书。**产品负责人已裁定 #606 排在本 Trace 之后、不并行。
- **`E-1⑤` 的读→提交毫秒窗口本批未关**——决定链已改成「有缺项就无条件重读」，关掉了补齐停在行锁上撑开的那个无界窗口（撤销当轮即生效，真库交错取证）；但重读之后到提交之间仍有一个毫秒级窗口，此间提交的一次撤销仍可能被这一轮的旧结论盖过一轮。彻底关掉它需要在提交事务内复核来源代次，属**设计改动、不在本 Trace 范围**，已在 `core/permission/decision_chain.py` 模块文档与真库用例文档里同步登记。
- **浏览器实测只覆盖卡片可见性**——按 #641 判据，流程 / 数据 / 状态 / 权限计算全部走真库与 API 只读回源，**没有**经过飞书真实验真。
- **IN-30 条件项**：收口时逐条声明本 Trace 是否改变了消费者部署 / 接管方式、是否升了 SDK、是否引入第二 MCP。**预判三项均不触发**；若批次 2 重构意外触及消费者接管方式，必须补旧执行者与新一轮的互斥验证，不得静默略过。
