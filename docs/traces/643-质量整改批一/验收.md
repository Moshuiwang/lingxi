# 验收（质量整改批一；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace #643 或 PR）

> **等级口径 = 产品负责人全局协议 1–7**（1 已分析 / 2 已改代码 / 3 本地测试通过 / 4 CI 通过 / 5 已部署 / 6 真实旅程验证 / 7 回源确认）。与仓库 L0–L6 的换算见[验证与门禁「与全局证据等级对照」](../../技术设计/验证与门禁.md)。**本文件一律写全局数字、不写 `L` 前缀**——该文件明写「跨体系引用时直接查下表，不再混写」，写成 `L1（已分析）` 就是把两套体系混在一起。
> 每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，**不得为了收口好看而标绿**。
> **本 Trace 的等级天花板 = 5（已部署，环境=预发）**——不碰生产、无真实用户、不做外部提供方回源。**任何一行写出「5（生产）」「6」「7」即为错误。**交付终点封顶 `Runtime Assembled`（[合同](合同.md) §1）。

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准 | 目标等级 | 现值 | 关联验收矩阵 `V-*` | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1 权限完整决定链**（IN-01 + IN-02 + IN-05） | ①来源读取失败与合法空**可分辨**，信息不完整时不提交冒充完整结果的权限决定；②首聊 / 每日 / 定向三入口用「同输入 / 不可读 / 合法空集 / 身份变化」四组比较后完整决定一致，各入口身份与通知差异**逐条保留**；③提交事务内复核授权组仍有有效授权依据，**撤销后的迟到补齐不再产生生效权限**；④正常补齐、重复补齐、单指标撤销、重新导入四种既有行为**逐条不变**；⑤受控交错下过时计算不覆盖较新决定；⑥重构后源码路径守卫仍扫真正持有实现的文件，关键防线**变异能验红** | **4** + 真库否定用例 + 变异验红 | — | `V-权限-07`／`V-权限-08`／`V-权限-12`／`V-权限-15` | — |
| **E-2 终态送达确认**（IN-03） | ①只有**本次终态的有效回执**才允许确认——旧进度卡消息编号、空响应都不算；②`230049`「仍在发送」不再归为明确拒绝，安全重试有依据；③卡片终态更新与文本兜底**都失败**时任务不记成功、会话不释放；④成功 / 明确失败 / 不明 / 安全重试 / 会话释放五分支各有真库回放；⑤按操作分别验文档写入与通知，**不为所有普通提示新增通用事务系统** | **4** + 真库回放；预发段 **5（环境=预发）** | — | `V-投递-03`／`V-投递-04`／`V-交付-06` | — |
| **E-3 到期清理覆盖**（IN-04） | ①盘点出的全部适用载体（`task.prompt`、`inbound_event`、`pending_action` 及其余）接入实际清理入口与调度；②过期 / 未过期对照、边界时间、重复执行、清理失败后恢复、**旧备份恢复后按原始期限**五组全成立；③不擅改任何保留承诺、不靠删全部历史任务「修复」；④清理不会重开已结束会话或破坏必要交付事实 | **4** + 真库五组对照 | — | `V-保留-01`…`V-保留-20`／`V-投递-06`／`V-投递-07` | — |
| **E-4 调度公平性**（IN-06） | ①真库里构造超过一批的不同用户任务，待重试任务**不持续占满**候选机会；②混合失败、恢复与重启下健康任务继续得到处理且**不重复交付**；③记录恢复后重复次数与等待时延；④**扩大批次上限只作对照、不作为修复** | **4** + 真库多用户 | — | `V-队列-04`／`V-队列-06`／`V-队列-08` | — |
| **E-5 管理回读与 SDK 错误**（IN-09 + IN-10） | ①管理动作已提交、后置回读故障时返回事务内已知决定与终态快照，或明确「已提交、展示待恢复」，**不把已执行说成未执行**；②重复确认 / 取消不重做业务动作；③按固定 SDK `0.2.128` 真实类型保留**有界、脱敏**的 `errors` 与 `api_error_status`，核到下游报告与恢复分支；④`is_error` 现有阻断行为保持——**不得**扩大成「失败必然判成功」 | **4** + 真库故障注入 + 官方数据类离线契约样例；预发段 **5（环境=预发）** | — | `V-管理-01`／`V-管理-30`／`V-管理-31`／`V-管理-32`／`V-执行-04`／`V-执行-06`／`V-执行-17` | — |
| **E-6 版本装配与预发受控验收** | ①候选镜像构建成功、四个 tag 与 digest 冻结回填「批终链」；②预发升级后三容器 `healthy`；③预发**首触冒烟**一条普通问数走通（发现进缺陷账本，不消耗正式候选）；④E-1…E-5 断言按 S-0-5 归类的取证方法逐条取证，用户 / 管理员可见断言**双通道一致**；⑤预发**还原镜像 tag 并停回 `exited`** | **5（已部署，环境=预发）**；写全为 `真实链路 · 预发 · 代理` | — | 同 E-1…E-5 各行 | — |
| **E-7 技术债**（#632 + #598） | **#632**：①方向定档带取舍依据（进程启动耗时实测 / 镜像体积 / 构建复杂度）；②改动落地、本机检查绿、CI 完整门禁绿；③**预发一段空闲期的分项读数**显示每次健康检查 `slab_reclaimable` 增量由 **74,192 B** 显著下降、`anon` 稳定、`memory.events` 三项仍全 0；④读数**只读采集**、与 #600 同一口径（按秒读 cgroup `memory.stat`，增量与健康检查时刻对齐），不进容器、不改配置、不重启。**#598**：⑤两条未合入分支按最新方案处置完毕；⑥约 135 KB 任务专属实验归档清理完成（**不删除用户原生历史**）；⑦卡关闭 | #632 **5（环境=预发）**；#598 **4** | — | 不适用（纯工程，不认领产品断言） | — |
| **E-8 流程两卡**（#641 + #642） | **#641**：①判据两栏表进《浏览器验收》与《协作约定》；②#638 模板改七列并要求完整 digest；③完成标准由执行者回填的约定落文档；④案例库文件建立、BA-001 六条入库；⑤待裁定项按裁定项 5 落地。**#642**：⑥分类表与四环节要求进《协作约定》；⑦只读自检脚本可跑并输出待修正清单（**先只报不拦**）；⑧存量 14 个 Issue 归位、自检输出为空或只剩已知豁免；⑨`长期参考` 与状态标签互斥落文档 | **4** | — | 不适用 | — |

## 批终链（全绿才进下一阶段；三批各走一次）

本机 `check.sh full` 绿（**必须见到 `Ran N tests` 行**；管道 `grep` 滤掉该行不算数；改 `src/` 后 venv 非 editable，不重装会假绿）→ **唯一独立审核者**批量审查（一次；Opus 5，档位比该候选内最高实施档高一级——批次 1 / 2 = `xhigh`，批次 3 = `high`）→ **外审并行**（codex `gpt-6-astra`，批次 1 `xhigh` 绕过面 / 批次 2 `high` 守卫失效面 / 批次 3 `medium` 配置与文档面；agy `gemini-3.8-flash-high` ≤ 3 轮做一般审查与一致性冷读，**额度不足时由 `gpt-6-astra` / `medium` 承接**；**两者结论均须编排者回源坐实后才进缺陷账本**）→ 统一修复包 → 同一审核者定向复核（一次）→ **Fable 5.1 `xhigh` 终审一次**（`S-1F-1` / `S-2F-1` / `S-3F-1`；额度不足如实记「未跑」，不顺延不挪用）→ 冻结候选 → CI 完整门禁 → 合 `main` → `Main Publish` 自动出镜像。

**批次 3 额外接**：镜像 digest 冻结 → 预发升级回读 → 首触冒烟 → 受控验收逐条取证 → 浏览器实测（≤ 2 次）→ **预发还原并停容器**。

冻结后回填：

- 批次 1 候选 SHA `________`；`Epic Full` run `________`；merge commit `________`；`Main Publish` tag `________`
- 批次 2 候选 SHA `________`；`Epic Full` run `________`；merge commit `________`；`Main Publish` tag `________`
- **批次 3（= 本 Trace 的正式验收对象）** 候选 SHA `________`；`Epic Full` run `________`；merge commit `________`
  - 四个镜像 tag `________`；digest（`docker manifest inspect` 解析，带 `@` 前缀）scheduler `________` / migrate `________` / gateway `________` / worker `________`
- **发布：不适用——本 Trace 不发布生产。**预发升级前的原 tag = `20260905-92d016be943e`（还原基准）。

## 未验证层级（收口时如实列）

以下各条在本 Trace **必然**保持未验证，收口时原样保留、**不得改写为已验证**：

- **生产上零验证**——生产全程停在 `v2.2.0`（`20260905-92d016be943e`），本 Trace 的任何修复在生产的实际效果**一次都没测**。预发读数不得用来陈述生产结论。
- **无真实用户结论**——预发受控验收用的是 `Bot-Test` 受控测试身份，不是真实用户；因此不产生 6（真实旅程验证），也不产生 `User-path Delivered`。
- **#632 的生产验收未做**——该卡真正的终点是「生产上线后一段真实空闲期的分项对照读数」，本 Trace 只到预发；卡保持 Open。
- **slab 在内存压力下能否被足额回收仍未实测**（#600 原话）——既不能断言必然 OOM，也不能保证永不 OOM。
- **外部提供方契约未取得**（IN-11 / #616 IF-R5）——MCP 调用模式与版本、401 / 403 的真实原因、逐工具成功 / 空 / 失败样例矩阵**全部仍是未知**；本 Trace 不联系外部团队、不代其新增承诺，`#604` 的加固也因此不做。
- **真实飞书平台的重投时限与次数未独立取证**——E-2 的修复基于官方文档回源与真库回放，不覆盖平台自身的重投行为。
- **旧制品回退未验证**（IN-14 / #617 DB-R3）——本 Trace 只产出一个新制品，**没有**用上一正式版本对新库做关键读写；「结构往返不等于数据无损」这条边界原样保留。
- **真实备份与恢复目标未核定**（IN-15）、**运行负载 / 访问 / 审计 / 容量证据未取得**（IN-16）、**成本计量未做**（IN-18）、**部署启用 / 回退记录未建立**（IN-19）——四项都需要生产面证据。
- **20 个真实业务任务 × 三名用户 × 连续七个工作日未做**（IN-22）——必须在生产，与本 Trace 的「不碰生产」直接冲突。
- **#623 的 22 个 IN 未处理**——逐条去向见[合同](合同.md)覆盖对照表与 S-Z-4 的移交评论；**本 Trace 的 `Complete` 不等于 #623 的 `Complete`。**
- **IN-13 只做了本 Trace 触及的四个接口族**——文档全量收口未做；与 #598 第二批的分工按 #623 第 6 节，两者验收不互代。
- **IN-28 只做了「重构后守卫与变异验红」那半**——冷读可理解性、影响范围、维护耗时抽样未做；#591 既有可读性重构不重做，其 K-8 人类同事抽读仍未做。
- **Fable 5.1 在批次 1 可能一次未跑**——额度 2026-09-06 已用 86%，2026-09-10 07:00 北京才重置；若批次 1 在此之前收口，该批的终审就是缺的，收口时如实标注，**不得**用批次 2 的 Fable 结论回溯覆盖批次 1 的候选。
- **`luna` 系模型在本机 codex 账号不可用**——2026-09-06 实测 `luna` 与 `gpt-6-luna` 均返回 `400`「not supported when using Codex with a ChatGPT account」；一致性冷读的承接方因此是 `gpt-6-astra` / `medium`，不是原建议的 `codex luna max`。
- **浏览器实测请求的交接方式尚未经多次验证**——[#640](https://github.com/Moshuiwang/lingxi/issues/640) 首跑出现过「Issue 已关闭、结果只在评论、正文完成标准复选框全空」；[#641](https://github.com/Moshuiwang/lingxi/issues/641) 第三节的「完成标准由执行者回填」是**新约定**，本 Trace 的两次实测是它的**第二、三次**实践，不能当成已验证机制。元守护 ≤ 30 分钟巡检必看这一项。
- **身份映射的接手方式未定**——映射已在执行机本地完成并 git 忽略（产品负责人 2026-09-06 告知）；「谁在什么条件下能拿到、拿不到时怎么办」由 E-8 的 #641 落地时补齐，补不齐则 #641 不得收口。
- **浏览器实测只覆盖卡片可见性**——按 #641 判据，流程 / 数据 / 状态 / 权限计算全部走真库与 API 只读回源，**没有**经过飞书真实验真。
- **IN-30 条件项**：收口时逐条声明本 Trace 是否改变了消费者部署 / 接管方式、是否升了 SDK、是否引入第二 MCP。**预判三项均不触发**；若批次 2 重构意外触及消费者接管方式，必须补旧执行者与新一轮的互斥验证，不得静默略过。
