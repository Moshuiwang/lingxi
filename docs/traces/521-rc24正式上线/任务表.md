# 任务表(状态即文件当前值,编排者随执行 commit 更新;第一恢复点)

> 执行路由(PM 2026-09-01):`claude_tmux_named_session`,session `TZ-server`,批次 window `rc24-b1`(W0+代码批)/`rc24-b2`(生产与硬切)/`rc24-close`(收口,可由 b2 兼)。编排 opus/max;实施 opus/high;唯一审核 fable/xhigh;元守护=规划者会话(fable)。
> 证据只认 GitHub/仓库留痕;每个 Step 完成时勾选并附指针(Issue 评论 / PR / run id)。

## 块 A(PM 前置输入,一次给齐;不回复按合同 §8 默认)

- [ ] D1 prod 命令通路
- [ ] D2 Bot-Prod 配置齐备
- [ ] D3 prod 七 env 文件与 GHCR 登录
- [ ] D4 正式表/花名册/存量令牌坐标与管理群 id
- [ ] D5 生产机型
- [ ] D6 #411 无异议
- [ ] D7 #497 AWS 三项数据
- [ ] D8 首批真实用户名单
- [ ] D9 prod 管理员名单
- [ ] D10 #498 L3 方案甲
- [ ] D11 #496 502 语义

## Wave 0(今夜,零依赖并行)

- [ ] W0-1 #492 根因判定与修法(G-2:代码可确定→直接修;否则日志路线)——落 #492 评论
- [ ] W0-2a #494 stage 轻量 L4a:写满→unhealthy→告警→清→healthy;≥10 次问数 /tmp 不单调增(双通道,落 #494)
- [ ] W0-2b #496 stage 轻量 L4a:6 并发只读任务,running≤4、零失败零重复(落 #496)
- [ ] W0-2c #499 stage 轻量 L4a:含表格问数一次→docx+降级提示+trace+授权回读(落 #499)
- [ ] W0-3 prod 只读盘点(等 D1)——落 #519
- [ ] W0-4 #263 材料:A-1/A-2 prompt 刷新、映射对账 prompt、G1 复核清单、差集导入 dry-run 复跑——落 #263
- [ ] W0-5 复评修复项归类:P1 全进 S-1;P2 登记/豁免——#520 评论确认账本
- [ ] W0-6 #498 处置:PR #518 已关(规划者);L3 现状与文档边界定稿——落 #498

## Wave 1 代码批(epic/rc24)

- [ ] S-1a bootstrap PR 刷 Story Fast → 建 `epic/rc24`
- [ ] S-1b #520 F1–F11 实施(独立 worktree,串行开 Story PR,合一个更一个)
- [ ] S-1c 本机 full 绿 → fable/xhigh 批终审查(抽 3–5 变异复做)→ 唯一 fixpack → 同一审核者定向复核
- [ ] S-1d 冻结 `Image-Candidate: true` → Epic Full → 合 main → Main Publish → stage 升级回读(tag/digest 回填验收.md)
- [ ] S-2 ◇块 B(PM 晨间):就绪自检 → #492 p2p 一次(照原样重发)→ #493 真人卡片步骤(含 P1-3 文案核)→ 卡关闭

## Wave 2 生产 ☢(块 C,PM 在场,每步单字放行)

- [ ] S-3a [ops #519] preflight:七文件 0600、GHCR、`LINGXI_DOCX_MARKDOWN_CONVERT` 就绪项、`config worker-queue` 五值回读留输出
- [ ] S-3b digest 固定四变量 → `migrate`(lingxi-prod 0→0084)→ 监控 timers 安装 → `admin_registry` 落行(D9)→ 内测名单(D8)
- [ ] S-4a #263 G1 复核:A-1 回执 + 正式表重读 + 映射对账零差异(G-3)
- [ ] S-4b ◇PM 转发 A-2 → 旧定时/旧 Bot/旧 API/旧文件发送全停 + 回执 → [D5 扩容若裁定]
- [ ] S-4c G2:跨满 15 分钟旧周期,正式表逐行不变(行数/键/状态/令牌形状)
- [ ] S-4d #441 差集导入 `--apply` + 幂等复跑
- [ ] S-4e `up -d`(Bot-Prod 长连接=Lingxi)→ 转 healthy → 15 分钟观察 → `images` digest 回读
- [ ] S-4f 首批用户:1 adopt + 1 issue → 开通完成 → 真问数成功(双通道)
- [ ] S-4g `v2.0.0` tag + CHANGELOG 定稿 → #263 关闭 → #519 关闭

## 收口

- [ ] S-5a 逐卡对账:#492 #493 #494 #496 #499 #498 关闭;#411 按 D6;#497 按 D7;#520 关闭
- [ ] S-5b docs 回写:当前能力(生产已部署+边界)、runbook 实录偏差、已知边界(Codex 遗留过重代码、L3 attestation、L1 轻通道)
- [ ] S-5c 环境盘点:tz 残留 worktree(rc23-*/reeval-main)与本地分支;远端 `codex/*`、`claude/rc23-*`、`bootstrap/*` 分支;tz `/tmp` 六个 harness 目录;stage `/tmp` replay_* 与家目录 results.jsonl/summary.json
- [ ] S-5d #502 以 Superseded 关闭(收口评论互链)
- [ ] S-5e 收口评论(UTC+北京、Epic Full 次数/commit 数/代理人次/审核轮次)+ 48h/7d 观察项交元守护+PM
