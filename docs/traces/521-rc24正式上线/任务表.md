# 任务表(状态即文件当前值,编排者随执行 commit 更新;第一恢复点)

> 执行路由(PM 2026-09-01):`claude_tmux_named_session`,session `TZ-server`,批次 window `rc24-b1`(W0+代码批)/`rc24-b2`(生产与硬切)/`rc24-close`(收口,可由 b2 兼)。编排 opus/max;实施 opus/high;唯一审核 fable/xhigh;元守护=规划者会话(fable)。
> 证据只认 GitHub/仓库留痕;每个 Step 完成时勾选并附指针(Issue 评论 / PR / run id)。

## 块 A(PM 前置输入,一次给齐;不回复则取 §8 默认值,见合同.md)

- [x] D1 prod 命令通路 — PM 答方案①：`ssh biplus-prod`（admin-wzp 免密 sudo），留痕 #521 评论 5495233925
- [x] D2 Bot-Prod 配置齐备 — 部分已解（事件订阅已是长连接模式；应用凭据在 prod 旧系统 runtime.env）；剩 scope 对齐并发版、入管理群、S-3.8 一次真人授权点击 — ✅ 09-02：E2/E5 由 PM 截图目视闭合（订阅含 card.action.trigger、可用范围=所有员工，#521）；仅剩 S-3.8 一次授权点击（按 R6 挪至 A-2 停写后）
- [x] D3 prod 七 env 文件与 GHCR — GHCR 包为 public 无需登录；七文件改由 S-3.4 从三处来源生成，PM 不手抄（剩 OAuth Bridge 归属待 PM）
- [x] D4 坐标与管理群 id — 坐标已解（正式表=旧系统 runtime.env 两坐标，同为存量令牌坐标；花名册同 stage）；剩 prod 管理群 chat id — ✅ 管理群 chat id 由 S-3.3 自旧系统 runtime.env 取（PM 09-01 裁定，#519 v2）
- [x] D5 生产机型 — prod 与 stage 同为 t3.medium（2 vCPU / 3.9 GiB，IMDS 实测），不扩容；#263 步骤 3「停机扩容」标 N/A
- [x] D6 #411 无异议 — 待 PM（不可逆删除，元守护不代裁）；不答按默认顺延留痕 — ✅ PM「无异议」，失效时点=本 Trace 收口（#519 v2）
- [x] D7 #497 AWS 三项数据 — 机器内取不到（IAM AccessDenied 实测）；待 PM 控制台，或按默认保持开放缓做 — ✅ PM「没有积分数据，关闭」；#497 已由元守护关闭
- [x] D8 首批真实用户名单 — 待 PM；内测名单未配置=全拒，直接挡住 S-4f — ✅ 首批=正式表全部用户走 adopt；真人样本=PM 本人（D8 终裁，#521）
- [x] D9 prod 管理员名单 — 按默认：与 stage 同一位（stage `admin_registry` 仅 1 行，三角色全开），open_id 在 Bot-Prod 下重解析
- [x] D10 #498 L3 方案甲 — 按甲执行（落 F3）
- [x] D11 #496 502 语义 — 维持「诚实失败」，登记已知边界

## Wave 0(今夜,零依赖并行)

- [x] W0-1 #492 根因判定与修法 — 18 形态实测：类别可确定（邮箱段被拆成 ≥2 段），具体形态不可确定 → 取证日志与归一同 PR；落 #492 评论 5495468501
- [x] W0-2a #494 stage 轻量 L4a — **PASS**（1 轮）：写满→79 秒转 unhealthy、Health.Log 逐字含「临时目录可用空间 0.0MiB 低于阈值 25.6MiB」、107 秒告警落管理群并由平台 API 回读消息 id→删 filler 回 healthy；11 次问数后 29.4MiB ≤ 128MiB。落 #494 评论 5495732898
- [x] W0-2b #496 stage 轻量 L4a — **PASS**（1 轮）：6 并发、309 个 1 秒采样中 running>4 样本数=0（峰值 4）、第 5/6 条排队后成功、6/6 succeeded、133 行投递事件 idempotency_key 全不同=零重复。502 腿见 `合同.md` §3 不做清单（不建 harness 工程），如实登记未做。落 #496 评论 5495733440
- [x] W0-2c #499 stage 轻量 L4a — **PASS**（1 轮）：四项全绿（docx 链接+「格式已简化」同条消息、正文含拍平表格、/admin trace 读到降级原因、平台回读 full_access 非孤儿）。落 #499 评论 5495734072
- [x] W0-3 prod 只读盘点 — 元守护已做，落 #519 与 #521 评论 5495233925；**关键缺件：prod 未装 Docker** → 新增 S-3.0
- [x] W0-4 #263 材料 — 五项齐，落 #263 评论 5495411381；G1 演练实测 rows=26 与 08-30 一致；**差集导入按现状计划 0 条，需 PM 裁定**
- [x] W0-5 复评修复项归类 — P1 全进 S-1、P2 逐条给去向；落 #520 评论 5495201042
- [x] W0-6 #498 处置 — PR #518 已关；L3 现状与文档边界定稿；落 #498 评论 5495209639

## Wave 1 代码批(epic/rc24)

- [x] S-1a bootstrap 刷 Story Fast → `epic/rc24` 建于 `5e633d1` — 四次尝试，真因：#523 基线过老被判 l3（正确的 fail-closed）；#524/#525 基线 `epic/rc23` 与 main 冲突→PR 不可合并→**工作流根本不触发**；#526 换 `epic/rc22-polish`（完全包含于 main 且权限 TOML 与 main 逐字相同）→ Story Fast 通过（run 33520181039）
- [x] S-1b #520 F1–F12 实施 — 5 个 Story PR 串行开合，**各只跑一轮 CI、零返工**：#527(F1/F2/F3)→#528(F4)→#529(F6/F7)→#530(F8–F12)→#531(F5)。四条并行分支零文件重叠（uniq -d 为空），合并面演练与实合均干净
- [x] S-1c 本机 full 绿（EXIT=0，含 29 条迁移真往返、工作树洁净、临时容器自清）→ fable/xhigh 批终审查**零 P0/P1**、三条 P2 登记级（F3 十场景矩阵、F4 二十对抗输入、变异独立复做 6 项对上）→ **按 P2 豁免不做 fixpack** → 定向复核针对本次 Trace 文档 delta。审核预算 1/3
- [x] S-1d 冻结 `Image-Candidate: true` → Epic Full → 合 main → Main Publish → stage 升级回读（tag/digest 回填验收.md）——**本提交携带冻结标记**，其余待执行 — ✅ 完成：Epic Full 33534816370 / Main Publish 33536131380 / tag `20260901-eea6b1215cb9` / stage 升级回读 healthy（#521 评论 5497734887）
- [x] S-2 ◇块 B(PM 晨间):就绪自检 → #492 p2p 一次(照原样重发)→ #493 真人卡片步骤(含 P1-3 文案核)→ 卡关闭 — ✅ 09-02 块 B 完成：#492/#493 均**等级 6 关闭**（#492 评论 5503077498 / #493 评论 5503344926；两次真实双击均被 0084 CAS 挡住）

## Wave 2 生产 ☢(块 C,PM 在场,每步单字放行)

- [x] S-3a [ops #519] preflight — 七文件 0600 属主部署用户 ✓；GHCR 包 public 匿名可拉（D-1，免登录）✓；`LINGXI_DOCX_MARKDOWN_CONVERT` **刻意不设 = 开启**（#499 stage L4a 验过的路径）；`config worker-queue` 五值回读（并发 4 / CPU 1.5 / memory `"2147483648"` / pids 512 / tmpfs 256m）+ scheduler/gateway memory `"536870912"` 均已留输出；E1/E3/E4/E7 探针通过、E2/E5 PM 目视闭合、E6 按预期 403 — #519 评论 5503551258 / 5504254972
- [x] S-3b digest 四变量固定并渲染回读带 `@sha256:` → `migrate` 空库 0→`0084_management_card_state_cas (head)`（29 版本无报错）→ 三个监控 timer（host-monitor/resource-sample/db-business-sample）安装并各跑通一轮（`User=` 改为部署用户，D-6）→ `admin_registry` 三角色 True/True/True active（D9，命令行零 open_id）→ 内测名单写 `.env.prod.scheduler` 与 `.env.prod.gateway` 两份逐字一致，26 邮箱解析 25/26（未解析者为正式表内测试行，非真人）— #519 评论 5503551258 / 5504758707 / 5504798460
- [x] S-3 可逆段（rc24-b2，09-02 上午，PM「批准」2–7）：S-3.0 Docker 25.0.16 + compose v5.2.0 + 入组（R9）/ S-3.1a swap 2G（R11）/ S-3.2+2a checkout `eea6b12` + py3.12 venv / S-3.4a runtime-config + system_prompt.md / S-3.4 七 env 5/7 / S-3.5 渲染（字节数判据全过）/ S-3.6 migrate 0→0084 / S-3.7a 银河批次 `gib_wwgLZPUHIADexrXo52ym3A`（R7 沿用）— #519 评论 5503461062 / 5503551258 / 5503562126。**S-3.3 待 `sudo grep` 规则**；S-3.8 按 R6 挪至 A-2 停写后；S-3.9–3.12 于 G1/G2 后
- [x] S-4a #263 G1 **通过**（七项核对全过）：A-1 回执逐字留痕（旧制品 1.40=基线、写入键=规范化邮箱、`full_set_reconcile` 会删未知行 → 单向门理由坐实、全部写入方枚举含 7 个手工脚本）；正式表重读 rows 26 / dup 0 / 全 approved，`rows_digest` 与 09-01 演练逐字节相同；映射对账 **D-B（更窄）为空**，三条登记（D-A 财务 +5 内容 +2 有意更宽、公司键 40–43 无对应归 R1、首批 4 人花名册重复）— #263 评论 5504293835
- [x] S-4b 停写完成 **04:27:50Z**（A-2 v3 五条回执逐字留痕，编排者独立核实四单元 inactive/disabled、旧用户进程 0、容器 0）；`g1′` 停写后基线 04:29:27Z 与 `g1` diff 为空。D5 扩容 N/A（prod 与 stage 同机型）。**登记为观察期风险**：7 个手工脚本按对方「承诺不运行」（选项 b）处置，属承诺非技术锁定 — #263 评论 5504396437
- [x] S-4c G2 **通过** 04:50:12Z（观察窗按全部「能写表」写入方的下次触发取最大值 +5 分钟，跨 04:30 / 04:45 两个旧 timer 整点边界，距停写 22 分钟）：rows 26 / distinct 26 / dup 0 / empty_key 0 / empty_token 0 / key_ne_email 0 / 全 approved，`rows_digest` 与 `g1′` 相同，`diff g1′ g2` **为空** — #263 评论 5504550667
- [ ] S-4d #441 差集导入 — **按 PM R1③ 顺延至观察期**（首发未执行）：观察期内真实导出 → dry-run 人工确认计划无 `metric=*`、无映射外公司（对抗审查 P-2）→ `--apply` → 幂等复跑。#263 完成标准「差集导入可审计可重跑」随之顺延，关卡时如实标注 — #521 评论 5504299875
- [x] S-4e `--profile mvp up -d` 04:50:57Z 发出 → 04:51:42Z 三服务 healthy、RestartCount 0，Bot-Prod 长连接自 **04:51:07Z** 起由 Lingxi gateway 持有（旧 bot 已停，无双连接）→ S-4e′ 三份基础数据非空（银河批次 1 / 花名册 1223 行 / 组织快照 711 行，与 stage 最新一轮相同）→ S-4e″ 15 分钟观察通过：三服务运行镜像 `Config.Image` 与 `RepoDigests` 与固定 digest **逐字节一致**，内存 187/46/270 MiB 均在限内，worker `/tmp` 188K（#494 预算内），`system_prompt_digest=2272bd4d40ae` 与 stage 一致、降级计数 0 — #519 评论 5504569455 / 5504798460 / 5504920086
- [x] S-4f 首批用户 **adopt 路径通过（等级 6，双通道）** 05:23–05:27Z：真人样本 = PM 本人（在正式表内 → adopt）；用户可见「已收到，正在核对」→「开通完成…当前可用范围」→ 真问数「查询完成 · 37 秒」表格+口径+环比；服务端 `onboarding.stock_token_adopted status_approved=True`（令牌沿用未重签）→ 发布 v1 → `onboarding.result state=completed`，worker 终态 `success` / `denied_count=0` / `system_prompt_digest` 与 stage 一致。**正式表首写只改一行**（其余 25 行逐字节不变，令牌密文哈希不变）。**issue（新用户）路径如实标未验证**，登记为「待 48h 观察期内首个自然出场用户」。观察项：MCP 侧对权限收窄未即时生效（不阻塞，需复测）— #263 评论 5504918339
- [x] S-4g `v2.0.0` annotated tag 已推送（05:38Z），tag 对象 `d95430f2…` → 提交 `eea6b12`（R10：指向制品树而非 main tip）；CHANGELOG 2.0.0 已在该提交中定稿为 2026-09-02。**M1/M2/M3 均已达成**。#263 / #519 待 MCP 收窄复测结果后由编排者关闭 — #521 评论 5504952336

## 收口

- [x] S-5a 逐卡对账 — #492 / #493 / #494 / #496 / #499 / #498 / #520 均已关闭（现值见 `验收.md`「逐卡现值」）；#497 已按 D7 关闭；#411 按 D6 待 S-5c 执行删除后关闭；#263 / #519 保持 open，待观察期复测结果
- [x] S-5b docs 回写:当前能力(生产已部署+边界)、runbook 实录偏差、已知边界(Codex 遗留过重代码、L3 attestation、L1 轻通道) — ✅ PR #536（main `17004e3`）：当前能力生产已部署条目、runbook「十一、实录偏差」、README D-1 更正、已知边界六项、任务表/验收终态
- [x] S-5c 环境盘点:tz 残留 worktree(rc23-*/reeval-main)与本地分支;远端 `codex/*`、`claude/rc23-*`、`bootstrap/*` 分支;tz `/tmp` 六个 harness 目录;stage `/tmp` replay_* 与家目录 results.jsonl/summary.json — ✅ tz 16 个 rc23 worktree 已删（`git worktree list` 仅主树）；远端 25 个 `codex/*`/`claude/rc23-*`/`claude/rc24-*`/`bootstrap/*` 已删（保留 `codex/rc23-fix498-owner-attestation`）；stage `lingxi-test-db` 容器+卷与 `~/lingxi-db-rollback/` 已删（#411 D6）、W0-2 测试行清零、`~/tmp-rc24-w04/` 已删；prod 临时文件已删；**保留**：stage `.env.stage.before-eea6b12`（48h 后清）、tz `.claude/settings.local.json` 20 条 prod 规则（由 PM 裁剪）、prod `lingxi-ops-venv`/`~/backups/`（长期）
- [x] S-5d #502 已以 Superseded 关闭并与本 Trace 互链
- [x] S-5e 收口评论(UTC+北京、Epic Full 次数/commit 数/代理人次/审核轮次)+ 48h/7d 观察项交元守护+PM — ✅ 收口评论落 #521 末条（UTC+北京时间线、预算实数、观察项）；Trace 以 Closed/Complete 关闭
