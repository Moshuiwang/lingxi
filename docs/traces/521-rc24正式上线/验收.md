# 验收(逐卡关卡=issue 可观察完成标准;L4a 一律先走最便宜的真实用户路径;双通道;证据落 GitHub)

> 证据等级目标:代码批 L2(Epic Full)+L3(fable 审查);stage 卡 L4a(受控测试身份非生产,全局 6 须连写「受控测试身份」);生产 = 全局 5「已部署」+ 6「首批真实用户旅程」。每项完成把「现值」改成实际等级并附指针。

## 逐卡关卡

- **#492**:真实 p2p 形态(PM 照原样重发一次)解析成功并执行 `audit`;三种相邻形态夹具 + 变异红;显示≠目标拒绝;纯文本/非邮箱@/真未知命令行为不变;回复含「实际收到 N 段」;`admin.command.unknown` 审计含 `token_count/token_shapes`(+仅 `/admin` 前缀的 `raw_admin_text`)。现值:—
- **#493**:真人旅程——`/admin user <邮箱>`→管理卡→职位+「全部(显示 43)」+原因→补充授权→确认卡→原卡就地刷新「已生效」;对已 suspend 目标补授时文案为「已停用,不会下发;恢复后次日批生效」(F5);服务端缺范围拒绝有否定用例(F6)。现值:—
**现值更新见文末「批终链现状」。**

- **#494**:stage 容器内写满 tmpfs→≤3 探测周期 `unhealthy`、Health.Log 含「临时目录可用空间」、管理群告警→删 filler→`healthy`+恢复通知;≥10 次问数(或 60 分钟浸泡)后 `du -sb /tmp/.claude/projects` ≤128MiB 且日志 `worker.session_transcripts_reclaimed`、无 `over_budget`;`docker inspect` 资源合同回读。现值:**L4a 通过,已关闭**(生产首发 worker `/tmp` 实测 188K,在 128MiB 预算内)
- **#496**:stage 6 并发只读任务:同一时刻 running≤4,第 5/6 排队后完成,零失败零重复投递;(可选)受控 502→`worker.mcp_server_unavailable`+`failure_code=mcp_bad_gateway`+`/admin trace` 可读+用户只收一条。现值:**L4a 通过,已关闭**(502 腿维持代码腿证据,如实标 stage 未验)
- **#499**:stage 含表格问数一次→docx 链接+「格式已简化」通知→正文含拍平表格→`/admin trace` 读到降级原因→平台 API 回读文档已授权(非孤儿)。现值:**L4a 通过,已关闭**
- **#498**:F1(L1 键集合检查,E1/E2 场景红)、F2(L1 回退 full)、F3(L3 方案甲:非空权限变更 PR 出 diff 不因缺注册而红)合入;门禁文档已知边界登记;PR #518 已关。现值:**L3,已关闭**
- **#483**:F7 真库并发用例合入(去 `FOR UPDATE` 必红)。现值:关闭维持(L3)
- **#495**:关闭维持(worker 日志证据)。现值:L4a
- **#520**:F1–F11 逐项「已改/未改+原因」+ 变异红绿写 PR 正文;本机 full 绿;Epic Full 绿。现值:**已关闭**(F1–F12 全部合入,#527–#531)
- **#263**:G1(A-1 回执+正式表重读+映射对账零差异)→停写回执(定时/Bot/API/文件发送)→G2 跨 15 分钟逐行不变→差集导入 `--apply` 幂等→Lingxi 唯一写入方;首批 adopt/issue/真问数三结果双通道。现值:**待关(open)**——G1/停写/G2/唯一写入方/adopt 真问数**均已达成**(#263 评论 5504293835 / 5504396437 / 5504550667 / 5504918339);**差集导入按 PM R1③ 顺延至观察期**、**issue 路径未验证**,两项在关卡评论里如实标注
- **#519 [ops]**:runbook §2.2–§2.5、§三 digest 四变量一致、§四单实例、§七 15 分钟观察两条判据、监控 timers 安装、`v2.0.0` tag 指向上线提交。现值:**待关(open)**——所列各项**全部达成**(#519 评论 5503551258 / 5504254972 / 5504569455 / 5504798460 / 5504920086;`v2.0.0` → `eea6b12`,#521 评论 5504952336);runbook 实录偏差已回写 `deploy/生产部署runbook.md` 十一节;`monitoring-push` 与日志留存两项如实登记为观察期项
- **#411**:D6 无异议→删容器+dump→回帖盘点;否则顺延留痕。现值:**待关(open)**,删除动作留 S-5c 环境盘点执行
- **#497**:D7 数据到→结论落卡关闭;否则保持开放缓做。现值:**已关闭**(PM「没有积分数据」)

## 批终链(全绿才进生产)

本机 full 绿 → fable/xhigh 批量审查(一次)→ 唯一 fixpack → 同一审核者复核(一次)→ 冻结 `Image-Candidate: true` → Epic Full 绿 → 合 main → Main Publish → stage 升级回读 → 块 B 真人 → 上线制品 tag/digest 回填:

- 上线 tag:`20260901-eea6b1215cb9`;四 digest 与两条 run id 见文末「上线制品回填」表(已填齐)。
- **生产段现状(2026-09-02 收口)**:全局 **5「已部署」**(`biplus-prod` 三服务 healthy on 上线 tag,运行 digest 与固定值逐字节一致,`v2.0.0` → `eea6b12`)+ 全局 **6「真实用户旅程」**(首批 **adopt** 路径,真人样本=PM 本人,开通完成卡 + 真问数 37 秒双通道);**issue(新用户)路径未验证**,登记为「待 48h 观察期内首个自然出场用户」;**差集导入按 R1③ 顺延至观察期**,首发未执行。

## 留证

逐卡证据落各卡评论;生产时间线落 #519/#263;收口评论落 #521(UTC+北京、Epic Full 次数/commit 数/代理人次/审核轮次、预算消耗、残留盘点);任务表随执行 commit。


---

## 批终链现状（2026-09-02 凌晨,编排者随执行回填）

### 逐卡现值

| 卡 | 现值 | 证据 |
| --- | --- | --- |
| #492 | 代码 L3(变异 6 项全红)+ 批终 fable 审查 20 项对抗输入零突破 | **等级 6 已关闭**（09-02 真人 p2p 5 条命令双通道，#492 评论 5503077498） |
| #493 | 代码 L3(F5 变异 8 项全红、F6 同删守卫红 6) | **等级 6 已关闭**（09-02 真人卡片全旅程 + F5 双通道 + 双击 CAS 实证，#493 评论 5503344926） |
| #494 | **L4a 通过,已关闭**(受控测试身份,非生产):79 秒转 unhealthy、107 秒告警落管理群、回收 29.4MiB ≤ 128MiB | #494 评论 5495732898;生产首发 worker `/tmp` 188K 复核在预算内 |
| #496 | **L4a 通过,已关闭**(受控测试身份,非生产):running>4 样本数 0、零重复投递 | #496 评论 5495733440；502 腿维持代码腿证据,如实标 stage 未验 |
| #499 | **L4a 通过,已关闭**(受控测试身份,非生产):四项全绿 | #499 评论 5495734072 |
| #498 | **L3,已关闭**:F1/F2/F3 合入,十场景矩阵实测降级边界无溢出 | #520 评论 5497347528 |
| #520 | **已关闭**:F1–F12 全部合入,PR 正文逐条已改/未改 + 变异红绿 | #527/#528/#529/#530/#531 |
| #263 | **待关(open)**:G1/停写/G2/唯一写入方/adopt 真问数已达成;差集导入顺延、issue 路径未验证 | #263 评论 5504293835 / 5504396437 / 5504550667 / 5504918339 |
| #519 | **待关(open)**:runbook 所列各项全部达成,`v2.0.0` → `eea6b12` | #519 评论 5503551258 / 5504254972 / 5504569455 / 5504798460 / 5504920086；#521 评论 5504952336 |
| #411 / #497 | #497 已关闭(D7「没有积分数据」);#411 待 S-5c 执行删除后关闭 | #519 评论 5495610130（失效时点=本 Trace 收口） |
| #483 / #495 | 关闭维持;F7 去 `FOR UPDATE` 真库变异红 1(红的正是新并发用例) | — |

### 审核与门禁消耗
本机 `check.sh full` **1/1**(EXIT=0);fable/xhigh 批终审查 **1 轮**,零 P0/P1,三条 P2 按 PM 2026-08-14 豁免登记不修;**不做 fixpack**;定向复核 1 轮针对本次 Trace 文档 delta。审核预算 2/3,余 1 轮留给上线前 runbook/ops 卡走查。

### ⚠️ 验收面收窄(PM D8 终裁带来,如实登记)
合同 §1 Delivered 原写「首批真实用户(**≥1 存量 adopt + ≥1 新用户 issue**)走到开通完成」。PM 最终裁定真人样本 = 产品负责人本人,路径由 S-4a 的 G1 正式表重读决定(在表→adopt;不在→issue)。因此**首发只会验证其中一条路径**,另一条登记为「待 48h 观察期内首个自然出场用户」,双通道证据届时补录 #263/#519。

**首发收口时对未走通的那条路径一律如实标注「未验证」,不得按已达成计。** 这是 PM 裁定带来的验收面收窄,不是执行侧降级。

### 上线制品回填(待 Main Publish 后填)
**已回填(S-1d 完成,2026-09-02 01:2x 北京 / 2026-09-01 17:2x UTC)**:

| 项 | 值 |
| --- | --- |
| 上线 tag | `20260901-eea6b1215cb9` |
| main 提交 | `eea6b12`(tree `702a7cb8a8795cdf1ae419890df956a261067642`,与冻结候选 `epic/rc24 @ 1993969` 的 tree **逐字节一致**) |
| scheduler digest | `sha256:a77f112baeb2bcb3c18863357676ab3afda31126fc749c834b6a38a490fcc2f6` |
| gateway digest | `sha256:ec611fa2b1dd414b1ebdeb584ca93bd3f03043f23be90386e8042c7138fdddae` |
| worker digest | `sha256:0abdf2b77458270dfbbcc4bd3d9cab39e4a53bc0f46a4aabea24d61b40a912f1` |
| migrate digest | `sha256:00558b23f1d16510b611199c950c798508d1df4aa0d5b1a0d957eafa0428e161` |
| Epic Full run | `33534816370`(10 pass / 1 skip;gate 6m42s 真库、image 4m31s 产候选证明) |
| Main Publish run | `33536131380`(success) |

**stage 升级回读(2026-09-02 01:2x 北京)**:三服务 `healthy` on `20260901-eea6b1215cb9`,`RestartCount` 均 0;`docker image inspect` 的 `RepoDigests` 与上表 scheduler/gateway/worker 三项**逐字节一致**(migrate 为一次性 job 镜像,无常驻容器,仅在 `.env.stage` 固定)。升级前 `.env.stage` 已备份为 `.env.stage.before-eea6b12`,回滚=拷回并 `up -d`。

**本批零迁移改动**(`git diff --name-only 8d25f23..eea6b12 -- migrations/` 计数 0),故 stage 升级未跑 `migrate`。
