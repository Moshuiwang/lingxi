# 验收(逐卡关卡=issue 可观察完成标准;L4a 一律先走最便宜的真实用户路径;双通道;证据落 GitHub)

> 证据等级目标:代码批 L2(Epic Full)+L3(fable 审查);stage 卡 L4a(受控测试身份非生产,全局 6 须连写「受控测试身份」);生产 = 全局 5「已部署」+ 6「首批真实用户旅程」。每项完成把「现值」改成实际等级并附指针。

## 逐卡关卡

- **#492**:真实 p2p 形态(PM 照原样重发一次)解析成功并执行 `audit`;三种相邻形态夹具 + 变异红;显示≠目标拒绝;纯文本/非邮箱@/真未知命令行为不变;回复含「实际收到 N 段」;`admin.command.unknown` 审计含 `token_count/token_shapes`(+仅 `/admin` 前缀的 `raw_admin_text`)。现值:—
- **#493**:真人旅程——`/admin user <邮箱>`→管理卡→职位+「全部(显示 43)」+原因→补充授权→确认卡→原卡就地刷新「已生效」;对已 suspend 目标补授时文案为「已停用,不会下发;恢复后次日批生效」(F5);服务端缺范围拒绝有否定用例(F6)。现值:—
**现值更新见文末「批终链现状」。**

- **#494**:stage 容器内写满 tmpfs→≤3 探测周期 `unhealthy`、Health.Log 含「临时目录可用空间」、管理群告警→删 filler→`healthy`+恢复通知;≥10 次问数(或 60 分钟浸泡)后 `du -sb /tmp/.claude/projects` ≤128MiB 且日志 `worker.session_transcripts_reclaimed`、无 `over_budget`;`docker inspect` 资源合同回读。现值:代码 L3(真实 tmpfs 探针实证)
- **#496**:stage 6 并发只读任务:同一时刻 running≤4,第 5/6 排队后完成,零失败零重复投递;(可选)受控 502→`worker.mcp_server_unavailable`+`failure_code=mcp_bad_gateway`+`/admin trace` 可读+用户只收一条。现值:代码 L2/L3
- **#499**:stage 含表格问数一次→docx 链接+「格式已简化」通知→正文含拍平表格→`/admin trace` 读到降级原因→平台 API 回读文档已授权(非孤儿)。现值:代码 L2(Epic Full)+变异 5/6 红
- **#498**:F1(L1 键集合检查,E1/E2 场景红)、F2(L1 回退 full)、F3(L3 方案甲:非空权限变更 PR 出 diff 不因缺注册而红)合入;门禁文档已知边界登记;PR #518 已关。现值:—
- **#483**:F7 真库并发用例合入(去 `FOR UPDATE` 必红)。现值:关闭维持(L3)
- **#495**:关闭维持(worker 日志证据)。现值:L4a
- **#520**:F1–F11 逐项「已改/未改+原因」+ 变异红绿写 PR 正文;本机 full 绿;Epic Full 绿。现值:—
- **#263**:G1(A-1 回执+正式表重读+映射对账零差异)→停写回执(定时/Bot/API/文件发送)→G2 跨 15 分钟逐行不变→差集导入 `--apply` 幂等→Lingxi 唯一写入方;首批 adopt/issue/真问数三结果双通道。现值:Pre-ready
- **#519 [ops]**:runbook §2.2–§2.5、§三 digest 四变量一致、§四单实例、§七 15 分钟观察两条判据、监控 timers 安装、`v2.0.0` tag 指向上线提交。现值:—
- **#411**:D6 无异议→删容器+dump→回帖盘点;否则顺延留痕。现值:—
- **#497**:D7 数据到→结论落卡关闭;否则保持开放缓做。现值:—

## 批终链(全绿才进生产)

本机 full 绿 → fable/xhigh 批量审查(一次)→ 唯一 fixpack → 同一审核者复核(一次)→ 冻结 `Image-Candidate: true` → Epic Full 绿 → 合 main → Main Publish → stage 升级回读 → 块 B 真人 → 上线制品 tag/digest 回填:

- 上线 tag:`________`;四 digest:scheduler `________` / gateway `________` / worker `________` / migrate `________`;Epic Full run:`________`;Main Publish run:`________`。

## 留证

逐卡证据落各卡评论;生产时间线落 #519/#263;收口评论落 #521(UTC+北京、Epic Full 次数/commit 数/代理人次/审核轮次、预算消耗、残留盘点);任务表随执行 commit。


---

## 批终链现状（2026-09-02 凌晨,编排者随执行回填）

### 逐卡现值

| 卡 | 现值 | 证据 |
| --- | --- | --- |
| #492 | 代码 L3(变异 6 项全红)+ 批终 fable 审查 20 项对抗输入零突破 | 等级 6 待块 B 真人 p2p |
| #493 | 代码 L3(F5 变异 8 项全红、F6 同删守卫红 6) | 等级 6 待块 B 真人卡片 |
| #494 | **L4a 通过**(受控测试身份,非生产):79 秒转 unhealthy、107 秒告警落管理群、回收 29.4MiB ≤ 128MiB | #494 评论 5495732898 |
| #496 | **L4a 通过**(受控测试身份,非生产):running>4 样本数 0、零重复投递 | #496 评论 5495733440；502 腿维持代码腿证据,如实标 stage 未验 |
| #499 | **L4a 通过**(受控测试身份,非生产):四项全绿 | #499 评论 5495734072 |
| #498 | F1/F2/F3 合入,L3 十场景矩阵实测降级边界无溢出 | #520 评论 5497347528 |
| #520 | F1–F12 全部合入,PR 正文逐条已改/未改 + 变异红绿 | #527/#528/#529/#530/#531 |
| #483 / #495 | 关闭维持;F7 去 `FOR UPDATE` 真库变异红 1(红的正是新并发用例) | — |

### 审核与门禁消耗
本机 `check.sh full` **1/1**(EXIT=0);fable/xhigh 批终审查 **1 轮**,零 P0/P1,三条 P2 按 PM 2026-08-14 豁免登记不修;**不做 fixpack**;定向复核 1 轮针对本次 Trace 文档 delta。审核预算 2/3,余 1 轮留给上线前 runbook/ops 卡走查。

### ⚠️ 验收面收窄(PM D8 终裁带来,如实登记)
合同 §1 Delivered 原写「首批真实用户(**≥1 存量 adopt + ≥1 新用户 issue**)走到开通完成」。PM 最终裁定真人样本 = 产品负责人本人,路径由 S-4a 的 G1 正式表重读决定(在表→adopt;不在→issue)。因此**首发只会验证其中一条路径**,另一条登记为「待 48h 观察期内首个自然出场用户」,双通道证据届时补录 #263/#519。

**首发收口时对未走通的那条路径一律如实标注「未验证」,不得按已达成计。** 这是 PM 裁定带来的验收面收窄,不是执行侧降级。

### 上线制品回填(待 Main Publish 后填)
上线 tag:`________`;四 digest:scheduler `________` / gateway `________` / worker `________` / migrate `________`;Epic Full run:`________`;Main Publish run:`________`。
**这四项在本提交时点尚不存在**(制品由本候选合入 main 后的 Main Publish 产出),由 S-1d 完成后另行回填,不预填、不猜。
