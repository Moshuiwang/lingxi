# 开工合同:rc24 正式上线批(Trace [#521](https://github.com/Moshuiwang/lingxi/issues/521))

> 方法版本:#147 **v16**。合同版本:**v1**。批准依据=产品负责人 2026-09-01 本机 Claude Code 会话指令(原话要点:「停止 Codex,由 Claude Code 接手」「重新评估 8/31 17:26 之后的所有工作」「重新新建 trace……主要目的是实现明天的 prod 部署,包括切换 bot-prod,也就是我们真实的业务上线。我希望能清空所有 issue,但如果有些 issue 不影响上线,可以缓做」「opus high 施工、fable xhigh review、opus max 编排、可以使用外部 codex 审核」「使用 tmux window 进行编排者的交接,你自己在交接后转变为元守护」「预算……可以重新开始设计」「关于你需要我裁定的内容,我的建议是你自定」)。**本 PR 合并即批准 v1**。
> 规划者=本机 Claude Code 会话 `9061204b-95af-4e70-9f29-d3e6c215d0e9`(fable);停止点=三件套合并、#521 瘦指针就位、继任编排者在 tmux window 完成接管登记;随后本会话转**元守护**,不实施、不持合并权。
> **合同只认两种来源**:PM 裁定与输入 Issue 的可观察完成标准。Codex 编排期(08-31 17:26→09-01 20:16 北京)自设的一次性额度、重型 harness、审核轮次一律不继承;其遗留代码处置见 §3/§7。

## 1. 目标

- **用户结果一句话**:2026-09-02(北京)完成 Lingxi 2.0 真实业务上线——`biplus-prod` 跑冻结制品、正式权限表由 Lingxi 独占写入(#263 硬切)、`Bot-Prod` 成为唯一正式飞书入口;首批真实用户(≥1 存量 adopt + ≥1 新用户 issue)走到「开通完成」且至少一人从 Bot-Prod 真问数成功。
- **里程碑**:M1 前置齐备(W0 全绿 + PM 前置输入到齐);M2 rc24 代码批合 main、发布、stage 升级,#492 p2p 与 #493 真人验收通过;M3 生产部署 + 硬切完成、15 分钟观察 healthy、首批用户旅程通过、`v2.0.0` tag 打出;M4 收口(现存 open issue 全部终态、环境零残留、文档回写、#502 收口)。
- **完成定义(Delivered)**:M1–M4 全部达成;#263 关闭;新建 `[ops]` 生产部署卡关闭;#492/#493/#494/#496/#499 关闭;#498/#497/#411 各按 §7 去向终态;#502 以 `Superseded` 关闭;#147 保持开放(方法正文,永不关);48h/7d 观察期由元守护 + PM 承接(不在 Delivered 定义内,登记为尾项)。

## 2. 授权终点

- **可自动执行**:合并批次 PR(含 `epic/rc24` Story PR 串行合并与 Epic PR 合 main,沿 `gh pr ready/merge` 常设授权);部署与升级 `biai-stage`;Bot-Test 真实收发与受控注入 L4a;关闭 issue 与回写文档;新建 `[ops]` 生产部署 Issue;tz/stage 环境清理(含 **#411 旧库容器与 dump 删除——仅在 PM 前置输入 D6 回「无异议」后**);`biplus-prod` **只读**核实(在 D1 通路就位后)。
- **逐步授权(PM 在场窗口,每步单字「批」后执行,一步一留痕)**:生产 env 七文件核对(凭据由 PM 放置,代理零接触明文)、GHCR 登录、digest 固定、`migrate`、监控 timers 安装、`up -d`(=Bot-Prod 长连接启动,**只能在 #263 G2 通过之后**)、`v2.0.0` tag 推送、差集导入 `--apply`。
- **PM 亲自执行**:旧系统停写/停 Bot/停 API 的协调 prompt 转发(biai-agent 侧 agent 执行);AWS 控制台动作(若 D5 裁定扩容);Bot-Prod 应用凭据与 scope 配置;首批用户的真实飞书旅程。
- **显式除外**:GitHub ruleset / CODEOWNERS / 分支保护变更(#518 不合);新增飞书 scope 以外的任何权限扩大;凭据轮换;#497 CPU 优化;删除任何用户数据(#411 例外除外);在生产现场改代码或构建。
- **执行通路**:见 D1(prod 命令由谁、从哪台机器执行——待 PM 前置裁定)。
- **常设授权引用**:`git-claude-tz push`、gh 机器身份、`gh pr ready/merge`(PM 2026-08-06「以后不要再问」)、bootstrap PR 先例、P2 豁免(PM 2026-08-14)、stage 全权限(PM 2026-08-30,prod 除外)。
- **中途裁定**:攒清单一次提交;单字有效留痕 #521;临时授权带失效时点(默认本批收口)。

## 3. 不做清单

- 不做 CD 自动化、不做灰度(runbook §五)、不做 OS 级密钥管理迁移;不改 GitHub 仓库治理(ruleset/CODEOWNERS)。
- **Codex 遗留处置(规划者裁定)**:已合 main 且复评无 P0/P1 的代码**保留**(过重之处登记代码旁「已知边界」,上线后再简化,不开新 issue);PR #518 **关闭不合**(留分支 `codex/rc23-fix498-owner-attestation` 供参考);tz `/tmp` 六个 harness 不采用(已归档 `~/project/lingxi-archive/codex-rc23-tmp-evidence-20260901.tgz`),收口时删;`codex/*` 远端分支收口时清。
- #498 L3「OWNER attestation」不做(见 §7);L2 提示词档不做;#499 完整表格转换不做;#496 不无限重试;#493 不新增管理员角色;不做用户记忆/新功能。
- 新发现默认只登记不立项——**主线灯塔:不做它,明天能不能上线?**;疑问句只答不动手。

## 4. 验收标准

- **每卡关卡**=其 issue 可观察完成标准,细则见 [验收.md](验收.md);L4a 一律先走**最便宜的真实用户路径**(Bot-Test 私聊/受控注入),不建 harness 工程;每卡 L4a 最多两轮,第二轮仍失败即归 blocker 上报,不堆脚本。
- **生产上线关卡**(证据等级:全局 5「已部署」+ 6「首批真实用户旅程」):runbook §二~§七逐条;#263 G1/G2 两道门;首批用户双通道(用户可见 + 平台 API/结构化日志);`v2.0.0` tag 指向 main 上线提交。
- 批终链:本机 full 绿 → **fable xhigh** 唯一审核者批量审查(一次)→ 统一修复包 → 同一审核者定向复核(一次)→ 冻结 `Image-Candidate: true` → Epic Full → 合 main → Main Publish → stage 升级回读 → L4a。可选 codex 外审一次(批终,证据内嵌 stdin 姿势),不作为第二审核者。
- **证据只认 GitHub/仓库留痕**:harness、审核结论、L4a 结果、失败事件当天落对应 Issue 评论或 PR;任务表随执行 commit 勾选(第一恢复点)。
- **PM 真人验证块只放两处**:块 A(前置输入 D1–D9,一次给齐,见 §8)/块 B(晨间:#492 p2p 复验 + #493 真人卡片,每次一条指令,先出就绪自检)/块 C(上线窗口:转发停写 prompt、首批用户旅程、放行单字)。中途不请 PM 试错。
- 变异验红按 v16 §6.6 硬姿势(`python -B` / 逐次清 `__pycache__`;**注意 `check.sh` 的 venv 是非 editable 安装,变异要改 site-packages 里那份或改用 editable venv**——rc23 复评实测踩坑)。

## 5. 成本预算

- **模型与角色(PM 2026-09-01)**:编排者 **opus / max**;实施与复修 **opus / high**;唯一独立审核者 **fable / xhigh**(批终一次 + 复核一次 + 上线前 runbook 走查一次,不参与实现、不直接修复);规划者/元守护 fable;外部 codex 审核可选 ≤1 次。
- **Epic Full ≤4**(rc24 代码批 1;#492 若走取证日志路线再 1;余量 2);本机 `check.sh full` 仅批终一次,卡级用 targeted + fast。
- **代理人次上限**:实施/复修 ≤10 人次;审核 ≤3 轮;每次派发编排者自挂兜底观察(预计时长 + 到点查外部证据),不裸等;subagent 不得再派 subagent。
- **时间窗**:2026-09-01 22:30 → 2026-09-02 24:00(北京,含跨夜);夜间推进所有不依赖 PM 的 Ready Step;PM 块 B 目标 09-02 上午,块 C 目标 09-02 下午。
- 大 workflow 开跑前查配额余量。

## 6. 主线声明与批次纪律

- **主线**:明天上线 + open issue 清零。任何派发前先问「不做它,明天能不能上线」。
- **`execution_adapter`**:`claude_tmux_named_session`——常驻 tmux session `TZ-server`,批次 window `rc24-b1`(代码批+W0)/`rc24-b2`(生产部署与硬切)/`rc24-close`(收口);拉起姿势见记忆 `tmux-orchestrator-spawn`(`claude --model opus --effort max --permission-mode auto`)。
- **元守护**:本规划者会话(fable);只以外部证据判活(window 存活 + #521/任务表留痕新鲜度,30–60 分钟一查);失联判定=window 死亡且 >90 分钟无留痕→取证后按最新交接拉继任;承接 PM 裁定并转发;不实施、不持合并权。
- **单一写入者**:当任编排者持 tz 主工作树与合并权;实施 subagent 独立 worktree(`/home/wangzp/project/lingxi-wt/rc24-*`),commit 先行再汇报,用完即清;审核 subagent 只读固定候选;stage/prod/Bridge/长连接同一时刻单客户端。
- 批次收口固定序列(v16 §6.8):收口评论 → 交接评论 → 继任 window → 接管登记 → 租约转移 → 退场。收尾必含环境盘点(tz 21 个残留 worktree、`codex/*` 与 `claude/rc23-*` 远端分支、`/tmp` harness、stage 无残留)、任务表勾选、收口评论(UTC+北京、Epic Full 次数/commit 数、代理人次)。

---

# 执行图(节点=Step;`──►`硬依赖;`◇`PM 门;`⚡`stage 注入通道独占;`☢`生产动作)

```
━━ 块 A(PM 前置输入 D1–D9,一次给齐)━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
━━ Wave 0(今夜,零依赖并行)━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 W0-1 #492 根因判定(代码可确定→直接修 | 不可确定→加结构化日志)
 W0-2 stage 轻量 L4a⚡:#494 写满变红+回收有界 → #496 临界并发一轮 → #499 含表格建档
 W0-3 prod 只读盘点(等 D1)     W0-4 #263 材料:A-1/A-2/映射对账 prompt、G1 清单、差集 dry-run
 W0-5 复评修复项归类(P1 必修进 S-1;P2 登记/豁免)     W0-6 #498 处置:关 #518 + L3 现状定稿
━━ Wave 1 代码批 epic/rc24 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 S-1 #492 修复 + 复评 P1 修复 + 文档修正(P1-1 决策记录/合同一致、P1-2 机型、P1-3 清单)
     + CHANGELOG 2.0.0 定稿 ──► fable 审查 → fixpack → 复核 → 冻结 → Epic Full → main → Publish → stage↑
 S-2 ◇块 B(晨间):#492 p2p 复验 → #493 真人卡片步骤(每步一条指令;若 W0-1 走日志路线,先发一次取证消息)
━━ Wave 2 生产 ☢(块 C,PM 在场)━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 S-3 [ops] 生产部署卡:env 七文件核对 → GHCR 登录 → digest 固定 → config 渲染 → migrate(lingxi-prod 从零→0084)
     → 监控 timers 安装 → **不 up**
 S-4 #263 硬切窗口:G1 复核(含映射对账零差异)→ ◇A-2 停写(PM 转发)→ 旧 Bot/API/文件发送停 → [D5 扩容若裁定]
     → G2(跨 15 分钟旧周期,正式表逐行不变)→ #441 差集导入 --apply → up -d(Bot-Prod 长连接=Lingxi)
     → healthy + 15 分钟观察 → 首批用户(1 adopt + 1 issue → 开通完成 → 真问数)→ v2.0.0 tag
━━ 收口 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 S-5 逐卡对账关闭 → docs 回写(当前能力:生产已部署+边界)→ 环境盘点 → #502 Superseded → 任务表勾选 → 收口评论 → 48h/7d 观察交接
```

**车道视图**:

```
代码道   │ W0-1 ━━► S-1(epic/rc24) ━━━━━━━━━━━━━━━━━━┫ 审查→fixpack→冻结→EpicFull→main→Publish→stage↑ ━━► S-2 ◇B
stage⚡  │ W0-2 #494→#496→#499(串行独占) ━━━━━━━━━━━┫                                          (S-1 后 stage 升级)
prod☢   │ W0-3(等 D1) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━► S-3 部署卡 ━━► S-4 硬切 ◇C
材料道   │ W0-4 #263 材料 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
账目道   │ W0-5 复评归类 ━► W0-6 #498 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━► S-5 收口
PM◇流   │ 块 A(D1–D9 今晚/明早一次) ─────────── 块 B(晨间 #492/#493)────────── 块 C(下午上线窗口)
```

要点:①夜间可推进 W0-1/2/4/5/6 与 S-1 全部实施,不依赖 PM;②W0-3 与 S-3/S-4 全部依赖 D1 通路;③stage 注入通道三段串行;④`up -d` 是 Bot-Prod 切换点,只能在 G2 之后。

---

# 合同区(v16 必填字段)

- 方法版本 v16;合同版本 v1;执行阶段 Ready(本 PR 合并即 Ready);整体交付结果 Undetermined。
- **基线(2026-09-01 21:00 北京核对)**:main=`c34b698`(tree `212db0a`);本机 full 门禁复跑 5199 tests OK;stage 三服务 healthy on `20260901-c34b6989a28b`,DB=Supabase 测试实例,迁移头 `0084`;prod 未部署,`lingxi-prod` Supabase 实例已建未接(凭据文件在 biplus-prod `/home/bi-ai-deploy/.config/lingxi/supabase-prod.env`,2026-08-29 验证过登录——本会话无法复核);正式权限表 2026-08-30 快照 26 行(易变,G1 重读);旧系统 `1.40` 每 15 分钟写正式表。
- `execution_adapter`:`claude_tmux_named_session`(见 §6)。
- 规划者会话与停止点:见文首。
- 角色模型:编排者 opus/max;实施者池 opus/high;唯一审核者 fable/xhigh;元守护 fable;codex 外审可选。
- 当前顶层编排:tmux `TZ-server:rc24-b1`(拉起时回填 session ID 到 #521 接管评论)。
- 编排者接手入口:`/takeover` → 读本三件套 + #521 最新交接/裁定评论 → 核实现场(main SHA、stage 回读、复评 Issue 评论、D 清单到齐情况)→ 接管登记 → 合并权转移。**第一恢复点=任务表勾选 + #521 最新评论**。批后继任:b1 收口后拉 `rc24-b2`;b2 收口后拉 `rc24-close` 或由 b2 直接收口(合同允许最终批不建继任)。

## 输入 Issue 准入与关闭资格(可达性审计 A)

| 输入 | 完成标准要点 | 当前事实(2026-09-01 复评) | Step | 收尾去向 |
| --- | --- | --- | --- | --- |
| #263 | 旧写路径全停+G2 跨 15 分钟证据;Lingxi 唯一写入方;差集导入可审计幂等;Bot-Prod adopt/issue/真问数三结果双通道 | 就绪清单齐备;旧写入方事实已核(08-30);映射对账新增项未做;执行卡草案在 #263 评论 | W0-4,S-3,S-4 | 关闭 |
| #519 [ops] 生产部署 | runbook §二~§七逐条;digest 固定;单实例;观察期;v2.0.0 tag | 已建(替代已废弃 #449) | S-3,S-4 | 关闭 |
| #492 | 真实 p2p 邮箱形态解析成功;不误伤;失败文案说清哪段 | **真人 p2p 失败**(`wrong_argument_count`,trace `01M1EFW6…`);代码可确定:消息为 `text` 型且邮箱段含空白 | W0-1,S-1,S-2 | 关闭 |
| #493 | 职位+范围表单/服务端拒绝/懒过期/按钮态/下发如实化/零退役术语 | 代码已合并部署;真人 L4a 未做;入口依赖 #492 | S-2 | 关闭 |
| #494 | 回收有界+写满健康变红(核心)+分环境限制 | 代码/compose/门禁齐;stage 双验证**零留痕**(Codex r9 失败) | W0-2 | 关闭 |
| #496 | 上限=4 显式配置+断言;502 降级零重复;临界并发 stage 一轮 | 代码齐;E1 建连 154/154;stage 完整任务一轮未做 | W0-2 | 关闭 |
| #499 | 降级实现+用例+文案+trace 可查+stage 含表格建档+runbook 就绪项 | 代码腿复评通过(5/6 变异红);stage 建档未做;缺 V-* 行(P2) | W0-2,S-1 | 关闭 |
| #498 | 三档分类+防伪装+影响面 diff;L3 升档 | L0/L1/分类器/diff 已合 main;L3 OWNER attestation(#518)审核未过;**CI 是否真触发非空 L3 路径待复评结论** | W0-6 | 关闭(L3 attestation 登记为已知边界)或按复评结论保留 |
| #497 | PM 数据到→结论去向 | 调查已固化;等 AWS 三项数据 | D7 | D7 给数据→关闭;不给→保持开放(缓做,不阻塞) |
| #411 | 观察期满+无异议→删旧库容器+dump 回帖 | 观察期已满;等「无异议」 | D6,S-5 | 关闭或顺延留痕 |
| #502 | rc23 Trace | 9 卡 2 关;任务表 0/16;本 Trace 接管剩余 | S-5 | `Superseded` 关闭 |
| #147 | 方法正文 | — | — | 保持开放(永不关) |

## 结果可达性(审计 B):首批真实用户从 Bot-Prod 问数成功

真实入口 Bot-Prod 私聊 → gateway(prod,长连接唯一)→ 内测名单闸(`LINGXI_INNERTEST_ROSTER_OPEN_IDS`,**未配置=全拒**,D8 提供首批 open_id)→ 首聊开通编排(scheduler;花名册 Base、正式权限表坐标、存量令牌坐标=正式表)→ 权限发布→就绪探针 → worker-queue 认领 → 只读 MCP 问数 → 投递 outbox → 用户可见卡片 + `worker.task.terminal` 日志。**缺件**:prod 三容器与七 env 文件(S-3);正式表写入权归属(S-4 G2);首批 open_id 名单(D8);管理员登记表 `admin_registry` 在 prod 库为空——需 S-3 按 D9 名单落行(迁移 0067 表,落行方式由实施者从 `adapters/admin_registry.py` 读出,不猜)。

## Wave 0 与 PM 门

| Gate | 待确认 | 探针/来源 | 依赖 Step | 成立后 | 不成立/未知 |
| --- | --- | --- | --- | --- | --- |
| G-1 | prod 命令通路(D1) | PM 裁定 | W0-3,S-3,S-4 | 只读盘点起手 | prod 步骤全部 Pre-ready,其余照跑 |
| G-2 | #492 真实形态是否可由代码确定 | 复评代理 B 结论 + `pipeline.py`/`feishu_events.py` 读码 | S-1 | 直接修 | 加 `admin.command.unknown` 结构化 token 形状日志→部署 stage→块 B 先发一次→再修(多消耗 1 次 Epic Full) |
| G-3 | 职位→指标映射两侧零差异(#263 新增对账) | PM 转发 prompt 取 biai-agent 侧映射 → 与两份 TOML 比对 | S-4 | 硬切放行 | 差异未清零**不硬切**(PM 裁定以哪边为准) |
| G-4 | 旧写入方现状(G1 复核) | A-1 prompt(PM 转发)+ 正式表重读 | S-4 | 停写 | 有未知写入方→停,回 PM |
| G-5 | 生产机型(D5):t3.medium 同 stage vs 8 核/32G | PM 裁定;架构设计 :539 与 runbook 矛盾(复评 P1-2) | S-1 文档修正,S-4 | 按裁定改文档与资源合同 | 默认按「与 stage 一致」,不扩容 |
| G-6 | Bot-Prod 配置齐备(D2/D3) | PM 确认;S-3 只读核对文件存在与权限 | S-3 | 部署 | 缺任一项→S-3 停在 preflight |

## 计划执行步骤

| Step | Wave | 动作与产物 | 启动条件 | Owner/路线 | 租约 | 成功证据 | 成功后 | 失败/等待去向 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| W0-1 | W0 | #492 根因判定与修法(G-2) | 批准 | 实施 opus/high(读码+单测) | 无 | Issue #492 评论:确定性推理+夹具形态+修法 | S-1 | 不可确定→日志路线,S-1 拆两段 |
| W0-2 | W0 | stage 轻量 L4a:#494(写满→unhealthy→告警→清→healthy;10 次真实问数 /tmp 不单调增)→#496(4+2 并发一轮,零重复交付,cap 生效)→#499(1 次含表格问数→docx+降级提示+trace) | 批准;stage 通道空闲 | 实施 opus/high(受控注入,`replay_inbound_event`/测试身份) | R-stage⚡ | 各卡评论:双通道证据(DB/日志 + 平台回读),含清场回读 | 卡可关(随 S-5) | 任一失败→最多再一轮;仍失败→归 blocker 进 S-1 |
| W0-3 | W0 | prod 只读盘点:主机规格、docker、七 env 文件存在+0600、GHCR 登录态、旧系统进程与 Bot-Prod 连接、监控 timers | D1 到齐 | 编排者或实施(只读) | 无 | [ops] 卡评论:盘点表(零凭据回显) | S-3 | 通路未给→Pre-ready |
| W0-4 | W0 | #263 材料:刷新 A-1/A-2 prompt(含停 Bot/API/文件发送与回执格式)、映射对账 prompt、G1 复核清单、差集导入 dry-run 复跑(stage) | 批准 | 实施 opus/high | 无 | #263 评论 | S-4 | — |
| W0-5 | W0 | 复评修复项归类:P1 进 S-1;P2 登记/豁免;各卡评论落结论 | 复评结论到齐(规划者已落) | 编排者 | 无 | 缺陷账本=#520 | S-1 | — |
| W0-6 | W0 | #498 处置:关 #518(留分支)、按复评结论定 main 上 L3 门禁现状与文档边界 | 复评 D 结论 | 编排者 | 无 | #498 评论 + PR #518 关闭 | S-5 | L3 被判不安全→进 S-1 最小修复(fail-closed) |
| S-1 | W1 | `epic/rc24`(bootstrap 先例刷 Story Fast):#492 修复(+日志)、复评 P1 修复、文档 P1(合同/决策记录一致、机型、清单三句)、CHANGELOG 2.0.0 补 rc21–rc23 条目并把日期改上线日、#499 V-* 行;串行合并→fable/xhigh 审查→fixpack→复核→冻结 trailer→Epic Full→main→Publish→stage 升级回读 | W0-1/W0-5 | 实施 opus/high ×≤4;审核 fable/xhigh | R-tree | PR 链+Epic Full run+Publish run+stage 回读 | S-2 | 审核 P0/P1 未闭→一轮 fixpack;仍未闭→上报 PM |
| S-2 | W1 | ◇块 B:就绪自检→PM p2p 发 `/admin audit <邮箱> 24`(一次)→若日志路线先取形状→修→再来一次;#493:先出最小安全真人步骤(Bot-Test 专用目标),逐条 | S-1 stage 升级完成;PM 在场 | 编排者 | R-stage | #492/#493 评论:双通道 | 卡关闭 | 失败→停,不重发,分析后归 S-1 二轮或上报 |
| S-3 | W2 | 新建 `[ops]` 生产部署卡;☢逐步:preflight(七文件 0600、GHCR)、digest 解析与固定四变量、`config worker-queue` 渲染核对五值、`migrate`(lingxi-prod 0→0084)、监控 timers 安装、`admin_registry` 落行(D9)、内测名单(D8)写入 env;**不 up** | D1–D4/D8/D9 齐;S-1 已 Publish(制品=上线 tag) | 编排者出指令+判据,执行按 D1 | R-prod | ops 卡评论逐步留痕 | S-4 | 任一 preflight 不过→停 |
| S-4 | W2 | ◇块 C 硬切:G1 复核(A-1 回执+正式表重读+G-3 零差异)→PM 转发 A-2→旧全部写路径与旧 Bot 停+回执→[D5 扩容]→G2 跨 15 分钟正式表逐行不变→#441 `--apply`→`up -d`→healthy→15 分钟观察→首批用户 adopt+issue→真问数→`v2.0.0` tag→#263 关闭 | S-3 完成;PM 在场 | 编排者+PM | R-prod,R-botprod | #263/ops 卡评论:时间线、回执、G2 证据、用户双通道 | S-5 | 立即停止条件(#263 正文)命中→停在当前门,不回退旧写入 |
| S-5 | 收口 | 逐卡对账关闭(#492/#493/#494/#496/#499/#498/#411/ops/#263);docs 回写(当前能力:生产已部署、边界;runbook 实录偏差);环境盘点(tz worktree/远端分支/`/tmp`;stage);#502 Superseded;任务表勾选;收口评论;48h/7d 观察项交元守护+PM | S-4 | 编排者 | — | 收口评论 | Trace Closed/Complete | 未完项逐条转去向 |

## 共享资源租约

| 资源 | Owner | 规则 |
| --- | --- | --- |
| R-tree tz 主工作树/合并权 | 当任编排者 | 唯一写入者;实施独立 worktree 用完即清;接管登记原子转移 |
| R-stage⚡ 注入通道 | 编排者调度 | 单客户端;W0-2 三段串行;S-1 升级;S-2 真人;用后清场回读 |
| R-prod biplus-prod compose 权 | 按 D1 | 任一时刻一个操作者;先停旧再起新;不并行 up |
| R-botprod Bot-Prod 长连接 | 硬切后=Lingxi gateway 唯一 | G2 前旧系统持有;`up -d` 即切换;不得出现双连接 |
| tmux `TZ-server` | 元守护 | 每批一个 window;退场 window 留供回翻 |

## 正式验收/发布对象

| 路线 | 对象 | 绑定 | 回填 |
| --- | --- | --- | --- |
| S-1 代码批 | 合并后 Main Publish 四镜像 `YYYYMMDD-<sha12>` | Epic Full candidate proof ↔ main tree | 冻结时回填 SHA/tree/digest 到任务表 |
| S-3/S-4 生产 | 同一 tag 四 digest(`LINGXI_*_IMAGE_DIGEST`) | runbook §三 | ops 卡 |
| 回滚 | stage:`.env.stage.before-*`;prod:首发无旧版本,回滚=`down`(硬切单向门,只前修) | runbook §六 | — |

## 授权与外部动作

| 动作 | 授权 | 执行者 | 副作用/恢复 |
| --- | --- | --- | --- |
| 合并 PR / 部署 stage / 关 issue / 回写 | 是(常设) | 编排者 | 可逆 |
| prod migrate/up/env/监控安装/tag 推送/差集 apply | 逐步「批」 | 按 D1 | migrate 先加后删;up=Bot-Prod 切换不可逆(单向门) |
| 旧系统停写/停 Bot | PM 转发 prompt | biai-agent 侧 | 不恢复(单向门) |
| AWS 扩容 | 若 D5 裁定 | PM 控制台 | 停机窗口 |
| #411 删旧库容器+dump | D6 无异议 | 编排者 | 不可逆 |
| GitHub ruleset/CODEOWNERS | 否 | — | — |

## 验收、审核与修复合同

- CI:Story PR→`epic/rc24` 走 Story Fast(串行开,合一个更一个);Epic PR→main 一次 Epic Full;Main Publish 一次。
- 唯一审核者 fable/xhigh:批终一次批量审查(需求/实现/测试/合并面,抽 3–5 个变异独立复做)+ fixpack 后一次定向复核 + 上线前 runbook/ops 卡走查一次;不审中间草稿。
- 首触冒烟:#492 p2p、#493 卡片、首批用户三条路径均先非正式冒烟(Bot-Test/受控),再正式。
- 双通道:每条用户可见断言 = 平台 API/客户端可见 + 服务端结构化日志/DB。
- 夹具:#494 写满用容器内 `fallocate/dd` 到 tmpfs 上限,用后删除并回读 `df`;#496 并发用受控注入 N 任务;#499 用含表格的固定问题;全部默认关闭、用后撤除。
- 静态排除对账:§3 不做清单逐项对到验收断言。

## 8. 块 A:请产品负责人一次给齐的前置输入(D 清单,单字/一行即可)

| # | 事项 | 默认(不回复即按此) |
| --- | --- | --- |
| D1 | **prod 命令通路**:①在 tz 加 `biplus-prod` SSH 配置并在权限规则放行 `ssh biplus-prod`(编排者从只读开始);或 ②prod 命令由 PM 照编排者给的逐条指令手敲、回贴输出 | 无默认——**不给则 S-3/S-4 全部 Pre-ready** |
| D2 | Bot-Prod 应用凭据(app id/secret)、事件订阅=长连接、scope 与 Bot-Test 一致(含 docx convert scope?)、已加入管理群——是否齐备 | 未知 |
| D3 | prod 七个 env 文件(`.env.prod` + 六服务)由谁放置、何时就位(代理零接触明文);GHCR 只读拉取登录 | PM 放置 |
| D4 | 正式权限表/花名册 Base/存量令牌坐标(prod 与 stage 是否同 Base);管理群 chat id | 与 #263 就绪清单一致 |
| D5 | 生产机型:与 stage 一致(t3.medium,不扩容)还是 8 核/32G(需停机扩容) | 与 stage 一致,不扩容 |
| D6 | #411:删本机旧库容器与 `~/lingxi-db-rollback` dump——「无异议」? | 顺延留痕不删 |
| D7 | #497:AWS credit specification / CPUCreditBalance / CPUSurplusCreditBalance 三项数据 | 不给→保持开放缓做 |
| D8 | 首批真实用户名单:≥1 存量令牌用户(adopt)+≥1 新用户(issue)的姓名/邮箱(open_id 由编排者从花名册快照解析);全量放行时机(48h 后?) | 未知 |
| D9 | prod 管理员名单(super_admin/permission_admin/ops_admin 各谁) | 与 stage 相同的真人管理员 |

## 现场事实与已知坑(编排者必读)

- 沿 #502/#469 合同「已知坑」全部条目(`--env-file` 不能省、`--profile mvp`、`fix:#521` 自动关卡、敏感 env 只列变量名、变异 `python -B`、comments 端点排序、epic ruleset 强制 base 最新→Story PR 串行开、`gh pr merge --delete-branch` 遇 worktree 占用中断、Image-Candidate trailer 必带)。
- **check.sh 的 venv 是非 editable 安装**(`.dev-check/venv-fast` 拷贝 src 到 site-packages):变异实测改 `src/` 不会生效,要改 site-packages 副本或另建 editable venv。
- **并行代理共用 scratchpad 会互相覆盖同名临时文件**(rc23 复评实测):派发卡写明各用私有子目录。
- `LINGXI_WORKER_MAX_CONCURRENCY` 在 compose `environment:` 块设值,写在每服务 env 文件里会被静默忽略(prod 用根 `.env.prod`)。
- 内测名单闸未配置=全拒;首批 open_id 必须进 prod env。
- `/tmp` 不是证据载体;Codex 归档包在 `~/project/lingxi-archive/`。
- prod SSH 与分类器:2026-09-01 实测 auto mode 分类器拦 `ssh biplus-prod`,tz 亦无 Host 配置——D1 未解决前不要尝试绕。
