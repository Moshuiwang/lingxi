# 开工快照：2.5.1 一版收口（Trace #812）

> 按 `#678` v21 §二与 `docs/traces/README.md`：开工能力、额度与环境快照类内容放本文件。**授权、验收门槛、已裁定事项、共享资源租约、外部写与恢复、各道 Gate 一律留在 `合同.md`，不得移出以绕过批准。**

## 合同区字段

- 方法版本 **v21**（[#678](https://github.com/Moshuiwang/lingxi/issues/678)，2026-09-16 06:22Z 发布，[发布评论](https://github.com/Moshuiwang/lingxi/issues/678#issuecomment-5692998645)）；合同版本 **v1（r2，2026-09-16 15:0x 北京按裁定重写为一版收口）**；执行阶段 **Planning → 待批准**（合同 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- **执行者与运行面**：Claude Code 运行面；执行机 tz（Ubuntu 24.04）；**本机时钟是 UTC**（`/etc/timezone` = `Etc/UTC`），面向产品负责人的时刻一律 UTC + 北京（+8）双标注且两个值都实读（`date -u` 与 `TZ=Asia/Shanghai date`）。本快照采样时刻：**2026-09-16 06:31–06:34Z｜北京 14:31–14:34**。
- **基线提交与核对时间**：`origin/main` = **`dcd3500`**（2026-09-16 06:31Z 实读，主检出工作树干净、无开放 PR）；`origin/release/2.5` 头 `176ca2e`（= v2.5.0）；`pyproject.version` = 2.5.0；最新正式版 Release `v2.5.0`（2026-09-15 12:17:27Z）、最新候选 `v2.5.0-rc.102`；生产现网 **v2.5.0 / 迁移头 `0098`**（来源 #770 收口评论 5681240884，本次未登机）；预发 rc.102 **已停**（timer disabled、三容器 Exited，镜像 / 卷 / 装配保留；来源同上，本次未登机）。
- **规划者任务 / 会话与停止点**：规划者会话在主检出 `~/project/lingxi`（tmux `TZ-server:812-plan`，Fable 5.1 xhigh），规划工作树 `.claude/worktrees/plan-812`（分支 `plan/trace-812-2.5.1-2.6.0`）。停止点 = 合同 v1 合并；**随后转任守望者**（合同 §6，裁定 8）——不实施、不持编排权与合并权，只以外部证据判活、承接并转发裁定、失联时取证后拉继任、核编排者退场序列（含复盘评论）。
- **角色模型 / 推理档位**：同合同 §5。
- **当前顶层编排任务 / 会话**：尚未拉起；合同合并且 Claude 周窗重置后由守望者（裁定 8）拉起 tmux `TZ-server:812-w1`（工作树 `.claude/worktrees/812-w1`，编排 opus max）；批次 = W1 部署链批 / W2 产品批 + 预发 + 生产 / W3 收口；接管登记见 Trace Issue。
- **编排者接手入口与第一恢复点**：`/trace-kit:takeover` → 读本目录四份文件 + Trace Issue 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + Trace Issue 最新评论**。

## 开工能力与额度快照（v21 §三；全部 2026-09-16 06:3xZ 实读或实机测试）

| 项 | 值 | 怎么查（可复用；主备两处） |
| --- | --- | --- |
| 当前主编码代理 | Claude Code **2.1.273**；本规划会话 Fable 5.1 **xhigh**；本机默认档位 xhigh（`~/.claude/settings.json` effortLevel） | `claude --version`；会话状态行 |
| Claude 额度（采样 06:32Z / 14:32 北京；均为**已用**百分比） | 5 小时窗已用 **7%**（07:30Z / 15:30 北京重置）；**7 天窗已用 89%**（**2026-09-16 23:00Z = 09-17 07:00 北京**重置）；Fable 已用 **64%**（同一重置）。**结论：实施从 09-17 07:00 北京重置后开工**；规划期不再派子代理 | 主：`~/.claude/tmux-claude-usage-api.dat`（七列：时刻 5h% 5h重置 7d% 7d重置 Fable% Fable重置，epoch 秒，轮询器每 5 分钟拉官方用量接口；会停更）；备：渲染器 `~/.claude/tmux-claude-usage.dat`（五列）与 tmux 状态栏橙底段；CLI 无额度子命令 |
| 外审一：codex **0.154.0** | 模型 **`gpt-6-astra`** high；**实机测试 2026-09-16 06:33:30Z：18 秒、退出码 0、stdout「就绪 gpt-6-astra high。」、4,694 token**；回退 `gpt-5.6-sol` high（本次未测，#770 09-13 实测可用）；`config.toml` 默认 `gpt-5.6-luna` / max（显式传模型，不靠默认；luna 不再用于实施） | 调用姿势：在 trusted 目录 `printf '%s' "<提示>" \| codex exec -s read-only -c model="gpt-6-astra" -c model_reasoning_effort="high" -`，输出直写文件；证据内联进提示、明示不跑命令；判成功只认 stdout 非空 |
| codex 额度（采样 06:32Z） | 7 天窗**已用 18%**（**2026-09-19 11:10Z = 19:10 北京**重置）；5 小时窗无数据 | 主：`~/.local/state/tmux-codex-quota/tmux-codex-usage.dat`（五列：时刻 5h% 5h重置 7d% 7d重置；`~/.codex/hooks.json` 会话钩子刷新，跑一次 `codex exec` 即更新）；备：tmux 状态栏蓝底段 |
| 外审二：agy **1.2.3**（Antigravity CLI） | 模型 **`gemini-3.8-flash-high`**（档位在模型名里，**不传 `--effort`**）；**实机测试 2026-09-16 06:33:49Z：12 秒、退出码 0、stdout 29 字节「就绪 gemini-3.8-flash-high」** | 调用姿势：`agy --add-dir <工作树> --model gemini-3.8-flash-high --print-timeout 600s --print='<提示>'`（`--print=` 带等号、`--model` 在前；提示首行写「不要调用任何工具」或让它只读工作树文件）；判成功只认 stdout 非空；白名单外命令静默零输出 |
| agy 额度（06:34Z 实读） | Gemini 系：**周剩 100%**（2026-09-23 06:33Z 重置）、五小时窗剩 100%；Claude / GPT 系经 agy：周与五小时均 100% | 主：`agy --print='/usage'`（四行制表）；备：无 |
| 外审频率（按额度定） | codex、agy 均充裕 → 每批次终点各一轮（W1、W3）+ 每版生产前 codex 走查一轮 = 预估 6 / 上限 8。**降档触发**：任一家周窗 ≥ 80% 或五小时窗 ≥ 90% → 该家只保留批次终点一轮 | 每次外审当场登记：时间、工具与模型、候选 SHA、卡号、结果条数 |
| 本机门禁 | `scripts/dev/check.sh full` 空闲机器约 24 分钟（#770 实测 `Ran 7320 tests`），须 tmux 窗口独占机器跑；本会话 shell `umask 0002`，跑门禁前 `umask 022` | #770 批次 1 / 收口实测 |

## 开工能力与环境快照（其余）

| 项 | 值 | 来源 |
| --- | --- | --- |
| 发布路径 | `publish.yml` 触发于推送 `release/**`（候选 job 要求该提交恰有一个已合并、base 为该 release 分支的 PR）；`release.yml` 手动 `workflow_dispatch`（机器人 403，产品负责人网页点）；rc 序号全局递增；第二次起同步 PR 须先并入 `release/2.5` 现头；候选版本号取自 `pyproject.version`；建 `release/2.6` 基点须同时带 `Epic Full` 与 `Epic Verdict` 成功记录的 PR 头 | 仓库实读；#754 / #770 实测 |
| 机器人权限边界 | `gh run rerun` / `workflow run` 403；`accept/`、`record/`、`trace/` 前缀可推；`docs/traces/**/合同.md`、`deploy/releases/acceptance/`、裁决层三文件受 CODEOWNERS（产品负责人批） | `.github/CODEOWNERS` 实读 |
| CI 成本 | `gate` 约 10 分 / 15 分上限；`image` 4–5 分；纯 docs PR 约 13 秒；`deploy/**` 或 `.github/**` 走完整门禁；`trace/**` 前缀把纯记录 PR 也路由成完整门禁 → 记录 PR 用 `record/` | 记忆 + #754 / #770 实测 |
| 部署链现状 | 拉取代理 + 部署器已在预发（rc.99 → rc.102 四轮）与生产（v2.4.3 → v2.5.0 一轮）真机跑过；F20（#804）/ F21（#805）为已登记边界，人工处置步骤在 `deploy/拉取代理.md` §十二；**代理自身是否随新版本升级 = 未知**（S-0-2 ①） | 仓库实读 |
| 预发 | Amazon Linux 2023，`/usr/bin/python3` = 3.9、监控与代理单元钉 `/opt/lingxi/bin/python3`（3.12）；权限发布表为独立预发表（正式表只读沿用）；老用户可真实问数、新人只到开通 + 欢迎卡；凭据卷无专用授权凭据 → **能否读花名册快照 = 未知**（S-0-2 ②）；持久日志 `/var/log/lingxi/`；同一测试机器人只允许一个客户端 | #770 W2 / #606 实测记忆 |
| 生产 | v2.5.0 / 0098；受限通道 + 拉取代理 timer 空转（每 5 分钟 `already_in_place`）；动态内测名单 = 本人 + 17（16 人预开通完成、1 人 `roster_multiple_rows` 失败、3 人 `identity_not_unique` 未入名单 → #807 共 4 人）；回滚点保留（产品负责人说不重要，不催）；代理只读形态 `ssh biplus-prod 'sudo -u bi-ai-deploy …'` 扁平单条 | #770 收口 / 交接评论 |
| 里程碑（2026-09-16 07:09Z 同步后） | 2.5.1（#10，9 单 + 本 Trace：#804 #805 #807 P1 Ready、#739 #745 #742 P2 Ready、#748 P1 / #735 #756 P2 待验收）；2.6.0（#13）= 数据库迁移 #809（产品负责人裁定提前，另开 Trace）；3.0.0 = #810 MCP；3.1.0 已腾空关闭；**无 2.6.1 里程碑**（06:28Z 实读） | GitHub 实读 + 同步回读 |
| tmux / 工作树 | `TZ-server` 常驻，当前只有本会话窗口 `812-plan` 与一个空 bash 窗口；无其他编排者在跑；主检出无其他工作树 | 本机现场（`tmux list-windows`、`git worktree list`） |
| 分类器边界 | 预发持久化动作（systemd / 系统账号 / root 文件）与 scp 含此类内容的脚本被本机自动模式分类器拦 → 幂等脚本 + 逐步回读 + 回退脚本交产品负责人 `!` 亲跑；生产写动作一律产品负责人本人 | #770 W2 实测 |

## 批后继任条件（命中）

三个批次窗口必然跨会话接力。继任：读本目录四份文件 + Trace Issue 最新交接评论 → 核实现场（工作树、批次分支 tip、PR 状态、预发与生产现状、额度余量）→ 接管登记 → 前任确认停止写入并释放租约 → 退场。**未确认转移前保持等待，不得出现两个写入者。** 接力只发生在批次收口序列或失联判定，不因上下文余量强制交接；退场前复盘评论落地才算退场完成。守望者失效时（窗口关闭即两类机制全失效）由产品负责人在主检出重新拉起守望者会话，接手 prompt 见 Trace Issue 最新交接评论。
