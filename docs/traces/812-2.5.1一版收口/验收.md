# 验收（Trace #812 · 2.5.1 一版收口；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace Issue 或 PR）

> 证据等级口径见 `docs/技术设计/验证与门禁.md`（L 名 = 成熟度 · 环境 · 执行者；全局 1–7 级对照见该文第三节）。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，不得为了收口好看而标绿。**判定字段只认系统写出的值，前置状态允许人工构造；答不出「这个值是谁写的」即无效判据。**

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准（判定值由谁写出） | 目标等级 | 现值 | 关联验收矩阵 | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1** #804 old / new 配置指纹 | ① 回归用例：版本 A 部署（指纹 X）→ 目标配置改 Y → 版本 B 计划 old = X、new = Y，不报 `unplanned_service_artifact_or_config`；② 篡改旧容器标签 / 制品仍被拒（否定用例）；③ old / new 在 plan / audit / 错误码可区分；④ 预发真实链路：rc.A `verified` → 人工改 `public-config.json` → rc.B 计划以 X 核 old、以 Y 描述 new → `verified`（判定值 = 部署器状态账与计划文件，人工构造的只有配置改动）；⑤ 取不到可信旧值时失败关闭、不把 desired 冒充历史值 | L2 → 预发 L4a | L2 + 预发 **L4a**；生产由 v2.5.1 自身升级顺带经过一次（old = new，阶段账 root 不可读、`old_source` 未直证） | V-部署-34 | [S-1-4 5696331092](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5696331092)（①②③⑤ L2）；[rc.104 5706196423](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5706196423)（④：rc.103 `verified` → 改 Y → 计划 old = X / new = Y → `verified`） |
| **E-2** #805 prepare-only 收敛 | ① 阶段意图先持久化再进副作用阶段；② prepare-only 失败 + 五条件全成立 → 失败计划归档、在途标记复位到上次 `verified`、下一计划正常部署；③ `stop` 及之后失败、意图已记录、旧执行者仍活、标记属其他计划、两个恢复者并发 → 不清理 / 不双重部署（用例逐条）；④ 崩溃注入（意图持久化前后、动作前后、完成记录前后）不误清；⑤ 收敛不产生新的 `verified` 记录，审计含失败 plan id、阶段、占用者、收敛依据；⑥ 预发注入一次 prepare 失败 → 自动收敛 → 下一轮 `verified`（判定值 = 状态账、`host.active.json`、审计行） | L2 → 预发 L4a | L2 + 预发 **L4a** | V-部署-35 | [S-1-4 5696331092](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5696331092)（①–⑤ L2，②「归档」读作原地保留 + 审计）；[rc.104 5706196423](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5706196423)（⑥：首轮注入 prepare 失败 → `released_markers restored_from=verified_file` → 次轮 `verified`） |
| **E-3** #739 umask | 同一候选 `umask 0002` 与 `0022` 下两条目标用例结论一致（两次都实跑、各留 `Ran N tests` 行）；显式安全 mode 通过、显式 group / other 可写 mode 判红；生产权限判据零改动（diff 证明）；相邻部署测试扫描无同型依赖或另行登记 | L1（双 umask）+ L2 | L1（双 umask，内审独立复跑 `Ran 73` ×2）+ L2 | — | [S-1-4 5696331092](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5696331092)（提交 `0b65cf1d`） |
| **E-4** #807 唯一在职身份 | ① 五个按邮箱定位的入口共用同一 resolver（代码引用证明，不复制判定树）；② 五态用例：唯一在职 / 全非在职 / 多在职冲突 / 未找到 / 快照不可用或字段未知，外加已有绑定与新身份不一致；③ 不可用快照不折算 0 在职、不自动授权、不触发批量撤权；④ 邮箱复用不静默转移旧 token / 会话 / 本地授权 / 历史数据；⑤ 审计含快照版本、候选数、在职数、最终原因；⑥ 预发两个真实失败入口（扩员准备、预开通）回归通过（判定值 = `operation_audit` / 扩员批次状态，样本邮箱为人工前置）；⑦ 生产 4 人按新规则重处理，逐人审计终态（G-3）；⑧ 产品合同 / 当前能力回写身份解析与生命周期边界 | L2 → 预发 L4a → 生产「真实链路 · 生产 · 真人 / 代理 · 写」（G-3） | L2 + 预发 **L4a**（两入口）+ **生产「真实链路 · 生产 · 真人 / 代理 · 写」**（G-3 三人逐人终态齐备；第 4 人人工处置） | V-开通-24 / V-开通-28 / V-权限-17 | ①–⑤⑧ [Fable 把关 5705515809](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5705515809)（威胁模型 ①–⑦ 不成立、变异六条各红）+ [定向复核 5707670271](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5707670271)；⑥ [入口一 5709137672](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5709137672) / [入口二 rc.106 5710576871](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5710576871)；⑦ [首轮 5711370589](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5711370589) → [⑫ 归档 5713927159](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5713927159) → [⑬ 5713935586](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5713935586) → [逐人终态 5714350463](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5714350463)（`completed` + `check_passed` ×3） |
| **E-5** #745 示例交集（D-13：不设单独验收与审核） | 用例：单公司不变、多公司有交集只取交集、无交集降级文案 / 不生成、部分独有指标不出现在「各公司」示例；用户原始查询权限与公司特有指标查询能力不变（否定用例）；随 W2 批次门禁与内审，不安排预发真人核对 | L2 | L2（v2.5.1，随 W2 批次门禁与内审） | V-开通-30 | 提交 `c105b1e6`；[Fable 把关 5705515809](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5705515809)（M4 交集改并集 → 红 7）；PR #821 |
| **E-6** #756 ③④ | ③ 正常代表任务下 RSS / cgroup 峰值样本（候选、环境、方法、脱敏样本）；④ 停 queue → 重启 gateway → 恢复 queue → 同一 task 最终送达且唯一（判定值 = task 表终态与投递记录）；文档回写 | L4a | L4a | — | [S-2-5 5709261607](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5709261607)（③ 峰值样本 rc.105；④ T2 同一 task `succeeded` 唯一送达）；`deploy/监控告警.md` §七 已回写 |
| **E-7** #748 剩余验收 | E-4 后半：撤属主行后再做一次同名伪造 PR 被拒 + 路径 b 复跑判红（真实 PR run）；E-1 ④：一个不改裁决核心的最小工作流 PR 无需产品负责人批准即合并（合并记录）；E-1 ⑤：规则集临时去掉 `Epic Verdict` 行 → 合并 → 加回，前后规则集 JSON 逐字相等（G-2；不放行则留去向）；「门禁定义」保护边界与不改 YAML 变更的既有测试证据登记；验收文档回写附候选 SHA / run | L2 + 真实 PR 实测（全局 4） | 真实 PR 实测（全局 4）+ L2 | — | [#748 5695225037](https://github.com/Moshuiwang/lingxi/issues/748#issuecomment-5695225037)：E-4 后半 PR #816 头 `5a4a39b` 主干裁决 run 35076457802 → `Epic Verdict = failure`（路径 b 判红、`BLOCKED`）；E-1 ④ PR #817 头 `e3656eb` run 35077310461 → success、免批合并 `3a5286c`；E-1 ⑤ 规则集 20559295 去行 → 合并 → 加回，JSON 逐字相等；「门禁定义」边界钉住用例 `tests/test_verdict_decision.py::DecisionRulesTest::test_changed_workflow_tree_takes_path_b_even_with_a_good_token` / `TreeDiffAndCheckRunTest::test_workflow_tree_diff_reports_added_removed_modified_only` |
| **E-8** #735 收口 | 「申请使用」是否真实授权来源有产品负责人一句话或飞书后台只读核实留痕；单账号对照证据（#799 A-5 / C-3、#770 5674442737 / 5675123356）与 apply 专属复查测试类（`tests/test_outreach_ops.py::StateAtSendTest`）及 CI 来源链接进验收文档；文档不再写「没有方法 / 从未验证」，也不扩大宣称 | 文档对账（全局 1）+ 既有 L4b 引用 | 文档对账（全局 1）+ 既有 L4b 引用 | V-管理-53 | [#735 5708521834](https://github.com/Moshuiwang/lingxi/issues/735#issuecomment-5708521834)（「申请使用」= 飞书应用可用范围申请、非业务授权来源）；V-管理-53 已登记单账号对照（#799 A-5 / C-3、#770 5674442737 / 5675123356）与 `StateAtSendTest` |
| **E-9** #742 复现量测 | 固定提交 + 固定环境连续跑 N = 50（或 60 分钟时间盒）：总次数、失败次数、失败分布、被测状态与时间字段、原始断言全部留档；有直接证据 → 修法读系统写出的状态 / 时间基准 + 正反例（随 W2 批次）；无 → 如实「当前未能复现（提交 / 环境 / 样本量）」 | L3（本机 / 探针） | L3（N = 24 未复现 + `pg_stat_activity` 未遂形态证据）→ 测试侧修法 `b9ad79a5` 随 W2 | — | [#742 5695366880](https://github.com/Moshuiwang/lingxi/issues/742#issuecomment-5695366880)；[#742 5703613243](https://github.com/Moshuiwang/lingxi/issues/742#issuecomment-5703613243) |
| **E-10** v2.5.1 上生产（拉式） | `release/2.5` 候选四镜像 digest 与正式版逐一相同（清单由 CI 写）；预发由拉取代理自动部署 rc、版本验收单固定项全过、验收记录 PR 合入 main；产品负责人点 Release Promotion 后生产拉取代理自动部署；生产迁移头 = 链头；三服务 healthy 且 15 分钟无非预期重启；既有业务行数不变；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**（由队列执行服务写入 task 表） | **L5（只认真人）** | **L5**（产品负责人真人问数 `succeeded`） | — | [验收单 5710789683](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5710789683)；[G-5 5711281671](https://github.com/Moshuiwang/lingxi/issues/812#issuecomment-5711281671)：正式版 `v2.5.1`（`5639500`，四镜像 = rc.106，清单 `cc8de2d1…`）07:58:55Z → 生产代理 08:00:04Z 拾取 → 08:01:11Z 三容器重建 → 08:17:01Z `verified`（15 分钟 healthy）→ 真人问数 `tsk_01M2Q76M…` `succeeded`；迁移头 0098、行数不变 |
| **E-11** 里程碑与工作单收口 | 2.5.1 里程碑 open 只剩 #812 或为 0，余项各有去向评论；四条部署链改道事项有登记单；#809 只读盘点事实表落 #809 评论；CHANGELOG `[2.5.1]`、当前能力、验收矩阵现值、`deploy/拉取代理.md` §十二 回写 | — | 收口 docs PR（本 PR） | — | 改道登记单 #814 / 代理自升级 #815；[#809 盘点 5693954544](https://github.com/Moshuiwang/lingxi/issues/809#issuecomment-5693954544)；CHANGELOG `[2.5.1]`、当前能力、验收矩阵、`deploy/拉取代理.md` §十二 均在本 PR；里程碑关闭在逐 Issue 关闭后 |

## 批终链（每个批次全绿才进下一阶段）

本机完整门禁绿（tmux 窗口独占跑，`umask 022`）→ 唯一独立审核者 fable xhigh 批量审查（一次，审查卡带威胁模型；W1 另附真机语义清单）→ 统一修复包 → 同一审核者定向复核并对冻结候选出「可合」（一次，兼把关）→ 外审（codex `gpt-6-astra` high 绕过面 + agy `gemini-3.8-flash-high` 用户可见后果，各一轮；W2 的外审即生产前走查）→ 冻结候选 → CI 完整门禁 + `Epic Verdict` → 合 main。冻结后回填：**W1**：候选 SHA `30f910f4`（PR #818 头 `7478363`）；本机 full `Ran 7361 tests` OK；门禁 run `35085883672` + Epic Verdict 路径 a；外审登记行 5695989739（codex astra 6 / agy 2，无 P0/P1）；合并 `3943857`。**W2**：候选 SHA `3457aa9a`（PR #821）→ 修复包 `92d1092f`（PR #824 头 `32fce497`，裁定 A）→ 修复包 D `28133a36`（PR #829 头 `a2d24947`，裁定 B）；本机 full `Ran 7408` / `Ran 7422` / `Ran 7425 tests` OK；门禁 run `35140351689` / `35175090356` / `35188291870`；内审 5705515809 / 5707670271 / Fable r3（收口 5713398318）；外审登记行 5703236348（agy 0/0/0）/ 5713398318（codex astra 定向 0/0/0）；合并 `0ab7b89` / `6362c9c` / `b8f28523`。

## 版本验收单（预发一次、生产固定项一次；E-10 用）

固定项：① 迁移头 = 链头；② 三服务 healthy 15 分钟；③ 既有业务行数不变；④ 一次真实问数 `succeeded`；⑤ 四镜像 digest 与正式版清单逐一相同；⑥ 部署由拉取代理 + 部署器完成的留痕（计划 / 执行 / 状态三份记录）。

随版本项：#804 真实链路（rc.A → 改配置 → rc.B `verified`，old / new 可读出）；#805 prepare-only 注入一次自动收敛（rc.B 第一轮）；#807 预发两入口回归；#739 双 umask 结论一致；#745 四类样本用例随批次绿（不另核卡片）；#756 ③④（未完成不阻塞，写去向）。

## 未验证层级（收口时如实列）

- **#804 / #805 生产段**：只在 v2.5.1 自身升级路径上顺带经过一次（old = new），`old_source` 与收敛逻辑未在生产单独注入；生产状态账 / 阶段账（root 0600）对代理不可读，`verified` 只有管理群通知原文 + systemd `Result=success` 两条间接证据。
- **#745**：按 D-13 不做预发真人核对，欢迎卡真实渲染留到下一次有真人触点的版本顺带看。
- **#742**：结论依赖本机 / 探针环境 N = 24（60 分钟时间盒内未到 50），与 CI 运行器时序可能不同；测试侧修法 `b9ad79a5` 只证明未遂形态不再误判，不证明根因消失。
- **#807 生产**：三位返聘者的开通链靠一次性归档脚本清掉旧行后走通；「返聘者旧行归属确认」尚无正式入口（2.5.2 候选）；第 4 人（同人员 ID 多行）未处理，待花名册去重。
- **生产巡检未做项**：「语料读取一次」需授权读者（产品负责人亲跑或留去向）；17 位预开通者截至 2026-09-17 无人首聊，首聊路径在生产仍无真人证据；`qa_corpus.user_result` 生产两行 `unknown`（保守口径）。
- **拉式部署链**：`recover` / `resume-migration` / 回滚未在真机触发（沿 #770 结论）。
- 规划期两项未知已取证：拉取代理不随新版本控制包自升级（S-0-2 ①，G-1b 重装一次）；预发能读花名册快照（S-0-2 ②）。
