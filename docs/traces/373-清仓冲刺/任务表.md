# 任务表：2026-08-28 清仓冲刺（状态=文件当前值，编排者随执行 commit 更新）

时间预算参考（非硬约束，落后即砍范围保主线）：W0 ≤1.5h → H1 ≤5h → H2 ≤5h → H3 ≤8h → H4 ≤3h，留 1.5h 缓冲。

## W0（并行，无副作用；实施者 sonnet，独立 worktree/只读）

- [x] S-W0-1 #359 CPU 高峰根因取证（stage 只读：healthcheck 频率与实现、psql 短连接来源、峰值时间相关性）→ 一页修复方案落 #359 评论
- [x] S-W0-2 #357 用户记忆 MVP 设计短文（D1 范围：表结构、命令面、清除钩子接线点、worker 注入点、跨用户隔离断言）→ 落 #357 评论
- [x] S-W0-3 #354 电子表格真实 API 探针（Bot-Test 受控：创建/写单元格/授权/发送四步真实形状，测试件建即焚）→ 结论落 #354 评论；**若有硬缺口 → S-H3-2 转登记路线（见 H3 备注）**
- [x] S-W0-4 #263 前置盘点（存量重签两前置的实现规格、切换清单草案、biai-prod 可达性核实——预期不可达，只登记不尝试绕行）→ 落 #263 评论

## H1 卫生修复批（epic/h1-hygiene；先 bootstrap PR 刷 Story Fast）

- [x] S-H1-1 #370 测试依赖门控双修（test_document_delivery 加 SDK 门控；真库门控改「DSN∧驱动」双条件清晰跳过）+ 变异验红
- [x] S-H1-2 #371-1 gateway GroupMentionHint 两类拆出（D10；纯移动拆分，行为零变化，复用 #358 拆分批模式）
- [x] S-H1-3 #371-2/7 两处例外登记入代码框架 §二 + 开工必读集体量预算等量裁剪（当前 32768B/32768B 恰好打满，必须先裁后加）；#371-4 门禁摘要更新
- [x] S-H1-4 #371-3 测试资产声明+§五清单补录；#371-8 admin_registry 查询收敛；#371-6 技术设计 JumpServer 清理 + `check_contract_attribution.py` 登记表 3 条同步（动 scripts/ci，批内自然提级，批终本就 Epic Full）
- [x] S-H1-5 #346 发布行值列表翻译接入（翻译层已有，接入开通链发布行；否定用例=未翻译标签整轮拒绝发布不写外部表）+ 变异验红
- [x] S-H1-6 #359 修复实施（按 S-W0-1 方案；改动限 healthcheck/连接姿势，不改业务语义）
- [x] S-H1-7 #284 处置（D7）：A 组 1–4（独立线程/分片心跳按 #340 已落地现状核对后按需、限频整轮预算、_throttle、裸 sleep→`_stop.wait`）+ C 组 7/8/9（token supply 加锁、stop()/join() 接线、消费口径注释化）；B 组 5/6 写模块 docstring；D 组 10 留数
- [x] S-H1-8 批终：本机 full → opus 批量审查 → 修复包 → codex 外审①（不可用则 opus 独立上下文） → Epic Full → 合 main → 关 #370/#371/#346/#359/#284（逐个按回写规则）→ 收口评论+交接评论 → **换会话**

## H2 部署就绪批（epic/h2-deploy）

- [x] S-H2-1 compose 资源限制（6 服务 mem/cpus/pids；worker 按架构 §九容量估算定档）+ `check_deploy_contract.py` 相应断言
- [x] S-H2-2 #343 日志留存 D3（json-file 上限提高+宿主持久收集 30 天窗口；stage 实测收集生效）
- [x] S-H2-3 最小监控告警 D5（宿主健康检查→受控管理群；stage 真实注入 unhealthy 取证一次告警送达）
- [x] S-H2-4 生产 runbook（`deploy/生产部署runbook.md`：首次部署步骤/镜像 digest 固定/单实例纪律/首发不灰度/回滚判据/观察期）+ #135 决策落盘 D4（新决策记录 + #369 台账回写）
- [x] S-H2-5 备份恢复真实演练（stage 一次性隔离实例：恢复→按内容原始写入时间重执行清理核对→销毁；零接触运行库）→ 对应 V-部署 行翻转
- [x] S-H2-6 deploy/README 头部校准 + 架构设计复审校准转「现行」（部署事实、四进程图对齐方案 A、灰度未实现声明；JumpServer 清理与 S-H1-4 合账不重复）
- [ ] S-H2-7 stage 受控升级至本批 main 镜像 + 配置回读绿
- [ ] S-H2-8 批终：同 H1-8（无 codex）→ 关 #343/#135/#369 → 收口+交接 → **换会话**

## H3 功能批（epic/h3-features）

- [ ] S-H3-1 #337 `/admin trace <追溯号>` 只读查询命令（复用方案 A 命令面+audit 查询；能答 failure_reason；输出脱敏；否定用例=非管理员拒绝、查无明确「不存在」）
- [ ] S-H3-2 #354 电子表格交付实施（D2 同构路由+检查点；失败关闭）——**若 S-W0-3 判硬缺口：本步改为探针结论落 #354 并按 Research Result 关闭，不硬做**
- [ ] S-H3-3 #357 用户记忆 MVP（D1 全范围；迁移+命令面+清除钩子+注入；否定用例=跨用户隔离、停用后清除）
- [ ] S-H3-4 三功能 L4a 受控验收（Bot-Test 真实收发逐条取证，含负向出口；记录「受控测试身份」边界）
- [ ] S-H3-5 批终：本机 full → opus 批量审查 → 修复包 → codex 外审②（同①回退规则） → Epic Full → 合 main → stage 升级 → 关 #337/#354/#357 → 收口+交接 → **换会话**

## H4 收尾批（可在主工作树直接小批，或末次 epic 分支）

- [ ] S-H4-1 #330 逐条判定去向表落评论（D8）后关闭；#18 按 D6 回写关闭
- [ ] S-H4-2 #263 执行卡草案落 issue（前置规格+切换清单+回滚路径+观察期；本体标注「留 PM 回归」）
- [ ] S-H4-3 全链路 L4a 冒烟 on 最终 main（开通负向出口、问数一句、文档交付、三个新功能各一击）
- [ ] S-H4-4 文档回写（当前能力新增本 Trace 条目含边界如实；D1–D12 中需长期化的进决策记录；如动用户可见文案按 content.toml 版本纪律）
- [ ] S-H4-5 残留盘点（tz+stage 逐台：临时进程/容器/worktree/一次性实例）+ PM 回归验证清单（D1–D12 追认点、#263 执行卡、L5 待验项、新功能真实旅程建议）+ Trace 终态收口评论 → #373 记 Delivered 等 PM 裁定关闭
