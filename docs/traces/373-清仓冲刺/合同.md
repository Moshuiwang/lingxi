# 开工合同：2026-08-28 清仓冲刺（Trace [#373](https://github.com/Moshuiwang/lingxi/issues/373)）

> 六段式合同（#147 v14 + `~/.claude/templates/trace-kickoff-contract.md`）。PR 合并即产品负责人批准；批准依据：产品负责人 2026-08-28 会话内确认窗口（留痕见 #373 首条裁定评论）。未写进本合同的授权不存在。

## 1. 目标

- **用户结果一句话**：24 小时内清空全部 open issue——15 个中关闭 13 个，产品在功能（3 个新能力 L4a）、卫生（全部登记债清偿）、部署就绪（#369 台账全部可执行项落地）三面同时收口。
- **里程碑**：四批次全部收口合入 `main` 并部署 stage、末批全链路 L4a 冒烟通过、PM 回归验证清单交付。
- **完成定义（Delivered）**：#370、#371、#346、#359、#284、#343、#135、#369、#337、#354、#357、#330、#18 共 13 个关闭；#147 保持开放（长期参考模板，设计如此）；#263 本体不动、其全部可做前置完成并落执行卡草案（PM 回归项）。

## 2. 授权终点

- **编排者可自动执行到**：合并批次 PR ✓；部署 `biai-stage` ✓；关闭 issue（按各 issue 关闭口径与回写规则）✓；`Bot-Test` 真实收发与 L4a 受控验收 ✓；文档回写（产品合同/当前能力/决策记录/技术设计）✓；受控管理群测试通知 ✓。
- **显式除外（本 Trace 内一律不做，不请示、直接不做）**：`biai-prod`、`Bot-Prod`、正式权限表的任何写入；#263 硬切本体（PM 确认窗口明确未选附加授权项）；删除任何用户数据；对外正式发布；花名册/银河上游写入；修改 `.claude/settings*.json` 权限白名单。
- **常设授权引用**：`git-claude-tz push`；gh 机器身份；epic 分支 bootstrap PR 先例（#106/#164）；P2 豁免授权（PM 2026-08-14：不重要 P2 确认后可不修直接合，红线/P1 不豁免，须留痕）。
- **中途裁定方式**：PM 离场 24 小时。既有开放决策按本合同 D1–D12 先行裁定执行；执行中新出现的决策点由编排者按「最小风险 + 可回滚」自裁并在 #373 留决策留痕评论（PM 回归后追认或修改）；**不可回滚、生产、权限边界类一律不自裁**——登记等 PM，不阻塞其余工作。

### 先行裁定 D1–D12（PM 确认窗口批准生效，回归后可改）

- **D1 #357 用户记忆 MVP 范围**：显式登记式（非自动学习）——术语映射、口径偏好、惯例模板三类；按用户隔离存 Lingxi 库；**不存任何数据值**；用户可查可清（`/memory` 命令面）；停用/删除/权限变化触发清除复用既有清除钩子；注入点为 worker 每任务系统提示词侧。合同回写仅登记「已实现的边界」，不承诺自动学习。
- **D2 #354 电子表格交付**：复用 #341 文档交付同构路由——随对话交付、接收人「可管理」授权、所有权留机器人、失败关闭；复用检查点架构。
- **D3 #343 日志留存**：不引入重型日志栈；compose json-file 上限提高 + 宿主侧持久收集（logrotate 卷），取证窗口 30 天（≤90 天合规上限）。
- **D4 #135 生产 secret**：研发/生产凭据集完全隔离；stage 维持 0600 env；生产路线 = docker compose secrets / 0600 env 文件 + OS 级密钥管理迁移路线书面化；生产执行本身留 runbook + 独立 ops 卡，不在本 Trace。
- **D5 监控最小方案**：不引 Prometheus；宿主 cron 健康检查脚本→受控管理群告警（「服务持续 unhealthy 时有人知道」这一条闭环即可）。
- **D6 #18 关闭口径**：方案 A 为现行入口（2026-08-24 裁定），管理 MCP 为触发式未来入口、触发条件已在决策记录与合同——#18 作为工作 issue 关闭，触发时新开。
- **D7 #284 处置**：A 组（1–4）与 C 组（7、8、9）修复；B 组（5、6）写入对应模块 docstring 已知边界后销项；D 组（10）留数即关。第 9 条口径疑问按「supply 侧上界为准」注释化并留痕。
- **D8 #330 关闭口径**：逐条 P0/P1/P2 判定去向（已固化 v14/CLAUDE.md/memory 的销项；仍有效未固化的转 #147 候选评论登记；过时的废弃），全表判定完成即关。
- **D9 台账三件**：#369/#371 各条目完成或明确登记去向后关闭；#370 修复即关。
- **D10 gateway 拆分落点**：GroupMentionHint 节流/应答两类迁出 `apps/gateway/__init__.py`（落点 `core/conversation/` 或 gateway 子模块，实施者择优、审查把关）。
- **D11 部署纪律**：单实例纪律（scheduler/gateway 单实例、重部署先停旧再起新）与「生产首发不做灰度」写入 runbook 与架构设计。
- **D12 结构性例外**：#147 不关闭；#263 本体不做（见授权除外）。

## 3. 不做清单

- 本 Trace 明确不做：#263 硬切本体；生产部署（`biplus-prod`）；L5 真实用户验收；管理 MCP 实现；JumpServer 任何恢复；重型基础设施（Prometheus/消息中间件/K8s）；#147 正文修订（候选只登记到评论）。
- 新发现问题默认只登记不立项（主线灯塔：不做它，13 个关闭是否受阻？）。
- 疑问句只答不动手；产品品味类修订只出草案不落地。

## 4. 验收标准

- 可观察完成标准逐条见[验收.md](验收.md)；证据等级要求：H1/H2 关键路径 ≥L2（CI）+本机 full；H3 三功能 ≥L4a（`Bot-Test` 真实收发取证，含负向出口）；备份恢复演练 L4a（stage 真实执行）。
- 双通道纪律：服务端证据 + 用户可见结果都过才算过；冲突登记等 PM。
- 变异验红硬规则适用（验证与门禁 §八）；变异实测后必须清 `__pycache__`。
- 宣称交付前未验证层级必须列明；L4a 结论须写明「受控测试身份，非生产」。

## 5. 成本预算

- **Epic Full ≤8 次**，仅批次终点消费；批内子改动走批次分支 Story Fast。
- **模型配比**：实施/调查/校验 = sonnet；审查/对抗验证/复杂设计 = opus（每批一次批量审查）。
- **编排者模型 = Fable（`--effort xhigh`），PM 2026-08-28 确认窗口批准**。
- **外部审查**：codex 于 H1、H3 批终各一处（嵌套沙箱不可用时按已登记姿势 stdin 喂证据；仍不可用则 opus 独立上下文顶替并留痕）。
- 大 workflow（>50 万 token）开跑前查配额余量；对抗验证先由编排者 grep 坐实机械项，只派行为面。

## 6. 主线声明

- **主线 = 关闭 13 个 issue**。任何派发/立项/修复先问「不做它，13 个关闭是否受阻」；不受阻的进登记不动手。
- **批次纪律**：默认攒批进 `epic/*` 批次分支（建分支前先 bootstrap PR 刷 Story Fast），批终一次本机 full → opus 批量审查 → 修复包 → Epic Full → 唯一合并人合 `main` → stage 升级回读。
- **接力（v14 §6.8）**：批次收口即换会话。收口评论（UTC 时间戳+预算消耗+提交数+commit 链接）与交接评论落 #373；继任者以 takeover 起手（读三件套+#373 最新交接评论+核实现场）。**元看守 = 规划者会话**：每 30–45 分钟核外部证据（origin/main、#373 评论、tmux 窗口存活），批次收口后拉起继任编排者；同一批次超 45 分钟无任何外部证据（无 commit、无评论、无 CI run）→ 判挂死，capture-pane 取证后杀窗口重拉并附交接摘要。编排者不依赖元看守存在——每步自身留外部痕迹。
- **单一写入者**：同一时刻仓库主工作树只有当任编排者一个写入者；实施 subagent 用独立 worktree；元看守只读（gh/git fetch），不碰工作树。
- **收尾必含**：环境清理与残留盘点（tz 与 stage 逐台：临时进程/容器/worktree 谁建谁清）；PM 回归验证清单（含 D1–D12 追认点、#263 执行卡、L5 待验项）；Trace 终态收口评论。
