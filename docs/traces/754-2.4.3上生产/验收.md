# 验收：2.4.3 上生产（Trace #754）

> 证据等级用仓库 [验证与门禁](../../技术设计/验证与门禁.md) 的 L 口径：L2 = CI 通过；L4a = 真实链路 · 预发 · 代理；L4b = 真实链路 · 预发 · 真人；L5 = 真实交付 · 生产 · 真实用户；代理在生产的只读回读写作「真实链路 · 生产 · 代理 · 只读」，不借 L 名。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末。**判定字段只认系统写出的值；前置状态允许人工构造；答不出「这个值是谁写的」即无效判据。**

## 一、逐 E 关卡

| E | 步骤 | 可观察完成标准（缺一不算） | 目标 | 现值 | 证据 |
| --- | --- | --- | --- | --- | --- |
| E-1 | S-1-1 | ① `pyproject.version == "2.4.3"`；② `CHANGELOG.md` 有 `[2.4.3]` 节且逐条可回溯到 #698 / #726 / #732 的用户可见变化；③ runbook §七有「重部署期间管理群会收到一组『容器不存在 → 已恢复』」一句；④ runbook §二为本批升级补段（0090 → 链头、0091 / 0092 单向门、0095 降级丢状态、回滚 = 切镜像不降级）；⑤ `verify_docs.sh` 绿 | L2 | — | — |
| E-2 | S-1-2 | ① 新迁移（链头 +1）后**全部** PL/pgSQL 函数 `pg_proc.proconfig` 含 `search_path`（分诊实读：21 个函数中 19 个缺，含 0095 新增 2 个）；② 函数体内未限定的表引用全部限定（分诊实读 5 个函数 7 处）；③ 清理函数对 `anon` / `authenticated` 的直接 EXECUTE 已 REVOKE，`pg_default_acl` 不再给二者函数 EXECUTE；④ 隔离正负例：搜索路径被改成恶意 schema 时函数仍访问 `public` 对象（正例）、旧定义在同一注入下访问到假表（负例，证明测试能分辨）；⑤ 往返：`upgrade head → downgrade -1 → upgrade head` 在有数据的库上通过，行数不变；⑥ 旧镜像新 schema：`verify_old_image_new_schema.sh` 对本批真实迁移通过；⑦ 变异验红：删掉 `SET search_path` 那一行，④ 的正例必转红；⑧ `docs/技术设计/数据库设计.md` 立「新 PL/pgSQL 函数必须 SET search_path」规则，`scripts/ci/` 有静态检查且对不带 `SET search_path` 的新函数判红；⑨ 现有测试全绿、函数行为零变化（0095 触发器的 E-18 ② 往返用例仍过）；⑩ 临时 `GRANT … WITH SET TRUE` 若有，迁移内 `REVOKE … GRANTED BY` 回收并有用例钉住 | L2 + 变异 | — | — |
| E-3 | S-1-3 | ① 脚本源改为 DSN `pg_dump -Fc`，不再 `docker exec`；② 预检 `pg_isready`；③ 恢复 `pg_restore --no-owner --no-privileges -n public`，目标镜像 `postgres:17`；④ 恢复后 `alembic upgrade head` 到链头；⑤ 进程内跑 scheduler 已装配的四项保留职责（不带任何外部凭据）；⑥ 本机对容器库跑通一次：备份 → 恢复 → 迁移 → 补清理 → 行数与到期行回读；⑦ runbook §八与 11.6 勘误（托管备份 2026-09-02 已开、手工 dump 已停、演练脚本现状）；⑧ 明文 dump 落盘位置与销毁步骤写在脚本头 | L1 | — | — |
| E-4 | S-1-4 | ① `docs/当前能力.md:162`（追溯）与 `:175`（扩员）按 #709 / #704 / #732 证据改写，等级用词只用 L4a / L4b，无裸 L5；② `验收矩阵-会话与执行.md` V-会话-14、`-接入与管理.md` V-管理-40 与 V-管理-49…55、`-部署与迁移.md` V-部署-14 现值升级并附指针；③ 未验证项（SSH forced-command 实走、未通过不发负例、名单外首聊、回退演练、20 人真实批次、`/admin trace` 飞书入口、三段日志回读）逐条列在各分册「未验证」；④ `deploy/README.md:479-481` 的 `up -d` 补 `--profile mvp`；⑤ `verify_docs.sh` 绿、`check_docs_wording.py` 零命中 | L2 | — | — |
| E-5 | S-1-5 | ① 本机完整门禁绿（`Ran N tests` 可见）；② 内审一轮 + 定向复核一轮记录在 Trace Issue；③ 二审 Fable 与外审 codex 对同一候选 SHA 各出一份记录（台账逐次登记）；④ CI `Epic Full` 绿，`gate` 最长单步耗时 / 900 秒登记；⑤ 合并提交带镜像证明（`Image-Candidate: true` 在最后一个提交）；⑥ Fable 把关「可合并」 | L2 | — | — |
| E-6 | S-0-3 / S-0-4 | ① 2.4.3 里程碑存在且描述写明「真正到达用户的那一版」；② 每张单的移入 / 移出 / 关闭在该单评论有原因与证据链接；③ 2.4.1 与 2.4.2 GitHub 回读 open = 0（#732 若仍由守望者持有则如实登记为例外）；④ #566 三张拆单已建并链接原单 | GitHub 对象回读（不适用 L） | — | — |
| E-7 | S-2-3 | ① `release-manifest.json` 的 `commit` = `release/2.4` 合并提交、`tree` = 该提交树、`run_id` 为成功的 `Release Candidate` run；② 四镜像 digest `docker manifest inspect` 均可解析；③ 清单迁移头 = W1 链头；④ push 步骤摘要无「降级留证」warning；⑤ 预发布 `prerelease=true`、`immutable=true` | GitHub 对象回读（不适用 L） | — | — |
| E-8 | S-2-4 | ① `down` 前新取库备份并回读大小；② #732 留驻物按裁定处置并回读不存在；③ 口令轮换后三服务 `Up (healthy)`、`RestartCount` 0、日志有连库成功，主机上其他持旧 DSN 的进程 / 脚本已盘点处置；④ 运行镜像 `RepoDigests` 逐一等于 rc 四 digest；⑤ 迁移头 = 链头，既有业务行数逐表不变（`task` / `app_user` / `inbound_event` / `admin_action_followup` 四表）；⑥ 所有回读经脱敏助手，会话输出零 DSN 片段 | L4a | — | — |
| E-9 | S-2-5 | ① 预发库 DSN `pg_dump -Fc` 成功、`pg_restore --list` 可读；② 隔离 `postgres:17` 容器恢复成功；③ `alembic upgrade head` 到链头；④ 补清理跑完，到期内容按**原写入时间**清掉（构造一条到期行验证）；⑤ 行数回读与源库一致（清理前）；⑥ 容器与 dump 销毁并回读不存在；⑦ 演练记录（时间、耗时、差异）写回 runbook §八（W5 docs PR） | L4a | — | — |
| E-10 | S-2-7 | ① `validate_acceptance` 本机通过；② `previous_tag` = S-0-2 回读的现网正式版本；③ 恢复说明写明「切回现网 tag + 四 digest、不降库；旧镜像跑新库结构只有 CI 合成证据」；④ 产品负责人批准并合并；⑤ `Release Promotion` 成功，`v2.4.3` `prerelease=false`，四 digest 与 rc 逐一相同 | GitHub 对象回读（不适用 L） | — | — |
| E-11 | S-3-1 | ① 在途 `0|0|0`；② 工作副本 `status --porcelain` 空、`.git/index` 属主 `bi-ai-deploy`；③ `resolve --environment production` 成功且拒 rc（拿正式版）；④ `.env.example` 键差异 = 0（本批实读）；⑤ `pg_dump --version` ≥ 17；⑥ `~/backups/` 可写、剩余空间足够；⑦ 管理群告警链路当天有心跳或最近一条告警时间；⑧ 自检贴 Trace Issue 且产品负责人已放行 | 真实链路 · 生产 · 代理 · 只读 | — | — |
| E-12 | S-3-2 / S-3-3 | ① 备份文件存在、大小合理、`pg_restore --list` 可读；② 步 0：`rev-parse HEAD` = 候选提交；渲染回读含本批 compose 差异；③ `.env.prod` 原文件备份为 `.env.prod.before-243-<tag>`；④ 拉取后四 digest 逐份比对一致；⑤ `run --rm migrate` 退出码 0，`current` = 链头；⑥ `up -d` 后三服务 healthy；⑦ 运行 `RepoDigests` 逐一等于正式版四 digest | 真实链路 · 生产 · 代理 · 写入（回读只读） | — | — |
| E-13 | S-3-5 | ① 默认干跑（不带 `--apply`）打印四来源计数给产品负责人过目（不含标识）；② `--apply` 回填人数 = 干跑计数；③ 四列人数回读；④ 再跑一次零新增；⑤ 审计行存在（若 #751 已修）或如实登记不可见 | 真实链路 · 生产 · 代理 · 写入（回读只读） | — | — |
| E-14 | S-3-6 | ① 产品负责人用 Bot-Prod 发一句问数，机器人先出进度后出结果；② 最新 `task` 行 `status=succeeded`、时间对上；③ 15 分钟窗口三服务 healthy、无非预期重启、既有行数不变、长连接建立、启动日志逐条回读（固定项 1/2/3/6） | L5（① ②）+ 只读（③） | — | — |
| E-15 | S-3-7 | ① 24 小时内三服务无非预期重启（定时型机制回读）；② Data API 仍关闭（只读回读）；③ `pg_proc.proconfig` 19 函数含 `search_path`（只读 SQL）；④ 无新的 `42703` 类错误（日志 grep） | 真实链路 · 生产 · 代理 · 只读 | — | — |
| E-16 | S-4-2 / S-4-3 | = #748 E-1 … E-5 原文（自证闭环堵死 / 删调用不能绕过 / 普通 PR 不受影响 / 属主规则可撤 / 留痕），每条造 PR 实测、用完即关 | L2 + 实测 | — | — |

## 二、版本验收单 v2.4.3（#699 形态：固定项 + 随版本项）

**固定项（每版都跑）**

| # | 项 | 预发（rc 四镜像） | 生产（正式版） |
| --- | --- | --- | --- |
| F-1 | 三服务起得来且健康检查转绿 | 代理，E-8 ③ | 代理只读，E-12 ⑥ |
| F-2 | 迁移到位且既有业务行数不变 | 代理，E-8 ⑤ | 代理只读，E-12 ⑤ + E-14 ③ |
| F-3 | 长连接建立（`connected to wss://`） | 代理 | 代理只读 |
| F-4 | 一次正常问数端到端成功 | **真人**（时段 C） | **真人**（时段 E，L5） |
| F-5 | 进程重启后在途任务接上同一 task 且唯一送达 | 沿用 #603 / #709 证据（W1 不触及队列与投递路径，差异分析写进验收记录） | 不在生产制造中断（未验证层级） |
| F-6 | 启动日志逐条回读：哪些职责注册了、哪些因缺配置不注册 | 代理 | 代理只读 |

**随版本项（来源 = 三个版本 PR 正文的「用户可见变化」+ W1）**

| # | 用户可见变化 | 来源 | 做什么 / 看到什么算过 | 谁 / 哪里 | 不可达时 |
| --- | --- | --- | --- | --- | --- |
| V-1 | 问数失败带可复制追溯号，三个容器都能按号查 | #698 / #726 ③ | 预发：对库里既有失败任务号在三个容器各跑一次 `python -m lingxi.apps.trace`，输出同一份记录且不含正文 | 代理，预发；生产只读一次 | 生产无失败任务 → 只验 CLI 可用 |
| V-2 | 受限内测入口装配两态日志 | #726 ① | 预发（叠 innertest 覆盖）启动日志「已注册」一条；生产（不叠）启动日志「因缺配置未注册」一条 | 代理 | — |
| V-3 | 名单工具 `verify` 回读模式 / 版本 / 成员数 | #726 ② ⑤ | 预发 `verify` 输出与库一致 | 代理，预发 | 生产不切动态名单 → 不适用 |
| V-4 | 「谁跟我们说过话」四列 + 回填幂等 | #732 E-17 / E-18 | 预发：rc 部署后干跑计数与库四列一致、`--apply` 零新增；生产：E-13 | 代理 | — |
| V-5 | 后台操作重启后可继续、三种结局分别显示 | #698 | 沿用 #603 预发实测（W1 不触及） | — | 生产不制造管理动作中断（未验证） |
| V-6 | 管理员本人确认卡、授权变化后明确拒绝、就绪检查 | #698 / #726 ④ ⑥ | 沿用 #704 Q02 / Q04（候选 `93daa3ba`，W1 不触及） | — | 生产不打开受限入口（未验证） |
| V-7 | 库抖动时告警不挂、常驻进程不退出、投递链路故障单独一格 | #732 E-9 | 沿用 #732 预发断网实测（W1 不触及） | — | 生产不注入故障（未验证） |
| V-8 | 五条用户可见表达（忙时未受理 / 已排队拆开等） | #732 E-8 | 沿用 L2 | — | 未验证层级 |
| V-9 | 文档产物缝隙态不重复创建 | #732 E-18 ⑤ | 沿用 | — | 未验证层级 |
| V-10 | 数据库函数 `search_path` 固定（#661） | W1 | 预发与生产：`pg_proc.proconfig` 只读 SQL 全部命中 | 代理 | — |

**真人清单（预发，时段 C，一张评论）**：① 轮换预发库口令并改 env（动作）；② 发一句问数 → 回「有结果 / 没结果」。**真人清单（生产，时段 E）**：① 一句「放行」；② 过目 #673 dry-run 计数；③ Bot-Prod 发一句问数；④ 管理群看到部署期间一组「容器不存在 → 已恢复」属预期。

## 三、批终链

W1：本机完整门禁绿 → 内审（opus）批量审查一次 → 统一修复包 → 同一审核者定向复核一次 → 二审（Fable）+ 外审（codex）各一份 → 冻结候选 → CI 完整门禁 → Fable 把关 → 合 `main`。冻结后回填：候选 SHA `________`；`gate` 耗时 / 上限 `________`；门禁 run `________`。
W2：同步 PR → `release/2.4` → Release Candidate run `________` → `v2.4.3-rc.N` = `________`；四 digest `________`；预发部署回读 → 验收记录 PR `________` → Promotion run `________` → `v2.4.3` 四 digest 与 rc 相同 ☐。
W3：放行时刻（UTC / 北京）`________`；备份文件 `________`；迁移头 `________`；`.env.prod` 备份 `________`；观察窗口结束时刻 `________`。

## 四、未验证层级（收口时如实列）

- 旧镜像（v2.3.1）跑在新库结构（0091–链头）上的兼容性：只有 CI 合成迁移证据，无本批真实回滚演练。
- 固定项 F-5 与随版本项 V-5 / V-6 / V-7 / V-8 / V-9 在 rc 四镜像与生产上均未重验，沿用 pr-750 / pr-726 证据 + 差异分析。
- #673 失败结局（「联系不上」→ 通知管理员）在生产未触发。
- #661 F 的 24 小时观察只覆盖一个自然日。
- （收口时补）

## 五、输入 Issue 准入与结果可达性（14 张）

| Issue | 里程碑现状 | 分诊判定 | 本批处置 | 关闭去向 |
| --- | --- | --- | --- | --- |
| #704 | 2.4.1 | 三项通过、Q03 不可验 | S-0-4 | 关闭（裁定 6） |
| #700 | 2.4.1 | 决定已执行、#692 已关 | S-0-4 | 关闭（裁定 7） |
| #699 | 2.4.1 | 裁定已定、验收单未落地 | 本文 §二即验收单 | 移 2.4.3，执行完关闭（裁定 8） |
| #661 | 2.4.1 | 仍成立（19 函数） | S-1-2 + S-3-7 | 移 2.4.3，本批关闭（裁定 1） |
| #693 | 2.4.1 | 仍成立（非硬阻塞） | S-1-3 + S-2-5 | 移 2.4.3，本批关闭（裁定 2） |
| #657 | 2.4.1 | 部分兑现（余 docs） | S-1-4 | 移 2.4.3，本批关闭 |
| #662 | 2.4.1 | 大半兑现 | S-0-3 | 关闭；四小项并一张 2.5.0 ops 卡 |
| #668 | 2.4.1 | 主线已兑现 | S-0-3 + S-1-4 | 关闭；#735 / SSH 通路另挂 |
| #566 | 2.4.1 | 部分兑现（D4–D6 未） | S-0-3 | 拆三单 → 2.5.0，原单关闭 |
| #748 | 2.5.0 → 2.4.3 | 未开工 | W4（裁定 5） | 关闭或留 2.4.3 |
| #712 | 2.4.2 | 留开写去向（#743） | S-0-3 | 移 2.5.0 |
| #735 | 2.4.2 | 设计题；可缩成验收方法 | S-0-3 | 移 2.5.0 |
| #732 | 2.4.2（Complete） | 守望者持有复盘 | S-0-3 登记 | 守望者关闭；不阻塞本批 |
| #673 | 2.4.2（已关） | 生产回填未做 | S-3-5 | 回填留痕在 #673 评论 |
