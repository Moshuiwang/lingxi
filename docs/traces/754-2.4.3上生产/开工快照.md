# 开工快照：2.4.3 上生产（Trace #754）

> 按 `#678` §二与 `docs/traces/README.md:18`：开工能力与环境快照类内容放本文件。**授权、验收门槛、已裁定事项、共享资源租约、外部写与恢复、各道 Gate 一律留在 `合同.md`，不得移出以绕过批准。**

## 合同区字段（`METHOD.md` §八）

- 方法版本 **v19**；合同版本 **v1**；执行阶段 **Ready**（本 PR 合并即 Ready）；整体交付结果 **Undetermined**。
- **执行者与运行面**：Claude Code 运行面（`#678` §四「Claude Code 运行面」）；执行机 tz（Ubuntu 24.04，用户 `wangzp`）；**本机时钟是 UTC**，面向产品负责人的时刻一律 UTC + 北京（+8）双标注且两个值都实读（`date -u` 与 `TZ=Asia/Shanghai date`）。
- **基线提交与核对时间**：`origin/main` = **`e6bdea8`**（2026-09-11 08:3xZ 实读）；`origin/release/2.3` = `e838471`（= v2.3.2）；主干领先 `release/2.3` 220 个提交，新增迁移 `0091`–`0095`；`07d01be`（预发在跑的 PR #750 候选）到 `e6bdea8` 除 `docs/traces/` 外零差异（`git diff --stat 07d01be..e6bdea8 -- . ':!docs/traces'` 为空）。**生产现网按仓库记录为 v2.3.1 / `20260907-71fcc88e7059` / 迁移头 `0090`**（#606 收口评论 5593933177；`当前能力.md:139/143`；#669 评论 5595436970「未部署生产」）——与守望者交接里的「生产 2.3.2」不一致，**S-0-2 只读回读定夺，以现场为准**。
- **规划者任务 / 会话与停止点**：规划者会话（工作树 `/tmp/lingxi-243-plan-20260911`，分支 `plan/trace-754-2.4.3`）。停止点 = 本合同 PR 合并；之后只读待命供合同修订，不实施、不持编排权与合并权、不转任守望者（守望者仍是 `lingxi-242-plan` 窗口）。
- **角色模型 / 推理档位**：同合同 §5。
- **当前顶层编排任务 / 会话**：未指派——由守望者在 G-1 满足后拉起 `243-w0` window 并完成接管登记。
- **编排者接手入口与第一恢复点**：`/takeover` → 读本三件套 + Trace Issue 最新交接 / 裁定评论 → 核实现场 → 接管登记；**第一恢复点 = `任务表.md` 勾选 + Trace Issue 最新评论**。

## 开工能力与环境快照

| 项 | 值 | 来源 |
| --- | --- | --- |
| 发布路径 | `publish.yml` 触发于推送 `release/**`（候选 job 用 `verify_epic_candidate.py` 要求该提交恰有一个已合并的、base 为该 release 分支的 PR）；`release.yml` 手动 `workflow_dispatch`（分支 main，填候选标签） | 仓库实读 |
| 机器人权限边界 | 推 `release/**` 被拒；`gh workflow run release.yml` 403；`accept/` 前缀分支可推；`deploy/releases/acceptance/`、`docs/traces/*/合同.md`、`.github/workflows/` 受 CODEOWNERS | #669 评论 5595436970；`.github/CODEOWNERS` |
| 生产执行通路 | tz 主检出的本地代理配置现存生产命令放行规则一条（另有 `gh pr ready` / `gh pr merge` 与预发 ssh 的登记项）；编排工作树无本地代理配置；分类器拦复合命令与自扩白名单 | 2026-09-11 实读 |
| 预发 | 三服务留驻 `pr-750-07d01befcfc2`（库 `0095`）；`pr-747` 一组镜像；两份库备份 + 两份 env 备份待处置；口令一次进过会话输出 → 本批轮换 | #732 收口 §七 |
| 生产备份形态 | 手工每日 `pg_dump` 于 2026-09-02 停止、Supabase 托管备份同刻开启；`~/backups/` 只有上线前一份 | #693 分诊（Trace #544 / #592 任务表） |
| CI 成本 | `gate` 8.5–10 分 / 15 分上限；`image` 4–5 分；纯 docs PR 约 13 秒全 skipping；验收记录 PR（`deploy/**` json）实测跑了 gate + extras + image | 记忆与 #687 实读 |
| 首次建 `release/2.4` | 分支创建推送可能触发一次判红的 `Release Candidate` run（该提交无合并 PR），属预期，不计预算 | `verify_epic_candidate.py:44-58` 推断，**未实测** |

## 批后继任条件（命中）

六个阶段必然跨会话接力。继任：读本三件套 + Trace Issue 最新交接评论 → 核实现场（worktree、阶段分支 tip、PR 状态、预发与生产现状）→ 接管登记 → 前任确认停止写入并释放租约 → 退场。**未确认转移前保持等待，不得出现两个写入者。** W3 窗口内不换编排者。
