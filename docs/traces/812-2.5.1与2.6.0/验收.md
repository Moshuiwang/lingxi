# 验收（Trace #812 · 2.5.1 与 2.6.0 两版连发；逐 Epic 可观察完成标准 + 证据等级目标 / 现值；证据落 Trace Issue 或 PR）

> 证据等级口径见 `docs/技术设计/验证与门禁.md`（L 名 = 成熟度 · 环境 · 执行者；全局 1–7 级对照见该文第三节）。每项完成把「现值」改成实际等级并附指针；未验证层级如实列在文末，不得为了收口好看而标绿。**判定字段只认系统写出的值，前置状态允许人工构造；答不出「这个值是谁写的」即无效判据。**

## 逐 Epic 关卡

| Epic / 项 | 可观察完成标准（判定值由谁写出） | 目标等级 | 现值 | 关联验收矩阵 | 证据 |
| --- | --- | --- | --- | --- | --- |
| **E-1** #804 old / new 配置指纹 | ① 回归用例：版本 A 部署（指纹 X）→ 目标配置改 Y → 版本 B 计划 old = X、new = Y，不报 `unplanned_service_artifact_or_config`；② 篡改旧容器标签 / 制品仍被拒（否定用例）；③ old / new 在 plan / audit / 错误码可区分；④ 预发真实链路：rc.A `verified` → 人工改 `public-config.json` → rc.B 计划以 X 核 old、以 Y 描述 new → `verified`（判定值 = 部署器状态账与计划文件，人工构造的只有配置改动）；⑤ 取不到可信旧值时失败关闭、不把 desired 冒充历史值 | L2 → 预发 L4a | — | V-部署-（编排者登记） | — |
| **E-2** #805 prepare-only 收敛 | ① 阶段意图先持久化再进副作用阶段；② prepare-only 失败 + 五条件全成立 → 失败计划归档、在途标记复位到上次 `verified`、下一计划正常部署；③ `stop` 及之后失败、意图已记录、旧执行者仍活、标记属其他计划、两个恢复者并发 → 不清理 / 不双重部署（用例逐条）；④ 崩溃注入（意图持久化前后、动作前后、完成记录前后）不误清；⑤ 收敛不产生新的 `verified` 记录，审计含失败 plan id、阶段、占用者、收敛依据；⑥ 预发注入一次 prepare 失败 → 自动收敛 → 下一版本 `verified`（判定值 = 状态账、`host.active.json`、审计行） | L2 → 预发 L4a | — | V-部署-（编排者登记） | — |
| **E-3** #739 umask | 同一候选 `umask 0002` 与 `0022` 下两条目标用例结论一致（两次都实跑、各留 `Ran N tests` 行）；显式安全 mode 通过、显式 group / other 可写 mode 判红；生产权限判据零改动（diff 证明）；相邻部署测试扫描无同型依赖或另行登记 | L1（双 umask）+ L2 | — | — | — |
| **E-4** #807 唯一在职身份 | ① 五个按邮箱定位的入口共用同一 resolver（代码引用证明，不复制判定树）；② 五态用例：唯一在职 / 全非在职 / 多在职冲突 / 未找到 / 快照不可用或字段未知，外加已有绑定与新身份不一致；③ 不可用快照不折算 0 在职、不自动授权、不触发批量撤权；④ 邮箱复用不静默转移旧 token / 会话 / 本地授权 / 历史数据；⑤ 审计含快照版本、候选数、在职数、最终原因；⑥ 预发两个真实失败入口（扩员准备、预开通）回归通过（判定值 = `operation_audit` / 扩员批次状态，样本邮箱为人工前置）；⑦ 生产 4 人按新规则重处理，逐人审计终态（G-3）；⑧ 产品合同 / 当前能力回写身份解析与生命周期边界 | L2 → 预发 L4a → 生产「真实链路 · 生产 · 真人 / 代理 · 写」（G-3） | — | V-权限-（编排者登记） | — |
| **E-5** #756 ③④ | ③ 正常代表任务下 RSS / cgroup 峰值样本（候选、环境、方法、脱敏样本）；④ 停 queue → 重启 gateway → 恢复 queue → 同一 task 最终送达且唯一（判定值 = task 表终态与投递记录）；文档回写 | L4a | — | — | — |
| **E-6** v2.5.1 上生产（拉式） | `release/2.5` 候选四镜像 digest 与正式版逐一相同（清单由 CI 写）；预发由拉取代理自动部署 rc、版本验收单固定项全过、验收记录 PR 合入 main；产品负责人点 Release Promotion 后生产拉取代理自动部署；生产迁移头 = 链头；三服务 healthy 且 15 分钟无非预期重启；既有业务行数不变；**产品负责人 Bot-Prod 一次真实问数 `succeeded`**（由队列执行服务写入 task 表） | **L5（只认真人）** | — | — | — |
| **E-7** #748 剩余验收 | E-4 后半：撤属主行后再做一次同名伪造 PR 被拒 + 路径 b 复跑判红（真实 PR run）；E-1 ④：一个不改裁决核心的最小工作流 PR 无需产品负责人批准即合并（合并记录）；E-1 ⑤：规则集临时去掉 `Epic Verdict` 行 → 合并 → 加回，前后规则集 JSON 逐字相等（G-2；不放行则留去向）；「门禁定义」保护边界与不改 YAML 变更的既有测试证据登记；验收文档回写附候选 SHA / run | L2 + 真实 PR 实测（全局 4） | — | — | — |
| **E-8** #735 收口 | 「申请使用」是否真实授权来源有产品负责人一句话或飞书后台只读核实留痕；单账号对照证据（#799 A-5 / C-3、#770 5674442737 / 5675123356）与 apply 专属复查测试类（`tests/test_outreach_ops.py::StateAtSendTest`）及 CI 来源链接进验收文档；文档不再写「没有方法 / 从未验证」，也不扩大宣称 | 文档对账（全局 1）+ 既有 L4b 引用 | — | V-触达-（编排者登记） | — |
| **E-9** #742 复现量测 | 固定提交 + 固定环境连续跑 N = 50（或 60 分钟时间盒）：总次数、失败次数、失败分布、被测状态与时间字段、原始断言全部留档；有直接证据 → 修法读系统写出的状态 / 时间基准 + 正反例；无 → 如实「当前未能复现（提交 / 环境 / 样本量）」 | L3（本机 / 探针） | — | — | — |
| **E-10** #745 示例交集 | 用例：单公司不变、多公司有交集只取交集、无交集降级文案 / 不生成、部分独有指标不出现在「各公司」示例；用户原始查询权限与公司特有指标查询能力不变（否定用例）；预发欢迎卡受控真实渲染一次由真人 / Codex 验证请求核对（判定值 = 卡片正文，Bot-Test） | L2 → 预发 L4b | — | V-触达-（编排者登记） | — |
| **E-11** v2.6.0 上生产（拉式） | 同 E-6 判据，对象换成 `release/2.6` 候选与正式版 v2.6.0；**产品负责人第二次真实问数 `succeeded`** | **L5（只认真人）** | — | — | — |
| **E-12** 里程碑与工作单收口 | 2.5.1 与 2.6.0 里程碑 open = 0 或余项各有去向评论；四条部署链改道事项有登记单；CHANGELOG `[2.5.1]` / `[2.6.0]`、当前能力、验收矩阵现值、`deploy/拉取代理.md` §十二 回写 | — | — | — | — |

## 批终链（每个批次全绿才进下一阶段）

本机完整门禁绿（tmux 窗口独占跑，`umask 022`）→ 唯一独立审核者 fable xhigh 批量审查（一次，审查卡带威胁模型与真机语义清单）→ 统一修复包 → 同一审核者定向复核（一次）→ 外审（codex `gpt-6-astra` high 绕过面 + agy `gemini-3.8-flash-high` 用户可见后果，各一轮）→ 把关（W1 批次 PR 与 2.6 首次同步 / #748 探针 PR 用 fable xhigh，其余 Opus xhigh）→ 冻结候选 → CI 完整门禁 → 合 main。冻结后回填：**W1**：候选 SHA `________`；门禁 run `________`；外审登记行 `________`；合并 `________`。**W3**：候选 SHA `________`；门禁 run `________`；外审登记行 `________`；合并 `________`。

## 版本验收单（每版预发一次、生产固定项一次；E-6 / E-11 用）

固定项：① 迁移头 = 链头；② 三服务 healthy 15 分钟；③ 既有业务行数不变；④ 一次真实问数 `succeeded`；⑤ 四镜像 digest 与正式版清单逐一相同；⑥ 部署由拉取代理 + 部署器完成的留痕（计划 / 执行 / 状态三份记录）。

- **v2.5.1 随版本项**：#804 真实链路（rc.A → 改配置 → rc.B `verified`，old / new 可读出）；#805 prepare-only 注入一次自动收敛；#807 预发两入口回归；#739 双 umask 结论一致；#756 ③④（未完成不阻塞，写去向）。
- **v2.6.0 随版本项**：#745 欢迎卡一次受控渲染核对；#748 E-1 ④ 探针 PR 合并记录；#742 复现记录链接；#735 验收文档对账链接。

## 未验证层级（收口时如实列）

- 规划期全部为静态回源与只读回读；预发与生产环境现值（预发是否仍停、生产迁移头、4 人现状）本次未登机，以 S-0-1 实读为准。
- 拉取代理自身是否随新版本控制包升级、预发能否读花名册快照两项为**未知**（S-0-2 取证后回填）。
- #804 / #805 的修法只在本机用例与一次预发真实链路上验证，生产侧只在 v2.5.1 自身升级路径上顺带经过一次（不另做生产故障注入）。
- #742 的结论依赖样本量 N = 50 的本机 / 探针环境，与 CI 运行器的时序可能不同，如实标注。
