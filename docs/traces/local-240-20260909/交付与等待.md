# 2.4.0 当前交付与等待

四项代码和本地适用检查已经完成；完整线上 CI 尚未实际跑通，当前不是本轮 Complete，仍停在合并前。

| 用户结果 | 当前证据 |
| --- | --- |
| 确认过的后台工作重启后可继续，结果不明保持待核实 | 持久事务、真实进程故障、20/120秒退出和共同资源验证通过 |
| 问数失败可复制号码，管理员在原期限内查询 | 关联、九正三负、并发、两入口与旧新同库验证通过 |
| 批量内测由本人确认，逐人查看真实状态，未就绪不通知 | 合成真库、Linux三工具/身份/权限/停止、期限否定与恢复验证通过 |
| 部署先有计划，断线按同一计划接续，恢复单独批准 | 固定包双次一致、实际只读安装、真实wheel恢复与状态核验通过 |

## 固定代码与本地检查

修复代码候选：`f0508309c6314b504fe5e9bf3457dea15990ef68`；完整运行代码检查对应前一候选`570814a9f95570de0da73e00cfd83a6a9d38aa5a`，两者运行代码、迁移与依赖完全相同。分支：`codex/2.4.0-execution-isolated-20260909`。后续如仅提交收口记录，分支头与代码候选分开记录；原代码提交保留不改写。

- 原仓库完整检查：6343项套件成功，314.036秒，3条root权限用例另用非root模块6条全部补验，无SDK守卫触发。
- 原迁移链：37条真实往返、两种基线等价、前滚与幂等通过，临时数据库全部清理。
- 真实退出：同一候选20.065秒/120.073秒，重复信号不延长截止；其超时提示来自合成故障注入。
- 实际2.4.0 wheel摘要 `d0f46ba9e799c550185d0b8972701a790ae3045c8ee5d016e2416bc9a91da0f2`；273个运行文件与集成源码逐字节一致。它是本地验证制品，不是已发布镜像。
- 机器可读证据与日志摘要见[运行证据](运行证据.json)，详细交付见各实施记录。

## 当前线上检查修复

指定GitHub App能读取Epic Full工作流，但创建运行返回HTTP 403 `Resource not accessible by integration`；App首次回读运行列表为空。随后用户手动触发run34316312942，精确命中代码候选，但classify及主体作业全被跳过，汇总正确失败。调度修复已整合，集成55条定向、相关静态及三处实施变异验证通过；仍待新候选实际完整运行。

修复和本地验证已完成，提交推送后的精确运行候选由元守护提供；请产品负责人在 [Epic Full](https://github.com/Moshuiwang/lingxi/actions/workflows/ci.yml) 选择本轮隔离分支手动运行。触发后编排者只读核对实际head SHA、各作业终态；若有红项继续原实施者修复和必要重验。未默认申请扩大App权限，不换个人身份、不创建PR绕过。观察入口是该工作流的新分支运行列表。

#661/#662和全部真实管理员、飞书、stage、正式提升、生产首用条件继续保留。本轮未执行线上Issue写入、PR、合并、发布或真实业务操作。其他执行者曾合入main到旧执行远端分支，本任务保留其原状并改用新隔离分支；不宣称全仓main没有别人修改。

SDK测试偏差、外部推送中断、准确清理和时间/token来源均在[任务表](任务表.md)及对应实施记录中。原始输入基线和Issue JSON按用户对照要求保留；没有启动DeepSeek或创建比较任务。
