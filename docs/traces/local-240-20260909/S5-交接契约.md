# S5 部署工具与控制包交接 v1

本地 Ready 记录，不是新产品合同，不授权真实安装/发布。唯一授权仍为本目录合同 v1.0 和固定 #566 来源。

## 已有前置

- #603 阶段恢复与统一退出基础已经整合；`PostgresFollowupStore.recovery_status()` 返回 contract_versions/compatible/inflight/recoverable/unknown，不能把 unknown 改成未发送。S6仍需实际隔离恢复和过程故障验证。
- #668 固定接口源提交 `8a29082`，集成提交 `343753f`；真实Linux客户端、UID/ACL/映射/容量/退出证据已在668实施记录。名单/批次业务仍由668完成，D1/D2不得因此伪称已经全部可运行。
- 安装协议正文为[内测管理通道安装契约](../../技术设计/内测管理通道安装契约.md)。MCP协议2025-11-25，通道schema_revision=1；relay与安装自检均宿主Python>=3.11标准库，无pip。scheduler持handler，仍四业务镜像。

## 控制包范围冻结

发布清单新schema固定为2；控制包索引schema_revision固定为1，独立于通道schema_revision=1。旧发布清单schema1只按显式历史/兼容核对处理，不能当作带新控制包的新目标通过。正式提升保留同四镜像与同控制包摘要，D4/D5/D6仍不执行。

白名单v1只包含以下源码或待实现路径；不是把尚未存在的文件说成已交付。构建时缺任何已声明条目必须拒绝，文件内容/模式/来源SHA均进入索引。不扫描打包整个目录，不包含.env私有文件、凭据、公钥绑定实值或用户数据。需要新增纯实现辅助文件时由编排者先修订本地白名单记录，不扩大工具范围。

| 路径 | 职责 / 来源 |
| --- | --- |
| `deploy/lingxi_deploy.py` | 待实现；唯一四操作入口 plan/apply/status/recover |
| `deploy/deploy_state.py` | 待实现；计划/批准摘要/原子状态/主机锁 |
| `deploy/deploy_runtime.py` | 待实现；固定命令执行、状态回读、安装/恢复步骤 |
| `deploy/control_bundle.py` | 待实现；固定白名单打包、摘要与安全解包/只读目录切换 |
| `scripts/ci/release_manifest.py` | 既有固定Release解析，扩展schema2和同包提升 |
| `deploy/compose.yaml` | 既有基础Compose，保持四镜像/三常驻服务 |
| `deploy/compose.stage.yaml` | 既有stage外部配置装配 |
| `deploy/compose.prod.yaml` | 既有生产外部配置装配 |
| `scripts/admin/innertest_relay.py` | 668已交固定标准库字节relay |
| `scripts/admin/innertest_install_check.py` | 668已交标准库安装自检 |
| `deploy/control/contract.json` | 待实现；非秘密配置结构、宿主运行环境、协议/UID映射版本契约 |
| `deploy/control/sshd_config.example` | 待实现；受限账号sshd策略模板，无实际账号/密钥 |
| `deploy/control/authorized_keys.example` | 待实现；forced-command约束模板，无真实公钥 |
| `deploy/control/README.md` | 待实现；固定包安装/环境材料注入/恢复边界 |

同一包索引绑定来源提交、各文件摘要/模式、包SHA256和运行环境要求；部署计划固定旧/新四镜像与控制包、配置非秘密摘要、迁移头、relay/socket/主体版本、执行窗口、恢复包和批准来源。具体Python内部结构由实施者按固定来源落实，不能新增第五业务镜像或任意运维RPC。

## 串行边界与验收

- 566拥有部署器、release manifest、Compose、控制包/模板、必要发布工作流与测试；可以修改发布工作流代码但不得触发publish/release/promote。
- 668继续拥有名单/批次/handler与业务装配；当前relay/安装检查及协议若需变更先交固定提交给566，禁止双方同时改控制包或共享配置。
- 566的本地测试只用明确合成的清单、私有状态目录、假命令执行器和隔离Linux/PostgreSQL；真实apply/recover、SSH账号/绑定/名单/卡片操作均不执行。
- dry-run零写、篡改/路径穿越/符号链接/错环境/过期窗口/并发/ENOSPC/未知迁移/恢复未批准必须被测试拒绝；未知不能盲重跑。
- #603/#668共同恢复、Linux socket/UID/userns实测和兼容制品在S6收口；D4stage中断/观察、D5正式提升、D6生产首用及全部原关闭条件保留。
- 实施者独占codex工作树，禁止审核/下派/线上Issue写入。未来真实安装由独立Ops任务负责，本轮不创建、不执行。
