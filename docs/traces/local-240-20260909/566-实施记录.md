# #566 实施记录

- 实施者 `/root/impl566`；会话 UUID `01a08434-e3bd-75e0-b2f0-397f73e50494`；模型和 token 以本 UUID 原始 session JSONL 的记录为准，当前未取得累计值，不估算。
- 独占树 `/private/tmp/lingxi-240-566-20260909`，分支 `codex/240-566-20260909`，基线 `34e5bee`。唯一合同 v1.0；源 #566 全文 SHA256 已实际核对 `b60659184f4dca04fb769ec1ae3430e6d4c3c71f924754cd9349bc9f82e5fad6`。
- 状态：S5 本地实施中，未完成 D1/D2/D3；未执行代码审核或继续下派。只使用合成清单、假执行器和独占私有目录；未真实 apply/recover、发布或改变业务。
- 初次命令 `pwd`、冻结合同与来源全文读取均显式使用独占 workdir。原始工具记录保留具体开始时间；03:45:05Z 实时回读会话 ID。

## 已实施与首轮定向证据

四操作入口、固定计划和批准、主机锁描述符传递、宿主未结束部署标记、0600 原子状态、迁移未知拒绝重跑、安全控制包和只读安装已形成。调用方使用同 ID 接续；新工具代码必须与固定包内实际字节一致。非秘密配置显式列举；私有环境文件仅由 Docker/Compose 既有机制消费。

包索引 schema revision 1、发布清单 schema 2。固定包内文件集合由 S5 白名单约束；安装先验证全部路径、类型、权限、摘要和来源，原子安装后版本目录只读。正式提升保留四镜像和同控制包，补 main 提升提交、运行编号与验收材料摘要；本轮只改工作流代码，没有触发发布。

- `python3 -B -m unittest discover -s tests -p test_deploy_automation.py -q`：首轮 12 条通过，覆盖接续/不重复重建、未知迁移、错误批准/窗口/环境、真实子进程继承锁、ENOSPC 保留旧账、秘密哨兵拒绝、dry-run 无文件变化及包路径/链接/权限/缺项/篡改。最初一个失败是 macOS `/var` 与 `/private/var` 路径别名的测试期望，改为比较解析后路径后复绿。
- `python3 -B -m unittest discover -s tests -p test_release_manifest.py -q`：16 条通过。旧 schema stage 选择改为显式历史兼容参数；迁移头测试通过迁移说明与实际声明核对，不再硬编码 0090。
- 定向 ruff 首次发现 deploy 新路径缺少函数说明与测试格式，按现有规则补齐；未扩大豁免或降低门禁。后续最终检查另记。

## 与 668 的固定装配

668 保留 relay 相邻 `innertest-relay.json` 契约。编排者明确批准独立版本化环境装配目录：复制冻结摘要 relay 与唯一非秘密配置，`installation.json` 收据分别绑定源包、脚本和配置摘要；不把环境配置加进发布包。新通道叠加配置由部署器在计划私有目录生成，内容由已批准 plan/config 唯一确定；不改变无 scope 的既有 Compose 启动。仅 scheduler 挂 `/run/lingxi-innertest/admin.sock` 及 `/etc/lingxi/innertest/binding.json`，不自动创建宿主目录。gateway 只接共同 scope/binding_id。

## 仍在本地完成的项目

实际 Docker 命令装配与只读状态回读、历史恢复格式、安装收据/主体版本复核、迁移及观察中断正反例、关键变异验红、实际隔离 Linux 安装/恢复和后续 668 业务恢复接口尚需补齐。当前测试不冒充这些证据，S6/D3 不标完成。

D4 stage 九处真实中断与连续观察、D5 正式同包提升、D6 生产首用和来源全部关闭条件继续保留。本轮没有申请占用真实服务、SSH 身份、名单、飞书卡片或真实数据库。

## 第一实施切片检查点

- 原始 session 起点 `2026-09-09T03:27:34.064Z`；`turn_context` 实际模型 `gpt-6-astra` / `medium`。原始来源 `/Users/wangzhipeng/.codex/sessions/2026/09/09/rollout-2026-09-09T11-27-33-01a08434-e3bd-75e0-b2f0-397f73e50494.jsonl`。04:19:26.647Z 累计 input=16460541、cached_input=16167808、output=70495、reasoning_output=19367；不是最终量，缓存/推理是子集，编排者按会话最终记录去重。
- 最新宿主定向 `PYTHONPATH=src python3 -B -m unittest discover -s tests -p 'test_deploy_*.py' -q`：174 条通过，1 条 Linux/root 安装用例在 macOS 明确跳过。首次扩大到既有部署集合时漏设 PYTHONPATH，2 项 import 错误；恢复仓库标准参数后全部通过，没有删除测试。
- 独占无网络 Linux 容器 `lingxi-566-local-20260909`（镜像 `lingxi-240-check-env:20260909`，容器 ID `8f26303bf0ddae06c39a01d55789897e77ab9ed5914a8361a1edb0639c159c60`）最新 27 条新增部署/运行态测试通过，零 skip。使用 docker cp 复制固定源码，未挂载宿主认证。覆盖真实文件系统只读安装、独立 relay 装配、实际 Unix socket 模式/组检查；映射输出由假 Docker 执行器提供，不能冒充真实容器内 SO_PEERCRED 三路验收，后者复用668/S6证据。
- 五个关键变异分别去掉批准摘要匹配、未知迁移拒绝、包摘要、fcntl 互斥、实际绑定版本检查；每个指定测试均 exit=1 且出现断言 FAIL。全部恢复原字节，采用 `-B`，随后174条复绿。没有进行代码审核。
- ruff check/format、`check_release_flow.py`、`check_deploy_contract.py`、`verify_docs.sh` 均通过；发布16条测试再次通过。文档门禁中 Git fsmonitor 报 `daemon terminated`，脚本最终 exit=0；后续提交/打包使用显式关闭 fsmonitor 的 Git 读取，不改全局配置。
- 完成新旧包只读校验、原部署绑定的历史恢复结构、部署后原地接续和独立恢复批准、三服务实际配置/包标签回读、启动就绪等待、连续采样及异常重启拒绝；成功结束只清本部署已停止的迁移容器，不清业务或卷。未知/失败迁移保留等待调查；恢复后只在明确同源且已停止时清理来源迁移容器。
- #668 源接口提交 `1950a24` 已交接；部署器调用固定 `python -m lingxi.apps.innertest_status`，逐项检查 roster/binding/followups，不把顶层 ok 当就绪。未知次数原样保留；历史目标遇到动态名单或仍在处理/等待/未知的阶段即拒绝。0092实际业务提交尚未在本树整合，真实库集成不认领通过。
- 本切片当前待做：提交后固定源码实际打包/同包安装回读，最新切片最终 Linux 复验和容器清理；后继 D3/S6 仍需完整668业务SHA和真实迁移/兼容制品证明。未运行本机全量或线上工作流。
