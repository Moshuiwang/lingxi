# #566 实施记录

- 实施者 `/root/impl566`；会话 UUID `01a08434-e3bd-75e0-b2f0-397f73e50494`；模型和 token 以本 UUID 原始 session JSONL 的记录为准，当前未取得累计值，不估算。
- 独占树 `/private/tmp/lingxi-240-566-20260909`，分支 `codex/240-566-20260909`，基线 `34e5bee`。唯一合同 v1.0；源 #566 全文 SHA256 已实际核对 `b60659184f4dca04fb769ec1ae3430e6d4c3c71f924754cd9349bc9f82e5fad6`。
- 状态：D1/D2本地交付、本代理分工内的D3隔离验证已完成；整体四镜像/CI及真实环境验收留后继；未执行代码审核或继续下派。只使用合成清单、假执行器和独占私有目录；未真实 apply/recover、发布或改变业务。
- 初次命令 `pwd`、冻结合同与来源全文读取均显式使用独占 workdir。原始工具记录保留具体开始时间；03:45:05Z 实时回读会话 ID。

## 已实施与首轮定向证据

四操作入口、固定计划和批准、主机锁描述符传递、宿主未结束部署标记、0600 原子状态、迁移未知拒绝重跑、安全控制包和只读安装已形成。调用方使用同 ID 接续；新工具代码必须与固定包内实际字节一致。非秘密配置显式列举；私有环境文件仅由 Docker/Compose 既有机制消费。

包索引 schema revision 1、发布清单 schema 2。固定包内文件集合由 S5 白名单约束；安装先验证全部路径、类型、权限、摘要和来源，原子安装后版本目录只读。正式提升保留四镜像和同控制包，补 main 提升提交、运行编号与验收材料摘要；本轮只改工作流代码，没有触发发布。

- `python3 -B -m unittest discover -s tests -p test_deploy_automation.py -q`：首轮 12 条通过，覆盖接续/不重复重建、未知迁移、错误批准/窗口/环境、真实子进程继承锁、ENOSPC 保留旧账、秘密哨兵拒绝、dry-run 无文件变化及包路径/链接/权限/缺项/篡改。最初一个失败是 macOS `/var` 与 `/private/var` 路径别名的测试期望，改为比较解析后路径后复绿。
- `python3 -B -m unittest discover -s tests -p test_release_manifest.py -q`：16 条通过。旧 schema stage 选择改为显式历史兼容参数；迁移头测试通过迁移说明与实际声明核对，不再硬编码 0090。
- 定向 ruff 首次发现 deploy 新路径缺少函数说明与测试格式，按现有规则补齐；未扩大豁免或降低门禁。后续最终检查另记。

## 与 668 的固定装配

668 保留 relay 相邻 `innertest-relay.json` 契约。编排者明确批准独立版本化环境装配目录：复制冻结摘要 relay 与唯一非秘密配置，`installation.json` 收据分别绑定源包、脚本和配置摘要；不把环境配置加进发布包。新通道叠加配置由部署器在计划私有目录生成，内容由已批准 plan/config 唯一确定；不改变无 scope 的既有 Compose 启动。仅 scheduler 挂 `/run/lingxi-innertest/admin.sock` 及 `/etc/lingxi/innertest/binding.json`，不自动创建宿主目录。gateway 只接共同 scope/binding_id。

## 第一轮时点的本地待办（后续进展见第二切片）

实际 Docker 命令装配与只读状态回读、历史恢复格式、安装收据/主体版本复核、迁移及观察中断正反例、关键变异验红、实际隔离 Linux 安装/恢复和后续 668 业务恢复接口尚需补齐。当前测试不冒充这些证据，S6/D3 不标完成。

D4 stage 九处真实中断与连续观察、D5 正式同包提升、D6 生产首用和来源全部关闭条件继续保留。本轮没有申请占用真实服务、SSH 身份、名单、飞书卡片或真实数据库。

## 第一实施切片检查点

- 原始 session 起点 `2026-09-09T03:27:34.064Z`；`turn_context` 实际模型 `gpt-6-astra` / `medium`。原始来源 `/Users/wangzhipeng/.codex/sessions/2026/09/09/rollout-2026-09-09T11-27-33-01a08434-e3bd-75e0-b2f0-397f73e50494.jsonl`。04:19:26.647Z 累计 input=16460541、cached_input=16167808、output=70495、reasoning_output=19367；不是最终量，缓存/推理是子集，编排者按会话最终记录去重。
- 最新宿主定向 `PYTHONPATH=src python3 -B -m unittest discover -s tests -p 'test_deploy_*.py' -q`：174 条通过，1 条 Linux/root 安装用例在 macOS 明确跳过。首次扩大到既有部署集合时漏设 PYTHONPATH，2 项 import 错误；恢复仓库标准参数后全部通过，没有删除测试。
- 独占无网络 Linux 容器 `lingxi-566-local-20260909`（镜像 `lingxi-240-check-env:20260909`，容器 ID `8f26303bf0ddae06c39a01d55789897e77ab9ed5914a8361a1edb0639c159c60`）最新 27 条新增部署/运行态测试通过，零 skip。使用 docker cp 复制固定源码，未挂载宿主认证。覆盖真实文件系统只读安装、独立 relay 装配、实际 Unix socket 模式/组检查；映射输出由假 Docker 执行器提供，不能冒充真实容器内 SO_PEERCRED 三路验收，后者复用668/S6证据。
- 五个关键变异分别去掉批准摘要匹配、未知迁移拒绝、包摘要、fcntl 互斥、实际绑定版本检查；每个指定测试均 exit=1 且出现断言 FAIL。全部恢复原字节，采用 `-B`，随后174条复绿。没有进行代码审核。
- ruff check/format、`check_release_flow.py`、`check_deploy_contract.py`、`verify_docs.sh` 均通过；发布16条测试再次通过。文档门禁中 Git fsmonitor 报 `daemon terminated`，脚本最终 exit=0；后续提交/打包使用显式关闭 fsmonitor 的 Git 读取，不改全局配置。
- 完成新旧包只读校验、原部署绑定的历史恢复结构、部署后原地接续和独立恢复批准、三服务实际配置/包标签回读、启动就绪等待、连续采样及异常重启拒绝；成功结束只清本部署已停止的迁移容器，不清业务或卷。未知/失败迁移保留等待调查；恢复后只在明确同源且已停止时清理来源迁移容器。
- #668 源接口提交 `1950a24` 已交接；部署器调用固定 `python -m lingxi.apps.innertest_status`，逐项检查 roster/binding/followups，不把顶层 ok 当就绪。未知次数原样保留；历史目标遇到动态名单或仍在处理/等待/未知的阶段即拒绝。0092实际业务提交尚未在本树整合，真实库集成不认领通过。
- 本切片当前待做：提交后固定源码实际打包/同包安装回读，最新切片最终 Linux 复验和容器清理；后继 D3/S6 仍需完整668业务SHA和真实迁移/兼容制品证明。未运行本机全量或线上工作流。

## 第二切片：实际制品与 D3

D1/D2 本地交付已由编排者接收。第一提交 e19f69d90eb0c521ce097b7319ad3e0436dd902a 未推送；双次实际打包逐字节相同：包 52d42de75c29dcf3d4b64e2b3b3d247ae11e061231a8cd55baeb66e7e97652d7，索引 deebc96f20a5b22c0977e3bd3fc78f990a03e25317765062db57cc8753e9ff8d，精确14文件。Linux实际安装/重复安装、安装态CLI dry-run零写均通过，切到明确合成的第二包后恢复原包也通过。这不是正式Release或业务四镜像恢复。

正式业务源码最终来源 ca65baa84a35fc1c334002e485388443df609e05，包含668完整业务/最终0092、2.4.0版本与603 listener停止修复。来源链有1950a24/e97c6dc/2b6114a/d500cb9/c9dc3ee，没有吸收外来main。2b6114a仅668实施记录冲突，完整采用该固定提交正文，不改写其业务或记录。

- 最终0092字节SHA256 a5934442dea8ab94c32485eb23005eb1ea45e3cbfa8d3a3dd3daad3c3711407c；没有在旧0092库上混用新DDL。独占Postgres lingxi-566-postgres-20260909，ID a3192f68684dae10c0939ad2a9019ae6202491f1191e05fe9e768116455567a4，内部网络 lingxi-566-check-network，不发布宿主端口，只用合成数据。
- 最终实际制品 lingxi-2.4.0-py3-none-any.whl，SHA256 d0f46ba9e799c550185d0b8972701a790ae3045c8ee5d016e2416bc9a91da0f2。离线 pip wheel --no-deps --no-build-isolation --no-index，安装到容器 /tmp/lingxi566-installed 后目录555/文件444；仅测试安装，不是生产pip。最初2.3.2临时构建作废；f281开头的2.4.0检查点也被包含c9dc3ee的新wheel替代，没有用旧摘要认领新退出能力。
- 最终wheel上，test_deploy_business_postgres三条真库用例与test_s6_listener_stop一条停止用例共同通过。CLI、listener、handlers、v1仓储/消费者的实际导入路径均断言位于只读安装目录。已确认阶段被子进程实际领取后强杀，短租期到期后恢复进程完成两阶段，第二进程零重复；原名单/版本仍在。外发unknown被下一进程保留、零盲重发。执行器和MCP业务查询均为合成替身，没有真实SDK或飞书交付。
- 同一wheel安装态，Linux peer UID/ACL/受保护uid_map、四连接与共同截止、本地MCP ClientSession经stdlib relay到handler，以及停止后缓冲请求拒绝共9条通过、零skip。真实个人密钥归属、实际sshd/sudo策略、真人卡片仍保留外部验收。
- 最终部署相关Linux181条通过、零skip，使用同一wheel和新0092合成库。宿主同集合181条通过，4条按Linux/真库条件跳过。最后回读 /tmp/lingxi-sdk-execution-attempted 不存在。
- 独立 lingxi566_revision 空库用原生Alembic和SQLAlchemy形式DSN实际upgrade到0092，current实读0092_innertest_membership (head)。注入9999_synthetic_unknown后current exit255明确拒绝；没有upgrade重试，原版本行已恢复。测试助手会删自身版本表，本段独立证据不与助手推算head混同。

第二切片补齐原生current回读、活动迁移不另起revision探针、非零结束作业不能认领验证；停止前核对实际旧镜像/配置/包，部分接续仅接受批准旧新两组；已知非秘密运行文件按白名单/摘要现读，不扫描私有env；无清单历史标签另用historical原始记录，不捏造release分支或构建编号。新增文件摘要、制品范围、迁移作业退出码三处变异均以目标断言FAIL验红，恢复字节后复绿，连前轮共八处。

上述本地D3证据已交编排者及603。旧四镜像在新schema完整启动、最终候选全部CI和S6其他过程故障由编排者/603统一，不用wheel冒充四镜像验收。D4真实stage、D5正式同包提升、D6生产首用及原关闭条件不变。

## 临时资源和后继清理

none网络容器在D3前已删除；Docker不允许none容器再接内部网络，故只重建本任务容器，新ID为98e2926bba66b4e82eed65e2c07f2097a8c35cb42e08c8832dffe619f3f1ce83。没有触碰其他项目容器。两个当前容器、内部网络和宿主命名临时tar/脚本由本实施者收口清理，删除回读追加如下。

最终非秘密wheel暂留本树 .dev-check/566-artifacts/lingxi-2.4.0-py3-none-any.whl，摘要与容器内一致；已通知编排者，供S6/S7回读后与独占worktree在S8移除，未进Git或发布附件。worktree/分支保留供整合，Owner为编排者，不删除用户原工作区。


## 收口回读

- 最新宿主部署集合182条通过，4条按环境条件跳过；新增缺少业务恢复模块时明确未具备的负例通过，去掉该防线也以断言FAIL验红，原字节已恢复。合计九处关键变异。最终统一完整检查留编排者，不再次扩跑已完成集合。
- 2026-09-09T05:20:22Z，两个本任务容器和内部网络已删除；docker ps -a按lingxi-566-过滤为空，network ls按本网络名过滤为空。清理前最后SDK标记仍不存在。八个宿主命名临时tar/元数据/脚本已逐项删除并回读不存在；所有测试子进程/监听器由用例退出回收，容器内安装目录和两份合成库随容器删除。
- 除已通知编排者的独占worktree和其中非秘密wheel外无保留资源。wheel不含凭据或用户数据，不作正式发布包使用；后继Owner/清理方式见上一节。
- 05:20:04.772Z原始会话累计input=37819501、cached_input=37137536、output=139533、reasoning_output=59500。不是最终token快照；缓存/推理均为子集，提交收口后的最终量由编排者按同UUID原始记录提取，不重复相加。
- 从03:27:34.064Z到上述清理结束为本轮开发/隔离验证墙钟区间，后续只有必要静态检查与提交。本轮未作任何代码审核、线上Issue写入、PR、推送、main/release写入、merge/publish/deploy、真实飞书或真实身份权限操作。

匿名卷补清：rm -f不会移除Postgres匿名卷，随后已补查。原始创建调用为04:27:18.550Z至04:27:38.273Z，唯一在该窗口创建的匿名卷为b92cb41659d981a6e3d8718d5bcba2182a33ff42e38e29983cfbced16fd19272（创建时间04:27:31Z）；当前镜像只声明/var/lib/postgresql/data一个卷。依据这组对应关系只删除本卷，回读volume ls为空，合成库实际数据至此清理完成。其他匿名卷没有触碰，并已提醒编排者在S8分别核对归属。
