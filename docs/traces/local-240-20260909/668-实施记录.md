# #668 实施记录

- 实施者 `/root/impl668`；实际会话 UUID `01a08425-b4ad-7b51-94d0-e100d1dc6d98`；模型 gpt-6-astra/medium。token 原始来源为本 UUID 对应 session JSONL，由编排者提取最终 token_count；当前未取得，不估算。
- 独占树 `/private/tmp/lingxi-240-668-20260909`，分支 `codex/240-668-20260909`，基线 f12067e；唯一合同 v1.0。源 #668 完整正文 SHA256 `f5decc3db686b1506c5ff8e24965f82fd06a6f97dae86612b235bc5196d61551` 已实读核验。
- 本地 S4 Ready 后实施，无代码审核、无下派。先交固定通道接口，业务事务与阶段接线继续实施，不代表本 Story 完成。

## 第一接口包

源码及安装契约见 [内测管理通道安装契约](../../技术设计/内测管理通道安装契约.md)。实际官方 Python MCP SDK 1.30.0 的协商版本是 `2025-11-25`，本实现固定这个交集；未知版本拒绝。纯标准库 relay 与实际 scheduler handler、Linux listener 分离，控制包打包由 #566 负责。

- 本机4项协议测试通过：严格 schema、原始20人上限、二选一查询、未知版本/方法、伪造身份字段和既有连接撤销。
- 独占 Linux Python3.12 容器3项通过：实际 SO_PEERCRED；合法 UID1234/同组错误UID1235/不同组ACL拒绝；受保护绑定及userns摘要/溢出UID拒绝；四连接上限和共同截止清 socket；真实 MCP ClientSession 经 stdio launcher→实际纯字节relay→Unix socket→handler 列出恰三工具、prepare调用、撤销后拒绝。
- Linux合成容器 `lingxi-668-protocol-synthetic-20260909`，python:3.12-slim 摘要 `sha256:78387bc3881b8273120a12ebe6c1ab22b018ccc2c9adf565ae1ac9b536e184ea`。临时装 mcp==1.30.0 仅用于实际客户端测试，未改宿主依赖。容器仍由本实施者使用，收口清理。
- Colima 不允许挂载本机 `/private/tmp`，首次容器创建失败（没有生成容器）；改用 docker cp 仅复制源码/合成测试，零宿主认证挂载。测试 socket、连接、子进程均在用例退出回收。
- 真实管理员 SSH 密钥归属、sudo/sshd生效策略、真实绑定写入、实际飞书卡片均保留外部验收。安装自检明确输出这些 manual_required，不把文件检查当作真实安装已验。
