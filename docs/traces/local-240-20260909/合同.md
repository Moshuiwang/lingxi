# Trace：2.4.0 审查修复、Draft PR 与 stage 验收

状态：v1.1，2026-09-09；产品负责人本轮直接授权生效，沿用 [#678 v18/r2](https://github.com/Moshuiwang/lingxi/issues/678)。此前免审核、线上 Issue 只读及不创建 PR 的 v1.0 已完成开发回合，本轮由此合同替代；历史合同及证据保留在提交 `8ed064fc847c40d14657b8f1c18273b2c6eda2bb`。

## 结果与授权终点

交付可供产品负责人合并的 2.4.0 Draft PR：回写来源 Issue，全面代码审查，修复已确认问题，完成同候选的适用检查和可执行 stage 验收，留下发布前仍需满足的条件。产品负责人说明 DeepSeek 4.1 Flash 尚不能取得使用权限，终止对比实验；这是用户说明与决定，不是模型对比结果。为实验保留的免审核和线上只读安排随之取消。

开发范围为 #603、#657、#668、#566；#662 为 stage 准备和应用验收前置。#661 的生产加固及其他原完成标准单列，#676 已完成无本轮新增，不因本 PR 自动关来源 Issue。#668 最新评论与已实现动态名单归属冲突，已交产品负责人选择；决定前继续安全审查和隔离回归，真实名单、管理员主体绑定及新增权限写入保持等待。

唯一授权终点：适用验证有明确结果，修改提交并推送原 `codex/2.4.0-execution-isolated-20260909` 分支，Draft PR 与剩余验收条件交产品负责人合并。合并由产品负责人执行；正式候选、同制品提升和生产发布由各自阶段承接，生产发布须独立 Ops 任务。

**北京时间 2026-09-09 18:00 前仍未完成时，将剩余工作交给 tz 新 tmux 窗口中的交互式 Claude Code，交接不能使用无头模式。**交接须包含候选、证据、当前环境、等待项及下一步，不能让两位执行者并行消费同一外部通道。

## 执行步骤

| 步骤 | 可观察产物 | 前置与负责人 | 通过证据 / 失败去向 |
| --- | --- | --- | --- |
| R1 范围与回写 | 开发状态和实验终止决定可在原 Issue 回读 | 主代理核正文及全部评论；DeepSeek 查询由 Astra low 子代理 | 写后独立回读；范围矛盾保留产品决定 |
| R2 全面审查 | 恢复、追溯、扩员、部署及跨项调用的具体发现 | 未参与原实现的独立上下文审核者；主代理审部署和整体接缝 | 固定 SHA、覆盖范围、可复现问题与未验证项；不以测试代替审查 |
| R3 修复复核 | 修复在独占工作树完成并串行汇合 | 各修复者实施，原审核者定向复核 | 定向回归、关键防线变异红/恢复绿；失败返回原修复包 |
| R4 外审与 Draft PR | 独立外审及新候选 PR | 外审者未参与实现；主代理冻结并统一检查 | tz Claude 与本机无头 Gemini 3.8 Flash 报告须有候选/基线/实际模型；配额或权限中断不算通过 |
| R5 同候选 CI | 主检查、全部依赖、镜像和最终汇总实际执行 | Draft PR 指向 main，主代理回读 | 同 head/tree 完整 CI；独立校验 PR 四镜像测试包，手动 CI 不替代该包 |
| R6 stage 验收 | 测试环境真实路径与失败恢复结果 | R3–R5、#662 前置、当前独占和具体执行包 | 固定镜像/数据库/身份；用户可见结果和接口回源；失败停止新增消费、保留数据修同代候选 |
| R7 交付或交接 | 产品负责人取得 Draft PR，或 tz 交互式 Claude 取得剩余工作 | 主代理，按 18:00 截止执行 | 同分支已推送、残留盘点、等待项/Owner/下一触发；不自动合并或关闭来源 Issue |

用户已明确要求独立上下文子代理审核；外部第二方为新会话 Claude Code，第三方按用户追加指令改用本机无头 Gemini。各方只对实际读到的候选负责，修复改变候选后按影响复核。具体模型、配额中断和通道切换记录在运行事实。

## stage 的具体边界

只用 biai-stage、Bot-Test、原测试数据库和受控测试身份。执行前实时核对独占、版本、配置目标、队列/通知/到期处理、容量和恢复材料；历史授权文件已恢复，不为核验再次重授权或刷新令牌。旧容器保持停放到兼容候选及具体执行包齐备。

未合并功能验收使用真实 PR 的四镜像测试包，不在 stage 构建，不伪造 Release 清单。原配置与数据保留；迁移固定作业，结果未知先回读。总体 stage 验收已获授权，主代理在启动前写明绝对窗口、测试输入、允许自动职责、停止和清理方式；新增人群/权限范围和不可恢复数据处置仍交产品负责人决定，不能把自动清理及群通知隐含成只有两条问数。

#662 的 T1 正常任务及 T2 确定性 queued 恢复沿用原断言，核相同任务和唯一送达；真实 Agent SDK 单次受控验收最多 3 回合。状态和权限只读回源，卡片/真人操作/真实送达用浏览器；queued 结果不覆盖运行中 SDK 恢复。

#566 D4 保留九处真实中断接续及连续 15 分钟观察。当前部署器的新目标要求 schema2 候选 Release，PR 测试镜像包不能代替；功能验收与部署器演练分开记录，后者在合法制品和安装前置到达后继续。D5 同制品提升和 D6 独立生产放行仍保留。

新持久后处理、动态名单及不确定外发不能交给未经兼容验证的旧程序。默认失败出口：停止新增消费，按宽限停本轮服务，保留新结构和业务记录，修同代候选。不得降库、删记录制造兼容或恢复旧库覆盖新结果。

## 工作区、恢复与清理

主代理串行整合；并行修复、测试和变异使用独占工作树、合成数据库及可辨识临时资源。保留原集成工作区；推送显式指定原 codex 分支，不 force-push，不移动 main/release。完整检查在修复汇合后统一执行，失败保留证据。

原 Issue 与 PR 记录共享进度；任务表记录当前、下一步和等待/占用，历史实施证据保留。谁创建的工作树、进程、容器和外审窗口谁清理；交接留用者写明路径、用途、接手者和清理方法，不能关闭他人既有 tmux 窗口或数据库。
