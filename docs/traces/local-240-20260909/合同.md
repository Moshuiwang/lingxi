# Trace：2.4.0 四项开发完成，停在合并前

状态：v1.0，2026-09-09；本次对话授权生效，仅在 codex 分支执行。方法为 [#678 v18/r2](https://github.com/Moshuiwang/lingxi/issues/678)。这是本地 Trace，不创建或修改线上 tracking Issue，目录编号不是 GitHub Issue 号。

## 给产品负责人

- 结果：交付后台工作恢复、失败追溯号、动态扩员及首次使用、部署工具四项实现，完成本地和线上适用 CI，留下可继续验收的固定候选。
- 唯一授权终点：全部本次开发范围实现、最终候选适用检查通过、修改已提交到干净的 codex 分支；停止在合并前。没有 stage、生产、正式发布或 Issue 关闭动作。
- 输入：里程碑 2.4.0 的七项见[来源索引](来源索引.md)。本次实施 #603、#657、#668、#566 的代码及离线/隔离验证；#661/#662 为外部前置；#676 已完成，不重做。#661 的隔离加固迁移和 #662 的监控清理开发同属这两项保留范围，不悄悄加入四项施工。
- 产品负责人已确认：“符合，完成开发并停在合并前”。原始要求同时明确免代码审核、线上 Issue 只读、仅 codex 分支行动、可运行线上 Actions，并要求规划完成后直接新建 Astra 高编排任务。该授权覆盖本合同在以上边界内的具体排程，不再要求先合并合同 PR 才开工。
- 不做：不执行任何代码审核或变相独立审查，不改线上 Issue 正文、标签、里程碑、评论、开关状态；不修改 main/release/其他非 codex 分支；不合并、不自动合并、不发布、不启动真实服务、不发飞书消息、不改真实身份权限、名单或业务数据。后置项 #658/#664/#673/#674/#675 不纳入。
- 触发：外部写=仅 codex 远端分支推送及 CI；共享资源=Git 工作树、迁移顺序与本机测试资源；跨任务=规划转编排及元守护；独立审核=无（用户明确免除）；特殊预算=无硬上限，记录实际时间和 token。

## 本次例外与完成口径

本次对话指令优先于仓库及来源工作项的默认流程：合同合并批准改为本对话直接授权；Ready 领取、交接、缺陷及拟修改 Issue 内容全部写本地；默认独立审核全部免除，不能因此标记 L3 通过或削弱自动检查。来源中“合入 SHA”在本次依赖传递中指整合到 codex 集成分支的提交，绝不指合入 main。

#668/#566 线上仍为 pre-ready。编排者只在本表要求的前置实际交付后，将本地 Step 改为 Ready 并登记证据；不得修改线上标签，也不得略过真实前置。#603/#657 已有 Ready 开工来源。

本次 Complete 仅证明四项开发范围到达上述终点。所有来源 Issue 的真实环境验收、观察、正式提升、生产首用与关闭条件完整保留到[验收账](验收.md)，不算已完成，不以代码测试替代。#566 D4–D6、#657 R657-C、#603 的外部验收及 #668 真实管理员/卡片/通知旅程不执行。

## 执行步骤

| ID | 可观察产物 | Ready 条件 / Gate | Owner / 工作区 | 通过证据 | 后继 / 失败去向 |
| --- | --- | --- | --- | --- | --- |
| S0 | 独立编排任务接管，锁定分支、角色与资源 | 合同提交；规划者停止写入 | Astra 高编排者 / 独立 codex 集成 worktree | 回读合同、基线、干净状态；登记任务 ID 与实际工作区 | S1；不一致则 Waiting，回元守护 |
| S1 | #603 R603-A/B：唯一持久阶段、确认同事务登记、有限消费、未知结果及兼容恢复 | S0；源接口完整，外部调用关闭 | 子代理 impl603，Astra 中 / 独立 codex/240-603-* worktree | 真库事务原子性、owner+attempt、未建档 NULL 目标、恢复与未知零盲重发；合同测试及阶段 v1 兼容提交 | S2/S3；失败仍由该实施者修复 |
| S2 | #603 R603-C 基础退出装配，gateway 20 秒 / scheduler 120 秒统一截止 | S1；固定 listener 生命周期端口，暂缺业务实现须明确未接入 | impl603 原工作区；编排者串行整合 | stop 后零领取，重复停止安全、有限退出、持久恢复；所有后台对象账 | S3/S4；失败回 S2 |
| S3 | #657 R657-A/B：三处精确 LEFT JOIN、稳定号码、九正三负、隔离日志、两查询入口与目录 | S0 可开始独立队列/worker；共享退出文件须 S2；整合必须早于 S4 文案/admin 变更 | 子代理 impl657，Astra 中 / 独立 codex/240-657-* worktree | 精确关联/缺源、并发、不串号、旧终态不补发、90 天边界、权限拒绝、目录兼容及变异验红 | S4；SQL 或安全问题回 S3，不改加列方案 |
| S4 | #668 动态名单、批次、本人确认、就绪检查及受限 MCP/socket/relay 代码 | S1/S2 接口已整合；S3 共享文件已整合；编排者登记本地 Ready | 子代理 impl668，Astra 中 / 独立 codex/240-668-* worktree | 三工具协议、20 人混合批次、同事务名单与阶段、版本/本人/撤销拒绝、同 request_key、两进程名单一致、缺就绪/通知授权零发送 | S5/S6；失败回 S4；新增业务选择转 G1 |
| S5 | #566 D1/D2：plan/apply/status/recover、控制包/清单、固定来源和安全安装恢复 | #603 停止/恢复接口及 #668 relay/协议交付；编排者冻结包文件白名单/schema revision 并登记本地 Ready | 子代理 impl566，Astra 中 / 独立 codex/240-566-* worktree | dry-run 零写、计划/锁/原子状态、篡改/路径穿越/错误环境/未知迁移拒绝；同四镜像加同控制包摘要 | S6；失败回 S5，不执行真实 apply |
| S6 | #603/#668/#566 D3 共同集成、迁移和恢复能力 | S1–S5 已整合；各实现者停止原写入 | 编排者串行整合；定向修复只派原实施者至独占 worktree | scheduler listener 与阶段共用预算；准备卡与确认阶段分工；Linux socket/UID/userns/SO_PEERCRED 正反例；兼容 v1 与动态名单恢复；真实隔离 PostgreSQL | S7；失败回归属 Step；无法离线证明的实际安装条件保留外部验收 |
| S7 | 全部开发候选冻结及适用完整 CI | S6；候选提交已固定，测试输出/变异已清理 | 编排者 / codex 集成分支；本机重检查串行 | 本机适用检查、GitHub ci.yml Epic Full 终态及 head SHA；extras/镜像等作业逐项记录，不把本机 full 当整条 CI | S8；红色保留，诊断修复后按影响重验 |
| S8 | 合并前干净交付、逐项验收账、待决策/Issue 拟修改稿、环境清理 | S7 全部适用检查通过 | 编排者 / 集成分支唯一写入者 | 固定候选、已提交/已推送、工作树干净；main 指针无本任务写入；临时资源清单；四项实现映射 | 通知元守护复盘，停止写入；不完整保持 Waiting，不伪 Complete |
| S9 | 整体时间、token、流转与中断复盘 | S8 或发生真实阻塞需报告；编排者交出记录 | 当前规划任务转元守护，只读；复盘文件写入需编排者单写协调 | 系统计时与去重后的会话用量、Step/代理/CI/人工决定记录 | 最终向产品负责人报告；剩余外部项继续保留 |

S1 与 S3 的独立部分可以并行；默认最多两名实施者并行。迁移、共享装配、文案和重检查按表串行。编排者不得把尚无依赖制品的 S4/S5 当 Ready，也不把未实现先测试成功作为开工前置。

## 已固定的接口与交付边界

具体字段、参数、错误集合、否定用例以[固定来源](来源索引.md)中的四项正文为合同来源。本节只记录跨项接缝。

- #603 唯一拥有 admin_action_followup、FollowupSpec/Ref、enqueue_followups 同事务端口、领取代数、恢复和生命周期；#668 不建第二套后台租约。confirmation_card_send 属准备事务；逐人预开通/就绪检查属本人确认事务。未建档目标 subject_key=batch_item_id，target_user_id 可空，随后核验原关联再补齐。
- gateway 只消费本进程发卡/重算/观察阶段；scheduler 只消费预开通/检查阶段，不持新增飞书发卡凭据。新增活动数据库操作预算按进程各不超过 2，scheduler listener 至多占 1；listener 单接收线程、4 个短请求、64KiB、5 秒，停止纳入统一 120 秒。
- #657 不新增迁移。合法原事件号来自精确关联及未过期事件；缺源使用持久 task ID 导出的 T-参考号，两种查询各守自身期限。先整合 #657 内容目录与 admin 查询，再交 #668。
- #668 提供真正 scheduler 内 MCP handler、标准库 relay、协议及 UID/ACL 安装契约；#566 打入非秘密控制包，仍只有四业务镜像。真实 SSH 账号、密钥、绑定、名单导入不在本次安装。
- #566 复用 #669 发布解析与清单；可以修改发布工作流以交付控制包代码，但不得触发 publish/release/promote，不新增 release 分支。兼容旧清单必须显式处理，未知/缺失不能当通过。
- 迁移编号由编排者在独立集成工作区串行确定，#603 先、#668 后。产品合同和断言只按已批准来源修订；“当前能力”只能陈述实际证据，不能把本次离线结果写成线上已具备。

## 外部写、隔离与恢复

授权外部写仅为本仓库 codex/* 分支的正常提交推送以及固定分支上的 ci.yml workflow_dispatch（Epic Full）；线上原 Issue 只读。无需创建 PR 即可跑该工作流，因此本次默认不创建 PR、不发评论。不得执行 issue edit/comment/close、标签/里程碑修改或自动关单关键字；拟变更统一进本地[任务表](任务表.md)。不更改分支保护或审核规则。

本次入口分支 codex/2.4.0-trace-20260909，从 caa2bc13545875e03a3bb7e2d976ede39daa69a0 创建。初始本地 main=4251004d23dc1ae7d8faed761ed147c67c5a78cd。所有正式写入和整合仅 codex/*；禁止无参 push，必须写明 origin HEAD:refs/heads/codex/...，避免初始 upstream 指向 origin/main 的风险。不 force-push、不移动 main，不触碰其他任务修改。

新任务使用隔离 worktree，以本 Trace 提交所在 codex 分支为来源，取得唯一编排权。实施 worktree 由编排者或实施者建立后登记实际路径和 codex 分支；一处一写者。各自数据库、端口、安装目录隔离；测试只用合成数据。整合采用保留来源的提交/cherry-pick，冲突只在 codex 集成分支解决；不能覆盖前项成果。重检查串行，变异恢复后核对净 diff，不把变异带入提交。

谁建谁清：实施临时 worktree、测试库、容器、进程及安装目录在提交/整合后逐项清理。最终保留规划工作区及用户可继续使用的集成 codex 分支/工作区，登记 Owner、用途与清理方式；不擅删用户原工作区。恢复依靠既有提交和可辨识的测试资源清单，不对用户文件 reset --hard、不全局 prune。

## 跨任务、预算与人类 Gate

规划者：任务 01a083ed-b813-75d1-9007-c20f02757e7d，合同提交后退出实施和工作树写入，转只读元守护。编排者：新建独立顶层任务 gpt-6-astra/high；接管后登记实际 ID、工作区和分支。实施者 gpt-6-astra/medium，不继续下派；简单机械收尾可由编排者用 gpt-6-astra/low，但不得派代码审核。

元守护观察主链状态、等待、越界及清理，只通过任务消息请求编排者在现有授权内恢复；不成为第二实施者。不改合同授权，不向其他人发送 Slack/邮件。可在本任务设置安静的定期跟进，任务无变化时不通知；全部收口后清理本次跟进。客户端离线时不承诺持续执行。

- G1：新增产品承诺、权限/数据范围、真实环境操作或原合同未决选择，受影响 Step Waiting，留下具体选择与证据，请产品负责人决定；其余 Ready 可继续。沉默不授权。
- G2：软件依赖/检查环境故障，先做可恢复的本机修复或使用已授权 Linux CI；若需要付费、改变用户/线上环境或降低检查，Waiting 并请产品负责人决定，不能删测试/调低门禁换绿。
- G3：到达合并前终点必须停止；合并、真实验收和生产需要下一次明确授权，不可从“实现全部 Issue”推导部署。

预计四个开发包、一次共同集成、一次最终冻结；没有用户指定的时间/费用/token 硬上限，不虚构预算。每个固定候选默认一次适用完整 CI，失败保留原因，修复后只重验受影响项及最终必要完整检查。每个批次回写实际耗时、检查次数与新增范围；连续两次同因失败且无新证据时转诊断/Waiting，不盲目重跑。

计时起点为本任务系统时间 2026-09-09T02:09:49.633Z（北京时间 10:09:49.633）。复盘用起止墙钟时间，不累加并行耗时；单列规划、开发、CI、等待人类/工具中断。token 以各任务及子代理原始 token_count 累积值的区间差去重汇总，单列输入、缓存输入、输出及其中推理；缓存是输入子集，推理是输出子集，不能重复相加。缺记录明确“未取得”，不用额度百分比、估算或重复上下文长度替代。子代理交付须登记其 ID/会话记录来源，避免遗漏或父子重复计数。
