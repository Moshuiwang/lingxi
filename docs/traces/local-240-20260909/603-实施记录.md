# #603 实施记录

- 状态：本地实现包交接前；共享 C 基础已提交，真实 listener、跨项集成与完整候选检查保留后继。
- Owner：子代理 /root/impl603；底层会话 01a083fa-9f8b-77c0-a4fc-496b1afe3e0b；gpt-6-astra / medium，实际 token 由编排者原始会话记录区间提取，当前未取得。
- 工作区：`/private/tmp/lingxi-240-603-20260909`，分支 `codex/240-603-20260909`；基线 c19d53f。
- 本地合同：v1.0；#603 来源正文 SHA256 已验证 `8d1ae95f75ca43a84813ff0ae007d4c1c98da569723374f2775311648f399ac7`。不执行任何代码审核。
- 迁移：编排者确认唯一 head 0090 后串行分配 0091_admin_action_followup；#668 后接 0092。
- 目标断言：确认业务与阶段同事务；owner + attempt 拒绝迟到完成；首次开通允许空用户；外发未知不重发；停止后零领取；20/120 秒共同截止。证据目标 L1/L2，外部真实平台验收保留。
- 临时资源：独占 `lingxi-240-603-test`，postgres:16-alpine，127.0.0.1:32772，合成数据库 lingxi603。Owner 本实施者，结束后 docker rm -f 清理；无真实外发或业务数据。
- 检查：初次阶段仓储定向真库测试运行中；未运行完整检查，未形成完成结论。
- 保留：#662 就绪后的真实 Bot-Test 重启回读、实际外发与恢复制品外部验收；#668 listener 业务由后继接入；生产与发布不在授权内。

## 基础与装配检查点

- 基础提交 `55d0cf1` 为 v1 表/仓储/消费者组件，不是完整恢复制品。随后装配确认同事务、gateway 三个固定消费者及集中续租、scheduler 统一 120 秒 Owner。
- 迁移链补齐 README 后通过（37 revisions，head 0091）。首轮测试合成人员缺必填字段、容器与宿主亚秒时差均已修正测试输入；不代表业务故障。确认及既有确认真库回归 78 条通过；卡片/发布关联/重算/阶段回归 143 条通过；gateway/scheduler/生命周期 37 条通过，2 条既有条件跳过。
- 新阶段定向真库 15 条通过，包括真实两个消费者进程竞争、旧代拒绝、子进程提交前/后强杀、v1 真实消费恢复、100 阶段/10000 容量、空用户与身份补齐、期限、未知版本、当前发布版本与追溯投影。
- 关键变异：移除确认登记、移除 owner/attempt、移除观察版本守卫、移除 stop 门，四个指定用例均以断言失败验红；恢复原始字节后 19 条阶段/生命周期用例通过。第一次 owner 变异产生 SQL 参数类型错误，未计作有效验红；改为保留参数类型的条件移除后断言验红。日志位于本机 `/private/tmp/603-mutation-*.log`，非线上证据。
- 静态：ruff、函数/文件体量及注释卫生定向检查通过；完整候选 CI 留给集成步骤，不声称整条 CI 已通过。

## 固定后继接口

- #668：core/admin/followup.py 的 FollowupSpec/Ref/ClaimedFollowup；target_version 为可空整数，subject_key=batch_item_id；enqueue_followups 同连接，不接受 open_id 参数。
- #668：PostgresFollowupStore.resolve_target 核对 owner+attempt，通过固定业务适配器注入的 batch_identity_lookup(connection,batch_item_id) 查询原批次身份，再与真实 app_user.feishu_open_id 对比；不改变 subject_key/id。结果码 resolved_target。
- #668：scheduler loop.register_background(component)，component 实现 request_stop/drain_until；loop.followup_db_slots 为可重入两槽预算。listener 业务未接入，由 #668 负责；须与其阶段和 listener 共用该预算并保持 listener 至多占一槽。
- #657：postgres_admin_followup_projection.fetch_followups(dsn,trace_id=...) 返回 AdminFollowupView 元组，精确匹配 trace_id、九十天期限、最多100条；followup_render.render_followups 输出只读中文段落，unknown 为“结果待核实”。既有查询的三个共享文件由 #657 串行接，不由本实施者改写。
- #566：PostgresFollowupStore.recovery_status() 返回 contract_versions、compatible、inflight、recoverable、unknown，真实数据库测试确认陌生版本 incompatible；不是任意旧镜像兼容承诺。

## 追加验证与交接界限

- 装配提交 `4faeb36` 已交编排者；此后 gateway 的启动/退出共享文件与 scheduler 的启动/loop 共享文件停止写入，交 #657 串行补查询与日志。
- 独立恢复适配器改为现读账号状态决定重算或撤回，防止旧停用动作在账号恢复后仍重演历史按钮；当前权限的发布前守卫仍复用既有实现。新增真库正反例通过。
- 最后定向集合 22 条通过（18 条真库与 4 条纯生命周期/资源预算），含真实消费者发送回执未知后重复轮询零盲重发、管理卡复用后的同语句迟到更新拒绝。
- 相关静态门禁通过：迁移链、core 分层、数据库超时、文件/函数体量、注释卫生、Markdown 链接、验收矩阵状态与合同映射、合同归属、文档预算、用词；新增及受影响文件 ruff 与格式检查通过。两个新增测试的导入排序曾判红，修复后通过。
- 未启动本机全量测试：编排者通知旧 SDK 隔离测试存在实际启动风险，已遵守停止全量计划，最终全量与线上 CI 由修复后的集成候选执行。未将定向测试冒充完整门禁。
- 本地两消费者进程与强杀子进程均由测试 finally 回收。容器清理见最终收口项；本工作树保留供编排者 cherry-pick 与核对，Owner 为编排者，整合后由编排者显式移除，不删除用户原工作区。
- #668 须接其真实固定处理器（含 confirmation_card_send）、同一身份关联解析和 listener；#657 须接既有只读查询；#566 使用集成候选构建实际 v1 恢复制品。UDS 容量/UID/socket及整个四项共同恢复由 S6 统一验证。
- 未认领真实外发、Bot-Test、组织/日报实际平台进程故障窗口、受控恢复镜像运行或线上已有能力；这些依据来源合同留给后继隔离集成/外部验收，不以单测证明真实送达。

## 本实施包收口

- 最后实现提交 `ac463fe`；基底链为 `55d0cf1 → 4faeb36 → ac463fe`，均经指定 Desktop 提交入口形成，未推送。共享启动文件仍冻结。
- 会话开始 2026-09-09T02:23:55.454Z；开发与定向检查结束/容器移除回读 2026-09-09T02:57:20Z–02:57:53Z。最终文档提交属于收口，最终任务终点由编排者从会话取得。
- 原始会话来源 `/Users/wangzhipeng/.codex/sessions/2026/09/09/rollout-2026-09-09T10-23-55-01a083fa-9f8b-77c0-a4fc-496b1afe3e0b.jsonl`。02:57:53.470Z 的累计快照：input 10658168、cached_input 10452224、output 51858、reasoning_output 11286；缓存/推理分别为输入/输出子集，不相加。不是最终量，需编排者按原始会话最终区间去重。
- `docker rm -f lingxi-240-603-test` 成功；再次 `docker ps -a --filter name=lingxi-240-603-test` 返回空，合成库随容器移除。未删除预存 postgres 镜像，未触碰其他容器。
- 临时测试子进程全部已退出；临时日志与变异脚本清理，原始命令输出仍在上述会话，测试与证据摘要在本文件。保留本独占工作树及 codex 分支供编排者整合，清理 Owner 和方式见上文。
- 本次没有线上 Issue 写入、PR、推送、main/release 修改、发布、部署、真实飞书消息或业务身份权限操作。最终完整 CI 和跨项共同验收尚未执行，不认领 L3/L4 或线上可用。
