# S6 生命周期与恢复验证

- 状态：本生命周期/过程故障包定向验证完成，等待编排者整合；S6整体与S7最终候选检查不在本文件冒称通过。
- Owner：原 /root/impl603，gpt-6-astra/medium，复用会话 01a083fa-9f8b-77c0-a4fc-496b1afe3e0b；用量按本次 S6 区间单列，不与先前累计重复相加。
- 唯一工作区 `/private/tmp/lingxi-240-s6-20260909`，分支 `codex/240-s6-20260909`；接管时干净，基线 `b16421e8e4b50c98aa8bc739ec47d252a3a36bbb`，完整 S1–S5 集成代码。
- 合同：本地 v1.0；不执行代码审核、不下派、不作任何线上写或真实业务操作。
- 本地测试仅复制源码到 Linux 受 SDK 执行守卫保护的容器；禁止宿主真实配置/凭据、外网与 Docker socket。每轮检查 `/tmp/lingxi-sdk-execution-attempted`，存在则整轮失败。
- 独占资源：internal 网络 `lingxi-240-s6-net`；合成 PG `lingxi-240-s6-pg`（无宿主端口）；检查容器 `lingxi-240-s6-check`（3GiB/2CPU）。谁建谁清，结束时回读。未使用或操作其他项目容器。

## 退出回归修复

四个旧用例 patch 了已移除的 `join_onboarding_executors`，第一批 full 已实证 AttributeError。本次保留四个原行为断言，改为实际 SchedulerLoop → BackgroundLifecycle → DutyLifecycle → 职责 stop/join；不添加空 alias，不跳测试。四条在隔离 Linux 通过，异常路径仍排空并传播原始异常，收尾失败不遮蔽原异常，空闲连接最后关闭。四条旧退出回归只更新测试接口，未恢复旧别名或改变正式退出行为。

## 验证分工

- 本实施者：组织/日报真实进程故障、共同生命周期、listener与20人/正常首聊资源预算、24小时与停止计数。
- 原 /root/impl566：控制包、实际安装恢复、最终0092真库固定CLI与动态名单兼容、UID映射D3；其实际wheel/包证据单列，不用源目录测试或假镜像名替代。
- 全部最终候选 full 与线上 Epic Full 由编排者执行；真实飞书、SSH账号、stage/生产仍不执行。

## 第一批共同证据

- 组织快照实际子进程强杀：读取中、整批事务提交前、提交后，三个窗口均保留完整旧/新批次；重启完成后当日再次启动不重复快照。不是只用异常替身推断回滚。
- 日报实际子进程强杀：聚合中、发送前、回执未知、写水位前、写水位后。未知和水位前窗口实际2次调用、相同原dedupe、持久假传输1次交付；水位落库后新进程零再调用。假传输实现既有同日去重契约，不扩大重试窗口，不能外推真实飞书去重保证或把未知解释为未发送。两项测试含8个实际故障窗口，4.928秒通过。
- 二十四小时精确边界与边界前一秒、停止释放保持failure_count、新领取只增加attempt、过期领取按年龄结束已在真库通过；初轮因导入TestCase被unittest重复发现，随后改为模块引用，避免重复计数，不改变测试语义。
- 同一实际scheduler装配：20人/40阶段、正常首聊真实事件认领并复用同一个OnboardingExecutor、真实四连接listener、组织/日报后台职责、合成凭据续期。39.449秒完成，新增线程峰值4，共享DB槽峰值2/listener1，RSS 67836→72444KiB，FD峰值18，1156次RPC，主循环最大间隔0.0391秒。假外部Runner/Probe不证明真实权限或消息送达；真实池、监听线程、原子事务与持久阶段为本机实际执行。
- 发现真实停止缺陷：同socket缓冲3条请求，首条在途时stop，原实现仍开始后2条（实际3次，应1次）。增加每条请求前stop门后复绿，单独提交 `c9dc3ee`，已交编排者和566重建制品。不是事后排除失败用例。


## 实际截止与安装制品

- 两个实际独立Linux进程均接真实SIGTERM并再次接SIGTERM，不能把重复信号当成重新计时。Gateway包含两个投递线程、非幂等阶段与续租；scheduler包含原开通池、组织/日报后台线程、阶段、续租和实际listener。
- 真实运行观察：gateway 20.066秒、scheduler 120.080秒退出；组件排空分别20.006秒/120.009秒；截止均未延长。超预算分别报告4/5个仍在途职责，不称已取消；退出后PID不存在，socket消失，持久阶段分别恢复为unknown/可恢复，未声称超时外发失败。
- 首轮长窗口全部行为断言完成，但测试打印字段elapsed重名产生TypeError，保留为非绿检查；仅修测试输出后重新实际运行141.064秒，两窗口完整通过，未模拟时钟缩短20/120秒。
- 三项追加关键变异：停止释放错误计入失败、24小时精确边界错误延后、main跳过共同排空，指定用例分别断言验红；每项恢复原字节并核对摘要。listener stop门另有真实修前红/修后绿。
- 566交接实际制品：`lingxi-2.4.0-py3-none-any.whl`，SHA256 `d0f46ba9e799c550185d0b8972701a790ae3045c8ee5d016e2416bc9a91da0f2`，来源最终0092+版本提交d500cb9+c9dc3ee停止修复；实际只读安装，模块路径断言覆盖CLI、socket、handlers、v1仓储/消费者。其安装态真实领取后强杀、租期到期新进程继续两scheduler阶段一次、第二进程零重复、unknown零重发与listener否定用例4条通过，完整来源/安装记录由566独占维护。本实施者未重建或冒认该制品为四个业务镜像。
- 上述wheel是实际安装制品，不是假镜像名；不证明四服务镜像能在新schema真实启动。四镜像/发布制品检查仍由编排者在最终候选相应作业单列，真实平台与生产边界不变。所有模型SDK实际执行入口仍被守卫阻断，迄今未出现触发标记。


## 最后定向回归与证据摘要

- 所有变异恢复后45条定向Linux检查50.429秒通过、零跳过：四条退出回归、阶段边界、8个过程窗口、停止缓冲门、共同资源、真实MCP/UID/ACL、首聊共池、scheduler与实际续期信号退出。另6条既有gateway进程回归2.023秒全部通过；真实20/120长窗口单独141.064秒通过，不重复加总此前运行。
- 共同资源最终复验：39.443秒，新增线程峰值4，DB活动槽2/listener1，RSS 69904→72720KiB，FD峰值18，1132次RPC，最大主循环间隔0.2257秒。停止后新首聊事件保持未领取，新阶段不进入running；全部线程收回基线，关闭空闲连接后真库pg_stat_activity确认无其他客户端残留。该样本不是生产容量SLA。
- 相关静态检查：ruff/格式、分层、数据库超时、函数体量、注释卫生及原verify_docs全部通过。最终full/线上Epic Full仍由编排者跑，不把定向集合当完整门禁。
- Linux实际源码与本工作树src逐文件排序、路径+NUL+内容摘要均为 `e9f19733f2e8f0f1d83fc772240b52cd20308106dac434d6de85615f3da75890`，确认变异未残留。唯一正式代码增量为c9dc3ee；其余为测试与记录，没有改部署包、业务权限或发布分支。
- 实际检查镜像 `sha256:1959b3645c886abe4b4de8665e9634a09dec2c286f367737b2b2ad0a15b2e532`；合成PG镜像 `sha256:e013e867e712fec275706a6c51c966f0bb0c93cfa8f51000f85a15f9865a28cb`。检查容器Mounts=[]；PG无宿主映射；network Internal=true；SDK执行守卫最终回读False。

清理前容器内原始输出SHA256（输出正文保留于本次原始会话，临时文件随容器清理）：

| 输出 | SHA256 |
|---|---|
| s6-process.log | `7f3da10bb22019b3e5f7fe6726c678d1aa45807bd4c8c6a376ea2e3ce3061456` |
| s6-boundaries.log（首轮重复发现22项） | `cef5563e5e84e3002c8662e478461e7d8f204f05f629eb33f341f2b751fa04bd` |
| s6-listener-red.log | `de07b8558134b7102f671ce724110490e0e45652d00a58bf5e0b325ed1355b03` |
| s6-listener-green.log | `deb19dcad6da0d5727b8be26effb299d63618c9741cc4186f1b3265b9b262100` |
| s6-deadlines-first.log（输出代码报错） | `fed730277af2561c5eb6041289058f539d4d6131d313850e9d761edf6f6f0929` |
| s6-deadlines.log | `c92aef738ba2f80166387a6c02c150a1569a0af9f79a05dfac06f491e7b837a8` |
| s6-final-targeted.log | `e8801d6d6b26ca8c7cb9b40df9d5c0f3005c8de7d0ed507f33bd50cae31f9e82` |
| s6-gateway.log | `658c98caf39c777a5d4b1278ca1f083dd1574083d8fb855d75fee4d51752770d` |
| s6-mutation-stop_count.log | `11106381a5329a47daebdf90a33a1326685b5636b74e7880909b06211eea577f` |
| s6-mutation-age_limit.log | `6073a636a69a9d1f45a4c7f3446af4c9f1f5a3548b009da46aaa3656092e09b9` |
| s6-mutation-main_drain.log | `18d1d34f18493715d55e35f1e110738205569b1d969c71e9f03d1004178056c5` |
