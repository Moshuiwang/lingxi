# #657 实施记录

- 开始：2026-09-09T02:24:17.352Z；结束：2026-09-09T03:07:23.388655+00:00。
- 实际代理：`/root/impl657`；会话 UUID `01a083fa-f597-7673-8be9-9949d9d823b9`；模型 gpt-6-astra，effort=medium，已从原始会话 turn_context 实际回读。
- 用量来源：`/Users/wangzhipeng/.codex/sessions/2026/09/09/rollout-2026-09-09T10-24-17-01a083fa-f597-7673-8be9-9949d9d823b9.jsonl` 的原始 token_count；此处不估算，由编排者去重统计。
- 唯一工作树 `/private/tmp/lingxi-240-657-20260909`，分支 `codex/240-657-20260909`，基线 c19d53f；来源 #657 正文 SHA256 `a90fd7a50c0c8781cc1ebb1c34c29c9cd619a0e5ee1938682f06bcf82af1d3d7` 已完整读取并校验。唯一授权合同 v1.0。

## 已形成结果

领取、上下文和投递候选按事件键读取未过期合法原号；缺源从持久任务主键派生稳定 T-号。九类系统失败共用无 I/O 追加文案规则，三类结束不带号，历史终态先幂等判断后直接复用。两查询入口共享安全投影，T-查精确任务且遵守2160小时；原号同时遵守事件与任务各自期限。SDK 和终态通过每任务不可变配置携带关系，进程日志改 worker_run_id，原环境变量兼容。投递告警区分任务和事件字段并按任务去重。未新增迁移。

## 验证进度

- 定向单元回归397项通过：worker入口/预检、管理员解析/授权、告警、投递恢复及新增号码测试。
- 独占 PostgreSQL16 定向回归200项通过：新增号码与期限、原CLI/admin、队列连接、投递和outbox。
- 新增核心18项最近一次通过，含四并发回合和迟到回调、九正三负、缺源/坏源、锁竞争和截止。
- 内容版本/lock、用词、函数/文件体量、矩阵行体量/覆盖、core分层通过；完整CI由集成候选执行，不认领 L2/L3。
- 关键变异及恢复绿、#603 固定接口接线已完成；A/B 开发验证完成。

## 偏差、资源与清理

- 一次创建新模块时漏传工作目录，`task_trace_query.py` 短暂落在集成树；发现后仅搬回本代理新建文件并移除集成树副本，没有修改既有集成文件。后续命令显式使用独占目录。
- 安装SDK的验证环境暴露旧测试隐含假设：`test_worker_user_env_root_precheck.QueueModeUserEnvRootPrecheckTests.test_turn_mode_does_not_require_user_env_root` 直接调用 main，原注释假定SDK未安装，意外拉起 bundled claude。第一轮测试/SDK PID 92006/92013，诊断轮97457/97461均已TERM并回读不存在，也未见匹配SDK后代残留。是否曾建立远端会话未取得可证实记录，记为未知；不能据合成参数称零网络。
- 修复只在该测试用例作用域显式模拟SDK不可导入，并断言未调用子进程创建；未改正式SDK绑定或安装检查。移除诊断性的全局运行注入后，在原安装SDK环境397项通过，未再启动该进程。
- 独占合成库容器 `lingxi-657-synthetic-20260909`（c0416a9e7f23，127.0.0.1:16557）仅用于本项定向验证；不使用其他项目容器。清理结果见本记录末尾。
- 本工作树保留到编排者整合完成，届时由编排者移除；没有启动真实飞书服务或修改真实业务数据。

## 保留条件

R657-C 真实系统失败到用户复制号码、两入口和三段日志回查，仍待 #662 环境与独立验收授权；不由单元或合成真库结果代替。没有线上 Issue 写、PR、合并、发布或部署。

## 固定提交和完成证据

- 本项独立实现提交 `7c3ce16`，基于 c19d53f；本提交没有迁移。
- #603 来源55d0cf1/4faeb36/ac463fe在本树的副本分别408da50/350d974/bde27fe；这些是先项来源，集成时不得重复摘取。本项最后一个接线/收口提交由编排者按实际HEAD登记。
- 管理查询接入 `fetch_followups`/`render_followups`：源事件缺失但仍有有效管理阶段时，不伪造入站事实；unknown显示“结果待核实”及原追溯号和工作号。T-查询继续只指向精确任务。
- 投递候选链包含普通待投递与结果不明扫描，均在既有批量查询中按事件键读取原号，不新增逐任务查询。告警按任务/故障类型去重，缺源trace_id为null且附T-参考号；非任务告警保留既有安全事件标识。gateway装配已直接传入该回调，沿用#603停止预算，无须改其生命周期。
- 最终定向集836项通过（48.349秒，无跳过）：`test_task_reference test_task_reference_postgres test_worker_queue_consumer test_worker_entry test_worker_process test_worker_user_env_root_precheck test_worker_workspace_precheck test_trace_cli test_admin_registry_postgres test_admin_router test_admin_commands test_content_catalog test_content_override test_content_version_check test_queue_connection_reuse test_gateway_delivery test_gateway_delivery_resilience test_gateway_delivery_fairness test_delivery_outbox test_alerting test_alerting_integration test_admin_followup_consumer test_admin_followup_postgres`。命令为 `PYTHONPATH=src:tests <既有venv>/bin/python -m unittest <以上模块>`；真库只指向本项专用容器。此前836项第一次唯一失败为年份检测夹具把非正式task ID当有效任务，已将该有效业务夹具改成合法合成任务ID；坏ID否定测试仍保留。
- 随后补齐告警审计空值/参考号字段，69项受影响测试通过（0.139秒）；16项变异再验全部红，恢复后19项通过，均在此最终实现上完成。
- `verify_docs.sh`全部通过；ruff check/format、文件/函数体量、注释卫生、core分层、内容版本/lock均通过。不抬门禁预算；产品合同75469B/75473B，当前能力54716B/55296B。
- 16项变异：领取LEFT→INNER、错误关联键、领取/上下文/普通投递/不明投递各自删除源过期过滤、T查询删除任务截止、原号任务投影删除截止、admin/CLI各自删除事件截止、系统终态漏追加、worker终态漏追加、标识校验移除、授权判断移除、告警移除任务去重键、服务共享current_task_id/current_trace_id。每项独占树修改→目标测试RED→finally恢复；最后整体19项GREEN。SDK迟到回调和同一服务四任务覆盖没有通过共享当前任务变量实现。
- 固定旧源码c19d53f经临时导出，在同一合成库用旧正式事务接口创建有源/无源/过期源/已有无号终态四类任务；新版分别读原号/T号/T号/旧正文，模拟新队列实例恢复仍唯一终态。旧版重新读取新版终态、继续创建执行第五任务均通过，未做schema回滚，回退后新失败保持旧无号提示。临时旧源码仅为测试，交付前删除。
- `EXPLAIN (FORMAT JSON)`读取实际claim方法内SQL，LockRows的关系后代仅为task；真库另一事务持有源事件FOR UPDATE时claim仍能完成。与并发屏障每任务仅领取一次、目标版本不变共同构成锁语义证据。
- 本任务没有代码审核（包括自审/独立审核），所有问题均来自实施自测与门禁；L3不认领。完整仓库和CI由集成候选继续，R657-C明确保留。

验证原始输出清理前SHA256：

- `final-targeted.log`：`bfc1a514fc0da3bfb1bff0400356e94e63fae99afd7905e91e80da8e86ce6933`。
- `final-affected.log`：`07f23c4c33b4e5d7bc7b967ca803129f051e57eb704e86bec1e591fb0edafe7c`。
- `final-mutations.log`：`a63448b5871c589611ddee605500fe39e78aee850f8979a0260facad8b956f3c`。
- `mutation-restored.log`：`8c322ee080f57b394291ec7ed01fd92e1fc32afc44dd8caf30ef537cbff714f1`。
- `compat-check.log`：`c10532e2b2d1df4f2b1bb421eda12d86134a4ca3bb2411b8ae0b4711eb30c1b5`。
- `explain-plan.json`：`70751daf49d8314b80659b404a67b4d80d6e7e17dd75b49f1a88047b0695543d`。

临时原始输出不包含真实业务数据，收口后删除；本记录保留结果和重放入口。

## 收口清理

- 2026-09-09T03:07:23.388655+00:00：本项合成库容器已停止并删除，按精确名称回读无记录。旧源码导出目录、变异脚本/缓存、兼容脚本和本项原始输出均删除。两轮意外SDK/测试PID精确回读无记录；最终用例作用域隔离已通过。
- 唯一保留：本实施worktree与codex分支，供编排者摘取最后提交；Owner为编排者，整合后使用 `git worktree remove /private/tmp/lingxi-240-657-20260909` 清理。未留数据库、临时服务或SDK进程。

## SDK 意外启动的最小事实补充

本节只使用上述原始会话中已取得的脱敏输出与 `CommandExecution.started_at_ms/completed_at_ms`。时间均为2026-09-09 UTC（北京时间加8小时）；没有读取宿主凭据或真实SDK配置，也没有为补证发起外部调用。

触发用例为 `test_worker_user_env_root_precheck.QueueModeUserEnvRootPrecheckTests.test_turn_mode_does_not_require_user_env_root`。它原来直接进入 turn 模式，注释假定SDK未安装。第二轮带测试名的输出停在该用例，紧接着进程快照显示 bundled claude；第一轮相同测试集与相同参数的启动据此定位为同一问题。第一轮未开启逐用例输出，不能声称单靠第一轮日志就取得了相同精度的定位。

| 轮次 | 实际取得的启动证据 | TERM 命令执行区间 | 清理回读 |
| --- | --- | --- | --- |
| 第一轮 | 测试调用时间02:40:48.727；02:43:15.250–02:43:15.458的快照见测试PID92006（已运行02:26）及SDK PID92013（已运行02:21）。因此SDK启动可界定在02:40:48.727–02:43:15.458；按整秒运行时长推算约02:40:54，精确启动时刻未知 | 02:43:32.079–02:43:32.255，`kill -TERM 92013 92006` 返回0 | 02:44:07.815–02:44:08.038快照已不列出92006/92013；此时识别出第二轮SDK |
| 第二轮诊断 | 诊断调用时间02:43:56.506；02:44:07.815–02:44:08.038快照见SDK PID97461、父PID97457，SDK已运行00:11。启动窗口02:43:56.506–02:44:08.038；精确启动时刻未知。诊断性的全局模块占位未能保持隔离，因此同一缺口再次触发 | 02:44:22.006–02:44:22.191，`kill -TERM 97461 97457` 返回0 | 02:46:12.541–02:46:13.949的测试及进程回读没有列出这两轮PID，也没有实际的匹配SDK进程行；匹配到的只是检查命令自身 |

03:06:38.715–03:06:39.170又按四个PID精确回读，输出仅有列标题、没有进程行；`ps`因没有匹配进程返回1。以上证明列明的测试/SDK进程及当时匹配到的SDK进程不再出现；没有取得完整历史进程树，不能扩写为证明所有可能曾出现的后代或远端对象都已清理。

| 外部行为 | 已取得的证据 | 可支持的结论 |
| --- | --- | --- |
| 网络连接或请求 | 只有本机进程命令行与合成测试输出；未取得这两个SDK PID的套接字、请求或网络审计记录 | 是否发起过网络连接、向何处请求、是否收到响应均未知，不能记为零网络 |
| 认证或宿主配置读取 | 进程参数包含 `--setting-sources=`、`--strict-mcp-config`；没有认证结果，也未读取凭据或真实配置。参数本身不能证明其他配置或凭据来源未被使用 | 是否读取宿主真实配置、取得凭据或完成认证均未知 |
| 远端会话 | 没有可归属这两个SDK PID的远端会话标识、创建/终止回执或服务端记录 | 是否创建过远端会话及其状态均未知；本机TERM不能证明远端会话不存在或已终止 |
| 业务调用与数据副作用 | 测试传入合成问题和 `mcp__query__noop`；没有可归属意外SDK进程的业务工具调用记录、服务端回执或数据回读。其他假SDK用例的502等日志不能作为这两轮远端调用的证据 | 是否发生业务调用或数据副作用均未知，不能用合成参数推断零调用 |

最终隔离修复只作用于该用例：用 `mock.patch.dict("sys.modules", {"claude_agent_sdk": None})` 明确安排“依赖不可用”，并用 `mock.patch("asyncio.create_subprocess_exec", ...)` 与 `spawn.assert_not_called()`证明该用例不创建SDK子进程。去掉诊断性的全局运行注入后，安装真实SDK的原venv于02:46:12.541–02:46:13.949完成397项测试、0.954秒、OK；该用例的作用域补丁和子进程创建断言包含在内。后续836项定向集也通过。

后续完整本地检查仍须防止意外加载宿主真实SDK配置：已证实的隔离入口是上述具体用例的作用域补丁，以及需要SDK交互的既有用例自行安装 `FakeAgentSDK`。可先定向运行该具体用例确认隔离仍在，再执行获批的完整检查；不能把这个单用例结论推成全套测试已排除宿主配置，也不能依赖“机器上没有安装SDK”。本次未改全局测试门禁、正式SDK绑定或安装检查，不将全局 `sys.modules=None` 作为建议入口。
