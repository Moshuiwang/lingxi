# rc21 清卡冲刺验收

## 用户可见验收

| Issue | 用户 / 管理员得到什么 | 关卡证据 | 目标等级 |
| --- | --- | --- | --- |
| #437 | 过期确认不会继续占坑；未过期拦截会告诉管理员正在等什么、怎么办 | stage 构造过期 / 未过期两态，数据库 + 回执双回读 | L4a（受控测试身份） |
| #438 | 管理员确认后分钟级生效，不再等每日批 | 真库确认回调→定向重算→发布行更新；PM 完成一次真实卡片点击 | L2 真库 + L4b（需 PM 约 10 秒） |
| #439 | 管理员只用邮箱、中文名称和一张管理卡即可查看、授权、抑制、收回 | 管理卡 JSON、提交/确认真库集成、真实送达回读；安全确认仍独立 | L2 真库 + L4a 送达 |
| #440 | 有限指标 `"*"` 用户的额外授权真实并入，不被误判冗余 | 修前验红、修后发布表回读、无窄化断言 | L2 真库 + L4a |
| #441 | 旧表额外权限可审计、可收回、可重复导入而不重复 | dry-run、测试副本导入、发布表回读、重跑零重复；正式导入留 #263 | L2 真库 + L4a stage |
| #442 | 文档标题、列表、正文和负号按原顺序交付；不支持结构明确失败且不留半成品 | 真实飞书文档创建 + API 逐块回读 + 嵌套否定探针 | L4a |
| #443 | 所有用户可见面只出现 BI Plus，不再暴露内部代号 | 六键渲染断言、白名单 grep、stage 真实提示回读 | L2 + L4a |
| #444 | 十几秒内必有更新；卡住时明确说卡在哪一步，恢复后继续 | 卡片事件序列、停滞注入、恢复、配额余量报告 | L4a |

## 固定候选与工程关卡

- [ ] 八张 Story PR 的定向测试与 Story Fast 通过；合并次序、内容版本和 Alembic 单链检查无冲突。
- [ ] 集成分支本机 `full` 通过；明确说明它不等同整条 Epic Full。
- [ ] 唯一候选 SHA 冻结，静态排除清单逐项对账。
- [ ] 恰好一名 Sol xhigh 独立审核者给出无 P0/P1 遗留结论；实施者自查、CI 与事实核账不算第二审核。
- [ ] 修复后同一审核者定向复核；最终候选变更已使旧结论作废并重新绑定。
- [ ] Epic Full 成功；合并树等于候选树；Main Publish 产出同一批四镜像 tag 与 digest。

## stage 关卡

- [ ] `biai-stage` 三个常驻容器 healthy，数据库仍为测试 Supabase，worker 并发不超过 stage 资源上界。
- [ ] Bot-Test / 测试数据库 / 受控注入的共享租约串行执行，无第二长连接、临时进程或测试残留。
- [ ] 八卡证据逐卡回写，所有“用户看到”断言都有用户可见回读 + 平台 API / 服务日志第二通道。
- [ ] #437 的两态复测通过，生产挡板解除仅表示可以进入 #448，不表示生产已上线。

## 关闭与交接

- [ ] #437～#444 均按自身完成标准关闭；#418 对账关闭；长期产品事实已回写仓库正文。
- [ ] `任务表.md` 与本文件状态更新，worktree / 临时容器 / 受控注入资产盘点为零残留。
- [ ] #445 留下批次收口和自足交接评论，包含候选、四 digest、证据、缺陷、待观察项与 #448 第一恢复点。
- [ ] #448 的新 Sol max 顶层编排任务已创建并登记接管后，rc21 编排任务才退出。

## 不得混淆的结论

- `Story Fast` / `Epic Full` 通过不等于用户路径完成。
- `Main Publish` 成功不等于 stage 已升级，也不等于生产可用。
- stage L4a 使用受控测试身份，不等于 `Bot-Prod` 或正式用户验收。
- 本 Trace Complete 只表示 rc21 候选与 stage 就绪；真实上线只由 #448 关闭来证明。
