# 浏览器验收操作指南

状态：首版操作方法，依据 [Issue #636](https://github.com/Moshuiwang/lingxi/issues/636)。仅固化验收手段，不新增产品承诺。证据等级以[验证与门禁](../技术设计/验证与门禁.md)为准。

## 入口与适用范围

适用于提供浏览器自动化工具、已有授权登录态的本地验收任务，**环境限于 `biai-stage` + Bot-Test**。开发提交请求，验收代理通过飞书真实用户入口发送并读取结果。工具由执行环境提供，仓库不保存凭据、不启动另一个机器人长连接；无工具、无登录态或不支持当前浏览器标签页时，**先按《验证与门禁》的真人兜底判断**：该断言若是真人可做的，转 L4b 由真人完成；**只有真人也无法完成时才记 BLOCKED**——工具故障不得成为验收的单一出口。另按同一处规则，**API 能判定的客观事实（状态、权限、产物、消息、审计）一律以 API 回读为主，本路径只验用户可见交互**，不得用浏览器观察替代 API 回读。（本文里「标签页」一律指浏览器标签页，「标签」一律指 GitHub label，不混用。）

内置浏览器可用不等于任何子代理或定时任务都能访问它。当前执行者必须实际发现目标标签页；禁止用其他账号、另行登录或 API 伪造入站代替。按工具自身文档初始化，以下脚本示例只适用于其暴露的 CUA/Playwright 接口，不是独立 Node 脚本。

## 执行前检查

1. 核对请求版本、受测部署版本、账号别名、机器人、允许副作用及截止条件。正式验收必须绑定实际部署；部署未知时只能出探索记录，不能给候选验收通过结论。证据等级按《验证与门禁》「三、证据层级」写：代理在 stage 走真实链路记 `L4a`，真人记 `L4b`；**没有 L 名的组合按「成熟度 · 环境 · 执行者 · 操作类型」显式写全，不得就近借用 L 名**。
2. 由**当前 Trace 的编排者**（无 Trace 时为提出请求的开发方）确认服务就绪并在请求里附就绪证据。**该证据有时效**：《验证与门禁》要求「执行前必须实时确认」，因此领取时须由执行者自己再实时回读一次服务状态，请求里的就绪证据只用于判断该不该领，不替代执行前的实时确认。测试环境未启动时不发送、不空等，不擅自启动被其他任务停止的实例。浏览器能打开不是机器人健康证明。
3. **只用 Bot-Test，不碰生产。**《验证与门禁》「安全与凭据底线」规定「任何测试不得使用 `Bot-Prod` 凭据或生产数据库」——浏览器验收没有例外。stage 不可用时记 BLOCKED，**不改测生产**。将来若确需生产冒烟，先另立卡取得产品负责人对该条底线的明示例外，本文不预留口子。注意向机器人发消息会产生入站消息、机器人回复、会话上下文与审计记录，**是写入不是只读**。运行中版本改变则本次证据不再覆盖新候选。
4. 一个浏览器同一时刻一个操作人。若用户接管，停止操作；先检查输入框是否有草稿，保留原草稿，不覆盖、不发送。**已知边界（跨 Issue 无机器互斥）**：领取互斥只在单个 Issue 内成立，两个不同请求各自满足领取条件时，协议层挡不住两个执行者同时操作同一个宿主浏览器。当前靠「人工唤起、同一时刻只唤起一个 Codex」兜住；改成自动唤起前必须先补一个跨 Issue 的机器互斥（例如宿主上的 lockfile）。

## 账号与机器人选择

在当前已登录页面发现账号切换入口和置顶会话，不能只凭头像、序号、上次 URL 或聊天历史识别身份。具体姓名、组织与租户映射由受限测试请求提供，公开报告用业务用户/管理员别名。

当前飞书页面已验证的定位方式（DOM 改版后重新发现）：

| 目标 | 选择方式 | 完成条件 |
|---|---|---|
| 当前身份 | `.navHeader_avatar` 打开资料菜单 | 姓名和组织均与请求一致，核对后收起菜单 |
| 账号切换 | `.user-auth-list_itemBox`，当前项带 `.user-auth-list_itemBox-active` | 等导航和消息区加载，再读资料确认身份 |
| 机器人 | 置顶 `region` 的 `scrollable content` 内按完整名称精确选择 | 聊天标题和输入框接收对象均吻合 |

已发现且恰好只有两个账号时，非 active 项可用于切到另一个；账号数增加或身份不明确时停止该捷径。测试机器人显示 `BI-Plus-测试用机器人`，**本路径只操作它**；正式机器人显示 `BI Plus`，列出仅为**识别并避开**，不得选中。每次仍须实时核对。

账号切换与会话切换允许分别等待最多 30 秒（执行预算，不是产品 SLA），按页面实际就绪提前结束；跨域加载期间重新获取定位器。资料菜单打开时第一次点击会话可能只关闭菜单，必须先收起再选择。工具报超时也先独立回读，不盲目重按。

## 每轮输入输出与计时

每轮记录 UTC 时间及毫秒差值：开始准备、账号就绪、聊天就绪、输入完成、**发送前**（`sendAt`）、**发出确认**（`sentAt`）、**工具状态刷新完成**（`axAt`）、**首次回应**（`observedAt`）、最终结果、核对完成、报告回读完成。未观测到的时间写 null，不能填 0 或推算成精确时刻。

- 准备时间包括宽裕的账号/会话等待；不混入服务响应时间。
- 用户等待从**发出确认**（`sentAt`，不是发送前）到首次回应／最终结果分别计算——`sendAt→sentAt` 是按键到真正发出的这一段，属工具开销不算用户等待。**`observedAt − sentAt` 里仍包含 `getAXState` 的耗时**（`axAt − sentAt`），报告时两个数都要给，不得声称已排除。表情、排队提示、正文终态分别标记，不能把表情当最终结果。
- 记录每次观察区间，浏览器时间是“首次观察到”，不是后台事件时间。工具内部计时不含派发到工具开始之前的开销；整条 case 还须记录领取到报告交回的时间。
- 关联新输入消息 ID、新输出消息 ID、所在会话及回复引用。同一请求重试增加 attempt，不覆盖失败记录。发送结果不明时先回读；已发未回复不得自动补发。
- 只读取本次消息；新挂载的旧历史、侧栏摘要、自己刚发出的文字不算机器人回应。首次结果之后继续等对应卡片的最终状态，不能全页搜索“已完成”。
- 服务处理、排队、MCP 和投递分段需要后台同号时间线；仅有浏览器证据时保留为合计，不能猜测根因。

## 可复用脚本步骤

以下是在已核对身份、机器人和空白输入框后执行的片段。**实测环境**：Codex 内置浏览器（CUA）暴露的 Playwright 兼容接口，2026-09-06 在 macOS 版 Codex 上跑通（**执行时须回读并记录具体版本号**：`codex --version` 与浏览器内核版本，填进结果模板的「工具版本」栏；日期与操作系统不足以唯一确定版本）；`timeoutMs`、`getAXState`、`nodeRepl` 都是该封装的接口，**不是 stock Playwright**，换工具或换版本必须重新验证本节全部调用。`tab` 与 `nodeRepl` 来自执行工具当前文档的标签页发现步骤。先读输入，再发送一次；每个动作后按工具要求取得最新状态。

```javascript
const editor = tab.playwright.locator('div[contenteditable="true"]');
if (await editor.count() !== 1) throw new Error('输入框不唯一');
const draft = await editor.locator('[data-string="true"]').allTextContents({});
if (draft.join('').replace(/\u200b/g, '').trim()) throw new Error('存在草稿');
const startedAt = Date.now();
// 先确认消息区已挂载，否则「空列表」分不清「全新会话」还是「还没加载完」——
// 后者会让下面的 fresh 选择器匹配到全部历史消息，造成假 PASS。
await tab.playwright.locator('.js-message-list, [class*="messageList"]').first()
  .waitFor({state:'attached', timeoutMs:5000});
const oldIds = await tab.playwright.locator('.js-message-item[id]')
  .evaluateAll(es => es.map(e => e.id));
if (oldIds.some(id => !/^\d+$/.test(id))) {
  throw new Error('消息基线形状异常');
}
// 走到这里 oldIds 为空才是可信的「全新会话」。
await editor.pressSequentially('/admin help');
await editor.locator('[data-string="true"]').filter({hasText:'/admin help'})
  .waitFor({state:'visible', timeoutMs:3000});
const input = await editor.locator('[data-string="true"]').allTextContents({});
if (input.join('').replace(/\u200b/g, '') !== '/admin help') {
  throw new Error('输入不一致，停止发送并检查草稿');
}
const sendAt = Date.now();
await editor.press('Enter');
// 发出确认：飞书可按账号设为 Ctrl+Enter 发送，此时 Enter 只换行。先确认输入框已清空。
// 这里超时**只说明发送结果不明**（可能没发出，也可能发出了而输入框清空延迟），
// 按本文「发送结果不明时先回读」处置，不得判为「没发出去」，更不得自动补发。
await editor.locator('[data-string="true"]').filter({hasText:'/admin help'})
  .waitFor({state:'detached', timeoutMs:3000});
const sentAt = Date.now();
await tab.getAXState({emit:false});
const axAt = Date.now();   // 该调用耗时不定，且必然落在 sendAt 与 observedAt 之间
const fresh = '.js-message-item[id]' + oldIds.map(id => `:not([id="${id}"])`).join('');
await tab.playwright.locator(fresh).filter({hasText:'当前角色：'}).first()
  .waitFor({state:'visible', timeoutMs:5000});
const observedAt = Date.now();
// 注意：'当前角色：' 是产品文案，改文案会让这里超时。超时先判「断言过期」再判「无回复」。
const messages = await tab.playwright.locator(fresh)
  .evaluateAll(es => es.map(e => ({id:e.id, text:e.innerText})));
// 接下来核对消息发言方、引用和正文；messages 是待判证据，不自动等于 PASS。
nodeRepl.write({startedAt, sendAt, sentAt, axAt, observedAt, messages});
```

这段只覆盖帮助样例：无回复时等待会抛出超时，停止并回读，不能重跑整个片段。若需要继续观察，保留 oldIds/sendAt，在请求的绝对截止时间前只重试读取；单次等待控制在工具允许上限内，不能借由循环延长总预算。真实权限、数据和交付用例需要各自的终态/回源断言，不能套用“当前角色”条件。

富文本框不能把占位文字当草稿；通用 fill 在实测中出现状态不同步，优先用键盘事件和文字回读。CDP 可用于页面诊断，但不直接篡改页面来制造通过结果，不抓取凭据。截图只用于布局、显示异常及脱敏后的失败证据。

## 首批用例

这里只增加执行配方，不复制产品规则，不翻转既有矩阵状态。一次帮助通过不能代表整行断言通过。

| Case | 输入/操作 | 预期及证据范围 |
|---|---|---|
| B00 | 两个已授权账号各自选中 **Bot-Test 会话**，不发送 | 姓名/组织、聊天标题和接收框一致；验收工具前置，无新增产品断言。**不得选中正式机器人会话**——本路径不碰生产 |
| A01 | 管理员 `/admin help` | 新回复含帮助和角色；只验证 [V-管理-26](../技术设计/验收矩阵-接入与管理.md) 的帮助入口，不证明注入防线或审计 |
| Q01 | 明确公司、指标、绝对日期的单次问数 | 公司/指标/单位/日期符合请求，关键数字与独立基准相符；依据[可信标准](../产品合同与外部边界.md#问数结果的可信标准)，基准缺失记数值未核验 |
| Q02 | 接 Q01，明确追问某维度 | 沿用正确上下文，不混其他用户/话题；依据[会话分册](../技术设计/验收矩阵-会话与执行.md)，认领时选择相应 V-会话 行 |
| D01 | 将已核对结果整理为文档 | 链接可打开、正文一致、本人权限正确；依据[交付分册](../技术设计/验收矩阵-交付与投递.md)，建档成功提示不能代替打开和权限回读 |
| S01 | 将已核对结果整理为电子表格 | 表头、行数和关键单元格正确，打开及权限回读；依据同一交付分册 |

Q01/Q02/D01/S01 在请求 READY 前填明具体 V-*、输入和基准；目前是待实例化配方，不是已执行清单。首批不包括停用、扩权、删除或故障注入；这些必须另行明确对象、副作用、审批和恢复方案。

## 请求与结果交接

沿用 Issue/PR，不建新平台；开发完成部署后发布请求，验收代理领取并写 `RUNNING`（对应 GitHub 侧的 `执行中` 标签）。当前为人工唤起执行，没有自动监听或全天候可用承诺。开发终端不是唯一收件箱，报告不会自动注入其他开发会话。

请求最少字段：`request_id`、`revision`、来源工作项、候选 SHA、部署制品、环境、账号别名、机器人、case/断言、精确输入、预期/独立基准引用、**授权来源**（谁在什么时候批准用该账号发送，附可点击留痕）、允许副作用、等待预算、清理责任、**受限证据存放位置**。缺项时 BLOCKED；探索记录可声明部署未知，但不得标记发布验收 PASS。

结果模板（每 case/attempt 一份）：

```text
request_id / revision / case_id / attempt:
执行者与模型 / **工具版本**（codex 版本 + 浏览器内核）/ 环境 / 账号别名 / 机器人:
候选 SHA / 实际部署 / 版本核对时间:
状态: PASS | FAIL | BLOCKED | RUNNING | NOT_RUN
  （标签映射：RUNNING=`执行中`；FAIL / 部分通过 / BLOCKED 后回 `pre-ready` 或 `阻塞`；
   PASS 且范围全部完成=关闭 Issue；NOT_RUN 用于本 attempt 未执行的 case，不单独置标签）
开始 / 账号就绪 / 聊天就绪 / 输入完成 / 发送前 / 发出确认:
发出确认 / 工具刷新完成 / 首次回应 / 最终结果 / 核对完成 / 报告回读完成:
输入、输出与消息关联的受限证据引用:
预期 / 实际 / 逐项结论 / 未核验项:
准备 / 首响应 / 最终响应 / 核对报告 / 总时间:
异常、恢复与清理:
证据层级及不能支持的结论:
```

**部分通过按 FAIL 处置**：只要请求范围内还有 case 未通过，整份请求就不算 PASS，逐 case 写清哪些过哪些没过。同一请求版本仅一个执行者，领取写有效期；到期或中断接手前先核对是否已经发送。重试另记 attempt，部署变化需新 revision。提交后读回报告。公开 Issue/PR 只放脱敏状态和差异；业务数据、姓名邮箱、聊天截图及内部地址不公开，受限证据须能由指定接手者访问，不能只给另一台机器的本地路径。**允许的存放位置**：`biai-stage` 上的 `/home/wangzhipeng/verification-evidence/<request_id>/`（**写绝对路径，不写 `~`**——`~` 随 ssh 登录用户变化，接手者可能解析到别处）。请求方必须在「受限证据存放位置」字段写明该绝对路径，并确认**指定接手者对该主机与目录确有读权限**；确认不了就记 BLOCKED。没有可用位置就记 BLOCKED，不要退回本地路径。

## 首版证据与限制

2026-09-06 已观察到账号与机器人会话可切换（该次探索在本路径收窄到 Bot-Test 之前进行，其中触及正式机器人会话的部分**不构成本路径的先例**，也未发送任何消息）；脚本帮助请求收到新回复，复用样例单次工具内约 3.54 秒（不含报告）；服务停止时无回复。它们是方法探索样本，未绑定部署 SHA，不据此升级产品当前能力、矩阵状态或响应 SLA。完整原始业务内容不入仓库，脱敏交接演练见 #636。

收口时清理自建的浏览器标签页、进程和临时 worktree；用户原有的标签页保留。**不按「清理」的名义动 GitHub label**——label 只按《协作约定》的状态链规则迁移（例如完成时把 `执行中` 换成关闭、FAIL 时换回 `pre-ready`），那是状态迁移不是清理。需要保留的测试产物登记用途、清理责任和方法，不自动删除用户聊天或既有产物。
