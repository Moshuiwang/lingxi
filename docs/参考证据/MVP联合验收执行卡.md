# MVP联合验收执行卡

> 状态：Epic E（Stage 安装恢复与 MVP 员工路径联合验收，[Issue #161](https://github.com/Moshuiwang/lingxi/issues/161)）验收资产之一（S-E-01）。最后整理：2026-08-18（编排者交叉审查修复包，35 条发现逐条处理，见文末各节标注）。
>
> 目标：产品负责人**一次坐下、按本卡逐条执行就能跑完**，中途不需要问任何人。每一条都写清楚**谁操作、预期看到什么、失败怎么办**。本卡不重复产品规则或断言正文，只给"这一刻该做什么"；规则正文见[产品合同与外部边界](../产品合同与外部边界.md)与[产品验收矩阵](../技术设计/验收矩阵.md)，逐点检查见[员工旅程验收检查单](员工旅程验收检查单.md)。

## 阅读方法

- 本卡只服务这**一次**集中验收窗口（阶段①取消后预计 3–3.5 小时，含缓冲，见文末）。窗口开始前的全部准备工作（候选冻结、环境搭建、凭据核实、Stage 安装与健康回读）**必须由编排者提前独立完成**，不占用产品负责人的窗口时间——见下方「窗口开始前：编排者独立完成」。
- 窗口内含②③④三个阶段（**②→③→④严格顺序**）。原阶段①（云盘九步探针）已随 2026-08-23 #140 产物路由裁定整体取消，见下方阶段①的历史注记。
  2. **Epic D 首次开通 L4a**（B/N1 各发一条消息，P1 首触改为引用 2026-08-21 已有证据、不重复发送，18–28 分钟——权限同步允许合同内最多十五分钟的合法等待，不是失败）
  3. **受控暂停窗口**（暂停生产旧系统双写入 30–60 分钟；本轮**不做真实发布写入**，见该阶段说明）
  4. **Epic E 联合验收**（完整员工路径 + 四线留证 + 并发/隔离/重连/重投）
- 三个测试身份一律用化名：**P1**（正向，产品负责人本人企业账号）、**B**（设计性负向，专用授权主体本体）、**N1**（负向，产品负责人个人租户账号）。三者**全部由产品负责人本人操作**，不得由他人代持；三者都是**一次性、不可复现的资源**——每个身份只能真实走一次首次开通，中断后不得重发，详见「中断与恢复」一节。
- 每条动作标注执行人：**P1** / **B** / **N1**（产品负责人以对应身份操作）、**编排者**（受控环境中的代理或工程操作，不需要产品负责人参与）。
- 任何一步的"预期"与实际不符：**立即停止本阶段后续步骤**，按该步「失败怎么办」处理，不要跳过继续做下一步再回头补——**唯一例外是合同内的合法慢路径**（例如阶段②的十五分钟权限同步等待），那不是失败，见阶段②说明。
- 本卡不含任何真实主机名、内部路由、凭据或真实账号标识；涉及内部坐标的操作细节由编排者在受控环境内自行核对，不写入本卡。
- 产品负责人窗口前 10 分钟自查（见文末「产品负责人窗口前自查」），确保窗口内不因为账号切换或截图设置浪费时间。

---

## 窗口开始前：编排者独立完成（不占用产品负责人时间）

这是 Trace v12「验收轮环境前置自检」机制的可执行落点：产品负责人 2026-08-17 已裁定该项**降级为由验收执行卡逐轮实例化**，即下表就是这一机制在本次窗口的实例化。

| # | 检查项 | 完成标准 | 来源/依据 |
|---|---|---|---|
| E0 | 候选装配核对 | 候选的 gateway 已注入**真实开通编排**（不是失败关闭桩 `_UnavailableOnboarding`——[当前能力](../当前能力.md)已登记 main 上装的就是桩，若候选未替换成真实实现，本窗口无法进行阶段②）；问数 MCP 从 `biai-stage` 可达且 P1 令牌可用。编排者以 E7 冒烟的**实际用户可见文案**为证，不接受"代码里有这个类"当证据 | 任一不成立 → 本窗口推迟，只保留不依赖 Lingxi 装配的阶段① |
| E1 | 冻结候选唯一 | 本次验收对象是同一个合并前冻结候选 SHA/artifact/digest；scheduler/migrate/worker/gateway 四镜像 manifest 一致 | 见 [Issue #161](https://github.com/Moshuiwang/lingxi/issues/161)「进入条件」 |
| E2 | Stage 安装与健康 | `scripts/stage_rehearsal_mvp_acceptance.sh --list-steps` 中 `preflight`/`migrate`/`up`/`healthcheck` 四步已在 `biai-stage` 真实跑通一遍（编排者自己先演练，不是产品负责人窗口内首次执行） | 见[Stage 演练脚本](../../scripts/stage_rehearsal_mvp_acceptance.sh) |
| E3 | 受控触发夹具**三面**均干净 | `python3 scripts/acceptance_fixtures.py --check-clear`（宿主 shell）+ `--check-files <七个env文件>`（env 文件）+ 对 gateway/worker-queue 两个容器执行 `env \| python3 scripts/acceptance_fixtures.py --check-files -`（容器内实际环境）均返回"未查到残留"。**只查宿主 shell 是不够的**——夹具经 env 文件注入的是容器，不是执行检查命令的那个进程（2026-08-18 修复包 F2） | 见[夹具契约测试](../../tests/test_acceptance_fixtures_contract.py) |
| E4 | 外部触点具名清单实时可用 | 逐项确认而非"历史可用"（[验证与门禁「六、受控真实链路」](../技术设计/验证与门禁.md#六受控真实链路)）：飞书开放平台、`Bot-Test` 长连接、权限发布多维表格（读写权限）、问数 MCP 端点、Lingxi 数据库连接，五项均在窗口开始前 30 分钟内逐项确认可达 | 窗口开始前 30 分钟内确认 |
| E5 |（已作废，#97 随 2026-08-23 #140 裁定关闭）#97 探针执行卡就绪 | 执行 Owner 与工具**在候选中已就位**（另一分支产出的[专属云盘目录九步探针执行卡](https://github.com/Moshuiwang/lingxi/issues/97) + `scripts/probe_drive_folder_permissions.py`，含契约测试与四条硬停止条款，已先于本卡合入——本卡阶段①**只引用它，不重复其九步正文**，避免两份卡各写一遍导致漂移），其 Wave 0 Gate `G-97` 全部启动门槛已由编排者盘点为 `ready` | [Issue #97](https://github.com/Moshuiwang/lingxi/issues/97) 门槛盘点评论 |
| E6 | #52 花名册专用主体凭据 | 若真实读取所需的专用主体凭据仍未落盘（[Issue #52](https://github.com/Moshuiwang/lingxi/issues/52) 的 G-READ 判定），登记为已知边界并通知产品负责人：本轮 #52 线只能给出结构化职责调用证据，不能给出真实群送达证据，不阻塞其余三线 | 见[员工旅程验收检查单 §E](员工旅程验收检查单.md#e-花名册审计日报52-线) |
| E7 | 真实链路首触冒烟（**一律用第四个受控账号，不得触碰 P1/B/N1**；**唯一例外见下方"附录 A 三句问句"改用 P1 的裁定**） | 每条本卡涉及的用户可见路径（开通成功/无权限、正常问数、`/stop`、`/new`、卡片降级、重投）已由编排者用**专用于冒烟、与 P1/B/N1 完全不同**的第四个受控测试账号非正式跑过一次真实链路；发现的问题已进入缺陷账本处理，不带着未知的"第一次真实接触"进产品负责人的窗口。**这条硬性要求是本次修复包新增的**：P1/B/N1 各自只能真实首次开通一次，冒烟一旦误用它们会让阶段②在窗口内永久失去可观察的三段文案。第四个受控账号已由产品负责人指定，登记于受控身份文件（本卡属公开正文，不写账号具体信息） | Trace v12 机制①，见下节 |
| E8 | P1/B/N1 账号初始态核对（**P1 按 2026-08-21 裁定改走复用证据，不再要求「无记录」**） | 编排者在窗口开始前回读确认：候选库中 B、N1 两个身份均**无** `app_user` 记录、**无**任何 `provisioning` 相关记录（即尚未被消耗）。若发现某个身份已被意外消耗（例如被上一轮排练误用），本条登记该身份的重置方法与执行人，且必须在窗口开始前完成重置并二次回读确认，不得带着已消耗的身份进入阶段②。**P1 不适用「无记录」判据**——P1 的首次开通已在 2026-08-21 真实验收中真实走通并留痕（[#203 决策留痕 B5-3](https://github.com/Moshuiwang/lingxi/issues/203)：复用已有证据、不重置），编排者改为回读确认 P1 当前记录（`app_user.provisioning_state='active'`、`account_state='enabled'`、`permission_version`）与该次证据一致，**不消耗、不重置**这份记录 | 阶段②的前置，E7 的直接推论 |
| E9 | 五类确定性无权限分支中另四类 + MCP 同步节奏的**窗口前**工程级证据（不是窗口内证据，也不能替代真实 Stage 等待） | 阶段②的 N1 只真实走通「查无对应记录」这一类；「同一人员ID多行」「双键冲突」「资料不完整」「无支持职能」四类，用与生产同一份判定函数核对的合成夹具在**窗口前**跑通，不占用产品负责人时间、也不需要为每一类另找一个真实测试账号，属 **L3**（合成输入 + 生产判定函数，不是 L4a，收口时单列，不并入 L4a 计数）。**MCP 同步「最多十五分钟」节奏的最小合法配置只用于这份夹具自己的纯单测**，已核实无法注入真实 Stage 进程（`ReadinessSchedule` 由调用方在代码里构造，不读环境变量；已知唯一运行期覆盖点 `LINGXI_QUERY_MCP_TIMEOUT_SECONDS` 只改探针超时，不改轮询间隔与总预算）——真实 Stage 窗口里的十五分钟等待要么按合同真等（阶段②已按此设计耗时预算），要么本轮不覆盖，**不得**暗示这份夹具能让窗口内的真实等待变短 | 见 [`scripts/acceptance_fixtures_identity.py`](../../scripts/acceptance_fixtures_identity.py) 与其[契约测试](../../tests/test_acceptance_fixtures_identity_contract.py) |
| E10 | Bitable 双写暂停授权 | 阶段③需要暂停生产旧系统对权限发布表的双写入；负责该系统的一方（Ops/相关方）已确认可在约定时间窗口暂停，产品负责人已知情 | 阶段③前 |
| E11 | 首次开通链就绪自检 | 窗口开始前跑一遍 [`scripts/ops/onboarding_preflight.sh`](../../scripts/ops/onboarding_preflight.sh)（只读，把开通链每一条装配/数据前提映射到会让链路卡在第几步），全部判定项 ✅。出处：2026-08-21 Epic D 验收批次真实发消息失败事故——原因当时其实写在日志里，但没人把日志内容和"链会在第几步停下"连起来；本脚本是事故后新增的机制，规则原文"自检不全绿，就不要请产品负责人发消息或做任何操作" | 见脚本文件头注释 |

**E7 首触冒烟清单**（Trace v12 机制①：非正式、不占用正式候选或发送预算，只是让"第一次真实接触"提前发生；**一律使用第四个受控账号**，见 E7/E8）：

| 路径 | 冒烟方式 |
|---|---|
| 开通成功 / 无可用权限 两个出口 | 编排者用第四个受控测试身份各触发一次，确认两条文案与状态机走通 |
| 正常问数成功终态（卡片） | 编排者用第四个受控测试身份发一条真实问数，确认卡片正常展示 |
| `/stop` / `/new` | 编排者各触发一次，确认停止终态与新会话提示正常 |
| 卡片降级为文本兜底 | 编排者用 `LINGXI_GATEWAY_CARD_FAILURE_INJECT` 触发一次，确认降级路径正常且用后清除该变量（清除后用三面检查回读确认，见 E3） |
| 重投语义 | 编排者用 `scripts/replay_inbound_event.py` 重放一次，确认幂等成立 |

冒烟中发现的任何问题记入缺陷账本（对应 Issue 评论），修复或裁定后再进入正式窗口；冒烟本身**不作为**正式验收证据。

---

## 中断与恢复

本次窗口存在三类**不可放任的中间态**：生产旧系统被留在暂停状态、云盘留了测试对象、一次性首次开通已被消耗。任何阶段中途需要暂停或中断时，必须遵守：

1. **先清理再离开**：中止前先执行当前阶段已经写好的清理步骤（阶段①收口清单、`scripts/stage_rehearsal_mvp_acceptance.sh cleanup`、阶段③步骤 5）；来不及清理的对象逐项登记 Owner，不得放任不管。
2. **P1/B/N1 的三条消息是不可逆一次性动作，中断后不得重发**：一旦某个身份的首条消息已经真实发出，无论后续观察是否完整，都不得对同一身份重复发送"首条消息"去补齐观察——那会破坏"首次"这个前提本身，产生与合同预期不符的状态。中断后交由编排者回读当前实际状态定位，不猜测、不重试。
3. **离开前无条件先恢复双写并留时间戳**：只要阶段③的暂停步骤已经执行，无论后续步骤是否完成，**离开前必须先执行阶段③步骤 5（恢复双写入）**并记录恢复时间戳，绝不能带着生产系统处于暂停状态离开窗口，这一条优先级高于"把当前子任务做完"。
4. **续做前的检查**：重新开始前，编排者必须先跑一次清理检查（三面夹具检查 + 容器/进程/临时数据库残留核对），并回读当前状态：`publish_outbox` 表的 `pending`/`publishing` 中间态（是否有遗留的 `running_task_id` 占用）；`app_user.provisioning_state` 是否有账号停在 `provisioning`/`mcp_syncing` 这类**真正的半路中间态**（未推进到 `active`、也未被收口成 `aborted`）。**`aborted` 不是半路中间态，而是一条链的收口结果**（rank 0，"重新开始的起点"，可以让同一用户下一条消息重新从头跑一条全新的链）——它的取值域自迁移 `008`（`app_user` 基线 CHECK 约束）起就在，[#282](https://github.com/Moshuiwang/lingxi/issues/282) 新增的是真正**写入**它的路径：`onboarding_runner` 自身失败时的当场收口（不必等待任何扫描周期）与兜底的 `StalledProvisioningDuty`（45 分钟租约扫描，处理当场收口本身也失败的情形）两者都会产生它，不是只有后者。看到某个用户处于 `aborted` 时不需要再判断"是否已被 `StalledProvisioningDuty` 正确处理"——它本身就是已经收口完成的结果，可以放心让用户重新开通；续做前真正要核对的半路状态只有 `provisioning`/`mcp_syncing`。续做断点以 [Issue #161](https://github.com/Moshuiwang/lingxi/issues/161) 最后一条留言为准，不凭记忆判断做到哪一步。

---

## 阶段①（已取消）#97 云盘九步探针

> **本阶段已随产品负责人 2026-08-23 的产物路由裁定整体取消**（[#140 裁定](https://github.com/Moshuiwang/lingxi/issues/140#issuecomment-5385116935)：产物改为随对话在线文档 + 文档级可管理授权，机器人共享目录方案出局；[#97 同日关闭](https://github.com/Moshuiwang/lingxi/issues/97#issuecomment-5385118464)，目录级探针失去测试对象；窗口形态变更登记见 [#203](https://github.com/Moshuiwang/lingxi/issues/203#issuecomment-5385120141)）。本窗口只含②③④三阶段，不再需要任何第三方测试成员。探针脚本与其执行卡保留在原处备将来复用。

## 阶段② Epic D 首次开通 L4a

**执行人：B / N1 各发一条消息；P1 首触改为引用 2026-08-21 已有证据（不重复发送、不重新等待权限同步，见下表步骤 1 与 E8）。耗时：18–28 分钟。**

> 目标：验证[产品合同「首次对话与自动准入」](../产品合同与外部边界.md#首次对话与自动准入)的两个用户出口在真实链路上成立，且 B 不会被误建档。对应[员工旅程验收检查单](员工旅程验收检查单.md#a-首次开通自动准入无授权卡片) A1–A6、B5。
>
> **重要：权限同步允许合同内最多十五分钟的合法等待，这不是失败。** 产品合同明写：权限发布后「立即校验一次、每三分钟一次、**最多等待十五分钟**」，期间卡片显示「权限正在同步，预计最多需要十五分钟」，超时后走「权限同步未完成，已转交处理，无需重复开通」。**2026-08-18 修复包更正**：此前本卡按 5 分钟预算、超时即判失败叫停——这会在第 6 分钟把一条合同内的合法慢路径误判为失败，进而叫停整个窗口。

| # | 执行人 | 操作 | 预期现象 | 失败怎么办 | 预计耗时 |
|---|---|---|---|---|---|
| 1 | 编排者 | **P1 首触改为引用已有证据**（产品负责人 2026-08-21 裁定，[#203 决策留痕 B5-3](https://github.com/Moshuiwang/lingxi/issues/203)：复用已有证据、不重置）：回读确认 P1 当前记录（`app_user.provisioning_state='active'`、`account_state='enabled'`、对应 `permission_version`）与 2026-08-21 真实验收产生的[端到端评论证据](https://github.com/Moshuiwang/lingxi/issues/203)一致；原设计要现场逐条观察的三段文案（"已收到，正在核对你的身份和银河权限，请稍候""权限正在同步，预计最多需要十五分钟"中间态、最终"开通完成……当前可用范围"）均以该次证据为准，**窗口内不重复发送 P1 的首条业务消息，也不重新等待权限同步** | P1 当前记录与 2026-08-21 证据逐项一致 | 不一致 → 停止，记录差异，升级编排者与产品负责人共同定位根因 | 5 分钟 |
| 1d | 编排者 | 回读确认 2026-08-21 证据中 P1 首触的两条消息内容均**不被执行、不保存、不在任何后续回复中回显**（对应检查单 A6） | `task` 表中查无对应记录——**生产装配下首条消息落 `handled_as='auto_provisioning'`，第二条（用户已处于 `PROVISIONING` 中间态期间再发）落 `handled_as='not_provisioned'`，两个分支都在建 `task` 行之前返回**（`not_provisioned` 不是"未开通用户恒定会走的分支"，只在已进入 `PROVISIONING` 状态期间再收到消息时命中，见 `core/conversation/pipeline.py::_within_transaction`）；`inbound_event` 表结构本身不含消息正文列（无 `content` 字段），不承载可回显的内容 | 出现回显、执行痕迹，或 `task` 表出现对应记录 → 记为红线缺陷，停止 | 3 分钟 |
| 2 | B | 以 B 身份向 Lingxi 发送任意一条业务消息 | B **不**被建成 `app_user` 记录（数据库回读确认查无对应新增记录）；B 收到的提示逐字为"这个账号是组织资料同步的专用账号，不提供问数服务，也不会建立使用记录。"（[产品合同与外部边界](../产品合同与外部边界.md) 第 76 行原文，`tests/test_identity_first_contact.py:455` `content_key="onboarding.delegated_subject"` 钉住），不是"开通完成" | B 被建档 → 立即停止，这是安全边界失效，需产品负责人与编排者共同确认根因，**不得**继续后续阶段；提示文案与该原文不逐字一致**不是**终止判据，记为观察项、照常继续，事后升级编排者核对措辞漂移的原因 | 5 分钟 |
| 3 | N1 | 以 N1 身份向 Lingxi 发送任意一条业务消息 | 收到统一的"当前没有可用的银河权限，请先在银河申请或补充权限……"提示；**不**进入等待状态，**不**生成人工待办 | N1 收到与合同文案不同的提示，或进入了等待/生成了待办 → 停止，记录实际提示文案与状态，升级编排者定位 | 5 分钟 |
| 4 | 编排者 | 回读三条记录：P1 的 `app_user`/权限/环境状态，B 的无新增记录确认，N1 的无 `manual_review` 确认 | 三项回读均与上表预期一致 | 任一不一致 → 记入证据对应性问题，升级产品负责人裁定 | 5 分钟 |

**说明**：本阶段只覆盖开通链路本身；P1 开通成功后的**第一条真实问数**留到阶段④"完整员工路径"部分执行，避免在阶段②过早消耗对话上下文。**每条消息发出后立即回读确认再进行下一步**（2026-08-18 修复包 P3 更正：此前按"发完三条再统一回读"设计，出问题时无法定位是哪一条导致——现在每条各自回读，见上表步骤 1d/2/3/4 已按此拆开）。

---

## 阶段③ 受控暂停窗口

**耗时：30–60 分钟。执行人：编排者主导，产品负责人只需在开始/结束两个时间点确认授权。**

> 目标：在暂停生产旧系统对权限发布表的双写入期间，验证权限变更后 B/N1 发布行不受影响的隔离断言（`V-权限-03`），并核对 G-155 探针法终判的语义正确性。**本轮不对外部发布表做额外的真实写入**——2026-08-18 修复包更正：此前的版本声称这是"可以安全对真实发布表写入而不与旧系统冲突的窗口"，这句话不成立且已删除。**2026-08-18 当日状态更新（产品负责人已裁定）**：「公司 + 职能 → 指标名」翻译层的运行时硬闸**已落地**（[#227](https://github.com/Moshuiwang/lingxi/issues/227)，写进[产品验收矩阵](../技术设计/验收矩阵.md) `V-权限-13`，变异验红实测通过）——翻译内容缺失或某个「公司+职能」组合未覆盖时，该用户本轮**不产生任何发布意图**，不再是此前"没接线才挡着、代码层面不会拒绝"的状态。但**当前映射文件是空的**，因此闸门对任何人都是关闭的：**闸门存在 ≠ 能发布**，映射内容为空时，即使触发真实写入也只会被闸门拒绝，拿不到 `V-权限-02/03/09/11` 的真实发布证据。[#226](https://github.com/Moshuiwang/lingxi/issues/226) 已裁定方向为应用身份 tenant token，但实现尚未开始。**产品负责人已裁定：Epic D 验收指向正式表，但必须等硬闸落地且映射内容就绪**——本轮映射为空，因此仍维持步骤 2 的 dry-run 设计，不因闸门已落地就贸然尝试真实写入。

| # | 步骤 | 执行人 | 预期现象 | 失败怎么办 | 预计耗时 |
|---|---|---|---|---|---|
| 1 | 确认暂停生产旧系统对权限发布表的双写入已生效 | 编排者（向负责旧系统的一方确认） | 收到明确的"已暂停"确认，非"稍后处理" | 未取得明确确认 → **不得**进入下一步，本阶段延后 | 5 分钟 |
| 2 | 对 P1 构造一次权限重算发布行（`V-权限-02/03/09/11`），**只做 dry-run 比对，不提交真实写入** | 编排者 | 构造出的发布行逐字段与数据库当前记录一致；`token_cipher` 字段的构造逻辑不清空、不覆盖既有密文（比对逻辑正确，不代表已经写入外部表）；同样构造一次"修改 P1 权限后"的发布行，确认 B/N1（若已有发布行）对应行逐字段不变 | 构造出的字段不一致 → 停止，记录差异，不得为了让比对通过而放宽判据 | 10 分钟 |
| 2b | **恢复与回读确认**（无论步骤 2 是否触发了任何针对 P1 权限记录的写入，即便只是 dry-run 比对依赖的重算职责被真实触发一次） | 编排者 | 确认 P1 当前生效权限与窗口开始前逐字段一致；如有差异，先执行恢复动作，再次逐字段回读确认 | 无法确认一致或恢复失败 → **停止，不得进入阶段④**——阶段④的问数结论必须建立在 P1 权限状态明确、未被本阶段测试动作污染的基础上，否则该轮④的问数结论作废 | 10 分钟 |
| 3 | 以 P1 本人令牌对问数 MCP 真实执行一次 `list_metrics`（`V-权限-06`，G-155 探针法终判），针对的是 P1 经阶段②真实开通流程已经建立的当前状态，不依赖本阶段步骤 2 的额外写入 | 编排者（代 P1 发起，使用其令牌） | 可见指标数量 > 0；结论绑定（P1，本次权限版本） | 可见指标为 0，或服务返回"在线"但未见指标 → 判定为**未就绪**，不得宣告成功，记录原因 | 10 分钟 |
| 4 | G-155 语义核对：确认判定依据是探针法而非版本字段回读比对 | 编排者 | 确认当前权限多维表格确实没有版本字段（G-155 已终判），流程未误用旧的"回读版本比对"路径 | 发现代码仍尝试回读版本字段 → 记为回归缺陷，升级 | 5 分钟 |
| 5 | 恢复生产旧系统的双写入 | 编排者（向负责旧系统的一方确认） | 收到"已恢复"确认 | 未能恢复 → 立即升级为事故处理，不得放任生产系统处于暂停状态（「中断与恢复」第 3 条同样适用：无论前面步骤是否完成，离开窗口前必须先做这一步） | 5 分钟 |
| 6 | 结论登记 | 编排者 | 步骤 1–5 的时间戳、结果、任何异常均写入 Issue #161；明确写明"本轮未对外部发布表做真实写入，`V-权限-02/03/09/11` 本轮只取 L2 证据" | — | 5 分钟 |

**放开真实写入的进入条件**（2026-08-18 产品负责人裁定后更新；三条必须**同时**满足，任一不满足则本卡继续维持"只做 dry-run"，不由编排者临场决定放开）：

1. [#227](https://github.com/Moshuiwang/lingxi/issues/227) 硬闸已落地**且映射内容已填入**——闸门本身已经落地（`V-权限-13`），但空映射时闸门对任何人关闭，等于写不出去；必须映射内容就绪，闸门才真正"打开"而不是"挡住全部人"；
2. [#226](https://github.com/Moshuiwang/lingxi/issues/226) 的应用身份令牌供给已接线（方向已裁定为 tenant token，本条要求的是接线完成，不是方向已定）；
3. **[#263](https://github.com/Moshuiwang/lingxi/issues/263) 硬切已完成**——即 `biai-prod` 上旧系统对正式权限表的写入方**已经停掉**。
4. 以上三者都满足后才放开真实写入——**目标表已经产品负责人裁定为正式表**，不再是本卡待决策项，但裁定本身不构成"可以现在就写"，仍要等前三条条件就绪。

> **2026-08-20 新增第 3 条，务必先读**：本卡 2026-08-18 成稿时只列了前两条。此后编排者回源核实到一条当时不知道的耦合——**正式权限表当前仍由 `biai-prod` 上的旧系统在写**。
> 前两条工程条件如今**都已满足**（#227 映射内容 2026-08-19 填入、#226 应用身份供给已随 Epic C 接线），因此**照旧版本卡片执行会得出"可以放开真实写入"的结论**——那会在旧系统仍在写的情况下对同一张生产表双写。
>
> **[#263](https://github.com/Moshuiwang/lingxi/issues/263) 排在本 Trace 之外、需产品负责人单独授权**，本轮窗口**一律维持 dry-run**，不由执行者临场决定放开。

**产品负责人在本阶段的角色**：只需在步骤 1 前确认"可以开始暂停"、步骤 5 后确认"已看到恢复确认"两个检查点；中间的构造比对、探针操作全部由编排者完成，不占用产品负责人的操作时间（但仍在同一窗口内，建议在场以便随时决策）。

**失败与升级**：任一步骤失败均应优先执行步骤 5（恢复双写入）后再排查，不得让生产旧系统长时间处于暂停状态等待排查结果。

---

## 阶段④ Epic E 联合验收

**耗时：约 90 分钟。执行人：P1/B/N1 发送真实消息，编排者执行受控构造与回读。**

> 本阶段按[员工旅程验收检查单](员工旅程验收检查单.md)逐行勾选；此处只给操作顺序、责任人与失败处理，不重复检查单已有的断言编号与观测通道。检查单里标注"可在窗口外独立完成"的行（C12 的构造部分、D1/D3/D4/D5、F1/F2）**已经或将由编排者在窗口外处理**，不在下表④-1/④-3 里重复列出、也不占用窗口时间；下表只列出需要产品负责人当场参与或需要在窗口内实时观察的项。

### ④-1 完整员工路径（约 33 分钟）

| # | 操作 | 执行人 | 对应检查单 | 你在屏幕上应看到 | 预计耗时 |
|---|---|---|---|---|---|
| 1 | P1 发送第一条真实业务问数 | P1 | C1 | 一张流式卡片，标题含公司/指标/时间范围，逐步展示查询进度，最终显示"已完成"与总耗时 | 5 分钟 |
| 2 | P1 对上一轮结果发起结果依赖型追问 | P1 | C2 | 基于上一轮已有结果的新答案，不是"重新查询中"的完整流程 | 5 分钟 |
| 3 | P1 发送一条有歧义的问题，确认收到澄清追问 | P1 | C3 | 一条要求澄清的追问，不是直接给出的猜测性答案 | 5 分钟 |
| 4 | P1 在空闲态执行 `/new`，确认成功文案与上下文清空 | P1 | C4 | "已开启新会话，可以开始提问。" | 3 分钟 |
| 5 | P1 发起新问数后执行 `/stop`，确认唯一停止终态 | P1 | C5 | "任务已停止。已产生的结果：……" | 5 分钟 |
| 6 | P1 在任务运行中发送普通消息，确认只提示不入队 | P1 | C6 | "当前任务仍在处理中" | 3 分钟 |
| 7 | P1 在忙碌期执行 `/new`，确认只提示不生效 | P1 | C7 | 同上忙碌提示，不是"已开启新会话" | 3 分钟 |
| 8 | P1 在忙碌期执行 `/stop`，确认不被忙碌判定拦截 | P1 | C8 | 停止终态，不是忙碌提示 | 3 分钟 |
| 9 | P1 请求一次飞书文档/表格/图片交付 | P1 | G1 | 明确说明当前无法安全交付正式产物的提示，**不**暴露内部文件或服务器路径；这条提示**不影响**同一轮问数正文本身的交付 | 3 分钟 |

**失败怎么办**：任一项现象与"你在屏幕上应看到"不符，立即停止④-1 后续步骤，记录当前话题/任务状态截图与数据库回读，升级编排者定位；不得跳过继续做下一项。

### ④-2 四线留证（约 25 分钟，编排者主导，产品负责人按需配合发消息）

| 线 | 留证内容 | 执行人 | 预计耗时 |
|---|---|---|---|
| #17（开通完成条件） | 阶段②三条记录的回读结果 + 本阶段 P1 正常问数成功终态，一并作为 #17 完成条件的真实链路证据 | 编排者整理，引用阶段②③④已有证据 | 5 分钟 |
| #52（花名册审计日报） | 若真实凭据可用：触发一次日报职责，确认受控管理群收到日报；若凭据不可用（见窗口前 E6）：给出结构化职责调用证据（服务端日志），登记为已知边界 | 编排者 | 10 分钟 |
| #57（飞书长连接与队列） | 一次真实断连重连（编排者可用网络层受控中断触发）+ 一次 `scripts/replay_inbound_event.py` 受控重放，确认幂等与重连均成立 | 编排者 | 5 分钟 |
| #62（部署编排） | 引用[Stage 演练脚本](../../scripts/stage_rehearsal_mvp_acceptance.sh)在本次候选上的真实执行记录（`preflight`/`migrate`/`up`/`healthcheck`/`persistence-restart`/`rollback-drill` 六步；`rollback-drill` 必须带 `--rollback-tag` 且回读镜像标签确有切换，不接受空转记录）。**数据库备份/恢复本轮不覆盖**——脚本的 `backup-restore-note` 步骤只核对两个持久卷存在，不构成真实备份/恢复的证据（Supabase 托管方案，需要独立的控制台/CLI 操作权限）；#62 线的结论据此明确标注"备份/恢复未验证"，不得因为其余六步通过而暗示备份/恢复也已覆盖 | 编排者 | 5 分钟 |

**失败怎么办**：任一线证据缺失或与预期不符，不阻塞其余三线的证据整理，但在收口清单中如实标注该线未达标，不得用其余三线的通过掩盖。

### ④-3 并发 / 隔离 / 重连 / 重投（约 20 分钟）

| # | 操作 | 执行人 | 对应检查单 | 你在屏幕上应看到 | 预计耗时 |
|---|---|---|---|---|---|
| 1 | P1 同时在两个飞书原生话题各发一条不同问题，观察是否并发执行、互不干扰 | P1 | C9 | 两个话题各自独立展示查询进度，互不出现对方的内容 | 10 分钟 |
| 2 | 编排者用受控卡片故障注入（`LINGXI_GATEWAY_CARD_FAILURE_INJECT`）触发一次降级，P1 在同一话题观察结果 | 编排者构造 + P1 观察 | 员工旅程验收检查单 D2 | 同话题出现一条完整文本终态，不是卡片，也没有重复的第二条终态 | 10 分钟 |

**说明（2026-08-18 修复包更正）**：断连重连（C10）与重投语义（C11）不需要 P1 在场观察，已移入检查单标注"可在窗口外由编排者独立完成"；此前把它们各算 10 分钟排进窗口内是多算的窗口时间，实际只需编排者事后在证据汇总里贴出对应回读。卡片故障注入验证后**立即撤除该变量并用三面检查（`--check-clear` + `--check-files` + 容器 `env`）回读确认**，不得只用宿主 shell 一面回读就判定已清除。

**失败怎么办**：并发测试中两个话题互相影响（串上下文、互相阻塞）→ 立即停止，这是隔离边界失效，需要产品负责人与编排者共同确认根因，按红线处理，不得继续后续验收声称整体通过。卡片降级异常 → 记录并停止该项。

---

## Trace v12 机制在本卡的落点小结

| v12 机制 | 本卡落点 |
|---|---|
| ①用户可见路径首触冒烟 | 「窗口开始前：编排者独立完成」E7（一律用第四个受控账号；附录 A 三句问句按 2026-08-21 裁定改用 P1，例外范围与理由见附录 A） |
| ②用户可见断言双通道观测（三取二） | [员工旅程验收检查单](员工旅程验收检查单.md)每行「观测通道」列 |
| ③受控触发夹具确定性合同 | `scripts/acceptance_fixtures.py`（环境变量注入类）+ `scripts/acceptance_fixtures_identity.py`（合成数据类，L3）+ 两份配套契约测试；本卡多处引用其 `--check-clear` / `--check-files` 作为开窗前/收窗后**三面**回读 |
| ④冻结前静态排除对账 | 见下节 |

## 冻结前静态排除对账

候选冻结前，把已登记的范围排除 / deferred / 知情接受清单逐项对到本次验收断言上，命中者标缺件或取得产品负责人明示接受，不得静默滑过。

| 已登记排除/知情接受项 | 来源 | 是否命中本卡断言 | 处置 |
|---|---|---|---|
| `V-投递-05` 后半句「在线临时文件、进程恢复材料」两类尚无产物形态 | [产品验收矩阵](../技术设计/验收矩阵.md#问数结果投递与正文生命周期) | 不命中本卡任何步骤（当前实现不产生此类临时文件） | 登记为待后续 Story，不阻塞本次窗口 |
| `V-花名册-31` 跨重启重发一份相同日报（知情接受，尚未消解） | 同上「花名册审计日报」节 | 若④-2 触发日报职责恰逢进程重启，可能观察到重发 | 视为已知边界，不判该行失败 |
| `V-投递-11`/`V-管理-*` 管理路径断言 | [产品验收矩阵](../技术设计/验收矩阵.md#管理与幂等) | 不命中（管理路径不在 Epic E 授权范围） | 明确不在本次验收范围，不得据本次窗口宣称管理路径已验证 |
| 通知投递 at-most-once 残余窗口（知情接受） | 「权限发布与同步」节 | 可能影响④-2 #17 线的通知观察 | 若通知未送达但发布本身成功，不判发布线失败，单独记录 |
| 权限变化通知公司位显示 `boss_company_id` 编号而非中文名（已裁定随翻译层 Story 一并修复，**该短板的渲染源属 S-D-02 未落地部分，标注为「需实证」，不是确定结论**） | 同上 | 命中③、④-1 步骤 9 与④-2 #17 线的通知/开通完成文案观察 | 已知且已裁定不在本次修复；若开通完成文案的公司位同样显示编号，按**同一已裁定短板**处理，**不**判为新缺陷 |
| `V-会话-07` 不同用户会话与交付物互不可见（未认领） | [产品验收矩阵「会话与任务」](../技术设计/验收矩阵.md#会话与任务) | 与④-3 并发测试相关但本卡只测试同一用户（P1）跨话题并发，不测试跨用户隔离 | 明确超出本次窗口范围，登记为后续验收项 |
| 「公司 + 职能 → 指标名」翻译层的运行时硬闸**已落地**（`V-权限-13`，[#227](https://github.com/Moshuiwang/lingxi/issues/227)），但**当前映射文件为空**，闸门对任何人关闭（2026-08-18 产品负责人裁定后更新登记）——**闸门存在 ≠ 能发布**，映射为空时真实发布这条线在窗口里拿不到 L4a，必须如实登记，不得写成「已具备」 | [产品验收矩阵](../技术设计/验收矩阵.md#权限发布与同步) `V-权限-13`；本卡阶段③详述 | 命中阶段③步骤 2（已改为 dry-run，不因闸门已落地就贸然尝试真实写入）与④-2 #17 线的发布证据完整性 | 阶段③本轮不做真实写入即可规避；若未来放开真实写入，必须先满足阶段③列出的三项进入条件（硬闸已落地且映射就绪、#226 应用身份令牌供给已接线、两者皆满足） |
| 发布令牌供给（[#226](https://github.com/Moshuiwang/lingxi/issues/226)）未接线，权限发布刷新链在真实环境一轮没跑过 | [产品验收矩阵](../技术设计/验收矩阵.md#权限发布与同步) | 命中阶段③（构造比对基于当前可用状态，不依赖该链路已跑通）与④-2 #17 线 | **若窗口前令牌供给仍未接线，阶段③的构造比对仍可进行（dry-run 不依赖它），但不得把"发布刷新链未跑通"记成本轮的新缺陷**——这是已知的先决条件缺口，不是本次窗口引入的问题 |
| 真实私聊投递行为（应用能否主动私聊、平台 `uuid` 去重窗口）未验证 | [产品验收矩阵](../技术设计/验收矩阵.md#权限发布与同步) | 命中④-2 #17 线的通知观察 | 本轮只承诺"重试携带同一个 uuid"这个代码事实，观察不到主动私聊通知时不判为新缺陷 |
| 九十天保留 `mcp_sync_check` 一面当前部署不装配（需 `LINGXI_MCP_TOKEN_ENCRYPT_KEY`） | [产品验收矩阵](../技术设计/验收矩阵.md#权限发布与同步) | 不命中本卡任何步骤（保留清理不在本次窗口观察范围内） | 登记为待后续 Story，不阻塞本次窗口 |

**执行前最终核对**：编排者在窗口开始前，逐条确认上表"处置"列的结论仍然成立（候选未变化导致这些排除项失效），并在 Issue #161 留言中附上本表核对时间戳。

---

## 窗口收口

1. **证据汇总**：阶段①的 Issue #97 结论、阶段②③④的观测记录（页面截图/API 回读/服务端日志，按双通道要求）分别整理，链接到 [Issue #161](https://github.com/Moshuiwang/lingxi/issues/161)。
2. **环境清理**：按[Stage 演练脚本](../../scripts/stage_rehearsal_mvp_acceptance.sh)的 `cleanup` 步骤执行，受控触发夹具**三面**均确认无残留；阶段①产生的测试对象按其清理 Owner 确认已处置或已登记跟进。
3. **未通过项如实登记**：任何未勾选、单通道证据或已知边界，写入[当前能力](../当前能力.md)（若涉及能力边界变化）或直接留在 Issue #161（若只是本轮未覆盖），不得据低层级证据宣称高层级完成。
4. **Issue #161 收口留言模板**：
   ```
   ## MVP 联合验收窗口收口（YYYY-MM-DD）

   - 阶段①#97九步探针：n/9 步完成，结论：……（若挪到窗口末尾/另约，注明原因与新时间）
   - 阶段②Epic D开通L4a：P1/B/N1 三条结果：……（含权限同步实际耗时，是否触发十五分钟超时分支）
   - 阶段③受控暂停窗口：暂停时长 __ 分钟，构造比对/探针/G-155结论：……（本轮未做真实发布写入）
   - 阶段④Epic E联合验收：员工旅程检查单 n/32 已勾选，四线留证结果：……，并发/隔离/降级结果：……
   - 未验证/单通道事项：……
   - 需要产品负责人决策的：……
   - 环境清理：已确认无残留 / 遗留项及 Owner：……
   ```

---

## 产品负责人窗口前自查（10 分钟，窗口开始前独立完成）

- [ ] P1、B、N1 三个飞书账号均能正常切换登录，且都能在飞书上私聊到 `Bot-Test`。
- [ ] 确认可以在飞书中为任一身份新建原生话题（阶段④-3 并发测试要用到）。
- [ ] 确认截图/录屏工具已就绪，且知道脱敏规则（不截出真实账号标识、内部路由）与保存位置。
- [ ] 确认可以正常访问 GitHub 并在 Issue #161 留言（用于窗口中随时记录异常，不必等到收口才写）。
- [ ] 已阅读本卡「中断与恢复」一节，了解三条消息不可重发、离开前必须先恢复双写入两条硬规则。

## 附录 A：窗口内使用的测试问句（编排者窗口前冒烟实测后填入，实测前不得凭猜测预先写死）

**实测账号改用 P1（产品负责人 2026-08-21 裁定链，留痕见 [#203 决策留痕评论 B5-1](https://github.com/Moshuiwang/lingxi/issues/203)）**：本节三句问句的实测原设计要求"一律用第四个受控账号，不得触碰 P1/B/N1"（见上方 E7），但 P1 的首触已在真实验收中被消耗、且产品负责人裁定复用该次证据；问句实测（问数）是可重复动作，不消耗任何一次性资源；E7 红线"不得触碰 P1"保护的对象是**首触的一次性**，P1 的首触既已不存在于"待发生"状态，该红线对这三句问句的保护对象已不存在。因此三句问句的实测改用 P1 账号，E7 表格里"不得触碰 P1/B/N1"这条红线对 E7 其余五项（开通成功/无权限、正常问数首触、`/stop`、`/new`、卡片降级、重投）仍然全额有效，不因本处例外而放宽。

- **稳定成功问句**：`列出我可以查询的数据指标`（P1 实测 2/2 成功：2026-08-22 03:05 与 03:30 UTC，候选 `9dd41b5`/镜像 `20260822-9dd41b5498be`，均 ~12 秒返回真实 9 指标表格，零安全拦截）。
- **稳定 ≥60 秒长任务问句**：`请针对这9个指标（新增用户数、充值用户数、充值金额、扣费用户数、扣费金额、汇率、增值税率、频道收视率、频道市占率），依次先说明每个指标的定义、可用维度和统计口径，再分别按国家、按频道、按最近6个月的月度粒度查询明细数据，查询失败时换一种维度组合重试一次，最后写一份非常详尽的季度经营分析报告，包含逐指标趋势、同比环比、国家间对比、异常识别和下季度预测建议，务必覆盖全部9个指标不要遗漏`（P1 实测 2026-08-22 04:07 UTC，候选 `9dd41b5`，真实耗时 182 秒、成功终态、内容含真实指标数据。**已知边界**：`query_metric` 大结果集可能触发一次性会话失败（SDK 消息缓冲上限，[#291 登记]），用户会收到明确失败文案，窗口内撞上时重发同句即可，不作为验收失败）。
- **已实测能触发澄清的歧义问句**：`帮我查个数`（P1 实测 2026-08-22 04:02 UTC 后、`/new` 新会话，候选 `9dd41b5`：回复为澄清性反问并附引导选项，未直接查询。注意：多轮上下文可能抑制澄清行为——窗口内此句应在**新会话**里发）。

## 附录 B（已作废）

> 原「给三名 #97 测试成员的一句话说明」随阶段①取消而作废（2026-08-23）。

---

## 总耗时预估与人员分工

| 阶段 | 预计耗时 | 产品负责人必须亲自做 | 编排者可提前/独立完成 |
|---|---|---|---|
| 窗口前置 | 不计入窗口（提前完成） | 阶段③步骤1/5的授权确认（可在窗口内做）；窗口前 10 分钟自查 | E0–E11 全部 |
| ①（已取消，见阶段①注记） | — | — | — |
| ②Epic D开通L4a | 18–28 分钟 | B/N1 两条消息发送；P1 首触改为引用 2026-08-21 已有证据，不重复发送、不重新等待权限同步 | 每条消息后的即时回读确认；P1 记录与既有证据的一致性核对 |
| ③受控暂停窗口 | 30–60 分钟 | 仅两个授权确认点（开始/结束） | 构造比对、探针、G-155核对、恢复回读、结论登记（本轮不做真实发布写入） |
| ④Epic E联合验收 | 约 78 分钟（④-1 33 + ④-2 25 + ④-3 20） | ④-1 全部 9 项、④-3 第 1 项（并发测试）、④-2 中需要发消息配合的部分 | ④-2 四线证据整理、④-3 第 2 项（卡片故障注入构造）、C10/C11（重连/重投，已移出窗口内清单） |
| 收口 | 15 分钟 | 决策确认 | 证据汇总、清理、留言整理 |
| **缓冲**（切换账号、取证整理、澄清对话） | **30 分钟** | — | — |
| **合计** | **约 3–3.5 小时（阶段①取消后）** | 约 105–120 分钟需产品负责人亲自操作或在场决策 | 其余时间编排者可独立或主导完成 |

**产品负责人必须亲自做的核心是**：阶段②中 B/N1 的真实消息发送（P1 首触改为引用已有证据，不再需要现场发送）、阶段④-1/④-3-1 的全部真实消息发送、阶段③两个授权确认点。**编排者可以提前独立完成或在窗口内主导的是**：全部环境准备与前置自检、阶段③的构造比对与探针操作、阶段④的四线证据整理与受控构造类验证（卡片故障注入、断连重连、重投——后两项已移出窗口内清单）。
