# 产品验收矩阵

> 状态：现行产品验收矩阵；随每次改动本文件的提交进入 `main` 后即生效，不单独维护「最后整理」日期戳——各章节内嵌日期与 git 历史即为改动时点的权威记录。
>
> 本文只保存产品与部署的 `V-*` 可验证断言、认领状态和产品合同章节映射。证据层级、关口、CI 与受控环境规则看[验证与门禁](验证与门禁.md)；规划者按 GitHub [#147 长期执行计划与 Execution Trace](https://github.com/Moshuiwang/lingxi/issues/147) 生成或修订具体 `[tracking]` Execution Trace，实际角色、时点、冻结与修复只看获批 Trace；缺项时退回规划者修订，编排者不得从模板补合同。

实现 Story 只读取并认领与自身范围相关的断言，不需要把整份矩阵加载进每个普通任务。新增或修改产品规则时，必须同步更新对应断言与会变红的检查。

## 体量预算

- **单行棘轮**：`V-*` 断言表格行超过 800B 登记进 `scripts/ci/matrix_row_size_baseline.txt`，只许缩不许涨；未超的不得新超（`scripts/ci/check_matrix_row_size_ratchet.py`）。
- **总量触发线**：400KB 或 1500 行，先触发者生效；触线只提示、不卡红，提示下一次改动本文件前先完成分册。
- **分册预案**：优先按存活状态归档到 `docs/参考证据/验收矩阵-归档.md`（保留本文件路径与现有章节标题/锚点不变）；不够再按产品域二次拆分。
- **裁定**：产品负责人 2026-08-28 批准方案 B（Issue #335 Gate G-4）。

## 状态口径

**2026-08-26 对账（Issue [#100](https://github.com/Moshuiwang/lingxi/issues/100)）**：逐条核对彼时全部「已认领」`V-*` 断言（261 条）——用 `grep`/读测试文件核实对应测试真实存在且断言语义匹配，并核对可指认的合并提交/PR；不重跑任何真实飞书、MCP、Claude Code + 百炼或生产链路，不把「Issue 已关闭」「CI 绿过」单独当证据。保守规则：仅当「测试存在 + 语义匹配 + 可指认提交/PR」三者齐备才改为「已验证」，证据索引以测试文件::类名/方法名 + commit 简写形式紧跟在相关表格后的对账说明段落中给出；任何一项存疑（尤其是声明层级含 L4a/L3 但本次未补验、或需要跑真实链路才能证明）一律保持「已认领」不翻转、不补验。本次共翻转 152 条为「已验证」，其余 109 条「已认领」原样保留；`未认领`（41 条）不因本次对账被排期或改状态。花名册审计日报组（V-花名册-01…48）虽多数为纯 L2 声明，但该组表格前已有编排者/产品负责人明确写下的组级裁定——真实花名册读取所需凭据未落盘、真实群通知与真实读取均未验证，因此该组不因个别行满足 L2 而单独翻转，本次对账尊重此既有组级决定，整组保持已认领。

## 一、关键机制验收矩阵

下表覆盖[架构设计](架构设计.md#六核心机制)中的核心机制。**每条给出可验证断言，而不是"测试 XX 功能"**——断言必须能判定真假。

编号用于[合同条款覆盖清单](#二合同条款覆盖清单)的逐条核对。

### 开通与匹配

`V-开通-04` 与 `V-开通-11…14` 的编排半边由 [Epic D](https://github.com/Moshuiwang/lingxi/issues/160) S-D-02（`OnboardingRunner`）在 L2 认领（`tests/test_onboarding_runner.py` 钉住状态只前进不回退、终态互斥、重试/并发/重入不重复创建）。L4a 半边（真实首次开通）未取得，编排住在 `lingxi-scheduler`（[决策记录](../决策记录/2026-08-18-首次开通编排住在scheduler.md)），前置未齐时不注册，详见[当前能力](../当前能力.md)。「已认领」不代表入口已可用。

`V-开通-01…03/06/09/15…17` 由 [#89](https://github.com/Moshuiwang/lingxi/issues/89) 认领；`V-开通-10…14` 由下游 [#65](https://github.com/Moshuiwang/lingxi/issues/65) 认领，须待 #89 完成后接入 gateway；`V-开通-07/08` 证据源自已关闭的 #16，正式入口仍随 #89/#65 复验。

`V-开通-18` 由迟到就绪恢复职责（`apps/scheduler/late_readiness_recovery.py`，同住 `lingxi-scheduler`）在 L2 认领，复用 `core/permission/mcp_readiness.py` 判定层，经外部独立审查修复定稿（`tests/test_mcp_readiness_machine.py`、`tests/test_late_readiness_recovery.py`、`tests/test_postgres_late_readiness_recovery.py` 钉住）。L4a 半边（真实等待超时后自然恢复）未取得；重试窗口受 `publish_outbox` 九十天保留期约束、候选查询无持久租约，两条边界登记在模块文档「已知边界」一节，不在本条断言验证范围内。

`V-开通-19` 的 L2 半边由 PR-1（Issue #282）认领：当场收口（`AutoOnboardingRunner._abort_if_stalled`）与停摆收口职责（`StalledProvisioningDuty`）两条互补路径共用同一收口写口，经外部独立审查（opus）BLOCK 后按 P2/P3 项修复复验通过（`tests/test_onboarding_runner.py`、`tests/test_stalled_provisioning.py`、`tests/test_postgres_stalled_provisioning.py`、`tests/test_identity_postgres_records.py`、`tests/test_scheduler_onboarding_assembly.py`）。L4a 半边（真实中途死亡后自然恢复）未取得，三格已知例外见下表断言正文；`docs/当前能力.md` 在 L4a 取得前不得写成「当前能力」。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-开通-01 | 匹配确认前，`app_user.permission_record_id` 为 `NULL`——**不先占位再回填** | L2（真库） | 已验证 |
| V-开通-02 | 匹配键（工号/邮箱）命中多条记录或两键结果冲突时不自动选择任何一条，统一返回无可用权限且不生成 `manual_review`；审计仍区分 `multiple_hits` 与 `key_conflict` | L2（真库） | 已认领 |
| V-开通-03 | 工号与邮箱在权限记录中均查不到时给出申请指引终态，且**不生成** `manual_review` 记录 | L2（真库） | 已认领 |
| V-开通-04 | 环境创建 / 发布 / MCP 同步失败时，`provisioning_state` **不回退**到 `matching` | L2（状态机） | 已验证 |
| V-开通-05 | `mcp_syncing` 状态下用户发起问数被拒绝 | L2（真库）+ L4a | 未认领 |
| V-开通-06 | 私聊发送者无法定位已同步员工资料、花名册查无对应记录，或工号与邮箱等必要资料均缺失时，不写入不完整的用户记录，统一返回无可用权限且不生成 `manual_review`；内部原因仍可区分 | L2（真库）+ L4a | 已认领 |
| V-开通-07 | 飞书成员详情的 `status` 显示**非在职**（`is_activated=False` 或 `is_frozen` / `is_resigned` / `is_exited` / `is_unjoin` 为 `True`）时，不建档、不匹配、不发布权限，统一返回无可用权限终态。**可见范围不做状态过滤**，710 人实测中含 5 名冻结、1 名未加入，定位到不等于在职 | L2（真库）+ L4a | 已认领 |
| V-开通-08 | 姓名不含汉字（710 人实测中 11 人为纯拉丁）时，建档、展示与内部诊断正常，不因语言分支报错或静默丢弃；姓名不参与匹配判定（2026-08-05 起匹配键为工号/邮箱） | L2（真库） | 已验证 |
| V-开通-09 | 花名册中同一「人员ID」存在多行（已实测 718 行对 710 值）时，即使各行字段相同也不是唯一原始记录：不自动去重或取任意一行，统一返回无可用权限且不生成待办；只有原始记录恰一行、工号缺失但邮箱唯一可用时才走邮箱回退 | L2（真库） | 已认领 |
| V-开通-10 | 未开通用户的首条业务消息只启动一次开通并发送一次“正在核对”提示；业务正文不保存、不执行、不回显。同一事件顺序重投、并发重投时均不再次启动或重复提示 | L2（可注入 + 真库并发）+ L4a | 已认领 |
| V-开通-11 | 唯一命中有效权限后只提示“正在完成开通和权限同步”，不提前宣告成功；只有环境创建、权限发布和当前用户 MCP 权限确认全部完成后才发送成功提示，并展示实际公司与职能范围 | L2（状态机 + 真库）+ L4a | 已认领 |
| V-开通-12 | 身份 / 花名册 / 银河权限的零条、多条、冲突、重复、资料不完整、无受支持职能、非在职或其他确定性非唯一成功结果，都发送同一固定银河申请指引并终止；不创建用户环境、不发布权限、不生成人工待办，内部失败原因仍可区分 | L2（可注入 + 真库）+ L4a | 已认领 |
| V-开通-13 | 提示分支互斥且优先级固定：所有确定性非唯一成功结果走无可用权限提示；十五分钟 MCP 同步超时走“权限同步未完成”；组织资料不可用或其余 Lingxi 内部意外故障才发送 `LX-ONBOARD-001`；一旦选定终态，后置异常不得改写为另一种用户结论 | L2（分支与注入失败）+ L4a | 已认领 |
| V-开通-14 | 开通在顺序重试、并发重试和进程重启后都不重复创建用户、环境或权限发布，不覆盖已确认资料；首次结果、恢复动作和最终结果均可按用户与事件回读审计 | L2（真库并发 + 进程重启）+ L4a | 已认领 |
| V-开通-15 | 工号与邮箱沿身份链写入 `app_user` 并可由正式适配器原值读回；写入-读回逐字段相等，不被静默丢弃、截断或规范化改写；真库 catalog 证明建档适配器查询列包含两列 | L2（真库） | 已验证 |
| V-开通-16 | #52 日报基线读取同一份建档资料：已开通用户建档后，日报三字段非空且与 `app_user` 逐字段一致；存储断口存在时用例必须变红 | L2（真库） | 已验证 |
| V-开通-17 | 确定性匹配失败的内部原因码互不合并，而用户侧提示逐字节相同；零条、原始多条（含字段相同重复行）、双键冲突、资料不完整与无受支持职能等失败不得静默变为成功 | L2（真库 + 纯逻辑） | 已认领 |
| V-开通-18 | 十五分钟同步超时之后仍然确认成功的用户，最终会被写成 `active` 并得到「可以开始使用」的主动通知；在此之前他不会收到任何暗示已经可用、实际却发不了问数的消息。**归属**：迟到就绪恢复职责（`apps/scheduler/late_readiness_recovery.py`），与 S-D-02 同住 `lingxi-scheduler`、不改动 `OnboardingRunner` 自身——`OnboardingRunner` 判超时时仍然当场返回，恢复不在它的调用栈里发生；`active` 现在有两个写入方，均只经过同一条 `advance_provisioning_state` 条件更新（只前进不回退），不构成竞态 | L2（状态机，注入时钟/假探针）+ L4a | 已认领 |
| V-开通-19 | **有条件**（外部独立审查 P3-5 收窄措辞，此前的无条件表述过强）：开通链在 `advance(provisioning)` 之后中途失败或中途死亡的用户，在通知能够送达、账号保持启用、且收口所需素材未过期的前提下，最终一定离开 `provisioning`/`mcp_syncing` 并收到一条能让他知道"可以再发一条消息重试"的终态；收口过程不释放 `inbound_event.onboarding_dispatched_at` 的认领，结构上不可能对同一事件双跑。**三格已知例外，明确登记、不在本条断言覆盖范围内**：①通知持续送不到（飞书长期不可用）——编排层「当场收口」结构上要求 `_notify` 已确认送达才收口（`V-开通-19` 的 P2-1 修复），因此用户会原样停在中途格直到通知恢复；停摆扫描职责同样"先通知后收口"，同一原因下也不会收口；②`SYNC_TIMEOUT` 归 `V-开通-18` 继续等待，且其重试窗口受 `publish_outbox` 内容快照九十天保留期约束（`V-开通-18` 已登记的同一条边界）；③账号在中途格期间被停用（`account_state≠'enabled'`）——收口 CAS 的账号启用守卫结构上不生效，中途状态原样保留，交给账号停用流程处理，不算作本条断言的失败。**归属**：两条互补路径共同承担（Issue #282）——「当场收口」（`core/identity/onboarding_runner.AutoOnboardingRunner._abort_if_stalled`，只在通知确认送达时触发）覆盖链跑到确定失败终态但仍停在中途格的一半；开通中途停摆收口职责（`apps/scheduler/stalled_provisioning.StalledProvisioningDuty`，与 S-D-02、V-开通-18 同住 `lingxi-scheduler`）按**该用户最新一次认领**时间 + 45 分钟租约周期性回看（候选查询按最新事件取到期时刻，不被更早的历史认领误判——P2-5 修复），覆盖链本身死掉、当场收口也够不到的另一半，且带进程内通知退避与候选过采样避免对失败收件人无上界重试或被"毒候选"饿死新候选（P2-2 修复）。两者共用同一个收口写口（`adapters/postgres_identity.PostgresAppUserStore.abort_stalled_provisioning`，SQL 层独立的 `provisioning_state <> 'active'` 与 `account_state = 'enabled'` 两道闸，不依赖调用方传对 `expected_states`——P2-3 修复），租约由装配断言 `assert_stalled_lease_exceeds_chain_budget` 守住必须严格长于链从最新认领起、跑到终态可能停留的最长时间。停摆扫描的候选集合与既有的未开通首聊交接对账、迟到就绪恢复（`V-开通-18`）按同一批构造数据验证互不重叠（该验证同时要求对方候选集合真实非空，不是空集与空集的空话比较——P3-1 修复） | L2（状态机 + 真库 + 可注入）+ L4a | 已认领 |
| V-开通-20 | 用户可见文案「错误码：`LX-ONBOARD-001`」（`onboarding.internal_error`）与「权限同步未完成，已转交处理」（`onboarding.sync_timeout`）都带**本次事件的追溯号**（`{reference}` 占位，渲染值为该次 `inbound_event.trace_id`）；两次不同追溯号的失败终态，用户正文不再逐字相同。占位名**不得**叫 `trace_id`——那会命中 `config/content.py` 的内容安全正则（`_PROCESS_MARKER_PATTERN`），在内容目录**加载期**让 gateway/worker/scheduler 三个进程全部起不来；该安全规则本身不放宽，并补一条主动违规测试证明它仍然拦得住。开通中途停摆收口（`V-开通-19`）改用专用文案键 `onboarding.stalled`（不再复用 `onboarding.internal_error` 逐字发送），同样带追溯号，说明"等待已久、可再发一条消息重新开始"，不套用一般性内部故障话术。`onboarding.internal_error` 措辞同时改口：去掉"请不要重复发送"（与"再发一条消息即可自愈"的路径直接矛盾），改为允许并引导稍后重试；"已转交管理员处理"这句承诺由 `V-告警-09` 落实为真实送达。不新增第二个开通错误码——判据是"用户要做的事不同"，需要用户/管理员补数据的本侧故障子类当前是空集，均已走 `onboarding.not_authorized` | L2（内容目录纯逻辑 + 编排层可注入，含目录加载期/渲染期两处主动违规测试） | 已验证 |
| V-开通-21 | **内测名单闸**（Issue #302 S-N-01；Bot-Test 全员可见，任何名单外员工都能真实私聊触达，编排者 2026-08-24 API 回读坐实）：内测名单外的 `open_id`（含从未出现在任何名单中的未知对象）触达开通链时，在身份定位、花名册/银河匹配、建档、用户环境创建、权限发布**发生之前**（`core/identity/onboarding_runner.AutoOnboardingRunner._run` 最前端）得到统一「内测未开放」终态（`onboarding.innertest_not_open`，不提及名单存在或内部细节、不带错误码/追溯号）；`app_user`/`mcp_access_token`/`publish_outbox`/用户环境目录**零新增行**；仅留一条审计（`onboarding.innertest_roster_rejected`，只含事件与追溯号，不含 open_id 的任何形式——包括脱敏形式，`redact_identifier()` 的返回值按其自身文档字符串只允许进日志、不可进结构化审计字段，`V-花名册-34` 拦着；需要还原身份时凭事件号回读 `inbound_event`——不含消息正文） | L2（可注入） | 已验证 |
| V-开通-22 | **默认拒绝**（[验证与门禁第八节](验证与门禁.md#八否定测试)否定测试第 4 条判据：用一个不在任何名单中的未知对象证明）：内测名单未配置（`LINGXI_INNERTEST_ROSTER_OPEN_IDS` 缺失/空白）时闸对任何 `open_id` 返回拒绝；名单包含无法识别条目时 `parse_innertest_roster` 整份拒绝（`InnerTestRosterConfigError`），`SchedulerConfig.from_env` 据此在进程启动期快速失败（错配不是未配，同本仓库其余标识类环境变量既有纪律），不静默退化为放行。变异删除或改坏该闸判定（如默认放行）必须使专用测试变红，恢复后转绿 | L2（可注入 + 构造期负向 + 纯逻辑） | 已验证 |
| V-开通-23 | 内测名单内的 `open_id` 不受闸影响，按既有开通链次序（`V-开通-01…20`）正常推进，不产生任何额外拒绝或延迟。已 `provisioning_state=active` 的存量用户结构上不经过本闸：`core/conversation/pipeline.py` 只对 `UserState.NOT_PROVISIONED` 才路由进 `HandledAs.AUTO_PROVISIONING`（进而进入 `AutoOnboardingRunner`），`ACTIVE`/`PROVISIONING`/`SUSPENDED` 三态各自另有分支，从不触达本闸 | L2（可注入） | 已验证 |
| V-开通-24 | **存量令牌 adopt-or-issue**（Issue #281 改道，产品负责人 2026-08-25 裁定，取代原「批量学习登记」ops 方向）：`AutoOnboardingRunner._issue_token` 在签发访问令牌前，若装配了存量令牌只读源（`core/identity/stock_token_source.StockTokenSource`），先按该用户邮箱查一次旧系统写入的正式表——**无行**或**有行无密文**都退回原路径 `issue_token` 签新；**有行有密文**则解密后经新增的 `TokenIssuer.adopt_token(user_id, secret)` 采纳（`INSERT ... ON CONFLICT DO NOTHING` 再回读，沿用签发同一条"绝不覆盖"纪律：Lingxi 库已有该用户令牌行时以库内既有为准，候选明文被丢弃）；**否定断言——解密失败必须响亮失败停该用户本次开通（`OnboardingChainError`，用户侧走 `LX-ONBOARD-001`，可重试），绝不回退签新**（签新会让这个人的用户环境令牌与正式表令牌错位，造成真实 MCP 认证静默失败）；有行但 `status` 非 `approved` 时按"有行含密文"同路径处理，仅在审计里标注，不改变采纳与否（权限面由银河同步权威决定，不由本步裁量）。**哨兵**：存量令牌只读源坐标（`LINGXI_STOCK_TOKEN_BITABLE_APP_TOKEN`/`_TABLE_ID`）未配置时装配层不建出该源，`_issue_token` 原样走 `issue_token`，与改动前逐字节一致。四态各留可分辨审计（`onboarding.stock_token_adopted`/`stock_token_existing_kept`/`stock_token_absent`/`stock_token_decrypt_failed`），字段只含内部用户标识与状态分类，零明文、零密文、零邮箱值 | L2（可注入假源 + 假 cipher，含变异验红）+ L2（真库 `adopt_token`，`tests/test_mcp_token_postgres.py`）；L4a（真实存量表读取 + 真实解密沿用）未取得，留待 stage 受控窗口 | 已认领 |
| V-开通-25 | 失败原因落库（Issue #337，S-H3-1，迁移 `0077` `onboarding_failure`，`trace_id` 主键）：`AutoOnboardingRunner._execute` 终态 `reason` 非空时紧邻 `onboarding.result` 审计落一行；`StalledProvisioningDuty` 租约到期成功收口时紧邻 `stalled_provisioning.aborted` 落一行，覆盖链死掉、前者从未写出的一半。否定断言：成功终态/CAS 未命中/通知失败均不落行；落库异常不带走已决定的终态。`ON CONFLICT DO NOTHING` 幂等。已知边界（详见迁移文件头部）：`start()` 两条同步 `INTERNAL_ERROR` 分支不经过 `_execute`，不在本条覆盖范围 | L2（可注入，含变异验红）+ L2（真库） | 已认领 |

**2026-08-26 对账（#100）翻转为已验证**（均为纯 L2 声明，L4a 分量未取得的行未翻转）：
- `V-开通-01` → `test_identity_postgres_records.py::AppUserRecordTest::test_a_new_identity_is_recorded_without_a_permission_record`/`::test_the_permission_column_exists_so_the_null_is_a_verifiable_fact`；commit `aedd722`
- `V-开通-04` → `test_identity_postgres_records.py::ProvisioningStateAdvanceTest::test_it_never_goes_backwards` + `AppUserProvisioningContractTest::test_reentry_never_rewinds_the_provisioning_state_or_the_permission_columns`；commit `e50bafd`
- `V-开通-08` → `test_identity_postgres_records.py::AppUserRecordTest::test_a_latin_only_name_is_recorded_unchanged`；commit `aedd722`
- `V-开通-15` → `test_identity_postgres_records.py::AppUserRecordTest::test_roster_fields_round_trip_through_app_user_adapter_and_catalog`；commit `86fdb91`
- `V-开通-16` → `test_identity_postgres_records.py::AppUserProvisioningContractTest::test_the_daily_report_baseline_reads_what_provisioning_wrote`；commit `a668be1`（#89 S-B-03）
- `V-开通-20` → `test_content_catalog.py::test_a_template_naming_the_trace_id_placeholder_is_rejected_at_load_time` + `test_a_rendered_text_smuggling_the_forbidden_marker_through_a_variable_is_rejected`；commit `9d38763`（Issue #280 裁定）
- `V-开通-21`/`V-开通-22`/`V-开通-23` → `test_onboarding_runner.py::InnerTestRosterGateTests`（类文档字符串自述认领这三条）+ `test_innertest_roster_gate.py::DefaultDenyTests`/`ParseInvalidConfigTests`；PR #309 commit `85ea72c14e`

`V-开通-02/03/09/17` 的纯函数半边有 `test_galaxy_account_match.py`（文件自述"只是纯函数层"）覆盖，但矩阵声明的"真库"半边本次未找到独立的真库断言，保守未翻转，非缺陷。`V-开通-06/07/10…14/18/19/24` 声明层级含 L4a（或 24 的 L4a 明确"未取得，留待 stage"），本任务范围内不补验，保持已认领。

### 身份与专用授权

由已关闭的 [#16](https://github.com/Moshuiwang/lingxi/issues/16) 交付；`V-身份-05…09` 由 [#67](https://github.com/Moshuiwang/lingxi/issues/67) E1 新增，只由本次 L2 检查负责，不继承旧证据；`V-身份-11` 由 [#137](https://github.com/Moshuiwang/lingxi/issues/137) 新增（首次建立与续期共用入口，显式确认参数区分），真实首次建立属受控 Ops 窗口，不由 L2 认领。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-身份-01 | 同一飞书身份重复触发，不产生第二条 `app_user` 记录 | L2（真库） | 已验证 |
| V-身份-02 | 专用授权用户自身不会被建成 `app_user` 记录。**应用层判定与数据库约束各挡一次**——只有应用层判定时，任何绕过该函数的写路径都能建出这条记录 | L2（真库） | 已验证 |
| V-身份-03 | 专用授权的 `refresh_token` 只以 Fernet 密文保存于**宿主机受控文件**（0600、原子替换；2026-08-05 选项 A 决策：凭据不进业务数据库，不随 Supabase 备份出司界），且不进入日志、Issue 或交付物；数据库侧 `feishu_delegated_subject` 登记表不含任何令牌、授权码或密文列 | L2（真库 + 文件与进程内出口断言） | 已验证 |
| V-身份-04 | 专用授权失效时，员工侧得到可理解终态，且不写入半条身份资料；续期失败或**结果不明确**时撤销凭据，不重放一次性 `refresh_token` | L2 + L4a | 已验证 |
| V-身份-05 | 重授权 state 载体只保存不可逆摘要、预期主体和时间元数据，文件 0600 且原子替换；缺失、过期、错配、重放、篡改五类 state 否定路径均在外部换码前拒绝，取消回调也先消费当前 state | L2（可注入 + 文件） | 已验证 |
| V-身份-06 | 重授权回调身份只取自换码后的正式飞书 `user_info.open_id`，并同时绑定发起 state 与当前正式主体登记；**续期模式**下登记缺失、身份不一致或登记在读取与保存间变化时均拒绝保存凭据，保存通过 expected 主体登记的原子 CAS 校验后才写入（首次建立模式的对应判定见 `V-身份-11`，两种模式互不放宽对方的条件） | L2（可注入） | 已验证 |
| V-身份-07 | 取消、换码失败、返回 scope 不完整、身份错绑或凭据落盘失败均返回失败终态，不保存或宣告半成品凭据为成功 | L2（可注入） | 已验证 |
| V-身份-08 | 授权码、access token、refresh token 及完整回调 URL 不进入 state、结果、终端输出或日志；失败日志只记录脱敏错误类别 | L2（进程内出口断言） | 已验证 |
| V-身份-09 | 正式重授权入口以 `python -m lingxi.apps.reauthorize` 进入随 scheduler 镜像发布的安装包，并由 `job` profile 的一次性 compose service 以 uid 10001 挂载凭据卷执行；回调经 OAuth Bridge 主动 WebSocket 接收，不开启交互终端；state 与凭据路径及锁文件不重叠 | L2（制品 + 编排检查） | 已验证 |
| V-身份-10 | OAuth Bridge 按精确 state 路由：已注册的重授权 state 只进入正式 `FeishuReauthorizationEntry` 换码与 delegated vault/CAS 路径，未注册的首次开通 state 仍进入 `OAuthResultProcessor`；两条路径不能串 | L2（注入式集成）+ L3 | 已认领 |
| V-身份-11 | 专用授权主体的**首次建立**只在同一重授权入口的显式确认下发生：两个参数缺一不可（缺任一即拒绝且不装配入口、不发起授权）；主体登记非空时一律拒绝，既不覆盖也不更新既有登记；写入走保存事务内「登记仍为空」的原子 CAS（不是先读后写），建立成功后由既有续期路径接管；每次发起、拒绝、完成、失败都写一条含运行时身份、主机、时间、脱敏主体与 `mode=bootstrap` 的审计记录，审计写失败不得静默——发起前失败即中止且不发起授权，建立后失败按不可恢复报错并以非零退出码结束 | L2（可注入 + 真库） | 已验证 |

2026-08-08 对账：`V-身份-01…03` 真实 PostgreSQL/文件/进程内断言通过；`V-身份-04` L2 负向路径与 [#51](https://github.com/Moshuiwang/lingxi/issues/51) 受控凭据轮换 L4a 通过（产品负责人确认范围内）。**不代表**常驻同步、正式开通入口、权限发布或生产部署已可用。

三条支撑性规则同样按可判定断言落实，不单独编号：**完整性校验不通过就不提交半轮快照**（真库负向：校验失败后成员行为 0、批次落 `failed`）；**组织快照不保存在职状态**（真库：成员快照表无 `status` 类列，在职状态每次实时回读，见 `V-开通-07`）；**快照到期时间由来源时间推导且不可后移**（真库：触发器覆盖调用方传入值）。

**2026-08-26 对账（#100）翻转为已验证**：
- `V-身份-05` → `test_feishu_reauthorization.py::StateStoreTest`（三个否定用例）；commit `36df601`
- `V-身份-06`/`V-身份-07` → `test_feishu_reauthorization.py::ReauthorizationEntryTest`（`test_callback_identity_is_read_from_feishu_and_mismatch_does_not_save`/`test_subject_registration_change_between_begin_and_callback_blocks_save`/`test_incomplete_exchange_result_fails_closed_without_saving` 等）；commit `36df601`
- `V-身份-08` → `test_success_result_and_logs_never_contain_authorization_values`；commit `11d0c69`
- `V-身份-09` → `test_reauthorize_app.py::ReauthorizeAppTest` + `deploy/compose.yaml` 的 `reauthorize` 服务（`user: "10001:10001"`、`profiles: [job]`、`command: ["python","-m","lingxi.apps.reauthorize"]`）+ `check_deploy_contract.py`；commit `bcbf605`
- `V-身份-11` → `test_feishu_reauthorization.py::BootstrapModeTest`（11 个用例覆盖两参数缺一不可/CAS/审计）；commit `843e9a3`（#137 S-B-05）

`V-身份-10` 声明层级含 L3（独立复查），本次未确认该独立复查是否已实例化，保守保持已认领。

### 会话与任务

`V-会话-01`…`V-会话-06` 与新增的 `V-会话-08`…`V-会话-10` 由 [#57](https://github.com/Moshuiwang/lingxi/issues/57)（S4 前半）认领；`V-会话-06` 本批只认领 L2 部分（`stop_requested` 落库与话题隔离），"真正中断执行并回复停止阶段"属 S4 下半与 L4a。`V-会话-07` 未认领。`V-会话-11` 由 [#304](https://github.com/Moshuiwang/lingxi/issues/304) 批次 5 认领（产品负责人 biai-stage 真实测试暴露：以 / 开头的业务消息被执行层解析成系统命令，gateway 层在入队前直接拦截）。

> **编号说明**：矩阵编号需形如 `V-<组>-<两位数字>`，#57 原表的字母后缀 `02a`/`05a`/`06a`/`07a` 分别续号为 `V-会话-08`/`09`/`10`、`V-接入-13`，断言正文未变。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-会话-01 | 两并发请求抢占同一话题，恰好一个成功、一个收到"任务仍在处理中"。**释放只能由持有者执行**：非持有任务的释放影响 0 行 | L2（真库并发） | 已验证 |
| V-会话-02 | 上一任务结束后 119 分钟内的新任务续用旧会话；121 分钟后不续用 | L2（纯逻辑，注入时钟） | 已验证 |
| V-会话-03 | 任务本身运行 3 小时后结束，紧接着的新任务**仍续用旧会话** | L2（纯逻辑）——易错点：阈值只能读结束时间 | 已验证 |
| V-会话-04 | 话题忙碌期收到的消息未写入 `task` 表，且未进入任何会话上下文；不排队、任务结束后不自动提交 | L2（真库） | 已验证 |
| V-会话-05 | 话题 A 执行 `/new` 后 A 的 `agent_session_id` 置空，话题 B 的 `agent_session_id` 与 `last_task_ended_at` 均不变 | L2（真库） | 已验证 |
| V-会话-06 | 话题 A 执行 `/stop` 时：A 的运行中任务被置 `stop_requested`，话题 B 的运行中任务 `stop_requested` 保持 `FALSE`、`running_task_id` 不变 | L2 + L4a | 已认领 |
| V-会话-07 | 不同用户的会话与交付物互不可见 | L2（真库）+ L4a | 未认领 |
| V-会话-08 | 续用判定发生在**入队时**并写入 `task.resumed_session`；该任务在队列中排队 3 小时后被领取，`resumed_session` 值不变（排队时长不改变会话归属） | L2（真库） | 已验证 |
| V-会话-09 | 话题**忙碌期**收到的 `/new` 只得到"当前任务仍在处理中"，**不清空上下文、不入队、不在任务结束后自动生效** | L2（可注入） | 已验证 |
| V-会话-10 | `/stop` **不被忙碌判定拦截**：话题忙碌时 `/stop` 仍被处理（3.2 第 6 步「忙碌 且 非 `/stop`」），而非回"当前任务仍在处理中" | L2（可注入） | 已验证 |
| V-会话-11 | 业务用户（非管理面）发送以 `/` 开头、但不是 `/new`/`/stop` 的文本消息时：不写入 `task` 表，直接回复固定文案，审计只记分类不记正文；**否定断言**：句中间出现的 `/`（日期、URL）不触发拦截；**不受忙碌判定影响**——忙碌话题收到同类消息仍得到同一条拒绝文案而非"当前任务仍在处理中"；名单外用户（`innertest_roster_gate` 判定不通过）仍优先落到既有内测未开放文案，不被本行拦截；已开通的普通业务用户发送此类消息不触达管理命令面 | L2（可注入） | 已验证 |

**2026-08-16 Epic A/S-A-07（[#154](https://github.com/Moshuiwang/lingxi/issues/154)）：本组一条都未翻转。** 真实链路只用单一话题，只能覆盖每条断言的一半：`V-会话-04`/`05`/`06`/`09` 各取得部分真实观察（不入队/清空上下文/唯一停止终态/忙碌提示），但对照话题 B 或"不清空/不入队"等否定半句未取证；`V-会话-10` 由同一次操作反向印证；`V-会话-02`/`03`/`08` 本次未验证。跨话题并发留后续受控窗口。

**2026-08-26 对账（#100）翻转为已验证**（全部 commit `0aa3a63`，#57，除注明外）：
- `V-会话-01` → `test_gateway_postgres.py::TopicSerialisationTests::test_concurrent_claims_exactly_one_wins` + `test_release_only_by_the_holder`
- `V-会话-02`/`V-会话-03` → `test_gateway_pipeline.py::SessionWindowTests::test_119_minutes_resumes_and_121_minutes_does_not` / `test_a_three_hour_long_task_still_resumes`
- `V-会话-04` → `test_gateway_postgres.py::TopicSerialisationTests::test_second_message_while_busy_gets_the_hint_and_writes_no_task`
- `V-会话-05` → `test_gateway_postgres.py::CommandIsolationTests::test_new_clears_only_its_own_topic`
- `V-会话-08` → `test_gateway_pipeline.py::ResumeDecisionAtEnqueueTests::test_resume_flag_is_computed_from_request_time`
- `V-会话-09`/`V-会话-10` → `test_gateway_pipeline.py::BusyCommandTests::test_new_during_busy_only_gets_the_hint_and_clears_nothing` / `test_stop_during_busy_is_processed_not_deflected`
- `V-会话-11`（本周新增，Trace #304 批次 5）→ `test_gateway_pipeline.py::SlashCommandRejectionTests`（类文档字符串自述"V-会话-11"，7 个用例逐条覆盖不入队/固定文案/审计不含正文/句中 `/` 不受影响/不受忙碌判定影响/名单外优先内测未开放/已知命令不受影响）；commit `48340f1`

`V-会话-06` 声明含 L4a 未取得；`V-会话-07` 未认领，不动。

### 用户记忆

`V-记忆-01`…`V-记忆-07` 由 [#357](https://github.com/Moshuiwang/lingxi/issues/357) S-H3-3（Trace #373 H3 批，D1 显式登记范围）认领：用户显式登记术语映射/口径偏好/惯例模板，下一次问数生效；不做自动学习/确认卡（M2 不在范围）。本机真库+可注入测试与 CI（`Story Fast`/`Epic Full`）均已通过；`biai-stage` 已完成两轮受控 L4a 验收（S-H3-4/S-H4-3）与 2026-08-29 产品负责人真人从飞书客户端发起的全真实入站验证（问数/文档/表格/`/memory` 三连全部真实走通，见 [#373](https://github.com/Moshuiwang/lingxi/issues/373)「PM 回归验证与裁定留痕」评论），逐行追加事实见下表后说明；这些追加事实均只印证既有 L2 结论，不改变各行认领所需的证据类型，`V-记忆-06`（停用清除）未被本轮验收覆盖，保持 L2 不动。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-记忆-01 | `/memory remember <类型> <关键词> => <说明>` 登记记忆（类型：`term_mapping`/`calibration_preference`/`convention_template`）；只接受 `key => value` 键值对形状，任意自由文本（无 `=>` 分隔符）一律拒绝——这是「不存数据值」红线在命令语法层面的落点 | L2（可注入+真库） | 已认领 |
| V-记忆-02 | `/memory list` 查已登记记忆；`/memory forget <id>` 删除单条，跨用户传入他人 id 结构性零生效（`WHERE user_id` 直接挡住，不是先查后判）；`/memory clear` 清空全部并返回清除计数 | L2（真库） | 已认领 |
| V-记忆-03 | 单用户记忆条数上限 50 条，超过直接拒绝、不静默截断；同一 (类型, 关键词) 重复登记是更新（不计入新增上限） | L2（真库） | 已认领 |
| V-记忆-04 | `/memory` 命令面放在忙碌判定之前，不受话题忙碌拦截（与 `/stop` 同一姿态）；格式不对的 `/memory` 消息得到专属用法提示，不与 `V-会话-11` 的泛用拒绝文案混同——否定断言：`/memoryabc`、`/config` 等仍受泛用拒绝拦截，精确豁免不外溢 | L2（可注入） | 已认领 |
| V-记忆-05 | 跨用户绝对隔离：worker 注入提示词段落与 `/memory list` 两条读路径均断言"渲染给用户 B 的最终文本里真的没有用户 A 的任何记忆内容"，不只断言查询条件写对了 | L2（真库） | 已认领 |
| V-记忆-06 | 账号停用（同 `V-管理-39` 「感知即清」同一事务）与权限真变（`record_decision` 的 `permissions` 列真变化，`UNCHANGED` 不清）时清空该用户全部记忆；硬删除，`resume_user` 不恢复已清记忆；清除/审计失败时整个事务回滚，不出现「已清、状态未变」半套状态 | L2（真库，变异验红） | 已认领 |
| V-记忆-07 | worker 侧记忆查询失败 **fail-open**（与 `.mcp.json` 的失败关闭相反）：任务不因记忆查询失败而失败，只留结构化告警 `worker.user_memory.degraded`；未装配读取器、或该用户没有任何记忆时，任务级系统提示词不追加任何内容 | L2（可注入） | 已认领 |

**2026-08-28/29 追加事实（均不改变各行认领状态，[#373](https://github.com/Moshuiwang/lingxi/issues/373)）**：
- `V-记忆-01`（登记）：命令面真实回执两轮坐实——S-H3-4 受控注入（用户A `/memory remember term_mapping 大黄 => 尼日利亚GMV` 真实落一行，`user_id` 外键正确绑定）；2026-08-29 产品负责人真人飞书入站 `/memory` 三连全部真实走通。语法层否定分支（自由文本无 `=>` 一律拒绝、类型枚举）未在任一轮 L4a 场景中复测，仍以本机真库/可注入测试为证据主体。
- `V-记忆-02`（`list`/`forget`/`clear`）：命令面真实回执部分坐实——S-H3-4 `/memory list` 真实收到列表（`reply.sent memory.list`）；S-H4-3 六击第 6 击 remember→forget 全链，`command.memory_forget`+`memory.forgotten` 回执真实送达、`user_memory` 归零；2026-08-29 真人入站复验三连。`clear` 命令未在任一轮 L4a 场景中被触发，仍以真库测试为唯一证据。
- `V-记忆-05`（跨用户隔离）：S-H3-4 受控验收阶段以真实数据库核对——用户A登记记忆后，`user_id`↔`open_id` 结构绑定核实用户B 无对应行（受控测试身份非生产），与本条真库+否定用例证据同向印证，未新增独立证据类型。
- `V-记忆-07`（提示词注入生效）：S-H4-3 六击第 6 击坐实"注入措辞体现"——用户A登记记忆后，下一次真实问数任务的模型回答开头引用「按你的记忆设置，『大黄』= 尼日利亚…」；本轮记忆读取正常，未做故障注入，fail-open 分支本身仍只有可注入测试证据。
- `V-记忆-06`（停用清除，**未覆盖，保持 L2 不动**）：停用触发记忆清除（同 `V-管理-39`「感知即清」同一事务）在 H3/H4 两轮 L4a 受控验收与 2026-08-29 真人入站验证中均未触发——受控 `replay_inbound_event` 载具不支持卡片回调，真人验证未做停用动作。仍以真库测试为唯一证据，留 PM 回归后续观察项。
- `V-记忆-03`（条数上限/重复登记）与 `V-记忆-04`（忙碌豁免/格式提示）：本轮两次验收均未触及，无新增证据，保持原状。

### 执行层工具边界与审计

由 [#29](https://github.com/Moshuiwang/lingxi/issues/29) 认领判定与审计组件，[#37](https://github.com/Moshuiwang/lingxi/issues/37) 认领把它们装进可执行 worker 入口（`V-执行-18…21`）。判定层是执行层自己的 `PreToolUse` 屏障，不是 `allowed_tools` / `disallowed_tools`——[当前能力](../当前能力.md)已记录后者不是严格白名单。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-执行-01 | 白名单外的工具调用在执行前被拒，且不产生任何副作用（文件、cron、网络） | L2 + L4a | 已验证 |
| V-执行-02 | 新增一个未在任何名单中出现的内置工具，默认被拒——**不需要维护禁用名单** | L2 | 已验证 |
| V-执行-03 | 被拒回合仍取得非空最终正文和恰好一次终止结果，同回合内白名单工具照常执行 | L2 + L4a | 已认领 |
| V-执行-04 | 工具抛错时审计中可查到工具名、入参与错误 | L2 | 已验证 |
| V-执行-05 | 调用被拒时审计中可查到该次拒绝及其理由——拒绝**没有任何 hook 回调**，只能由执行层自己在 `PreToolUse` 记账 | L2 + L4a | 已验证 |
| V-执行-06 | MCP 以正常响应（`isError=false`）返回的业务失败，审计中可判定用户未拿到结果；无法归类的回执记为未知，不得当作已送达 | L2 | 已验证 |
| V-执行-07 | 白名单不接受通配符与前缀匹配，空白名单在构造期即失败 | L2 | 已验证 |
| V-执行-08 | 拒绝理由不含白名单内容，并要求模型不重试、不向用户转述内部工具名 | L2 | 已验证 |
| V-执行-09 | 规则层（`disallowed_tools`）拦下的调用不经过 `PreToolUse`，其错误回执在审计中仍可查到，并被标为「本层未判定」而非放行或本层拒绝 | L2 + L4a | 已认领 |
| V-执行-10 | **执行成功却找不到本层判定记录的调用必须留痕**：`PostToolUse` 或回执认不出对应判定时补记「本层未判定」，审计不得因此断言本回合没有工具调用 | L2 | 已验证 |
| V-执行-11 | 收到认不出的 hook 事件名且带工具名时，审计中留痕 | L2 | 已验证 |
| V-执行-12 | 记账失败不得降级为放行：模型可控的入参使记账抛异常时，拒绝响应照常返回，并留下一条「明细丢失」记录 | L2 | 已验证 |
| V-执行-13 | 回执认不出属于哪一次调用时另记一条，不得挂到任一次同名放行调用上 | L2 | 已验证 |
| V-执行-14 | 一个会话跑多个回合时，回合结论互不污染（`hooks` 是会话级的，`TurnAudit` 必须能翻页） | L2 | 已验证 |
| V-执行-15 | 回执归类与键的遍历顺序无关，不随 `PYTHONHASHSEED` 在进程之间翻转 | L2 | 已验证 |
| V-执行-16 | SDK 绑定层注册的事件集合与回调签名被锁住（桩模块）——**桩只能证明本侧形状没变，证明不了 SDK 接受这些事件名**，后者只有 L4a 能答 | L2 | 已认领 |
| V-执行-17 | **一次辅助调用成功不得宣告整个回合已送达**：同一回合内既有成功、又有业务失败／工具抛错／空结果／无法归类／缺失回执时，结论记 `UNKNOWN`；全部放行调用都成功才记 `OBTAINED`，一次成功都没有才记 `NOT_OBTAINED` | L2 | 已验证 |
| V-执行-18 | **可执行 worker 入口实际构造 `ToolGateway`、把 matchers 装进真实 `ClaudeAgentOptions.hooks`，并在每回合开始清空审计状态**；hook 判定、hook 回调、工具回执、最终正文汇入**同一个**回合审计。删除任一段接线后测试变红 | L2（桩 SDK 装配 + 真实 SDK 构造冒烟）+ L4a | 已认领 |
| V-执行-19 | 白名单内唯一的只读工具可以执行；`Write` 固定探针经 worker 入口在**执行前**被拒，目标文件不存在，拒绝及其原因码在审计中可查 | L2（桩 SDK 真写盘探针）+ L4a | 已验证 |
| V-执行-20 | 合法形态未知工具名、未知字段名、白名单字段值、自由错误正文与最终正文中的固定假凭据，均不以完整形式进入 worker 输出的报告与结构化日志 | L2 | 已验证 |
| V-执行-21 | 最终提交在 `biai-stage` 分别完成一次成功、一次越界拒绝、一次业务失败；每次均取得非空最终正文和恰好一次主回合终止结果。若真实 MCP 只能得到未知而不能稳定复现业务失败，本断言不通过 | L4a | 已认领 |
| V-执行-22 | **否定断言**：子代理派生工具（`Task` 及等价能力）不在白名单中，经 `PreToolUse` 默认拒绝；构造一次派生调用后无子会话、无子回合审计，主回合仍正常收口 | L2 | 已验证 |
| V-执行-23 | **否定断言**：规则拒绝（`disallowed_tools` / 权限规则）不被当作安全边界；即使规则层为空，白名单外调用仍由 `PreToolUse` 拒绝 | L2 | 已验证 |
| V-执行-24 | CLI 对超大 MCP 工具结果的通用截断提示（"exceeds maximum allowed tokens…offset and limit parameters"）经 `PostToolUse` 改写为不引导读本地文件的重查提示，只作用于以 `mcp__` 前缀命名的工具；改写次数（`oversize_rewrite_count`）经出口报告可查。文本提取覆盖字符串、内容块数组、`{"text": ...}`、`CallToolResult` 典型信封（`{"content": [...], "isError": ...}`）与未识别形状的 JSON dump 兜底五种输入形状（Issue #323 落地，Issue #328 opus 审查 P1-2 补齐 `CallToolResult` 信封这一最典型真实形状此前从未真正生效的缺口） | L2 | 已验证 |

**2026-08-09 受控真实验收（Plan #104/E3-A）：** 已发布不可变镜像在 `biai-stage` 以真实模型端点+真实只读 MCP 运行，未现场构建。证据见 [#29 评论](https://github.com/Moshuiwang/lingxi/issues/29#issuecomment-5230024441)。

- `V-执行-01` → **已验证**（L4a）：白名单外调用全部被拒、`ungated_count=0`，文件副作用探针配正向对照排除"目录不可写"误报；未覆盖 cron/网络出站探针，仍 L2。
- `V-执行-05` → **已验证**（L4a）：拒绝及原因码在真实回合审计中逐条可查，入参只留字段名不留取值。
- `V-执行-19` → **已验证**（L4a）：白名单只读工具执行成功；写文件探针在执行前被拒，原因码可查。

**同一次运行未翻转的四条**：`V-执行-21` 业务失败态未复现，按自身判据维持已认领；`V-执行-09` 本次名单为空未验证；`V-执行-03` 的非空正文/终止结果未逐项取证；`V-执行-18` 的跨回合清空未在单回合验收中复验。

**对外口径问题（[#141](https://github.com/Moshuiwang/lingxi/issues/141)）**：出口命中时整段正文被替换为兜底文案，回合报告仍记 `obtained`；`V-执行-17` 只判工具结果、看不到出口丢弃，不构成反例，但需待 #141 裁定后收口。

当前边界只认下面五条：

- `V-执行-18…20` 的 L2 只证明本侧装配；真实 SDK 触发与越界阻断需 `V-执行-21` 的 L4a 证明，三态齐备前不翻转。
- `V-执行-20` 只收紧 worker 输出边界，审计核心仍受 `V-审计-03` 残余缺口约束。
- `V-执行-08` 只约束模型提示，用户可见内部标识由 `V-卡片-04` 输出侧检查器拦截。
- `V-执行-06` 只按显式规则分类，无法识别数据归 `UNKNOWN`。
- `V-执行-11` 不保证发现 SDK 事件改名，真实触发只能由 L4a 证明。

本层不区分「承载结果的调用」与「辅助调用」，混合回合保守记 `UNKNOWN`——知情取舍：谎报 `OBTAINED` 比过多 `UNKNOWN` 更危险。

**2026-08-26 对账（#100）翻转为已验证**（测试方法名带 `v_zhixing_NN`/`v_shenji_NN` 前缀的即矩阵编号本身，已逐条独立核实语义匹配）：
- `V-执行-02` → `test_execution_tool_gate.py::DenyByDefaultTest::test_v_zhixing_02_an_unknown_builtin_tool_falls_into_the_deny_branch`；commit `89cf05b`
- `V-执行-04` → `ToolFailureAuditTest::test_v_zhixing_04_post_tool_use_failure_is_recorded_with_name_input_and_error`；commit `89cf05b`
- `V-执行-06` → `BusinessFailureAuditTest::test_v_zhixing_06_business_failure_wrapped_as_a_normal_response_is_not_obtained`；commit `89cf05b`
- `V-执行-07` → `ToolPolicyConfigurationTest::test_wildcard_entries_are_rejected_so_new_server_tools_cannot_self_authorize` + `test_empty_whitelist_is_rejected_instead_of_silently_allowing_nothing`；commit `89cf05b`
- `V-执行-08` → `DenyReasonTextTest`（3 用例）；commit `89cf05b`
- `V-执行-10`/`V-执行-11` → `UngatedCallAuditTest::test_successful_but_ungated_calls_are_recorded_instead_of_silently_dropped` / `test_unknown_hook_event_name_carrying_a_tool_leaves_a_trace`；commit `9a7db71`
- `V-执行-12` → `DecisionSurvivesAuditFailureTest::test_deny_is_still_returned_when_bookkeeping_raises`；commit `9a7db71`
- `V-执行-13` → `AmbiguousAttributionTest::test_failure_without_id_is_not_pinned_onto_one_of_two_same_named_calls`；commit `9a7db71`
- `V-执行-14` → `TurnBoundaryTest::test_second_turn_does_not_inherit_the_first_turns_terminal_result`；commit `9a7db71`
- `V-执行-15` → `ParsableButUnrecognisedResultTest::test_v_zhixing_15_classification_is_stable_across_eight_hash_seeds`；commit `0c7731c`
- `V-执行-17` → `MixedTurnResultTest`（7 用例）；commit `09c8a5e`
- `V-执行-20` → `tests/test_worker_entry.py::WorkerOutputRedactionTest::test_v_zhixing_20_no_fixed_fake_credential_survives_anywhere_in_the_report`；commit `0b70944`（#37）
- `V-执行-22` → `DenyByDefaultTest::test_v_zhixing_22_subagent_tool_is_denied_by_default_under_both_known_names`；commit `0c7731c`
- `V-执行-23` → `tests/test_worker_entry.py::ReadOnlyBoundaryTest::test_v_zhixing_23_empty_rule_layer_cannot_replace_the_pre_tool_use_boundary`；commit `0c7731c`

`V-执行-03/09/18` 含 L4a 未在本次同步补验；`V-执行-16`（桩模块形状锁）与 `V-执行-21`（纯 L4a）保守保持已认领。

### 输入内容安全与输出约束

由 [#93](https://github.com/Moshuiwang/lingxi/issues/93) 认领。本组 L2/L3 只证明固定注入夹具与出口约束；真实模型是否被未知样本诱导留给受控窗口。2026-08-09 已做一次窄样本 L4a（部分通过，详见表后对账），整组状态未翻转。`V-注入-10` 例外：由 [#304](https://github.com/Moshuiwang/lingxi/issues/304) 批次 5 认领，约束的是执行层入口的命令解析边界，不是模型输出的出口约束，与本组其余行证据范围各自独立。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-注入-01 | **否定断言**：指标名称 / 描述、花名册字段值、MCP 返回文本中嵌入固定指令样本后，工具调用集合不扩大；白名单外调用数恒为 0，白名单内调用集合与无注入基线一致 | L2 + L3 | 已认领 |
| V-注入-02 | **否定断言**：注入文本不改变用户可见的公司 / 职能范围，不产生写操作或新的执行入口；外部文本只作为数据，不授予权限 | L2 + L3 | 已认领 |
| V-注入-03 | **否定断言**：固定假凭据、他人姓名 / 工号 / 邮箱、内部工具名与系统提示不以完整形式进入最终输出、卡片或结构化日志 | L2 + L3 | 已认领 |
| V-注入-04 | 注入文本、业务失败与无数据均得到非空且可理解的安全终态；不把注入内容当作系统指令回显，不输出“权限不足”类误导措辞。终态正文若为模型生成，其输出校验范围按 `V-内容-07` 的 [#322](https://github.com/Moshuiwang/lingxi/issues/322) 新限定执行，不再套用固定词表 | L2 + L3 | 已认领 |
| V-注入-05 | **否定断言**：外部文本进入上下文时带有“待分析内容”数据标记；试图闭合标签或伪造边界的文本不能改变该角色 | L2 + L3 | 已认领 |
| V-注入-06 | 出口命中已知敏感值、工具名或标记时优先局部遮蔽：命中范围之外的正文原样保留，不因一次命中清空整段有效业务结论（[#141](https://github.com/Moshuiwang/lingxi/issues/141) 合同第 1 条） | L2 | 已认领 |
| V-注入-07 | **否定断言**：仅当命中范围覆盖了正文里全部非空白内容、没有任何可安全展示的业务结论时，才整段拒发；此时报告使用独立、可查询的 `redacted_withheld` 终态，`user_result` 不再记 `obtained`（#141 合同第 2 条） | L2 | 已认领 |
| V-注入-08 | 出口标记检测覆盖归一化后的中英文系统提示类高风险标记，并能识别已知敏感文本中的结构化片段（不要求整串精确匹配）；改坏中文分支或片段切分逻辑必须使对应用例变红（[#142](https://github.com/Moshuiwang/lingxi/issues/142) 两处缺口） | L2 | 已认领 |
| V-注入-09 | 跨事件边界的流式安全处理：检测状态跨 `feed()` 调用保留、维持有界尾部缓冲，`withheld` 后禁止后续释放；本条只证明 core 原语本身，接入 worker 事件流并对外投递留给 #151/#152 | L2 | 已认领 |
| V-注入-10 | `compose_agent_prompt` 把交给执行层的问题前，对**去除首尾空白后以 `/` 开头**的问题前置中性前缀，防止 Agent SDK 底层的 Claude Code CLI 把用户文本解析成系统斜杠命令（gateway 层已是主防线，本条是纵深防御，覆盖历史任务重试等绕过 gateway 拦截入队的路径）；**否定断言/哨兵测试**：不触发中性化的问题（含中间出现 `/` 的正常文本）逐字节不变 | L2 | 已验证 |

**2026-08-13 #149 落地（承接 #141/#142，本组无 L4a 输入，未翻转任何断言）：** 出口改为定位全部命中区间、局部替换（`V-注入-06`）；仅全部非空白正文被命中时才整段拒发（`V-注入-07`）；出口标记新增中文分支与结构化片段切分（`V-注入-08`）；`StreamingOutputGuard` 提供跨事件边界增量安全原语（`V-注入-09`）。均为 L2，真实模型能否规避检测留 Epic A 后置 L4a。

**2026-08-16 Epic A/S-A-07（[#154](https://github.com/Moshuiwang/lingxi/issues/154)）：本组一条都未翻转。** 真实链路上合成中文标记未被出口检测命中（`withheld=false`），证明规避检测问题依然开放，[#142](https://github.com/Moshuiwang/lingxi/issues/142) 保持打开；下游处理链路经默认关闭的注入夹具在真实进程/数据库/投递下验证了 `V-注入-06`/`07` 承诺的行为，但夹具跳过判定本身，只抬高路径信心、不改变断言层级。

**2026-08-09 受控真实验收（Plan #104/E3-A）：部分通过，全组维持已认领。** 证据见 [#93 评论](https://github.com/Moshuiwang/lingxi/issues/93#issuecomment-5230024737)。

- **入口（`V-注入-01/02/04/05`）真实链路未被诱导**：两条强对抗注入文本经 `external_texts` 传入，真实模型均未被诱导、只回答合法业务问题，且确认外部文本确实送达并被消费；入口转义确定性有效。
- **为什么仍不翻转**：本次只用一个模型、两条注入文本、单一来源，样本量不足以支撑一般性否定结论；工具返回内嵌注入、多轮渐进式注入、其他模型均未覆盖。
- **出口（`V-注入-03`）生效但有两处实测缺口**，由 [#142](https://github.com/Moshuiwang/lingxi/issues/142) 承接：系统提示片段泄露不拦（整串精确匹配）、中文「系统提示」字样不拦（正则只覆盖英文）；本次未泄露，但出口保护不得依赖模型自觉，`V-注入-03` 不得标为已验证。
- **另一处对外口径问题**由 [#141](https://github.com/Moshuiwang/lingxi/issues/141) 承接：出口命中即整段替换为兜底文案，报告仍记 `obtained`；`V-注入-04` 的非空终态字面成立，但"拿到结果"口径不成立，裁定前不翻转。

**2026-08-26 对账（#100）**：`V-注入-10`（本周新增，Trace #304 批次 5，已重点核实）翻转为已验证 → `tests/test_input_safety.py::SlashCommandNeutralizationTests`（类文档字符串自述"V-注入-10"，含哨兵否定测试逐字节不变、句中 `/` 不受影响）；commit `48340f1`。`V-注入-01…09` 声明层级均含 L3，组内 2026-08-09/08-13/08-16 多轮既往复核已明确"未翻转"，本次未发现推翻既往判断的新证据，尊重既有保守结论不重翻。

### 单任务资源护栏与用量

由 [#91](https://github.com/Moshuiwang/lingxi/issues/91) 认领。这里的 L2 只证明 worker 本侧配置、SDK 选项接线、可注入边界和安全摘要；受控真实超时 / 超轮数及百炼实际 usage 可得性属于 E3 后续 L4a，不能用本机结果替代。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-护栏-01 | 单任务最大 Agent 轮数从部署配置读取，缺失使用默认值；非正值或越过硬上限在启动期拒绝；配置值实际传入 SDK，达到上限时报告 `max_turns_exceeded` | L2（可注入 SDK）+ L4a | 已验证 |
| V-护栏-02 | 单任务墙钟上限从部署配置读取且不越过硬上限；墙钟超时报告 `turn_timeout`，与超轮数、正常完成、取消的原因码互不相同 | L2（注入时钟 + 可注入 SDK）+ L4a | 已验证 |
| V-护栏-03 | 护栏触发后报告为明确的 `guarded` 终态，不停留在处理中、不静默返回空结果；已有可确认结果标记为提前结束，未有可确认结果标记为未确认，不把半截正文标成正常完成 | L2（可注入） | 已验证 |
| V-护栏-04 | 任务报告记录 SDK 实际 Agent 轮数、注入时钟测得耗时、工具调用尝试数与实际执行数，数值与执行过程一致；SDK 未提供轮数时保持未知 | L2（可注入）+ L4a | 已验证 |
| V-护栏-05 | **否定断言**：SDK 未提供 usage 时报告仍包含 `status=unknown`，不填 0、不估算、不省略；可注入 mock usage 带 `source=mock`，不得被标为真实计费证据 | L2（可注入）+ L4a | 已验证 |
| V-护栏-06 | **否定断言**：usage 与资源摘要只保留受控数值字段，不出现问题正文、模型正文、凭据或个人资料原值 | L2（模式扫描 + 固定假凭据探针） | 已验证 |
| V-护栏-07 | **否定断言**：把轮数 / 墙钟配置调到最小值时，SDK 仍收到护栏且任务提前结束；执行器没有跳过护栏或自动重试的分支 | L2（可注入） | 已验证 |
| V-护栏-08 | 收尾宽限与业务执行预算彼此独立、各自有界：业务阶段超时不连带跳过或截断收尾（`__aexit__` 仍被调用一次），收尾本身挂起时在自己的宽限内失败并报告独立的 `drain_timeout` 原因码；任务报告分别记录业务执行预算、实测业务执行耗时、实测收尾耗时与实际总耗时，四者不得互相混淆或省略（[#143](https://github.com/Moshuiwang/lingxi/issues/143)） | L2（注入时钟 + 可注入 SDK） | 已验证 |

任务占用的数据库释放与最终用户呈现由 #90 / E4 负责，本组只输出可区分的 worker 终态信号。

**2026-08-13 #149（承接 #143 产品决定，未翻转既有断言）：** `LINGXI_WORKER_TURN_TIMEOUT_SECONDS` 口径明确为业务执行预算而非端到端总耗时；`resources` 新增预算/业务耗时/收尾耗时三字段与总耗时并列，不互相覆盖（`V-护栏-08`）；收尾独立配置 `drain_grace_seconds`（默认 30 秒），业务阶段超时不影响收尾完整跑完。

**2026-08-09 受控真实验收（Plan #104/E3-A）**，证据见 [#91 评论](https://github.com/Moshuiwang/lingxi/issues/91#issuecomment-5230024619)：

- `V-护栏-01` → **已验证**（L4a）：真实超轮数终态 `max_turns_exceeded`。
- `V-护栏-02` → **已验证**（L4a）：真实墙钟超时终态 `turn_timeout`，与超轮数可清晰区分，护栏在过程中生效而非跑完才报。
- `V-护栏-04` → **已验证**（L4a）：真实链路记录的轮数/耗时/工具调用数与执行过程一致，未提供时如实保持未知。
- `V-护栏-05` → **已验证**（L4a）：终止消息缺失时如实记 `unknown`，不填 0、不估算；usage 数字经回源核对确认为 SDK 真实返回值。

**同一次运行未翻转的三条**：`V-护栏-03` 的 `guarded` 终态可见，但两分支未逐项取证；`V-护栏-06`/`07` 未做针对性真实探测，维持 L2。

**未覆盖边界（本组不得被读成"资源护栏整组已验收"）**：SDK 给了终止消息但 usage 字段缺失或不可识别那条分支**未在真实链路触发**；**超时中止后模型侧请求是否真正停止计费未验证**；只验收了单任务护栏，**队列 / 并发模式下的资源护栏未验收**。

**2026-08-16 Epic A/S-A-07（[#154](https://github.com/Moshuiwang/lingxi/issues/154)，未翻转任何断言）：** 队列链路上真实取得墙钟超时与超轮数两个终态，用户均收到明确文案；但这只是把已验证的单回合结论扩到队列收口，不改变状态。`V-护栏-03` 两分支、`V-护栏-04…07`、并发模式护栏仍未验证。

**对外口径问题（[#143](https://github.com/Moshuiwang/lingxi/issues/143)）：墙钟上限不是回合总耗时上限。** 护栏在配置时刻触发，但收尾另需约 6 秒固定开销，`V-护栏-02` 不承诺总耗时不超过配置值；对外承诺"单任务最多 N 秒"前须由 #143 裁定口径。

**2026-08-26 对账（#100）翻转为已验证**：
- `V-护栏-03` → `tests/test_worker_entry.py::WorkerResourceGuardTest::test_v_hulan_01_max_turns_is_passed_to_sdk_and_has_distinct_guard_state`（`result_delivery="not_confirmed"` 半句）+ `test_v_hulan_03_guarded_turn_with_confirmed_result_is_marked_early`（`"confirmed"` 半句）；commit `98f676a`
- `V-护栏-06` → `test_v_hulan_05_mock_usage_is_numeric_summary_only`；commit `98f676a`
- `V-护栏-07` → `test_v_hulan_07_minimum_turn_guard_reaches_sdk_and_ends_early`；commit `d9ea6fa`
- `V-护栏-08` → `test_v_hulan_04_resource_counters_match_injected_execution`（budget/business_duration/drain_duration/duration_seconds 四字段）+ `tests/test_claude_agent_session_adapter.py::test_business_timeout_and_drain_timeout_together_do_not_mask_turn_timeout`（`__aexit__` 独立宽限）；commit `98f676a` / `470a1ae`

**核实中发现的可追溯性问题（非行为矛盾，登记供编排者知悉）**：`test_worker_entry.py` 里 `test_v_hulan_NN` 系列的编号后缀与当前矩阵 `V-护栏-NN` 编号存在偏移——按名称对应会分别对应到 `V-护栏-05`（实际内容是 `V-护栏-06` 的凭据/业务正文不进 usage）与 `V-护栏-04`（实际内容是 `V-护栏-08` 的四字段耗时）。以上引用已按读取到的实际断言内容而非方法名后缀核对，结论不受影响；若未来复用该测试文件的方法名做机械匹配，需先核实内容而非只信编号后缀。

### 最小运行告警

状态机（心跳、滞留、连续失败、去重、恢复、安全摘要）由 [#92](https://github.com/Moshuiwang/lingxi/issues/92) 认领；装进三个进程 `main()` 与健康检查由 [#153](https://github.com/Moshuiwang/lingxi/issues/153) 完成。告警发送失败不得改变三进程原职责；真实进程停止、真实管理群连续失败与恢复留后续 Ops Story。

`AlertDispatcher`/`AlertingDuty` 位于 `core/alerting.py`（#153）：三个进程独立部署、互不 import `apps.<name>`，共用同一套状态机与投递/去重语义。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-告警-01 | scheduler、gateway、worker **三个进程 `main()` 均已装配**心跳记录（#153：scheduler 的 `build_alerting_duty`、gateway 的同名函数、worker 的 `_build_alerting_duty` 各自建一份 `AlertManager`，互不共享——告警仍是进程内状态机，跨进程健康观测需 DB 载体留 S9）；组件在配置窗口内无心跳时从活跃翻转为不活跃并可观察，恢复后只产生一条恢复记录。**gateway 一个进程两条循环**（长连接主线程、#152 投递消费后台线程）分别注册 `gateway-longconn`/`gateway-delivery` 两个心跳键，任一条停摆都能被单独发现，不被另一条掩盖 | L2（注入时钟 + 真实进程构造）+ L4a | 已认领 |
| V-告警-02 | 排队滞留、运行心跳超时、重试耗尽、**待投递（awaiting_delivery）二十四小时强制到期收敛、目标 worker 版本不可用**（#153 新增后两类，`AlertKind.AWAITING_DELIVERY_STUCK`/`WORKER_VERSION_UNAVAILABLE`）使用五个互不合并的事件类型与安全摘要 | L2（可注入） | 已认领 |
| V-告警-03 | 飞书非终态发送连续失败达到 3 次 / 5 分钟才告警，终态失败立即告警；告警发送重试采用正向、单调退避且不忙循环。**#152 的 `DeliveryConsumer.on_alert` 注入点**（#153）经 `AlertingDuty.delivery_alert_callback()` 归入同一条 `FEISHU_SEND_FAILED` 通道，原始 kind 字符串（如 `dispatch_uncertain:card_finish`）消毒后作为 `scope` 独立限流，不同子类型互不掩盖 | L2（可注入） | 已验证 |
| V-告警-04 | 同一故障窗口只产生一条主告警，稳定成功 5 分钟后只产生一条恢复记录；恢复发送失败可重试但不重复产生逻辑记录 | L2（纯逻辑 + 可注入） | 已验证 |
| V-告警-05 | 告警观察、审计或发送失败不跳过其他 scheduler 职责、不停止 gateway 事件泵、不改变 worker 任务状态 | L2（失败注入） | 已验证 |
| V-告警-06 | 告警文本与审计摘要只含事件类型、时间、数量、允许的 trace_id 和错误类型，不含用户正文、姓名、工号、邮箱、凭据或外部链接。**trace_id 格式异常时降级为不带 trace_id，不丢弃整条告警**（#153，`delivery_alert_callback` 对非法 `task_id` 的处理） | L2（输出断言） | 已验证 |
| V-告警-07 | 告警路径只有纯文本通知；不执行重启、修复、权限变更、任务变更、用户重发，也不产生按钮、链接、回调入口 | L2（注入 sender + 负向断言） | 已认领 |
| V-告警-08 | 未配置管理群 chat_id（`LINGXI_ADMIN_GROUP_CHAT_ID`/`LINGXI_GATEWAY_ADMIN_GROUP_CHAT_ID`）时，三个进程仍能正常启动且状态机继续运行，只是发送出口退化为结构化日志（`_LogOnlyAlertSender`），不产生任何异常或崩溃——"告警不可用时主流程行为有明确定义"（#153 合同原文） | L2（构造装配 + 缺配置注入） | 已认领 |
| V-告警-09 | **开通失败（`LX-ONBOARD-001` / `internal_error` 终态、十五分钟同步超时 `sync_timeout` 终态）与开通中途停摆收口的收口计数经新增 `AlertKind.ONBOARDING_FAILED` 真实送达管理群**（Issue #280 §7.3，此前 `LINGXI_ADMIN_GROUP_CHAT_ID` 的全部消费方与开通失败无关，「已转交管理员处理」这句用户文案背后没有任何送达动作；`sync_timeout` 的覆盖由独立审查 codex P1-3 补上——它对应的用户文案「权限同步未完成，已转交处理」同样承诺了转交，此前只有 `internal_error` 真的送达）。群消息只含**故障类别**（内部原因码，经归一化，不是自由文本；`sync_timeout` 与 `internal_error` 各自的原因码可区分，如 `mcp_sync_timeout` 与 `directory_unavailable`）、**计数**与**追溯号**，不含 open_id、姓名或任何资料值——回调签名（`reason: str, trace_id: str` / `count: int`）里根本没有传这些的位置。回调由编排（`AutoOnboardingRunner._execute`，覆盖 `INTERNAL_ERROR` 与 `SYNC_TIMEOUT` 两个终态）与停摆收口职责（`StalledProvisioningDuty.run_once`）**可选注入**，未装配 `AlertingDuty` 时行为与此前一致（仅用户通知，无送达）；告警回调本身失败不得带走已经做完的用户通知或收口结果。**告警不改变 `V-开通-18` 的自动恢复语义**：`sync_timeout` 触发告警之后，`provisioning_state` 仍然停在 `mcp_syncing`，推进与恢复仍然只由迟到就绪恢复职责完成。成功终态**不得**产生告警 | L2（可注入，含正文不含资料值/成功终态不产生告警两类否定断言） | 已验证 |

本组不把本地注入假传输当作真实飞书证据；真实管理群消息、连续失败恢复的 L4a 留 E4 受控窗口，跨重启持久去重依赖 S9 审计载体。`worker` 进程不持有飞书出站密钥是架构设计自身职责划分（产品合同正文未规定各进程持有哪些凭据，2026-08-19 归属更正见 #238），告警出口恒为结构化日志，设计如此。

**已知后果（PR #173 P2-3）**：`V-告警-02` 五类事件类型全部只由 `WorkerService._housekeep()` 产生，没有一条能到达管理群，只能靠翻容器日志；目前只有 gateway（投递失败）与 scheduler（授权续期失败）的告警真的会发进管理群。

**已知未修复缺口（PR #173 P2-2，登记不解决）**：`V-告警-01`"任一条停摆都能被单独发现"只对活性文件机制成立，对状态机本身的 `PROCESS_INACTIVE` 检查不成立——三进程都是"先记心跳、再检查心跳"落在同一循环迭代里，循环一停就没人检查停摆本身。跨三进程的看门狗改动留后续 Story；活性文件（`V-部署-11`）不受影响，容器健康检查仍可靠。

**部分收窄（Issue #191，只对 gateway 生效）**：投递消费线程死亡现在跨循环可观测（`delivery_thread_watchdog` 触发 `delivery_loop_dead:*` 并冲刷队列），瞬时数据库错误不再是常见死因。**仍未覆盖**：线程活着但卡死时同循环失明依旧成立；worker/scheduler 不受本次改动影响。

**2026-08-26 对账（#100）翻转为已验证**（除注明外均 commit `b3a03f0`，MVP runtime alert visibility）：
- `V-告警-03` → `test_alerting.py::test_backoff_is_positive_and_strictly_increasing_before_ceiling` + `test_non_final_failures_need_three_failures_in_five_minutes` + `test_final_failure_is_immediate_and_fault_window_is_deduplicated` + `test_alerting_integration.py::DeliveryAlertCallbackTests`（kind 消毒/独立限流）
- `V-告警-04` → `test_alerting.py::test_recovery_requires_five_stable_minutes_and_is_exactly_once` + `test_dedupe_window_boundary_alerts_again_exactly_at_threshold`
- `V-告警-05` → `test_alerting_integration.py::test_alert_failure_does_not_skip_another_scheduler_duty` + `test_scheduler_heartbeat_failure_does_not_skip_duties` + `test_long_connection_heartbeat_observer_failure_does_not_stop_the_pump` + `test_worker_reports_three_stuck_categories_and_keeps_claiming_when_alert_fails`
- `V-告警-06` → `test_alerting.py::test_notice_text_has_only_safe_summary_fields` + `test_alerting_integration.py::test_an_unsafe_task_id_degrades_to_no_trace_id_instead_of_dropping_the_alert`
- `V-告警-09` → `test_onboarding_runner.py::OnboardingFailedAlertCallbackTests`（5 用例，含两类否定断言）+ `test_alerting_integration.py::OnboardingFailedAlertCallbackTests`/`OnboardingStalledAlertCallbackTests`；commit `90de658`（#251）

`V-告警-01` 声明含 L4a 未取得。`V-告警-02` 仅确认 `queued_stuck`/`running_heartbeat_timeout`/`retry_exhausted` 三类旧事件有测试，#153 新增的 `AWAITING_DELIVERY_STUCK`/`WORKER_VERSION_UNAVAILABLE` 两类未按名找到直接覆盖，证据不完整，保守不翻。`V-告警-07`（无按钮/链接/回调的否定断言）与 `V-告警-08`（缺 chat_id 时三进程仍启动、退化为结构化日志的装配级测试）本次未找到直接命中的测试，保守保持已认领。

### 内测每日通报

由 [#303](https://github.com/Moshuiwang/lingxi/issues/303) S-O-01 认领。职责实现在 `apps/scheduler/daily_report.py`（`DailyReportDuty`），聚合与渲染的纯函数在 `core/daily_report.py`，六段真库读取在 `adapters/postgres_daily_report.py`；与花名册审计日报共用同一个 `FeishuGroupMessages` 出站适配器与告警接线，各自持有独立的投递去重前缀（`DAILY_REPORT_UUID_PREFIX`）。

**MCP 调用次数对照（#296 基线）在当前架构下恒为「不可判定」**：只存在于 worker 进程自己的结构化日志（`worker.task.terminal` 的 `audit.call_count`），scheduler 与 worker 是独立部署进程、不共享文件系统或日志聚合通道，没有任何代码路径能读到，明确登记为超出本 Story 范围，正文如实呈现不可判定及原因。

**token 用量与工具调用拒绝计数已脱离「恒不可判定」（Issue #304 批次 4）**：迁移 `0070` 给 `task` 表新增 `token_usage`/`guard_denied_count` 两列，`apps/worker/service.py` 的终态收口点同事务落库；这两段现在是**真实聚合**（`adapters/postgres_daily_report.py` 的哑 `SUM`/`COUNT(*) FILTER` + `core/daily_report.py` 的纯函数判定），只在查询失败或窗口内该字段**全部**为 `NULL` 时才判「不可判定」，见 `V-通报-09`。

**判重水位持久化 + 重聚合挪出主线程（Issue #325）**：内测实测坐实两处残留——scheduler 重启后同一统计窗口重发通报（判重水位只在进程内存，`_completed_on` 跨重启归零）、午夜聚合耗时挤晚同一 tick 内 `AlertingDuty` 的心跳评估、触发假 `process_inactive` 告警。迁移 `0071` 新增 `daily_report_watermark` 表（`adapters/postgres_daily_report_watermark.py`），发送成功后写入，下一轮（含重启后）判重先查这张表；六段聚合、渲染、发送与收尾整体挪进后台线程（`DailyReportDuty._aggregate_and_send`），调用方所在的线程只等一个远小于心跳超时的短上限即可拿回控制权。见 `V-通报-10`/`V-通报-11`。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-通报-01 | 活跃用户与任务量分布、任务结果与失败分类 Top（含守卫触发计数）、时延分布、投递结果分布四段各自独立读取；任一段本轮读取失败只让**该段**显式标「不可判定」（含读取失败原因类型的审计留痕），其余段落照常渲染、不影响整轮发送 | L2（可注入，逐段独立故障） | 已验证 |
| V-通报-02 | **否定断言**：数据段取不到时不得渲染成零、省略该段或静默跳过；`Section.undetermined` 必须携带非空理由，空理由在构造期即拒绝。MCP 调用次数对照（#296 基线）在当前架构下恒为不可判定，正文逐字呈现理由；token 用量与工具调用拒绝计数两段自 #304 批次 4 起改为真实聚合，「全 NULL 仍判不可判定、部分 NULL 不静默计零」的纪律见 `V-通报-09` | L2（可注入 + 固定架构现状断言） | 已验证 |
| V-通报-03 | 同一 UTC 自然日内至多发送一条通报；判重水位只在发送**成功**之后才置位，发送失败的当轮不算已完成、下一轮重试 | L2（注入时钟） | 已验证 |
| V-通报-04 | 失败分类 Top 中同一原因码连续出现满 7 天仍完整展开，第 8 个连续自然日起折叠为「计数 + 连续在榜天数」、不再重复展开明细；某天未出现在 Top 榜即连续计数归零，重新出现按第 1 天计。节流状态只在**成功送达之后**才提交，送达失败的一轮不得推进或清零连续计数 | L2（纯函数 + 可注入职责层） | 已验证 |
| V-通报-05 | **否定断言**：正文只含统计数字与匿名分桶，不含用户对话原文、姓名、工号、邮箱，也不含任何形式（含缩短/脱敏形式）的可反查用户标识——「逐用户任务量」以匿名分桶呈现，SQL 层面不选出 `user_id`；正文不含链接、按钮或回调标记 | L2（固定敏感值扫描 + 源码 SQL 检查） | 已验证 |
| V-通报-06 | 正文标题同时标注 UTC 与北京时间（UTC+8）的统计窗口完整起止日期时间，两端各自带完整日期，不产生「同一时刻」的歧义写法 | L2（输出断言） | 已验证 |
| V-通报-07 | 发送失败记一条 `daily_report.send_failed` 结构化审计（只含异常类型）并通过与花名册日报共用的 `AlertingDuty.send_outcome_callback()` 告警接线上报，不静默；判重水位与节流状态均不提交，下一轮照常重试 | L2（可注入，含审计与告警回调断言） | 已验证 |
| V-通报-08 | 唯一前置管理群 chat_id（`LINGXI_ADMIN_GROUP_CHAT_ID`）缺失时职责不注册，留**恰一条**指名原因的审计（不回显任何值），其余定时职责（含总能注册的组织快照同步、迟到就绪恢复等）照常运行；chat_id 齐备时通过 `build_loop` 真实注册，装配阶段不发任何请求 | L2（装配注入 + 真实进程构造） | 已验证 |
| V-通报-09 | token 用量（`task.token_usage`）与工具调用拒绝计数（`task.guard_denied_count`，迁移 `0070`，Issue #304 批次 4）改为真实聚合：`apps/worker/service.py` 终态收口点与写终态事件同事务落库，任一取不到（早退分支、结构不符）留 `NULL`、不编造 0；`adapters/postgres_daily_report.py` 用 `COUNT(*) FILTER`/`SUM` 哑聚合出窗口内覆盖任务数、未覆盖任务数与总和/分字段和；`core/daily_report.py` 的 `build_denied_count_stats`/`build_token_usage_stats` 据此判定——窗口内零任务是确定的零（不可判定与"没有任务"是两回事）；窗口内有任务但该字段**全部** `NULL` 时整段仍判「不可判定」（`RESOURCE_USAGE_ALL_NULL_REASON`/`DENIED_COUNT_ALL_NULL_REASON`，与查询本身失败是不同原因）；部分任务缺字段时正文显式呈现覆盖度（"覆盖 N/M 个任务"），不静默把缺失的那部分算作零 | L2（真库聚合 + 纯函数否定测试，变异验红） | 已验证 |
| V-通报-10 | 判重水位持久化，跨进程重启不重发同一统计窗口的通报（Issue #325）：新表 `daily_report_watermark`（迁移 `0071`，`(report_date, chat_id)` 复合主键）；发送**成功**之后才写入（`ON CONFLICT DO NOTHING` 幂等，重复调用不产生第二行）；每轮发送前先查持久水位，查到即跳过、不重新聚合、不重新发送——内存水位（`_completed_on`）跨重启归零，持久水位不会 | L2（可注入 watermark 桩证明重启场景 + 真库幂等/唯一约束验证，变异验红） | 已验证 |
| V-通报-11 | 六段聚合、渲染、发送与收尾整体在后台线程执行，调用方（scheduler 主循环所在的线程，心跳记录与 `AlertingDuty` 的心跳评估都在其上）只等一个远小于 `AlertPolicy.heartbeat_timeout_seconds`（120 秒）的短上限即可拿回控制权；聚合耗时因此不再推迟同一 tick 内排在本职责之后的 `AlertingDuty` 心跳评估，避免午夜聚合触发假 `process_inactive` 告警（Issue #325） | L2（可注入慢聚合桩证明调用方不被阻塞，含派发去重否定断言） | 已验证 |
| V-通报-12 | 「本地权限覆盖活动」段（Issue #319 S-P-1c，Trace #328）：三态语义与 `metric_coverage_gap`（`V-通报-02` 旁）同一姿态——未接线（`local_override_activity=None`）正文完全不出现这一段；接线了但本窗口零活动且当前生效总数为零（`build_local_override_activity` 返回 `None`）同样「无差异不报」；接线了但取数失败渲染为不可判定；有活动或有生效登记（`Section.of(activity)` 非空）时才出现，正文只含六个整数计数（本窗口新增授权/抑制/收回笔数、当前生效授权/抑制条数、涉及用户数），**不含 open_id、公司 ID、指标名或理由文本**（`V-花名册-34` 隐私纪律同向）。**措辞如实化**（Trace #328 opus 审查；2026-08-29 再次如实化，PM 裁定，Issue #419）：正文用「本窗口」不用「今日」——本段与其余六段统计的是同一个 UTC 窗口，而这个窗口实际是「昨天」（`V-通报-01` 旁模块文档「投递结果段为什么用一个独立、更早的窗口」一节旁的既有约定），说成「今日」不准确；正文用「生效」不用「登记」——`V-权限-15` 曾登记的已知限制已消除，零银河权限用户的本地授权现在无条件参与合并，措辞回改「生效」 | L2（纯函数：`test_daily_report_render.py::LocalOverrideActivityBuildTests`——计数正确/当日零活动仍判无差异不报的边界用例；`LocalOverrideActivityRenderTests`——渲染三态、计数正确、正文零身份含公司/指标/原因扫描；可注入接线：`test_daily_report_duty.py::LocalOverrideActivityWiringTests`） | 已验证 |

**连续 ≥5 个自然日真实送达管理群属 L4a，留 biai-stage 观察期**（Trace #304 Epic O 终点条件），不在本 Story L2 声明范围；`docs/当前能力.md` 在 L4a 取得前不得写成「当前能力」。`V-告警-09` 补充：本组新增的否定断言（确定性业务失败——`NOT_AUTHORIZED`，即无可用银河权限一类——不触发管理员送达回调，与成功终态不告警是两条独立取证，`tests/test_onboarding_runner.py::OnboardingFailedAlertCallbackTests`）与同步超时是否告警的判据说明（`sync_timeout` 因用户文案同样承诺「已转交处理」而与 `internal_error` 同等送达，理由见 `core/identity/onboarding_runner.py` 模块文档）由本 Story（#303 S-O-01 / #251 开通告警）一并核对，`V-告警-09` 状态与既有认领方不变。

**2026-08-26 对账（#100）翻转为已验证**：
- `V-通报-01` → `test_daily_report_duty.py::SectionIndependentFailureTests`；commit `db438fa`（#303 S-O-01）
- `V-通报-02` → `test_daily_report_render.py::RenderUndeterminedSegmentTests`；commit `e974765`
- `V-通报-03` → `test_daily_report_duty.py::OncePerDayTests`；commit `db438fa`
- `V-通报-04` → `test_daily_report_duty.py::RepeatThrottleTests` + `test_daily_report_render.py::RepeatThrottleTests`/`ThrottleAcrossDaysTests`；commit `e974765`/`db438fa`
- `V-通报-05` → `test_daily_report_render.py::RenderContentSafetyTests`；commit `e974765`
- `V-通报-06` → `test_daily_report_render.py::WindowHeaderTests`；commit `e974765`
- `V-通报-07` → `test_daily_report_duty.py::SendFailureTests` + `SendFailureReasonClassificationTests`；commit `db438fa`
- `V-通报-08` → `test_scheduler_daily_report_assembly.py::BuilderFunctionTests::test_missing_chat_id_refuses_registration_with_exactly_one_audit_record` + `FullAssemblyTests::test_without_chat_id_the_process_still_assembles_its_other_duties`；commit `db438fa`
- `V-通报-09` → `test_postgres_daily_report.py::TokenUsageStatsTests`/`GuardDeniedCountStatsTests` + `test_daily_report_render.py::DeniedCountAndResourceUsageBuildTests`/`MetricCoverageGapBuildTests`/`MetricCoverageGapRenderTests`；commit `0cdc694`（Trace #304 批次 4）
- `V-通报-10`（本周新增，已重点核实）→ `test_daily_report_duty.py::PersistedWatermarkTests`（类文档字符串明确自述"V-通报-10（Issue #325）"）+ `test_postgres_daily_report_watermark.py`（真库唯一约束/幂等）；commit `e1d8ae3`（#325）
- `V-通报-11`（本周新增，已重点核实）→ `test_daily_report_duty.py::AggregationDoesNotBlockCallerTests`（类文档字符串明确自述"V-通报-11（Issue #325）"，含慢聚合桩证明不阻塞调用方 + 派发去重否定断言）；commit `e1d8ae3`（#325）
- `V-通报-12`（本批次新增，已重点核实）→ `test_daily_report_render.py::LocalOverrideActivityBuildTests`/`LocalOverrideActivityRenderTests` + `test_daily_report_duty.py::LocalOverrideActivityWiringTests`（Trace #328 E-P opus 审查修复包）

### 内测轮内容级采集

由 [#251](https://github.com/Moshuiwang/lingxi/issues/251)（内测轮开工前置三项·第二项）/ [#304](https://github.com/Moshuiwang/lingxi/issues/304)（批次 3）认领。核心逻辑在 `core/innertest_content_capture.py`（凭据形状过滤、原始素材收集与记录构造，复用 `core/execution/audit.py` 的 `redact_free_text_with_count`）、开关判定在 `apps/worker/config.py` 的 `_innertest_content_capture`、写入点在 `apps/worker/service.py` 的 `_capture_content_if_enabled`、落库适配器在 `adapters/postgres_content_capture.py`、表结构与保留在迁移 `0069_innertest_content_capture.py`。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-采集-01 | 默认（`LINGXI_INNERTEST_CONTENT_CAPTURE` 未配置）时 `WorkerConfig.innertest_content_capture_enabled` 为 `False`，`WorkerTurnExecutor` 不构造 `RawTurnCapture`、`WorkerService` 不调用任何写入方，真库场景下 `innertest_content_capture` 表零新增行 | L2（纯构造期断言+装配级假队列，变异验红） | 已验证 |
| V-采集-02 | **变异验红**：把 `_innertest_content_capture` 的默认返回值改为 `(True, False)` 时，V-采集-01 的零写入断言必须变红——用一次真实改坏代码的实测证明该断言真的在守 | L2（纯构造期断言+装配级假队列，故意改坏后复原） | 已验证 |
| V-采集-03 | 主开关精确为 `"1"` 且第二确认变量精确匹配 `CONTENT_CAPTURE_ENVIRONMENT_CONFIRM_VALUE` 时开关生效；主开关配置但取值不是精确 `"1"` 时启动即失败（`WorkerConfigError`）；主开关为 `"1"` 但确认变量缺失/不匹配时开关**不生效**且 `innertest_content_capture_misconfigured=True`（不影响该任务正常处理问数） | L2（构造期单测，四种取值组合） | 已验证 |
| V-采集-04 | 开关生效时一次真实回合结束后，`innertest_content_capture` 表新增恰一行，`question_content`/`answer_content` 与真实问题/模型回答一致（凭据形状过滤前提下），`tool_calls` 数组条数与该回合工具调用数一致、每条含 `tool_name`/`tool_input`/`result_summary` | L2（两段拼接：`tests/test_worker_entry.py` 的 `ContentCaptureEndToEndWiringTest` 用假 SDK 驱动真实 `WorkerTurnExecutor` 装配路径证明"真实回合→正确记录"；`tests/test_postgres_content_capture.py` 的 `WriteAndReadBackTests` 用真库证明"记录→落库行"；未合并成单一端到端用例） | 已验证 |
| V-采集-05 | 凭据形状（`token=`/`Bearer `一类赋值或认证头、含数字的 16+ 字符或 32+ 字符裸令牌串）出现在问题、回答或工具入参/回执任一处时，落库内容里对应片段被替换为占位符，且 `*_redaction_count` 对应列 > 0；不含凭据形状的普通内容 `*_redaction_count` 恒为 0 | L2（构造期单测 + 真库落库核对） | 已验证 |
| V-采集-06 | **否定断言**：`PostgresContentCaptureWriter.write` 注入抛出异常时，该次任务的终态仍按真实回合结果正常收口（成功/失败/超时不受影响），只产生一条结构化错误日志，任务表与投递事件不受采集失败牵连 | L2（可注入写入方抛错） | 已验证 |
| V-采集-07 | 正式环境结构性不生效：`deploy/compose.yaml`/`compose.stage.yaml`/`compose.prod.yaml` 三个入库编排文件（去注释后）均不包含 `LINGXI_INNERTEST_CONTENT_CAPTURE`、`LINGXI_INNERTEST_CONTENT_CAPTURE_ENVIRONMENT_CONFIRM` 两个变量名或第二确认变量要求的精确字面量；`deploy/.env.example` 的 worker-queue 小节示范该开关且含「生产环境禁止配置」警示；`deploy/验收前部署配置清单.md` 登记该变量。**变异验红**：故意在 `deploy/compose.prod.yaml` 写入该字面量，`check_content_capture_prod_guard` 必须变红 | L2（`scripts/ci/check_deploy_contract.py`，故意改坏后复原） | 已验证 |
| V-采集-08 | 迁移 `0069` 的 `expires_at` 触发器：写入时忽略调用方传入值、固定为 `created_at + 2160` 小时；`UPDATE` 不能后移 `expires_at`、不能修改 `created_at`/`task_id` | L2（PostgreSQL 16 真库） | 已验证 |
| V-采集-09 | 轮次结束清理：迁移文件头登记的 `DELETE ... WHERE created_at >= ... AND created_at < ...` 与 `TRUNCATE innertest_content_capture` 两条受控 SQL 在真库上可执行；执行后按 `task_id` 回读（`read_recent_for_task`）为空，其余表（`task`、`task_delivery_event`）不受影响 | L2（真库） | 已验证 |

**结构性保证的证据边界（如实登记，不得声称已彻底堵住）**：`V-采集-07` 只证明"入库的编排文件里没有"，证明不了"运维不会把 stage 的未入库本地 env 文件整份复制进生产对应文件"——那是当前 stage/生产共用完全相同镜像与编排结构下，任何静态仓库检查都做不到的事，最后一道防线是部署操作纪律（`deploy/验收前部署配置清单.md`）。凭据形状过滤（`V-采集-05`）与 `V-审计-03` 共享同一条已知残余缺口：纯字母且短于 32 字符的裸秘密不会被过滤，`*_redaction_count` 是"命中并替换了几处"的可观测计数，不是"零命中即无凭据"的证明。

**2026-08-26 对账（#100）翻转为已验证**（全部 commit `cc55ff3`，提交信息逐字写明"V-采集-01…09"）：
- `V-采集-01` → `test_worker_queue_consumer.py::ContentCaptureWiringTests::test_default_disabled_never_touches_the_executor_capture_hook`
- `V-采集-02` → `test_worker_content_capture_config.py::InnertestContentCaptureFunctionTests::test_unset_is_disabled_and_not_misconfigured`（直接断言默认返回值元组，等价于该变异会使其变红）
- `V-采集-03` → `test_worker_content_capture_config.py::InnertestContentCaptureFunctionTests`（四态用例）
- `V-采集-05` → `test_innertest_content_capture.py::RawTurnCaptureBuildRecordTests::test_credential_shapes_in_tool_input_and_result_are_redacted_and_counted` + `test_clean_content_has_zero_redaction_count`
- `V-采集-06` → `test_worker_queue_consumer.py::ContentCaptureWiringTests::test_capture_failure_does_not_affect_the_task_terminal_outcome`
- `V-采集-07` → `test_deploy_checks.py::ContentCaptureProdGuardTest`（含改坏复原用例 `test_the_confirm_literal_leaking_into_compose_prod_is_caught`）
- `V-采集-08` → `test_postgres_content_capture.py::ExpiryTriggerTests`
- `V-采集-09` → `test_postgres_content_capture.py::RoundEndCleanupTests`

### 飞书接入与事件幂等

`V-接入-01`、`V-接入-02`、`V-接入-04`…`V-接入-13` 由 [#57](https://github.com/Moshuiwang/lingxi/issues/57)（S4 前半）认领；`V-接入-03` 的受限清理由 [#54](https://github.com/Moshuiwang/lingxi/issues/54) 交付，#57 只建表并落 `expires_at`。

长连接语义下**没有逐事件验签**：`ws.Client` 认证发生在握手期、是通道级的。承接同一产品意图的是 `V-接入-10`——进程不监听任何入站端口，不存在"签名对了就受理"的旁路。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-接入-01 | 同一 `event_id` 顺序投递两次，只产生一个处理结果和至多一个任务 | L2（真库） | 已验证 |
| V-接入-02 | 同一 `event_id` 并发投递两次，恰好一个插入成功，且只产生一个任务 | L2（真库并发） | 已验证 |
| V-接入-03 | 业务角色不能直接删除入站事件；受限清理函数拒绝删除九十天内的事件、拒绝越界批量，并只删除截止时间前的指定批量 | L2（真库） | 未认领 |
| V-接入-04 | 长连接建立后，投入一条 `im.message.receive_v1` 事件，恰好触发一次对应处理器，且事件体的发送者标识、`event_id`、话题标识被完整传入 | L2（可注入） | 已验证 |
| V-接入-05 | 非终止型断连（传输层中断、非 403 非 514 的握手失败）后自动重连并恢复收取事件；重连间隔单调退避且首次重连不早于配置下限——固定间隔或零间隔的忙循环必须使该用例变红 | L2（可注入） | 已验证 |
| V-接入-06 | 握手返回 **403**，或 **514 且 `autherrcode=1000040350`**（超连接数上限）时**不发起任何重连**：注入该错误后重连次数恒为 0，进程进入明确的终止态并记审计。判定由我们自己可注入的一层作出，不依赖 SDK 内部分支 | L2（可注入） | 已验证 |
| V-接入-07 | 五类消息**各触发恰好一次**加表情：正常问数、未开通用户消息、话题忙碌期消息、`/stop`、`/new`；且加表情发生在 `inbound_event` 去重**之后**、用户状态判定**之前** | L2（可注入 + 真库） | 已验证 |
| V-接入-08 | 加表情调用抛错或返回失败时，后续处理照常完成（正常消息仍入队、忙碌期仍回提示、未开通用户路径照常走完），失败只记审计 | L2（可注入，注入失败） | 已验证 |
| V-接入-09 | 重复投递在**用户可见面**同样不重复：第二次不再加表情、不再发送任何回复。断言对象是出站调用次数，不只是数据库行数 | L2（可注入 + 真库） | 已验证 |
| V-接入-10 | gateway 进程**不监听任何入站端口**，事件只能经已认证的长连接通道进入；构造一个不经该通道的投递路径时无任何处理器被触发 | L2（可注入 + 真实进程套接字表断言） | 已验证 |
| V-接入-11 | 任务归属的 `user_id` **只**来自事件发送者标识的解析结果；事件体其他字段或消息正文中声明的用户标识被忽略，不能把任务挂到他人名下 | L2（可注入 + 真库） | 已验证 |
| V-接入-12 | 单个事件的处理器抛异常（含收到未订阅、无处理器的事件类型）时：长连接不断开、后续事件照常处理、该事件**不被标记为已成功处理** | L2（可注入） | 已验证 |
| V-接入-13 | 真实飞书上对一条真实私聊消息完成一次加表情，飞书侧可见 | L4a | 已验证 |
| V-接入-14 | 群聊中 @ 机器人自身（精确命中，@别人或无 @ 均不算）恰好触发一条固定的引导私聊文案，同一个群一小时内最多发一条；未配置机器人 `open_id` 时功能整体关闭；出站发送失败（含未预期异常）时整体判定失败关闭：事件仍正常 ack、留一条只含异常类名的审计、且这次失败不消耗节流额度，下一次同一个群的 @ 仍可正常收到引导；不同 `chat_id` 的节流互不影响（Issue #318，#328 v1.0 裁定 #5；Issue #328 opus 审查 P1-1 修复真库实测发现的"发送失败即永久静默"缺口） | L2（可注入，含失败注入与跨群隔离两类否定断言） | 已验证 |

**2026-08-16 受控真实验收（Epic A/S-A-07，[#154](https://github.com/Moshuiwang/lingxi/issues/154)）：** 候选为 PR [#174](https://github.com/Moshuiwang/lingxi/pull/174) 冻结镜像，在 `biai-stage`+Bot-Test 隔离验收库上运行，测试身份为已开通用户（非经真实开通链路）。

- `V-接入-01` → **已验证**（L4a）：受控脚本对同一入站事件体顺序重放两次，第 2 轮被判重，隔离库恰 1 个任务、1 条终态。边界：是脚本重放，非飞书平台真实重投。
- `V-接入-09` → **已验证**：飞书只读回读确认第二次投递既未再加表情也未再发回复。
- `V-接入-13` → **已验证**（L4a）：两条真实消息经只读探针回读 `app_reactions=1`；空闲 `/new` 探针与现场目视不一致，留 [#175](https://github.com/Moshuiwang/lingxi/issues/175) 追查，不影响本条通过依据。

**未翻转的三条**：`V-接入-02` 并发重复投递本次未做真实样本；`V-接入-07` 只对部分消息类型取得表情事实，`/stop` 与未开通用户消息未单独取证；`V-接入-08` 未构造加表情失败样本。

**2026-08-26 对账（#100）翻转为已验证**（全部 commit `0aa3a63`，#57）：
- `V-接入-02` → `test_gateway_postgres.py::EventIdempotencyTests::test_concurrent_duplicate_inserts_exactly_one`
- `V-接入-04` → `test_gateway_longconn.py::EventRoutingTests`
- `V-接入-05` → `test_gateway_longconn.py::ReconnectTests`
- `V-接入-06` → `test_gateway_longconn.py::TerminalFailureTests`
- `V-接入-07` → `test_gateway_pipeline.py::ReactionOrderTests`（5 种消息各恰一次 + dedup 前后顺序）
- `V-接入-08` → `test_gateway_pipeline.py::ReactionFailureTests`
- `V-接入-10` → `test_gateway_longconn.py::NoInboundPortTests`
- `V-接入-11` → `test_gateway_postgres.py::TaskOwnershipTests::test_task_user_id_comes_from_the_sender_not_the_body`
- `V-接入-12` → `test_gateway_longconn.py::HandlerFailureTests`

### 任务队列与入队

`V-队列-01`…`05` 由 [#57](https://github.com/Moshuiwang/lingxi/issues/57)（S4 前半）认领；`V-队列-06` 属 S4 下半，本批无消费者、无从验证；`V-队列-07` 记录已确认的体验，持久通知幂等机制未认领。

**单独成组的理由**：`V-会话-` 组约束的是上下文与串行语义，失败形态是"串味 / 并发执行"；本组约束的是事务边界与队列可用性，失败形态是"消息静默丢失 / 话题永久忙碌 / 任务永久滞留"。两者混编会让"哪些断言属于本切片"不可判定。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-队列-01 | `inbound_event` 插入、`conversation` 抢占、`task` 插入在**同一事务**内：注入 `task` 插入失败后 `inbound_event` 无该行，飞书重投时该消息能被重新完整处理。事件先提交、任务后失败的实现必须使本条变红 | L2（真库，注入写失败） | 已验证 |
| V-队列-02 | 抢占成功但入队失败时，`conversation.running_task_id` 回到 `NULL`：注入失败后该话题的下一条消息能正常入队，**不会永久停在"当前任务仍在处理中"** | L2（真库，注入写失败） | 已验证 |
| V-队列-03 | 入队未成功时，用户**不收到任何表示已受理或已开始处理的回复**（已加的表情按合同只表示"已收到"），且事件不被标记为已成功处理 | L2（可注入 + 真库） | 已验证 |
| V-队列-04 | 两个 worker 并发领取同一批 `queued` 任务时，每个任务**恰好被一个 worker 领到**，无重复领取、无饿死；被领任务 `status` 变 `running` 且 `worker_id` 唯一 | L2（真库并发） | 已验证 |
| V-队列-05 | 入队提交后发出 `NOTIFY task_queued`；事务回滚时**不**发出该通知 | L2（真库） | 已验证 |
| V-队列-06 | 监听方收到 `NOTIFY task_queued` 即领取；**丢弃全部 NOTIFY** 时，兜底轮询仍在**配置的上限内**领到该任务（任务不永久滞留） | L2（真库） | 未认领 |
| V-队列-07 | 注入任务插入失败时，首次投递发送一次 `LX-QUEUE-001` 且不标记成功；同一事件顺序重投、并发重投和进程重启后均不重复发送该提示。撤销故障后，该事件仍可重新处理并恰好入队一次 | L2（可注入 + 真库并发 + 进程重启） | 未认领 |

**本组当前覆盖不到的两件事（#57 独立复查）：**

- **成功入队但永远没有 worker 领取的话题没有回收路径**：`reclaim_stale` 认不到 `queued` 状态，`/stop` 不释放占用；gateway 因此不能单独上线，S11 前须先有 `queued` 超时治理。
- **长连接中途静默死亡的检测未在真实环境验证**：靠轮询 SDK 私有属性 `_conn` 发现掉线，属性改名会降级为无检测；真实断线重连属 L4a。

**2026-08-26 对账（#100）翻转为已验证**（全部 commit `0aa3a63`，#57）：
- `V-队列-01` → `test_gateway_postgres.py::TransactionBoundaryTests::test_task_insert_failure_leaves_no_inbound_event_row` + `test_reprocessing_after_failure_succeeds`
- `V-队列-02` → `TransactionBoundaryTests::test_claim_is_rolled_back_so_the_topic_is_not_stuck_busy`
- `V-队列-03` → `TransactionBoundaryTests::test_failed_enqueue_sends_no_acceptance_reply` + `test_task_insert_failure_leaves_no_inbound_event_row`（覆盖断言后半句"事件不被标记为已成功处理"，#328 opus 批量审查 P3 补指针）
- `V-队列-04` → `QueueClaimTests::test_two_workers_never_claim_the_same_task`
- `V-队列-05` → `QueueClaimTests::test_notify_is_emitted_on_commit` + `test_notify_is_not_emitted_when_the_transaction_rolls_back`

### worker 版本灰度

由 [#57](https://github.com/Moshuiwang/lingxi/issues/57) 认领 [#45 决策](https://github.com/Moshuiwang/lingxi/issues/45) 提前到 S4 的**两项预留**。其余灰度能力（分流规则形态、双 worker 并存、排空式回滚、会话级版本固化）是 S11 的设计输入，本组从 `V-灰度-03` 起续号，届时不必重排。

**本批不得声称"同一会话固定同一版本"已达成**：那是会话级固化，属 S11。本批只到"单任务版本不可改写"与"按版本领取"。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-灰度-01 | 目标 worker 版本在 gateway **入队时**按用户发起请求的时间求值一次并写入任务记录；此后任务重试（`attempts` 增加）、进程重启、scheduler 从心跳超时回收重置为 `queued`，该字段值**均不改变**。任何在领取或重试路径写该字段的实现必须使本条变红 | L2（真库，含重试与回收路径） | 已验证 |
| V-灰度-02 | 领取 SQL 带版本条件：声明版本为 `stable` 的 worker 领不到目标版本为 `canary` 的任务（反之亦然）；两版本 worker 并发领取时各自只拿到匹配自身版本的任务，且不匹配的任务保持 `queued` 不被饿死或误改状态 | L2（真库并发，双版本） | 已验证 |

**2026-08-26 对账（#100）翻转为已验证**（commit `0aa3a63`，#57）：`V-灰度-01` → `test_gateway_postgres.py::WorkerVersionTests::test_version_is_written_at_enqueue` + `test_version_survives_retry_and_reclaim` + `test_database_refuses_to_rewrite_the_version` + `test_no_claim_path_writes_the_version_column_at_all`；`V-灰度-02` → `WorkerVersionTests::test_claim_is_filtered_by_version` + `test_two_versions_claim_concurrently_without_crossing` + `test_unmatched_tasks_stay_queued_and_untouched`。

### 管理与幂等

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-管理-01 | 同一待确认操作重复确认，第二次起影响 0 行，返回既有结果 | L2（真库） | 未认领 |
| V-管理-02 | 非发起人确认，影响 0 行 | L2（真库） | 未认领 |
| V-管理-03 | 过期后确认，影响 0 行 | L2（真库） | 未认领 |
| V-管理-04 | 目标权限版本变化后确认，影响 0 行 | L2（真库） | 未认领 |
| V-管理-05 | 审计写入失败时事务整体回滚，用户状态与 `pending_action` 状态均不变 | L2（真库，注入审计写失败） | 未认领 |
| V-管理-06 | 卡片发送失败的操作不可被确认 | L2 + L4a | 未认领 |
| V-管理-07 | 系统无超级管理员时，Root 运维可在受控流程中把身份已核实并绑定飞书的现有用户恢复为超级管理员，且普通管理入口不获得 Root 能力 | L4a（受控恢复演练） | 未认领 |
| V-管理-08 | 权限管理员的 `list_tools` 结果**不含**运维与超管工具 | L2（工具注册逻辑单测） | 未认领 |
| V-管理-09 | 真实 AI Agent 客户端连接管理 MCP 时身份可追溯、角色正确 | L4a | 未认领 |
| V-管理-10 | 管理 MCP 的工具列表不含任意 SQL、任意字段修改或系统命令 | L2（工具注册逻辑单测） | 未认领 |
| V-管理-11 | 受控管理群中的消息或卡片不能执行任何管理写操作 | L2（入口路由测试）+ L4a | 未认领 |
| V-管理-12 | 管理 MCP 认证主体、`pending_action.initiated_by`、确认卡接收人和点击人均解析到同一 `admin_identity`；任一映射缺失、重复或变化时不查询、不建动作、不执行 | L2（真库）+ L4a | 未认领 |
| V-管理-13 | 确认从当前运行环境删除后立即禁止该用户的新问数与交付；当前环境中的身份、权限、会话、任务内容和其他可识别运行数据全部清理前，状态不得变为 `deleted` 或返回成功；编排不修改灾备副本 | L2（真库 + 文件系统） | 未认领 |
| V-管理-14 | 删除编排任一步失败后可从 `last_completed_step` 恢复，不重复交付、不恢复权限，也不在当前运行环境遗留可识别的“已删除”用户数据 | L2（逐步注入失败） | 未认领 |
| V-管理-15 | 同一 `prepare_*` 重发（模拟 `2026-07-28` 响应流断开后客户端以新请求 ID 重试）只产生一项待确认操作、只发送一张确认卡。**两种重发都必须覆盖**：携带相同 `idempotency_key`，以及携带**不同** `idempotency_key` 但自然键（`admin_identity` + 动作类型 + 目标对象 + 目标状态版本）相同 | L2（真库并发/重发） | 未认领 |
| V-管理-16 | 管理 MCP 的 `tools/list` 结果 `cacheScope` 为 `private` 且 `ttlMs ≤ 60000`；角色被收回后，服务端下一次 `tools/list` 不再含该角色工具，且该角色的工具调用即时返回 `not_authorized`（不依赖客户端何时重取列表） | L2（工具注册逻辑单测 + 真库） | 未认领 |
| V-管理-17 | 管理 MCP 不下发任何 `elicitation/create`（任何工具、任何分支）；写操作在未经飞书卡片确认时一律不改变状态 | L2（协议层断言 + 真库） | 未认领 |
| V-管理-18 | `server/discover` 返回受支持的协议版本与能力，且 `extensions` 为空（首期不启用任何可选扩展） | L2（协议层断言） | 未认领 |
| V-管理-19 | 目标员工的银河权限查无、记录不完整、没有受支持职能或其他银河权限异常时，系统只给出先去银河申请或补充权限、同步后再回 Lingxi 的提示；不修改或扩大银河权限、不代员工申请、不创建 Lingxi 权限人工待办；银河已有权限但 Lingxi 仍异常时提示联系银河管理员 | L2（纯逻辑 + 真库）+ L4a（真实用户路径） | 未认领 |
| V-管理-20 | 当前管理员自己的身份、角色、管理 MCP 身份、本人飞书绑定或确认链路异常时，当前写操作和已有待确认动作均不执行；系统生成异常编号并通知受控管理群、提示联系 Ops；Ops 修复后必须重新授权并重新发起，旧动作不恢复，管理员不能自助修复或绕过确认 | L2（状态与路由）+ L4a（受控异常演练） | 未认领 |
| V-管理-21 | 服务端管理员角色登记表（`admin_registry`，迁移 `0067`）按 `feishu_open_id` 判定当前是否为有效管理员：命中一条 `entry_status='active'` 的行即视为管理员；未登记、条目已撤销、条目 `active` 但零角色三种情形一律默认拒绝，且判定结果彼此不可区分（不给探测者可利用的信号） | L2（真库） | 已验证 |
| V-管理-22 | 判定实时读表、不缓存：一个此前有效的条目被撤销（`entry_status='revoked'`）后，下一次判定立即读到拒绝结果，不依赖任何 TTL 或应用层缓存失效路径 | L2（真库） | 已验证 |
| V-管理-23 | 同一飞书身份同一时刻只允许一条 `entry_status='active'` 登记，由部分唯一索引强制（不是应用层"恰好没有并发"）；已撤销的历史行不阻挡同一身份重新登记为 `active` | L2（真库否定） | 已验证 |
| V-管理-24 | 私聊分流：登记表当前有效条目（MVP 唯一初始条目=组织资料同步的专用授权主体账号，未来任何登记表内有效管理员同理）的 Bot-Test 私聊**文本**消息，从既有「确定性拒绝出口」（`onboarding.delegated_subject`）改道进入管理命令面，不进入自动开通、不产生 `inbound_event.auto_provisioning`；登记表内无有效条目的发送者维持既有行为——普通用户仍走自动开通业务路径，专用账号条目被撤销后仍回到确定性拒绝出口（opus P3-1 修复后：专用主体判定移到按用户状态分派之前，即便因数据漂移意外获得 `app_user` 行也仍命中，不再要求 `state=NOT_PROVISIONED`；判定口未装配 `delegated_subject_open_id` 时对该主体行为与本项加入之前逐字节一致，`admin_router` 单独未装配时回落既有确定性拒绝出口）；完全不装配管理路由时，登记表内其余（非专用主体）条目的行为与本项加入之前逐字节一致 | L2（可注入） | 已验证 |
| V-管理-25 | 分流范围精确限定于私聊文本消息：非文本消息即便发送者已登记也不触达管理路由；已开通的**普通**业务用户结构上完全不触达管理路由——分流点原本只在 `NOT_PROVISIONED` 分支内，opus P3-1 修复后新增专用主体判定这一个例外（比对配置中已解析好的单个 open_id，与"该 open_id 是否已开通"无关，见 V-管理-24），除此之外业务用户开通与问数行为不因管理命令面的存在或装配而改变 | L2（可注入） | 已验证 |
| V-管理-26 | 只读查询命令组提供三条 MVP 命令（查询用户状态、追溯/审计查询「按用户/按时间窗列最近关键事件」的最小可用版、帮助）；命令语法封闭——任何不匹配四种已知形状的输入（含 SQL 注入、命令注入、任意子命令等形态）一律得到 `UNKNOWN`，不产生任何查询条件、不拼接任何字符串进 SQL、不执行系统命令；命令面本身不提供任意数据库查询或系统命令入口 | L2（纯逻辑，含伪造查询端口证明零调用） | 已验证 |
| V-管理-27 | 每次管理命令面交互都产生一条审计记录：**已确认为当前有效管理员**之后的交互（帮助、查询、内部错误）字段含调用人身份（产品合同「管理员处理入口与安全确认」第 130 行明确要求"每次管理查询和操作都记录管理员身份"）、当时角色、命令类型、目标对象与结果；**登记表判定未通过**的交互（未登记/已撤销/角色不全、判定本身异常）不记录发送者身份，只含 event/trace_id，与 `V-花名册-34` 纪律一致——需要还原是谁时凭 `trace_id` 回链 `inbound_event`；判定或查询失败均失败关闭（不崩溃、不放行），已确认为管理员之后的失败仍给出明确回复；审计器自身抛异常时，交互统一退化为确定性拒绝，不误放行、不崩溃（opus P2 修复） | L2（可注入） | 已验证 |
| V-管理-28 | 登记表初始条目由一次性种子命令（`python -m lingxi.apps.admin_bootstrap`）幂等播种：读取 `feishu_delegated_subject`（Issue #137 同一识别机制）取得专用授权主体标识，三类角色合并授予（种子函数结构上不接受角色子集入参，opus P2 修复）；已有不一致的 active 行时（标签或角色与本次意图播种的内容不一致）响亮拒绝、非零退出，不误报幂等成功；重复执行且内容一致不产生第二条 `active` 记录；不带 `--confirm` 时只读预演、不写库；仓库源码不含任何真实 `open_id`——由 CLI 输出的否定断言（标准输出/错误流不含取到的 open_id）与人工核查源码（`admin_bootstrap` 唯一的标识来源是运行期读取 `feishu_delegated_subject` 登记表，不含任何硬编码字面量）共同支撑，不是自动化源码扫描 | L2（真库）+ CLI 输出否定断言 + 人工核查 | 已验证 |
| V-管理-29 | `prepare_action` 只在目标当前状态允许该动作时才创建待确认操作，不直接改变业务状态：目标不存在，或状态不允许该动作（如对已停用用户再次发起停用、对已启用用户发起恢复）时不建任何行 | L2（纯逻辑 + 真库） | 已验证 |
| V-管理-30 | `confirm_action` 完整核对链：未找到/从未真正送达（`card_delivered=false` 与"未找到"不可区分）、早已是终态（幂等返回既有结果，不重复处理）、已过期（首次发现即转终态 `expired`）、点击人不是发起人（零业务变更，不改变 `pending_action` 任何字段，真正的发起人随后仍可能点对）、发起人当前角色已被收回（转终态 `failed`，要求重新查询发起）、目标状态相对 `prepare` 时刻的快照已经漂移（转终态 `failed`）——任一核对不通过，目标账号状态与 `pending_action` 状态均不改变 | L2（纯逻辑 + 真库） | 已验证 |
| V-管理-31 | 同一待确认操作重复确认（含真实并发的两个独立数据库连接几乎同时确认）只成功执行一次，`app_user.account_state` 恰好翻转一次；由 `SELECT ... FOR UPDATE` 的行锁序列化保证，不依赖应用层分布式锁 | L2（真库并发，两条独立连接） | 已验证 |
| V-管理-32 | 审计写入失败时，`confirm`/`cancel` 所在的数据库事务整体回滚：`pending_action` 与目标 `app_user.account_state` 均保持事务开始前的状态，不提前终态化（接口设计错误码 `audit_write_failed`，"调用方可重试"）；换回正常审计器后重试确实能够成功 | L2（真库，注入审计写失败） | 已验证 |
| V-管理-33 | `suspend_user` 确认执行后立即挡住该用户后续问数与开通：`account_state` 翻转为 `suspended`，既有 `postgres_conversation._transaction._user_state` 与开通中途状态复核路径原样消费该字段（未改动）；`resume_user` 对称把 `account_state` 翻回 `enabled`。**2026-08-25 追加事实（不改变本条 L2 认领状态）**：产品负责人在 `biai-stage`+`Bot-Test` 真实执行 `/admin suspend`/`/admin resume` 全流程并观察到账号状态真实翻转，构成 L4a 层面的独立真实印证，但本条断言本身仍以真库注入证据认领 | L2（真库） | 已验证 |
| V-管理-34 | 确认卡片只作为触发该写命令消息的回复发出，结构上只送达发起管理员本人私聊；发送失败或结果不明时待确认操作视为作废（`card_delivered` 保持 `false`），确认/取消均安全拒绝，不根据失败原因推断人员状态 | L2（可注入假传输层） | 已验证 |
| V-管理-35 | 卡片回调伪造（不存在的待确认操作 ID、篡改的按钮回传值）安全拒绝，不崩溃、不产生任何状态变化；回调事件体解析只信任飞书自己标注的 `operator.open_id`，不信任回传值内任何自称的身份字段 | L2（纯逻辑 + 真库 + 事件解析否定用例） | 已验证 |
| V-管理-36 | 管理群只在动作终态（已确认执行/取消/过期/失败）**首次**发生时收到一条脱敏通知：不含目标或发起人的 open_id 明文，不提供任何按钮、命令提示或执行入口；幂等重放（重复点击已是终态的卡片）不重复通知。**2026-08-25 追加事实（不改变本条 L2 认领状态）**：产品负责人真实点击确认卡片，受控管理群真实收到脱敏通知，重复点击已终态卡片未重复通知，构成 L4a 层面的独立真实印证，但本条断言本身仍以可注入假传输层证据认领 | L2（可注入假传输层） | 已验证 |
| V-管理-37 | `/admin suspend`/`/admin resume` 与既有只读命令共用同一套登记表默认拒绝判定：未登记者或普通业务用户发送这两条命令得到与只读命令相同的默认拒绝结论（`handled=False`），不存在绕开顶层身份判定单独进入写命令分支的路径 | L2（纯逻辑） | 已验证 |
| V-管理-38 | 确认执行、取消、过期或失败（角色被撤销/目标状态漂移）后，原确认卡片必须以正确的 CardKit 载荷更新为不可再次操作的最终状态（产品合同「管理员处理入口与安全确认」"执行完成、失败或取消后，原卡片更新为不可再次操作的最终状态"）：`UpdateCardRequestBody.card` 为 `Card` 对象（`type=card_json`、`data` 为卡片 JSON 字符串）而不是裸 JSON 字符串，且携带与前序调用无缝递增的 `sequence`；更新后的卡片结构上 0 个按钮，不存在任何可点击入口。**2026-08-25 载体 #96 追加事实（不改变本条认领状态）**：真实点击验收发现原确认卡片会回弹为带按钮的原始状态——根因是 `card.action.trigger` 回调的应答帧此前恒为空，lark SDK 据此按"维持原卡"处理，与本条断言覆盖的出带外 `update()` 调用是否正确是两回事（出带外调用本身此前已经修复，见下方归属说明）。修复后 `AdminCardCallbackHandler.handle()` 直接返回应答字典，经 `apps/gateway`、`LongConnectionSupervisor._dispatch`、`_RawEventSink._do_without_validation` 透传给 SDK marshal 进 `resp.data`，应答里携带的终态卡 JSON 与本条断言的 `update()` 调用共用同一份 `render_card_payload()` 构造。应答换卡机制已实现。**L4a 真实点击 2026-08-25**：产品负责人在 `biai-stage`+`Bot-Test` 真实点击确认卡片，终态换卡、toast 提示、群通知、幂等重放全部真实走通——原卡片正确收敛为不可再操作的终态卡，此前"回弹为带按钮原始状态"的缺陷未再复现。 | L4a（真实 CardKit 链路） | 已验证 |
| V-管理-39 | 停用「感知即清」接入（Issue #304 批次 4，产品合同「数据保留与删除」：停用一经 Lingxi 感知即触发，统一、全部、不可逆清除，不等待下一次问数任务入队）：`suspend_user` 确认执行（EXECUTE 分支）在 `confirm()` 自己的同一个数据库事务、同一个连接上，为该用户排队全部会话保留正文清理（复用 `_Transaction.clear_delivered_content_for_user`，迁移 `0061` 已预留的 `reason=user_cleared` 分类值，未新增迁移）：`task_delivery_event.content` 与 `conversation.agent_session_id` 随 `account_state` 翻转在同一事务内同步、不可逆清空/置空；`agent_session_cleanup` 同事务排队。`resume_user` 不做任何清理，不恢复已清正文（不可逆，合同语义）；重复确认因 `decide_confirm` 幂等短路（第二次点击落在 `ALREADY_TERMINAL` 分支，不再进入 `EXECUTE`）结构上只可能排队一次，`agent_session_cleanup.agent_session_id` 唯一索引兜底。**覆盖面**：Outbox 正文与会话上下文指针（能否被 resume）两项**同步、不可逆**清除，满足"感知即清、不等待下一次任务入队"的字面要求；Agent 会话 JSONL **物理文件**的删除仍是异步排队（既有 Worker 周期性收口消费，典型延迟约一次 `poll_interval_seconds`——默认 2 秒，上限受当轮 worker 正在处理的并发任务耗时影响，可能拉长到分钟级），且该收口路径默认**先归档、后删除**（`LINGXI_WORKER_SESSION_ARCHIVE_ENABLED` 默认开启，Issue #291 2026-08-22 既有设计），归档目录按数量/时间双重上限保留（默认最近 10 份或 7 天）——这段保留期内技术上仍可经运维权限从归档目录找回原始 JSONL，与合同「不可逆清除」的字面要求存在落差；该落差覆盖全部四类清除触发点（`/new`、空闲到点、停用、权限变化），不是本行新引入，如实登记供产品负责人判断是否需要为停用/权限变化两类触发单独收紧为跳过归档。**另一处覆盖面落差（批次 4 opus 审查 P2-2 附带登记）**：产品合同「统一用户记录与权限变化」149 行原文「停用前已经开始的任务继续正常完成和交付，不额外停止或撤回，交付完成后该任务产生的结果正文随即按停用清除口径清除，不再保留」——本行实现的清除谓词是 `confirm()` 执行那一刻对该用户全部会话的一次性快照（只清 `platform_received_at IS NOT NULL` 的已送达正文），停用确认那一刻仍在途（尚未送达）的任务，交付完成后其正文**当前不会被随即清除**：一次性快照不会因为这次投递事后补触发第二次，这类正文要等到所在会话满两小时空闲、被 `sweep_idle_conversations` 扫描到才会被清除，与合同「随即清除」的字面要求之间存在一个可测得的时间落差（最长约两小时，取决于扫描周期与该会话何时转入空闲，扫描本身默认每两小时一轮）。是否在投递确认路径（`confirm_delivery`）里补一次停用状态判断、让"停用后才送达"的这类任务也做到真正随即清除，待产品负责人裁定（已列入裁定清单） | L2（真库，变异验红） | 已验证 |
| V-管理-40 | `/admin trace <追溯号>`（Issue #337，S-H3-1）：非管理员默认拒绝、零下游查询；`<追溯号>` 须为裸 ULID，不合形状一律 `UNKNOWN`；查无追溯号明确回复「查无此追溯号」；存在但无失败记录如实回「无失败记录」+ 当前开通状态；存在且有失败记录回显 `failure_reason`（验收关键：管理员凭追溯号自助查到此前只能靠检索容器日志才能拿到的答案）。输出脱敏：不回显 open_id/用户消息正文/姓名/邮箱。详见 `core/admin/router._render_trace` 与 `adapters/admin_registry.PostgresAdminQueries.trace_lookup` 文档字符串 | L2（纯逻辑，含伪造查询端口）+ L2（真库） | 已认领 |

**2026-08-28 L4a 部分验收（S-H3-4 + S-H4-3，[#373](https://github.com/Moshuiwang/lingxi/issues/373)，不改变本条认领状态）**：`biai-stage` 候选/正式镜像上受控测试身份的真实链路验收取得两个负向 + 一个正向。**两个负向已坐实**：非管理员拒绝（审计 `command.unsupported_slash` 回落 `gateway.slash_rejected`，与未知命令同型回落，不泄露命令存在性）；查无追溯号（审计 `admin.command.query_trace {found:False}`，管理员真实收到「查无此追溯号」）。**正向部分坐实**（S-H4-3 六击第 2 击）：管理员对击 1 产生的真实 trace_id 发起查询，真实收到含 `failure_reason` 的脱敏回复（`admin.command.query_trace {found:True}` + `reply.sent admin.trace_query`）——查询与回复链真实，但样本为**受控合成**（复用击 1 内测名单闸真实产生的 trace_id 与拒绝原因，插入 `onboarding_failure` → 查询 → 即删），不是开通链自然产生的失败样本。**登记边界**：真实开通链路内自然产生的失败样本（而非受控插入）尚未在 `biai-stage` 自然出场，留 PM 回归观察项；2026-08-29 产品负责人真人入站验证未覆盖此项，边界未解除。

**归属**：V-管理-21…28 由 [Issue #95](https://github.com/Moshuiwang/lingxi/issues/95) S-M-01（2026-08-24 范围重定为「服务端管理员角色登记表」，方案 A 终裁见[决策记录](../决策记录/2026-08-24-管理员职责集与银河外权限动作边界.md)）认领。**本组不覆盖什么**：管理 MCP 身份认证与绑定（触发式未来入口）、登记表的授予/撤销写路径与本人确认卡（S-M-02 #96）、停用/恢复类写动作均不在本组范围，`V-管理-01…20` 继续代表那些未来能力、保持未认领。真实 `biai-stage` + `Bot-Test` 私聊链路（管理员真实收到并操作管理命令面）属 L4a，未验证。

**归属**：V-管理-29…38 由 [Issue #96](https://github.com/Moshuiwang/lingxi/issues/96) S-M-02（管理员本人飞书确认卡片与待确认动作闭环 + 停用/恢复动作）认领；`V-管理-39` 由 [Issue #304](https://github.com/Moshuiwang/lingxi/issues/304) 批次 4 认领——建立在本组 S-M-02 已交付的 `confirm()` 同事务机制之上，不是独立的新事务边界。依据该 Issue 2026-08-24 的范围确认评论——本组是通用待确认操作机制（`pending_action` 表，迁移 `0068`）在**停用/恢复**这两条 MVP 写动作上的落地，身份来源沿用 S-M-01 已交付的 `admin_registry` 登记表判定（V-管理-21…28），不是 `V-管理-01…20` 设想的 `admin_identity`/管理 MCP 身份绑定方案——那一组仍然代表管理 MCP 的未来能力，保持未认领；本组新增的三段式机制（`prepare`/`confirm`/`cancel`）与卡片回调闭环供本地抑制、单用户重同步、登记表变更命令等未来批次复用同一张表，不需要重新设计核对链。**本组不覆盖什么**：管理 MCP 身份认证与绑定、本地抑制/单用户重同步/登记表变更三条未来写动作本身、当前运行环境删除编排（V-管理-13/14 继续未认领）。**2026-08-25 收口（编排者裁定，依据产品负责人当日在 `biai-stage`+`Bot-Test` 的真实走查）**：真实私聊链路里"确认卡片发送/点击回调/终态换卡/群脱敏通知/停用恢复动作/幂等重放"这一组已经真实验证，事实分别记在 `V-管理-33`（停用/恢复动作）、`V-管理-36`（群通知与幂等重放）、`V-管理-38`（发卡/点击/终态换卡，本条状态已随之改为已验证）三行的追加说明里——这三行各自仍以各自原有的注入式证据（真库/可注入假传输层）作为其 L2 认领依据，追加说明只是补充一条独立的 L4a 真实印证，不改变认领所需的证据类型。**这次真实走查明确没有覆盖、因此仍然只有注入证据、不构成真实链路证据的部分**：审计写入失败时的真实事务回滚（`V-管理-32`）、真实并发的两个数据库连接同时确认（`V-管理-31`）、卡片回调伪造（`V-管理-35`）、非发起人点击（`V-管理-34`）、待确认操作过期（`V-管理-30`/`V-管理-29` 的过期分支）、发起人角色在中途被撤销或目标状态漂移（`V-管理-30`）、只读查询命令组的真实链路（归属见 `V-管理-21…28` 组，不在本组范围）——这些行的 L4a 状态继续保持未验证，不因这次走查一并升级。**新增 `V-管理-38`（批次二审查修复，编排者裁定）**：产品合同「管理员处理入口与安全确认」"执行完成、失败或取消后，原卡片更新为不可再次操作的最终状态"这条承诺，本组此前没有任何一行认领——`V-管理-34`/`V-管理-35` 覆盖的是卡片收件人与回调伪造防护，`V-管理-36` 覆盖的是管理群通知触发逻辑，都不是"终态卡真实 CardKit 载荷是否正确"这件事。opus 用真实 `lark-oapi==1.7.1` 本地构造实测证伪：终态卡更新此前把整份 JSON 字符串直接传给 `UpdateCardRequestBody.card`（真实类型是 `Card` 对象）、且完全没有传 `sequence`，已修复（`adapters/feishu_admin_card.py` 的 `LarkAdminCardTransport.update()`，见该文件与迁移 `0068` 文件头部）——这是本组唯一一条已经**实测证伪**过真实链路一个具体环节的缺陷，不只是"尚未真实验收"这一笼统状态，因此单独认领为 `V-管理-38`、证据要求定为 L4a：真实平台是否接受这个载荷、终态卡是否真的在管理员面前收敛为 0 按钮，只有真实 CardKit 链路才能证明，本地构造核实（`tests/test_feishu_admin_card_transport.py`）只证明"不再是已知错误的形状"，不构成 L4a 证据。`V-管理-36` 断言的群通知触发逻辑（首次/幂等/脱敏）与终态卡是否真的更新成功完全无关——`core/admin/card_callback.AdminCardCallbackHandler._update_card_to_terminal`/`_notify_group` 是两个独立的 best-effort 分支，前者失败不影响后者——因此 `V-管理-36` 的 L2 断言不受这次证伪影响，保持已验证，不随 `V-管理-38` 一起降级。**建卡环节另一处已被真实探针证伪并修复（2026-08-25，编排者裁定，不改变 `V-管理-38` 的认领状态）**：产品负责人 2026-08-25 真实走查 `/admin suspend` 时，命令面与待确认机制均正常，但确认卡片发送失败（用户看到"确认卡片发送失败，本次操作不会执行"，失败关闭正确，未产生错误的待确认状态）。编排者随后用受控探针在 `biai-stage` 对 CardKit `POST /open-apis/cardkit/v1/cards`（即 `LarkAdminCardTransport.create()` 对应的真实建卡接口，与 `V-管理-38` 覆盖的 `update()` 终态更新是同一个 `_card_payload()` 函数产出但不同的调用点）实测定位：`_card_payload()` 此前把按钮包进 `{"tag": "action", "actions": [...]}` 容器，这个形状被真实 CardKit 以 `code=200861`（`ErrPath: elements->[1](tag: action)`，"cards of schema V2 no longer support this capability"）拒绝——这正是产品负责人观察到"确认卡片发送失败"的真实原因。同一轮探针证实按钮直接作为 `body.elements` 顶层元素（不套 action 容器）建卡成功，已按此改用飞书文档描述的 `behaviors` 回调标准写法修复（`adapters/feishu_admin_card.py`，2026-08-25）。**这条探针只证明 CardKit 接受新的建卡载荷形状，不证明真实点击回调、卡片在管理员面前的视觉表现或收件人路由**——`create()` 路径的真实链路证据仍是 L4a 未验证；`V-管理-38` 覆盖的是 `update()`/终态卡形状与 sequence，与本条是同一函数不同调用点的两件事，状态继续保持未认领，不因这次探针合并升级。

### 权限发布与同步

`V-权限-01/02/03/09/10/11/12` 由 [#156](https://github.com/Moshuiwang/lingxi/issues/156) 的 S-C-01（发布 outbox 与多维表格写读回）与 S-C-02（令牌签发与 MCP 就绪状态机）认领。

**2026-08-17 产品负责人裁定改写了两条断言的措辞**（依据：Trace [#203](https://github.com/Moshuiwang/lingxi/issues/203) G-155 终判）：

- **`V-权限-06` 由「回读版本比对」改为「探针法」**：权限表无版本字段，就绪改用目标用户令牌真实执行一次 `list_metrics`、可见指标大于零才算就绪，结论绑定（用户，权限版本）。正文见[架构设计 6.4](架构设计.md#64-权限发布与-mcp-同步确认)。
- **`V-权限-11` 由「永不写入 `token_cipher`」改为「新建行必须携带、更新行不清空不覆盖」**：没有密文的行对 MCP 无意义；令牌明文与主密钥不进数据库任何列、outbox、日志与告警，进 outbox 与发布表的只有密文。正文见 `core/permission/publish_row.py` 与迁移 `0065`。

**「密文被改写」保证的边界（2026-08-17 编排者裁定，不做代码修复）**：识别改写依赖「这一行是我们建的」这一事实，只在同一条发布意图内有效，因此新版本产生的新意图对「上一版曾建过的行」、以及创建结果不明时识别不到；两者都不做代码修复——就绪探针是功能性最终门，带错误密文的行永远探不成功，会以十五分钟超时转运维，不产生假成功；密文归属随存量用户令牌归属决策一并确定（裁定 6：硬切窗口统一重签）。

**`V-权限-07` 由 S-C-03a + S-C-03b 认领**（[#156](https://github.com/Moshuiwang/lingxi/issues/156)，Trace [#203](https://github.com/Moshuiwang/lingxi/issues/203)）：

- **重算**（S-C-03a）：`lingxi-scheduler` 每日权限重算严格先花名册、再银河，权限有变化才推进版本。
- **发布与就绪确认**（S-C-03b）：每轮驱动发布执行器并按 tick 推进就绪探针，一轮调度不被十五分钟等待占住。
- **通知**：就绪成功发「范围已更新」，撤权在读回一致后发「暂无可用范围」。

**银河快照刷新口径**：MVP 期银河数据由人工 CLI 按需导入，重算职责每日消费当前有效批次；「刷新」= 消费当前批次，不承诺自动拉取。

**仍未验证的部分（不冒充已验证）**：①L4a 真实多维表格回读与真实 MCP 探针留受控窗口，翻译层硬闸已由 [#227](https://github.com/Moshuiwang/lingxi/issues/227) 落地为 `V-权限-13`；②发布/花名册所需环境变量未全配，写快照与发日报按 [#275](https://github.com/Moshuiwang/lingxi/issues/275) 已拆为互斥职责；③通知的真实私聊投递行为未验证；④权限变化通知的公司位显示内部编号而非中文名，待翻译层 Story 一并修复；⑤`mcp_sync_check` 清理需 `LINGXI_MCP_TOKEN_ENCRYPT_KEY`，未配时不装配，该表无可识别内容列不构成保留违规；⑥翻译闸落地前的遗留发布意图已用独立运维脚本一次性作废，需在部署前手动执行一次，未在 `biai-stage`/生产验证。

**通知投递的 at-most-once 残余窗口（知情接受）**：就绪落库后、通知发出前进程崩溃会丢失该条通知，不建通知 outbox 是刻意取舍（产品负责人 2026-08-18 裁定 4：有限重试 + 审计，非 exactly-once）；先记后发保证不重复打扰用户。

**刷新链与开通链的十五分钟不是同一个承诺**：十五分钟约束首次开通（Epic D 用阻塞形态、真实 `sleep`）；刷新链的用户已在用 Lingxi，停机恢复后确认成功并告知即可。

**单实例假设（登记不修）**：本职责按 `lingxi-scheduler` 单副本设计，不做分布式租约，同时跑两实例可能重复通知。

**`V-权限-08` 认领的是刷新侧那一半**：权限从有变无时保留发布表那一行、`permissions` 清空、`status` 维持 `approved`、`token_cipher` 不动（撤权行只走更新路径，建不出新行），随后通知用户；在发布链上无足迹的人跳过，"在途也算足迹"（产品负责人 2026-08-18 决策补充 2）；匹配阶段失败不算撤权信号；重复撤权判 `UNCHANGED`；「全部角色都未映射」同样走这一路（对应 `V-银河-11` 末句在刷新侧的落点）。

**`V-权限-08` 的开通侧那一半仍属 Epic D**：首次开通时的未授权提示执行点在 `OnboardingRunner`，本 Story 不认领；存量用户令牌归属（裁定 6）同样属后续切片。

`V-权限-04/05` 由 S-C-02 认领判定这一半：就绪只有探针成功一条路成立，十五分钟预算耗尽后收口为超时终态（注入时钟）。把状态推到 `active`/`manual_review`、发送成功通知、生成人工待办属 Epic D 开通编排，其真库证据随那批交付；S-C-02 不接任何进程，也不改用户状态。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-权限-01 | 权限变更事务回滚时，`publish_outbox` 记录一并回滚（不产生孤立发布意图） | L2（真库） | 已验证 |
| V-权限-02 | 发布内容与数据库当前记录**逐字段一致** | L2（比对函数单测）+ L4a（真实多维表格回读） | 已认领 |
| V-权限-03 | 修改用户 A 的权限后，用户 B 的发布行逐字段不变 | L2（可注入发布表）+ L4a | 已认领 |
| V-权限-04 | 同步未确认前，用户状态不为 `active`，且未发送开通成功通知 | L2（真库状态机） | 已认领 |
| V-权限-05 | 十五分钟未确认时转 `manual_review` 并生成待办 | L2（注入时钟） | 已验证 |
| V-权限-06 | 就绪的唯一依据是**以目标用户身份**（用该用户自己的令牌）对问数 MCP 真实执行一次 `list_metrics` 并看见大于零的可见指标；结论绑定（用户，权限版本），只对当次绑定的那一版有效；明确空结果、"服务在线"或"他人可用"均不被单独当作就绪 | L4a（真实 MCP） | 已认领 |
| V-权限-07 | 每日权限刷新严格先刷新花名册、再刷新银河快照（快照刷新＝消费当前有效批次，导入按 CLI 决策）；新快照对已有用户同样生效，权限变化会更新当前权限多维表格并产生 MCP 同步确认链 | L2（调度与真库）+ L4a（真实多维表格回读） | 已认领 |
| V-权限-08 | **（本 Story 认领「刷新侧」半边）**权限从有变无时保留发布行并把 `permissions` 清空——不删行、不改 `status`、不碰 `token_cipher`、不新建行——随后告知用户；仅有其他受支持角色时忽略未映射角色。**「开通侧」半边（银河权限异常时给未授权提示、不创建用户环境、不生成权限人工待办）执行点在 Epic D 的开通编排，本 Story 未认领**。**全抑制走撤权出口（红线-2，Trace #328 opus 审查坐实并修复）**：银河这一侧原本是有效授权（`aggregate.granted=True`，因此没有走`no_galaxy_roles`/`no_supported_function`/`no_company_scope` 三个原因码），但四源合并（`V-权限-15`）里本地抑制把翻译出的全部指标压光到空字典——这不是「无可用银河权限」，是「银河给了、本地行政性地收回到零」，语义与撤权一致，因此复用 `_revoke` 同一套机制（保行清空、只对发布链上留过足迹的人发），但传一个可分辨的原因码 `fully_suppressed`（`REASON_FULLY_SUPPRESSED`），不再落到 `build_translated_publish_row` 对空输入的 `ValueError` 冒泡成通用 `permission_refresh.user_failed`（修复前的实际行为，坐实即改）。**否定用例**：抑掉用户全部指标 → 发布 `{}` 撤权行 + `permission_refresh.user_revoked` 专属审计（`reason=fully_suppressed`）（可注入：`test_permission_refresh_duty.py::FullSuppressionRevocationTest`；真库：`test_permission_refresh_postgres.py::FullSuppressionRealDbTest.test_a_fully_suppressed_grant_with_a_real_publish_footprint_gets_an_empty_revocation`，含无发布足迹时按既有边界跳过、只抑掉部分指标时仍走正常授权发布两条姊妹否定/正向断言）。**变异验红实测**：临时删掉 `_refresh_user` 里 `if not merged.permissions` 的空判断后，上述否定用例按预期变红（改回抛 `ValueError`、report.failed 从 0 变 1）；恢复后转绿 | L2（纯逻辑 + 真库） | 已验证 |
| V-权限-09 | 同一 `record_key` 重复发布收敛到**同一行**（写入前必查、命中即更新），不产生第二行；发布表里已存在同邮箱但 `record_key` 口径不同的行、或命中多行时**失败关闭**——既不更新也不新建，且不重试 | L2（可注入发布表）+ L4a（真实多维表格） | 已认领 |
| V-权限-10 | 写入后逐字段读回并按**字符串值**比对（数字字段被读取接口字符串化时仍算一致，不按 Python 类型严格相等）；任一字段不一致即不记发布完成、不写 `published_at`，并产生可分辨的告警事实 | L2（比对函数单测 + 可注入发布表） | 已验证 |
| V-权限-11 | **新建**发布行必须携带 Lingxi 签发的 `token_cipher`（缺它则失败关闭，不新建）；**更新**既有行时既有密文既不被清空也不被覆盖，仅当该列为**空**时才补写我方密文；**`token_cipher` 缺失（含纯空白）时任何路径都不得判发布完成**；**同一发布意图内**的密文被改写必被识别为不一致；令牌**明文与主密钥**不进数据库任何列、outbox、日志与告警，且**原样令牌明文写不进**令牌表（数据库 CHECK 钉住签发格式的精确 envelope，不宣称证明内容已加密）、已签发密文改不掉（数据库触发器） | L2（字段集断言 + 可注入发布表 + 真库 catalog、CHECK/触发器否定用例与全表扫描） | 已验证 |
| V-权限-12 | 旧权限版本的发布意图不覆盖新版本（认领时比对当前 `permission_version`，过期意图**一次外部调用都不发**）；同一用户同一时刻只有一条发布在途；消费者重启后滞留的 `publishing` 可回收重入且不产生第二行；迟到的记账不能改写已被回收的意图 | L2（可注入发布表 + 真库） | 已验证 |
| V-权限-13 | **运行时硬闸，两层判据（Issue #227 载体 + 2026-08-18 外部独立审查坐实并修复的 P1）**。**Layer 1（整轮判据，`run_once` 遍历任何用户之前）**：翻译映射（`lingxi/config/company_function_metric_map.toml`）整体为空时，本轮**一条发布意图都不排**——授权、撤权**一起**挡住。这一层存在的理由是撤权（`_revoke` → `build_revocation_row`）写的是不含指标名的 `{}`，从不调用翻译函数，因此只有把判据挪到遍历开始之前、判"翻译层这一轮可不可用"而不是"这一行要不要翻译"，才挡得住撤权。**Layer 2（逐用户判据，只在 Layer 1 已通过、映射非空之后才可能触发）**：某个「公司 + 职能」组合在非空映射里仍查不到时，只跳过这一个授权用户——不发布、不回落成职能标签、不猜测、不产出部分结果；这个人仍在 `granted=True` 分支，从未进入 `_revoke`，因此这一层与撤权无关。**背景（如实登记，不掩盖）**：2026-08-18 首版实现只有 Layer 2，外部独立审查（codex gpt-5.6-sol/xhigh）指出撤权路径完全绕过这道闸、映射为空时仍能直接把 `{}` 写进正式表；编排者回源坐实为真（P1）。修复选择让**实现覆盖全部路径**（新增 Layer 1），不是让文档承认例外。**闸门打开的条件**：Layer 1——映射非空（哪怕只有一条内容）；Layer 2——为用户当次持有的每一个「公司 + 职能」组合都填入指标名列表。当前文件是空的，因此 Layer 1 对任何人都不成立，整轮（含撤权）零发布意图。**闸门位置与穷尽核对（补 #346）**：全仓库调用权限发布 `record_decision`（唯一能把内容写进 `publish_outbox` 的入口；`enqueue_publish` 无其他调用方，`publish_outbox` 无别的 `INSERT` 来源，见 `adapters/postgres_permission_publish.py`）的位置恰好三处：`permission_refresh.py` 的 `_refresh_user`（授权，Layer 1+2 都挡）与 `_revoke`（撤权，只挡 Layer 1，已足够——撤权内容不含指标名，Layer 2 无意义），两处都在 `run_once` 循环内被 Layer 1 提前挡住；第三处 `onboarding_runner.py::AutoOnboardingRunner._publish`（开通侧，Layer 2 由 `#346` 补齐，同一套函数，见 `V-权限-15`）。`build_publish_row`/`serialize_permissions`（旧序列化路径）无生产调用方；`scripts/` 下无脚本调用 `record_decision`/`enqueue_publish`；`core/execution/audit.py` 另有同名方法，与本条目无关（工具执行审计非权限发布）。**与 #226 令牌供给相互独立、非替代关系**：本闸门挡的是"outbox 里能不能出现一条待发布内容"，#226 挡的是"已经在 outbox 里的意图能不能被真正写进外部表"——两道闸各自独立生效，互不替代，也互不放松。**可观察性**：Layer 1 拒绝时记恰一条 `permission_refresh.skipped_metric_translation_unavailable` 审计（与花名册不新鲜、无当前有效银河批次同一姿态，同一天同一原因只记一条）；Layer 2 拒绝时记 `permission_refresh.user_skipped`（`reason=metric_translation_uncovered`）；两者动作名不同、可分辨，字段只含内部用户标识/阶段/原因码，不回显指标名、公司范围或权限内容。**变异验红实测**：Layer 1（本次 P1 修复新增）——临时移除整轮判据后，`tests/test_permission_refresh_duty.py` 的 `RoundLevelTranslationGateTest` 全部 4 个用例（含专门钉"撤权被挡住"的 `test_the_gate_blocks_a_revocation_that_would_otherwise_publish`）按预期变红，真库对照用例 `tests/test_permission_refresh_postgres.py` 的 `test_a_revocation_is_blocked_when_the_translation_map_is_empty` 同样变红，恢复后全部转绿；Layer 2（载体首版已实测）——临时替换成直接调用未翻译的旧路径后，`MetricTranslationGateTest` 与 `GrantedUserTest.test_the_published_row_is_built_from_the_archived_identity` 共 8 个用例及真库用例 `test_the_intent_carries_the_refresh_reason_and_the_issued_cipher` 按预期变红，恢复后全部转绿 | L2（纯逻辑 + 可注入发布表）+ L2（真库，`test_permission_refresh_postgres.py`） | 已验证 |
| V-权限-14 | **B 口径（产品负责人 2026-08-27 裁定，Trace #328 裁定 #1，取代 [#320](https://github.com/Moshuiwang/lingxi/issues/320) 落地的 A 口径）**：持有『后台管理员』这个 **Lingxi 职能标签**（`ADMIN_FULL_ACCESS_FUNCTION`）的用户，聚合结果 `all_companies` 恒为真——不依赖这次银河快照解释出的公司范围是「全非」通配还是具体公司枚举。**判据措辞如实化**（Trace #328 opus 审查）：触发条件是「有没有持有这个职能标签」，不是「role_id 是不是 513」——聚合层（`core/permission/publish_row.py` 的 `aggregate_permission`）读的是账号持有的**角色名**（`role_names_for_user`，不是 `role_id`），经 `galaxy_role_function_map.toml` 的「"后台管理员" = "后台管理员"」这一行映射成职能标签后才判定；`role_id 513` 只是这条映射当前唯一已知对应的银河侧角色 ID，是一次观测事实（供审计/测试数据标注留痕），不是代码里的判据本身——判据完全由角色名经配置文件映射决定，若银河将来把这个角色名挂到别的 `role_id`，或把同名角色拆分成多个 `role_id`，只要角色**名**不变，特例照常触发，不需要改任何代码或配置。因此覆盖**全公司，含未来新增公司**（通配键 `*` 从不按已知公司数量枚举，新公司出现时不需要回填任何配置）× 全指标（`company_function_metric_map.toml` 的 `[companies."*"]` 「后台管理员」条目，当前 9 个）。**A 口径已证明不足**：给 44 个已知公司键各写一条「后台管理员」映射后，具体公司枚举的持有人聚合结果仍只覆盖枚举到的那些公司——`tests/test_admin_role_full_access.py` 的 `AdminRoleWithoutWildcardCompanyScopeStillTranslatesTest` 曾经钉住这一点（当时是有意为之的健壮性断言），现已按 B 口径反转为本条目的核心断言。**否定断言**：不持有该职能的普通用户不受这条特例影响，具体公司枚举依然是具体公司枚举（`NonAdminFunctionUserIsUnaffectedByTheSpecialCaseTest`）；聚合结果的 `companies` 字段本身不被特例清空或改写，只是序列化与翻译层在 `all_companies=True` 时本来就只查通配键、不看该字段。**变异验红实测**：把特例条件（`ADMIN_FULL_ACCESS_FUNCTION in functions`）改成恒 `True` 后，上述否定用例按预期变红；临时删除特例分支后，`AdminRoleWithoutWildcardCompanyScopeStillTranslatesTest` 的核心断言按预期变红；恢复后全部转绿。**A 口径遗留**：`company_function_metric_map.toml` 里 44 个具体公司键下的「后台管理员」条目保留不清理（死代码无害，B 口径生效后恒不被查到），文件内已加注说明。**已验证与未验证的边界**：单测已验证聚合与翻译两层的纯逻辑结果；U1 在 `biai-stage` 上的真实端到端回归（真实银河快照 + 真实发布表读回）留待本批次收口时批终认领，本条目当前不据此宣称已在 stage 验证过 | L2（纯逻辑单测：`tests/test_admin_role_full_access.py`，已验证）+ L4a（U1 stage 回归，批次收口时批终认领） | 已认领 |
| V-权限-15 | **四源聚合集中合并（S-P-3，Issue #319）**：真实权限 = (银河 ∪ 本地授权 ∪ 存量沿用) − 本地抑制，全仓库只有一份实现（`core/permission/merge_sources.py` 的 `merge_permission_sources`），两个写发布行的调用点（`apps/scheduler/permission_refresh.py::_refresh_user`、`core/identity/onboarding_runner.py::AutoOnboardingRunner._publish`）共用同一个函数，不各自维护一份等价逻辑。**抑制优先否定用例**：本地抑制命中的即便是**银河这一侧**翻译/聚合出的指标名（不是本地授权自己新增的那条），减法仍然生效，同键 grant+suppress 不出现（`suppress` 赢由 `local_override.resolve_local_overrides` 早一步钉死，本函数直接复用其结果）；某个公司的值集合被抑制到空时**丢弃该公司键**，不写空列表——理由是非通配分支里 `galaxy` 从不含通配键 `"*"`，丢键与写空列表对读侧 `lookup_metrics` 完全等价，因此不需要放宽 `serialize_translated_permissions` 既有的「写侧不产出空列表」纪律（`tests/test_permission_publish_row.py::TranslatedSerializationTest.test_an_empty_metric_list_for_any_company_is_rejected` 不受影响、原样保留）。**通配角 v1 语义（编排者裁定，如实登记）**：`galaxy` 出现全公司通配键（当前唯一形态 = 513 后台管理员，`all_companies=True`，对应 `V-权限-14`）时，本地授权与本地抑制**整体不参与合并**（授权冗余、抑制拦不住通配，均不写额外的具体公司键——避免读侧对该公司不再回退通配、造成窄范围少给权限的回归），产出与银河原始映射逐字节相同；两个理由 (`grant_redundant_wildcard`/`suppress_inapplicable_wildcard`) 各自可分辨地登记在纯函数返回值里，由调用点各记一条审计（`permission_refresh.local_override_skipped`/`onboarding.local_override_skipped`）。**挂点与设计初稿的层级差异（如实登记，2026-08-28 补 #346 修复更正）**：设计初稿假设两个调用点都消费翻译层产出的 `{公司:[指标名]}`，因此把 `galaxy` 参数类型定为 `PermissionAggregate`；S-P-3 落地时（08-27）只有 `permission_refresh.py` 调用了 `translate_company_functions`，`onboarding_runner.py` 当时仍用未翻译的职能标签（历史沿革见 `merge_sources.py` 模块文档「挂点」一节）。`Issue #346`（Trace #373 S-H1-5）已修复：`_publish` 现在同样调用 `translate_company_functions`，两个调用点自此都传入翻译后的指标名，`galaxy` 参数类型仍保留通用的 `Mapping[str, Sequence[str]]`（本函数不关心值语义，签名不必收紧）。开通侧真正的接线点不是聚合发生的 `_match`，而是拿到内部 `app_user.id`（本地覆盖条目的 `user_id` 口径，迁移 `0072`）之后的 `_publish`——聚合发生时还没有这个键。**读取失败**（数据库异常）：该用户/该次开通跳过本地源、不整轮/整链失败，响亮记一条 `local_override_skipped`（`reason=local_override_read_failed`），装配层未接线（`local_overrides=None`）与读取失败是两种不同的运维状态，前者静默、后者响亮。**装配**：`apps/scheduler/assembly.py` 的两个装配点各自新建 `PostgresLocalPermissionOverrideStore` + 共用的 `LocalOverrideEntryReader` 适配（`adapters/postgres_local_permission.py`），无可选前置、无条件装配。**变异验红实测**：临时把并集之后的减法 `values -= suppressions` 改成 `values \|= suppressions` 后，`tests/test_permission_merge_sources.py::UnionThenSubtractTests.test_suppression_wins_even_over_a_galaxy_granted_metric` 按预期变红；临时删掉两条通配跳过判定后，`WildcardRoleTests` 的两个正面断言按预期变红；临时让通配分支也合并 `legacy` 后，`LegacyIdentityTests.test_legacy_is_ignored_under_the_wildcard_too` 按预期变红；分别临时把两个调用点改回不经过合并结果（直接传翻译/聚合产出而非 `merged.permissions`）后，`tests/test_permission_refresh_duty.py::LocalOverrideMergeTest` 与 `tests/test_onboarding_runner.py::LocalOverrideMergeTests` 里含并集/抑制断言的用例各自按预期变红；以上全部恢复后转绿。**存量沿用已接入（legacy 源，S-P-2，Issue #319 / Trace #328 S-P-2）**：`core/permission/legacy_source.py` 新增只读读取（`read_legacy_permissions`/`resolve_legacy_source`），按用户当前邮箱（与发布路径同一个规范化邮箱、`record_key`/`email` 两个查找键同源）查正式权限发布表的存量行、解析 `permissions` 列为 `{公司ID:(指标名,…)}`，接入两个调用点各自那一处 `merge_permission_sources(..., legacy=...)` 调用——本函数自身不改一行代码，只是 `legacy` 参数第一次真的收到非 `None` 的输入。找不到存量行是预期情形（空映射，对合并恒等）；命中多行、命中行 `record_key` 口径不一致、`permissions` 列解析失败、传输层异常四类失败统一失败关闭，该用户本轮跳过存量源并各记一条响亮审计（`permission_refresh.legacy_source_skipped`/`onboarding.legacy_source_skipped`，`reason=` 取原因码、`error=` 取异常类名不含正文），不整轮/整链失败；装配层未接线（`legacy_source=None`）与四类失败因此分别是静默/响亮两种姿态，与本地覆盖读取失败同一分层。通配角下同样整体不参与合并——`merge_permission_sources` 通配分支本就不读 `legacy` 参数，行为已经覆盖，未新增专门的跳过原因码（读该分支代码确认）。**装配**：`apps/scheduler/assembly.py` 的 `build_loop` 新建一个共享的 `BitablePermissionTable` 实例（复用权限发布表既有的 `LINGXI_PERMISSION_BITABLE_*` 坐标与应用身份令牌供给 `permission_table_supply`，不新增环境变量），坐标缺失时为 `None`，同一个实例传给 `_build_permission_refresh_duty` 与 `_build_onboarding_duty` 两个装配点。**变异验红实测（S-P-2）**：临时删掉合并结果里对 `legacy` 的并集参与后，`tests/test_permission_legacy_source.py` 与 `tests/test_permission_refresh_duty.py::LegacySourceMergeTest.test_legacy_permissions_are_unioned_into_the_published_metrics` 按预期变红；临时把 `resolve_legacy_source` 里读取/解析失败的异常吞成静默（不记审计、直接返回空映射）后，`ResolveLegacySourceTests.test_read_failure_skips_and_audits_with_reason_and_error` 按预期变红；以上恢复后转绿。**legacy 有界化：只在「Lingxi 从未为该用户成功发布过」时参与合并（红线-1，Trace #328 opus 审查坐实并修复）**：存量源读的与发布通道写的是**同一张表**——不加边界会形成自反馈环（Lingxi 今天发布收窄后的权限 → 写进这张表 → 明天当作「存量沿用」读回来、并进合并结果 → 收窄被自己昨天写的内容原样抵消，且因内容与上一条意图逐字段相同而判 `UNCHANGED`，连审计都看不出回归在发生）。两个调用点在读 legacy 之前先查 `has_publish_footprint`（`PostgresPermissionPublishStore` 的既有只读口，与撤权侧复用同一个）：有发布足迹（发布成功过，或当前还有 `pending`/`publishing` 的意图在途）→ legacy 按 `None` 处理，且**不发起** `find_rows` 调用（省读放大，`513` 通配用户同理合并前即跳过，不进入通配分支之后才丢弃）；从未有过足迹 → legacy 照常参与合并。**否定用例**：昨日已发布行含今日银河没有的指标 → 今日发布内容不含它，收窄真正生效（`test_permission_refresh_duty.py::LegacySourceMergeTest.test_a_user_with_a_publish_footprint_never_reads_legacy`、`test_onboarding_runner.py::LegacySourceMergeTests.test_a_user_with_a_publish_footprint_never_reads_legacy` 均为可注入用例；真库用例 `test_permission_refresh_postgres.py::LegacyBoundednessRealDbTest.test_a_user_with_a_real_publish_footprint_never_reads_legacy` 用真实 `publish_outbox` 证明 `has_publish_footprint` 这一半判据）。**正向不回归**：从未发布用户 + 存量行 → 沿用生效，同一测试类的姊妹用例覆盖，真库版本见 `LegacyBoundednessRealDbTest.test_a_never_published_user_still_uses_legacy`。**变异验红实测**：临时删掉 `_resolve_legacy_source` 里的 `has_publish_footprint` 判据后，上述否定用例按预期变红（存量指标重新出现在发布内容里）；恢复后转绿。**零银河权限用户的本地授权兜底成立（PM 2026-08-29 裁定，Issue #419，此前登记的已知限制已消除）**：四源合并不再挂在 `aggregate.granted` 判据之后——`permission_refresh.py::_refresh_user`（新增 `_refresh_zero_galaxy_user`）与 `onboarding_runner.py::AutoOnboardingRunner`（`_match` 不再直接拒绝，新增 `_reject_zero_galaxy_without_local_grant`，`_publish` 改传 `galaxy={}`）都改为「`aggregate.granted` 为假时也合并一次，非空则按结果发布、仍空则维持撤权/无权限语义」，`suspend` 撤权链不受影响。确认卡与 `/admin user` 的「暂不生效」提示（`_ZERO_GALAXY_PERMISSION_CAVEAT`）已删除；`V-通报-12` 措辞回改「生效」。**用例**：`test_permission_refresh_duty.py::ZeroGalaxyLocalGrantTest`、`test_onboarding_runner.py::ZeroGalaxyLocalGrantTests`（精确本地集合/同键抑制仍撤权/并集恢复/零令牌零环境）。**变异验红**：改回跳过合并直调 `_revoke`/`_not_authorized`，新增用例全部变红，恢复后转绿。**已验证与未验证的边界**：纯函数矩阵（并集/抑制/通配跳过/legacy 恒等/空结果丢键/legacy 有界化）与两个调用点的可注入接线用例（含并集、抑制后不含、store 缺席零行为差异、读取失败降级、发布足迹有界化、零银河本地兜底）均为 L2 单测覆盖；存量沿用自身的读取/降级矩阵与两个调用点的可注入接线用例同样是 L2 单测覆盖；legacy 有界化判据与全抑制撤权出口（见 `V-权限-08` 刷新侧扩展）已各自补上真库实测（`test_permission_refresh_postgres.py::LegacyBoundednessRealDbTest`/`FullSuppressionRealDbTest`）；U1、普通用户双案例与零银河本地兜底的真实端到端回归均留待批次收口时批终认领，本条目当前不据此宣称已在 stage 验证过 | L2（纯逻辑单测：`tests/test_permission_merge_sources.py`、`tests/test_permission_legacy_source.py`；可注入接线单测：`tests/test_permission_refresh_duty.py::LocalOverrideMergeTest`/`LegacySourceMergeTest`/`ZeroGalaxyLocalGrantTest`、`tests/test_onboarding_runner.py::LocalOverrideMergeTests`/`LegacySourceMergeTests`/`ZeroGalaxyLocalGrantTests`；L2 真库：`tests/test_permission_refresh_postgres.py::LegacyBoundednessRealDbTest`）+ L4a（U1/普通用户双案例+零银河本地兜底 stage 回归，批次收口时批终认领） | 已认领 |

**2026-08-26 对账（#100）翻转为已验证**（均 #156）：
- `V-权限-01` → `test_permission_publish_postgres.py::SameTransactionTest::test_rollback_leaves_neither_new_version_nor_orphan_intent`；commit `83ad3b9`（S-C-01）
- `V-权限-05` → `test_mcp_readiness_machine.py::BudgetTest`（899/900/901 秒边界精确覆盖 15 分钟预算）；commit `67ba225`（S-C-02）
- `V-权限-08` → `test_permission_refresh_duty.py::RevocationTest::test_revocation_does_not_delete_or_disable_any_published_row` + `test_a_match_failure_never_triggers_a_revocation_publish` + `RevocationPublishTest`；commit `b010884`（S-C-03b）
- `V-权限-10` → `test_permission_publish_row.py::ReadbackTest::test_number_readback_compares_by_string_value` + `test_mismatch_lists_field_names_in_registered_order`；commit `83ad3b9`
- `V-权限-11` → `test_permission_publish_row.py::PublishRowTest::test_create_field_set_requires_token_cipher` + `test_update_field_set_never_includes_token_cipher` + `test_plaintext_token_can_never_be_stored_as_a_cipher` + `test_malformed_cipher_is_rejected`；commit `2e6a6fe`（S-C-02）
- `V-权限-12` → `test_permission_publish_postgres.py::ClaimTest::test_one_user_has_at_most_one_publish_in_flight` + `test_an_uncommitted_claim_still_blocks_the_newer_version` + `test_a_newer_version_waits_until_the_older_one_reaches_a_terminal_state`；commit `83ad3b9`
- `V-权限-13` → `test_permission_refresh_duty.py::RoundLevelTranslationGateTest`/`MetricTranslationGateTest`/`GrantedUserTest` + `test_permission_refresh_postgres.py` 对应真库用例（矩阵正文已逐一列出测试名，本次逐一核实存在）；commit `e39b8d7`

`V-权限-02/03/04/06/07/09` 声明层级含 L4a（或纯 L4a），本任务不补验；`V-权限-04` 虽声明为纯 L2 真库，但其"同步未确认前用户状态不为 active"横跨 S-C-02 与 Epic D，本次未找到独立命中的真库断言，保守未翻转。

### 银河权限落库与匹配

由 [#17](https://github.com/Moshuiwang/lingxi/issues/17) 的 S3 切片认领。本组只约束银河导入、连接与匹配；权限发布由上一组约束。

本组整组是数据正确性断言，其中 `V-银河-01/02/03/10/11/13` 是否定断言（证明系统**不会**按错误的键连接、**不会**猜测未配置的职能、**不会**回显人员数据）。三个连接陷阱来自 [银河用户权限数据结构](../参考证据/银河用户权限数据结构.md)，每条各有一个测试并逐条做过受控变异。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-银河-01 | 三张表一律按 `user_id` 连接；按 `user_role.user_name`（值实测是中文姓名）连接会取到**另一个人**的角色，实现不得使用任何名称列 | L2（纯逻辑） | 已验证 |
| V-银河-02 | 公司范围按 `DATACOUNTRY_ID = sys_country.country_key` 连接；按主键 `id` 连接会取到另一个国家，实现不得使用 `id` | L2（纯逻辑） | 已验证 |
| V-银河-03 | 菜单一律以 `menu_id` 为准；同名不同 `menu_id` 的菜单各算一项，不得按 `menu_name` 归并 | L2（纯逻辑） | 已验证 |
| V-银河-04 | `全非` 哨兵（`country_key=0` / `name=ALL` / `name_cn=全非`）原样落库、不被改写或丢弃；解释层把它展开为「所有国家所有公司」，且哨兵本身不作为一个国家出现在展开结果里；通配**只在哨兵行验证通过**（唯一一行 `country_key=0` 且 `name=ALL`、`name_cn=全非`）时展开，哨兵缺失或损坏一律失败关闭（`0` 记入 unresolved、不给任何范围），显式路径永不把哨兵行当国家返回 | L2（纯逻辑）+ L2（真库） | 已验证 |
| V-银河-05 | 校验失败或写入中途失败时，五张表与批次表**一行都不写**（不留半批） | L2（真库，注入写入失败） | 已验证 |
| V-银河-06 | 同一份导出（内容摘要相同）重复导入不产生第二份数据，返回既有批次——**包括其批次已被更新导出取代（`superseded`）之后**：旧导出重导不得写出第二份全量、更不得把权限快照回退，回滚须显式操作；只有 `failed` 批次允许同摘要重来。新导出导入后旧的完成批次转 `superseded`，当前有效批次唯一（导入全程持 advisory 锁串行化），且**过期批次不作为当前有效批次返回**；内容恰与历史批次相同的**真实新导出**（连续刷新无变化、权限 A→B→A）经 `--confirm-unchanged` 显式确认后按新批次导入并成为当前有效——摘要不是导出身份，默认路径只防误重导 | L2（真库） | 已验证 |
| V-银河-07 | 写入后按批次回读逐表计数，与校验阶段的行数不一致即整批回滚，批次不得标记为 `complete` | L2（真库，注入回读缺行） | 已验证 |
| V-银河-08 | `sys_user_datacountry` 的大写源列名与规范化列名都被导入器接受，落库列名唯一且全小写 | L2（纯逻辑）+ L2（真库） | 已验证 |
| V-银河-09 | 匹配只有 `matched` / `not_found` 两个用户级结果，按「飞书 `user_id` → 花名册人员ID → 工号（主）/邮箱（辅）→ 银河 `user.user_name` / `user.email`」判定（[花名册身份链与工号邮箱匹配决策](../决策记录/2026-08-05-花名册身份链与工号邮箱匹配.md)）：只有原始身份行唯一且工号或回退邮箱唯一、一致命中才 `matched`；零条、多条（含字段相同重复行）、双键冲突、资料缺失均 `not_found`，但 `reason` 必须区分具体失败。是 `V-开通-02/03/06/09` 的纯函数层 | L2（纯逻辑） | 已验证 |
| V-银河-10 | `nick_name` 不参与匹配判定：改变或清空全部 `nick_name` 不改变结果，它只作为内部诊断信息输出，不用于选择候选 | L2（纯逻辑） | 已验证 |
| V-银河-11 | 角色职能标签只来自配置文件；配置未覆盖的角色返回 `function=None`，**不做「去掉开头的 A」这类猜测**；APP 角色和「A海外本地员工营业厅」不得被映射。本断言只覆盖精确映射与未映射标记；“全部未映射时用户级拒绝”分属两处：**刷新侧**已由 `V-权限-08` 的刷新侧半边认领（`no_supported_function` 走撤权那一路，见本文件 `V-权限-08` 一节），**开通侧**（首次开通时给未授权提示、不创建用户环境）仍未认领，属 Epic D 与 #17/#89/#65 的后续接线 | L2（纯逻辑） | 已验证 |
| V-银河-12 | `sys_country.boss_company_id` 落库保留，且在列注释中登记为「对 MCP 申请权限的公司字段」 | L2（真库） | 已验证 |
| V-银河-13 | 校验结论只含表名、列名、行号与计数，**不回显**姓名、邮箱、账号等人员数据值 | L2（纯逻辑） | 已验证 |

**本组不认领的相邻断言**（各自的状态见[开通与匹配](#开通与匹配)表的状态列）：`V-开通-01/02/03/06/09` 涉及 `app_user` 真库写入以及“确定性匹配失败不产生 `manual_review`”的真库行为；本组只交付它们上游的纯函数判定层。

**2026-08-26 对账（#100）翻转为已验证**（全组，#17，commit `0c469be`，`V-银河-09`/`10` 另见 commit `86fdb91`）：
- `V-银河-01`/`02`/`03` → `test_galaxy_scope_join.py::GalaxyUserIdJoinTest`/`GalaxyCountryKeyJoinTest`/`GalaxyMenuIdTest`
- `V-银河-04` → `test_galaxy_scope_join.py::GalaxyAllAfricaSentinelTest` + `test_galaxy_import_postgres.py::test_all_africa_sentinel_row_is_stored_verbatim`
- `V-银河-05` → `test_galaxy_import_postgres.py::test_validation_failure_writes_no_batch_and_no_row` + `test_failure_during_write_leaves_no_half_batch`
- `V-银河-06` → `test_galaxy_import_postgres.py::test_repeated_import_of_the_same_digest_is_idempotent` + `test_a_superseded_digest_is_still_idempotent_and_never_rolls_back` + `test_a_confirmed_unchanged_export_becomes_a_fresh_current_batch`
- `V-银河-07` → `test_galaxy_import_postgres.py::test_readback_mismatch_aborts_the_batch`
- `V-银河-08` → `test_galaxy_import_postgres.py::test_uppercase_source_columns_land_in_normalized_columns`
- `V-银河-09` → `test_galaxy_account_match.py`（matched/not_found/reason-code 全套用例）；commit `86fdb91`
- `V-银河-10` → `test_galaxy_account_match.py::GalaxyAccountNickNameIsAdvisoryOnlyTest`；commit `86fdb91`
- `V-银河-11` → `test_galaxy_role_function.py::RoleFunctionMappingTest`/`RoleFunctionMapDocumentTest`/`ShippedRoleFunctionMapFileTest`
- `V-银河-12` → `test_galaxy_import_postgres.py::test_boss_company_id_is_kept_and_registered_as_the_mcp_company_field`
- `V-银河-13` → `test_galaxy_export_validation.py::GalaxyExportIssueRedactionTest`

### 交付与隔离

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-交付-01 | 交付请求体中伪造的 `user_id` 被忽略，归属以 `SO_PEERCRED` 的 uid 为准 | L1/L2（本地 socket 测试） | 未认领 |
| V-交付-02 | `source_path` 含 `..`、符号链接或越界绝对路径时被拒绝 | L2（单测） | 未认领 |
| V-交付-03 | 授权 API 未返回成功时，交付状态为 `uncertain` 而非 `sent`，且不自动重发 | L2（真库 + 回放）+ L4a | 已认领 |
| V-交付-04 | 正式产物只有在已批准的交付路径上创建、归属与访问控制均完成真实读回后才记为成功；当前路径不可用或归属不满足时失败关闭，不以聊天正文冒充正式产物 | L4a（真实平台创建 + 权限回读） | 已认领 |
| V-交付-05 | 相同 `idempotency_key` 的重复交付请求不产生第二份产物 | L2（真库） | 已认领 |
| V-交付-06 | **飞书凭据**不出现在用户 shell、环境变量、进程参数、家目录、Agent JSONL 和日志中 | L2（模式扫描） | 未认领 |
| V-交付-07 | 用户环境里的问数 MCP 令牌**只有该用户自己那一份**（两个用户的配置互不含对方令牌），且落盘纪律成立：`.mcp.json` 权限恰为 `440`、家目录恰为 `700`、**根目录恰为 `750`**（不得停在 umask 默认值，也不得对属组可写）、根目录路径必须绝对；写入经同目录临时文件 + `fchmod` + `os.replace`（**临时文件在带上令牌的那一刻起就不宽于目标**，磁盘上不存在权限更宽的中间态）；内容逐字节相同即不重写但仍收紧权限；令牌明文、路径与文件内容**不出现在**该模块的日志、审计与异常里 | L2（临时目录 + 日志捕获） | 已验证 |

> **`V-交付-06` 于 2026-08-18 按产品负责人 2026-08-17 裁定改写**（[Trace #203](https://github.com/Moshuiwang/lingxi/issues/203)，正文见[《用户环境持有问数 MCP 令牌》决策记录](../决策记录/2026-08-18-用户环境持有问数MCP令牌.md)）：MCP 令牌裁定写进用户环境 `.mcp.json`，「家目录无 MCP 凭据」不再是产品承诺，本机 MCP 代理方案作废；补偿纪律改由 `V-交付-07` 承接。飞书凭据那一半原样保留在 `V-交付-06`。

`V-交付-03`/`04`/`05` 的文档（docx）分支另受 [#408](https://github.com/Moshuiwang/lingxi/issues/408)（markdown 字符剥离层的负号数据正确性缺陷，修复在途）已知缺口约束——该层是文档内容加工专属步骤，本组断言"正式产物创建/权限/送达"本身不受影响，但文档正文内容可能被错误改写；电子表格分支不经过该层，不受 #408 影响（详见下文 2026-08-28 扩展段）。

`V-交付-03`/`04`/`05` 由 [#341](https://github.com/Moshuiwang/lingxi/issues/341)（文档投递触发+建档+持久化检查点+读回判据，S-ES-1/2/3）认领并 L2 达标——本组三条断言描述的"正式产物"正是这里的飞书文档：`V-交付-03` → `apps/gateway/document_delivery.py::DocumentDeliveryConsumer._process_claim` 的白名单反转分类（`test_document_delivery_transport.py::DocumentDeliveryTransportTestCase.test_indefinite_error_is_uncertain_not_failed`，真库断言 `uncertain` 终态且不再被 `claim_pending` 认领；`test_a_deterministic_precondition_valueerror_is_failed_not_uncertain` 补上确定性配置错误的反向对照）；`V-交付-04` → 成功判据要求 `read_members` 真实读回确认目标 `open_id` 具备 `full_access` 才能记 `succeeded`（`test_missing_target_in_read_members_is_uncertain_not_succeeded`/`test_wrong_permission_tier_in_read_members_is_uncertain_not_succeeded`，去掉这道确认判据两条用例均变红）；`V-交付-05` → `task_document_delivery_request.task_id` 的 `UNIQUE` 约束（迁移 0074）+ 建档检查点单独提交（`test_duplicate_task_id_insert_is_rejected_by_unique_constraint`、`test_checkpoint_recovery_never_creates_the_document_twice` 断言 `create_document` 全程只调用一次）。**未达 L4a**：真实飞书 docx 创建/写正文/授权/读回四步留 Bot-Test/Stage（S0 探针已验证单步调用形状，见 `scripts/probe_drive_folder_permissions.py`，但完整链路的真实端到端验证不在本批范围）。

**2026-08-28 扩展（Issue #354 S-H3-2，D2 裁定：同构 #341 文档交付路由）**：`V-交付-03`/`04`/`05` 的"正式产物"用词本身是介质中立的（不限定"文档"），表格分支复用同一张检查点表（`task_document_delivery_request`，迁移 0078 新增 `delivery_type`/`resource_url` 两列）、同一个状态机、同一套白名单反转分类与 `UNIQUE(task_id)` 幂等键，三条断言因此同样覆盖飞书电子表格产物，证据见 `tests/test_sheet_delivery_transport.py::SheetDeliveryTransportTestCase`：`V-交付-03` → `test_write_failure_does_not_send_the_ready_link_and_is_uncertain_not_succeeded`/`test_a_deterministic_precondition_valueerror_is_failed_not_uncertain`；`V-交付-04` → `test_permission_downgrade_is_uncertain_not_succeeded`/`test_missing_target_in_read_members_is_uncertain_not_succeeded`；`V-交付-05` → 同一条 `UNIQUE(task_id)` 约束、`test_checkpoint_recovery_after_a_crash_between_create_and_write_never_recreates_the_spreadsheet` 断言 `create_spreadsheet` 全程只调用一次。真实 API 调用形状由 S-W0-3 探针验证（[#354 评论](https://github.com/Moshuiwang/lingxi/issues/354)，证据等级 6，Bot-Test 真实调用：建表/查 sheet_id/写值/授权/读回/发送/删除全部一次成功），本批（S-H3-2）实现与真库断言测过。**已达 L4a（两轮，[#373](https://github.com/Moshuiwang/lingxi/issues/373)）**：S-H3-4（2026-08-28，`biai-stage` 候选镜像 `pr-403-80a1d29556f2`，受控测试身份非生产）真实端到端建表 `succeeded`、权限回读用户 `full_access`+机器人 `full_access`（所有权留机器人）、用户会话真实收到交付链接、内容逐格正确含短行补齐（无错位交付）；S-H4-3 六击冒烟第 5 击（2026-08-28，最终 main 镜像 `20260828-fac2e9be7406`）复验建表 `succeeded`+权限回读+链接送达，并补齐 `=SUM(1,2)` 公式语义专项探针——ToString 与 Formula 两种渲染视图均返回原文本、未被求值为 3，H3 遗留的 `=` 开头公式语义边界闭合。两轮入站腿均为受控注入（`replay_inbound_event`，真实 DB/worker/投递/出站）；2026-08-29 产品负责人真人从飞书客户端发起的全真实入站验证补齐这一环（问数/文档交付/电子表格/`/memory` 三连全部真实走通，电子表格「无问题」，见 [#373](https://github.com/Moshuiwang/lingxi/issues/373)「PM 回归验证与裁定留痕」评论）。**边界注记**：[#408](https://github.com/Moshuiwang/lingxi/issues/408)（文档 markdown 字符剥离层的负号数据正确性缺陷）明确不影响本段——该层是文档(docx)专属步骤，表格分支从不经过它（产品负责人 2026-08-29 实测表格正常，见 #408 正文），本段 L4a 结论不受 #408 影响。

**2026-08-26 对账（#100）翻转为已验证**：`V-交付-07` → `test_user_environment.py::FileSystemTests`（`test_the_credential_file_is_440_the_home_is_700_and_the_root_is_750` 精确数字、`test_the_temporary_file_is_never_wider_than_the_target`、`test_a_second_call_with_the_same_token_does_not_rewrite_the_file`、`test_the_token_never_appears_in_logs`、`test_a_write_failure_reports_only_an_error_code` 逐句对应）；commit `e50bafd`（#65/#160/#203）。

### 卡片与降级

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-卡片-01 | 卡片更新序列号严格递增，无重复无回退 | L2（单测）+ L4a | 已认领 |
| V-卡片-02 | 单话题更新频率 ≤ 2 次/秒；多话题并发时全局 ≤ 50 次/秒 | L2（令牌桶单测）+ L4a（真实不触发 429） | 已认领 |
| V-卡片-03 | 卡片链路首次失败后停止后续卡片更新，并在**同一话题**最多发送一次经过可信输出检查的文本终态；不得同时形成卡片终态与重复文本终态 | L2（注入失败）+ L4a | 已认领 |
| V-卡片-04 | 卡片状态区不出现内部工具名、过程日志或预计剩余时间 | L2（输出断言） | 已验证 |
| V-卡片-05 | 等待卡片的状态文案在长任务中途语义化：工具调用命中问数查询前缀时渲染"正在第 N 次查询指标数据"、其它信号（模型文本输出/非问数工具调用）渲染"正在整理与生成回答"，两次模型输出之间超过 30 秒无任何新信号时由兜底计时强制推一次纯用时更新，不让卡片长时间完全静止在默认文案；进度事件密集时按 ≥5 秒节流合并，不逐条写库；文案不回显工具名、参数或任何查询内容（Issue #321 方向 C，产品负责人 2026-08-27 裁定） | L2（数据管路 + 节流判据 + 真实 `_monitor` 循环，虚拟时钟） | 已验证 |

**2026-08-26 对账（#100）翻转为已验证**：`V-卡片-04` → `test_content_catalog.py::UserVisibleOutputTests::test_output_checker_rejects_internal_tools_process_logs_and_remaining_time` + `test_card_output_checker_also_rejects_injected_internal_terms`（卡片路径专用）；commit `45b02e8`（#101）。`V-卡片-01/02/03` 均声明 L2+L4a，未补验，不动。`V-卡片-05`（Issue #328 opus 审查 P2-2 补 Gateway 侧解码集成用例）→ `test_worker_queue_consumer.py::SemanticProgressTests`（worker 侧编码、节流、30 秒兜底四用例）+ `test_gateway_delivery.py::HappyPathCardDeliveryTests::test_progress_content_decodes_to_the_semantic_status_text`（Gateway 侧 `decode_progress_action` 解码集成，真库）。真实 CardKit 渲染留 L4a，未取得。

### 问数结果投递与正文生命周期

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-投递-01 | Worker 按任务严格递增地持久化 `started`、`progress`、`safely_releasable_answer`、`terminal` 事件；任何含正文事件在入库前已经通过包含跨分片边界的可信输出检查，原始流片段不进入数据库、Outbox 或日志 | L2（真库 + 分片变异） | 已验证 |
| V-投递-02 | `terminal`、任务等待态与话题占用释放在同一事务提交；Gateway 只消费已提交事件，并能从最后已确认序号恢复，重启与重放不产生重复终态 | L2（真库 + 故障注入 + 重启回放） | 已验证 |
| V-投递-03 | 只有飞书权威发送接口明确受理并返回可回读的消息或卡片标识才记为 `platform_received`；该状态不冒充 `user_read`，后者未知；超时或响应不明确记为 `uncertain` 且不自动重发 | L2（真库 + 回放 + adapter 分类白盒）+ L4a | 已认领 |
| V-投递-04 | 问数业务结果与投递结果分别持久化和展示：业务成功不因投递不确定而改写为业务失败，业务失败也不因平台受理而改写为成功 | L2（真库 + 状态组合测试） | 已验证 |
| V-投递-05 | 最终形态已达 `platform_received` 后，安全结果正文不按固定 `expires_at` 清理，而是转为随当前飞书会话保留：正文在会话存续期间可持续存在于会话上下文（Agent 会话文件、聊天上下文副本）与 Outbox 等运行位置；触发 `/new`、会话空闲满两小时的到点主动清理、用户停用感知或权限变化感知中任一清除边界时，统一、全部、不可逆清除全部位置的正文副本，不逐位置单独判断或延后，也不提供清除后的后台恢复；停用与权限变化两类边界感知即触发，不依赖下一次任务入队 | L2（真库 + 会话边界状态机测试）+ L4a（真实发送 + 会话内多轮读取 + 分别触发四类清除边界后核对不可恢复） | 已认领 |
| V-投递-06 | 待投递、失败、`uncertain` 或尚未证明清除后可访问的正文最迟 24 小时清除；到期记为 `delivery_expired` 并释放话题，之后只在该用户下一条主动飞书消息上提示重新提问，不主动推送、不自动回放旧答案；该 24 小时上限只适用于 `platform_received` 之前，不随方案 B 变化 | L2（注入时钟 + 真库）+ L4a | 已认领 |
| V-投递-07 | 任务状态、时间戳、错误分类、去重键与投递回执等低敏事实最长保留 90 天；备份或 WAL 中无法逐条即时删除的内容（含尚未结束的会话保留窗口）按原写入时间计算到期，恢复后在重新提供服务前完成补清理，不把恢复时间当成新的保留起点，也不借恢复重新打开已经结束的会话窗口 | L2（`ImmutableExpiryTriggerTest` 到期由原始写入时间派生、UPDATE 不可后移）+ L4a（2026-08-28 biai-stage 隔离实例真实备份恢复演练，受控测试身份非生产，见 PR #391） | 已认领 |
| V-投递-08 | 会话存续期内，结果依赖型追问直接读取仍保留的安全结果正文给出答案，不重新调用 MCP、不猜测口径；触发任一会话清除边界后，同一话题的下一次追问按新会话处理，系统不重建、不猜测旧答案，只提示用户重新提问 | L2（真库 + 状态组合测试）+ L4a（真实多轮追问 + 跨清除边界追问） | 未认领 |
| V-投递-09 | 会话窗口清除 Lingxi 侧全部正文副本后，目标用户仍可从同一飞书会话直接访问此前已送达的飞书卡片或消息本身（经 G-CARD 验证的交付形态）；该访问不依赖 Lingxi 后台保留的任何正文副本，也不是 Lingxi 提供的“找回”入口 | L4a（真实发送 + 会话窗口清除 + 目标用户同会话回读） | 未认领 |
| V-投递-10 | 会话空闲满两小时后，即使用户未再发起新的问数任务，安全结果正文也由定时清理机制在到点时主动清除，不依赖下一次任务入队；用户停用或权限变化一经感知立即清除该用户已送达的结果正文，含停用前已开始任务继续完成交付后产生的正文（交付完成后随即清除，不再保留） | L2（真库 + 定时任务模拟时钟推进 + 停用中任务交付后状态断言）+ L4a（真实空闲等待到点验证 + 真实停用场景验证） | 已认领 |

**`V-投递-07`（S-H2-5，2026-08-28）翻为「已认领」，覆盖范围如实登记如下**：断言中「按原写入时间计算到期，恢复不重置保留起点」这条不变式，当前**唯一**已落地的实现是迁移 `0054_retention_cleanup` 给 `galaxy_import_batch`/`feishu_org_sync_run` 建立的不可后移 `expires_at` 触发器 + 受限清理函数（`src/lingxi/adapters/retention.py`）——L2 由 `tests/test_retention_postgres.py::ImmutableExpiryTriggerTest`（到期由 `started_at` 派生、UPDATE 与改来源时间均被拒绝/覆盖）与 `CleanupFunctionContractTest`（清理只认 `expires_at <= p_now`，与「何时恢复」无关）真库承载；`test_an_update_cannot_postpone_the_expiry` 已做变异验红（改坏触发器只在 INSERT 时派生到期时间 → 该用例变红 → 恢复原状复绿，见 PR #391）。L4a 是本 Story 在 `biai-stage` 对 `lingxi-test-db` 做的一次真实只读 `pg_dump` → 隔离实例恢复 → 注入固定化名的过期/未过期合成行 → 用当前 scheduler 镜像的 `PostgresRetentionCleaner`（真实代码路径，非手写 SQL）补跑一次清理 → 核对过期行被删、未过期合成行与全部真实恢复行（`feishu_org_sync_run` 9 行）逐行保留 → 销毁隔离实例，详见 PR #391 演练记录。**明确不在本次覆盖范围内**：断言原文点名的「任务状态、时间戳、错误分类、去重键与投递回执」（`task`/`task_delivery_event`/投递 outbox 等表）目前没有对应这条 90 天低敏事实机制的实现，它们各自的清理窗口更短、按另一套语义运作（`V-投递-05`/`06`/`10`）；「不借恢复重新打开已经结束的会话窗口」这半句本次演练未涉及会话状态机，未验证。两者留待覆盖到对应表时再各自翻转。

`V-投递-05`/`06` 的「已认领」覆盖 Outbox 与 Agent 会话 JSONL 两个位置的清理（#153 常驻 Worker 装配）；断言原文的在线临时文件、进程恢复材料两类尚无对应产物，登记待后续 Story 补齐。

**第四个触发点（PR #173 P2-4 修复）**：`agent_session_id` 被覆盖时旧值现在也会排队清理（`reason='session_overwritten'`），此前话题闲置未满两小时被新任务覆盖时旧 JSONL 会永久留在磁盘上不进任何清理候选集，现已修复。

**Agent 会话 JSONL 物理清理**（`apps/worker/session_cleanup.py`）：三个触发点各自排队（迁移 0061），真正删除延后到 Worker 周期性收口执行；按**文件名**而非目录路径匹配（per-user 工作目录隔离尚未实现），代价是把"CLI 文件名约定"列为未经 L4a 验证的假设；停用/权限变化感知的触发方仍待建立，`clear_delivered_content_for_user` 是冻结接口。

**`V-投递-02` 覆盖范围扩展（#178，红线修复）**：断言此前只在 worker 正常收口时成立，`PostgresTaskQueue` 三条"系统代为收口"路径此前完全绕开 outbox（S-A-07 r3 真实验收证实用户因此永远收不到终态）；修复后四条收口路径统一复用同一份终态写入，断言现在对全部路径成立。真库证据见 `tests/test_worker_queue_consumer.py::RealQueueTerminalTests`。

`V-卡片-01`/`02`/`03`/`V-投递-03` 由 [#152](https://github.com/Moshuiwang/lingxi/issues/152)（Gateway 投递消费循环）认领并 L2 达标：真实 PostgreSQL + 可注入传输层验证了序列递增、限流、失败降级文本通道、`platform_received` 获得路径与崩溃恢复语义。**未达 L4a**：真实 CardKit 载荷、真实飞书发送与限流响应留 Bot-Test/Stage。`V-投递-06` 后半句同由 #152 落地并 L2 验证。`V-投递-08`（结果依赖型追问读侧）不在 #152 范围，保持未认领。

`V-卡片-02` 补充：终态更新+关闭刻意不经过单话题节流（结果被节流吞掉更糟），但全进程 50 次/秒预算仍对这两次调用计数。

`V-卡片-03` 补充（独立审核 P1-2）：更新失败时整体降级文本兜底；更新已成功后仅关闭失败不再触发文本兜底——依据是 G-CARD 已登记的平台 10 分钟自动关闭事实，关闭失败不构成结果丢失。

`V-投递-03` 三轮独立审核修复（B-1/R-1/N-3，2026-08-14～17）收敛为：`adapters/feishu_delivery.py` 用白名单专用异常 `DeliveryRejected` 标记服务端明确拒绝，其余任何异常（网络类、JSON 解析失败、响应缺可回读标识等）一律归"结果不明"，不重发、不降级、转入 `uncertain` 告警路径。`tests/test_feishu_delivery_classification.py` 用 25 条用例覆盖五个外发点的 20 种失败响应形状，并经三次变异验红确认分类边界。断言原文余下的真实平台受理、`user_read` 窗口仍是 L4a 未取证项。

**G-CARD 补充引用（独立审核 P2-5）**：流式卡片距上次开启 10 分钟后被平台自动关闭；恢复后若已被关闭，`update`/`close` 会失败并整体降级为文本兜底——已知体验代价，未消除，留待 Bot-Test/Stage 验证。

**2026-08-16 Epic A/S-A-07 受控真实验收（[#154](https://github.com/Moshuiwang/lingxi/issues/154)）：本组一条都未翻转，但真实证据不再空白。** 已在真实链路取得：①CardKit 载荷与真实发送成立，无重复无降级；②`V-卡片-03` 三分支经受控注入开关各跑一次，结果与设计一致；③`V-投递-03` 的 `uncertain` 半句在双路径失败下成立；④`V-投递-06` 的到期收敛与提示成立（隔离库构造，未真实等满 24 小时）；⑤`V-投递-10` 前半句（空闲两小时清理）成立。

**未翻转的理由**：`V-卡片-01`/`02` 序列号与限流阈值无真实观察点；`V-卡片-03` 的"可信输出检查"未回读判定事实；`V-投递-06` 未真实等满 24 小时；`V-投递-10` 后半句（停用/权限变化触发清除）因无可授权测试账号按"不适用（环境能力边界）"处理；`V-投递-05` 仅覆盖空闲两小时一类边界；`V-投递-01`/`02`/`04` 无故障注入对照；`V-投递-08` 已观察到追问正确收窄但仍未认领。

**2026-08-26 对账（#100）翻转为已验证**：
- `V-投递-01` → `test_delivery_outbox.py::SequenceAndIdempotencyTests`（类文档字符串自述"V-投递-01"，严格递增）+ `test_worker_queue_consumer.py::test_withheld_output_writes_redacted_withheld_terminal_not_success`（安全检查未过不落库）；commit `8ba8882`（#151）
- `V-投递-02` → `test_worker_queue_consumer.py::RealQueueTerminalTests`（矩阵正文已直接点名，核实存在）+ `test_delivery_outbox.py::TerminalEventTests`；commit `e00ef66`
- `V-投递-04` → `test_delivery_outbox.py::DeliveryResolutionTests`（类文档字符串自述"V-投递-04"）::`test_confirm_delivery_keeps_business_failure_not_upgraded_by_successful_delivery`；commit `8ba8882`

`V-投递-03/05/06/10` 均声明含 L4a，未补验，不动。

### 内容目录与正式展示入口

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-内容-01 | 默认内容目录版本存在，文案键与卡片键分别和登记表精确相等；缺少或多出键时在渲染前失败 | L2（目录校验单测） | 已验证 |
| V-内容-02 | 仅修改内容目录中的展示文本即可被正式日报渲染入口读取，不改变业务逻辑代码 | L2（可注入） | 已验证 |
| V-内容-03 | 文案和卡片占位变量必须双向精确匹配；少给、多给或拼写不符时失败，且失败信息不回显变量值 | L2（渲染单测） | 已验证 |
| V-内容-04 | 卡片配置只允许 `title`、`body`、`button_labels`；写入 `action`、`url`、`button_type`、`permission` 等动作或权限字段时失败 | L2（配置校验单测） | 已验证 |
| V-内容-05 | `pipeline`、`first_contact`、`roster_report` 三个正式渲染入口不含用户可见的固定中文字符串 | L2（源码扫描） | 已验证 |
| V-内容-06 | 导入三个正式渲染入口后的传递依赖闭包不包含 Bot-Test 资产模块（含 `lingxi.core.identity.onboarding` 等；`lingxi.adapters.oauth_bridge` 已随 #203 清退删除，不再纳入本条检查）；经中间模块一跳导入时断言必须变红 | L2（干净解释器导入图 + 变异） | 已验证 |
| V-内容-07 | 用户可见输出检查器拒绝内部工具名、过程日志、预计剩余时间及“权限不足”“无权限”类措辞；不安全内容配置失败关闭。**本条适用范围限于固定模板与非模型正文**；模型生成的终态正文按 [#322](https://github.com/Moshuiwang/lingxi/issues/322) 产品负责人 2026-08-26 裁定收窄，出口只保留协议泄漏防线（`config/content.py` 的 `_PROCESS_MARKER_PATTERN`），本条的自然语言固定词表不再适用于模型正文——见 `tests/test_content_catalog.py::ModelGeneratedTextValidationTests` | L2（输出断言） | 已验证 |

**2026-08-26 对账（#100）翻转为已验证**（全组，commit `45b02e8`/`61f8ae5`，#101）：
- `V-内容-01` → `ContentDirectoryTests::test_default_catalog_has_exact_registered_keys_and_a_version` + `test_missing_or_unregistered_text_key_fails_before_rendering`
- `V-内容-02` → `test_a_content_only_change_changes_rendered_text_without_code_change` + `test_a_content_only_change_reaches_a_formal_renderer`
- `V-内容-03` → `test_placeholder_variables_must_match_in_both_directions`
- `V-内容-04` → `test_card_configuration_is_display_only` + `test_card_configuration_rejects_a_button_type_or_url`
- `V-内容-05` → `test_formal_renderers_do_not_embed_chinese_user_text_literals`
- `V-内容-06` → `test_formal_renderers_do_not_import_bot_test_assets_transitively`；commit `61f8ae5`
- `V-内容-07`（模型正文 #322 收窄部分，本周新增/今日裁定，已重点核实）→ `test_content_catalog.py::UserVisibleOutputTests` + `ModelGeneratedTextValidationTests`（矩阵正文直接点名此类；逐条核实：日常措辞模型正文可正常渲染 `test_model_final_answer_with_everyday_wording_delivers_through_the_result_card`/协议泄漏防线仍拒绝 `test_model_text_still_faces_the_protocol_leak_defense`/非模型固定模板仍用旧词表 `test_calls_without_the_model_text_flag_still_reject_the_fixed_marker_word_list`，三个用例分别对应断言三个分句）；commit `c42bb75`（#322 根因修复），已确认为 main 祖先提交

### 审计与保留

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-审计-01 | 普通业务与 scheduler 身份对 `audit_event` 的直接 `UPDATE` / `DELETE` 被拒绝；只有受限清理函数可删除已经到期的行 | L2（真库） | 未认领 |
| V-审计-02 | 新增字段默认**不**进入审计明细，必须显式加入白名单 | L2（脱敏机制单测）——防止未来新字段静默泄露 | 已验证 |
| V-审计-03 | 审计明细中不出现凭据、完整令牌。由三道合成：结构化入参按字段白名单收录、凭据类键名不落原值、**字段名本身也按自由文本脱敏**；自由文本（错误原文、回执正文、畸形工具名、白名单字段的字符串值）另加与键名无关的长度上界——含数字的 16+ 字符连串、或 32+ 字符的任意连串一律抹除 | L2（模式扫描 + 否定断言） | 已认领 |
| V-审计-04 | `chat_message`、`audit_event`、`task.prompt`、Agent JSONL、`manual_review.detail`、`pending_action` 内容、发布/同步详情、`inbound_event` 和 `delivery` 运行记录均在各自 `expires_at` 到达时删除或不可逆脱敏；其他未到期内容完好。清理函数固定 `search_path = pg_catalog` 且目标对象全限定，伪造调用方搜索路径不能改删其他对象 | L2（真库 + 文件系统，注入时钟与伪造 schema） | 未认领 |
| V-审计-05 | 开通前发送的内容不写入 `chat_message`，且不在任何回复中回显 | L2（真库）+ L4a | 已认领 |
| V-审计-06 | 月分区边界、清理失败或 scheduler 延迟均不会把可识别内容的到期时间后移；发现到期残留时告警并阻止发布候选。`PUBLIC` 与非 scheduler 身份不能调用清理函数，scheduler 只获精确函数签名的 `EXECUTE` 且无分区 DDL 权限 | L2（真库，注入时钟、失败与角色切换） | 未认领 |
| V-审计-07 | 数据库迁移在 PostgreSQL 16 真库实际执行成功；跨时区样例的 `expires_at` 与来源时间相差固定 2160 小时，调用方不能自定义或后移到期时间 | L2（PostgreSQL 16 真库） | 未认领 |

**V-审计-03 的残余缺口必须写在这里，不许再声称它是绝对保证。** 产品合同第 109 / 121 行用的是绝对措辞，但实现已被证明做不到绝对——下面两类完整凭据仍会原样进入审计明细：

| 残余缺口 | 为什么不堵 |
|---|---|
| **纯字母、短于 32 字符的裸秘密**出现在错误正文、回执正文或工具名里 | 这三处不经过字段白名单，长度上界是唯一可用的规则，而它认不出纯字母短串。曾经写过"这一类由字段白名单承担"——**那句话是假的闭合**，字段白名单根本不覆盖自由文本 |
| **模型把凭据当成合法形态的工具名发出来**（`sk-live-...` 符合 `[A-Za-z0-9_.-]+`） | 对拒绝式白名单来说，"模型试图调用什么"是最重要的一条审计事实。把未知工具名投影成哈希或长度能堵住这条，但会让屏障的留痕失去用处——**代价大于收益，故意不堵** |

白名单字段里的**数字值**同样原样保留，这不是缺口而是设计：字段进白名单意味着已显式批准记录它，epoch 毫秒与雪花 ID 正是排障要看的事实。若某字段可能承载凭据，正确做法是不把它加进白名单。

上述残余缺口是产品负责人知情接受的当前边界，不得自行放宽。`V-执行-20` 只收紧 worker 出口，审计核心两条缺口仍存在；形成经过见[形成记录](../参考证据/验证与门禁形成记录.md)。

以上九十天断言和单用户删除断言只覆盖正常运行库与在线文件；数据库备份、WAL 归档和用户目录备份按产品决定明确排除，不要求删除编排修改灾备副本。灾备保存、恢复和销毁由公司的灾备与恢复规则另行验收。

**2026-08-26 对账（#100）翻转为已验证**：`V-审计-02` → `test_execution_tool_gate.py::ToolFailureAuditTest::test_audit_input_fields_are_opt_in_and_credential_like_keys_are_redacted`；commit `89cf05b`。`V-审计-03` 保守保持已认领——断言本身声明"不出现凭据、完整令牌"，但本文件已详细自述已知残余缺口（纯字母短于 32 字符裸密钥、伪装成工具名的凭据），标"已验证"会与紧邻的残余缺口说明冲突，故不翻转。`V-审计-05` 声明含 L4a，未补验。

### 数据保留清理

`V-审计-04/06/07` 覆盖的对象大多还未建表，仍是「未认领」。本组只覆盖已存在的两张表（`feishu_org_sync_run`、`galaxy_import_batch`）上的保留执行机制，全部由 [#54](https://github.com/Moshuiwang/lingxi/issues/54) 认领。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-保留-01 | 两张父表中 `expires_at <= p_now` 的行被删除；未到期行**逐字段**未变；`feishu_delegated_subject` 与 `app_user` 行数分毫不动 | L2（真库） | 已验证 |
| V-保留-02 | 一次调用同时处理两张父表，各自返回自己的删除数；只实现一张时另一张恒为 0 | L2（真库） | 已验证 |
| V-保留-03 | 边界逐点：`started_at + 2159:59:59` 不删、恰好 `+2160h` 删、`+2160h+1s` 删（`<=` 语义） | L2（真库） | 已验证 |
| V-保留-04 | 清理条件与 `current_batch_id()` 的过期过滤严格互补：不存在"既是当前有效批次、又可被清理"的瞬间；清理前后当前批次不变、其子行不变 | L2（真库） | 已验证 |
| V-保留-05 | 跨夏令时时区里 `expires_at - started_at` 恰为 2160 绝对小时，且**不等于** `started_at + interval '90 days'` | L2（真库） | 已验证 |
| V-保留-06 | 8 张子表逐表随父行消失、无孤儿子行、未到期批次的子行内容未变；`pg_constraint` 中指向两张父表的外键恰 8 条且 `confdeltype` 全为 `'c'` | L2（真库 + catalog） | 已验证 |
| V-保留-07 | `galaxy_import_batch` 写入触发器：调用方传入的 `expires_at` 被覆盖为 `started_at + 2160h`，UPDATE 不能后移、不能改来源时间；`feishu_org_sync_run` 既有同型行为不变 | L2（真库） | 已验证 |
| V-保留-08 | 迁移前写下的偏离行被回填校正为 `started_at + 2160h`；既有代码路径写出的行本来就精确满足该不变式 | L2（真库） | 已验证 |
| V-保留-09 | 批量上限生效：`p_limit` 恰好限住删除数；超大值被夹取到函数自身上限；非正值被拒绝 | L2（真库） | 已验证 |
| V-保留-10 | 幂等：循环调用至返回 0 时总删除数等于到期行数，再调一次返回 0 且不报错 | L2（真库） | 已验证 |
| V-保留-11 | 拒绝晚于当前时间的 `p_now` 且零删除；到期时间晚于传入 `p_now` 的行不删；函数定义保留 `LEAST(p_now, clock_timestamp())` 下限 | L2（真库 + 定义检查） | 已验证 |
| V-保留-12 | 函数固定 `search_path = pg_catalog` 且目标对象全限定；调用方把同名影子表放进搜索路径时，影子表分毫未动、真表照删 | L2（真库，伪造搜索路径） | 已验证 |
| V-保留-13 | 函数 `SECURITY DEFINER` 且属主为 `lingxi_retention_owner`；`PUBLIC` 与 `lingxi_app` 无 `EXECUTE`、`lingxi_scheduler` 按精确签名可调；`lingxi_app` 与 `lingxi_scheduler` 都不能直接 `DELETE` 目标表、也不能取得属主角色；四个角色随迁移幂等建立且均无登录能力；越权授出的属主成员资格在重新应用迁移时被收回 | L2（真库，切换会话身份） | 已验证 |
| V-保留-14 | 返回摘要只含表名、删除数与到期时间范围；摘要与由它产生的日志不含任何人员数据字段值 | L2（真库） | 已验证 |
| V-保留-15 | `lingxi-scheduler` 进程按周期实际调用清理（观察点为到期行真的消失）；职责之间失败隔离，一个职责抛异常时另一个本轮仍执行；连续失败不退出进程且日志不含用户内容 | L2（真库 + 可注入） | 已验证 |
| V-保留-16 | 清理失败只能重试，不会把任何行的 `expires_at` 或 `started_at` 往后挪 | L2（真库 + 可注入） | 已验证 |
| V-保留-17 | 收到 `SIGTERM` 后不再领取新的清理批次（`calls_after_stop == 0`），且库中不存在没有父行的子行；进程报告的删除数与库里实际少掉的行数一致 | L2（真实进程 + 真实信号 + 真库） | 已验证 |
| V-保留-18 | 迁移在 PostgreSQL 16 真库前滚成功；回滚干净移除本次新增的函数与触发器、不删除数据、不删除集群级角色；前滚与回滚往返两次成功；不含本次改动的既有写路径在升级后的库上仍然成功 | L2（真库） | 已验证 |
| V-保留-19 | 批次被回收后，同一份导出可以重新导入，得到新批次、重新起算的 `expires_at`，并成为当前有效批次 | L2（真库） | 已验证 |
| V-保留-20 | 豁免只由 `expires_at <= p_now` 表达：全部批次均已过期时连最近完成的那个也被删除；组织快照侧的可观察状态因此由 `STALE` 变为 `UNAVAILABLE`（两者都不建档、不产生候选） | L2（真库） | 已验证 |
| V-保留-21 | 两张父表的 `BEFORE DELETE` 行级防线按**双条件**放行——`expires_at` 已到期**且** `current_user = 'lingxi_retention_owner'`。四象限逐格：属主×到期=成功、属主×未到期=拒、非属主×到期=拒、非属主×未到期=拒；误授 `lingxi_app` DELETE 后连过期行也删不掉。`TRUNCATE` 不经行级触发器，由权限挡：没有任何 `lingxi_*` 角色持有它 | L2（真库，切换会话身份） | 已验证 |
| V-保留-22 | 并发持锁时清理在有限时间返回（函数级 `lock_timeout`），被锁的表整批让路、行未被删、下一轮收走；摘要**区分**「让路」与「本来就没有到期行」（两者删除数都是 0），scheduler 把让路的一轮记为 warning 而非正常完成；并发两次清理的摘要之和不超过实际删除量 | L2（真库 + 并发连接） | 已验证 |
| V-保留-23 | 预建同名角色被强制校验：超级用户身份响亮失败；属主角色的 LOGIN 被收紧；属主角色不得（直接或经中间角色**间接**）与业务角色互相取得权限。属主移交前后成员关系三元组（admin / inherit / set）与 schema 授权面完全一致 | L2（真库 + CREATEROLE 身份） | 已验证 |
| V-保留-24 | 迁移在**非超级用户**（`CREATEROLE`，生产托管方形态）身份下可完整执行：首次 upgrade 与两轮 downgrade→upgrade 均成功，函数属主、`EXECUTE` 授权与成员关系还原逐项正确 | L2（真库，非超级用户执行身份） | 已验证 |
| V-保留-25 | 回滚后不残留本 revision 授出的任何权限：两张父表上四个角色的五类表权限、schema `public` 的显式 ACL 条目、属主角色的成员关系均已收回 | L2（真库） | 已验证 |

**`V-保留-21` 的双条件是编排者裁定的结果**：只按到期时间判会让误授权限删除过期行，只按角色判属主自己能删未到期行，故取双条件；运维紧急删除路径见 [migrations/README.md](../../migrations/README.md)。

**`V-保留-11` 第三分句是源文本断言，非行为断言**：`LEAST(p_now, clock_timestamp())` 是第二道闸，当前与直接用 `p_now` 行为一致，价值只在校验被放宽那天兑现，如实标注为文本检查。

**`V-保留-20` 的两条后果是知情接受的产品选择**（[Issue #54 编排者裁定](https://github.com/Moshuiwang/lingxi/issues/54) ②④）：九十天是硬上限，删行不改变下游可观察结果；[#69](https://github.com/Moshuiwang/lingxi/issues/69) 决定不为临时导出加永久墓碑，`V-保留-19` 固化该行为。

**2026-08-26 对账（#100）翻转为已验证**（全组 25 条，#54，主提交 `580d4cc`，逐条精确对应 `tests/test_retention_postgres.py`）：
- `V-保留-01`/`02` → `ExpiryCollectionTest::test_expired_rows_in_both_parent_tables_are_removed_in_one_call` + `test_a_row_that_expires_one_second_later_is_untouched_field_by_field`
- `V-保留-03` → `ExpiryCollectionTest::test_the_expiry_boundary_is_inclusive_and_exact_to_the_second`
- `V-保留-04` → `ExpiryCollectionTest::test_the_cleanup_condition_and_the_current_batch_filter_are_complementary`
- `V-保留-05` → `ExpiryCollectionTest::test_the_window_is_2160_absolute_hours_not_ninety_calendar_days`
- `V-保留-06` → `CascadeBoundaryTest::test_exactly_eight_foreign_keys_point_at_the_parents_and_all_of_them_cascade` + `test_each_child_table_is_emptied_by_its_own_cascade_edge`
- `V-保留-07` → `ImmutableExpiryTriggerTest`（5 用例）
- `V-保留-08` → `CleanupFunctionContractTest` 内类文档字符串标注"V-保留-08（验收 D-06）"的回填校正用例
- `V-保留-09` → `CleanupFunctionContractTest::test_a_batch_limit_caps_how_many_rows_a_single_call_removes` + `test_an_oversized_limit_is_clamped_and_a_non_positive_limit_is_refused`
- `V-保留-10` → `CleanupFunctionContractTest::test_repeated_calls_converge_and_then_report_zero`
- `V-保留-11` → `CleanupFunctionContractTest::test_a_future_p_now_is_refused_and_deletes_nothing` + `test_rows_that_expired_after_the_given_moment_are_kept` + `test_the_definition_keeps_the_clock_floor`
- `V-保留-12` → `CleanupFunctionContractTest::test_a_shadow_schema_cannot_redirect_the_cleanup` + `test_a_shadow_temp_type_neither_diverts_nor_breaks_the_cleanup`
- `V-保留-13` → `RolePrivilegeTest`（19 用例，含 security definer/属主/权限矩阵）
- `V-保留-14` → `SummaryContentTest::test_the_summary_carries_table_names_counts_and_a_time_range_only`
- `V-保留-15`/`16` → `SchedulerDutyTest::test_the_scheduler_loop_actually_removes_expired_rows` + `test_a_failing_cleanup_never_postpones_an_expiry`
- `V-保留-17` → `SchedulerDutyTest::test_sigterm_leaves_no_half_deleted_state_and_claims_nothing_after_stopping`
- `V-保留-18` → `MigrationRoundTripTest::test_upgrade_and_downgrade_round_trip_twice` + `test_the_previous_image_still_works_against_the_upgraded_database`
- `V-保留-19` → `ReimportAfterCleanupTest::test_the_same_export_can_be_imported_again_after_its_batch_was_collected`
- `V-保留-20` → `NoUnconditionalReprieveTest`（2 用例）
- `V-保留-21` → `RolePrivilegeTest::test_the_delete_guard_covers_all_four_quadrants`
- `V-保留-22` → `CleanupFunctionContractTest::test_a_locked_row_bounds_the_wait_instead_of_blocking_the_whole_duty` + `test_two_concurrent_cleanups_never_delete_the_same_row_twice` + `BlockedRoundLoggingTest`（区分让路与无到期行）
- `V-保留-23` → `RolePrivilegeTest::test_a_pre_existing_login_owner_role_is_normalised` + `test_an_owner_role_that_inherits_other_privileges_is_refused` + `test_the_handover_restores_the_membership_graph_exactly`
- `V-保留-24` → `RolePrivilegeTest`（`NOSUPERUSER CREATEROLE` 探针角色跑整条迁移链）
- `V-保留-25` → `MigrationRoundTripTest::test_downgrade_leaves_no_privilege_granted_by_this_revision`

### 花名册审计日报

合同「统一用户记录与权限变化」要求 Lingxi **每日比对已开通用户的花名册资料与存档**，变化经受控管理群的审计日报通知管理员人工处理；飞书账号复用换人不做自动拦截，就靠这份日报被发现。全部由 [#52](https://github.com/Moshuiwang/lingxi/issues/52) 认领。

**现行口径以产品负责人 2026-08-08 的 D2 裁定为准**（[#52](https://github.com/Moshuiwang/lingxi/issues/52) 决策登记、[Plan #104 D2](https://github.com/Moshuiwang/lingxi/issues/104#issuecomment-5225625313)）。D2 覆盖了 2026-08-06 的「选项 A + 甲」中的两项：

- **持久快照载体**：花名册读取保存最近一次成功、完整、非空的快照，跨重启与发布都还在；源头异常一律保留上一份、不覆盖。载体是数据库表（`roster_snapshot`/`roster_snapshot_row`，迁移 `0063`），此前「零新表」表述已作废。
- **展示口径**：受控管理群的审计日报允许展示姓名/工号/邮箱原值（否则管理员无法定位处理），仍不得含凭据、令牌或与处理无关的敏感数据，也无可执行入口；`V-花名册-21/22` 按此改写。

**未被 D2 改动的**：比对基线仍是 `app_user` 建档时刻存档的三字段，不是「昨天的快照」；快照只保证花名册值来自一整轮可信读取；`V-花名册-33`（审计日志不含花名册字段值）照旧生效。

**快照的保留期由产品负责人 2026-08-17 裁定**：**始终保留最近一份，豁免九十天规则**；替换即删旧，不积累历史版本；快照因源头长期读不到而超龄时按日报告警提醒、**不自动删除**（`V-花名册-47`，S-B-04 已接线）。

**本节实现分四个已认领切片**（Trace #203/Epic B）：S-B-01 交付分页 reader 与四态判定（`V-花名册-37…40`），S-B-02 交付持久快照载体（`V-花名册-41…46`），S-B-04 交付调度注册与日报接线（`V-花名册-21/22/47/48`）；真实花名册读取与真实群送达仍属 L4a，未做。

**写侧与通知侧在 [#275](https://github.com/Moshuiwang/lingxi/issues/275) 之后是两个互斥职责**（改变了 `V-花名册-29` 判定面）：此前"写快照"绑在 `RosterAuditDuty` 内部、依赖管理群配置，导致没配管理群时快照永不写、员工永不能开通（2026-08-21 冒烟实测坐实）。拆分后 `RosterSnapshotSyncDuty` 只依赖花名册 Base 坐标独立注册（只写不发），与 `RosterAuditDuty` 互斥装配，两者齐全时仍由后者一次完成全链路。断言在 `tests/test_roster_snapshot_sync_duty.py`。

**写侧的当日水位判据**：`_completed_on` 只在本轮真的换上新快照时才置位，保旧轮与失败轮不置位、下一 tick 重试，同日重试安全（整体替换）。`RosterAuditDuty.run_once` 先取花名册（含快照写入）、后读比对基线，避免日报正文故障拖停开通链硬前提。


**状态列口径**：本组全部为「已认领」。L2 绿灯不等于真实链路验收——真实花名册读取所需的专用主体凭据自 2026-08-09 起未落盘（Issue #52 的 G-READ 判定），真实群通知与真实读取均未做，因此没有一条可以标「已验证」。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-花名册-01 | 姓名 / 工号 / 邮箱三字段**逐一**独立用例：单字段变化恰产生一条差异且指名该字段 | L2（纯函数） | 已认领 |
| V-花名册-02 | 三态：全等得空差异集（非 None 不抛）；不等得变化；花名册查无得移除。A 方案下**无「新增人员」态** | L2（纯函数） | 已认领 |
| V-花名册-03 | 转交肯定面：姓名与邮箱同时变化时单独标出，标记可独立取出 | L2（纯函数） | 已认领 |
| V-花名册-04 | 转交否定面：只改姓名、只改邮箱、姓名与工号同变，三者均**不**标为转交 | L2（纯函数） | 已认领 |
| V-花名册-05 | 人员 ID 按**完整值**比对：前 6 位相同、完整值不同的两个 ID 不得判为同一人 | L2（纯函数） | 已认领 |
| V-花名册-06 | 同一人员 ID 多行时行为唯一确定，日报单列一类，不静默取任意一行 | L2（纯函数） | 已认领 |
| V-花名册-07 | **不对称空值口径**：存档为空的字段不参与比对；存档有值而花名册变空必须报。双向用例 | L2（纯函数） | 已认领 |
| V-花名册-08 | 比对输出对象**本身不含字段值**，只有标识、变化字段名与转交标志；比对模块不 import 任何可做 I/O 的模块。D2 之后这是**分层**约束（取值属渲染层）而不再是脱敏承诺的落点 | L2（纯函数 + 源码扫描） | 已认领 |
| V-花名册-09 | 部门与账号状态不进差异集（即便输入行携带）；花名册 adapter 的读取字段集不因本切片扩大 | L2（纯函数） | 已认领 |
| V-花名册-10 | 比对集只来自 `provisioning_state='active'`：其余六种状态各一条均不出现 | L2（真库） | 已认领 |
| V-花名册-11 | `account_state` 为 `deleting` / `deleted` 的用户排除出比对集，删除编排清空字段不得误报为「姓名变化」 | L2（真库） | 已认领 |
| V-花名册-12 | 未开通人员的任何字段值不出现在比对输出、日报正文、审计或日志中（断言字符串不出现，非行数为 0） | L2（真库） | 已认领 |
| V-花名册-13 | **迁移是 DDL 的唯一载体**（D2 之后本组确有新表，「零新表 / 零迁移」旧表述作废）：比对、渲染、读取与快照适配四层源码均不 import alembic / sqlalchemy、不含建表语句；建表只在 `0063_roster_snapshot` 里，其单 head、连通性与非空 `downgrade` 由 `check_alembic_revisions.py` 承担，逐条真往返由 `check_migration_chain.sh` 承担，且两个委托确实接在门禁里 | L1（源码扫描）+ L2（真库往返） | 已认领 |
| V-花名册-14 | **存档不写回**：三字段全变跑完一整轮后，`app_user` 的姓名 / 工号 / 邮箱与 `updated_at` 逐字段与运行前完全相同；读取模块的 SQL 常量中无任何写语句 | L2（真库 + 源码扫描） | 已认领 |
| V-花名册-15 | 账号状态按需回读**不落库**：哨兵值经输入行走完整轮后，全库文本列中查无此值 | L2（真库） | 已认领 |
| V-花名册-16 | 周期实际调用日报职责，观察点是注入的基线读取与发送接口真的被调到 | L2（可注入） | 已认领 |
| V-花名册-17 | 职责间失败隔离：日报炸掉时轮换与清理本轮仍执行，反之亦然（两个方向各一用例） | L2（可注入） | 已认领 |
| V-花名册-18 | 连续失败不退出进程；日志只记异常类型，不记异常正文 | L2（可注入） | 已认领 |
| V-花名册-19 | `SIGTERM` 后不开新轮、在途发送完整做完、无半发送、超时内退出（真实子进程 + 真实信号） | L2（子进程） | 已认领 |
| V-花名册-20 | 停止之后发送次数为 0，连基线都不再读取；停止信号落在**读取阶段**时同样不发送、不置水位（干净中断） | L2（可注入） | 已认领 |
| V-花名册-21 | 日报逐条含人员标识、**用于定位的存档身份原值（姓名 + 工号）**、变化字段名与转交标注，转交条目与普通变化条目可区分；没有变化字段的条目（花名册查无、同一 ID 多行）同样给全身份；存档为空的字段渲染成显式占位符而不是空白；正文另含快照读取时间与同步状态，日期与时间一律标注 UTC | L2（纯函数） | 已认领 |
| V-花名册-22 | **D2 的否定面**（取代旧「甲否定面：正文无任何真实值」）：正文不含凭据、令牌、认证信息或与管理员处理无关的敏感数据；不含未开通人员的任何资料；不含比对结论以外的花名册整表内容——**花名册当前值一律不进正文**，也不出现新旧值对形态；未被报出的人一个字段都不露面；存档邮箱值不印（定位靠姓名 + 工号，「邮箱变了」由字段名说明） | L2（纯函数 + 模式扫描） | 已认领 |
| V-花名册-23 | 人员标识为 `app_user.id` 完整 ULID：外部标识前 6 位相同的两名用户在日报中标识不同；正文不出现脱敏缩短形态、不出现飞书外部标识原值、不对 ULID 截断 | L2（纯函数） | 已认领 |
| V-花名册-24 | 正文无任何可执行入口：无链接、按钮、回调；发送载荷为纯文本而非卡片 | L2（纯函数 + 可注入） | 已认领 |
| V-花名册-25 | 空差异日出站 0 次且审计恰 1 条；非空差异日发送恰 1 次（两面用例） | L2（可注入 + 真库） | 已认领 |
| V-花名册-26 | 花名册查无此人单列一类且零自动动作：不改 `app_user`、不停用、不发布、不产生待办 | L2（可注入 + 源码扫描） | 已认领 |
| V-花名册-27 | 出站可注入：构造函数内无任何 import、不建 client、不发请求 | L2（源码扫描 + 可注入） | 已认领 |
| V-花名册-28 | 群 ID 由环境变量注入，真实形状的群 ID 不出现在代码库任何文件中 | L1（源码扫描） | 已认领 |
| V-花名册-29 | 四个前置（管理群 ID、花名册 Base `app_token`、`table_id`、读取令牌供给）任缺其一，日报职责（`RosterAuditDuty`）不注册、进程照常启动、审计**恰 1 条**（`roster_audit.duty_not_registered`）且指名原因 / 变量不回显值；前置齐备时职责由 `build_loop` 真实注册且装配阶段不发任何请求。**日报职责未注册时**，写侧职责 `RosterSnapshotSyncDuty`（Issue #275）按它**自己的两个前置**（花名册 Base `app_token`、`table_id`；读取令牌供给由 `build_loop` 默认建出）单独尝试注册，缺项审计同样**恰 1 条**、前缀为 `roster_snapshot_sync.*` 以便与日报那条分辨；两个职责**互斥注册**，任一时刻至多一个在触发花名册读取。**注册完全由配置驱动**，不得在装配里写死一个永远不满足的前置 | L2（可注入） | 已认领 |
| V-花名册-30 | 发送失败不影响其他职责、只记审计、不算已发送，同日下一轮重试 | L2（可注入） | 已认领 |
| V-花名册-31 | 不重复发送：单轮一次；同进程同日跨轮一次（注入时钟）；重启当日至多重发一次且重发载荷与首次逐字段一致；**投递不确定态**（请求已达飞书而响应超时）下的重试携带与首次相同的投递 `uuid`，同日相同、跨日与跨群不同 | L2（可注入） | 已认领 |
| V-花名册-32 | 日报动作经 `AuditSink` 记审计，字段含日期、比对集规模与条目计数 | L2（可注入） | 已认领 |
| V-花名册-33 | 审计记录与日志均不含任何花名册字段值或外部标识原值（模式扫描） | L2（可注入 + 真库） | 已认领 |
| V-花名册-34 | 运行时源码中 `redact_identifier()` 的每一次调用都是日志参数，不被当作键比较、去重或定位；日报渲染完全不调用它 | L1（源码扫描） | 已认领 |
| V-花名册-35 | 合成端到端：6 名已开通 + 1 名 guest，日报恰含 4 条、转交恰 1、移除恰 1；缺字段者、无变化者与 guest 均不出现；`app_user` 三字段零变化。正文的取值形态按 `V-花名册-21/22` 的 D2 口径判定：被报出的四人存档身份必须出现，花名册当前值、存档邮箱与未被报出的人一律不出现，同一批值在审计记录里仍然一个都不许有 | L2（真库） | 已认领 |
| V-花名册-36 | 端到端在 CI 实跑并打印执行计数；配置了真库却把该用例跳过时变红 | L2（真库） | 已认领 |
| V-花名册-37 | 一轮读取结算成**四态**且判定次序固定：先判完整性再判空——源头自报有行却一行没给判「不完整」而不是「空源」；四态各一用例，且只有 `COMPLETE` 的 `complete_nonempty` 为真 | L2（可注入） | 已认领 |
| V-花名册-38 | 读取失败分**明确失败 / 结果不明**两类：业务错误码明确非 0 为明确失败；传输异常、超时、响应体不可解析、响应形状不对、游标停滞、翻页超上限一律为结果不明。分类反向时变红；凭据 provider 自身抛出的异常不折成源头读取失败 | L2（可注入） | 已认领 |
| V-花名册-39 | **半轮不外泄**：读取中途失败时返回的行恒为空，半轮读到的行只以 `partial_rows` / `partial_pages` 计数出现；未预期异常不被吞成「源头读取失败」，原样上抛 | L2（可注入） | 已认领 |
| V-花名册-40 | 重复与空值只上报计数、不做任何自动取舍：同一人员 ID / 工号的重复行原样保留；**空键不算重复**（缺工号的行不得被算成一组重复）；必需列清单里重复配置同一列时，整列缺失的判定不被击穿 | L2（可注入） | 已认领 |
| V-花名册-41 | **快照替换门槛只认 `status`（`complete_nonempty`）**：`INCOMPLETE` 携带非空行时**不得**替换（否定用例），`EMPTY_SOURCE` 与两类 `FAILED` 同样不替换；以「行非空」作判据的实现在该否定用例上变红 | L2（可注入） | 已认领 |
| V-花名册-42 | **原子替换**：删旧、写元信息、写行在同一个事务内完成；写行或写元信息中途失败后，库里仍是替换前那一份完整快照，不留半份（真库注入失败两个方向各一用例） | L2（真库） | 已认领 |
| V-花名册-43 | 空源 / 不完整 / 明确失败 / 结果不明四类**保旧原因互不合并**，各自产出可断言的告警事实（含错误码与失败分类）；`COMPLETE` 轮不产生告警 | L2（可注入） | 已认领 |
| V-花名册-44 | **首轮语义**：库中从未有过快照时不得报「保留了上一份」——该情形单独成一类事实，上一份的读取时间、行数与年龄均为空；有旧快照时事实里带上它的读取时间、行数与年龄（年龄为空而不是 0） | L2（可注入 + 真库） | 已认领 |
| V-花名册-45 | 载体**只保存最近一份**：替换后库中恰一份快照元信息、无孤儿行；数据库层的单例约束拒绝第二份，零行快照被适配器与 `CHECK` 各拒一次 | L2（真库） | 已认领 |
| V-花名册-46 | 快照层的判定对象、审计事实、日志与快照元信息表**均不含任何花名册字段值**（只有状态、计数、列名、错误码）；行本身只存在于行表，且原样保留不去重 | L2（可注入 + 真库） | 已认领 |
| V-花名册-47 | 快照因源头长期读不到而超龄时，按日报告警提醒管理员且**不自动删除**（产品负责人 2026-08-17 裁定：始终保留最近一份、豁免九十天规则）：超龄判定与告警送达由日报侧接线承担，快照载体不得出现任何按时间删除快照的路径。阈值默认 48 小时、可由环境变量覆盖且覆盖值必须真的生效，边界属「还没超龄」那一侧，0 / 负数 / 非数字 / 大到等于关掉告警的值一律快速失败；**超龄那一天即使零差异也要发日报**（沉默是这一天最危险的输出），正文同时写明「快照不会被自动删除」 | L2（可注入 + 源码扫描） | 已认领 |
| V-花名册-48 | **没有任何可用快照时不进行比对**：库中从未有过快照而本轮又读不成功时，日报不得产生任何「花名册查无此人」条目（拿空行集比对会把全体已开通用户一次性报成移除），也不得渲染成「本次发现 0 条」；改为发出一条写明「本次未进行比对」与保旧原因的告警。有快照时（含超龄）照常比对，两种情形在正文与审计事实里可区分 | L2（可注入） | 已认领 |

**本组不覆盖什么。** 真实群通知与真实花名册读取属 L4a，两者都**未验证（证据等级 1）**：`FeishuGroupMessages` 与花名册 reader 的全部断言跑在注入的假传输层上，与 `adapters/feishu_directory.py` 同一姿态；真实读取所需的专用主体凭据自 2026-08-09 起未落盘（Issue #52 的 G-READ 判定，E1-A 授权码被烧事故的直接后果），真实面改依赖后续的 bootstrap 重授权切片。持久快照的**接线**已由 S-B-04 完成（`V-花名册-47/48`）：快照链挂进了每日日报职责，保旧告警事实接到 scheduler 的结构化警告日志，面向管理员的提醒走日报本身——`core/alerting.py` 的状态机只认心跳、任务滞留与发送连续失败三类信号，刻意没有为快照新鲜度新增第四类。**花名册读取所用的短期令牌供给已由 [#215](https://github.com/Moshuiwang/lingxi/issues/215) 接上**（方案 C 主接线，产品负责人 2026-08-18 裁定接受按需消费节奏，留痕见 [#203 决策评论](https://github.com/Moshuiwang/lingxi/issues/203#issuecomment-5321623142) 第 7 项）：凭据轮换职责**仍是一次性 `refresh_token` 的唯一消费者**，它按需消费一次并把派生的短期 `access_token` 放进**进程内**持有者（不落盘、不进日志与审计，重启即空），日报侧按新鲜度取用。消费频率随之从约 5.6 天一次变成按需（令牌寿命约 2 小时，正常一天约 12 次）。**[#276](https://github.com/Moshuiwang/lingxi/issues/276)（产品负责人 2026-08-21 裁定）解除了此前"每 UTC 日至多消费一次"的自设上界**，改为两次消费的最小间隔（默认 5 分钟）与每日消费次数上界（默认 100 次）双重保护；判据仍随凭据落盘、进程内不留第二份账本副本——`refresh_consumed_at`（最近一次消费时刻）与新增的 `refresh_consumed_count`（当日消费计数），均在文件锁内、置位消费标记之前判定。**唯一消费者与频率上界是两件不同的事**：2026-08-08 授权码被烧那次事故的形状是两个客户端抢占同一条通道，不是"换取太频繁"（详见 `core/identity/access_token_supply.py` 模块文档）。因此 `V-花名册-29` 的第四个前置现在只取决于配置：三个环境变量配齐即注册。**这一段全部是 L2 事实**：真实续期返回的短期令牌寿命、真实按需消费节奏与真实日报送达仍属 L4a、未验证。部门比对与账号状态落库由定案排除；账号复用换人的自动拦截由 [#34](https://github.com/Moshuiwang/lingxi/issues/34) 永久排除；群内处置由合同排除；`audit_event` 表属 S9，当前审计走结构化日志。

**`V-花名册-47`「源码扫描」半边在 [#215](https://github.com/Moshuiwang/lingxi/issues/215) 才真正兑现**：「不自动删」要证明一条路径不存在，此前无可断言的检查点。现由 `SnapshotHasNoTimeBasedDeletionPathTest` 扫描相关模块与迁移，拒绝带时间条件的删除语句、到期列与触发器，并有防空转的分类器自测用例。

**它是绊线，不是安全边界。** 已登记的盲区（Python 侧先算 ID 再删、词表外命名、动态 SQL 组装、仓库外运维脚本等）靠人工审查兜底；交付前已种入四种违规样本验红、九次分类器变异确认全部变红，详见[验证与门禁第八节](验证与门禁.md#八否定测试)。

**残留风险登记**（[#52](https://github.com/Moshuiwang/lingxi/issues/52) 决策登记与裁定 C2）：

- **同一处漂移每天重复上报**（知情接受）：基线是建档存档而非昨天的快照，人工处理前会每天重复出现，持久快照不改变这一条。
- **跨重启重发一份相同日报**（知情接受，尚未消解）：判重水位仍在进程内存里，真幂等要等水位有持久载体后另行交付；S-B-04 已核对并放弃「拿快照时间当水位」这条捷径，因为会让当天日报永远发不出去。
- **快照保留期已裁定，不再是残留**：始终保留最近一份、豁免九十天，超龄告警（`V-花名册-47`）与无快照时不比对（`V-花名册-48`）已由 S-B-04 交付。

**"一日"按 UTC 日切分**：日报在北京时间每天 08:00 后首轮发出，标题日期与管理员自然日相差 8 小时；是否改用固定业务时区待产品负责人裁定，本切片不改行为。

**投递不确定态的保护不是幂等承诺**：响应超时时重试会携带同一投递 `uuid` 交服务端去重，但平台侧去重窗口未验证（L4a），矩阵只承诺"重试携带同一 uuid"这一代码事实。

### 问数输出

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-问数-01 | MCP 未返回某项数据时，输出**不含**"权限不足""无权限"类措辞。终态正文若为模型生成，其输出校验范围按 `V-内容-07` 的 [#322](https://github.com/Moshuiwang/lingxi/issues/322) 新限定执行，不再套用固定词表 | L2（输出检查器单测）+ L4a | 已认领 |
| V-问数-02 | MCP 未返回数据最新时间时，输出省略该字段且不提示"数据时间未知" | L2 + L4a | 已认领 |
| V-问数-03 | 用户被停用后无法发起新问数；停用前已开始的任务正常完成并交付 | L2（真库）+ L4a | 未认领 |
| V-问数-04 | 固定 20 个真实问数逐题由提问用户判断，只有正确、可用的答案算通过；明确失败或未取得结果仍算未通过。至少 18 个通过且最多 2 个未通过，不加权、不取主观平均分 | L5 | 未认领 |
| V-问数-05 | 20 个问题中一旦出现关键数字错误、错误指标、跨用户泄露或三轮内既未收敛也未明确失败，即使其余 19 个通过也判定整体不通过 | L5 | 未认领 |

---

## 二、合同条款覆盖清单

维护机器可读映射：`产品合同条款 → 验收断言编号 → 测试用例`。CI 目标检查三条：每条已认领断言至少一个测试用例、没有引用不存在的断言编号、已验证断言确实在声明层级执行过——**这是目标形态，当前门禁只执行**下面[清单本体与门禁实际检查的三条](#清单本体与门禁实际检查的三条)所列内容。

**未认领条款不参与强制**，避免绝大多数条款尚无断言时让门禁直接全红；随切片推进，未认领项逐步归零。

#### 清单本体与门禁实际检查的三条

清单就是下表，与第一节「状态」列由同一脚本（`scripts/ci/check_acceptance_matrix.py`）解析，实际检查：

- 产品合同每个章节必须在表里出现且只出现一次；
- 「对应断言」格给出至少一个真实存在的断言编号，或写 `无对应断言`；留空即失败；
- 写 `无对应断言` 必须在「说明」里写明理由。

开头列的另外三项（测试用例绑定）尚未实装，需要测试与断言编号间的机器可读绑定，当前只有自由文本约定，留作后续。清单按章节粒度维护，不逐条抄合同规则，避免形成第二份合同正文；`V-部署-*` 等不对应合同章节的断言不要求出现在此。

| 合同章节 | 对应断言 | 说明 |
|---|---|---|
| 产品是什么 | 无对应断言 | 产品定位陈述，不含独立的可判定规则；其中的准入与问数前置由「首次对话与自动准入」「统一用户记录与权限变化」承担 |
| 用户与入口 | V-开通-03、V-开通-06、V-管理-08、V-管理-11、V-交付-06、V-问数-03 | — |
| 首次对话与自动准入 | V-开通-01…18、V-身份-01、V-身份-02、V-身份-04、V-银河-09、V-银河-10、V-审计-05 | — |
| 开通流程 | V-开通-01、V-开通-06、V-开通-07、V-开通-09…17、V-身份-02、V-身份-03、V-身份-05…09、V-权限-01…06、V-银河-09 | — |
| 开通成功后 | V-开通-11、V-开通-14、V-开通-18、V-权限-04、V-权限-06、V-审计-05 | — |
| 匹配结果与异常处理 | V-开通-02、V-开通-03、V-开通-06、V-开通-09、V-开通-12、V-开通-13、V-开通-17、V-开通-19、V-开通-20、V-权限-04、V-权限-05、V-银河-09、V-银河-10、V-银河-11 | — |
| 管理员处理入口与安全确认 | V-管理-01…28、V-审计-01、V-花名册-24、V-告警-01…09 | — |
| 统一用户记录与权限变化 | V-开通-01、V-开通-15、V-开通-16、V-身份-01、V-身份-03、V-权限-01…06、V-权限-09…12、V-银河-01…13、V-管理-13、V-管理-14、V-问数-03、V-花名册-01…48 | 本节「每日比对已开通用户的花名册资料与存档」一条由 V-花名册 组承担 |
| 问数与多轮对话 | V-会话-01…10、V-接入-01、V-接入-02、V-接入-04…13、V-队列-01…07、V-护栏-01…03、V-护栏-07、V-问数-03、V-投递-02、V-投递-06、V-投递-08 | — |
| 用户记忆 | V-记忆-01…07 | — |
| 问数结果的可信标准 | V-问数-01…05、V-卡片-04、V-注入-01…05 | — |
| 结果与交付 | 无对应断言 | 本节只有小节标题，规则正文在下属三个小节 |
| 交付样式 | V-卡片-01…04、V-投递-01…06 | — |
| 正式产物归属与当前失败关闭 | V-交付-03…05 | — |
| 交付规则 | V-交付-01…07、V-投递-03、V-投递-04 | — |
| 高级工作台 | 无对应断言 | 已于 2026-08-28 取消（[决策记录](../决策记录/2026-08-28-取消JumpServer高级工作台.md)），原断言映射随之移除；合同保留墓碑小节仅为引用不断链 |
| 数据保留与删除 | V-接入-03、V-管理-13、V-管理-14、V-审计-04、V-审计-06、V-审计-07、V-保留-01…25、V-投递-05…10 | — |
| 审核、审计与持续优化方向 | V-审计-01、V-审计-02、V-审计-03、V-执行-04、V-执行-05、V-执行-06、V-执行-17、V-护栏-04…06、V-通报-01…12、V-采集-01…09 | 内测每日统计通报（#303 S-O-01，V-通报-09 为 #304 批次 4 通报补数，V-通报-10/11 为 #325 判重水位持久化与重聚合挪出主线程，V-通报-12 为 #319/Trace #328 本地权限覆盖活动段）由 V-通报 组承担；内测轮内容级采集（#251/#304 批次 3）由 V-采集 组承担 |
| 系统与外部依赖边界 | V-开通-07、V-身份-02、V-身份-03、V-身份-05…09、V-身份-11、V-执行-01…21、V-注入-01…05、V-权限-01…03、V-权限-06、V-权限-09…12、V-管理-10、V-管理-12、V-管理-16…18、V-银河-01…13、V-交付-01、V-交付-04、V-交付-06、V-投递-01…10、V-问数-01、V-问数-02、V-审计-01、V-部署-08、V-护栏-01…07 | — |
| 不提供 | V-管理-10、V-管理-11、V-管理-17、V-管理-26、V-执行-02、V-交付-06、V-审计-03 | — |
| 动态工作的去向 | 无对应断言 | 规定工作项去向（Issue / Project / PR），不产生用户可见规则 |

**这张表回答"哪条合同规则由哪些断言承担"，不回答"到哪一步了"**——后者看断言自己的状态列。两者分开维护：同一条断言可能被多个章节引用，把进度抄进这张表会立刻出现两个互相矛盾的副本。

**为什么不用覆盖率数字做门禁**：行覆盖率鼓励为达标而写的测试，且对"系统是否会拒绝违规行为"完全不敏感——一个 90% 覆盖率的套件可以一条否定测试都没有。合同条款覆盖清单是可数、可核对、且直接对应产品承诺的指标。

## 三、部署与迁移断言

部署方案本身在[架构设计](架构设计.md#八部署与发布)里已经定了（单机 Docker Compose、镜像 tag 即冻结版本、切 tag 回滚），**本文不重复，也不新写部署文档**。发布链路由[协作约定](../协作约定.md)规定。

但有一类东西必须现在就约束：**部署对代码的要求**。它们在写第一行代码时就被固化，事后修改要动很多处；而且架构设计里"镜像 tag 就是冻结的版本、回滚等于切 tag 重启"这条，**只有在代码满足下列约束时才成立**。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-部署-01 | 代码中不存在硬编码的主机名、端口、路径或密钥；全部配置来自环境变量 | L2（模式扫描） | 已认领 |
| V-部署-02 | 除用户环境目录外，进程不在容器内写入任何需要持久化的本地状态 | L2（运行期文件系统监视） | 已认领 |
| V-部署-03 | 进程收到 `SIGTERM` 后停止领取新任务、完成或安全中断在途任务、在超时内退出。**部署约束**：`LINGXI_DELEGATED_CREDENTIAL_PATH` 必须指向**跨部署持久**的宿主机挂载路径（compose 卷/绑定挂载），镜像替换与重启不得丢失——目标是部署对该凭据「零特殊处理」；文件丢失或过期时的保底是重新走一次「四达文档会议助手」授权，该保底流程写入 S11 部署手册（产品负责人 2026-08-05）。`lingxi-scheduler` 的停止宽限期必须大于「续期 HTTP 超时（当前 20 秒）+ 落盘耗时」，否则 SIGKILL 会落在「已向飞书换过新凭据、尚未写回数据库」的窗口里——Docker 默认 10 秒宽限期不满足，S11 编排时须显式加大（PR #48 独立复查发现）。`lingxi-gateway`（#57）同型认领：收到 `SIGTERM` 后停止接收新事件、完成在途事件的落库、在超时内退出，不遗留"抢占了话题但任务没写进去"的中间态——在途事件各自是一个数据库事务，要么整体落库要么整体回滚。**判定必须覆盖"连接空闲、当前没有任何用户消息"这个最常见的状态**：只在收到事件之后才检查停机信号的实现，夜间与周末根本停不下来，只能等编排层 SIGKILL；因此传输层空闲时须定期让出控制权，用例用**只产出空闲心跳**的传输层而不是持续产出事件的传输层（后者恰好是该缺陷唯一能工作的情形，会给出假绿——#57 独立复查实测） | L2（真实进程信号测试） | 已认领 |
| V-部署-04 | 日志输出到 stdout / stderr，不写日志文件、不自行轮转 | L2 | 已认领 |
| V-部署-05 | 每个数据库迁移可前滚；旧版本镜像能在新库结构上正常启动 | L2（真库：迁移后用上一版本代码跑一遍冒烟）+ L4a（2026-08-23 `biai-stage` 回滚演练：整队切回旧候选 `20260822-9dd41b5498be` 三容器在当前库结构上全部 healthy 再滚回，[#203 演练记录](https://github.com/Moshuiwang/lingxi/issues/203#issuecomment-5386447758)） | 已验证 |
| V-部署-06 | 镜像构建不依赖构建机的本地状态；同一提交两次构建产出等价镜像 | L2 | 已认领 |
| V-部署-07 | 容器以非 root 运行；仅持有架构设计中说明的能力（`CAP_SETUID` 等） | L2（镜像与 compose 检查） | 已认领 |
| V-部署-08 | 运行时依赖全部在 `pyproject.toml` 声明；**安全边界所依赖的外部组件锁精确版本**（`==`，不用范围），升级前必须重跑对应的受控验证 | L2（配置检查）+ 升级时 L4a | 已认领 |
| V-部署-09 | 门禁跑在 `pyproject.toml` 声明支持的解释器版本上；低于下限即失败，不得靠运行环境碰巧满足 | L2（门禁自检） | 已认领 |
| V-部署-10 | **已安装的制品**（不是 `src/` 目录）能导入全部关键模块，且必须在仓库目录之外校验；新增子目录漏进制品会红 | L2（CI 独立步骤） | 已认领 |
| V-部署-11 | 健康检查不得只证明进程 PID 存活：依赖（数据库）不可达时必须如实变红；主循环停摆（依赖可达但活性文件过期）也必须如实变红。**不新增入站监听端口**——健康检查以 `docker exec` 语义在容器内跑 `python -m lingxi.apps.healthcheck`，与被检查进程共享同一文件系统/网络命名空间 | L2（真库 up/down 负向测试 + 真实文件系统） | 已验证 |
| V-部署-12 | **常驻 queue worker**（`LINGXI_WORKER_MODE=queue`）收到 `SIGTERM` 后：停止 `claim()` 领取新任务；在途任务的 `_monitor` 把这次停机等同用户 `/stop`，在一个 `stop_poll_interval_seconds` 量级的时间内请求 Agent SDK 中断，收口为 `stopped` 终态；超过 `LINGXI_WORKER_SHUTDOWN_TIMEOUT_SECONDS`（默认 45s）仍未收口则不再等待、进程退出，任务保持 `running` 状态等待未来一次心跳超时回收，不丢结果、不重复副作用 | L2（进程内 asyncio 注入 + 真实子进程信号）+ L4a（2026-08-16 r9 隔离验收在途重启唯一 stopped 终态；2026-08-23 `biai-stage` 演练在途 343 秒级任务重启收口恰一个 `stopped` 终态、无双终态/丢失/重复交付，[#203 演练记录](https://github.com/Moshuiwang/lingxi/issues/203#issuecomment-5386447758)） | 已验证 |
| V-部署-13 | 明确命名的受控部署 profile（`mvp`）能同时启动迁移后的 scheduler、gateway、常驻 queue worker；裸 `docker compose up -d`（不带任何 profile）默认行为不变，仍只启动 scheduler | L2（`docker compose config` 结构核对） | 已认领 |
| V-部署-14 | 追溯号只读查询命令（`python -m lingxi.apps.trace`，Issue #280 §7.2）**没有任何写路径**：全程只用一个只读事务（连接层设置 `read_only`）读 `inbound_event`/`app_user`/`publish_outbox`/`mcp_sync_check` 四张表，不接受任何写参数；随 scheduler 镜像随附（不是独立 compose 服务，由运维以 `docker exec` 语义手动调用）。默认输出**不含 `open_id`**，需要 `--include-open-id` 显式打开；未知追溯号给出明确的"查无此追溯号"，不输出空壳或半条记录 | L2（真库注入只读连接 + 输出断言） | 已认领 |
| V-部署-15 | 六个服务（scheduler/gateway/worker/worker-queue/migrate/reauthorize）均在 `deploy.resources.limits` 显式声明 cpus/memory/pids 三项资源上限；stage/生产数值分环境覆盖，漏配任一服务的任一项即门禁红 | L2（结构扫描，`check_resource_limits`） | 已认领 |
| V-部署-16 | 容器 json-file 驱动的 max-size/max-file 不低于取证留存下限（当前 50m/5，门禁核对）；宿主侧增量收集脚本 + logrotate 独立于容器生命周期持久化日志，重部署不丢失近期历史（Issue #343）。**30 天窗口、总量硬封顶 ≤2GB 是安装姿势的配置目标值，不是门禁能核对的断言**，由 deploy/日志留存.md 的安装步骤与 L4a 核对承载 | L2（结构扫描，`check_log_retention_floor`）；30 天/2GB 配置目标待 L4a | 已认领 |
| V-部署-17 | 宿主层监控告警（D5 裁定，独立于容器内部的 `AlertingDuty`）：容器整体 `unhealthy`/`missing`/`stopped` 时，宿主脚本在有界时延内送达受控管理群一条告警；恢复后送达一条恢复通知；同一容器同一故障原因不重复告警（去重，仅发送成功后落盘）；发送失败留本地日志、不崩溃，同一时刻只允许一个实例执行检查（`fcntl.flock` 独占锁） | L2（单元测试 39 用例）+ L4a（2026-08-28 `biai-stage` 真实注入取证） | 已验证 |
| V-部署-18 | 生产部署 runbook（D4 裁定）明确以下判据均已书面化且供执行时遵照：digest 固定（不现场构建）、单实例纪律（D11，不灰度）、首发不灰度、回滚判据与观察期时长、healthcheck 发现时延新数值（gateway 69s/worker 119s）；生产凭据集与研发/Stage 完全隔离，任何一个值不与 `.env.stage.*` 系列重复（百炼端点凭据除外），凭据事实源为目标机器私有文件、按服务最小分发。任何一次真实生产执行仍须独立 `[ops]` Issue 授权，本条不构成执行授权 | L2（决策记录+runbook 文档产出，未执行任何生产操作） | 已认领 |

**`V-部署-17`/`18` 证据来源**：D5（PR #390）与 D4（PR #387，[决策记录](../决策记录/2026-08-28-生产secret注入与凭据隔离.md)）均为 Trace #373 H2 批交付。`V-部署-17` 的 L4a 取证：worker-queue 真实 unhealthy（docker journal 17:00:40Z）→ 17:01:18Z 受控管理群告警送达、17:03:11Z 恢复通知送达，期间无重复告警（去重生效），详见 `deploy/监控告警.md`「七、已知限制」与 [#373 H2 收口评论](https://github.com/Moshuiwang/lingxi/issues/373)。`V-部署-18` **截至 2026-08-29 未执行**：runbook 与决策记录均已就位，但生产（`biplus-prod`）尚未切换/部署，凭据隔离机制本身未经真实生产执行验证，因此保持已认领而非已验证；未验证事项：监控告警长窗口稳定性（当前仅一轮注入取证）。

**`V-部署-13` 证据边界**：L2 只验证 `docker compose config` 渲染结构对，不构成"三服务真的能同时启动"的证据（需 `biai-stage` L4a）。已修复的缺口：`worker-queue` 此前与一次性 `worker` job 共用 env 文件、缺 `LINGXI_POSTGRES_DSN` 会无限崩溃重启，结构核对天生不敏感；现 `worker-queue` 有独立 env 文件并经 `check_worker_queue_env_example` 校验。

**V-部署-05 是最容易漏、代价最高的一条。** 回滚策略依赖"旧镜像能在新库上工作"，而这一点只有显式测过才成立——破坏性迁移一旦合并，回滚就从"切 tag"变成"恢复备份"。

新增可自动化的审查要求必须同步形成 `V-*` 断言和会变红的 CI；散文提醒不构成门禁。`V-部署-08/09/10` 的形成经过见[形成记录](../参考证据/验证与门禁形成记录.md)。

**2026-08-26 对账（#100）翻转为已验证**：
- `V-部署-08` → `test_deploy_checks.py::ExactPinTest`（wildcard 拒绝/精确 pin 通过）+ `LazyImportScanTest` + `RealRepositoryTest::test_declared_requirements_cover_the_known_extras`；commit `2ece428`（提交信息自述"补上 V-部署-08 的首个执行器"）
- `V-部署-11` → `test_liveness_and_healthcheck.py::HealthcheckDatabaseCheckTests::test_unreachable_host_is_unhealthy_within_a_bounded_time` + `HealthcheckLivenessCheckTests::test_an_old_liveness_file_exceeding_the_threshold_fails` + `HealthcheckCommandRealDatabaseTests`（真库 up/down 正负两态）；commit `dbc678f`（#153）

`V-部署-01/02/03/04/06/07/09/10/13/14` 的执行器脚本与 CI 作业身份已在下表列明，但多数校验点是 `Epic Full / image` 里的 shell/CI 步骤，本次在可控时间内未找到逐条独立对应的 pytest 级直接证据（`V-部署-02`/`03` 有部分信号：`OverrideBypassTest::test_override_disabling_read_only_is_caught`、`RealRepositoryTest::test_compose_declares_a_sufficient_stop_grace_period`，但不足以独立确认三要件），保守保留已认领；`V-部署-13` 的表格行下方已有编排者自述"L2 只验证 `docker compose config` 渲染结构对，不构成三服务真的能同时启动的证据"，与断言本身要求的运行时行为不完全一致，本次未翻转。

**2026-08-27 退回（#328 opus 批量审查 P2）**：`V-部署-08` 从已验证退回已认领——层级列含条件触发的『升级时 L4a』，当前无升级事件，违反本批『声明层级含未补验 L4a/L3 一律不翻转』的保守规则；上面 2026-08-26 对账登记的 `test_deploy_checks.py::ExactPinTest` 等 L2 执行器证据依旧成立，本次退回只纠正状态判定，不否定 L2 部分。

### 3.1 部署断言执行器（Issue #62 落地）

上表在 [#62](https://github.com/Moshuiwang/lingxi/issues/62) 之前有五条是"未认领"，也就是**没有任何东西会因为违反它而变红**。下表登记每条断言现在由谁执行。编排文件与镜像本身见 `deploy/README.md`。

| 断言 | 执行器 | 挂在哪 |
| --- | --- | --- |
| `V-部署-01` | `scripts/ci/check_deploy_contract.py`（扩面到 Dockerfile 与 compose） | `verify_repository.sh` |
| `V-部署-02` | `deploy/compose.yaml` 的 `read_only: true` + `verify_old_image_new_schema.sh` 的 `docker diff` | `Epic Full / image` |
| `V-部署-03` | `check_deploy_contract.py` 的停止宽限期联动 + `verify_old_image_new_schema.sh` 的真实 `docker stop` | 两处 |
| `V-部署-04` | `verify_old_image_new_schema.sh`（`docker diff` 无日志文件、`docker logs` 非空） | `Epic Full / image` |
| `V-部署-05` | `scripts/ci/verify_old_image_new_schema.sh` | `Epic Full / image` |
| `V-部署-06` | `scripts/ci/image_manifest.py` + 两个不同路径的干净检出各构建一次 | `Epic Full / image` |
| `V-部署-07` | `check_deploy_contract.py` + `verify_image_contract.sh` | 两处 |
| `V-部署-08` | `scripts/ci/check_runtime_dependencies.py` | `verify_repository.sh` |
| `V-部署-09` | `verify_repository.sh` 的解释器自检 + `verify_image_contract.sh` 的镜像面 | 两处 |
| `V-部署-10` | `scripts/ci/check_installed_package.py`（镜像内以只读挂载跑同一份脚本） | `Epic Full / image` |

**`V-部署-08` 此前是"已认领却没有执行脚本"**（Issue #58 遗留）。现有两半：`--imports` 静态扫描全部 import（含函数内延迟导入），`--pins` 断言安全边界组件锁 `==`。

**`V-部署-05` 已于 2026-08-23 随 `biai-stage` 真实回滚演练翻转为已验证**（见矩阵行证据链接）：本地真库演法用合成前滚迁移证明旧镜像在新库结构上可用，`--destructive` 参数证明该检查在破坏性迁移下确实会变红。

### 3.2 迁移机制（Alembic）

由 [#53](https://github.com/Moshuiwang/lingxi/issues/53) 认领。这一组把 `V-部署-05` 的"可前滚"落到具体机制上，并守住引入 Alembic 时**新出现的**几类沉默失败。执行者是 `scripts/ci/check_alembic_revisions.py`（不连数据库）与 `scripts/ci/check_migration_chain.sh`（真库），两者都挂在 `scripts/ci/verify_repository.sh` 上。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-迁移-01 | 空库上 `alembic upgrade <基线>` 与顶层编号 SQL 按序建库，两者 `pg_dump --schema-only` 归一化后**零差异**（仅排除 `alembic_version` 表及其主键）。约束名、索引名、触发器定义、函数体、部分索引谓词、表达式索引、COMMENT 全部逐字节相同 | L2（真库 + diff） | 已验证 |
| V-迁移-02 | 基线不含 `IF NOT EXISTS` 一类幂等伪装：已按编号 SQL 建成的库上直接 `alembic upgrade` **必须失败**（对象已存在），失败后表结构与数据不变，且不留下 `alembic_version` 表 | L2（真库否定） | 已验证 |
| V-迁移-03 | `alembic upgrade head` 后 `alembic_version` 恰 1 行且等于 head；在 head 上重跑 `upgrade` 是空操作，结构不变 | L2（真库） | 已验证 |
| V-迁移-04 | 迁移工具链不进运行时：`src/` 下不出现 `sqlalchemy` 或 `alembic` 字样，生产进程镜像不装 SQLAlchemy | L2（门禁扫描） | 已验证 |
| V-迁移-05 | `alembic.ini` 不含可用的默认连接串；未设 `LINGXI_MIGRATION_DSN` 时迁移**明确失败**，且不回落到业务的 `LINGXI_POSTGRES_DSN` | L2（配置检查 + 真库否定） | 已验证 |
| V-迁移-06 | 门禁的迁移检查只在一次性 scratch 库上进行并在结束后删除；主测试库全程不被写入（无 `alembic_version`）。迁移与门禁输出不打印连接串 | L2（真库） | 已验证 |
| V-迁移-07 | revision 链恰好 1 个 head、1 个 base，无孤儿 revision；每个 `downgrade()` 要么真正逆转要么显式 `raise`，不存在静默空实现；基线内嵌的编号 SQL 副本与磁盘原文**逐字节**相同，且每个 `migrations/*.sql` 都被覆盖；`migrations/README.md` 记录的 head 与基线 id 与实际链一致 | L2（门禁静态检查） | 已验证 |
| V-迁移-08 | 基线**之上**的每条 revision 真往返：`downgrade` 回上一版的结果与空库直达上一版逐字节相同，再 `upgrade` 回来与往返前逐字节相同 | L2（真库往返） | 已验证 |

**`V-迁移-07` 补的是改造引入的覆盖面缩水**：换成 `alembic upgrade head` 后孤儿 revision 不再保证被执行、也不报错，故须显式断言。

**V-迁移-02 是这一组里最容易被"修好"的一条。** 让基线在旧库上跑得通只需要加几个 `IF NOT EXISTS`，看起来还更健壮；代价是把"版本号对不上"这种响亮的错误，换成"迁移记录说做过了、实际什么都没做"这种沉默的错误。旧库的正确接管路径是先 `alembic stamp`，写在 `migrations/README.md`。

**2026-08-26 对账（#100）翻转为已验证**（全组，#53，主提交 `b8a00a5`）：
- `V-迁移-01` → `scripts/ci/check_migration_chain.sh`（脚本头部注释自述"断言：V-迁移-01（等价）"，`pg_dump --exclude-table=alembic_version` 逐字节比对，已直接读取脚本源码确认逻辑）
- `V-迁移-02` → 同脚本"旧库未 stamp 直接 upgrade 必须失败（V-迁移-02）"段落，否定断言；commit `b8a00a5`/`3aeeb74`
- `V-迁移-03` → 同脚本"重复执行必须是空操作"段落 + `test_alembic_revision_check.py`
- `V-迁移-04` → `test_alembic_revision_check.py::RuntimeIsolationTest` + `RealRepositoryStateTest::test_repository_source_has_no_migration_toolchain`
- `V-迁移-05` → `test_alembic_revision_check.py::AlembicIniTest` + `MigrationDsnTest` + `MigrationEnvWiringTest::test_env_does_not_fall_back_to_the_business_dsn`
- `V-迁移-06` → `scripts/ci/check_migration_chain.sh` 使用一次性 scratch 库并 DROP + `test_alembic_revision_check.py::MigrationDsnTest::test_error_messages_never_echo_the_connection_string`
- `V-迁移-07` → `test_alembic_revision_check.py::DowngradeShapeTest` + `EmbeddedCopyTest` + `RealRepositoryStateTest::test_every_revision_file_has_a_real_downgrade` + `test_baseline_embedded_copy_matches_disk`
- `V-迁移-08` → `scripts/ci/check_migration_chain.sh`"逐条 revision 真往返（V-迁移-08）"段落，脚本自述引用；commit `b8a00a5`/`88f43bc`

> **`V-部署-08/09/10` 补于 2026-07-29（Issue #29）**：此前只以散文存在于 `AGENTS.md`，从未被实装，暴露了三处缺口（SDK 事件名无声明、CI 用裸 `python3` 掩盖版本问题、新增子目录未验证进制品）。
>
> **教训：散文规则写一段话就能满足，只有给断言编号并接入会变红的 CI，它才会被实装。**

**现在不做**：CD 流水线、发布脚本、蓝绿/金丝雀发布、跨进程聚合观测（需 S9）。宿主层最小监控告警（D5）已真实运行并在 `biai-stage` 取得注入取证（`V-部署-17`，见上文）——不再列入「现在不做」；面向 AI 可读的完整持续监控体系仍是未来能力，见 [#410](https://github.com/Moshuiwang/lingxi/issues/410)。#92/#153 交付的应用内告警与健康检查仍只是 L2，不等于真实生产已在 `biai-stage`/`biplus-prod` 验证。

---
