# 产品验收矩阵 · 审计与保留

> 本册是[产品验收矩阵](验收矩阵.md)的分册，收录第一章「关键机制验收矩阵」的「审计与保留」、「数据保留清理」、「花名册审计日报」共 3 节，断言族 `V-审计-*`、`V-保留-*`、`V-花名册-*`。断言正文、层级与状态列的口径不因分册改变；状态三态与合同章节映射仍以总册为准。
>
> **导航**：[总册（分册索引 / 体量预算 / 状态口径 / 合同条款覆盖清单）](验收矩阵.md)；同级分册 [开通与身份](验收矩阵-开通与身份.md)、[会话与执行](验收矩阵-会话与执行.md)、[告警与内测通报](验收矩阵-告警与内测通报.md)、[接入与管理](验收矩阵-接入与管理.md)、[权限与银河](验收矩阵-权限与银河.md)、[交付与投递](验收矩阵-交付与投递.md)、[部署与迁移](验收矩阵-部署与迁移.md)。
>
> **体量预算**：分册后总量触发线（400KB 或 1500 行，先触发者生效）按**每个分册文件**独立计量，数值不变；`V-*` 断言单行 800B 棘轮跨总册与全部分册按断言编号丈量，同样不变。两条口径与裁定见[总册「体量预算」](验收矩阵.md#体量预算)，门禁是 `scripts/ci/check_matrix_row_size_ratchet.py`。

### 审计与保留

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-审计-01 | 普通业务与 scheduler 身份对 `audit_event` 的直接 `UPDATE` / `DELETE` 被拒绝；只有受限清理函数可删除已经到期的行 | L2（真库） | 未认领 |
| V-审计-02 | 新增字段默认**不**进入审计明细，必须显式加入白名单 | L2（脱敏机制单测）——防止未来新字段静默泄露 | 已验证 |
| V-审计-03 | 审计明细中不出现凭据、完整令牌。由三道合成：结构化入参按字段白名单收录、凭据类键名不落原值、**字段名本身也按自由文本脱敏**；自由文本（错误原文、回执正文、畸形工具名、白名单字段的字符串值）另加与键名无关的长度上界——含数字的 16+ 字符连串、或 32+ 字符的任意连串一律抹除 | L2（模式扫描 + 否定断言） | 已认领 |
| V-审计-04 | `chat_message`、`audit_event`、`task.prompt`、Agent JSONL、`manual_review.detail`、`pending_action` 内容、发布/同步详情、`inbound_event` 和 `delivery` 运行记录均在各自 `expires_at` 到达时删除或不可逆脱敏；其他未到期内容完好。清理函数固定 `search_path = pg_catalog` 且目标对象全限定，伪造调用方搜索路径不能改删其他对象 | L2（真库 + 文件系统，注入时钟与伪造 schema） | 未认领 |
| V-审计-05 | 开通前发送的内容不写入 `chat_message`，且不在任何回复中回显 | L2（真库）+ L4a | 已认领 |
| V-审计-06 | 月分区边界、清理失败或 scheduler 延迟均不会把可识别内容的到期时间后移；发现到期残留时告警并阻止发布候选。`PUBLIC` 与非 scheduler 身份不能调用清理函数，scheduler 只获精确函数签名的 `EXECUTE` 且无分区 DDL 权限 | L2（真库，注入时钟、失败与角色切换） | 未认领 |
| V-审计-07 | 数据库迁移在 PostgreSQL 16 真库实际执行成功；跨时区样例的 `expires_at` 与来源时间相差固定 2160 小时，调用方不能自定义或后移到期时间 | L2（PostgreSQL 16 真库） | 未认领 |

**V-审计-03 的残余缺口必须写在这里，不许再声称它是绝对保证。** 产品合同第 109 / 121 行用的是绝对措辞，但实现已被证明做不到绝对——下面两类完整凭据仍会原样进入审计明细：

| 残余缺口 | 为什么不堵 |
|---|---|
| **纯字母、短于 32 字符的裸秘密**出现在错误正文、回执正文或工具名里 | 这三处不经过字段白名单，长度上界是唯一可用的规则，而它认不出纯字母短串。曾经写过"这一类由字段白名单承担"——**那句话是假的闭合**，字段白名单根本不覆盖自由文本 |
| **模型把凭据当成合法形态的工具名发出来**（`sk-live-...` 符合 `[A-Za-z0-9_.-]+`） | 对拒绝式白名单来说，"模型试图调用什么"是最重要的一条审计事实。把未知工具名投影成哈希或长度能堵住这条，但会让屏障的留痕失去用处——**代价大于收益，故意不堵** |

白名单字段里的**数字值**同样原样保留，这不是缺口而是设计：字段进白名单意味着已显式批准记录它，epoch 毫秒与雪花 ID 正是排障要看的事实。若某字段可能承载凭据，正确做法是不把它加进白名单。

上述残余缺口是产品负责人知情接受的当前边界，不得自行放宽。`V-执行-20` 只收紧 worker 出口，审计核心两条缺口仍存在；形成经过见[形成记录](../参考证据/验证与门禁形成记录.md)。

以上九十天断言和单用户删除断言只覆盖正常运行库与在线文件；数据库备份、WAL 归档和用户目录备份按产品决定明确排除，不要求删除编排修改灾备副本。灾备保存、恢复和销毁由公司的灾备与恢复规则另行验收。

**2026-08-26 对账（#100）翻转为已验证**：`V-审计-02` → `test_execution_tool_gate.py::ToolFailureAuditTest::test_audit_input_fields_are_opt_in_and_credential_like_keys_are_redacted`；commit `89cf05b`。`V-审计-03` 保守保持已认领——断言本身声明"不出现凭据、完整令牌"，但本文件已详细自述已知残余缺口（纯字母短于 32 字符裸密钥、伪装成工具名的凭据），标"已验证"会与紧邻的残余缺口说明冲突，故不翻转。`V-审计-05` 声明含 L4a，未补验。

### 数据保留清理

`V-审计-04/06/07` 覆盖的对象大多还未建表，仍是「未认领」。本组只覆盖已存在的两张表（`feishu_org_sync_run`、`galaxy_import_batch`）上的保留执行机制，全部由 [#54](https://github.com/Moshuiwang/lingxi/issues/54) 认领。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-保留-01 | 两张父表中 `expires_at <= p_now` 的行被删除；未到期行**逐字段**未变；`feishu_delegated_subject` 与 `app_user` 行数分毫不动 | L2（真库） | 已验证 |
| V-保留-02 | 一次调用同时处理两张父表，各自返回自己的删除数；只实现一张时另一张恒为 0 | L2（真库） | 已验证 |
| V-保留-03 | 边界逐点：`started_at + 2159:59:59` 不删、恰好 `+2160h` 删、`+2160h+1s` 删（`<=` 语义） | L2（真库） | 已验证 |
| V-保留-04 | 清理条件与 `current_batch_id()` 的过期过滤严格互补：不存在"既是当前有效批次、又可被清理"的瞬间；清理前后当前批次不变、其子行不变 | L2（真库） | 已验证 |
| V-保留-05 | 跨夏令时时区里 `expires_at - started_at` 恰为 2160 绝对小时，且**不等于** `started_at + interval '90 days'` | L2（真库） | 已验证 |
| V-保留-06 | 8 张子表逐表随父行消失、无孤儿子行、未到期批次的子行内容未变；`pg_constraint` 中指向两张父表的外键恰 8 条且 `confdeltype` 全为 `'c'` | L2（真库 + catalog） | 已验证 |
| V-保留-07 | `galaxy_import_batch` 写入触发器：调用方传入的 `expires_at` 被覆盖为 `started_at + 2160h`，UPDATE 不能后移、不能改来源时间；`feishu_org_sync_run` 既有同型行为不变 | L2（真库） | 已验证 |
| V-保留-08 | 迁移前写下的偏离行被回填校正为 `started_at + 2160h`；既有代码路径写出的行本来就精确满足该不变式 | L2（真库） | 已验证 |
| V-保留-09 | 批量上限生效：`p_limit` 恰好限住删除数；超大值被夹取到函数自身上限；非正值被拒绝 | L2（真库） | 已验证 |
| V-保留-10 | 幂等：循环调用至返回 0 时总删除数等于到期行数，再调一次返回 0 且不报错 | L2（真库） | 已验证 |
| V-保留-11 | 拒绝晚于当前时间的 `p_now` 且零删除；到期时间晚于传入 `p_now` 的行不删；函数定义保留 `LEAST(p_now, clock_timestamp())` 下限 | L2（真库 + 定义检查） | 已验证 |
| V-保留-12 | 函数固定 `search_path = pg_catalog` 且目标对象全限定；调用方把同名影子表放进搜索路径时，影子表分毫未动、真表照删 | L2（真库，伪造搜索路径） | 已验证 |
| V-保留-13 | 函数 `SECURITY DEFINER` 且属主为 `lingxi_retention_owner`；`PUBLIC` 与 `lingxi_app` 无 `EXECUTE`、`lingxi_scheduler` 按精确签名可调；`lingxi_app` 与 `lingxi_scheduler` 都不能直接 `DELETE` 目标表、也不能取得属主角色；四个角色随迁移幂等建立且均无登录能力；越权授出的属主成员资格在重新应用迁移时被收回 | L2（真库，切换会话身份） | 已验证 |
| V-保留-14 | 返回摘要只含表名、删除数与到期时间范围；摘要与由它产生的日志不含任何人员数据字段值 | L2（真库） | 已验证 |
| V-保留-15 | `lingxi-scheduler` 进程按周期实际调用清理（观察点为到期行真的消失）；职责之间失败隔离，一个职责抛异常时另一个本轮仍执行；连续失败不退出进程且日志不含用户内容 | L2（真库 + 可注入） | 已验证 |
| V-保留-16 | 清理失败只能重试，不会把任何行的 `expires_at` 或 `started_at` 往后挪 | L2（真库 + 可注入） | 已验证 |
| V-保留-17 | 收到 `SIGTERM` 后不再领取新的清理批次（`calls_after_stop == 0`），且库中不存在没有父行的子行；进程报告的删除数与库里实际少掉的行数一致 | L2（真实进程 + 真实信号 + 真库） | 已验证 |
| V-保留-18 | 迁移在 PostgreSQL 16 真库前滚成功；回滚干净移除本次新增的函数与触发器、不删除数据、不删除集群级角色；前滚与回滚往返两次成功；不含本次改动的既有写路径在升级后的库上仍然成功 | L2（真库） | 已验证 |
| V-保留-19 | 批次被回收后，同一份导出可以重新导入，得到新批次、重新起算的 `expires_at`，并成为当前有效批次 | L2（真库） | 已验证 |
| V-保留-20 | 豁免只由 `expires_at <= p_now` 表达：全部批次均已过期时连最近完成的那个也被删除；组织快照侧的可观察状态因此由 `STALE` 变为 `UNAVAILABLE`（两者都不建档、不产生候选） | L2（真库） | 已验证 |
| V-保留-21 | 两张父表的 `BEFORE DELETE` 行级防线按**双条件**放行——`expires_at` 已到期**且** `current_user = 'lingxi_retention_owner'`。四象限逐格：属主×到期=成功、属主×未到期=拒、非属主×到期=拒、非属主×未到期=拒；误授 `lingxi_app` DELETE 后连过期行也删不掉。`TRUNCATE` 不经行级触发器，由权限挡：没有任何 `lingxi_*` 角色持有它 | L2（真库，切换会话身份） | 已验证 |
| V-保留-22 | 并发持锁时清理在有限时间返回（函数级 `lock_timeout`），被锁的表整批让路、行未被删、下一轮收走；摘要**区分**「让路」与「本来就没有到期行」（两者删除数都是 0），scheduler 把让路的一轮记为 warning 而非正常完成；并发两次清理的摘要之和不超过实际删除量 | L2（真库 + 并发连接） | 已验证 |
| V-保留-23 | 预建同名角色被强制校验：超级用户身份响亮失败；属主角色的 LOGIN 被收紧；属主角色不得（直接或经中间角色**间接**）与业务角色互相取得权限。属主移交前后成员关系三元组（admin / inherit / set）与 schema 授权面完全一致 | L2（真库 + CREATEROLE 身份） | 已验证 |
| V-保留-24 | 迁移在**非超级用户**（`CREATEROLE`，生产托管方形态）身份下可完整执行：首次 upgrade 与两轮 downgrade→upgrade 均成功，函数属主、`EXECUTE` 授权与成员关系还原逐项正确 | L2（真库，非超级用户执行身份） | 已验证 |
| V-保留-25 | 回滚后不残留本 revision 授出的任何权限：两张父表上四个角色的五类表权限、schema `public` 的显式 ACL 条目、属主角色的成员关系均已收回 | L2（真库） | 已验证 |

**`V-保留-21` 的双条件是编排者裁定的结果**：只按到期时间判会让误授权限删除过期行，只按角色判属主自己能删未到期行，故取双条件；运维紧急删除路径见 [migrations/README.md](../../migrations/README.md)。

**`V-保留-11` 第三分句是源文本断言，非行为断言**：`LEAST(p_now, clock_timestamp())` 是第二道闸，当前与直接用 `p_now` 行为一致，价值只在校验被放宽那天兑现，如实标注为文本检查。

**`V-保留-20` 的两条后果是知情接受的产品选择**（[Issue #54 编排者裁定](https://github.com/Moshuiwang/lingxi/issues/54) ②④）：九十天是硬上限，删行不改变下游可观察结果；[#69](https://github.com/Moshuiwang/lingxi/issues/69) 决定不为临时导出加永久墓碑，`V-保留-19` 固化该行为。

**2026-08-26 对账（#100）翻转为已验证**（全组 25 条，#54，主提交 `580d4cc`，逐条精确对应 `tests/test_retention_postgres.py`）：
- `V-保留-01`/`02` → `ExpiryCollectionTest::test_expired_rows_in_both_parent_tables_are_removed_in_one_call` + `test_a_row_that_expires_one_second_later_is_untouched_field_by_field`
- `V-保留-03` → `ExpiryCollectionTest::test_the_expiry_boundary_is_inclusive_and_exact_to_the_second`
- `V-保留-04` → `ExpiryCollectionTest::test_the_cleanup_condition_and_the_current_batch_filter_are_complementary`
- `V-保留-05` → `ExpiryCollectionTest::test_the_window_is_2160_absolute_hours_not_ninety_calendar_days`
- `V-保留-06` → `CascadeBoundaryTest::test_exactly_eight_foreign_keys_point_at_the_parents_and_all_of_them_cascade` + `test_each_child_table_is_emptied_by_its_own_cascade_edge`
- `V-保留-07` → `ImmutableExpiryTriggerTest`（5 用例）
- `V-保留-08` → `CleanupFunctionContractTest` 内类文档字符串标注"V-保留-08（验收 D-06）"的回填校正用例
- `V-保留-09` → `CleanupFunctionContractTest::test_a_batch_limit_caps_how_many_rows_a_single_call_removes` + `test_an_oversized_limit_is_clamped_and_a_non_positive_limit_is_refused`
- `V-保留-10` → `CleanupFunctionContractTest::test_repeated_calls_converge_and_then_report_zero`
- `V-保留-11` → `CleanupFunctionContractTest::test_a_future_p_now_is_refused_and_deletes_nothing` + `test_rows_that_expired_after_the_given_moment_are_kept` + `test_the_definition_keeps_the_clock_floor`
- `V-保留-12` → `CleanupFunctionContractTest::test_a_shadow_schema_cannot_redirect_the_cleanup` + `test_a_shadow_temp_type_neither_diverts_nor_breaks_the_cleanup`
- `V-保留-13` → `RolePrivilegeTest`（19 用例，含 security definer/属主/权限矩阵）
- `V-保留-14` → `SummaryContentTest::test_the_summary_carries_table_names_counts_and_a_time_range_only`
- `V-保留-15`/`16` → `SchedulerDutyTest::test_the_scheduler_loop_actually_removes_expired_rows` + `test_a_failing_cleanup_never_postpones_an_expiry`
- `V-保留-17` → `SchedulerDutyTest::test_sigterm_leaves_no_half_deleted_state_and_claims_nothing_after_stopping`
- `V-保留-18` → `MigrationRoundTripTest::test_upgrade_and_downgrade_round_trip_twice` + `test_the_previous_image_still_works_against_the_upgraded_database`
- `V-保留-19` → `ReimportAfterCleanupTest::test_the_same_export_can_be_imported_again_after_its_batch_was_collected`
- `V-保留-20` → `NoUnconditionalReprieveTest`（2 用例）
- `V-保留-21` → `RolePrivilegeTest::test_the_delete_guard_covers_all_four_quadrants`
- `V-保留-22` → `CleanupFunctionContractTest::test_a_locked_row_bounds_the_wait_instead_of_blocking_the_whole_duty` + `test_two_concurrent_cleanups_never_delete_the_same_row_twice` + `BlockedRoundLoggingTest`（区分让路与无到期行）
- `V-保留-23` → `RolePrivilegeTest::test_a_pre_existing_login_owner_role_is_normalised` + `test_an_owner_role_that_inherits_other_privileges_is_refused` + `test_the_handover_restores_the_membership_graph_exactly`
- `V-保留-24` → `RolePrivilegeTest`（`NOSUPERUSER CREATEROLE` 探针角色跑整条迁移链）
- `V-保留-25` → `MigrationRoundTripTest::test_downgrade_leaves_no_privilege_granted_by_this_revision`

### 花名册审计日报

合同「统一用户记录与权限变化」要求 Lingxi **每日比对已开通用户的花名册资料与存档**，变化经受控管理群的审计日报通知管理员人工处理；飞书账号复用换人不做自动拦截，就靠这份日报被发现。全部由 [#52](https://github.com/Moshuiwang/lingxi/issues/52) 认领。

**现行口径以产品负责人 2026-08-08 的 D2 裁定为准**（[#52](https://github.com/Moshuiwang/lingxi/issues/52) 决策登记、[Plan #104 D2](https://github.com/Moshuiwang/lingxi/issues/104#issuecomment-5225625313)）。D2 覆盖了 2026-08-06 的「选项 A + 甲」中的两项：

- **持久快照载体**：花名册读取保存最近一次成功、完整、非空的快照，跨重启与发布都还在；源头异常一律保留上一份、不覆盖。载体是数据库表（`roster_snapshot`/`roster_snapshot_row`，迁移 `0063`），此前「零新表」表述已作废。
- **展示口径**：受控管理群的审计日报允许展示姓名/工号/邮箱原值（否则管理员无法定位处理），仍不得含凭据、令牌或与处理无关的敏感数据，也无可执行入口；`V-花名册-21/22` 按此改写。

**未被 D2 改动的**：比对基线仍是 `app_user` 建档时刻存档的三字段，不是「昨天的快照」；快照只保证花名册值来自一整轮可信读取；`V-花名册-33`（审计日志不含花名册字段值）照旧生效。

**快照的保留期由产品负责人 2026-08-17 裁定**：**始终保留最近一份，豁免九十天规则**；替换即删旧，不积累历史版本；快照因源头长期读不到而超龄时按日报告警提醒、**不自动删除**（`V-花名册-47`，S-B-04 已接线）。

**本节实现分四个已认领切片**（Trace #203/Epic B）：S-B-01 交付分页 reader 与四态判定（`V-花名册-37…40`），S-B-02 交付持久快照载体（`V-花名册-41…46`），S-B-04 交付调度注册与日报接线（`V-花名册-21/22/47/48`）；真实花名册读取与真实群送达仍属 L4a，未做。

**写侧与通知侧在 [#275](https://github.com/Moshuiwang/lingxi/issues/275) 之后是两个互斥职责**（改变了 `V-花名册-29` 判定面）：此前"写快照"绑在 `RosterAuditDuty` 内部、依赖管理群配置，导致没配管理群时快照永不写、员工永不能开通（2026-08-21 冒烟实测坐实）。拆分后 `RosterSnapshotSyncDuty` 只依赖花名册 Base 坐标独立注册（只写不发），与 `RosterAuditDuty` 互斥装配，两者齐全时仍由后者一次完成全链路。断言在 `tests/test_roster_snapshot_sync_duty.py`。

**写侧的当日水位判据**：`_completed_on` 只在本轮真的换上新快照时才置位，保旧轮与失败轮不置位、下一 tick 重试，同日重试安全（整体替换）。`RosterAuditDuty.run_once` 先取花名册（含快照写入）、后读比对基线，避免日报正文故障拖停开通链硬前提。


**状态列口径**：本组全部为「已认领」。L2 绿灯不等于真实链路验收——真实花名册读取所需的专用主体凭据自 2026-08-09 起未落盘（Issue #52 的 G-READ 判定），真实群通知与真实读取均未做，因此没有一条可以标「已验证」。

| # | 可验证断言 | 层级 | 状态 |
|---|---|---|---|
| V-花名册-01 | 姓名 / 工号 / 邮箱三字段**逐一**独立用例：单字段变化恰产生一条差异且指名该字段 | L2（纯函数） | 已认领 |
| V-花名册-02 | 三态：全等得空差异集（非 None 不抛）；不等得变化；花名册查无得移除。A 方案下**无「新增人员」态** | L2（纯函数） | 已认领 |
| V-花名册-03 | 转交肯定面：姓名与邮箱同时变化时单独标出，标记可独立取出 | L2（纯函数） | 已认领 |
| V-花名册-04 | 转交否定面：只改姓名、只改邮箱、姓名与工号同变，三者均**不**标为转交 | L2（纯函数） | 已认领 |
| V-花名册-05 | 人员 ID 按**完整值**比对：前 6 位相同、完整值不同的两个 ID 不得判为同一人 | L2（纯函数） | 已认领 |
| V-花名册-06 | 同一人员 ID 多行时行为唯一确定，日报单列一类，不静默取任意一行 | L2（纯函数） | 已认领 |
| V-花名册-07 | **不对称空值口径**：存档为空的字段不参与比对；存档有值而花名册变空必须报。双向用例 | L2（纯函数） | 已认领 |
| V-花名册-08 | 比对输出对象**本身不含字段值**，只有标识、变化字段名与转交标志；比对模块不 import 任何可做 I/O 的模块。D2 之后这是**分层**约束（取值属渲染层）而不再是脱敏承诺的落点 | L2（纯函数 + 源码扫描） | 已认领 |
| V-花名册-09 | 部门与账号状态不进差异集（即便输入行携带）；花名册 adapter 的读取字段集不因本切片扩大 | L2（纯函数） | 已认领 |
| V-花名册-10 | 比对集只来自 `provisioning_state='active'`：其余六种状态各一条均不出现 | L2（真库） | 已认领 |
| V-花名册-11 | `account_state` 为 `deleting` / `deleted` 的用户排除出比对集，删除编排清空字段不得误报为「姓名变化」 | L2（真库） | 已认领 |
| V-花名册-12 | 未开通人员的任何字段值不出现在比对输出、日报正文、审计或日志中（断言字符串不出现，非行数为 0） | L2（真库） | 已认领 |
| V-花名册-13 | **迁移是 DDL 的唯一载体**（D2 之后本组确有新表，「零新表 / 零迁移」旧表述作废）：比对、渲染、读取与快照适配四层源码均不 import alembic / sqlalchemy、不含建表语句；建表只在 `0063_roster_snapshot` 里，其单 head、连通性与非空 `downgrade` 由 `check_alembic_revisions.py` 承担，逐条真往返由 `check_migration_chain.sh` 承担，且两个委托确实接在门禁里 | L1（源码扫描）+ L2（真库往返） | 已认领 |
| V-花名册-14 | **存档不写回**：三字段全变跑完一整轮后，`app_user` 的姓名 / 工号 / 邮箱与 `updated_at` 逐字段与运行前完全相同；读取模块的 SQL 常量中无任何写语句 | L2（真库 + 源码扫描） | 已认领 |
| V-花名册-15 | 账号状态按需回读**不落库**：哨兵值经输入行走完整轮后，全库文本列中查无此值 | L2（真库） | 已认领 |
| V-花名册-16 | 周期实际调用日报职责，观察点是注入的基线读取与发送接口真的被调到 | L2（可注入） | 已认领 |
| V-花名册-17 | 职责间失败隔离：日报炸掉时轮换与清理本轮仍执行，反之亦然（两个方向各一用例） | L2（可注入） | 已认领 |
| V-花名册-18 | 连续失败不退出进程；日志只记异常类型，不记异常正文 | L2（可注入） | 已认领 |
| V-花名册-19 | `SIGTERM` 后不开新轮、在途发送完整做完、无半发送、超时内退出（真实子进程 + 真实信号） | L2（子进程） | 已认领 |
| V-花名册-20 | 停止之后发送次数为 0，连基线都不再读取；停止信号落在**读取阶段**时同样不发送、不置水位（干净中断） | L2（可注入） | 已认领 |
| V-花名册-21 | 日报逐条含人员标识、**用于定位的存档身份原值（姓名 + 工号）**、变化字段名与转交标注，转交条目与普通变化条目可区分；没有变化字段的条目（花名册查无、同一 ID 多行）同样给全身份；存档为空的字段渲染成显式占位符而不是空白；正文另含快照读取时间与同步状态，日期与时间一律标注 UTC | L2（纯函数） | 已认领 |
| V-花名册-22 | **D2 的否定面**（取代旧「甲否定面：正文无任何真实值」）：正文不含凭据、令牌、认证信息或与管理员处理无关的敏感数据；不含未开通人员的任何资料；不含比对结论以外的花名册整表内容——**花名册当前值一律不进正文**，也不出现新旧值对形态；未被报出的人一个字段都不露面；存档邮箱值不印（定位靠姓名 + 工号，「邮箱变了」由字段名说明） | L2（纯函数 + 模式扫描） | 已认领 |
| V-花名册-23 | 人员标识为 `app_user.id` 完整 ULID：外部标识前 6 位相同的两名用户在日报中标识不同；正文不出现脱敏缩短形态、不出现飞书外部标识原值、不对 ULID 截断 | L2（纯函数） | 已认领 |
| V-花名册-24 | 正文无任何可执行入口：无链接、按钮、回调；发送载荷为纯文本而非卡片 | L2（纯函数 + 可注入） | 已认领 |
| V-花名册-25 | 空差异日出站 0 次且审计恰 1 条；非空差异日发送恰 1 次（两面用例） | L2（可注入 + 真库） | 已认领 |
| V-花名册-26 | 花名册查无此人单列一类且零自动动作：不改 `app_user`、不停用、不发布、不产生待办 | L2（可注入 + 源码扫描） | 已认领 |
| V-花名册-27 | 出站可注入：构造函数内无任何 import、不建 client、不发请求 | L2（源码扫描 + 可注入） | 已认领 |
| V-花名册-28 | 群 ID 由环境变量注入，真实形状的群 ID 不出现在代码库任何文件中 | L1（源码扫描） | 已认领 |
| V-花名册-29 | 四个前置（管理群 ID、花名册 Base `app_token`、`table_id`、读取令牌供给）任缺其一，日报职责（`RosterAuditDuty`）不注册、进程照常启动、审计**恰 1 条**（`roster_audit.duty_not_registered`）且指名原因 / 变量不回显值；前置齐备时职责由 `build_loop` 真实注册且装配阶段不发任何请求。**日报职责未注册时**，写侧职责 `RosterSnapshotSyncDuty`（Issue #275）按它**自己的两个前置**（花名册 Base `app_token`、`table_id`；读取令牌供给由 `build_loop` 默认建出）单独尝试注册，缺项审计同样**恰 1 条**、前缀为 `roster_snapshot_sync.*` 以便与日报那条分辨；两个职责**互斥注册**，任一时刻至多一个在触发花名册读取。**注册完全由配置驱动**，不得在装配里写死一个永远不满足的前置 | L2（可注入） | 已认领 |
| V-花名册-30 | 发送失败不影响其他职责、只记审计、不算已发送，同日下一轮重试 | L2（可注入） | 已认领 |
| V-花名册-31 | 不重复发送：单轮一次；同进程同日跨轮一次（注入时钟）；重启当日至多重发一次且重发载荷与首次逐字段一致；**投递不确定态**（请求已达飞书而响应超时）下的重试携带与首次相同的投递 `uuid`，同日相同、跨日与跨群不同 | L2（可注入） | 已认领 |
| V-花名册-32 | 日报动作经 `AuditSink` 记审计，字段含日期、比对集规模与条目计数 | L2（可注入） | 已认领 |
| V-花名册-33 | 审计记录与日志均不含任何花名册字段值或外部标识原值（模式扫描） | L2（可注入 + 真库） | 已认领 |
| V-花名册-34 | 运行时源码中 `redact_identifier()` 的每一次调用都是日志参数，不被当作键比较、去重或定位；日报渲染完全不调用它 | L1（源码扫描） | 已认领 |
| V-花名册-35 | 合成端到端：6 名已开通 + 1 名 guest，日报恰含 4 条、转交恰 1、移除恰 1；缺字段者、无变化者与 guest 均不出现；`app_user` 三字段零变化。正文的取值形态按 `V-花名册-21/22` 的 D2 口径判定：被报出的四人存档身份必须出现，花名册当前值、存档邮箱与未被报出的人一律不出现，同一批值在审计记录里仍然一个都不许有 | L2（真库） | 已认领 |
| V-花名册-36 | 端到端在 CI 实跑并打印执行计数；配置了真库却把该用例跳过时变红 | L2（真库） | 已认领 |
| V-花名册-37 | 一轮读取结算成**四态**且判定次序固定：先判完整性再判空——源头自报有行却一行没给判「不完整」而不是「空源」；四态各一用例，且只有 `COMPLETE` 的 `complete_nonempty` 为真 | L2（可注入） | 已认领 |
| V-花名册-38 | 读取失败分**明确失败 / 结果不明**两类：业务错误码明确非 0 为明确失败；传输异常、超时、响应体不可解析、响应形状不对、游标停滞、翻页超上限一律为结果不明。分类反向时变红；凭据 provider 自身抛出的异常不折成源头读取失败 | L2（可注入） | 已认领 |
| V-花名册-39 | **半轮不外泄**：读取中途失败时返回的行恒为空，半轮读到的行只以 `partial_rows` / `partial_pages` 计数出现；未预期异常不被吞成「源头读取失败」，原样上抛 | L2（可注入） | 已认领 |
| V-花名册-40 | 重复与空值只上报计数、不做任何自动取舍：同一人员 ID / 工号的重复行原样保留；**空键不算重复**（缺工号的行不得被算成一组重复）；必需列清单里重复配置同一列时，整列缺失的判定不被击穿 | L2（可注入） | 已认领 |
| V-花名册-41 | **快照替换门槛只认 `status`（`complete_nonempty`）**：`INCOMPLETE` 携带非空行时**不得**替换（否定用例），`EMPTY_SOURCE` 与两类 `FAILED` 同样不替换；以「行非空」作判据的实现在该否定用例上变红 | L2（可注入） | 已认领 |
| V-花名册-42 | **原子替换**：删旧、写元信息、写行在同一个事务内完成；写行或写元信息中途失败后，库里仍是替换前那一份完整快照，不留半份（真库注入失败两个方向各一用例） | L2（真库） | 已认领 |
| V-花名册-43 | 空源 / 不完整 / 明确失败 / 结果不明四类**保旧原因互不合并**，各自产出可断言的告警事实（含错误码与失败分类）；`COMPLETE` 轮不产生告警 | L2（可注入） | 已认领 |
| V-花名册-44 | **首轮语义**：库中从未有过快照时不得报「保留了上一份」——该情形单独成一类事实，上一份的读取时间、行数与年龄均为空；有旧快照时事实里带上它的读取时间、行数与年龄（年龄为空而不是 0） | L2（可注入 + 真库） | 已认领 |
| V-花名册-45 | 载体**只保存最近一份**：替换后库中恰一份快照元信息、无孤儿行；数据库层的单例约束拒绝第二份，零行快照被适配器与 `CHECK` 各拒一次 | L2（真库） | 已认领 |
| V-花名册-46 | 快照层的判定对象、审计事实、日志与快照元信息表**均不含任何花名册字段值**（只有状态、计数、列名、错误码）；行本身只存在于行表，且原样保留不去重 | L2（可注入 + 真库） | 已认领 |
| V-花名册-47 | 快照因源头长期读不到而超龄时，按日报告警提醒管理员且**不自动删除**（产品负责人 2026-08-17 裁定：始终保留最近一份、豁免九十天规则）：超龄判定与告警送达由日报侧接线承担，快照载体不得出现任何按时间删除快照的路径。阈值默认 48 小时、可由环境变量覆盖且覆盖值必须真的生效，边界属「还没超龄」那一侧，0 / 负数 / 非数字 / 大到等于关掉告警的值一律快速失败；**超龄那一天即使零差异也要发日报**（沉默是这一天最危险的输出），正文同时写明「快照不会被自动删除」 | L2（可注入 + 源码扫描） | 已认领 |
| V-花名册-48 | **没有任何可用快照时不进行比对**：库中从未有过快照而本轮又读不成功时，日报不得产生任何「花名册查无此人」条目（拿空行集比对会把全体已开通用户一次性报成移除），也不得渲染成「本次发现 0 条」；改为发出一条写明「本次未进行比对」与保旧原因的告警。有快照时（含超龄）照常比对，两种情形在正文与审计事实里可区分 | L2（可注入） | 已认领 |

**本组不覆盖什么。** 真实群通知与真实花名册读取属 L4a，两者都**未验证（证据等级 1）**：`FeishuGroupMessages` 与花名册 reader 的全部断言跑在注入的假传输层上，与 `adapters/feishu_directory.py` 同一姿态；真实读取所需的专用主体凭据自 2026-08-09 起未落盘（Issue #52 的 G-READ 判定，E1-A 授权码被烧事故的直接后果），真实面改依赖后续的 bootstrap 重授权切片。持久快照的**接线**已由 S-B-04 完成（`V-花名册-47/48`）：快照链挂进了每日日报职责，保旧告警事实接到 scheduler 的结构化警告日志，面向管理员的提醒走日报本身——`core/alerting.py` 的状态机只认心跳、任务滞留与发送连续失败三类信号，刻意没有为快照新鲜度新增第四类。**花名册读取所用的短期令牌供给已由 [#215](https://github.com/Moshuiwang/lingxi/issues/215) 接上**（方案 C 主接线，产品负责人 2026-08-18 裁定接受按需消费节奏，留痕见 [#203 决策评论](https://github.com/Moshuiwang/lingxi/issues/203#issuecomment-5321623142) 第 7 项）：凭据轮换职责**仍是一次性 `refresh_token` 的唯一消费者**，它按需消费一次并把派生的短期 `access_token` 放进**进程内**持有者（不落盘、不进日志与审计，重启即空），日报侧按新鲜度取用。消费频率随之从约 5.6 天一次变成按需（令牌寿命约 2 小时，正常一天约 12 次）。**[#276](https://github.com/Moshuiwang/lingxi/issues/276)（产品负责人 2026-08-21 裁定）解除了此前"每 UTC 日至多消费一次"的自设上界**，改为两次消费的最小间隔（默认 5 分钟）与每日消费次数上界（默认 100 次）双重保护；判据仍随凭据落盘、进程内不留第二份账本副本——`refresh_consumed_at`（最近一次消费时刻）与新增的 `refresh_consumed_count`（当日消费计数），均在文件锁内、置位消费标记之前判定。**唯一消费者与频率上界是两件不同的事**：2026-08-08 授权码被烧那次事故的形状是两个客户端抢占同一条通道，不是"换取太频繁"（详见 `core/identity/access_token_supply.py` 模块文档）。因此 `V-花名册-29` 的第四个前置现在只取决于配置：三个环境变量配齐即注册。**这一段全部是 L2 事实**：真实续期返回的短期令牌寿命、真实按需消费节奏与真实日报送达仍属 L4a、未验证。部门比对与账号状态落库由定案排除；账号复用换人的自动拦截由 [#34](https://github.com/Moshuiwang/lingxi/issues/34) 永久排除；群内处置由合同排除；`audit_event` 表属 S9，当前审计走结构化日志。

**`V-花名册-47`「源码扫描」半边在 [#215](https://github.com/Moshuiwang/lingxi/issues/215) 才真正兑现**：「不自动删」要证明一条路径不存在，此前无可断言的检查点。现由 `SnapshotHasNoTimeBasedDeletionPathTest` 扫描相关模块与迁移，拒绝带时间条件的删除语句、到期列与触发器，并有防空转的分类器自测用例。

**它是绊线，不是安全边界。** 已登记的盲区（Python 侧先算 ID 再删、词表外命名、动态 SQL 组装、仓库外运维脚本等）靠人工审查兜底；交付前已种入四种违规样本验红、九次分类器变异确认全部变红，详见[验证与门禁第八节](验证与门禁.md#八否定测试)。

**残留风险登记**（[#52](https://github.com/Moshuiwang/lingxi/issues/52) 决策登记与裁定 C2）：

- **同一处漂移每天重复上报**（知情接受）：基线是建档存档而非昨天的快照，人工处理前会每天重复出现，持久快照不改变这一条。
- **跨重启重发一份相同日报**（知情接受，尚未消解）：判重水位仍在进程内存里，真幂等要等水位有持久载体后另行交付；S-B-04 已核对并放弃「拿快照时间当水位」这条捷径，因为会让当天日报永远发不出去。
- **快照保留期已裁定，不再是残留**：始终保留最近一份、豁免九十天，超龄告警（`V-花名册-47`）与无快照时不比对（`V-花名册-48`）已由 S-B-04 交付。

**"一日"按 UTC 日切分**：日报在北京时间每天 08:00 后首轮发出，标题日期与管理员自然日相差 8 小时；是否改用固定业务时区待产品负责人裁定，本切片不改行为。

**投递不确定态的保护不是幂等承诺**：响应超时时重试会携带同一投递 `uuid` 交服务端去重，但平台侧去重窗口未验证（L4a），矩阵只承诺"重试携带同一 uuid"这一代码事实。

