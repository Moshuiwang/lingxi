# 开发计划

> 状态：草案，等待产品负责人复审。最后核对：2026-08-05。
> 依据：[产品合同与外部边界](../产品合同与外部边界.md)、[当前能力](../当前能力.md)、[架构设计](架构设计.md)、[验证与门禁](验证与门禁.md)、[代码框架](代码框架.md)、开放 Issue #16–#41。
>
> 本文只记录**切片划分、依赖顺序和排序理由**这类在单个 Issue 关闭后仍然有效的依据。进度、负责人、优先级调整在 [Lingxi 开发 Project](https://github.com/users/Moshuiwang/projects/2) 与各 Issue 中维护，本文不写完成状态、不排日历日期、不指派代理。切片实际开工时按[协作约定](../协作约定.md)领取或新建 Issue、写 TO PM 与验收断言，本文不替代那一步。

## 一、当前位置（2026-08-05）

- 四篇技术设计已成文；跨切片开工门禁（Agent SDK + 百炼最小真实链路）已于 2026-07-28 全部通过并解除（[验证与门禁 10.0](验证与门禁.md#100-唯一跨切片开工门禁agent-sdk--百炼最小真实链路)）。
- 唯一进行中的变更包是执行层组件（[PR #36](https://github.com/Moshuiwang/lingxi/pull/36)，Draft），被两项 P1 阻塞：完整凭据仍可能进入审计、辅助调用成功会掩盖核心查询失败，另需统一证据层级口径。
- 仓库中的正式业务代码为零；现有代码全部是 Bot-Test 测试资产（[代码框架第五节](代码框架.md#五测试资产与正式代码的边界)）。
- 身份链路的外部依赖已全部收敛（#16 可整体开工）；权限链路仍有多项产品决策未出（#17）。

## 二、切片与顺序

| 切片 | 内容 | 工作项 | 前置 |
|---|---|---|---|
| S0 执行层组件收口 | 修复 PR #36 两项 P1，统一证据层级口径，合并 | #29 / PR #36 | 无 |
| S1 最薄执行器闭环 | 可执行 worker 入口装上只读屏障与合成审计，`biai-stage` L4a | #37 | S0 |
| S2 首次开通身份链路 | 专用授权正式实现、资料同步、`open_id` 定位、建档与终态 | #16 | 无；「已存在 `open_id` 但姓名不一致」分支等 #34 |
| S3 银河权限落库与匹配 | 五张表落库导入、`user_id` 连接、姓名匹配三态 | #17 无阻塞部分 | 无 |
| S4 网关与会话任务基座 | 长连接、事件幂等、加表情、任务队列、同话题串行、`/new` `/stop`、两小时规则、流式卡片与文本兜底 | 开工时新建 Issue | 飞书接入与队列半边可即时做；Agent 会话接线在 S1 后 |
| S5 权限发布与就绪确认 | outbox → 多维表格 → MCP 同步确认循环 → 开通成功与转人工 | #17 主体 | S3；#17 决策 1/2/3/4/5；MCP 可比对字段探针 |
| S6 问数主链路组装 | S1+S2+S4+S5 串成首条端到端问数，含能力问答与输出约束检查 | 开工时新建 Issue | S1、S2、S4、S5 |
| S7 交付与云盘 | 目录权限模型探针 → 专属目录、文档/表格/图片交付、socket 交付服务 | 开工时新建 Issue | 探针先行；S4 |
| S8 管理 MCP 与确认卡 | 管理 MCP 服务端、待确认操作、本人卡片、三类角色、Root 恢复演练 | #18（parked） | 客户端选定并核实支持 MCP `2026-07-28` |
| S9 保留、删除与审计加固 | `expires_at` 触发器、受限清理函数、删除编排、数据库角色 | 开工时新建 Issue | 随 S5/S7 的表结构成形后即可做 |
| S10 高级工作台 | JumpServer 用户环境、交付 Skill 复用 | 开工时新建 Issue | S7 |
| S11 部署编排与生产准备 | Compose、镜像冻结、Alembic、V-部署断言、备份恢复演练 | 开工时新建 Issue | #40 |
| S12 首期真实验收 | 三名真实用户七个工作日、20 问数 18 通过 | 开工时新建 Issue | S6、S7、S8；门槛正文见[首期服务范围与真实验收](../决策记录/2026-07-26-首期服务范围与真实验收.md) |

## 三、排序理由

- **执行层先收口（S0→S1）。** 它是所有问数体验的承重结构；PR #36 的两项 P1 属于审计会「说错话」的正确性问题，悬置越久越可能被后续切片当作可用组件引用。S1 的停止线（#37 已写明）防止在未验证的入口上继续堆抽象。
- **身份（S2）与权限数据（S3）两条线互相独立、也不依赖执行层，可并行。** 并行以变更包为单位，遵守协作约定「同一变更只能有一个主实施代理」。S2 属高风险（身份与用户隔离），Draft PR 前强制独立复查。
- **S4 拆成两半。** 飞书接入、幂等与队列不依赖执行器，可尽早开工；把 Agent 会话接进任务的那半边等 S1 合并，避免在未经真实入口验证的执行器上叠飞书复杂度。
- **S5 是当前最大的决策依赖聚集点。** S3 先把数据落库与匹配做通，决策（见第四节）落地后 S5 才能如实兑现「展示当前公司和职能范围」等合同承诺；在此之前不实现任何向用户展示权限范围的逻辑（展示错误的范围比不展示更糟，#17 已有此判断）。
- **探针先行原则。** 依赖未验证外部能力的切片，第一个动作是最小探针而不是编码：S5 的 MCP 可比对字段、S7 的云盘目录跨租户授权、S8 的客户端协议支持、L4a 的飞书 Web 自动登录（[验证与门禁第六节](验证与门禁.md#六受控真实链路飞书-web-与-api-回读)的关键前提，建议在 S4 期间顺带验证）。撞不通即回 Issue 重新设计，不得先向用户承诺。
- **S9 不必等主链路全通。** 保留与删除是数据库层的横切能力，表结构一成形就做，晚做会让「九十天上限」在已积累数据上追溯执行，成本更高。
- **#39（lark_oapi 内存评估）是 S4 的输入。** 非阻塞，但 S4 选择飞书接入实现方式前应先有结论，避免刚写完就换客户端。

## 四、等待产品负责人的外部输入

以下每项都有独立 Issue 跟踪，此处只列对切片的影响；状态以 Issue 为准。

| 输入 | Issue | 卡住什么 |
|---|---|---|
| 职能范围展示形态、`全非` 哨兵语义、约六分之一账号落入人工分支的处理、银河权限时效、快照刷新机制 | [#17](https://github.com/Moshuiwang/lingxi/issues/17) | S5 完整收口（S3 不受影响） |
| 飞书账号复用换人的身份判定规则 | [#34](https://github.com/Moshuiwang/lingxi/issues/34) | S2 的「已存在 `open_id` 姓名不一致」分支；未决策前该段不实现不验收 |
| 全租户花名册表格到手后的核实 | [#35](https://github.com/Moshuiwang/lingxi/issues/35) | 不阻塞切片；结果可能改变 #34 方案与匹配键设计 |
| 管理 Agent 客户端选型（含 MCP `2026-07-28` 支持核实） | [#18](https://github.com/Moshuiwang/lingxi/issues/18) | S8 开工 |
| 生产数据库托管方式与灾备落点 | [#40](https://github.com/Moshuiwang/lingxi/issues/40) | S11 |
| L4a 受控验证的百炼调用额度约定 | [#41](https://github.com/Moshuiwang/lingxi/issues/41) | 各切片 L4a 的执行节奏 |

## 五、每个切片的开工纪律

不重复既有规范，只列入口清单：

1. 领取或新建 Issue，变更类正文首个区块写 TO PM（[协作约定](../协作约定.md#issue-正文的-to-pm-模块)）；
2. 编码前写好验收断言并认领验证矩阵编号，含否定断言（[验证与门禁 10.2](验证与门禁.md#102-实现-issue-必须先写验收断言)）；
3. 高风险 PR（身份、权限、安全、隔离、真实交付、迁移、部署恢复）强制独立复查；真实外部风险强制 L4a；
4. PR 标注实际达到的证据层级，`docs/当前能力.md` 只回写已验证事实；
5. 达到切片停止线即收口，新发现另建 Issue，不扩大当前变更包。

## 六、复审条件

- 任一切片完成、被拆分或被替代时，更新第二节对应行（这是切片划分事实的变化，不是进度更新）；
- 第四节任一决策落地后，把受影响切片的等待项移入其 Issue 范围；
- 产品合同、架构设计的约束前提或首期验收门槛发生实质变化时，本文整体复审。
