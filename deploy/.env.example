# Lingxi 部署环境变量样例（Issue #62 / S11）。
#
# **本文件只说明需要哪些变量，不含任何真实值。**
#
# 一个环境要**七个**文件，不是一个（Issue #62 codex 审查 P1-1；gateway 随 #57 增补，
# 正式重授权 job 随 Issue #67 增补，常驻 queue worker 独立分文件随 Issue #153 PR #173
# 复核 P1-2 增补——它不再与一次性 worker job 共用同一份）：
#
#   deploy/.env.stage             compose 插值用（镜像仓库与 tag），由 --env-file 读取
#   deploy/.env.stage.scheduler   scheduler 的凭据
#   deploy/.env.stage.gateway     gateway 的凭据
#   deploy/.env.stage.worker      一次性 worker job 的凭据（不含数据库/Fernet/飞书）
#   deploy/.env.stage.worker-queue 常驻 worker-queue 的凭据（含数据库 DSN）
#   deploy/.env.stage.migrate     migrate 的凭据
#   deploy/.env.stage.reauthorize 一次性重授权 job 的凭据与授权配置
#
# 生产同型（`.env.prod` / `.env.prod.scheduler` / …）。这些名字都匹配 .gitignore 既有的
# `.env.*` 规则，因此天然不入库；`scripts/ci/verify_repository.sh` 既有的敏感配置扫描
# 也已覆盖它们（只放行 `.env.example` 这一个名字）。刻意复用这两道既有护栏，不新增机制。
#
# **为什么必须按服务拆，且 worker 与 worker-queue 也必须分开。** worker（一次性 job）跑的
# 是 Claude Agent SDK，而 **SDK 会把自己的进程环境继承给 Claude Code CLI 子进程和每一个
# MCP 子进程**。给它挂一份含数据库连接串、Fernet 密钥与飞书应用密钥的共享 env，等于把这些
# 凭据送进模型执行环境和第三方 MCP 进程——正是产品合同「凭据不进用户环境」要挡住的方向。
# worker-queue（常驻队列消费者）不跑 Agent SDK、不继承环境给任何子进程，但它需要
# `LINGXI_POSTGRES_DSN` 才能领任务——这正是它必须有自己独立的一份 env 文件、而不能借用
# `worker` 那份（PR #173 复核 P1-2：早期版本让两者共用 `.env.<env>.worker`，结果要么
# worker-queue 拿不到 DSN 无限崩溃重启，要么 worker 意外拿到了它不该有的数据库凭据）。
#
# 镜像里**不预置任何凭据**：Dockerfile 没有一条 ENV 写凭据，全部由运行期注入。
# 凭据不进版本库、不进镜像、不进日志、不进 PR 正文（产品合同）。

# ===========================================================================
# 文件一：deploy/.env.stage —— 只给 compose 做插值，**不含凭据**
# ===========================================================================
# 这个文件由 `--env-file deploy/.env.stage` 读取。`env_file:` 只把变量注入容器，
# **不参与 compose 文件自身的 ${VAR:?} 插值**，所以镜像仓库与 tag 必须走这里。
#
# 镜像仓库前缀。GHCR，2026-08-06 产品负责人拍板（Issue #62 决策登记）。
LINGXI_IMAGE_REGISTRY=ghcr.io/moshuiwang
# 不可变镜像 tag：<YYYYMMDD>-<commit sha 前 12 位>，语义见 deploy/README.md。
# 回滚就是把这一行改回上一个 tag 再 `up -d`。禁止 latest 或分支名。
LINGXI_IMAGE_TAG=20260806-7a9bcf3fac4a
# 可选：扫描间隔秒数（stage 默认 30，生产默认 60）
# LINGXI_SCHEDULER_INTERVAL_SECONDS=30
# 可选：scheduler 只读挂载的运行时配置宿主机目录（Issue #320 / #328 S-P0-2），
# 默认 /opt/lingxi/runtime-config——与 worker-queue 运行时目录（LINGXI_WORKER_
# RUNTIME_CONFIG_DIR）默认同一宿主目录：一台宿主机一个 runtime-config 目录，
# 闸①指标映射表与 worker 运行时提示词同住。LINGXI_WORKER_RUNTIME_CONFIG_DIR
# 同为 compose 插值变量，取值来源正确惯例同本变量一样是本「文件一」（`--env-file`
# 指向的文件），不是「文件五」——那边的散文说明只讲挂载用途，是历史遗留的说明
# 位置，不是配置入口。只在需要把 scheduler 挪到另一个宿主目录时才配。挂载细节
# 见 compose.stage.yaml / compose.prod.yaml scheduler 服务的卷注释。
# LINGXI_SCHEDULER_RUNTIME_CONFIG_DIR=/opt/lingxi/runtime-config

# ===========================================================================
# 文件二：deploy/.env.stage.scheduler —— 专用授权凭据续期扫描
# ===========================================================================
# 业务数据库连接串。
#
# 示例 DSN 保留三个超时参数，作为与连接工厂默认值的可见对账基线；它们不是运行时
# 唯一控制点，也不直接决定应用连接的停机预算。连接工厂通过 kwargs 覆盖 DSN 同名参数，
# 事实源是 src/lingxi/adapters/postgres.py；合法覆盖使用下列环境变量：
#   LINGXI_POSTGRES_CONNECT_TIMEOUT_SECONDS
#   LINGXI_POSTGRES_STATEMENT_TIMEOUT_SECONDS
#   LINGXI_POSTGRES_LOCK_TIMEOUT_SECONDS
#
#   connect_timeout=5        DSN 侧只约束**建连**；应用实际由连接工厂传入同名 kwargs。
#   statement_timeout=3000   DSN 侧约束**已经发出去的语句**。
#   lock_timeout=2000        DSN 侧约束**等锁**；应用实际由连接工厂传入同名 kwargs。
#
# 连接工厂默认值是 5/3/2 秒；示例 DSN 的名义单次操作上界因此是 5+3+3=11 秒。
# 停机预算还必须覆盖合法环境覆盖的最坏值，完整推导见 deploy/README.md。
# `scripts/ci/check_deploy_contract.py` 会核对本行仍带齐这三个默认参数，但预算不再从
# DSN 推导。
LINGXI_POSTGRES_DSN=postgresql://<用户>:<口令>@<主机>:<端口>/<库>?connect_timeout=5&options=-c%20statement_timeout%3D3000%20-c%20lock_timeout%3D2000

# refresh_token 的 Fernet 加密密钥（44 字符 base64）。
# 安全边界：cryptography 在 pyproject.toml 里锁精确版本正是为了这个（V-部署-08）。
LINGXI_DELEGATED_CREDENTIAL_KEY=<Fernet 密钥，勿提交>

# 飞书自建应用凭据（「四达文档会议助手」）。
LINGXI_FEISHU_APP_ID=<应用 ID>
LINGXI_FEISHU_APP_SECRET=<应用密钥，勿提交>

# 可选：飞书开放平台地址，默认 https://open.feishu.cn/open-apis
# LINGXI_FEISHU_BASE_URL=

# 注意：LINGXI_DELEGATED_CREDENTIAL_PATH **不在这里设**。它由 deploy/compose.yaml
# 固定为 /var/lib/lingxi/credentials/delegated_credential.enc，指向跨部署持久卷。
# 把它改成容器内路径会让每次镜像替换都丢失授权（断言 V-部署-03 / M2-62-27），
# scripts/ci/check_deploy_contract.py 会拦住这种改法。

# 可选：最小告警装配（Issue #153）。不填时状态机（阈值/去重/恢复计时）照常运行，
# 只是不真正发进管理群、退化为结构化日志（合同「告警不可用时主流程行为有明确
# 定义」）。群 ID 形如 oc_ 开头的飞书群 chat_id。
# LINGXI_ADMIN_GROUP_CHAT_ID=oc_<群 ID>
# 阈值/退避覆盖，默认值见 lingxi.core.alerting.AlertPolicy：
#   LINGXI_ALERT_HEARTBEAT_TIMEOUT_SECONDS=120
#   LINGXI_ALERT_QUEUED_TIMEOUT_SECONDS=180
#   LINGXI_ALERT_RUNNING_HEARTBEAT_TIMEOUT_SECONDS=90
#   LINGXI_ALERT_SEND_FAILURE_WINDOW_SECONDS=300
#   LINGXI_ALERT_SEND_FAILURE_THRESHOLD=3
#   LINGXI_ALERT_DEDUPE_WINDOW_SECONDS=1800
#   LINGXI_ALERT_RECOVERY_STABLE_SECONDS=300
#   LINGXI_ALERT_RETRY_BASE_SECONDS=1
#   LINGXI_ALERT_RETRY_FACTOR=2
#   LINGXI_ALERT_RETRY_CEILING_SECONDS=60

# 可选：每日花名册比对与管理群审计日报（Issue #52）。**四个前置任缺其一，该职责
# 就不注册**（进程照常启动，其余定时职责照常运行，并留一条指名原因的审计）：
# 上面的 LINGXI_ADMIN_GROUP_CHAT_ID、下面两个花名册标识，以及花名册读取所用的
# 短期令牌供给。第四项自 Issue #215 起由 scheduler 自己装配（凭据轮换职责按需换一次、
# 派生短期令牌只留在进程内），**不需要任何环境变量**；因此把下面两个变量连同上面的
# 群 ID 一起填上，日报职责就会注册并**每天尝试执行**——没有可用凭据、或读取 / 发送
# 失败时一律失败关闭并留审计，**不代表日报已经真的发出**。真实读取与真实送达都未经
# 受控验证，启用前请与产品负责人确认。
# 两个标识都是飞书多维表格的外部标识，从环境注入、不进代码；值里不得有空白。
# LINGXI_ROSTER_BITABLE_APP_TOKEN=<花名册 Base 的 app_token>
# LINGXI_ROSTER_BITABLE_TABLE_ID=<花名册表的 table_id>
# 可选：快照超龄阈值（小时），默认 48。超龄只在日报里告警提醒，**不自动删快照**
# （产品负责人 2026-08-17 裁定：始终保留最近一份）。取值范围 0 到 8760。
# LINGXI_ROSTER_SNAPSHOT_STALE_AFTER_HOURS=48

# 可选：Epic C 权限链三面（#156 / #159）。各面前置不齐就**各自不注册**（互不牵连，
# 进程照常启动，并各留一条指名原因的审计）；填齐只表示职责会注册并**每轮尝试执行**，
# 真实发布、真实 MCP 探针与真实通知送达都未经受控验证，**不代表链路已真实生效**。
# 每日权限重算 + 令牌签发/解密所需的 MCP 主密钥（32 字节 base64，与问数 MCP 同一把，
# 带外交接、不进仓库不进日志）：
# LINGXI_MCP_TOKEN_ENCRYPT_KEY=<32B base64 主密钥>
# 发布消费面：权限发布多维表格标识与其访问令牌供给（后者仍未接线，见 R3 登记；
# 因此当前**任何部署下发布面都不会注册**，此处仅预留标识）：
# LINGXI_PERMISSION_BITABLE_APP_TOKEN=<权限发布 Base 的 app_token>
# LINGXI_PERMISSION_BITABLE_TABLE_ID=<权限发布表的 table_id>
# 可选：存量令牌只读源（Issue #281 改道，产品负责人 2026-08-25）。首聊建档签发令牌前
# 先按邮箱查这两个坐标指向的正式表：有存量密文→解密沿用（登记进 Lingxi 令牌库，幂等、
# 绝不覆盖）；无行/无密文→原路径签新；有密文但解不开→响亮失败停该用户本次开通、绝不
# 签新（避免与正式表令牌错位）。解密失败会响亮失败停开通——这是防令牌错位的设计；
# **不要用摘掉坐标的方式绕过**（摘掉后开通会签发新令牌，与正式表既有令牌错位，用户
# 问数将静默认证失败），应排查主密钥/数据后重试。**未配置＝该能力关闭**，开通链行为
# 与改动前逐字节一致，不是首次开通的硬前提。生产环境很可能与上面两个
# LINGXI_PERMISSION_BITABLE_* 指向同一个 Base/表（同一份正式表），但坐标独立配置，
# 可单独打开/关闭。
# LINGXI_STOCK_TOKEN_BITABLE_APP_TOKEN=<存量令牌源 Base 的 app_token>
# LINGXI_STOCK_TOKEN_BITABLE_TABLE_ID=<存量令牌源表的 table_id>
# 就绪探针面：问数 MCP 端点与单次探针超时（秒，须 ≤ 轮询间隔）：
# LINGXI_QUERY_MCP_ENDPOINT=<https 端点>
# LINGXI_QUERY_MCP_TIMEOUT_SECONDS=20
# 首次开通编排（S-D-02）：用户环境根目录，**不带** LINGXI_SCHEDULER_ 一类前缀——
# 与「文件五」worker-queue 读同一个裸变量名 LINGXI_USER_ENV_ROOT，两个进程指向
# 同一个持久卷挂载点（compose 的 lingxi-users 卷）。scheduler 在这里给每个用户
# 建家目录、写各自的 .mcp.json；worker-queue 处理任务时再从同一棵目录树按
# user_id 读出来用（Epic D 闸⑤/⑥）。与上面几面同一姿态：**可选**——连同
# LINGXI_MCP_TOKEN_ENCRYPT_KEY、LINGXI_QUERY_MCP_ENDPOINT 一起缺任何一项，
# 首次开通编排就不注册（其余定时职责照常运行，只留一条指名原因的审计）；
# 真实部署要让首次开通生效，三者必须一起配齐，且必须指向已挂载的持久卷路径，
# 不能是容器本地临时路径（否则每次镜像替换都会丢失已开通用户的环境）。
# LINGXI_USER_ENV_ROOT=/var/lib/lingxi/users
#
# 可选：首次开通编排的执行线程数，默认 8、上限 64（apps/scheduler/config.py 的
# DEFAULT_ONBOARDING_WORKERS/MAX_ONBOARDING_WORKERS）。它就是「同一时刻最多几个人
# 在开通」——默认值按 710 人规模的排空账推导，见该常量的头部注释。与闸门开关无关，
# 只影响吞吐；缺省即用默认值，不需要每个环境都显式配置。
# LINGXI_ONBOARDING_WORKERS=8
#
# 内测名单闸（Issue #302 S-N-01）：开通链最前端的白名单，只在 open_id 命中时才继续
# 走身份定位；命中之外一律得到「内测未开放」且零建档（判据与理由见
# lingxi.core.identity.innertest_roster_gate 模块文档）。**默认空＝全拒**——不填这
# 一项时任何人都会被拒绝，这不是"未启用"，是这道闸唯一的失败关闭状态。
# 值是一到多个飞书用户 open_id（以 ou_ 开头），用英文逗号或换行分隔；真实名单值只经
# 本变量在 stage/prod 环境注入，不进本文件、不进仓库、不进 Issue。配了但存在无法
# 识别的条目会在启动期整份拒绝并报错（错配不是未配，同本文件其余标识类变量纪律）。
# LINGXI_INNERTEST_ROSTER_OPEN_IDS=<ou_ 开头的 open_id，逗号或换行分隔，可写多个>
#
# 可选：组织快照同步（Issue #250）单轮读取的整轮预算（秒），默认 1200（20 分钟，
# apps/scheduler/config.py 的 DEFAULT_ORG_SNAPSHOT_ROUND_BUDGET_SECONDS）。撞线后
# 本轮读取原样中止，按既有的读取失败路径退避重试、保留库里最近一次成功批次，不
# 影响首次开通链走的另一个独立 client。与闸门开关无关，只影响"一轮最多愿意为限频
# 重试花多久"；关联组织规模变化、345 秒基线不再成立时可运维调大或调小，不需要
# 每个环境都显式配置。
#
# **预算必须大于全量轮实际耗时**（stage 实测 345 秒基准）——配成更小的值会让每一轮
# 都必然撞 round_budget_exceeded，快照因此永远无法更新，且这个状态在"保留上一份、
# 不覆盖"的既有语义下是静默的（库里旧数据仍在，不会像空表那样显眼）。进程启动时
# 会拒绝低于 60 秒的配置（apps/scheduler/config.py 的
# MIN_ORG_SNAPSHOT_ROUND_BUDGET_SECONDS），但 60 秒只是挡明显不可能的误配，不代表
# 60 秒本身是安全值——必须按当前关联组织的实测规模留出余量。若已经调到明显大于
# 实测基线仍连续撞线，OrgSnapshotSyncDuty 会在连续三轮后额外升一条
# org_snapshot_sync.round_budget_persistently_exceeded 审计与 WARNING 日志。
# LINGXI_ORG_SNAPSHOT_ROUND_BUDGET_SECONDS=1200
#
# 可选：闸①「公司+职能→指标名」映射表的外置路径（Issue #320）。未配置时 loader
# 落回随包发布的默认文件（lingxi/config/company_function_metric_map.toml），与外置
# 能力交付前逐字节一致。配了就**优先**于随包默认——不是"缺失时的兜底"，是"配了
# 这个变量，这台机器就只认这个文件"。维护人＝产品负责人：外置的目的正是让维护人
# 不必再为一行映射改动走一次完整镜像构建发布
# （与「文件五」worker-queue 的运行时提示词目录同一模式）。**错配不是未配**：配了
# 但指向的文件缺失或格式非法，与 LINGXI_MCP_TOKEN_ENCRYPT_KEY 缺失同一分支响亮
# 失败（permission_refresh.duty_not_registered，reason=metric_translation_map_
# unavailable），不会静默回落随包默认那份"看起来安全"的内容。**挂载要求**：必须
# 挂载该文件所在的**目录**、不能只挂单个文件（编辑器"写临时文件再 rename"的保存
# 方式会让单文件绑定挂载停在旧 inode 上，见「文件五」运行时提示词目录挂载注释
# 同一条坑）；compose.stage.yaml / compose.prod.yaml 的 scheduler 服务已接上这条
# 只读目录挂载（`${LINGXI_SCHEDULER_RUNTIME_CONFIG_DIR:-/opt/lingxi/runtime-config}
# :/etc/lingxi/runtime:ro`，#328 S-P0-2）。**生效时机与「文件五」worker-queue 不
# 同、不要套用"编辑即生效"**：权限重算职责在 scheduler 进程启动时只装配一次
# （apps/scheduler/assembly.py 的 build_loop 只调一次
# _build_permission_refresh_duty），编辑外置文件后需要重启 scheduler 容器
# （`docker compose --env-file deploy/.env.stage -f deploy/compose.yaml -f
# deploy/compose.stage.yaml restart scheduler`，prod 同构换成
# `.env.prod`/`compose.prod.yaml`；不需重建镜像）才生效——不能用
# `up -d` 重启 scheduler：compose 配置未变时它判 up-to-date 不重启，而改的是外置文件
# 不是 compose 配置，正是此情形；重启后可用加载成功
# 时记的 digest 日志行（logger lingxi.adapters.company_function_metric_map_file，
# 字段 path/digest/companies）核对读到了哪一版。例外：每日通报「未覆新指标」
# 日检段（apps/scheduler/daily_report.py 的 _check）每次现读，无需重启。
# LINGXI_COMPANY_FUNCTION_METRIC_MAP_PATH=/etc/lingxi/runtime/company_function_metric_map.toml

# ===========================================================================
# 文件三：deploy/.env.stage.gateway —— 飞书长连接接入（#57 / S4 前半）
# ===========================================================================
# 白名单：飞书应用凭据 + 数据库 DSN + 一个非密钥标识（文档投递独立消费循环的
# 租户域名，见下方），**仅此三类**。gateway 不需要 Fernet 密钥（那是 scheduler
# 的专用授权凭据边界），也不需要模型端点凭据（那是 worker 的）。
#
# 注意变量名带 `LINGXI_GATEWAY_` 前缀，与 scheduler 那组**不是同一批变量**：
# 两个进程各自 `load_config` 一次，前缀不同正是为了让"给错了文件"在启动期就失败。
LINGXI_GATEWAY_APP_ID=<应用 ID>
LINGXI_GATEWAY_APP_SECRET=<应用密钥，勿提交>
# 与 scheduler 的示例 DSN 同样保留三个超时参数，运行时控制点与覆盖方式见上一节。
LINGXI_GATEWAY_POSTGRES_DSN=postgresql://<用户>:<口令>@<主机>:<端口>/<库>?connect_timeout=5&options=-c%20statement_timeout%3D3000%20-c%20lock_timeout%3D2000

# 文档投递独立消费循环（Issue #341 S-ES-3）的租户域名——不是密钥，是拼文档链接
# 用的裸域名（例如 gv3qfk4q2rp.feishu.cn，S0 探针实测值；飞书没有开放接口能
# 查询"当前租户的裸域名"，只能由运维显式提供）。**可选，未配置时这条消费循环
# 整体不注册**（失败关闭，与内测名单闸「默认空＝全拒」同一姿态）：本环境不需要
# 「问数结果生成飞书文档」这项能力时，留空即可，其余能力不受影响。**必须与
# 「文件五」worker-queue 的 LINGXI_WORKER_DOCUMENT_DELIVERY_ENABLED 同开同关**
# ——只开这一侧完全无害（没有 worker 会插入任何一行）；只开 worker 那一侧会产生
# 无人消费、最终被 scheduler 死信扫描判死的文档请求（Issue #341 R-2），见该变量
# 的说明与 deploy/验收前部署配置清单.md 对应条目。
# LINGXI_GATEWAY_TENANT_DOMAIN=<裸域名，例如 example.feishu.cn>

# 内测名单闸的 gateway 侧前移一份（Issue #302 S-N-01 的纵深，opus 批量审查 P1）：
# **刻意不带 `LINGXI_GATEWAY_` 前缀**——与上一节 scheduler 读的是**同一个变量名**
# `LINGXI_INNERTEST_ROSTER_OPEN_IDS`，因为这不是 gateway 私有配置，是两个进程各自
# 独立部署但必须表达"同一份内测名单"的共享概念。两处进程必须分别配置这个变量、
# 值应当一致（运维纪律，代码不做跨进程一致性校验）——只配一处，另一处仍会按
# 「默认空＝全拒」正常拒绝所有人，不会静默放行。语义、失败关闭行为与格式要求同
# 上一节 LINGXI_INNERTEST_ROSTER_OPEN_IDS 的说明，不再重复。
# LINGXI_INNERTEST_ROSTER_OPEN_IDS=<ou_ 开头的 open_id，逗号或换行分隔，与 scheduler 那份一致>

# 可选（有默认值，改动会影响 compose 的 stop_grace_period，见 deploy/README.md）：
#   LINGXI_GATEWAY_SHUTDOWN_TIMEOUT_SECONDS=20   # 停机超时，出站超时取它的 1/4
#   LINGXI_GATEWAY_RECONNECT_BASE_SECONDS=1
#   LINGXI_GATEWAY_RECONNECT_FACTOR=2
#   LINGXI_GATEWAY_RECONNECT_CEILING_SECONDS=60
#
# 可选：最小告警装配（Issue #153），与 scheduler 那份同一取舍——不填就退化为
# 结构化日志，状态机本身照常运行。gateway 有自己独立的一份（三个进程各自持有
# AlertManager，互不共享，见 core/alerting.py 模块说明），因此变量名各自独立：
# LINGXI_GATEWAY_ADMIN_GROUP_CHAT_ID=oc_<群 ID>
# LINGXI_GATEWAY_ALERT_HEARTBEAT_TIMEOUT_SECONDS=120     # 其余阈值变量名同上一节，加 GATEWAY_ 前缀
# 可选：飞书开放平台地址（只有上面的告警群发出口用得到，走 urllib 而不是
# lark-oapi），默认 https://open.feishu.cn/open-apis：
# LINGXI_GATEWAY_FEISHU_BASE_URL=
#
# 可选：S-A-07 受控验收专用的卡片故障注入开关（Issue #152 验收缺口）。合法值
# create / update / close / all，对应让 #152 投递消费循环里被选中的那一步卡片
# 外发调用确定性收到服务端拒绝。**只在受控验收执行期间临时设置，默认必须不
# 设置**；设置非法值会让 gateway 启动即失败（失败关闭）。开启时进程启动日志
# 会打一条显眼的 WARNING（`gateway.delivery.card_failure_injection_enabled`），
# 发现这条日志出现在非验收窗口应立即核实并清空该变量。
# 四个值的实测语义（独立审核 P2-1）：create 用于验证"关闭卡片路径 + 同话题
# 一次完整文本终态"的降级路径；update 用于验证注入发生在**建卡成功之后**
# （终态更新阶段）的同一条降级路径；all 在正常单轮场景下等价于 create（建卡
# 先被拒即整体降级，update/close 没有机会被调用到），只有任务从已持久化
# card_id 恢复时才会真的命中 update/close 那一支；close 单独命中**不产生
# 降级**（关闭失败不构成结果丢失，是 V-卡片-03 的正确行为），用于验证"关闭
# 失败不得产生第二条文本终态"这条否定断言：
# LINGXI_GATEWAY_CARD_FAILURE_INJECT=
#
# **gateway 默认不启动**：compose 把它放在非默认 profile 里（队列无消费者，
# S4 下半接线前不启用）。填了这个文件也不会让它自己跑起来。**mvp profile
# 启用时会跟常驻 queue worker 一起启动**（见 deploy/README.md）。

# ===========================================================================
# 文件四：deploy/.env.stage.worker —— 一次性单回合受控执行作业
# ===========================================================================
# **这里不放数据库连接串、不放 Fernet 密钥、不放飞书应用密钥。** worker（`docker
# compose run --rm worker`）不需要它们——它是无状态单回合 CLI，读环境变量跑一个
# Agent SDK 回合、往 stdout 写一个 JSON 报告就退出，从不碰数据库；而它的子进程
# （Claude CLI、每个 MCP server）会继承这份环境（见本文件开头），所以这份文件必须
# 只装它真正需要的东西。
#
# **只属于 worker，不与 worker-queue 共用**（PR #173 复核 P1-2）：常驻队列消费者
# `worker-queue` 需要 `LINGXI_POSTGRES_DSN` 才能领任务，配置在独立的「文件五」里，
# 不经这个文件——否则要么这里被迫塞进 DSN、连累一次性 job 的 Agent SDK 子进程也拿到
# 数据库凭据，要么 worker-queue 拿不到 DSN 直接崩溃重启（两种后果都不可接受）。
#
# 下面这些多数由调用方在 `docker compose run` 时按本次回合传入；这里列出的是形状说明。
#
# 必填：
#   LINGXI_WORKER_QUESTION=<本回合的问题原文>
#   LINGXI_WORKER_READONLY_TOOLS=<逗号分隔的一个或多个 mcp__<服务名>__<工具名>>
#       多值白名单（Issue #291 P0）：每一项必须是精确工具名，不支持通配符，不支持
#       空白分隔——只接受逗号。服务名段必须与用户环境 .mcp.json 里的 MCP 服务名一致
#       （单一事实来源见 adapters/user_environment.py 的 QUERY_MCP_SERVER_NAME，当前
#       固定为 `query`），worker 装配期会断言这一点，不一致直接启动失败关闭。
#       当前真实值示例（2026-08-21 从 biai-stage 只读回读 Agent 会话记录中该用户
#       实际尝试调用过的工具名确认；**这只是模型尝试过的子集，不是问数 MCP 侧
#       tools/list 注册的完整清单**——回读期间没有找到完整清单的持久化记录，
#       全集尚未回源问数 MCP 方确认，见 Issue #291）：
#   LINGXI_WORKER_READONLY_TOOLS=mcp__query__list_metrics,mcp__query__describe_metric,mcp__query__search_dimension,mcp__query__query_metric
#   （2026-08-22 实测定稿的问数 MCP 只读工具全集共 4 个；漏 query_metric——真正取数的那个——
#   时一切真取数问题会静默降级成元数据回答，Issue #291 实测坐实）
# 可选：
#   LINGXI_WORKER_TRACE_ID=<26 位 Crockford ULID>        # 不填则自动生成
#   LINGXI_WORKER_MAX_TURNS=20                         # 最大 30；缺失使用 20
#   LINGXI_WORKER_TURN_TIMEOUT_SECONDS=600             # 秒；最大 900；缺失使用 600
#   LINGXI_WORKER_MCP_SERVERS={"<服务名>":{"type":"stdio","command":"..."}}
#   LINGXI_WORKER_WORKSPACE=/var/lib/lingxi/users/<用户目录>
#   LINGXI_WORKER_MODEL=<模型名>
#   LINGXI_WORKER_SYSTEM_PROMPT=<系统提示词>
#   LINGXI_WORKER_AUDIT_INPUT_FIELDS=<逗号分隔字段名>
#   LINGXI_WORKER_FAILURE_MARKERS=["<已从真实回执确认的业务失败措辞>"]
#
# 可选：S-A-07 受控验收专用的输出安全 canary 开关（Issue #142 验收缺口）。合法值
# masked / withheld：在出口安全约束之前把已配置的**合成** LINGXI_WORKER_SYSTEM_PROMPT
# 确定性注入最终正文——masked 验证「局部遮蔽、业务结论幸存」，withheld 验证
# 「整段拒发、独立 redacted_withheld 终态」，不依赖模型"恰好复述"提示词。
# **只在受控验收执行期间临时设置，默认必须不设置**；设置非法值、未同时配置
# SYSTEM_PROMPT、或合成提示是安全终态固定文案/注入幸存句的子串，都会让 worker
# 启动即失败（失败关闭）。合成提示请使用一段互不重叠的多句文本。失败回合不注入：
# 超时/中断/会话失败保留真实失败终态，canary 只作用于本会成功交付的回合。
# 开启时启动日志会打一条显眼的
# WARNING（`worker.output_safety_canary_enabled`），每轮注入另有
# `worker.output_safety_canary_injected` 低敏痕迹；发现这些日志出现在非验收窗口
# 应立即核实并清空该变量。只允许配合合成 canary 提示使用，真实系统提示不进验收。
# LINGXI_WORKER_OUTPUT_SAFETY_CANARY=
#
# ---- 模型端点认证（变量名占位，本仓库尚未接线）----------------------------
# 执行路径已约定为百炼兼容端点（决策记录「仅提供 Claude 执行路径」）。Agent SDK 通过
# 环境变量拿模型凭据，因此它们**属于本文件**而不是 scheduler 那份。
# **本批未做真实模型调用，下面只是变量名登记，值留空**；实际取哪几个、叫什么名字，
# 须在 stage 上用 Bot-Test 跑通一次受控回合后回填（列为 stage 验证项，见 deploy/README.md）。
# ANTHROPIC_BASE_URL=<百炼兼容端点>
# ANTHROPIC_AUTH_TOKEN=<端点令牌，勿提交>
#
# ---- HOME ------------------------------------------------------------------
# 镜像已把 HOME 设为 /tmp（compose 给 worker 挂了 256m tmpfs）。原因：useradd
# --no-create-home 留下的 /home/lingxi **并不存在**，而根文件系统是只读的，
# Claude CLI 与 MCP 子进程往 $HOME 写配置会直接失败。不要在这里覆盖 HOME，
# 除非同时给那个路径挂上可写卷。
#
# ===========================================================================
# 文件五：deploy/.env.stage.worker-queue —— 常驻队列消费者（Issue #153）
# ===========================================================================
# **与文件四（一次性 worker job）是同一镜像、不同信任边界，各自独立一份文件**
# （PR #173 复核 P1-2）：worker-queue 常驻领取真实用户任务，必须拿到数据库连接串
# 才能启动；一次性 worker job 不需要数据库，因此不共用。两者的白名单不同：
# 数据库 DSN + 模型端点凭据，**不含** Fernet 密钥、不含飞书应用密钥
# （`LINGXI_WORKER_MODE=queue` 由 compose 基线的 `environment:` 直接给值，不经本文件）。
#
# 与 scheduler/gateway 的示例 DSN 同样保留三个超时参数，运行时控制点与覆盖方式见
# 「文件二」。`scripts/ci/check_deploy_contract.py` 会核对这份文件确实带了
# LINGXI_POSTGRES_DSN，缺失时 worker-queue 会在启动期报
# `worker.queue.config.invalid` 并以 exit=3 退出——挂 `restart: unless-stopped`
# 就是无限崩溃重启，不是能事后靠健康检查发现的慢性问题。
LINGXI_POSTGRES_DSN=postgresql://<用户>:<口令>@<主机>:<端口>/<库>?connect_timeout=5&options=-c%20statement_timeout%3D3000%20-c%20lock_timeout%3D2000
#
# 用户环境根目录（Epic D 闸⑥）：**不带** LINGXI_WORKER_ 前缀——与 scheduler 那份
# 「文件二」读同一个裸变量名 LINGXI_USER_ENV_ROOT，两个进程指向同一个持久卷挂载点
# （compose 的 lingxi-users 卷，挂载点 /var/lib/lingxi/users）。worker-queue 处理
# 每个任务时都会按该任务的 user_id 读 <这个目录>/<user_id>/.mcp.json 作为这次会话
# **专属**的 MCP 配置——每个用户的问数只用他自己的那份令牌，绝不共用。缺失时
# worker-queue 会在启动期报 `worker.queue.config.invalid` 并以 exit=3 退出（与
# LINGXI_POSTGRES_DSN 同一姿态：一旦缺失，队列 worker 领到的每一个任务都必然读
# 不到配置而失败，因此在启动期一次性拒绝，而不是让每个任务分别撞上同一个原因）。
LINGXI_USER_ENV_ROOT=/var/lib/lingxi/users
#
# 队列消费者专属的可选配置：
#   LINGXI_WORKER_SHUTDOWN_TIMEOUT_SECONDS=45   # SIGTERM 优雅停机预算；推导见
#                                                 apps/worker/config.py
#   LINGXI_WORKER_SESSION_ROOT=<会话 JSONL 根目录，留空按 $HOME/.claude/projects 推导>
#                                                # Agent 会话 JSONL 物理清理的搜索根目录。
#                                                # 镜像固定 HOME=/tmp，因此**不要**填一个
#                                                # 不存在的路径（例如
#                                                # /var/lib/lingxi/users/.claude/projects）
#                                                # ——根目录不存在时清理会静默判定
#                                                # "本轮无文件可删"并把数据库那一侧标记为
#                                                # 已完成，事后改对配置也补不回来
#                                                # （PR #173 复核 P2-6）。留空使用默认推导
#                                                # 是更安全的选择。
#   LINGXI_WORKER_SESSION_CLEANUP_BATCH_LIMIT=20
#   LINGXI_WORKER_MAX_CONCURRENCY=16
#   LINGXI_WORKER_HEARTBEAT_INTERVAL_SECONDS=30
#   LINGXI_WORKER_POLL_INTERVAL_SECONDS=2
#
# 其余变量（读写权限、模型端点）与「文件四」同一批变量名、同一形状说明，见上方；
# worker-queue 与 worker 是同一套 LINGXI_WORKER_* / ANTHROPIC_* 变量面，只是取值
# 各自独立配置。**例外是 LINGXI_WORKER_MCP_SERVERS**：那是一次性受控回合（`turn`
# 模式）用的**全进程共用**配置，`queue` 模式处理真实用户任务时已经改为按上面的
# LINGXI_USER_ENV_ROOT 逐用户读取（Epic D 闸⑥）。`LINGXI_WORKER_MODE=queue` 下
# `load_config` **根本不解析**这个变量（不是"解析了但不用"）：即使这里配的值
# 是非法 JSON，queue 进程也不会因此启动失败；配了合法值，那份共享令牌也不会
# 被读进 queue 进程内存。不要指望靠它给 worker-queue 配 MCP 服务器。
#
# 可选：默认提示词文件（2026-08-23，产品负责人裁定提示词不进代码、不进镜像，
# 随时可改快速验证）。指向挂载卷上的 UTF-8 文本文件（绝对、已规范化路径），
# worker **每个任务开始时现读**——在宿主机编辑该文件后下一条消息即生效，不需要
# 重启容器或重建镜像。stage 覆盖文件已把宿主机目录（默认
# /opt/lingxi/runtime-config，可用 LINGXI_WORKER_RUNTIME_CONFIG_DIR 改）只读挂到
# 容器 /etc/lingxi/runtime。文件缺失/不可读/超过 64KiB/与安全终态固定文案互相
# 命中时，该任务降级为无提示词执行并在 stderr 留 `worker.system_prompt.degraded`
# 告警；每轮终态审计事件（worker.task.terminal）带 system_prompt_digest（sha256
# 前 12 位）供追溯「这一轮用的哪版提示词」。与 LINGXI_WORKER_SYSTEM_PROMPT、
# LINGXI_WORKER_OUTPUT_SAFETY_CANARY 互斥（同时配置启动即失败）。
#   LINGXI_WORKER_SYSTEM_PROMPT_FILE=/etc/lingxi/runtime/system_prompt.md
#
# 内测轮内容级采集开关（Issue #251/#304 批次 3）：默认不填＝关闭，
# **生产环境禁止配置这两项，无论出于什么理由**——它们只应出现在 stage 的
# .env.stage.worker-queue 里，绝不允许出现在任何 .env.prod.* 文件、绝不允许写进
# 任何 compose 编排文件的 `environment:` 块（`scripts/ci/check_deploy_contract.py`
# 的 check_content_capture_prod_guard 会核对第二个变量的精确取值与两个变量名都不
# 出现在入库的 compose 文件里，但**读不到未入库的生产 env 文件**，因此这条禁令
# 最终仍要靠部署操作纪律兑现，见 deploy/验收前部署配置清单.md）。两项都精确匹配
# 才生效，只填一项等于没填（详见 apps/worker/config.py 的
# `_innertest_content_capture` 模块内文档）：
#   LINGXI_INNERTEST_CONTENT_CAPTURE=1
#   LINGXI_INNERTEST_CONTENT_CAPTURE_ENVIRONMENT_CONFIRM=stage-innertest-explicit-opt-in
# 开启后，本进程处理的每个任务的用户问题/模型回答/工具调用详情（凭据形状已过滤，
# 其余原文与结果正文不设限，2026-08-24 产品负责人裁定）会写入
# innertest_content_capture 表；写入失败降级为结构化审计日志，不影响任务完成。
# 保留与轮次结束清理见迁移 0069_innertest_content_capture 的文件头文档。
#
# 文档交付触发开关（Issue #341 S-ES-2/#328 P2-4）：默认不填＝关闭，模型看不到
# `deliver_document` 工具，问数流程逐字节不变。**必须与「文件三」gateway 的
# `LINGXI_GATEWAY_TENANT_DOMAIN` 同开同关**——只开这一侧会产生无人消费的文档
# 请求（这一侧登记的请求写进 `task_document_delivery_request` 后，gateway 独立
# 消费循环若未配置，30 分钟后被 scheduler 的死信扫描职责（Issue #341 R-2，
# `apps/scheduler/document_delivery_dead_letter.py`）判定为 `pending_expired_
# unconsumed` 转 `failed` 并上报管理群告警，用户请求文档后不会收到任何主动通知，
# 只能看到问数本身正常完成）；只开 gateway 那一侧则完全无害（没有 worker 会插入
# 任何一行，消费循环永远无事可做）。见 deploy/验收前部署配置清单.md 对应条目。
#   LINGXI_WORKER_DOCUMENT_DELIVERY_ENABLED=1

# ===========================================================================
# 文件六：deploy/.env.stage.migrate —— 数据库迁移作业
# ===========================================================================
# 与业务的 LINGXI_POSTGRES_DSN 是**两个变量**，迁移不回落到后者：迁移要 DDL 权限、
# 业务进程不要，两者本就该是不同的数据库角色（migrations/README.md，V-迁移-05）。
# 迁移是一次性作业、不受 stop_grace_period 约束，因此这里不要求 statement_timeout
# ——给迁移设语句超时反而会让一条正常的大表 DDL 中途失败。
LINGXI_MIGRATION_DSN=postgresql+psycopg://<迁移用户>:<口令>@<主机>:<端口>/<库>

# ===========================================================================
# 文件七：deploy/.env.stage.reauthorize —— 一次性正式重授权作业
# ===========================================================================
# 该文件只由 `reauthorize` job 使用，仍须 0600；job 以 uid 10001 运行并挂载
# `lingxi-stage-credentials`，因此不会把凭据写成 root 属主。
LINGXI_POSTGRES_DSN=postgresql://<用户>:<口令>@<主机>:<端口>/<库>?connect_timeout=5&options=-c%20statement_timeout%3D3000%20-c%20lock_timeout%3D2000
LINGXI_DELEGATED_CREDENTIAL_KEY=<Fernet 密钥，勿提交>
LINGXI_FEISHU_APP_ID=<应用 ID>
LINGXI_FEISHU_APP_SECRET=<应用密钥，勿提交>
LINGXI_FEISHU_BASE_URL=https://open.feishu.cn/open-apis
LINGXI_FEISHU_REDIRECT_URI=https://<受控回跳域名>/<路径>
LINGXI_FEISHU_AUTHORIZATION_ENDPOINT=https://<飞书授权端点>
# 必须填写本次应用配置的**全部**目录只读 scope，并包含 offline_access；不得只填后者。
LINGXI_FEISHU_SCOPE=<正式应用发布的全部 scope，含 offline_access>
# biai-stage 主动连接 OAuth Bridge；token 只写入受控环境文件和 Worker secret，不入库、不打印。
LINGXI_OAUTH_BRIDGE_URL=wss://<受控 OAuth Bridge 地址>/oauth/bridge
LINGXI_OAUTH_BRIDGE_TOKEN=<Bridge token，勿提交>
# 可选：一次性 job 等待回调的秒数，默认 600（与 E1 state 有效期一致）。
# LINGXI_OAUTH_BRIDGE_WAIT_SECONDS=600

# 下面两个路径由 compose 固定到凭据持久卷，通常不在 env 文件中重复设置：
# LINGXI_DELEGATED_CREDENTIAL_PATH=/var/lib/lingxi/credentials/delegated_credential.enc
# LINGXI_DELEGATED_REAUTH_STATE_PATH=/var/lib/lingxi/credentials/delegated_credential.reauth-state
# 可选：独立 state 完整性密钥；不填则复用 LINGXI_DELEGATED_CREDENTIAL_KEY。
# LINGXI_DELEGATED_REAUTH_STATE_KEY=<独立 state 完整性密钥，勿提交>
# 可选：不填则从正式 feishu_delegated_subject 登记表读取。
# LINGXI_DELEGATED_SUBJECT_OPEN_ID=<专用授权主体 open_id>
