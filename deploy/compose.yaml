# Lingxi 部署编排：共用结构（Issue #62 / S11）。
#
# 用法（stage 与生产**用同一份本文件**，只叠加各自的覆盖文件）：
#
#   docker compose -f deploy/compose.yaml -f deploy/compose.stage.yaml up -d
#   docker compose -f deploy/compose.yaml -f deploy/compose.prod.yaml  up -d
#
# 本文件**没有 `build:` 键，一个都没有**。生产只拉镜像、不构建（架构设计「八、部署与
# 发布」）。这不是纪律而是机制：compose 里没有构建定义，`biplus-prod` 上就不存在
# "顺手改一行再 build 一下"这条路径，镜像 tag 因此真的是被冻结的版本。
#
# ---------------------------------------------------------------------------
# 常驻服务只有 scheduler。
#
# worker 与 migrate 都是**一次性作业**，放在 `job` profile 里：`docker compose up -d`
# 不会启动它们，要显式 `docker compose run --rm <名字>`。这不是权宜之计，是当前事实：
#   - worker 在 main 上是单回合 CLI（读 LINGXI_WORKER_* 跑一个回合、往 stdout 写一个
#     JSON、退出）。它不领任务、不接飞书、不处理 SIGTERM。常驻 worker 需要任务队列与
#     gateway 入队，属 S4 下半，不在本批。给它配 restart 策略会把"作业正常退出"变成
#     无限重启循环，并让人误以为 worker 服务已经上线。
#   - migrate 是部署时跑一次的迁移作业，不是进程。
#
# gateway（S4 前半，#57）落地时在这里**新增第三个 service**，与 scheduler 同型：
# image 走同一套 tag 变量、同样非 root、同样 read_only、日志同样走 stdout。
# 它需要的持久卷是 lingxi-users（用户环境目录），已在下方定义好，无需改动卷结构。

name: lingxi

# ---------------------------------------------------------------------------
# 镜像引用：仓库与 tag 都来自环境变量，且都用 `:?` 形式——**没有默认值**。
# 漏设变量时 compose 直接报错退出，而不是悄悄拉一个 `latest`。
# tag 语义见 deploy/README.md：`<YYYYMMDD>-<commit sha 前 12 位>`，不可变、可回溯到源码。
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# 资源限制（Trace #373 H2 / S-H2-1，产品负责人 2026-08-28 先行裁定）。完整推导、
# 逐服务数值表与两档预算见 deploy/README.md「资源限制」一节，这里只说结构。
#
# 语法选型：`deploy.resources.limits`（cpus/memory/pids），不是顶层
# `mem_limit`/`cpus`/`pids_limit`——两者在 `docker compose up`（非 swarm）下都
# 真实生效（本机 Docker 29.1.3 + Compose 2.40.3 实测核对过），选前者是因为
# 三项限制归在同一个键下，覆盖文件用 `deploy: resources: limits: {...}` 整块替换
# 更不容易漏改某一项；`docker compose config` 会把两种写法都渲染成生效值，验证
# 方式见 deploy/README.md 同一节。
#
# **结构在这份基线文件里统一定义、数值分环境覆盖**：每个服务的三项限制都写成
# `${LINGXI_<SERVICE>_{MEM,CPU,PIDS}_LIMIT:-<安全默认值>}`，基线的默认值等于
# 生产档（与 LINGXI_SCHEDULER_INTERVAL_SECONDS 的既有写法同一惯例：基线默认值
# 取更保守/更宽松的那一档，避免漏挂 --env-file 时退化成一个明显偏紧、可能杀死
# 正常负载的数值）；`compose.stage.yaml`/`compose.prod.yaml` 用同一个变量名各自
# 覆盖成本环境的建议值。**数值必须分环境**：stage 主机只有 2 核 / 3.7GiB 内存，
# 按生产档（worker 类 8–16GB）配置会在 stage 上直接顶穿物理内存；反过来照抄 stage
# 的紧张预算去配生产，会在真实负载下把 worker-queue 的 Agent SDK 回合（Claude CLI
# + 多个 MCP 子进程）系统性地杀掉——这是本轮设计里判定"比限制偏松更严重"的错误，
# 因此 pids 一项尤其不能照抄 stage 的量级。
#
# 门禁只核对结构（`scripts/ci/check_deploy_contract.py` 的 `check_resource_limits`）：
# 六个服务是否都声明了这三项键，不核对具体数值——具体数值是产品/容量判断，不是
# 机械可判定的对错。
# ---------------------------------------------------------------------------

services:
  # 常驻服务：专用授权凭据（「四达文档会议助手」refresh_token）到期轮换扫描。
  scheduler:
    image: ${LINGXI_IMAGE_REGISTRY:?必须指定镜像仓库}/lingxi-scheduler:${LINGXI_IMAGE_TAG:?必须指定不可变镜像 tag}
    restart: unless-stopped

    # ---- 停止宽限期（断言 V-部署-03 / M2-62-24 / M2-62-25）--------------------
    # Docker 默认 10 秒，**不满足**。SIGKILL 若落在"已经向飞书换过新凭据、尚未写回
    # 数据库"的窗口里，那条 refresh_token 就永久丢失了——飞书侧旧凭据在续期成功那一刻
    # 已作废，一次性有效，没有任何补救手段，只能人工重新授权。
    #
    # 150 秒的算法（三项相加再留余量）：
    #   20.0s  续期 HTTP 超时     REQUEST_TIMEOUT_SECONDS，adapters/feishu_directory.py:33
    #    4.2s  落盘重试退避等待   SAVE_RETRY_BACKOFF_SECONDS=(0.2,1.0,3.0)，apps/scheduler/credential_rotation.py:39
    #   75.0s  数据库往返预算     5 次操作（4 次 save 重试 + 1 次 revoke），
    #                             每次最坏 = MAX_TIMEOUT_SECONDS 5s（建连）+
    #                             5s（语句）+ 5s（提交）
    #   -----
    #   99.2s  最坏情况 → ×1.5 安全系数 = 148.8s → 要求 149s → compose 保留 150s
    #                             余量 1s
    #
    # **上一版这里写的是 90 秒，而且算法是错的**（Issue #62 codex 审查 P1-3）：它只把
    # `connect_timeout` 算进去，没覆盖已建连后的语句和提交。当前运行时边界由
    # `src/lingxi/adapters/postgres.py` 的连接工厂 kwargs 提供；示例 DSN 只是与工厂默认值的
    # 对账基线，不能作为唯一事实源。合法覆盖必须使用 `LINGXI_POSTGRES_*_TIMEOUT_SECONDS`。
    #
    # **这个不等式由 scripts/ci/check_deploy_contract.py 自动守住，不是靠这段注释。**
    # 改了 REQUEST_TIMEOUT_SECONDS 而不改这里，门禁会红（断言 M2-62-25）。
    stop_grace_period: 150s

    # ---- 容器权限最小化（断言 V-部署-07 / M2-62-29 / M2-62-30）----------------
    # 镜像里已经 USER 10001，这里再写一遍是为了让 compose 层面可核对，也防止将来有人
    # 用覆盖文件把它改回 root 而无人察觉。无 cap_add、无 privileged。
    user: "10001:10001"
    read_only: true
    # read_only 之后进程仍需要一处可写的临时目录（Python 的 tempfile 默认位置）。
    # 给 tmpfs 而不是放开 rootfs：它随容器消失，天然满足"除用户环境目录外不写
    # 需要持久化的本地状态"（断言 V-部署-02）。
    tmpfs:
      - /tmp:mode=1777,size=16m
    security_opt:
      - no-new-privileges:true

    # ---- 资源限制（结构见文件头部说明，数值见 deploy/README.md）--------------
    deploy:
      resources:
        limits:
          cpus: "${LINGXI_SCHEDULER_CPU_LIMIT:-1.0}"
          memory: ${LINGXI_SCHEDULER_MEM_LIMIT:-1G}
          pids: ${LINGXI_SCHEDULER_PIDS_LIMIT:-512}

    # ---- 日志（断言 V-部署-04 / #343 取证留存下限）----------------------------
    # 进程只往 stdout / stderr 写，不写日志文件、不自行轮转（apps/scheduler 的
    # logging.basicConfig(stream=sys.stdout)）。轮转交给 Docker 的日志驱动。
    # max-size/max-file 是 #343 取证窗口的第一层缓冲（宿主侧增量收集见
    # deploy/日志留存.md，那才是跨重部署持久的那一份）；下限由
    # scripts/ci/check_deploy_contract.py 的 check_log_retention_floor 守住，
    # 改小了会直接打红门禁。
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"

    # ---- 配置与凭据 -----------------------------------------------------------
    # 凭据一律走 env_file，且那个文件**不在版本库里**（见各覆盖文件与 .gitignore）。
    # 这里只放非机密的默认值。
    environment:
      # 凭据文件必须落在**持久卷**里（断言 V-部署-03 / M2-62-27）。指向容器本地路径
      # 会让每次镜像替换都丢失授权——而这个后果要到下一次轮换才暴露。
      LINGXI_DELEGATED_CREDENTIAL_PATH: /var/lib/lingxi/credentials/delegated_credential.enc
      LINGXI_SCHEDULER_INTERVAL_SECONDS: ${LINGXI_SCHEDULER_INTERVAL_SECONDS:-60}

    volumes:
      # 专用授权凭据：跨部署持久，镜像替换与重启不得丢失。
      - lingxi-credentials:/var/lib/lingxi/credentials

    # ---- 健康检查（断言 V-部署-* / Issue #153）---------------------------------
    # 不监听任何端口：用 `docker exec` 语义在容器内跑一条受控命令（合同第 5 条：
    # "healthcheck 使用进程/数据库心跳或受控命令，不为健康检查扩大网络攻击面"）。
    # 该命令同时探测数据库可达性与主循环活性文件——依赖不可用或主循环停摆都会
    # 让它如实变红，不接受"PID 存活即健康"（`scripts/ci/check_deploy_contract.py`
    # 会核对每个常驻服务都配了 healthcheck）。
    healthcheck:
      test: ["CMD", "python", "-m", "lingxi.apps.healthcheck", "--role", "scheduler"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # 飞书长连接接入：收事件、落库成任务（#57 / S4 前半）。
  #
  # ---- 为什么放在**非默认 profile** 里 ----------------------------------------
  # **gateway 不能单独上线。** 它把任务写进队列，而队列此刻**没有消费者**：main 上的
  # worker 是单回合 CLI（读环境变量跑一个回合就退出），不领任务。启用 gateway 等于
  # 开始接收真实用户消息、把它们排进一个没人处理的队列——用户看到的是"发了没反应"，
  # 比不接入更糟。
  #
  # 因此 `docker compose up -d` **不会**启动它；要启动必须显式
  # `--profile gateway`。S4 下半（常驻 worker 与任务领取）接线之后，把这一行
  # profiles 删掉即可转为默认服务，届时它与 scheduler 同型。
  # 这与 #57 的登记一致：组件已交付，尚未接线。
  gateway:
    image: ${LINGXI_IMAGE_REGISTRY:?必须指定镜像仓库}/lingxi-gateway:${LINGXI_IMAGE_TAG:?必须指定不可变镜像 tag}
    # ---- profile 归属（Issue #153）------------------------------------------
    # 仍然保留独立的 `gateway` profile：`deploy/README.md` 里 `--profile gateway`
    # 这条既有命令继续按原样工作，不因本次改动改变含义。同时加入新的 `mvp`
    # profile——它与 scheduler（无 profile，恒常驻）、`worker-queue`
    # （新增，见下）一起构成"同时启动迁移后的 scheduler、gateway、常驻 queue
    # worker"这个受控部署形态（#153 范围第 1 条）。两个 profile 名字任一命中都
    # 会启动这个服务（compose 的 profiles 语义是 OR，不是 AND）。
    profiles: ["gateway", "mvp"]
    restart: unless-stopped

    # ---- 停止宽限期（断言 V-部署-03，#57 同型认领）---------------------------
    # 收到 SIGTERM 后停止接收新事件、把在途事件**落库完成**再退出。中途被 SIGKILL
    # 会留下"抢占了话题但任务没写进去"的中间态——用户发了消息、系统记得占了坑、
    # 却没有任何任务在跑。
    #
    # 60 秒的算法（全部来自 gateway 自己的配置，不是拍脑袋）：
    #   20.0s  停机超时       LINGXI_GATEWAY_SHUTDOWN_TIMEOUT_SECONDS 默认值，
    #                         apps/gateway/config.py 的 load_config
    #    5.0s  出站 HTTP 超时 apps/gateway/__init__.py 取 shutdown_timeout / 4
    #   11.0s  数据库往返预算 一个在途事件事务，按与 scheduler 同一口径
    #                         （connect 5s + 语句 3s + 提交 3s）
    #   -----
    #   36.0s  最坏情况 → ×1.5 安全系数 = 54s → 取整到 60s
    #
    # 与 scheduler 那条一样，这个不等式由 scripts/ci/check_deploy_contract.py 守住：
    # 改了 SHUTDOWN_TIMEOUT_SECONDS 的默认值而不改这里，门禁会红。
    stop_grace_period: 60s

    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp:mode=1777,size=16m
    security_opt:
      - no-new-privileges:true

    # ---- 资源限制（结构见文件头部说明，数值见 deploy/README.md）--------------
    deploy:
      resources:
        limits:
          cpus: "${LINGXI_GATEWAY_CPU_LIMIT:-1.0}"
          memory: ${LINGXI_GATEWAY_MEM_LIMIT:-1G}
          pids: ${LINGXI_GATEWAY_PIDS_LIMIT:-512}

    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    # 不挂任何持久卷：gateway 的状态全部在数据库里，容器内不写需要持久化的东西
    # （断言 V-部署-02）。凭据走 env_file，见各环境的覆盖文件。

    # ---- 健康检查节奏（S-H1-6，#359 根因取证方案第 1 条）------------------------
    # 原 15s/15s 与 worker-queue 完全同频，两个最大 CPU 峰值源（各自冷启动一个
    # 全新 Python 解释器 + 新建一次数据库连接）经常同秒重叠（`biai-stage` 独立
    # 复测：`.State.Health.Log` 里 gateway/worker 的 Start 时间戳反复相隔 ≤2s）。
    # interval 提到 23s（gateway liveness 阈值 30s 仍留 7s 冗余），与 worker-queue
    # 的 29s 互质——LCM(23,29)=667s，两条检查此后只会每约 11 分钟短暂重合一次，
    # 而不是像 15s/15s 那样几乎每轮都可能重叠，Python 冷启动次数同时降约 35%。
    # 不改判定逻辑：仍是"依赖不可达或主循环停摆必须如实变红"，只改触发节奏。
    healthcheck:
      test: ["CMD", "python", "-m", "lingxi.apps.healthcheck", "--role", "gateway"]
      interval: 23s
      timeout: 10s
      retries: 3
      start_period: 20s

  # 一次性作业：单回合受控执行。`docker compose run --rm worker`
  #
  # **不是常驻服务**，因此没有 restart 策略、没有 stop_grace_period、没有 healthcheck。
  # 它跑完一个回合就退出，退出码 0/2/3/4/5 是调用方的判定依据。
  worker:
    image: ${LINGXI_IMAGE_REGISTRY:?必须指定镜像仓库}/lingxi-worker:${LINGXI_IMAGE_TAG:?必须指定不可变镜像 tag}
    profiles: ["job"]
    restart: "no"
    user: "10001:10001"
    read_only: true
    # 镜像把 HOME 指到 /tmp，Claude Code CLI 与 MCP 子进程要往那里写配置与会话，
    # 因此给得比另外两个服务大。
    tmpfs:
      - /tmp:mode=1777,size=256m
    security_opt:
      - no-new-privileges:true

    # ---- 资源限制（结构见文件头部说明，数值见 deploy/README.md）--------------
    # 单回合受控执行：一次只跑一个 Agent SDK 回合，不像 worker-queue 要同时装下
    # LINGXI_WORKER_MAX_CONCURRENCY 个并发回合，因此限制远小于 worker-queue。
    deploy:
      resources:
        limits:
          cpus: "${LINGXI_WORKER_CPU_LIMIT:-2.0}"
          memory: ${LINGXI_WORKER_MEM_LIMIT:-4G}
          pids: ${LINGXI_WORKER_PIDS_LIMIT:-1024}

    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    volumes:
      # 用户环境目录：与凭据**不同的卷**（断言 M2-62-28）。用户产物与会话在这里，
      # 备份周期、恢复策略与删除语义都与凭据不同，合成一个卷会让两者无法分别处置。
      - lingxi-users:/var/lib/lingxi/users

  # 常驻服务：队列消费者。`LINGXI_WORKER_MODE=queue` 让**同一个镜像**跑
  # apps/worker/cli.py 的长期循环分支而不是单回合分支（Issue #153）——与
  # `worker`（一次性 `turn` job）刻意是**两个不同的 compose service**，而不是
  # 同一个 service 靠 profile 切行为：compose 的 service 定义只有一份，
  # `check_deploy_contract.py` 对 `worker` 的既有断言（无 restart、无
  # stop_grace_period、无 healthcheck）必须继续成立，不能因为新增常驻用法而放松。
  #
  # 只在 `mvp` profile 里，不进默认 `docker compose up -d`——与 scheduler/gateway
  # 一起构成 Epic A 的候选/Stage 验收对象，不静默改变生产发布入口（#153 范围第 1
  # 条、可观察完成标准第 1 条）。
  worker-queue:
    image: ${LINGXI_IMAGE_REGISTRY:?必须指定镜像仓库}/lingxi-worker:${LINGXI_IMAGE_TAG:?必须指定不可变镜像 tag}
    profiles: ["mvp"]
    restart: unless-stopped

    environment:
      LINGXI_WORKER_MODE: queue

    # ---- 停止宽限期（Issue #153；apps/worker/config.py 的
    # DEFAULT_SHUTDOWN_TIMEOUT_SECONDS 头部注释有完整推导）----------------------
    # 与 scripts/ci/check_deploy_contract.py::_worker_worst_case_seconds() 的
    # 实际门禁模型逐项对齐（PR #173 独立复核 P3-2：旧版注释另起一套推导，算出
    # 的余量与门禁实际要求的数字对不上）：
    #  45.0s   进程自身停机预算   LINGXI_WORKER_SHUTDOWN_TIMEOUT_SECONDS 默认值
    #                             （DEFAULT_SHUTDOWN_TIMEOUT_SECONDS），它自己的
    #                             推导已经包含 /stop 检测延迟（1.0s）、SDK 收尾
    #                             宽限（DEFAULT_DRAIN_GRACE_SECONDS 30.0s）与一次
    #                             终态写库预算，见该常量的头部注释
    #  15.0s   终态写库预算       门禁模型在上面之外再单独留一次，作为 SIGKILL
    #                             兜底窗口，按连接工厂合法覆盖上界
    #                             MAX_TIMEOUT_SECONDS=5 建模（5 + 2×5）
    #  -----
    #  60.0s   最坏情况 → ×1.5 安全系数 = 90s（这是门禁要求的下限，不是留了
    #          余量后取整——90 秒恰好等于门禁最低要求，一点富余都没有；改小
    #          任何一点都会打红门禁）。
    # 这个不等式由 scripts/ci/check_deploy_contract.py 守住，改了
    # DEFAULT_SHUTDOWN_TIMEOUT_SECONDS 而不改这里，门禁会红。
    stop_grace_period: 90s

    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp:mode=1777,size=256m
    security_opt:
      - no-new-privileges:true

    # ---- 资源限制（结构见文件头部说明，数值见 deploy/README.md）--------------
    # 这是六个服务里唯一直接对应架构设计「九、容量与资源」worker 内存估算
    # （峰值并发任务 × 单任务内存 = 8–16GB）的服务：常驻领取真实用户任务，
    # 一个容器内同时装下 LINGXI_WORKER_MAX_CONCURRENCY 个并发 Agent SDK 回合，
    # 每个回合各自的 Claude CLI 子进程与多个 MCP 子进程都算在这一个容器的
    # cgroup 里。pids 限制尤其不能照抄 worker（一次性 job）或其余服务的量级
    # ——低估这一项会在真实并发负载下杀掉本该正常完成的用户任务。
    deploy:
      resources:
        limits:
          cpus: "${LINGXI_WORKER_QUEUE_CPU_LIMIT:-4.0}"
          memory: ${LINGXI_WORKER_QUEUE_MEM_LIMIT:-16G}
          pids: ${LINGXI_WORKER_QUEUE_PIDS_LIMIT:-4096}

    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    volumes:
      - lingxi-users:/var/lib/lingxi/users

    # ---- 健康检查节奏（S-H1-6，#359 根因取证方案第 1 条）------------------------
    # 与 gateway 同一诊断：interval 提到 29s（worker liveness 阈值 60s 冗余充分），
    # 与 gateway 的 23s 互质错开稳态相位；start_period 顺带偏移到 32s（约 gateway
    # interval 的一半），让两者第一轮检查也不再落在 compose up 同一时刻的 ≤2s
    # 窗口内。同样不改判定逻辑，只改触发节奏——见 gateway 服务块的完整推导。
    healthcheck:
      test: ["CMD", "python", "-m", "lingxi.apps.healthcheck", "--role", "worker"]
      interval: 29s
      timeout: 10s
      retries: 3
      start_period: 32s

  # 一次性作业：数据库迁移。`docker compose run --rm migrate`
  #
  # 镜像的 ENTRYPOINT 已经是 `python -m alembic -c /opt/lingxi/alembic.ini`，
  # 默认 CMD 是 `upgrade head`。要跑别的子命令直接追加参数：
  #   docker compose ... run --rm migrate current
  #   docker compose ... run --rm migrate stamp 20260806_baseline
  #
  # **不得配 restart 策略**（断言 M2-62-23）：迁移失败必须停下来让人看见。自动重启会
  # 把一次失败的迁移变成一个反复撞墙的循环，并掩盖真正的失败原因。
  migrate:
    image: ${LINGXI_IMAGE_REGISTRY:?必须指定镜像仓库}/lingxi-migrate:${LINGXI_IMAGE_TAG:?必须指定不可变镜像 tag}
    profiles: ["job"]
    restart: "no"
    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp:mode=1777,size=16m
    security_opt:
      - no-new-privileges:true

    # ---- 资源限制（结构见文件头部说明，数值见 deploy/README.md）--------------
    deploy:
      resources:
        limits:
          cpus: "${LINGXI_MIGRATE_CPU_LIMIT:-1.0}"
          memory: ${LINGXI_MIGRATE_MEM_LIMIT:-1G}
          pids: ${LINGXI_MIGRATE_PIDS_LIMIT:-512}

    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    # LINGXI_MIGRATION_DSN 与业务的 LINGXI_POSTGRES_DSN 是**两个变量**，迁移不回落到
    # 后者：迁移要 DDL 权限、业务进程不要，两者本就该是不同的数据库角色
    # （migrations/README.md，断言 V-迁移-05）。它只在覆盖文件的 env_file 里出现。

  # 一次性作业：正式重授权。`docker compose --profile job run --rm reauthorize`
  #
  # 入口随 scheduler 镜像发布，但这是受控运维动作，不是常驻服务。它必须以与
  # scheduler 相同的 uid 写入凭据卷，避免 root 生成的文件让 scheduler 无法读回。
  # state 文件与凭据文件均在同一个持久卷中，由入口启动前拒绝路径/锁文件冲突。
  # 浏览器回调经 OAuth Bridge 主动回传，不接收终端粘贴的回跳地址。
  reauthorize:
    image: ${LINGXI_IMAGE_REGISTRY:?必须指定镜像仓库}/lingxi-scheduler:${LINGXI_IMAGE_TAG:?必须指定不可变镜像 tag}
    profiles: ["job"]
    restart: "no"
    user: "10001:10001"
    read_only: true
    tmpfs:
      - /tmp:mode=1777,size=16m
    security_opt:
      - no-new-privileges:true

    # ---- 资源限制（结构见文件头部说明，数值见 deploy/README.md）--------------
    deploy:
      resources:
        limits:
          cpus: "${LINGXI_REAUTHORIZE_CPU_LIMIT:-1.0}"
          memory: ${LINGXI_REAUTHORIZE_MEM_LIMIT:-1G}
          pids: ${LINGXI_REAUTHORIZE_PIDS_LIMIT:-512}

    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    command: ["python", "-m", "lingxi.apps.reauthorize"]
    environment:
      LINGXI_DELEGATED_CREDENTIAL_PATH: /var/lib/lingxi/credentials/delegated_credential.enc
      LINGXI_DELEGATED_REAUTH_STATE_PATH: /var/lib/lingxi/credentials/delegated_credential.reauth-state
    volumes:
      - lingxi-credentials:/var/lib/lingxi/credentials

volumes:
  # 两个卷的实际名字由覆盖文件指定（stage 与生产各一套），这里只定义结构。
  lingxi-credentials:
  lingxi-users:
