# Lingxi：AI 协作规则

本仓库以 AI coding 为主要开发方式。AI 代理应把文档视为受版本控制的工作合同，而不是背景材料。

## 先读什么

1. [产品合同与外部边界](docs/产品合同与外部边界.md)：用户体验承诺和不可突破的外部约束。
2. [当前能力](docs/当前能力.md)：当前已确认能力、已知限制和验证状态。
3. 与任务相关的 GitHub Issue、PR 与 [Lingxi 开发 Project](https://github.com/users/Moshuiwang/projects/2)：动态范围、讨论、负责人、进度和验证证据。

## 必须遵守

- 默认使用中文沟通、撰写 Issue、PR、规格和文档；只有外部平台字段、代码标识、命令或用户明确要求时才保留英文。
- 面向产品负责人时，优先使用产品经理能理解的语言：说明用户是谁、要完成什么、会看到什么、何时成功/等待/失败以及需要作出的选择。
- 描述需求、变更和验收时，优先说用户体验、业务规则和可观察结果；API、表结构、命令、模型参数、部署等技术细节只在实现设计或排障确有必要时出现，并应解释其对用户体验的影响。
- 不把参考项目、旧代码、模型推测或实现便利性当作产品决策。
- 遇到会改变用户承诺、扩大权限/数据范围、触发不可逆外部副作用，或外部依赖规范不明确的选择时，停止该选择并请产品负责人决策；继续不依赖该选择的研究、测试和低风险编码。
- 小型、可逆且不改变用户体验/权限/外部依赖边界的改动可直接编码，不要求 Issue 或编号；在 PR 中写清目的和验证即可。
- 会改变用户体验、业务规则、权限、安全或外部依赖使用方式的改动，编码前在 GitHub Issue 写明目标、范围、待决策和可观察的完成标准。需求编号、完整设计和任务拆分仅在复杂、高风险或多人协作时补充。
- 外部平台的身份、权限、能力和失败语义以其有效规范为准；不伪造成功，不绕过授权。
- 先快速研究和明确完成标准，再开始编码；开始后优先持续完成、验证和收口，除非触及上述决策门，不因文档形式本身阻塞编码。
- 保持指令精简且不冲突；局部规则放在靠近代码的 `AGENTS.md` 中，不把所有细节堆进本文件。

## 固定执行流程

每次编码任务都按以下顺序执行，并在首条工作更新中用五行中文说明：**目标、已知约束、计划、完成标准、唯一阻塞项（没有则写“无”）**。

1. 判断是“直接编码”还是“需要最小变更说明”。不确定时先提问或做只读研究。
2. 在不触及决策门时，立即开始编码；不等待详尽设计、任务拆分或看板整理。
3. 编码后运行与改动相称的验证，修复发现的问题。
4. 用三行收口：**改了什么、如何验证、还剩什么风险或待决策**。
5. 多代理或跨设备任务，额外执行下方的领取和交接规则。

固定提示词就是本节“五行开场、三行收口”规则；无需另建本地提示词文件。

## 跨设备与跨代理协作

- 远端 Git 仓库、GitHub Issue / PR / Project 和已推送文档才是共享事实；聊天记录、代理会话、本地未提交文件、缓存和个人待办不是交接依据。
- 只有多人、跨设备、跨 PR 或高风险变更才使用 GitHub 父 Issue、分支和 PR 相互链接。分支格式为 `change/<Issue 编号>-主题`。
- 对这类协作变更，主实施代理在开始前在父 Issue 留下简短领取评论，写明代理、分支和计划；同时将 GitHub Project 的内建 Status 设为 `In Progress`。同一变更同时只能有一个主实施代理。
- 另一个代理需要参与时，只能领取独立子 Issue 或在父 Issue 中进行研究/审查；不得并行修改同一变更包或同一代码范围。
- 交接前必须提交、推送并在 Issue 留下交接评论：已完成项、验证结果、未完成项、风险、下一步和当前 commit / PR 链接。
- 协作变更的 PR 必须链接父 Issue；小改动只需说明目的、验证和风险。合并后，仅在产品承诺或当前能力实际改变时更新相应本地文档。
