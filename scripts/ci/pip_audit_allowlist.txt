# 依赖漏洞扫描的豁免清单（scripts/ci/pip_audit_check.py 的唯一豁免来源）。
#
# 每行三段，空白分隔：编号 到期日 理由
#   编号   = GHSA-xxxx-xxxx-xxxx / PYSEC-YYYY-N / CVE-YYYY-N 任一别名（同一漏洞的别名互通）
#   到期日 = YYYY-MM-DD，含当天；过期即失效，被它挡住的高 / 严重漏洞随即判红
#   理由   = 为什么可以暂不修（影响面、缓解措施、跟进的 Issue 号）
# 例：GHSA-xxxx-xxxx-xxxx 2026-12-31 只影响未启用的 X 功能，跟进 #NNN
#
# 规则：
# - 空行与 # 开头的行忽略；其余行格式不对（少一段、日期不合法、编号形状错、编号重复）
#   整个检查按「未知」失败（退出码 2），不是跳过该行。
# - 只能改本文件加条目；检查脚本不接受环境变量或命令行追加豁免。
# - 本文件在 scripts/ci/ 下，改动即走完整门禁（Epic Full），与所有门禁脚本同一待遇。
# - 条目未命中任何已发现漏洞时在作业摘要里提示可删除，不判红。
